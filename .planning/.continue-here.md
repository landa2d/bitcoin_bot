---
context: default
phase: Phase 6 COMPLETE + verified + live → Phase 7 next
status: ready
last_updated: 2026-05-30T21:15:00Z
---

# Continue Here — Phase 7 (Synthesis Loop Core)

<current_state>
**Phase 6 (Telegram Read-Only Scaffolding) is COMPLETE, verified (4/4 must-haves), and LIVE.**
`/map-status` and `/map-pending` are running on the prod `gato_brain` container (scoped,
operator-approved rebuild; container Up healthy). Read-only by construction (GET-only economy_map
wrapper; code review confirmed zero write verbs). The operator now has full situational awareness of
the map — seven blocks with maturity pills + unabsorbed/draft counts, and the pending inbox
(drafts + unsorted entries) — entirely via Telegram, before any write commands exist.

Code review returned 0 Critical / 2 Warning / 4 Info — all advisory, none silent-data-loss or
fail-loud violations, captured in
`.planning/todos/pending/2026-05-30-phase06-review-followups-wr01-wr02.md` (WR-01 is best fixed when
Phase 9/10 makes the handlers directly callable).
</current_state>

<remaining_work>
1. **Start Phase 7 — Synthesis Loop Core** (SYNT-01..06). Depends on Phase 2 + Phase 5. This is the
   autonomous editorial core: per-block synthesis trigger (≥N=5 new entries OR ≥T=30 days),
   input assembly (current published body + new timeline entries + live_tension + maturity),
   a single Claude Sonnet editorial call routed through `llm-proxy:8200` (NO direct Anthropic SDK —
   RivalScope anti-pattern), prompt in hot-reloadable `economy_map/synth_identity.md`, output =
   rewritten `body_md` + `proposed_maturity` landing as a `draft` version.
   - No CONTEXT.md yet → recommended entry `/gsd-discuss-phase 7`, then `/gsd-plan-phase 7`.
   - Phase 6's `/map-pending` already surfaces the `version_id`s Phase 9 will approve and the
     `unsorted` entries Phase 10 will assign — the read surface is ready for the write loop.
</remaining_work>

<carried_items>
- 6 pending todos in `.planning/todos/pending/`: phase-06 review follow-ups (WR-01/WR-02 + 4 Info),
  phase-05 review follow-ups (WR-02/04/05), analyst predictions.title expire bug, soft-cap hardening,
  pay-endpoint 500 activation check, research trigger file permissions.
- `lab-data-provider` code drift is **deferred (D-07)** — the one expected non-zero drift-check line.
- REQUIREMENTS.md traceability omits the v2-deferred IDs (NEGB/TUNE/DSGN/EUAI) — intentional, not a gap.
- Remote URL embeds a PAT — rotate / move to a credential helper sometime.
</carried_items>

<infrastructure_state>
- **Prod IS this host**: live containers run from `/root/bitcoin_bot/docker`. Deploys = scoped local
  rebuilds (`docker compose up -d --build --no-deps <svc>`). `gato_brain` was just rebuilt for Phase 6.
- `workflow.auto_advance` = **false**; operator wants evidence-backed, per-step-approved prod changes
  (memory `feedback-prod-cutover-discipline`).
- Supabase project `zxzaaqfowtqvmsbitqpu`; migrations via MCP `apply_migration`. Latest applied: 037.
- Run `scripts/drift-check.sh` before any deploy (exits non-zero only on lab-data-provider D-07).
- Synthesis LLM access: `gato_brain`/services route through `http://llm-proxy:8200` with per-agent
  keys; `economy_map` access via direct PostgREST + `Accept-Profile: economy_map` (never supabase-py
  `.schema()`/`.in_()`).
</infrastructure_state>

<next_action>
Begin Phase 7. Recommended entry: `/gsd-discuss-phase 7` (gather context first) or `/gsd-plan-phase 7`.
</next_action>
