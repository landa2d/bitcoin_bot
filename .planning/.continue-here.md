---
context: default
phase: post-04.1 (cross-phase follow-up COMPLETE) → Phase 5 next
status: ready
last_updated: 2026-05-28T13:40:00Z
---

# Continue Here — Phase 5 (intake-classifier + `unsorted` handling)

<current_state>
**Phase 04.1 is COMPLETE and verified, and the post-04.1 follow-up is DONE.** Production now
enforces DB-based governance (`agent_wallets_v2`, fail-loud-on-missing); prod==main for all
in-scope services (lab-data-provider deferred D-07); a standing drift check exists; and the
silent-RPC `search_path` class is fully remediated.

The foundation is clean. The next work is **Phase 5: intake-classifier + `unsorted` handling**.
</current_state>

<completed_work>
- Phase 04.1: governance cutover (migration 034 on prod; fail-loud + downgrade `proxy.py` deployed; canary PASS — soft caps via `allow_negative=true` accepted), staged deploy (gato_brain/analyst/research/gato verified), migration 035 (claim_research_task). VERIFICATION status:passed.
- Standing drift check: `scripts/drift-check.sh` + migration 036 `gsd_drift_audit()`.
- **Search_path RPC class FIXED**: migration 037 (`037_fix_rpc_search_paths`) applied + verified to prod — all 8 empty-search_path functions now `pg_catalog, public`; `get_scrape_stats()` confirmed working (was 42P01); drift-check RPC section clean. Bodies inspected first (7 live-broken, 1 benign). Resolved the audit todo (→ completed/) and the structural part of the pay-500 todo (residual = activation-time `/pay → 200` E2E only).
- Local work pushed to `origin/main` (durability risk cleared).
</completed_work>

<remaining_work>
1. **Start Phase 5** — intake-classifier + `unsorted` handling (REQ-INTAKE-AUTO / -TRACE, REQ-CMD-ASSIGN). Build on the now-clean foundation.
</remaining_work>

<carried_items>
- `lab-data-provider` code drift is **deferred (D-07)** — the one expected non-zero line in drift-check; not a regression.
- pay-500 todo stays in `pending/` purely as an activation reminder (verify `/v1/proxy/pay → 200` when agent→agent payments are first turned on).
- 3 other pending todos: predictions.title expire bug, research trigger file perms, soft-cap hardening.
- Remote URL embeds a PAT — rotate / move to a credential helper sometime.
</carried_items>

<infrastructure_state>
- **Prod IS this host**: live containers run from `/root/bitcoin_bot/docker`. Deploys = local scoped rebuilds (`docker compose up -d --build --no-deps <svc>`). `/opt/bitcoin_bot` is a stale Mar-15 checkout.
- `workflow.auto_advance` = **false**.
- Supabase project `zxzaaqfowtqvmsbitqpu`; migrations via MCP `apply_migration`. Latest applied: 037.
- Run `scripts/drift-check.sh` before any deploy (exits non-zero only on lab-data-provider D-07 until that's addressed).
</infrastructure_state>

<next_action>
Begin Phase 5. Recommended entry: `/gsd-discuss-phase 5` (gather context first) or
`/gsd-plan-phase 5`. The operator wants evidence-backed, per-step-approved prod changes — see
memory `feedback-prod-cutover-discipline`.
</next_action>
