---
context: default
phase: post-04.1 (cross-phase follow-up)
status: paused
last_updated: 2026-05-28T13:23:03.392Z
---

# Continue Here — RPC search_path fix → push → Phase 5

<current_state>
**Phase 04.1 is COMPLETE and verified.** Production now enforces DB-based governance
(`agent_wallets_v2`) with fail-loud-on-missing; prod==main for all in-scope services
(lab-data-provider deferred D-07); a standing drift check exists.

The immediate next work is a **cross-phase follow-up, not a new phase**: fix the silent-RPC
`search_path` class (8 functions, several confirmed live-broken in prod), then push 102 unpushed
commits, then start Phase 5. The operator has already approved the search_path batch-fix.
</current_state>

<completed_work>
- Phase 04.1: governance cutover (migration 034 on prod; fail-loud + downgrade `proxy.py` deployed; canary PASS accepted — soft caps via pre-existing `allow_negative=true` accepted by operator), staged deploy (gato_brain/analyst/research/gato governed-cycle verified; gato via real Telegram round-trip), migration 035 (claim_research_task search_path fix). VERIFICATION status:passed. Source drift todo resolved → completed/.
- Built standing drift check: `scripts/drift-check.sh` + migration 036 `gsd_drift_audit()`.
- Filed 5 follow-up todos in `.planning/todos/pending/`.
</completed_work>

<remaining_work>
1. **Fix the 8 empty-search_path RPC functions (migration 037).** APPROVED. Confirm each body, then `ALTER FUNCTION … SET search_path = pg_catalog, public` (all 8 are non-SECURITY-DEFINER → safe; the migration-035 pattern). Apply to prod under approval; re-run `scripts/drift-check.sh` until the RPC section is clean.
2. **Push 102 commits** to `origin/main` (remote configured; durability risk — work is local-only).
3. **Start Phase 5** (intake-classifier + `unsorted` handling).
</remaining_work>

<decisions_made>
- **Fix search_path class BEFORE Phase 5** — it's a CONFIRMED live outage (see below), not latent risk. Don't build Phase 5 on a broken foundation.
- `record_agent_spend` is broken-but-harmless (no caller; spend logging uses another path).
- Soft caps accepted as intended (operator decision in the 04.1 canary).
</decisions_made>

<blockers>
- None blocking. The 8-function outage IS the priority work, not a blocker on it.
</blockers>

## The confirmed live outage (the 8-function search_path class)

`drift-check.sh` surfaced 8 public functions with `search_path=""` (all non-SECURITY-DEFINER),
broken by the 2026-04-01 `fix_function_search_paths` migration. **Confirmed live-broken:**
`get_scrape_stats()` → `ERROR 42P01: relation "moltbook_posts" does not exist`. The class breaks
any function whose body references tables unqualified.

| Function | Used by | Status |
|----------|---------|--------|
| `search_corpus` | gato_brain corpus_probe.py:66,126 | **broken + actively used** (gato KB retrieval) |
| `topup_agent_wallet` | processor:6102 | **broken + actively used** (wallet top-ups) |
| `transfer_between_agents` | research:265 | **broken** (pay-500; agent payments — not yet a live dependency) |
| `get_scrape_stats` | (scraper stats) | **broken (confirmed 42P01)** |
| `record_agent_spend` | none | broken but **harmless** (no caller) |
| `next_newsletter_edition`, `increment_problem_frequency`, `compute_opportunity_score` | — | table-touching → likely broken; confirm each |

## Required Reading (in order)
1. Auto-memory (loads automatically) — `project-agentpulse-governance-and-deploy-topology` (state + the 8-function class + deploy topology) and `feedback-prod-cutover-discipline` (how the operator wants prod changes driven).
2. `.planning/todos/pending/2026-05-28-audit-rpc-search-path-class.md` — the P3 worklist with the 8-function inventory + fix guidance.
3. `scripts/drift-check.sh` — the standing check; run it before/after prod changes (RPC section lists the offenders live).
4. `.planning/phases/04.1-…/04.1-VERIFICATION.md` — what 04.1 delivered + all carried-forward items.

## Critical Anti-Patterns
| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Silent-RPC search_path | A function with `search_path=""` referencing unqualified objects fails at runtime (42P01) and callers swallow it. Recurring signature on this project. | advisory | Run `scripts/drift-check.sh` before/after any prod change; fix with `SET search_path = pg_catalog, public` (non-secdef) or schema-qualify the body. |
| Blind prod mutation | Operator requires evidence-backed, per-step-approved prod DDL/deploys; never auto-advance into prod. | advisory | Confirm each function body before ALTER; apply under approval; verify with drift-check + DB assertions. |

## Infrastructure State
- **Prod IS this host**: live containers run from `/root/bitcoin_bot/docker`. Deploys = local scoped rebuilds (`docker compose up -d --build --no-deps <svc>`). `deploy.sh`'s remote-rsync/`/opt` model is obsolete; `/opt/bitcoin_bot` is a stale Mar-15 checkout.
- `workflow.auto_advance` = **false** (flipped this session).
- `scripts/drift-check.sh` **exits non-zero by design** until the 8 functions are fixed (+ lab-data-provider D-07).
- Supabase project `zxzaaqfowtqvmsbitqpu`; migrations applied via MCP `apply_migration`. Latest applied: 036.
- git: `main` is **102 commits ahead of origin/main** (remote configured: github.com/landa2d/bitcoin_bot; note the remote URL embeds a PAT — rotate/credential-helper sometime). Uncommitted: `.planning/config.json` (+ pre-existing `.claude/settings.local.json`, `config/agentpulse-config.json`).
- A stale background bash task (gato build attempt `bbmdtfcnm`) may report later — it's superseded; ignore it.

<context>
Phase 04.1 went clean. The standing drift check did double duty: satisfied the operator's close
condition AND ran the P3 audit, surfacing that the search_path class is 8 functions (not 2) with
several actively broken in prod now. That's why the priority is fixes → push → Phase 5. The fix is
cheap and uniform; the main discipline is per-function body confirmation + per-step prod approval.
</context>

<next_action>
Start with: read each of the 8 functions' bodies (`pg_get_functiondef` via Supabase MCP), build
migration `037_fix_rpc_search_paths.sql` (one `ALTER FUNCTION … SET search_path = pg_catalog, public`
per function), apply to prod `zxzaaqfowtqvmsbitqpu` under operator approval, then run
`./scripts/drift-check.sh` and confirm the RPC section is clean. Then offer to push the 102 commits.
</next_action>
