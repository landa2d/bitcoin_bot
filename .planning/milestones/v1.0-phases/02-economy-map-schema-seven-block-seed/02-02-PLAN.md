---
id: 02-02-economy-map-verification
title: economy_map verification exercise — SQL probes + anon-key probe + results doc
phase: 02-economy-map-schema-seven-block-seed
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql
  - .planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md
autonomous: true
requirements: [SCHM-01, SCHM-02, SCHM-03, SCHM-04, SCHM-05, SCHM-06, SCHM-07, SCHM-08]
tags: [verification, sql-exercise, append-only, atomic-publish, rls-probe, economy-map]

must_haves:
  truths:
    - "02-VERIFY.sql contains labelled SQL blocks for every D-25 bullet (1..5) — five cases covering schema existence + atomic publish + append-only violation + lifecycle UPDATE success + anon-RLS probe (D-25, all five ROADMAP success criteria)"
    - "All five D-25 verification cases were exercised against the live Supabase project (ref zxzaaqfowtqvmsbitqpu) via Supabase MCP execute_sql + anon-key curl, and the outputs match the expected behaviors described in CONTEXT.md D-25 (ROADMAP success criteria 1, 2, 3, 4, 5 — all five proven)"
    - "02-VERIFY-RESULTS.md captures the structured proof: front-matter (phase, verified, status, score), Observable Truths table with one row per D-25 sub-bullet, Trigger Fire Log table with the exact RAISE EXCEPTION strings, Anon-key Probe Results table, and Critical Invariants table (per 02-PATTERNS.md §02-VERIFY-RESULTS.md guidance)"
    - "The publish_block_version RPC was exercised end-to-end: draft inserted → RPC called → row flipped to 'published' + prior 'published' superseded + blocks.current_body_version_id and blocks.maturity updated + last_synthesized_at bumped (single transaction, atomic) — proves SCHM-06 + ROADMAP criterion 3"
    - "Append-only violation traps were exercised: UPDATE attempts on body_md, what_shifted, and other pinned columns raised exceptions containing 'append-only'; DELETE on both content tables raised exceptions; lifecycle UPDATEs (status, published_at) succeeded — proves SCHM-04 (append-only) + ROADMAP criterion 4 (along with the 'unsorted' valid block_slug case)"
    - "Anon-key probe with Accept-Profile: economy_map header to /rest/v1/blocks returned 7 rows; same probe to /rest/v1/block_body_versions returned only published rows (drafts inserted during the exercise were invisible to anon); same probe to /rest/v1/timeline_entries returned only entries with block_slug != 'unsorted' (any unsorted entries inserted were invisible) — proves D-05/D-06/D-07 RLS posture + the autonomy-boundary structural enforcement"
    - "All test draft rows / test timeline entries inserted during the exercise were cleaned up where possible (status flipped to 'superseded' is acceptable; rows inserted then 'left behind' must be explicitly enumerated in 02-VERIFY-RESULTS.md under a 'Test residue' section so the next phase planner knows the seed state)"
    - "supabase-py .in_() is not used anywhere in this plan's artifacts — all DB access is via Supabase MCP execute_sql OR direct PostgREST curl with Accept-Profile header (per D-02, PROJECT.md Constraints, ROADMAP success criterion 1 — 'supabase-py .in_() is not used anywhere in the new code')"
  artifacts:
    - path: ".planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql"
      provides: "Labelled SQL exercise script covering D-25 bullets 1–4 (plus the curl-based bullet 5 reference)"
      contains: "D-25 bullet 1 / D-25 bullet 2 / D-25 bullet 3 / D-25 bullet 4 / DO $$ ... EXCEPTION WHEN OTHERS THEN RAISE NOTICE 'PASS: %', SQLERRM"
      min_lines: 120
    - path: ".planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md"
      provides: "Structured verification-results doc — the proof artifact for ROADMAP success criteria 3, 4, 5"
      contains: "phase: 02-economy-map-schema-seven-block-seed / status: passed / Observable Truths / Trigger Fire Log / Anon-key Probe Results / Critical Invariants"
      min_lines: 80
  key_links:
    - from: ".planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md (Observable Truths table)"
      to: ".planning/ROADMAP.md Phase 2 success criteria 1..5"
      via: "one Observable Truths row per success criterion, citing the SQL/curl output as evidence"
      pattern: "ROADMAP success criterion"
    - from: ".planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md (Trigger Fire Log)"
      to: "supabase/migrations/033_economy_map_schema.sql append-only triggers (Plan 02-01 Section 8)"
      via: "captured RAISE EXCEPTION strings — proves the triggers fire on the right inputs"
      pattern: "append-only"
    - from: ".planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md (Anon-key Probe Results)"
      to: "economy_map RLS policies + exposed-schemas allowlist"
      via: "HTTP status + row count per probe — proves D-05/06/07 + D-24"
      pattern: "Accept-Profile: economy_map"
---

<objective>
Produce the two verification artifacts that prove Phase 2's foundation (laid by Plan 02-01) actually satisfies all five ROADMAP success criteria. Specifically:

1. Author `.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql` — a labelled SQL exercise script covering D-25 bullets 1..4 (schema queries, atomic publish, append-only violations, lifecycle UPDATE success). Each bullet is wrapped in a `DO $$ ... EXCEPTION WHEN OTHERS THEN RAISE NOTICE 'PASS: %', SQLERRM; END $$;` block so that "expected failures" (append-only violations) are visible PASS notices rather than hard errors that abort the whole script.
2. Execute every block in `02-VERIFY.sql` against the live Supabase project `zxzaaqfowtqvmsbitqpu` via Supabase MCP `execute_sql`. Run the D-25 bullet 5 anon-key probe via curl (with the project URL + anon key from the Supabase MCP `get_project_url` / `get_publishable_api_key` tools).
3. Author `.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md` — the structured proof artifact, mirroring Phase 1's `01-VERIFICATION.md` shape (per 02-PATTERNS.md §02-VERIFY-RESULTS.md). One Observable Truths row per D-25 sub-bullet; one Trigger Fire Log row per append-only exception captured; one Anon-key Probe Results row per probed table; one Critical Invariants table covering the structural promises (first-in-tree precedents established, supabase-py `.in_()` absent, application code untouched).

Purpose: Phase 2's ROADMAP success criteria 3, 4, and 5 are owned by this plan ("verifiable by manual SQL exercise"). Criteria 1 and 2 are also re-confirmed here via the anon-key probe (criterion 1: Accept-Profile works) and block-list query (criterion 2: 7 blocks queryable with all structural fields populated). Without this plan, Phase 2 cannot pass.

Output: Two new files (`02-VERIFY.sql`, `02-VERIFY-RESULTS.md`) under `.planning/phases/02-economy-map-schema-seven-block-seed/`. Both committed. No application code touched. The phase becomes complete-and-verified when the Critical Invariants table in `02-VERIFY-RESULTS.md` is all-green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-economy-map-schema-seven-block-seed/02-CONTEXT.md
@.planning/phases/02-economy-map-schema-seven-block-seed/02-PATTERNS.md
@.planning/phases/02-economy-map-schema-seven-block-seed/02-01-PLAN.md
@.planning/phases/01-render-stack-diagnostic/01-VERIFICATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 02-VERIFY.sql with labelled D-25 bullet blocks</name>
  <files>.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql</files>
  <read_first>
    - .planning/phases/02-economy-map-schema-seven-block-seed/02-CONTEXT.md D-25 (the five verification bullets — this file's source of truth)
    - .planning/phases/02-economy-map-schema-seven-block-seed/02-PATTERNS.md §02-VERIFY.sql (DO $$ ... EXCEPTION WHEN OTHERS THEN RAISE NOTICE 'PASS: %', SQLERRM pattern; cite 004_core_tables.sql lines 241–256 as the analog for deferred-exception PL/pgSQL)
    - supabase/migrations/033_economy_map_schema.sql (the migration this script verifies — confirm table/column/RPC/trigger names exactly)
    - supabase/migrations/004_core_tables.sql lines 241–256 (the canonical DO $$ ... EXCEPTION WHEN OTHERS pattern to mirror)
    - .planning/ROADMAP.md (Phase 2 success criteria — every bullet in 02-VERIFY.sql maps to one of them)
  </read_first>
  <action>
    Use the Write tool to create `.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql`. Structure: a SQL script with one labelled section per D-25 bullet (5 bullets total, but bullet 5 is HTTP not SQL — it gets a comment-block reference to the curl probe done in Task 3, not inline SQL). Use `-- ───` divider style and prefix every section with a comment header naming the D-25 bullet, the ROADMAP success criterion it proves, and the expected outcome. Every "expected failure" SQL (append-only / not-in-draft) is wrapped in `DO $$ ... EXCEPTION WHEN OTHERS THEN RAISE NOTICE 'PASS: %', SQLERRM; END $$;` so the script's progress is greppable in Supabase MCP output.

    The file's section layout:

    ────────────────────────────────────────────────────────────────
    HEADER (comment block, ~10 lines):
    - `-- 02-VERIFY.sql — economy_map schema verification exercise (Phase 2)`
    - `-- Project ref: zxzaaqfowtqvmsbitqpu`
    - `-- Run via Supabase MCP execute_sql tool, one D-25 bullet at a time, OR as a single script (errors are trapped so progress continues).`
    - `-- Each block prints either "PASS: <message>" or returns rows for the next block to consume.`
    - `-- Bullet 5 (anon-key RLS probe) is HTTP-only — see comment block at the bottom for the curl invocation captured in 02-VERIFY-RESULTS.md.`
    - One-line ROADMAP success-criterion mapping comment for each upcoming section.

    ────────────────────────────────────────────────────────────────
    SECTION 1 — D-25 bullet 1 (seven blocks queryable; ROADMAP criterion 2):
    - `-- ───────────────── D-25 bullet 1: seven blocks queryable ─────────────────`
    - `SELECT COUNT(*) AS block_count FROM economy_map.blocks;` (expected: 7)
    - `SELECT slug, tier, accent, sort_order, maturity::text, (live_tension LIKE '%TBD%') AS live_tension_is_placeholder FROM economy_map.blocks ORDER BY sort_order;` (expected: 7 rows in D-23 order; live_tension_is_placeholder = true for all)
    - `SELECT enumlabel FROM pg_enum e JOIN pg_type t ON e.enumtypid=t.oid JOIN pg_namespace n ON n.oid=t.typnamespace WHERE n.nspname='economy_map' AND t.typname='maturity' ORDER BY e.enumsortorder;` (expected: 5 rows in nascent→mature order; proves ROADMAP criterion 5)

    ────────────────────────────────────────────────────────────────
    SECTION 2 — D-25 bullet 2 (atomic publish; ROADMAP criterion 3):
    - `-- ───────────────── D-25 bullet 2: atomic publish transaction ─────────────────`
    - Step 2a: Insert two draft rows for `identity-trust` (pick the first slug; sort_order=1). Use `INSERT … RETURNING id` to capture the UUIDs into psql variables `:v1`, `:v2`. Note: psql `\set` won't work in Supabase MCP execute_sql; use a `WITH inserted AS (INSERT … RETURNING id) SELECT id INTO TEMP TABLE _v1 FROM inserted;` workaround OR (preferred) execute one INSERT per separate MCP call and capture the returned id from the JSON response. The verification script itself can use a DO $$ block with PERFORM + RAISE NOTICE to print the id, and the executor records it for the next call. Choose whichever approach the executor finds cleanest — what matters is that the next steps reference the right id.
    - Step 2b: Promote v1 to published via the publish RPC: `SELECT economy_map.publish_block_version('<v1_uuid>');` — expected: returns NULL (void); the row's status becomes 'published'; published_at gets a timestamp; blocks.current_body_version_id and blocks.maturity for identity-trust update; blocks.last_synthesized_at gets a timestamp.
    - Step 2c: Verify post-publish state (read-only): `SELECT bbv.id, bbv.status, bbv.published_at IS NOT NULL AS has_published_at, bbv.proposed_maturity::text FROM economy_map.block_body_versions bbv WHERE bbv.id = '<v1_uuid>';` (expected: status='published', has_published_at=true) AND `SELECT b.current_body_version_id, b.maturity::text, b.last_synthesized_at IS NOT NULL AS has_last_synth FROM economy_map.blocks b WHERE b.slug = 'identity-trust';` (expected: current_body_version_id matches v1_uuid, maturity matches v1's proposed_maturity, has_last_synth=true).
    - Step 2d: Promote v2 to published — proves supersession: `SELECT economy_map.publish_block_version('<v2_uuid>');` — expected: v2.status flips to 'published', v1.status flips to 'superseded', blocks.current_body_version_id now points to v2.
    - Step 2e: Verify supersession (read-only): `SELECT id, status FROM economy_map.block_body_versions WHERE block_slug='identity-trust' ORDER BY created_at;` — expected: v1='superseded', v2='published'. AND `SELECT current_body_version_id FROM economy_map.blocks WHERE slug='identity-trust';` — expected: equals v2_uuid.
    - Step 2f: Negative case — publish a non-existent version_id → expect RAISE EXCEPTION with 'not found or not in draft status': wrap in DO $$ ... EXCEPTION WHEN OTHERS THEN RAISE NOTICE 'PASS (v2f): %', SQLERRM; END $$;` with body `BEGIN PERFORM economy_map.publish_block_version('00000000-0000-0000-0000-000000000000'::uuid); RAISE EXCEPTION 'TEST FAILED: bogus version was accepted'; EXCEPTION WHEN OTHERS THEN RAISE NOTICE 'PASS (v2f): %', SQLERRM; END;`.
    - Step 2g: Negative case — re-publish an already-published version → expect same RAISE EXCEPTION: wrap in DO $$ block calling `economy_map.publish_block_version('<v2_uuid>')` (which is now 'published'); expect 'not found or not in draft status'.

    ────────────────────────────────────────────────────────────────
    SECTION 3 — D-25 bullet 3 (append-only violations; ROADMAP criterion 4):
    - `-- ───────────────── D-25 bullet 3: append-only violation traps ─────────────────`
    - Step 3a (block_body_versions.body_md UPDATE → exception): DO $$ block; pick the v1 or v2 id from Section 2; attempt `UPDATE economy_map.block_body_versions SET body_md = 'mutated' WHERE id = '<v1_uuid>';` — expect `RAISE NOTICE 'PASS (3a): %'` with SQLERRM containing 'append-only'.
    - Step 3b (block_body_versions.proposed_maturity UPDATE → exception): same pattern; `UPDATE … SET proposed_maturity = 'mature' WHERE id = …;` — expect SQLERRM containing 'append-only' (specifically 'block_body_versions.proposed_maturity is append-only' or similar).
    - Step 3c (block_body_versions DELETE → exception): same DO $$ pattern; `DELETE FROM economy_map.block_body_versions WHERE id = …;` — expect SQLERRM containing 'append-only' or 'not permitted' (per Section 8 of 033 migration).
    - Step 3d (block_body_versions lifecycle UPDATE → success): NOT wrapped in DO $$ — this should succeed. `UPDATE economy_map.block_body_versions SET status='superseded' WHERE id='<v2_uuid>';` then immediately put it back: `UPDATE economy_map.block_body_versions SET status='published' WHERE id='<v2_uuid>';` — both succeed (proves D-12, ROADMAP criterion 4's lifecycle-mutable carve-out is honored).
    - Step 3e (timeline_entries.what_shifted UPDATE → exception): first insert a test timeline_entries row for `identity-trust` to have something to UPDATE: `INSERT INTO economy_map.timeline_entries (block_slug, event_date, what_shifted, why_it_mattered, source_url, source_edition_id, tag_confidence) VALUES ('identity-trust', CURRENT_DATE, 'TEST entry — Plan 02-02 verification', 'matters because verification', 'https://example.com/verify', 'verify-202602-01', 0.95) RETURNING id;`. Capture the entry id. Then DO $$ block attempting `UPDATE economy_map.timeline_entries SET what_shifted = 'mutated' WHERE id = '<entry_id>';` — expect SQLERRM containing 'append-only'.
    - Step 3f (timeline_entries DELETE → exception): DO $$ block; `DELETE FROM economy_map.timeline_entries WHERE id = '<entry_id>';` — expect SQLERRM containing 'append-only' or 'not permitted'.
    - Step 3g (unsorted is a valid block_slug — SCHM-08): NOT wrapped in DO $$; `INSERT INTO economy_map.timeline_entries (block_slug, event_date, what_shifted, why_it_mattered, tag_confidence) VALUES ('unsorted', CURRENT_DATE, 'TEST unsorted entry', 'low-confidence classifier output', 0.42) RETURNING id;` — expected: insert succeeds; capture id for the anon-key RLS probe in Task 3. SCHM-08 + ROADMAP criterion 4 second half proven.

    ────────────────────────────────────────────────────────────────
    SECTION 4 — D-25 bullet 4 (lifecycle column UPDATE succeeds):
    - Already covered in step 3d. Add a comment-only block here pointing back to step 3d so the script's structure mirrors D-25's bullet list 1..5 explicitly. No new SQL.

    ────────────────────────────────────────────────────────────────
    SECTION 5 — D-25 bullet 5 (anon-key RLS probe — HTTP, not SQL):
    - `-- ───────────────── D-25 bullet 5: anon-key RLS probe (curl, not SQL) ─────────────────`
    - Comment block (NOT executable SQL — readers paste this into a shell). Three curl invocations against `<PROJECT_URL>/rest/v1/{blocks,block_body_versions,timeline_entries}` with `Accept-Profile: economy_map` + `apikey: <ANON_KEY>` headers. Expected:
      - `/rest/v1/blocks?select=*` returns 7 rows.
      - `/rest/v1/block_body_versions?select=id,status` returns ONLY rows with `status='published'` — at the time of the probe, this should be exactly 1 row (the v2 from Section 2, which is the current published version for identity-trust). v1 ('superseded') and any draft rows MUST be absent from the anon-view.
      - `/rest/v1/timeline_entries?select=id,block_slug` returns ONLY rows with `block_slug != 'unsorted'`. The test entry for identity-trust from step 3e should appear; the 'unsorted' entry from step 3g should NOT appear.
    - The script does NOT execute these — Task 3 of this plan executes them and captures the output in `02-VERIFY-RESULTS.md`.

    ────────────────────────────────────────────────────────────────
    SECTION 6 — Cleanup / residue note:
    - `-- ───────────────── Cleanup ─────────────────`
    - Comment-only: "Test rows inserted by this verification (two block_body_versions for identity-trust, two timeline_entries: one named + one unsorted) are left in place. DELETE attempts would raise append-only exceptions (which is the point). Subsequent phases inherit this seed state — Phase 5 (intake classifier) and Phase 7 (synthesis loop) operate on real data, so the test rows do NOT interfere with their pipelines. To clear them entirely, drop the schema and re-apply the migration."
    - This residue note is the planner's transparency about post-verification state — it goes in 02-VERIFY-RESULTS.md too.

    Write the file in one Write call. Do NOT inline expected outputs in the SQL file (those go in 02-VERIFY-RESULTS.md). Keep section comments grep-friendly: every section starts with `-- ─────────` and a `D-25 bullet N` marker.
  </action>
  <verify>
    <automated>test -f /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql && grep -cE '^-- ─.*D-25 bullet [1-5]' /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql | grep -qE '^[5-9]$' && grep -c 'DO \$\$' /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql | grep -qE '^[5-9]$' && grep -c "RAISE NOTICE 'PASS" /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql | grep -qE '^[5-9]$' && grep -c 'economy_map.publish_block_version' /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql | grep -qE '^[1-9]' && grep -c "block_slug = 'unsorted'" /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql | grep -qE '^[0-9]+$' && grep -c 'identity-trust' /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql | grep -qE '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql` exists.
    - All 5 D-25 bullet section markers present: `grep -cE '^-- ─.*D-25 bullet [1-5]' …` returns 5.
    - At least 5 `DO $$` blocks (the negative-case wrappers): `grep -c 'DO \$\$' …` returns ≥ 5.
    - At least 5 `RAISE NOTICE 'PASS' …` lines: `grep -c "RAISE NOTICE 'PASS" …` returns ≥ 5.
    - References to all the right identifiers: `economy_map.publish_block_version` appears ≥ 2 times (one positive case in Section 2, one negative case in Section 2), `economy_map.block_body_versions` appears ≥ 5 times, `economy_map.timeline_entries` appears ≥ 3 times, `economy_map.blocks` appears ≥ 3 times.
    - The placeholder substring check for live_tension is present: `grep -c "live_tension LIKE '%TBD%'" …` returns ≥ 1 OR equivalent (proves Section 1's live_tension placeholder probe is in place).
    - Section 5 (anon-key probe) is a comment block (NOT executable SQL): `grep -A 3 "D-25 bullet 5" .planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql | grep -c '^-- ' | grep -qE '^[1-9]'` (the next few lines after the bullet 5 header start with `-- `).
    - File does NOT contain `supabase.in_(` or `.in_(` (the banned client-side pattern per D-02 / PROJECT.md / CLAUDE.md).
    - File is ≥ 120 lines (substantive script, not a stub).
  </acceptance_criteria>
  <done>
    `02-VERIFY.sql` exists with one labelled section per D-25 bullet (1..5), all "expected failures" wrapped in DO $$ ... EXCEPTION WHEN OTHERS THEN RAISE NOTICE 'PASS' blocks, the publish RPC exercised positively + negatively, the append-only triggers exercised on body_md / proposed_maturity / what_shifted / DELETE, lifecycle UPDATEs (status, published_at) shown to succeed, and the unsorted insert proven valid. Section 5 references the curl probe that Task 3 will execute.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute 02-VERIFY.sql against live Supabase + capture every output</name>
  <files>
    (live-database task — no repo files modified; outputs captured in memory for Task 3 doc-write)
  </files>
  <read_first>
    - .planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql (the script Task 1 produced — executor needs to walk it section-by-section)
    - .planning/phases/02-economy-map-schema-seven-block-seed/02-CONTEXT.md D-25 (expected behavior per bullet)
    - CLAUDE.md (Supabase project ref: zxzaaqfowtqvmsbitqpu)
    - supabase/migrations/033_economy_map_schema.sql (so executor knows the exact RAISE EXCEPTION strings to expect)
  </read_first>
  <action>
    Walk through `02-VERIFY.sql` section-by-section, executing each block via Supabase MCP `mcp__claude_ai_Supabase__execute_sql` with `project_id: zxzaaqfowtqvmsbitqpu`. Capture every output verbatim for use in Task 3 (writing `02-VERIFY-RESULTS.md`). The walk order:

    1. **Section 1 (D-25 bullet 1 — schema/blocks/enum queries)**: Execute the three SELECTs from Task 1 Section 1. Expected outputs:
       - `block_count = 7`
       - 7 rows in D-23 slug order with all `live_tension_is_placeholder = true` and `maturity = 'nascent'`
       - 5 enum labels in `nascent, emerging, contested, consolidating, mature` order
       Capture the JSON responses from the MCP tool.

    2. **Section 2 (D-25 bullet 2 — atomic publish)**: Multi-step. For each step, execute via MCP and capture:
       - Step 2a: Two `INSERT INTO economy_map.block_body_versions (block_slug, body_md, proposed_maturity, status) VALUES ('identity-trust', '# Test body v1 (verify-202602)', 'emerging', 'draft') RETURNING id;` calls. Use `proposed_maturity='emerging'` for v1 (different from blocks.maturity='nascent' so the publish RPC's maturity bump is observable). Use `proposed_maturity='contested'` for v2 so the second publish bumps maturity again. Capture both UUIDs as `v1_uuid` and `v2_uuid` for subsequent steps.
       - Step 2b: `SELECT economy_map.publish_block_version('<v1_uuid>'::uuid);` — capture (should return no rows / NULL).
       - Step 2c: The verification SELECTs from Task 1 Section 2 step 2c. Capture: v1.status='published', has_published_at=true, blocks.current_body_version_id=v1_uuid, blocks.maturity='emerging', blocks.last_synthesized_at IS NOT NULL.
       - Step 2d: `SELECT economy_map.publish_block_version('<v2_uuid>'::uuid);` — capture.
       - Step 2e: The supersession verification SELECTs. Capture: v1.status='superseded', v2.status='published', blocks.current_body_version_id=v2_uuid, blocks.maturity='contested'.
       - Step 2f: Bogus-UUID DO $$ block. Capture the RAISE NOTICE output — should contain `'not found or not in draft status'`.
       - Step 2g: Already-published DO $$ block. Capture the RAISE NOTICE — should contain `'not found or not in draft status'`.

    3. **Section 3 (D-25 bullet 3 — append-only violations)**: Execute each step in order, capturing the exact RAISE NOTICE output from each DO $$ block (the SQLERRM substring is critical for `02-VERIFY-RESULTS.md` Trigger Fire Log):
       - Step 3a: body_md UPDATE → exception containing `'block_body_versions.body_md is append-only'` (or similar — match the actual migration's exception message format).
       - Step 3b: proposed_maturity UPDATE → exception containing `'block_body_versions.proposed_maturity is append-only'`.
       - Step 3c: DELETE → exception containing `'block_body_versions is append-only'` or `'DELETE not permitted'`.
       - Step 3d: Lifecycle UPDATE (status superseded → published) → both should succeed silently (no exception, no NOTICE; capture the rowcount = 1 confirmation from the MCP response).
       - Step 3e: Insert test timeline_entries row for identity-trust; capture the `entry_id_named` UUID. Then attempt `UPDATE timeline_entries SET what_shifted='mutated'` → exception containing `'timeline_entries.what_shifted is append-only'`.
       - Step 3f: DELETE the named entry → exception containing `'timeline_entries is append-only'` or `'DELETE not permitted'`.
       - Step 3g: Insert test timeline_entries row with `block_slug='unsorted'`; capture the `entry_id_unsorted` UUID; verify insert succeeded.

    4. **Section 4**: No SQL — already covered by step 3d.

    5. **Section 5 (D-25 bullet 5 — anon-key RLS probe)**: Task 3 of this plan handles the curl probes; Task 2 stops here.

    After every MCP execute_sql call, immediately capture:
    - The full JSON response (including `rows`, `notices`, and any error).
    - The exact SQLERRM text from each `RAISE NOTICE 'PASS: %'` block (this becomes the Trigger Fire Log in `02-VERIFY-RESULTS.md`).

    If any step produces an unexpected outcome (e.g., a positive UPDATE that SHOULD have raised an exception silently succeeds, or a positive RPC call raises an unexpected error), do NOT silently continue. Stop, document the discrepancy, and surface to the operator as a Phase 2 verification failure that must be diagnosed before proceeding to Task 3. The Critical Invariants section of `02-VERIFY-RESULTS.md` MUST be all-PASS for Phase 2 to pass; surfacing an early discrepancy is cheaper than writing it up as a failure later.

    Store the captured outputs (verbatim text) in the executor's working memory / scratch — Task 3 will assemble them into `02-VERIFY-RESULTS.md`. Do NOT write any file in this task.
  </action>
  <verify>
    <automated>echo "This task does not modify the filesystem. The next task's existence check (02-VERIFY-RESULTS.md) is the proof that this task captured the outputs needed."</automated>
  </verify>
  <acceptance_criteria>
    - Every section of `02-VERIFY.sql` was executed against project `zxzaaqfowtqvmsbitqpu` via Supabase MCP `execute_sql`.
    - Section 1 outputs captured: block_count=7, seven slugs in D-23 order, 5 enum labels in maturity order.
    - Section 2 outputs captured: v1 inserted, publish_block_version called twice (positive cases), both supersession effects verified (current_body_version_id pointer + maturity bump), two negative cases captured with exception strings containing `'not found or not in draft status'`.
    - Section 3 outputs captured: six exception strings (body_md, proposed_maturity, DELETE body, what_shifted, DELETE timeline) all containing the `'append-only'` substring; lifecycle UPDATE step 3d succeeded without exception; unsorted insert step 3g succeeded.
    - All UUIDs captured (v1_uuid, v2_uuid, entry_id_named, entry_id_unsorted) — needed by Task 3 to reference specific rows in the results doc.
    - No file written in this task.
    - If ANY step produced an unexpected outcome, the executor stopped, documented it, and did not advance to Task 3 silently.
  </acceptance_criteria>
  <done>
    All 11 SQL exercise outputs (Section 1 × 3 queries, Section 2 × 6 steps, Section 3 × 7 steps) are captured in executor memory. Every expected exception fired with the expected substring (`'append-only'` or `'not found or not in draft status'`). Lifecycle UPDATEs succeeded. The unsorted insert succeeded. Phase 2 ROADMAP success criteria 2, 3, 4, and 5 are demonstrably satisfied by the captured outputs (criterion 1's Accept-Profile probe + criterion 4's anon-key RLS posture probe happens in Task 3).
  </done>
</task>

<task type="auto">
  <name>Task 3: Run anon-key probes (curl) + author 02-VERIFY-RESULTS.md (proof artifact)</name>
  <files>.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md</files>
  <read_first>
    - .planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql (the script Task 2 just executed — the source of expected outputs)
    - .planning/phases/01-render-stack-diagnostic/01-VERIFICATION.md (Phase 1's verification doc — the structural model to mirror per 02-PATTERNS.md §02-VERIFY-RESULTS.md; in particular front-matter, Observable Truths table, Critical Invariants table)
    - .planning/phases/02-economy-map-schema-seven-block-seed/02-PATTERNS.md §02-VERIFY-RESULTS.md (the Phase-2-specific differences from Phase 1: evidence column cites SQL output not file:line; Trigger Fire Log table is new; Anon-key Probe Results table is new)
    - .planning/phases/02-economy-map-schema-seven-block-seed/02-CONTEXT.md D-25 (the structural promise this doc proves)
    - .planning/ROADMAP.md (Phase 2 success criteria 1..5 — one Observable Truths row per criterion)
    - CLAUDE.md (project ref + anon-key access via Supabase MCP)
  </read_first>
  <action>
    First, run the anon-key probes (D-25 bullet 5), then write the results doc.

    **Step A — Anon-key probes** (resolve project URL + anon key, then run 3 curl probes):

    1. Call `mcp__claude_ai_Supabase__get_project_url` with `project_id: zxzaaqfowtqvmsbitqpu`. Capture the URL.
    2. Call `mcp__claude_ai_Supabase__get_publishable_api_key` with `project_id: zxzaaqfowtqvmsbitqpu`. Capture the anon key.
    3. Run three curl probes via Bash (one per table). For each, capture HTTP status code + body. Use `curl -sS -w "\nHTTP_STATUS:%{http_code}\n"` so both status and body are visible. Set environment variables locally — do NOT print the anon key into any file (the file should reference "ANON_KEY (redacted)" only):
       ```
       curl -sS -w "\nHTTP_STATUS:%{http_code}\n" \
         -H "apikey: $ANON_KEY" \
         -H "Authorization: Bearer $ANON_KEY" \
         -H "Accept-Profile: economy_map" \
         "$PROJECT_URL/rest/v1/blocks?select=slug,tier,accent,sort_order&order=sort_order.asc"
       ```
       Expected: HTTP 200, 7 rows in D-23 slug order.
    4. Same for `/rest/v1/block_body_versions?select=id,status`. Expected: HTTP 200, exactly 1 row — the v2 from Plan 02-02 Task 2 step 2d (block_slug='identity-trust', status='published'). The draft rows (which don't exist anymore — both v1 and v2 went through the publish RPC) and the superseded v1 must NOT appear. The expected count is **1** because the verification exercise published two rows (v1 then v2), v1 was superseded by v2's publish, and v2 remains 'published' — RLS shows published only.
    5. Same for `/rest/v1/timeline_entries?select=id,block_slug`. Expected: HTTP 200, exactly 1 row — the named-block entry from Task 2 step 3e (block_slug='identity-trust'). The unsorted entry from step 3g must NOT appear.
    6. Capture all three responses (status + body) for the Anon-key Probe Results table.

    **Step B — Author `02-VERIFY-RESULTS.md`** (Use the Write tool). Structure:

    Front-matter (mirror Phase 1's `01-VERIFICATION.md` shape per 02-PATTERNS.md):
    ```
    ---
    phase: 02-economy-map-schema-seven-block-seed
    verified: <ISO timestamp from `date -u +'%Y-%m-%dT%H:%M:%SZ'`>
    status: passed
    score: 5/5 success criteria verified
    overrides_applied: 0
    project_ref: zxzaaqfowtqvmsbitqpu
    ---
    ```

    Section 1 — Title + opening paragraph:
    - `# Phase 2: economy_map Schema + Seven-Block Seed — Verification Report`
    - 2-3 line opening: describe this doc as the structured proof for ROADMAP criteria 3, 4, 5 (with criteria 1 and 2 re-confirmed inline), produced by executing `02-VERIFY.sql` via Supabase MCP `execute_sql` against project `zxzaaqfowtqvmsbitqpu` plus three anon-key curl probes against `/rest/v1/{blocks,block_body_versions,timeline_entries}` with `Accept-Profile: economy_map`.

    Section 2 — Observable Truths table (one row per ROADMAP Phase 2 success criterion):
    | # | Truth | Status | Evidence |
    |---|-------|--------|----------|
    | 1 | `economy_map` schema responds to direct PostgREST with `Accept-Profile: economy_map`; supabase-py `.in_()` not used in new code (ROADMAP criterion 1) | VERIFIED | Anon-key curl probe to `/rest/v1/blocks` with `Accept-Profile: economy_map` returned HTTP 200 + 7 rows (see Anon-key Probe Results §4). `grep -rE 'supabase.*\.in_\(' supabase/migrations/033_economy_map_schema.sql .planning/phases/02-economy-map-schema-seven-block-seed/` returns 0 hits. |
    | 2 | Seven blocks queryable with `live_tension`, `subtitle`, `tier`, `accent`, `sort_order` populated; tiers/accents/sort_orders match D-23 (ROADMAP criterion 2) | VERIFIED | `SELECT slug, tier, accent, sort_order FROM economy_map.blocks ORDER BY sort_order` returned 7 rows in D-23 order: <list the 7 slugs verbatim>. All live_tension values contain the substring 'TBD' per D-21. |
    | 3 | Atomic publish RPC flips draft → published, supersedes prior, updates `blocks.current_body_version_id` and `blocks.maturity` (ROADMAP criterion 3, SCHM-06) | VERIFIED | Section 2 of `02-VERIFY.sql` exercised: v1 inserted as draft, `publish_block_version(v1)` → v1.status='published' + blocks.current_body_version_id=v1 + blocks.maturity='emerging' + blocks.last_synthesized_at set. Then v2 inserted+published → v1.status='superseded' (atomically), v2.status='published', blocks.current_body_version_id=v2, blocks.maturity='contested'. See Trigger Fire Log §3 for the two negative-case exceptions (bogus uuid + already-published). |
    | 4 | `block_body_versions` and `timeline_entries` reject content-column UPDATEs (append-only enforced); `unsorted` accepted as valid `block_slug` (ROADMAP criterion 4, SCHM-04 + SCHM-08) | VERIFIED | Section 3 of `02-VERIFY.sql`: 6 RAISE EXCEPTION trips captured (see Trigger Fire Log §3 — body_md UPDATE, proposed_maturity UPDATE, body DELETE, what_shifted UPDATE, timeline DELETE, and one more bonus case). All exceptions contain the substring `'append-only'`. Lifecycle UPDATEs (status, published_at) succeeded in step 3d. Unsorted-block insert (step 3g) succeeded — `block_slug='unsorted'` is accepted by the schema. |
    | 5 | Maturity enum (`nascent → emerging → contested → consolidating → mature`) is the only accepted value for `blocks.maturity` and `block_body_versions.proposed_maturity` (ROADMAP criterion 5, SCHM-05) | VERIFIED | `pg_enum` query in Section 1 returned 5 labels in the expected order. Step 2a inserted with `proposed_maturity='emerging'` and `'contested'` (valid enum values) — both accepted. The maturity column on both tables is declared as `economy_map.maturity` (real Postgres ENUM), not `TEXT … CHECK`, so any invalid value would raise an enum-cast error at INSERT time (not relied on as a verification — the declaration itself is the structural guarantee). |

    Section 3 — Trigger Fire Log table (captures every RAISE NOTICE / RAISE EXCEPTION output from Task 2):
    | # | Trigger / RPC | Test | Captured SQLERRM | Result |
    |---|---------------|------|------------------|--------|
    | 3.1 | block_body_versions_append_only_trg | UPDATE body_md (step 3a) | <verbatim SQLERRM containing 'append-only'> | PASS |
    | 3.2 | block_body_versions_append_only_trg | UPDATE proposed_maturity (step 3b) | <verbatim SQLERRM> | PASS |
    | 3.3 | block_body_versions_append_only_trg | DELETE (step 3c) | <verbatim SQLERRM containing 'append-only' or 'not permitted'> | PASS |
    | 3.4 | (no trigger — lifecycle update permitted) | UPDATE status='superseded' then back (step 3d) | (no exception, no notice) | PASS — lifecycle column UPDATE succeeded as designed (D-12) |
    | 3.5 | timeline_entries_append_only_trg | UPDATE what_shifted (step 3e) | <verbatim SQLERRM containing 'append-only'> | PASS |
    | 3.6 | timeline_entries_append_only_trg | DELETE (step 3f) | <verbatim SQLERRM> | PASS |
    | 3.7 | publish_block_version RPC | Bogus uuid (step 2f) | <verbatim SQLERRM containing 'not found or not in draft status'> | PASS |
    | 3.8 | publish_block_version RPC | Already-published (step 2g) | <verbatim SQLERRM> | PASS |

    Section 4 — Anon-key Probe Results table (Step A outputs):
    | Endpoint | Headers | HTTP Status | Row Count | Visible / Hidden | Result |
    |----------|---------|-------------|-----------|------------------|--------|
    | `/rest/v1/blocks?select=slug,tier,accent,sort_order&order=sort_order.asc` | Accept-Profile: economy_map; apikey: ANON_KEY (redacted) | 200 | 7 | all 7 blocks in D-23 order | PASS — exposed-schemas allowlist honored, anon-read on blocks works (D-05) |
    | `/rest/v1/block_body_versions?select=id,status` | same | 200 | 1 | v2 (status='published') visible; v1 (superseded) hidden | PASS — RLS predicate `status='published'` honored (D-06, T-03) |
    | `/rest/v1/timeline_entries?select=id,block_slug` | same | 200 | 1 | named-block entry visible; unsorted entry hidden | PASS — RLS predicate `block_slug != 'unsorted'` honored (D-07, T-04) |

    Section 5 — Critical Invariants table (mirrors Phase 1's structural model):
    | Invariant | Check | Result |
    |-----------|-------|--------|
    | Migration file landed | `test -f supabase/migrations/033_economy_map_schema.sql` | exists — PASS |
    | First-in-tree pattern: CREATE SCHEMA | `grep -c 'CREATE SCHEMA' supabase/migrations/*.sql` (non-comment lines only) | 1 (the new one) — PASS, D-04 satisfied |
    | First-in-tree pattern: CREATE TYPE … AS ENUM | `grep -c 'CREATE TYPE' supabase/migrations/*.sql` | 1 (the new one) — PASS |
    | First-in-tree pattern: BEFORE UPDATE/DELETE trigger | `grep -cE 'BEFORE UPDATE OR DELETE' supabase/migrations/*.sql` | 2 (both new) — PASS, D-10/D-14 satisfied |
    | Loud comment block present | `grep -c 'APPEND-ONLY ENFORCEMENT' supabase/migrations/033_economy_map_schema.sql` | ≥ 1 — PASS, future-agent guard rail in place |
    | search_path pinned on both RPCs | `grep -c 'SET search_path = economy_map' supabase/migrations/033_economy_map_schema.sql` | 2 — PASS, T-02 mitigated |
    | EXECUTE on RPCs revoked from PUBLIC | `grep -c 'REVOKE ALL ON FUNCTION economy_map' …` | 2 — PASS, T-06 mitigated |
    | Anon does NOT have EXECUTE on RPCs | `SELECT has_function_privilege('anon', 'economy_map.publish_block_version(uuid)', 'EXECUTE')` | false — PASS, D-19 |
    | Maturity is a real ENUM (not text + CHECK) | `\d economy_map.blocks` shows `maturity \| economy_map.maturity` | yes — PASS, D-03, SCHM-05 |
    | Idempotent seed (re-runs don't clobber operator-set live_tension) | seed uses `ON CONFLICT (slug) DO NOTHING` (NOT DO UPDATE) | yes — PASS, D-20/D-21 |
    | supabase-py `.in_()` not introduced | `grep -rE '\.in_\(' supabase/migrations/033_economy_map_schema.sql .planning/phases/02-economy-map-schema-seven-block-seed/` | 0 hits — PASS, ROADMAP criterion 1 second clause |
    | Application code untouched | `git diff --name-only HEAD~ HEAD \| grep -v -E '^(supabase/migrations/033|\.planning/)'` (from Plan 02-01 + 02-02 combined commit set) | empty — PASS, CONTEXT.md `<code_context>` "Integration Points" honored |
    | Exposed-schemas allowlist updated | Anon-key curl with `Accept-Profile: economy_map` to `/rest/v1/blocks` returned HTTP 200 + 7 rows | PASS — D-24 satisfied, Phase 1 §4.5 known unknown RESOLVED |

    Section 6 — Test residue (transparency, not failure):
    - Two `economy_map.block_body_versions` rows exist for `identity-trust` (v1 status='superseded', v2 status='published'). These are the test rows from Section 2 of `02-VERIFY.sql`. They remain in place (DELETE attempts would raise — that's the whole point). Phase 5 (intake classifier) and Phase 7 (synthesis loop) operate on real data; these test rows do not interfere with their pipelines.
    - One `economy_map.timeline_entries` row for `identity-trust` (the named-block test entry from step 3e) and one for `unsorted` (step 3g) remain. Same posture: real-pipeline data will append; these test rows do not interfere.
    - To clear residue entirely, drop the schema (`DROP SCHEMA economy_map CASCADE;`) and re-apply migration 033. Operator may choose to do this before Phase 5 begins, but it is NOT required — the test rows are tagged with `'TEST'` substrings in `body_md` and `what_shifted` for easy identification.

    Section 7 — Closing line:
    - `*Phase 2 verification complete. All 5/5 ROADMAP success criteria proven by SQL exercise + anon-key probe. Zero application code changes — phase stayed inside the database boundary.*`

    Do NOT include the anon key value anywhere in this file — use the literal string `ANON_KEY (redacted)` in the Headers column.

    Capture the exact verbatim SQLERRM text from Task 2's outputs — do NOT paraphrase. The grep-friendly substrings (`'append-only'`, `'not found or not in draft status'`) are the contract.
  </action>
  <verify>
    <automated>test -f /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md && grep -c '^---$' /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md | grep -qE '^[2-9]$' && grep -c 'status: passed' /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md | grep -qE '^[1-9]' && grep -c '5/5' /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md | grep -qE '^[1-9]' && grep -cE '^## ' /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md | grep -qE '^[3-9]' && grep -c 'Observable Truths' /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md | grep -qE '^[1-9]' && grep -c 'Trigger Fire Log' /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md | grep -qE '^[1-9]' && grep -c 'Anon-key Probe' /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md | grep -qE '^[1-9]' && grep -c 'Critical Invariants' /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md | grep -qE '^[1-9]' && grep -c 'append-only' /root/bitcoin_bot/.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md | grep -qE '^[3-9]'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md` exists.
    - Front-matter present and parseable: `phase: 02-economy-map-schema-seven-block-seed`, `verified:` (ISO timestamp), `status: passed`, `score: 5/5 success criteria verified`, `project_ref: zxzaaqfowtqvmsbitqpu`. `grep -c '^---$' …` returns 2 (open + close).
    - All 5 major sections present: Observable Truths, Trigger Fire Log, Anon-key Probe Results, Critical Invariants, Test residue / Closing. `grep -cE '^## ' …` returns ≥ 5.
    - Observable Truths table has exactly 5 data rows (one per ROADMAP success criterion). Each row's Status column is `VERIFIED`.
    - Trigger Fire Log table has ≥ 6 rows of captured RAISE EXCEPTION strings. Every SQLERRM string for an append-only trip contains the literal substring `'append-only'` (grep-friendly per CONTEXT.md "Claude's Discretion").
    - Anon-key Probe Results table has 3 rows (blocks, block_body_versions, timeline_entries). All three show HTTP 200. Blocks row count = 7. The other two row counts are consistent with RLS filtering (1 published body_version visible; 1 named-block timeline_entry visible; superseded + unsorted hidden).
    - Critical Invariants table has ≥ 10 rows; all marked PASS. Includes the four first-in-tree precedent invariants (CREATE SCHEMA, CREATE TYPE … AS ENUM, BEFORE UPDATE/DELETE trigger, loud comment block) per D-04 / D-10.
    - Test residue section explicitly enumerates the rows left behind (2 block_body_versions, 2 timeline_entries) and notes they do not interfere with downstream phases.
    - The closing line states "Phase 2 verification complete" and "5/5 ROADMAP success criteria".
    - File does NOT leak the anon key — `grep -c '^ANON_KEY' …` returns 0; the key appears only as `ANON_KEY (redacted)`.
    - File does NOT reference `supabase.in_(` anywhere (per D-02).
    - `git status --porcelain | awk '{print $2}' | grep -v -E '^\\.planning/'` returns empty for this plan's changes (no application code touched).
  </acceptance_criteria>
  <done>
    `02-VERIFY-RESULTS.md` exists with all five structured sections, every Observable Truths row marked VERIFIED, every Trigger Fire Log row marked PASS with the verbatim SQLERRM string, every Anon-key Probe Results row showing HTTP 200 with the expected row count, every Critical Invariants row marked PASS, and the test residue clearly documented. The phase's ROADMAP success criteria 1..5 are conclusively proven. Phase 2 can be marked complete in STATE.md / ROADMAP.md by the executor's harness after this plan's SUMMARY is written.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser anon → PostgREST → `economy_map.*` | Verification exercises this boundary directly via curl probes — the same path the future browser SPA (Phase 4) will use. |
| executor agent (service_role) → `economy_map.*` | All Supabase MCP `execute_sql` calls run as service_role (which bypasses RLS). The verification script's append-only trip tests run AS service_role and still trap — proves the structural guarantee. |
| operator's local machine | The anon key probed via curl in Task 3 is captured from the Supabase MCP and used in-memory; it is never written to disk (the results doc redacts it). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 (re-test) | Tampering | `economy_map.block_body_versions` / `economy_map.timeline_entries` against service_role | mitigate (verified) | Task 2 step 3a/3b/3c/3e/3f exercises every pinned-column UPDATE + every DELETE as service_role; the exceptions captured in the Trigger Fire Log table prove the triggers fire. This is the EXISTENCE PROOF for the Plan 02-01 trigger mitigation. |
| T-02-02 (re-test) | Elevation of Privilege | publish/reject RPC search_path | mitigate (verified) | Task 3 Critical Invariants table row "search_path pinned on both RPCs" greps the migration; presence of `SET search_path = economy_map, public` on both RPCs is the proof. |
| T-02-03 (re-test) | Information Disclosure | drafts visible to anon | mitigate (verified) | Task 3 Anon-key Probe Results row for `/rest/v1/block_body_versions` returned only the published v2 — the superseded v1 was hidden by RLS. EXISTENCE PROOF for the Plan 02-01 RLS predicate `(status = 'published')`. |
| T-02-04 (re-test) | Information Disclosure | unsorted entries visible to anon | mitigate (verified) | Task 3 Anon-key Probe Results row for `/rest/v1/timeline_entries` returned only the named-block entry — the unsorted entry from step 3g was hidden by RLS. EXISTENCE PROOF for the Plan 02-01 RLS predicate `(block_slug != 'unsorted')`. |
| T-02-05 (re-test) | Denial of Service | publish-race / double-publish | mitigate (verified) | Task 2 step 2g re-publishes a now-already-published row → exception `'not found or not in draft status'`. The single-statement WHERE clause + RETURNING resolved the race to one winner. EXISTENCE PROOF for the Plan 02-01 RPC body's `IF v_slug IS NULL THEN RAISE EXCEPTION …` guard. |
| T-02-08 | Information Disclosure | Anon-key leak in verification artifacts | mitigate | Task 3 explicitly redacts the anon key in `02-VERIFY-RESULTS.md` (Headers column says `ANON_KEY (redacted)`). The Supabase publishable / anon key is widely scoped to read-only RLS-gated views in this project's posture — it is not a high-sensitivity secret — but redacting it preserves operational hygiene. |
| T-02-09 | Information Disclosure | SQLERRM leaking row contents in verification doc | accept | Trigger exceptions deliberately include OLD vs NEW values for debugging (e.g., `'block_body_versions.body_md is append-only (was %, now %)'`). The verification doc captures these verbatim. The "leaked" content is test data the executor inserted (literally `'# Test body v1 (verify-202602)'`), not production data — accepted. |
| T-02-SC | Tampering | npm/pip/cargo installs in this phase | accept | This plan performs NO package installations. Work is two file writes + Supabase MCP execute_sql calls + curl. Per RESEARCH.md `## Package Legitimacy Audit` policy: no installs occur, no audit needed. |

This plan does not introduce new trust boundaries — it exercises the boundaries Plan 02-01 created. Every mitigation listed in 02-01's threat model gets an existence proof in this plan's Trigger Fire Log + Anon-key Probe Results tables.
</threat_model>

<verification>
Per-task `<verify>` blocks cover the bulk of the work. Plan-level checks:

- `test -f .planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql` — script exists.
- `test -f .planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md` — results doc exists.
- `grep -c 'status: passed' .planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md` returns ≥ 1.
- `grep -c '5/5' .planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md` returns ≥ 1.
- Trigger Fire Log captures ≥ 6 append-only exceptions (verbatim SQLERRM strings).
- Anon-key Probe Results shows HTTP 200 on all three probes with the expected row counts (7 / 1 / 1).
- Critical Invariants table has ≥ 10 rows, all PASS.
- `git status --porcelain | awk '{print $2}' | grep -v -E '^\.planning/'` returns empty (no application code touched).
</verification>

<success_criteria>
Plan 02-02 is complete when:

1. ✓ `.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY.sql` exists with all 5 D-25 bullet sections + DO $$ ... EXCEPTION wrappers for negative cases.
2. ✓ Every section of `02-VERIFY.sql` was executed against project `zxzaaqfowtqvmsbitqpu` via Supabase MCP execute_sql, with all outputs captured (no unexpected outcomes).
3. ✓ Anon-key curl probes against `/rest/v1/{blocks,block_body_versions,timeline_entries}` with `Accept-Profile: economy_map` returned HTTP 200 with the expected row counts (7 / 1 / 1) and the right rows visible/hidden.
4. ✓ `.planning/phases/02-economy-map-schema-seven-block-seed/02-VERIFY-RESULTS.md` exists with front-matter `status: passed`, all 5 ROADMAP success criteria marked VERIFIED in the Observable Truths table, all append-only exceptions captured in the Trigger Fire Log, all anon-key probes captured in the Anon-key Probe Results, and all Critical Invariants marked PASS.
5. ✓ Phase 2 ROADMAP success criteria 1..5 are conclusively proven by this plan's artifacts.
6. ✓ Zero application code touched — only `.planning/phases/02-economy-map-schema-seven-block-seed/` is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/02-economy-map-schema-seven-block-seed/02-02-SUMMARY.md` when done. The summary should:
- State that all 5/5 Phase 2 ROADMAP success criteria are conclusively verified.
- Link to `02-VERIFY.sql` (the script) and `02-VERIFY-RESULTS.md` (the proof).
- Reference the Trigger Fire Log: ≥ 6 append-only exceptions captured + 2 RPC negative-case exceptions captured, all containing the expected grep-friendly substrings.
- Reference the Anon-key Probe Results: 3 probes, all HTTP 200, row counts (7 / 1 / 1) consistent with RLS filtering — Phase 1 §4.5 known unknown RESOLVED end-to-end.
- List the test residue rows left in the live DB (2 block_body_versions for identity-trust, 2 timeline_entries) so the next phase planner is aware.
- Note that Phase 2 can be marked complete in STATE.md / ROADMAP.md.
- Reference the three first-in-tree patterns this phase landed (CREATE SCHEMA, CREATE TYPE … AS ENUM, BEFORE UPDATE/DELETE trigger) — future agents reference 033_economy_map_schema.sql + 02-VERIFY.sql + 02-VERIFY-RESULTS.md as the canonical example for isolated-schema work.
</output>
</content>
</invoke>
