---
phase: 03-design-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/web/site/style-map.css
autonomous: true
requirements:
  - TOKN-01
  - TOKN-02
  - TOKN-03
  - TOKN-04
must_haves:
  truths:
    - "Four tier-accent CSS custom properties exist with pinned base hex values per TOKN-01 (D-05)"
    - "Four matching on-dark variant CSS custom properties exist, documented as WCAG AA against #0a0a0f"
    - "--accent-tier resolves correctly via body.technical/body.strategic + [data-accent] selectors"
    - "Maturity-pill CSS renders five segments with left-to-right fill keyed off data-stage 1..5 (TOKN-02)"
    - "Timeline-entry CSS pins the two-line format with the literal ↗ glyph (TOKN-03)"
    - "Empty-source timeline rule (:not([data-source])) is present so source-less entries render without [source ↗] (D-11 default)"
    - "No font-family, container width, or page-chrome rules are introduced (TOKN-04 / D-08)"
    - "Selectors only fire inside elements carrying data-accent — edition pages are unaffected (D-06)"
  artifacts:
    - path: "docker/web/site/style-map.css"
      provides: "Tier-accent tokens + --accent-tier resolution + .maturity-pill component + .timeline-entry component"
      contains: "--accent-teal-base: #0F6E56"
      min_lines: 100
  key_links:
    - from: "style-map.css [data-accent=\"...\"]"
      to: "blocks.accent column (teal|purple|coral|gray) via Phase 4 wrapper attribute"
      via: "data-accent attribute (not data-tier — psychology=coral override)"
      pattern: "\\[data-accent=\"(teal|purple|coral|gray)\"\\]"
    - from: ".maturity-pill[data-stage=N] .seg"
      to: "blocks.maturity enum stage 1..5 via Phase 4"
      via: ":nth-child(-n+N) structural selector"
      pattern: ":nth-child\\(-n\\+[1-5]\\)"
---

<objective>
Create `docker/web/site/style-map.css` — the single Phase 3 stylesheet that ships the four tier-accent tokens (TOKN-01), the maturity-pill component contract (TOKN-02), and the pinned timeline-entry format (TOKN-03), while introducing zero typography or page-chrome rules so existing site defaults remain authoritative (TOKN-04 / D-08).

Purpose: Phase 4 will emit `data-accent="teal|purple|coral|gray"` and `data-stage="1..5"` attributes on DOM nodes. This file is the CSS contract that turns those attributes into the visible tokens. Selectors are scoped exclusively to elements carrying `data-accent`, so existing edition pages (`#list-view`, `#reader-view`) are unaffected (D-06).

Output: `docker/web/site/style-map.css`. No other file is modified by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-design-tokens/03-CONTEXT.md
@.planning/phases/03-design-tokens/03-PATTERNS.md
@docker/web/site/style-shared.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author style-map.css — header + tier-accent tokens + --accent-tier resolution</name>
  <files>docker/web/site/style-map.css</files>
  <read_first>
    - docker/web/site/style-shared.css (lines 1-75: header convention, body.technical/body.strategic variable blocks at lines 7-49, container max-width 720px at line 60, mode-transition cascade at lines 66-73)
    - .planning/phases/03-design-tokens/03-CONTEXT.md (D-03 two-variant tokens, D-04 data-tier mechanism, D-05 mapping with psychology=coral override, D-06 edition-page isolation, D-08 inheritance contract)
    - .planning/phases/03-design-tokens/03-PATTERNS.md (§1a file header convention, §1b tier-accent token block, §1c --accent-tier resolution mechanism)
  </read_first>
  <behavior>
    - File opens with a comment block naming the file's role AND the data-attribute contract (data-accent, data-stage, data-source) because all three are first-in-tree CSS patterns.
    - `:root` block defines exactly eight CSS custom properties: `--accent-teal-base`, `--accent-teal-on-dark`, `--accent-purple-base`, `--accent-purple-on-dark`, `--accent-coral-base`, `--accent-coral-on-dark`, `--accent-gray-base`, `--accent-gray-on-dark`.
    - Base hex values are pinned verbatim from TOKN-01 / D-05: teal `#0F6E56`, purple `#534AB7`, coral `#993C1D`, gray `#5F5E5A`.
    - On-dark hex values (planner-chosen, documented in this file as inline comments with measured contrast ratio against `#0a0a0f`): teal `#4FCBA8` (9.99:1), purple `#9D95E8` (7.37:1), coral `#E89072` (7.45:1), gray `#B0AEA8` (9.10:1). All four exceed WCAG AA for text (4.5:1) and non-text (3:1).
    - Eight `--accent-tier` resolution rules exist: four under `body.strategic [data-accent="..."]` mapping to the `-base` variants, four under `body.technical [data-accent="..."]` mapping to the `-on-dark` variants.
    - Selectors use the attribute key `data-accent` (NOT `data-tier`) so the seeded `blocks.accent` column (teal|purple|coral|gray, Phase 2 D-23) flows verbatim, including the deliberate `psychology=coral` override (build spec §1).
  </behavior>
  <action>
Create `docker/web/site/style-map.css` with the following sections, in order:

SECTION 1 — File header comment block. Open the file with this exact header (commenting the data-attribute contract since `data-*`, `:nth-child`, and `:not()` are all first-in-tree per the patterns audit):

```
/* === AgentPulse — Economy-Map Design Tokens === */
/* Layered onto style-shared.css. Activates only inside elements
   carrying data-accent="teal|purple|coral|gray". Pages without
   data-accent see no change (edition pages are unaffected — D-06).

   Markup contract (emitted by Phase 4):
     <div data-accent="teal" data-stage="3"> ... </div>      — block wrapper
     <span class="seg"></span> × 5                            — pill segments
     <article class="timeline-entry" data-source="https://..."> ... </article>

   Tier accent mapping (per blocks.accent column, Phase 2 D-23):
     payments-settlement, identity-trust, memory-context → teal
     autonomy-control, governance-accountability         → purple
     psychology-disposition                              → coral (deliberate)
     regulation-legal                                    → gray
*/
```

SECTION 2 — Tier-accent token variables on `:root`. Define exactly eight CSS custom properties. Use the pinned hex values verbatim. Include inline comments for the on-dark variants documenting the contrast ratio measured against `#0a0a0f`:

```
:root {
    /* Base hex — TOKN-01, calibrated for white/light surfaces */
    --accent-teal-base:   #0F6E56;  /* substrate (identity-trust, memory-context, payments-settlement) */
    --accent-purple-base: #534AB7;  /* behavior (autonomy-control, governance-accountability) */
    --accent-coral-base:  #993C1D;  /* psychology (deliberate distinction — build spec §1) */
    --accent-gray-base:   #5F5E5A;  /* frame (regulation-legal) */

    /* On-dark variants — WCAG AA against #0a0a0f (technical-mode bg).
       Pinned-hue family preserved; measured contrast ratios per planner audit. */
    --accent-teal-on-dark:   #4FCBA8;  /* contrast 9.99:1 — passes AA text (4.5:1) and non-text (3:1) */
    --accent-purple-on-dark: #9D95E8;  /* contrast 7.37:1 — passes AA text and non-text */
    --accent-coral-on-dark:  #E89072;  /* contrast 7.45:1 — passes AA text and non-text */
    --accent-gray-on-dark:   #B0AEA8;  /* contrast 9.10:1 — passes AA text and non-text */
}
```

SECTION 3 — `--accent-tier` resolution by `body.{technical,strategic}` × `[data-accent="..."]`. Eight rules total (four per mode). Each rule sets `--accent-tier` on the data-accent element so descendants (pill segments, timeline source links, etc.) inherit it via `var(--accent-tier)`:

```
/* Strategic (light) mode — pinned-base hex on every data-accent value */
body.strategic [data-accent="teal"]   { --accent-tier: var(--accent-teal-base);   }
body.strategic [data-accent="purple"] { --accent-tier: var(--accent-purple-base); }
body.strategic [data-accent="coral"]  { --accent-tier: var(--accent-coral-base);  }
body.strategic [data-accent="gray"]   { --accent-tier: var(--accent-gray-base);   }

/* Technical (dark) mode — on-dark variants on every data-accent value */
body.technical [data-accent="teal"]   { --accent-tier: var(--accent-teal-on-dark);   }
body.technical [data-accent="purple"] { --accent-tier: var(--accent-purple-on-dark); }
body.technical [data-accent="coral"]  { --accent-tier: var(--accent-coral-on-dark);  }
body.technical [data-accent="gray"]   { --accent-tier: var(--accent-gray-on-dark);   }
```

DO NOT redefine `--accent`, `--bg`, `--text-primary`, or any other variable owned by `style-shared.css`. DO NOT add font-family, container width, or padding rules — those are inherited from `style-shared.css` per TOKN-04 / D-08.

Stop after Section 3 — the next task adds the maturity-pill and timeline-entry component rules.
  </action>
  <verify>
    <automated>grep -c "^/\* === AgentPulse — Economy-Map Design Tokens === \*/" docker/web/site/style-map.css | grep -q "^1$" && grep -c -- "--accent-teal-base:   #0F6E56" docker/web/site/style-map.css | grep -q "^1$" && grep -c -- "--accent-purple-base: #534AB7" docker/web/site/style-map.css | grep -q "^1$" && grep -c -- "--accent-coral-base:  #993C1D" docker/web/site/style-map.css | grep -q "^1$" && grep -c -- "--accent-gray-base:   #5F5E5A" docker/web/site/style-map.css | grep -q "^1$" && grep -c -- "--accent-teal-on-dark:   #4FCBA8" docker/web/site/style-map.css | grep -q "^1$" && grep -c -- "--accent-purple-on-dark: #9D95E8" docker/web/site/style-map.css | grep -q "^1$" && grep -c -- "--accent-coral-on-dark:  #E89072" docker/web/site/style-map.css | grep -q "^1$" && grep -c -- "--accent-gray-on-dark:   #B0AEA8" docker/web/site/style-map.css | grep -q "^1$" && grep -E 'body\.strategic \[data-accent="teal"\]' docker/web/site/style-map.css && grep -E 'body\.technical \[data-accent="coral"\]' docker/web/site/style-map.css</automated>
  </verify>
  <acceptance_criteria>
    - `docker/web/site/style-map.css` exists
    - File contains header comment line `/* === AgentPulse — Economy-Map Design Tokens === */`
    - File contains string `--accent-teal-base:   #0F6E56` (TOKN-01 pinned hex, exact)
    - File contains string `--accent-purple-base: #534AB7` (TOKN-01 pinned hex, exact)
    - File contains string `--accent-coral-base:  #993C1D` (TOKN-01 pinned hex, exact)
    - File contains string `--accent-gray-base:   #5F5E5A` (TOKN-01 pinned hex, exact)
    - File contains all four `-on-dark` variables with the planner-chosen hex values `#4FCBA8` (teal), `#9D95E8` (purple), `#E89072` (coral), `#B0AEA8` (gray)
    - File contains four `body.strategic [data-accent="..."]` rules (teal, purple, coral, gray)
    - File contains four `body.technical [data-accent="..."]` rules (teal, purple, coral, gray)
    - File contains zero occurrences of `data-tier` (the selector key is `data-accent`, not `data-tier`)
    - File contains zero `font-family:` declarations (TOKN-04 — typography inherits)
    - File contains zero `max-width:` declarations (TOKN-04 — container width inherits)
  </acceptance_criteria>
  <done>
    Section 1 (header), Section 2 (`:root` token block), and Section 3 (`--accent-tier` resolution rules) are present in `docker/web/site/style-map.css` and all acceptance assertions pass via the verify command.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append .maturity-pill and .timeline-entry component rules to style-map.css</name>
  <files>docker/web/site/style-map.css</files>
  <read_first>
    - docker/web/site/style-map.css (Task 1 output — verify Sections 1-3 are present before appending)
    - docker/web/site/style-shared.css (lines 7-49: --border, --text-primary, --text-secondary, --text-body, --text-hint, --accent — the cascade variables this task consumes via var(...))
    - .planning/phases/03-design-tokens/03-CONTEXT.md (D-10 pill markup contract, D-11 timeline format, "Other Claude discretions" empty-source rule + literal ↗ glyph)
    - .planning/phases/03-design-tokens/03-PATTERNS.md (§1d maturity-pill CSS pattern, §1e timeline-entry CSS pattern + variable cascade contract)
  </read_first>
  <behavior>
    - `.maturity-pill` renders as a horizontal inline-flex container holding five `.seg` children.
    - `.maturity-pill .seg` defaults to transparent background + `var(--border)` 1px border (unfilled state, mode-aware).
    - `.maturity-pill[data-stage="N"] .seg:nth-child(-n+N)` fills the first N segments with `var(--accent-tier)` background and matching border color, for N in 1..5.
    - The pill is informational only — no `:hover` / `:active` rules (per CONTEXT.md `<deferred>`).
    - `.timeline-entry` pins the two-line format: line 1 is `<time>` + `·` + what-shifted; line 2 is why-it-mattered + optional `source ↗` link.
    - `.timeline-line2` uses `display: flex` with `justify-content: space-between` so the source link right-anchors and why-it-mattered wraps inside the remaining width.
    - The literal `↗` glyph (U+2197) appears in the source-link markup contract (documented in a comment); CSS does not generate the glyph via `::before`.
    - `.timeline-entry:not([data-source]) .timeline-source { display: none }` exists as a defensive/documentary rule for the empty-source case.
    - `.timeline-source` color uses `var(--accent-tier, var(--accent))` so it tier-tints when inside a `data-accent` wrapper and falls back to the mode accent otherwise.
    - Zero `font-family` rules — Courier New body comes from `style-shared.css` `body { font-family: 'Courier New', monospace; }` per TOKN-04.
  </behavior>
  <action>
APPEND to the existing `docker/web/site/style-map.css` (do not overwrite Sections 1-3). Add Section 4 (maturity pill) and Section 5 (timeline entry) as follows.

SECTION 4 — Maturity pill component. Markup contract from D-10:
`<div class="maturity-pill" data-accent="teal" data-stage="3" aria-label="Maturity: contested (3 of 5)"><span class="seg"></span>×5</div>`

Write:

```
/* ── Maturity Pill ──────────────────────────────────────────── */
/* Markup contract:
   <div class="maturity-pill" data-accent="teal" data-stage="3"
        aria-label="Maturity: contested (3 of 5)">
     <span class="seg"></span> × 5
   </div>
   Single source of truth across hub / block / status (TOKN-02).
   Informational only — no hover / interactive states.
*/

.maturity-pill {
    display: inline-flex;
    gap: 2px;
    vertical-align: middle;
}

.maturity-pill .seg {
    display: inline-block;
    width: 18px;
    height: 8px;
    background: transparent;
    border: 1px solid var(--border);
    border-radius: 2px;
}

/* Left-to-right fill by stage. data-stage="N" fills first N segments. */
.maturity-pill[data-stage="1"] .seg:nth-child(-n+1) { background: var(--accent-tier); border-color: var(--accent-tier); }
.maturity-pill[data-stage="2"] .seg:nth-child(-n+2) { background: var(--accent-tier); border-color: var(--accent-tier); }
.maturity-pill[data-stage="3"] .seg:nth-child(-n+3) { background: var(--accent-tier); border-color: var(--accent-tier); }
.maturity-pill[data-stage="4"] .seg:nth-child(-n+4) { background: var(--accent-tier); border-color: var(--accent-tier); }
.maturity-pill[data-stage="5"] .seg:nth-child(-n+5) { background: var(--accent-tier); border-color: var(--accent-tier); }
```

SECTION 5 — Timeline entry component. Markup contract from CONTEXT.md `<specifics>` and build spec §8.3:

Line 1: `<event_date> · <what_shifted>`
Line 2: `<why_it_mattered> [source ↗]` (the literal `↗` glyph, U+2197)
When `source_url` is null/empty, the `<a class="timeline-source">` element is omitted from markup and the `data-source` attribute is absent.

Write:

```
/* ── Timeline Entry (TOKN-03) ───────────────────────────────── */
/* Markup contract (Phase 4 emits):
   <article class="timeline-entry" data-source="https://...">
     <div class="timeline-line1">
       <time class="timeline-date">2026-05-15</time>
       <span class="timeline-sep">·</span>
       <span class="timeline-what">What shifted goes here</span>
     </div>
     <div class="timeline-line2">
       <span class="timeline-why">Why it mattered, one line.</span>
       <a class="timeline-source" href="https://...">source ↗</a>
     </div>
   </article>

   Empty-source variant: omit data-source attribute and the
   <a class="timeline-source"> element from markup entirely. The
   :not([data-source]) rule below is defensive/documentary.
*/

.timeline-entry {
    margin-bottom: 14px;
    line-height: 1.5;
}

.timeline-entry .timeline-line1 {
    font-size: 15px;
    color: var(--text-primary);
    margin-bottom: 2px;
}

.timeline-entry .timeline-date {
    color: var(--text-secondary);
}

.timeline-entry .timeline-sep {
    color: var(--text-hint);
    margin: 0 6px;
}

.timeline-entry .timeline-what {
    color: var(--text-primary);
}

.timeline-entry .timeline-line2 {
    font-size: 14px;
    color: var(--text-body);
    display: flex;
    justify-content: space-between;
    gap: 12px;
}

.timeline-entry .timeline-why {
    flex: 1;
}

.timeline-entry .timeline-source {
    color: var(--accent-tier, var(--accent));
    text-decoration: none;
    flex-shrink: 0;
    white-space: nowrap;
}

.timeline-entry .timeline-source:hover {
    text-decoration: underline;
}

/* Empty-source graceful degradation — defensive rule. Phase 4 omits
   the <a class="timeline-source"> from markup when source_url is null;
   this rule ensures it stays hidden if the wrapper is reused. */
.timeline-entry:not([data-source]) .timeline-source {
    display: none;
}
```

DO NOT introduce any `font-family` rules — Courier New body is inherited from `style-shared.css` `body { font-family: 'Courier New', monospace; }` per TOKN-04.

DO NOT scope `.maturity-pill` or `.timeline-entry` to a parent `[data-accent]` selector — those component selectors are class-keyed and the `--accent-tier` cascade handles tier-specificity automatically (the pill itself carries `data-accent` and resolves `--accent-tier` on itself; the timeline-entry inherits `--accent-tier` from any ancestor with `data-accent`, and falls back to `--accent` outside one).
  </action>
  <verify>
    <automated>grep -c "^.maturity-pill {" docker/web/site/style-map.css | grep -q "^1$" && grep -c '.maturity-pill .seg' docker/web/site/style-map.css | (read n; [ "$n" -ge 1 ]) && grep -E '\.maturity-pill\[data-stage="1"\] \.seg:nth-child\(-n\+1\)' docker/web/site/style-map.css && grep -E '\.maturity-pill\[data-stage="5"\] \.seg:nth-child\(-n\+5\)' docker/web/site/style-map.css && grep -c "^.timeline-entry {" docker/web/site/style-map.css | grep -q "^1$" && grep -F 'source ↗' docker/web/site/style-map.css && grep -E '\.timeline-entry:not\(\[data-source\]\) \.timeline-source' docker/web/site/style-map.css && grep -F 'var(--accent-tier, var(--accent))' docker/web/site/style-map.css && ! grep -E '^[[:space:]]*font-family:' docker/web/site/style-map.css</automated>
  </verify>
  <acceptance_criteria>
    - `style-map.css` contains `.maturity-pill {` rule
    - `style-map.css` contains five distinct `[data-stage="N"] .seg:nth-child(-n+N)` rules for N=1..5
    - `style-map.css` contains `.timeline-entry {` rule
    - `style-map.css` contains the literal string `source ↗` (U+2197 glyph) in a comment documenting the markup contract
    - `style-map.css` contains the rule `.timeline-entry:not([data-source]) .timeline-source { display: none; }`
    - `style-map.css` contains `var(--accent-tier, var(--accent))` (fallback cascade for `.timeline-source` color)
    - `style-map.css` contains ZERO `font-family:` declarations (TOKN-04 enforcement)
    - File size grew (file remains a single coherent stylesheet with Sections 1-5)
  </acceptance_criteria>
  <done>
    Sections 4 (maturity pill) and 5 (timeline entry) are appended to `style-map.css`. All acceptance assertions pass. The file now contains the complete CSS contract Phase 4 will consume.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caddy → browser | Static CSS shipped to any visitor of aiagentspulse.com (public, no auth) |
| `style-map.css` selectors → existing edition pages | Selector scope must not bleed into `#list-view`/`#reader-view`/`.article-entry` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `style-map.css` selectors | mitigate | All non-`:root` rules are gated either by a class selector (`.maturity-pill`, `.timeline-entry`) that does not exist on edition pages, or by an attribute selector (`[data-accent="..."]`) that Phase 4 introduces only on map-page wrappers. Edition pages never gain `data-accent` (D-06) and never use `.maturity-pill`/`.timeline-entry` classes — selectors are inert there. |
| T-03-02 | Information Disclosure | Token file public | accept | CSS file is shipped to every public visitor; the four pinned hex values + chosen on-dark variants are not secrets. No PII, no credentials. |
| T-03-03 | Denial of Service | Stylesheet size | accept | File is well under 200 lines of CSS; download cost is negligible (gzip via Caddy). |
| T-03-SC | Tampering | No new package installs in this plan | accept | This plan only writes CSS to an existing tree; no npm/pip/cargo installs. RESEARCH.md package legitimacy gate is not applicable. |
</threat_model>

<verification>
- `docker/web/site/style-map.css` exists, syntactically valid CSS (no preprocessor, opens with `/* ... */`)
- All eight base + on-dark hex values present per Task 1 acceptance
- Five `[data-stage="N"]` rules present per Task 2 acceptance
- `.timeline-entry`, `.timeline-source`, `.timeline-why`, `.timeline-line1`, `.timeline-line2` selectors all present
- `:not([data-source]) .timeline-source` rule present
- Zero `font-family:` declarations (TOKN-04 inheritance enforced)
- Zero `data-tier` references (the selector key is `data-accent`)
- File is the only file modified by this plan — `git diff --name-only` shows exactly `docker/web/site/style-map.css`
</verification>

<success_criteria>
- ROADMAP SC#2 (maturity pill — single source of truth): CSS contract for the pill exists in one file, keyed off `data-stage` 1..5
- ROADMAP SC#3 (timeline entry format pinned): CSS contract for the two-line format with the literal `↗` glyph exists
- ROADMAP SC#4 (inheritance — no bespoke typography): zero `font-family` / `max-width` / nav-chrome rules introduced
- TOKN-01: four base + four on-dark CSS custom properties shipped, base hex pinned verbatim
- TOKN-02: `.maturity-pill` + `.seg` + `[data-stage="N"]` contract ready for hub/block/status reuse
- TOKN-03: `.timeline-entry` two-line format with `↗` glyph ready
- TOKN-04: zero typography rules introduced
- D-05 honored: selector is `data-accent`, not `data-tier`, so psychology=coral override flows from `blocks.accent` verbatim
</success_criteria>

<output>
Create `.planning/phases/03-design-tokens/03-01-SUMMARY.md` when done, recording:
- The exact on-dark hex values chosen and their measured WCAG contrast ratios against `#0a0a0f`
- File line count of `style-map.css`
- Confirmation that no `font-family` rules were introduced (TOKN-04 self-check)
</output>
