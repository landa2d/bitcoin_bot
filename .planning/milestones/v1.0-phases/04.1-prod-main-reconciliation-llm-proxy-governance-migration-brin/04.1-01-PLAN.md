---
phase: 04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/034_governance_caps_and_oncap_behavior.sql
  - docker/llm-proxy/proxy.py
  - .planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-CANARY.md
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-09]
must_haves:
  truths:
    - "D-03: a null/absent spending_cap_sats that is NOT uncapped=true causes check_governance to return reject + emit a loud governance_event (never silent allow)"
    - "D-03: an unknown agent (no wallet row) causes check_governance to return reject + emit a loud governance_event (never silent allow)"
    - "D-04: the only legal way to run uncapped is the explicit uncapped=true column on agent_wallets_v2"
    - "D-02: check_governance returns {action: downgrade, new_model: <mapped>} for gato when its cap is exceeded, and both call sites act on it"
    - "D-01: migration 034 UPDATEs existing rows to analyst=28000/daily, processor=1000/daily, research=5000/weekly, newsletter=2000/weekly, gato=50000/daily"
    - "D-01: every prod agent NOT in governance_config.json (rivalscope, lab_data-provider, code_*, lab_*) is set uncapped=TRUE so it does not hard-reject post-cutover"
    - "D-09: proxy.py contains zero references to governance_config.json (no file fallback added)"
    - "migration is re-runnable: ADD COLUMN IF NOT EXISTS + explicit UPDATE, no ON CONFLICT DO NOTHING for the cap backfill"
  artifacts:
    - path: "supabase/migrations/034_governance_caps_and_oncap_behavior.sql"
      provides: "uncapped + on_cap_behavior + downgrade_map columns, governance_events CHECK extension, exact D-01 cap backfill, uncapped sweep"
      contains: "ADD COLUMN IF NOT EXISTS uncapped"
    - path: "docker/llm-proxy/proxy.py"
      provides: "fail-loud three-way cap contract + implemented downgrade action at both call sites"
      contains: "downgrade"
    - path: ".planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-CANARY.md"
      provides: "the canary procedure (reject case + gato downgrade case) that Plan 02 executes against prod"
  key_links:
    - from: "docker/llm-proxy/proxy.py:check_governance"
      to: "agent_wallets_v2.uncapped / on_cap_behavior / downgrade_map"
      via: "_get_wallet select(*) read"
      pattern: "wallet.get\\(.uncapped.\\)"
    - from: "docker/llm-proxy/proxy.py call sites (chat ~764, anthropic ~960)"
      to: "check_governance downgrade action"
      via: "elif gov[action] == downgrade branch"
      pattern: "downgrade"
    - from: "proxy.py _emit_governance_event new event_type"
      to: "governance_events.event_type CHECK constraint"
      via: "migration 034 ALTER ... DROP/ADD CHECK including the new type"
      pattern: "event_type IN"
---

<objective>
Build the governance migration and the fail-loud + downgrade proxy edits as ONE locally-verifiable, repo-side unit. NO production is touched in this plan — it produces `supabase/migrations/034_*.sql`, the edited `docker/llm-proxy/proxy.py`, and a written canary procedure that Plan 02 executes after deploying the unit to prod.

The migration and the proxy edits are coupled and MUST ship together (the proxy reads the new `uncapped`/`on_cap_behavior`/`downgrade_map` columns; if the proxy emits a new governance event_type, the migration MUST extend the closed CHECK constraint or the loud event silently fails to insert — re-creating the exact silent-failure class this phase exists to kill).

Purpose: produce the buildable, syntax-clean artifacts so the atomic prod cutover in Plan 02 has zero in-flight authoring. Implements D-01 (exact caps), D-02 (downgrade built, not just migrated), D-03 (fail-loud spine), D-04 (explicit uncapped opt-in), D-09 (no file fallback).
Output: migration 034 SQL, edited proxy.py (syntax-checked), 04.1-CANARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-CONTEXT.md
@.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-PATTERNS.md
@docker/llm-proxy/governance_config.json
@docker/llm-proxy/proxy.py
@supabase/migrations/023_llm_proxy_tables.sql
@supabase/migrations/033_economy_map_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 034 — schema extension + exact cap backfill + uncapped sweep + CHECK extension</name>
  <files>supabase/migrations/034_governance_caps_and_oncap_behavior.sql</files>
  <read_first>
    - supabase/migrations/023_llm_proxy_tables.sql (the target table agent_wallets_v2 lines 20-34; the governance_events CHECK list lines 69-72 — CLOSED list, MUST be extended if a new event_type is emitted)
    - supabase/migrations/033_economy_map_schema.sql (header/section-banner convention, ADD COLUMN guard idiom, UPDATE precedent)
    - docker/llm-proxy/governance_config.json (canonical D-01 cap values — read verbatim)
    - .planning/phases/04.1-.../04.1-PATTERNS.md (section 2: ADD COLUMN IF NOT EXISTS idiom, per-agent UPDATE idiom, CHECK-constraint coupling warning, the "every prod agent needs cap OR uncapped" sweep)
    - .planning/phases/04.1-.../04.1-CONTEXT.md (D-01..D-04, D-09)
  </read_first>
  <action>
Create `supabase/migrations/034_governance_caps_and_oncap_behavior.sql` following the 033 header/section-banner style (numbered `-- Migration 034: ...` header, bulleted "this migration does" list, `-- SECTION N` banners). The migration does FOUR things, idempotently (re-runnable):

(1) SCHEMA EXTENSION (D-02/D-04). On `agent_wallets_v2`, add three columns using `ADD COLUMN IF NOT EXISTS`:
- `uncapped BOOLEAN NOT NULL DEFAULT FALSE` — the explicit opt-in (D-04).
- `on_cap_behavior TEXT NOT NULL DEFAULT 'reject' CHECK (on_cap_behavior IN ('reject','downgrade'))` — per-agent on-cap behavior (D-02).
- `downgrade_map JSONB NOT NULL DEFAULT '{}'::jsonb` — old_model -> new_model map for the downgrade behavior (D-02).

(2) STRUCTURAL FAIL-LOUD (operator preference: structure over application). Add a table CHECK constraint guarded by an existence check that makes the illegal ambiguous state unrepresentable: `CHECK ((spending_cap_sats IS NOT NULL AND spending_cap_sats > 0) OR uncapped = TRUE)`. Guard it so re-running does not error (query pg_constraint / `DO $$ ... IF NOT EXISTS ... $$` pattern like 033 lines 120-138). IMPORTANT: add this constraint AFTER the cap backfill UPDATEs in this same migration so existing null-cap rows do not violate it mid-migration — order the sections accordingly.

(3) CAP BACKFILL (D-01) — explicit per-agent UPDATEs (rows already exist on prod; analyst is currently 1000 and MUST become 28000). Use the EXACT values: analyst spending_cap_sats=28000 window='daily' on_cap_behavior='reject' uncapped=FALSE; processor=1000 'daily' 'reject' FALSE; research=5000 'weekly' 'reject' FALSE; newsletter=2000 'weekly' 'reject' FALSE; gato=50000 'daily' on_cap_behavior='downgrade' uncapped=FALSE downgrade_map='{"claude-sonnet-4-20250514": "deepseek-chat"}'::jsonb. Do NOT use ON CONFLICT DO NOTHING (would skip the analyst correction).

(4) UNCAPPED SWEEP (D-03/D-04). Every prod agent NOT in the five above (per the CONTEXT audit: rivalscope, lab_data-provider, and all code_* / lab_* with null caps) MUST be made non-ambiguous BEFORE the new proxy goes live or its next call hard-rejects. Set `uncapped = TRUE, on_cap_behavior = 'reject'` for every row where `spending_cap_sats IS NULL AND uncapped = FALSE AND agent_name NOT IN (the five capped agents)`. Use a single guarded UPDATE keyed on the null-cap predicate so it is data-driven (do not enumerate code_*/lab_* by hand — they are operator-created and may grow). rivalscope is deferred for its negative-balance anomaly but still needs uncapped=TRUE here so it does not hard-reject.

(5) CHECK-CONSTRAINT COUPLING (load-bearing for fail-loud). Decide the fail-loud event_type and make it insertable. Recommended: emit a NEW event_type `'cap_missing'` (clearer audit trail than overloading 'cap_hit'). Because `governance_events.event_type` (023:69-72) is a CLOSED CHECK list, ALTER it to add `'cap_missing'`: drop the existing CHECK and re-add it including all six existing values plus 'cap_missing' (and 'model_downgrade' is already present for the downgrade event). Guard the ALTER so re-running is safe. Cross-check: the exact event_type string chosen here MUST match the string Task 2 passes to `_emit_governance_event`. State the chosen string in a SQL comment so Task 2 can cite it.

End with a pointer comment to the phase canary artifact (04.1-CANARY.md) per the 033 VERIFY-pointer convention. Do NOT add RLS or touch any other table.
  </action>
  <verify>
    <automated>test -f supabase/migrations/034_governance_caps_and_oncap_behavior.sql && grep -q "ADD COLUMN IF NOT EXISTS uncapped" supabase/migrations/034_governance_caps_and_oncap_behavior.sql && grep -q "downgrade_map" supabase/migrations/034_governance_caps_and_oncap_behavior.sql && grep -q "28000" supabase/migrations/034_governance_caps_and_oncap_behavior.sql && grep -q "50000" supabase/migrations/034_governance_caps_and_oncap_behavior.sql && grep -q "uncapped = TRUE" supabase/migrations/034_governance_caps_and_oncap_behavior.sql && ! grep -qi "ON CONFLICT.*DO NOTHING" supabase/migrations/034_governance_caps_and_oncap_behavior.sql && echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/034_governance_caps_and_oncap_behavior.sql` exists.
    - Contains `ADD COLUMN IF NOT EXISTS uncapped`, `on_cap_behavior`, `downgrade_map`.
    - Contains exact backfill values: analyst 28000, processor 1000, research 5000, newsletter 2000, gato 50000; gato's downgrade_map maps `claude-sonnet-4-20250514` -> `deepseek-chat`.
    - Contains an `uncapped = TRUE` data-driven sweep keyed on `spending_cap_sats IS NULL`.
    - Contains an ALTER of `governance_events` event_type CHECK that includes the chosen fail-loud type (e.g. `'cap_missing'`) plus the existing six types.
    - Contains a table CHECK making `(cap IS NOT NULL AND >0) OR uncapped` invariant, added AFTER the backfill section.
    - Does NOT contain `ON CONFLICT ... DO NOTHING` for the cap backfill.
    - A SQL comment names the exact event_type string Task 2 must emit.
  </acceptance_criteria>
  <done>Migration 034 SQL exists, is idempotent (ADD COLUMN IF NOT EXISTS + guarded ALTER/constraint + explicit UPDATE), encodes the exact D-01 caps + gato downgrade_map, sweeps all other agents to uncapped=TRUE, and extends the event_type CHECK for the fail-loud event.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Edit proxy.py — fail-loud three-way cap contract (D-03/D-04) + implement the downgrade action (D-02) at both call sites</name>
  <files>docker/llm-proxy/proxy.py</files>
  <read_first>
    - docker/llm-proxy/proxy.py (check_governance lines 323-365; _emit_governance_event 265-280; _get_wallet 249-262; _get_spending_window_total 283-320; chat call site 727-769; anthropic call site 926-965; _wallet_summary_sync cap read 1268-1297; MODEL_ROUTES 51-77; ESTIMATED_COST_SATS 80-85)
    - .planning/phases/04.1-.../04.1-PATTERNS.md (findings #1 and #2 — the line 351 silent no-op, the unimplemented downgrade, the reserve-then-govern ordering wrinkle, the cross-provider downgrade hazard)
    - supabase/migrations/034_governance_caps_and_oncap_behavior.sql (written in Task 1 — read the SQL comment naming the exact fail-loud event_type string)
    - .planning/phases/04.1-.../04.1-CONTEXT.md (D-02, D-03, D-04, D-09)
  </read_first>
  <action>
Edit `docker/llm-proxy/proxy.py` to make governance fail loud and to actually implement the advertised `downgrade` action. Do NOT add any read of `governance_config.json` (D-09 — the file stays as audit trail only; the proxy stays DB-only). Match repo conventions (snake_case, logger name "llm-proxy", `_sb_*` helpers, broad `except Exception`) EXCEPT for the enforcement decision itself, which must hard-reject rather than swallow (D-03 overrides the swallow-and-continue habit).

(A) Replace the unknown-agent silent allow at lines 330-331. `if not wallet:` MUST emit the fail-loud governance event (the exact event_type string from migration 034's comment, e.g. `'cap_missing'`, with details `{"reason": "unknown agent — no wallet row", "model": model}`) and `return {"action": "reject", "reason": "No wallet/governance record for agent <name>"}`. Never `{"action": "allow"}` for an unknown agent.

(B) Replace the silent no-op cap guard at line 351 (`if cap_sats and cap_sats > 0:`) with an explicit three-way contract reading the new columns surfaced by `_get_wallet`'s `select("*")`:
  1. `uncapped = wallet.get("uncapped", False)` is truthy -> skip cap enforcement, allow (no event). This is the only legal uncapped path (D-04).
  2. `cap_sats` is a positive int -> enforce exactly as today (compute window_total via `_get_spending_window_total`, compare, emit `cap_hit`, then branch on behavior — see (C)).
  3. ELSE (cap_sats is None/0/absent AND not uncapped) -> emit the fail-loud event (same event_type as (A), details `{"reason": "missing spending cap and not uncapped", "model": model, "spending_cap_sats": cap_sats}`) and `return {"action": "reject", "reason": "Agent <name> has no spending cap and is not marked uncapped — refusing to serve (fail-loud)"}`. Never fall through to allow.
  Keep the existing `allow_negative` internal-agent alert-but-allow behavior ONLY inside branch 2 (a hit on a real cap) — do NOT let allow_negative re-open the missing-cap hole in branch 3.

(C) Implement the downgrade action (D-02). Inside branch 2, when `window_total >= cap_sats`, read `on_cap_behavior = wallet.get("on_cap_behavior", "reject")` and `downgrade_map = wallet.get("downgrade_map") or {}`. If `on_cap_behavior == "downgrade"` and `downgrade_map.get(model)` resolves to a target model that exists in `MODEL_ROUTES`, emit a `model_downgrade` governance_event (details: from_model, to_model, cap, window_total) and `return {"action": "downgrade", "new_model": <target>, "reason": ...}`. Otherwise reject as today. (gato is the only agent with on_cap_behavior='downgrade' per migration 034.)

(D) Honor `downgrade` at BOTH call sites — chat (~760-769) and anthropic (~957-965). Add an `elif gov["action"] == "downgrade":` branch. The branch MUST, BEFORE building the upstream request, rebind the locals: `model = gov["new_model"]; route = MODEL_ROUTES.get(model); body["model"] = model`. Then re-run the allowed-models check and provider-key resolution against the NEW model (re-use the existing checks at ~735-748 / ~934-946). Handle the reserve/cost ordering wrinkle: governance runs AFTER `reserve_balance` (line 753 / 950), so the reservation was made at the OLD model's `estimated_sats`. The branch must re-estimate at the new model (`new_estimated = ESTIMATED_COST_SATS.get(new_model, default)`) and reconcile the reservation — settle/refund the old estimate and reserve the new one, OR settle the difference — so the wallet is not over- or under-charged. Pick the simplest correct reconciliation and add a short code comment explaining it.

(E) CROSS-PROVIDER/ENDPOINT HAZARD (load-bearing — flag and handle). gato's downgrade is `claude-sonnet-4-20250514` (anthropic provider, `/v1/messages` path, anthropic-style request body) -> `deepseek-chat` (deepseek provider, `/chat/completions` path, OpenAI-style body). A downgrade detected inside the ANTHROPIC handler CANNOT simply rebind `route` and continue the anthropic upstream code path (it would POST an anthropic body to a deepseek `/v1/messages` URL). Decide and implement one of: (i) when a downgrade in the anthropic handler crosses to a non-anthropic provider, translate the request to the target provider's chat-completions shape and dispatch down the chat path; OR (ii) if a clean translation is out of scope, the anthropic handler returns a typed 429 governance response instructing the caller to retry against the downgraded model on the chat endpoint, and emit the model_downgrade event so it is auditable. Whichever is chosen, the canary (Plan 02) must observe a real downgrade-or-redirect for gato, NOT a silent sonnet pass-through and NOT a plain reject. Document the choice in a code comment and in the SUMMARY. Do NOT leave the anthropic-path downgrade as a no-op.

(F) Reporting consistency. In `_wallet_summary_sync` (lines 1268-1274), update the `if cap_sats and cap_sats > 0:` block so a null cap reports as either "uncapped (explicit)" when `wallet.get("uncapped")` is true or "MISCONFIGURED" when not, instead of silently leaving `utilization = None` with no signal. This is reporting only (not an enforcement path) but should not hide the misconfigured state.

(G) Update the `check_governance` docstring so it no longer lies — it now genuinely returns allow/reject/downgrade.

After editing, syntax-check: `python3 -c "import ast; ast.parse(open('docker/llm-proxy/proxy.py').read())"`.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/llm-proxy/proxy.py').read())" && grep -q 'gov\["action"\] == "downgrade"\|gov\[.action.\] == .downgrade.' docker/llm-proxy/proxy.py && grep -q 'uncapped' docker/llm-proxy/proxy.py && grep -q 'on_cap_behavior' docker/llm-proxy/proxy.py && ! grep -qi "governance_config" docker/llm-proxy/proxy.py && ! grep -A1 'if not wallet' docker/llm-proxy/proxy.py | grep -q '"action": "allow"' && echo PROXY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('docker/llm-proxy/proxy.py').read())"` exits 0 (no syntax error).
    - proxy.py: `check_governance` returns `{"action": "reject", ...}` (NOT allow) when `spending_cap_sats` is null AND `uncapped` is not true, and emits the fail-loud governance event whose event_type string matches migration 034's comment.
    - proxy.py: `check_governance` returns reject + emits the fail-loud event for an unknown agent (no wallet) — the line 330-331 `return {"action": "allow"}` is gone.
    - proxy.py: `check_governance` returns `{"action": "downgrade", "new_model": "deepseek-chat", ...}` for gato when its 50000/daily cap is exceeded and on_cap_behavior='downgrade'.
    - proxy.py: BOTH call sites (chat ~764 and anthropic ~960) have a `downgrade` branch that rebinds `model`/`route`/`body["model"]`, re-checks allowed-models + provider key, and reconciles the reservation for the new model's estimated cost.
    - proxy.py: the cross-provider downgrade in the anthropic handler is explicitly handled (translate-and-dispatch OR typed redirect + model_downgrade event) — NOT a silent sonnet pass-through.
    - `grep -i governance_config docker/llm-proxy/proxy.py` returns nothing (D-09: no file fallback).
    - `_wallet_summary_sync` distinguishes uncapped vs misconfigured instead of silently None.
  </acceptance_criteria>
  <done>proxy.py fails loud on missing-cap and unknown-agent (reject + loud event), implements the three-way cap contract gated on the new uncapped column, implements and honors the downgrade action at both call sites including the cross-provider gato case, reads only the DB (no governance_config.json), and passes the AST syntax check.</done>
</task>

<task type="auto">
  <name>Task 3: Write the canary procedure (04.1-CANARY.md) that Plan 02 runs against prod</name>
  <files>.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-CANARY.md</files>
  <read_first>
    - docker/llm-proxy/proxy.py (the two enforcement endpoints + the /wallet/{agent}/summary endpoint; the api-key scheme ap_<agent>_<hash>; check_rate_limit; the 429 governance_error response shape)
    - supabase/migrations/034_governance_caps_and_oncap_behavior.sql (Task 1 — the exact caps + the uncapped sweep + the fail-loud event_type)
    - docker/docker-compose.yml (llm-proxy service block ~291, container agentpulse-llm-proxy, port 8200)
    - .planning/phases/04.1-.../04.1-CONTEXT.md (D-05 canary gate, D-08 ordering)
  </read_first>
  <action>
Author `04.1-CANARY.md` — the deterministic, operator-runnable verification gate that Plan 02 executes AFTER applying migration 034 to prod and AFTER deploying the new proxy.py, BEFORE any other service deploys (D-05/D-08). Write it as a numbered checklist with exact commands and exact expected outputs. It MUST cover all four governance behaviors this phase ships. No fenced code blocks are required inside the action, but the canary FILE itself should contain the literal commands. Cover:

(1) DB STATE PROOF (post-migration, pre/post proxy deploy). Exact SQL (run via Supabase MCP read or psql) asserting: analyst spending_cap_sats=28000; processor=1000; research=5000; newsletter=2000; gato=50000 with on_cap_behavior='downgrade' and downgrade_map containing the sonnet->deepseek mapping; AND a query asserting `SELECT count(*) FROM agent_wallets_v2 WHERE spending_cap_sats IS NULL AND uncapped = FALSE` returns 0 (no agent left in the illegal ambiguous state — the D-03 invariant).

(2) FAIL-LOUD CANARY (D-03). Describe how to drive a capped agent OVER its window and confirm a 429 governance_error reject, AND how to confirm the loud governance_event row was inserted (SELECT from governance_events for the chosen event_type within the last few minutes). Also: a deliberate negative test — temporarily reference a non-existent/unknown agent key and confirm reject (not allow). Note the WALLET_CACHE_TTL=15s and GOV_EVENT_COOLDOWN=300s timing so the operator does not misread a cached/deduped result.

(3) DOWNGRADE CANARY (D-02). Describe how to drive gato over its 50000/daily cap with a sonnet request and confirm the response is a DOWNGRADE to deepseek (per the mechanism chosen in Plan 01 Task 2 — either the response served by deepseek, or the typed redirect + a model_downgrade governance_event), NOT a reject and NOT a silent sonnet pass-through. Include the SQL to confirm the model_downgrade governance_event row.

(4) ZERO-BEHAVIOR-CHANGE CHECK. A normal under-cap call for each of the five agents returns 200 and is NOT rejected/downgraded (the migration tightened nothing for already-correct agents and the uncapped sweep did not break the uncapped agents).

(5) PASS/FAIL GATE + ROLLBACK NOTE. State the binary gate: ALL of (1)-(4) must pass before Plan 02 proceeds to staged deploy. Include the rollback note: if the canary fails, the prod proxy.py can be rolled back to the prior image (the migration is additive/idempotent and safe to leave; the uncapped sweep is the only data change that could be reverted by re-nulling, but document that the prior file-based proxy is what would resume serving on rollback).

This file is documentation/procedure only — it mutates nothing. Plan 02 executes it.
  </action>
  <verify>
    <automated>test -f .planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-CANARY.md && grep -q "28000" .planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-CANARY.md && grep -qi "downgrade" .planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-CANARY.md && grep -qi "uncapped = FALSE\|uncapped=false" .planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-CANARY.md && echo CANARY_OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists and is a numbered, command-bearing checklist (not prose).
    - Contains the DB-state SQL asserting the five exact caps + gato downgrade_map + the zero-ambiguous-rows invariant query.
    - Contains a reject canary (capped agent over window -> 429 governance_error + governance_event row) and an unknown-agent reject negative test.
    - Contains a downgrade canary for gato (sonnet over cap -> downgrade/redirect to deepseek + model_downgrade event), explicitly NOT a reject and NOT a silent pass-through.
    - Contains a zero-behavior-change under-cap 200 check for all five agents.
    - States the binary PASS/FAIL gate and a rollback note.
    - Notes WALLET_CACHE_TTL=15s / GOV_EVENT_COOLDOWN=300s timing caveats.
  </acceptance_criteria>
  <done>04.1-CANARY.md is a runnable, deterministic gate covering DB-state proof, fail-loud reject, gato downgrade, zero-behavior-change, and a pass/fail+rollback decision — ready for Plan 02 to execute against prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → llm-proxy:8200 | Every agent's LLM call crosses here carrying an `ap_<agent>_<hash>` key; this is the single governance enforcement point. |
| llm-proxy → agent_wallets_v2 (Supabase) | Governance config (caps, uncapped, on_cap_behavior, downgrade_map) is read from the DB with a 15s cache; the migration writes it. |
| migration author → live agent_wallets_v2 | Migration 034 mutates per-agent caps; a partial/incorrect backfill is a governance integrity threat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-041-01 | Elevation of Privilege | check_governance null-cap fall-through (proxy.py:351) | mitigate | D-03 three-way contract: null/absent cap AND not uncapped → reject + loud `cap_missing` governance_event. The line-351 silent no-op is removed (Task 2 B). |
| T-041-02 | Elevation of Privilege | check_governance unknown-agent branch (proxy.py:330) | mitigate | D-03: unknown agent (no wallet) → reject + loud event, never `{"action":"allow"}` (Task 2 A). |
| T-041-03 | Spoofing/Bypass | uncapped opt-in misuse | mitigate | uncapped is an explicit boolean column (D-04); the structural CHECK `(cap>0) OR uncapped` makes the ambiguous state unrepresentable (Task 1 §2); canary asserts zero ambiguous rows. |
| T-041-04 | Repudiation | fail-loud event silently dropped by closed CHECK | mitigate | governance_events.event_type CHECK is extended to include the new fail-loud type (Task 1 §5) so `_emit_governance_event` insert does not get swallowed — the loud event actually persists. |
| T-041-05 | Tampering | migration 034 partial/incorrect backfill (analyst stays 1000, others null) | mitigate | explicit per-agent UPDATEs with verbatim D-01 values + data-driven uncapped sweep (Task 1 §3/§4); canary DB-state proof asserts all five caps + zero null-and-not-uncapped rows (Task 3 §1). |
| T-041-06 | Denial of Service | downgrade across provider/endpoint mishandled → gato calls fail or pass uncapped | mitigate | cross-provider downgrade hazard explicitly handled (Task 2 E): translate-and-dispatch or typed redirect + model_downgrade event; canary observes a real downgrade for gato (Task 3 §3), never a silent sonnet pass-through. |
| T-041-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — proxy.py edits use existing imports (supabase, httpx, json); migration is raw SQL. No legitimacy audit needed. |
</threat_model>

<verification>
- `supabase/migrations/034_governance_caps_and_oncap_behavior.sql` exists, is idempotent, encodes exact D-01 caps + gato downgrade_map, the uncapped sweep, and the event_type CHECK extension.
- `python3 -c "import ast; ast.parse(open('docker/llm-proxy/proxy.py').read())"` passes.
- `grep -i governance_config docker/llm-proxy/proxy.py` is empty (D-09).
- proxy.py check_governance has the three-way contract (uncapped / cap / reject-loud) and a downgrade return; both call sites have a downgrade branch.
- `04.1-CANARY.md` exists and covers reject, downgrade, DB-state proof, zero-behavior-change, pass/fail gate.
- NO production mutation occurred in this plan (no migration applied, no deploy).
</verification>

<success_criteria>
- Migration 034 SQL, edited proxy.py, and 04.1-CANARY.md all exist and are internally consistent (the fail-loud event_type string matches between SQL and proxy; the canary asserts the exact caps the migration writes).
- The buildable unit is syntax-clean and ready for the atomic prod cutover in Plan 02.
- D-01, D-02, D-03, D-04, D-09 are each represented in the artifacts produced here.
</success_criteria>

<output>
Create `.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-01-SUMMARY.md` when done. The SUMMARY MUST record: the exact fail-loud event_type string chosen, the reservation-reconciliation approach for downgrade, and the cross-provider downgrade decision (translate-and-dispatch vs typed redirect) — Plan 02's canary depends on these.
</output>
