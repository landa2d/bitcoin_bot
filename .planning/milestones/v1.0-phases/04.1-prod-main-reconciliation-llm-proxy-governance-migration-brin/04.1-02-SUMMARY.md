# 04.1-02 Summary — Governance Atomic Cutover (PROD)

**Status:** Complete
**Verdict:** Canary PASS (accepted) — governance unit committed to prod.
**Date:** 2026-05-28

## What shipped (live prod, project zxzaaqfowtqvmsbitqpu)

D-08 order, all three blocking steps:

1. **Migration 034 applied** to live Supabase. Verified: analyst=28000/daily, processor=1000/daily, research=5000/weekly, newsletter=2000/weekly, gato=50000/daily(downgrade, sonnet→deepseek); `ambiguous_rows=0`; 23 agents swept `uncapped=TRUE`; structural `(cap>0) OR uncapped` CHECK; event_type CHECK = 8 values.
2. **llm-proxy redeployed** (scoped `docker compose up -d --build llm-proxy` on the prod host — equivalent to `--force-rebuild llm-proxy`, never `all`). Container healthy; `/v1/proxy/health` → 200; running the fail-loud + downgrade code (0 `governance_config` refs).
3. **Canary** executed/recorded in `04.1-02-CANARY-RESULTS.md`.

## Deviation (caught at apply gate)

Migration 034 SECTION 5 originally re-added only the migration-023 six event types + `cap_missing`, dropping the live `system_audit` value (added by `add_audit_event_type` 2026-04-27, 2 rows in use). Would have failed the `ALTER` outright. Fixed in commit `064c4f2` to preserve `system_audit` (now 8 values). Pure prod-drift reconciliation — exactly this phase's purpose.

## Material finding (operator-decided)

All five capped agents have `allow_negative=true`, so their caps are **soft** (over-cap → `cap_hit` event but the call is allowed; pre-existing behavior, preserved). Operator accepted soft caps as intended. The **fail-loud on missing/unknown caps** path (D-03) hard-rejects unconditionally and is verified (`ambiguous_rows=0` + code). The §2.1 hard-reject and §3 gato-downgrade canary cases were not driven live (no agent over cap; forcing would mean real spend + halting real agent work).

Follow-up (not a phase blocker): to harden caps, set `agent_wallets_v2.allow_negative=false` for the capped agents (balance tolerance is driven separately by `access_tier=="internal"`).

## Decisions recorded for Plan 03

- Canary PASS → Plan 03 (staged per-service deploy) is authorized.
- On-box deploy mechanism: this host is prod (compose project at `/root/bitcoin_bot/docker`); deploys are local scoped container rebuilds, not `deploy.sh` remote rsync.

## Self-Check: PASS

Migration applied + verified live; proxy healthy on new code; D-08 order preserved; canary verdict recorded with honest scope.
