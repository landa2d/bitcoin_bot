# 04.1-03 Summary — Staged Per-Service Deploy + Deploy-Baseline Guardrail

**Status:** Complete
**Date:** 2026-05-28

## What shipped (prod, local scoped rebuilds on this host)

Staged, one service at a time, governed-cycle verified, halt-on-failure:
- **gato_brain** → clean (proxy clients init as `gato`).
- **analyst** → governed poll cycle verified (settled `llm_call` via proxy).
- **research** → governed cycle verified **after** repairing a pre-existing drift bug: `claim_research_task` had `search_path=""` (migration 035), which had silently stalled its queue since April.
- **gato** → rebuilt + verified by a **real Telegram round-trip** (operator-sent message → gato → gato_brain → governed proxy calls as `gato` → reply; 4 settled `gato` txns; 0 cap_missing). Not just container health.
- newsletter/processor were already current (no deploy). lab-data-provider excluded (D-07).

`cap_missing = 0` across every rebuild — the new fail-loud proxy never misfired on a real agent.

## Deploy-baseline guardrail (standing drift check — close condition 2)

Built `scripts/drift-check.sh` + migration 036 `gsd_drift_audit()` (read-only, SECURITY DEFINER, correctly hardened — a model of the search_path fix). Detects three drift classes: code (image vs commit), RPC (empty search_path), unapplied migrations. Run before every deploy.

## Major finding (the class)

The RPC check surfaced the **full silent-RPC search_path class: 8 public functions** with empty search_path (not the 2 we tripped over). Several are load-bearing (`record_agent_spend`, `search_corpus`, `topup_agent_wallet`) and may be silently broken in prod now. Recorded in the P3 audit todo with a per-function worklist. Remediation is follow-up, not this phase; detection now exists.

## Reconcile + closeout

- CODE: prod == main for all in-scope services (only D-07-deferred lab-data-provider remains).
- Source drift todo (`reconcile-prod-main-drift-and-close-the-gap`) resolved → moved to completed/, pointer to `04.1-03-DEPLOY-LOG.md`. Carried-forward deferred items noted (lab-data-provider deploy, rivalscope negative balance, RPC search_path class).
- Topology corrected: prod is `/root/bitcoin_bot/docker` on this host; `/opt/bitcoin_bot` is a stale Mar-15 checkout; deploy.sh's remote-rsync model is obsolete.

## Self-Check: PASS

All four in-scope services on current main + governed-verified; standing drift guardrail built and proven; source todo resolved; outstanding items tracked as todos (not silent).
