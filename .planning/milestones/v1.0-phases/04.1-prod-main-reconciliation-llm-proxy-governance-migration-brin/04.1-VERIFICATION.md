---
status: passed
phase: 04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin
verified: 2026-05-28
method: live-prod evidence (DB assertions, settled transactions, governance_events, container/log trace) — operator-driven interactive cutover
---

# Phase 04.1 Verification — Prod↔Main Reconciliation + LLM-Proxy Governance Migration

**Goal:** Production matches `main` with no untracked drift, and per-agent spending governance is enforced from `agent_wallets_v2` — shipped as ONE atomic schema/data-then-code unit, with `proxy.py` failing loudly rather than silently running ungoverned if caps are absent.

**Verdict: PASSED** (with explicitly tracked follow-ups — see below).

## Goal-backward evidence

| must-have (D-XX) | Verified how | Result |
|---|---|---|
| D-01 exact caps in `agent_wallets_v2` | live SELECT post-034 | analyst 28000, processor 1000, research 5000/wk, newsletter 2000/wk, gato 50000 ✓ |
| D-02 gato downgrade preserved | live downgrade_map + governed Telegram round-trip | sonnet→deepseek map present; gato served 200 ✓ |
| D-03 fail-loud on missing/unknown cap | code (proxy.py:343/411 unconditional reject) + `ambiguous_rows=0` invariant | enforced; 0 cap_missing across all deploys ✓ |
| D-04 explicit `uncapped` opt-in | migration 034 column + structural CHECK | present ✓ |
| D-05 canary gate | 04.1-02-CANARY-RESULTS.md | PASS (accepted: soft caps via pre-existing allow_negative, operator-accepted) ✓ |
| D-06 staged scoped deploy | per-service rebuilds, never `--force-rebuild all` | gato_brain/analyst/research/gato one at a time, health+governed-cycle gated ✓ |
| D-07 lab-data-provider excluded | not deployed | ✓ (deferred) |
| D-08 atomic order | migration → proxy → canary, enforced by sequence | preserved ✓ |
| D-09 no governance_config.json fallback | `grep` proxy.py = 0 refs | ✓ |
| prod == main (code) | `scripts/drift-check.sh` post-gato | all in-scope services current; only D-07 lab-data-provider remains ✓ |
| deploy-baseline guardrail | `scripts/drift-check.sh` + migration 036 | built + proven (caught gato pre-deploy; surfaced RPC class) ✓ |

## Live verification highlights

- Migration 034 applied to prod (zxzaaqfowtqvmsbitqpu); pre-apply catch preserved the live `system_audit` event type the migration would have dropped (fixed before apply).
- llm-proxy redeployed (fail-loud + downgrade code); `/v1/proxy/health` 200.
- Backend services governed-cycle verified (settled `llm_call` transactions via proxy); research unblocked via migration 035 (claim_research_task search_path).
- gato verified by a **real operator Telegram interaction** → full governed round-trip → 4 settled `gato` transactions → reply.

## Tracked follow-ups (NOT phase requirements — discovered, recorded as todos)

1. **RPC `search_path` class — 8 functions** with empty search_path (`record_agent_spend`, `search_corpus`, `topup_agent_wallet`, `next_newsletter_edition`, `increment_problem_frequency`, `get_scrape_stats`, `compute_opportunity_score`, `transfer_between_agents`). Some load-bearing and may be silently broken in prod now. Detected by the new standing check; remediation is the P3 audit todo. **`drift-check.sh` exits non-zero by design until this is fixed.**
2. pay-500 (`transfer_between_agents`) — blocker-on-activation for agent payments.
3. analyst `predictions.title` expire bug; research trigger file perms; optional soft-cap hardening.
4. lab-data-provider deploy (D-07) + rivalscope negative balance — deferred decisions.

**Topology note:** prod runs from `/root/bitcoin_bot/docker` on this host (not `hetzner:/opt/bitcoin_bot`).
