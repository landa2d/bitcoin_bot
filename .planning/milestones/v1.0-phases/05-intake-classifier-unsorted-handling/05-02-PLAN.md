---
phase: 05-intake-classifier-unsorted-handling
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docker/processor/agentpulse_processor.py
autonomous: true
requirements: [INTK-01, INTK-03, INTK-04]
must_haves:
  truths:
    - "When a newsletter edition reaches status 'published', one timeline candidate is emitted per tier-1 event"
    - "An event classified with tag_confidence >= the config floor writes to the named block's timeline; below the floor it writes to 'unsorted'"
    - "A classifier error / DeepSeek circuit-break still emits an 'unsorted' entry with tag_confidence = NULL (never silently dropped)"
    - "Every emitted entry carries source_edition_id = the originating newsletter row's id"
    - "An edition whose entries were already emitted is skipped on re-run (idempotent)"
    - "The classifier poller is registered on the processor schedule"
  artifacts:
    - path: "docker/processor/agentpulse_processor.py"
      provides: "classify_intake_for_edition()/classify_intake_poller(), scheduled_classify_intake() wrapper, and the schedule.every(...).do(...) registration"
      contains: "scheduled_classify_intake"
  key_links:
    - from: "docker/processor/agentpulse_processor.py intake poller"
      to: "public.newsletters status == 'published'"
      via: "supabase.table('newsletters').select(...).eq('status','published')"
      pattern: "eq\\(['\"]status['\"],\\s*['\"]published['\"]\\)"
    - from: "docker/processor/agentpulse_processor.py intake poller"
      to: "economy_map.timeline_entries INSERT"
      via: "economy_map_insert_timeline_entry helper (Plan 01)"
      pattern: "economy_map_insert_timeline_entry"
    - from: "docker/processor/agentpulse_processor.py intake poller"
      to: "classify_intake_event (proxy-routed, Plan 01)"
      via: "per-event classify call"
      pattern: "classify_intake_event"
---

<objective>
Wire the autonomous intake path end-to-end inside the processor monolith: read finalized (status `'published'`) newsletter editions, pull each edition's tier-1 events, classify each via the proxy-routed `classify_intake_event` (Plan 01), route by the config confidence floor (Plan 01), and INSERT one `timeline_entries` row per event via the `economy_map` PostgREST helper (Plan 01) — with idempotency, source traceability, and the fail-loud "never drop an event" guarantee. Register it on the processor's existing `schedule`.

Purpose: Delivers INTK-01 (emit per finalized tier-1 event), INTK-03 (>=0.6 → named block / below → 'unsorted'), and INTK-04 (source_edition_id traceback). Honors the project spine: intake is autonomous; silence is the enemy — a flagged 'unsorted' entry always beats a missing one (D-05).

Output: A poller function, its scheduled wrapper, and one `schedule.every(...).do(...)` registration line — all in `docker/processor/agentpulse_processor.py`. No schema change (Phase 2 migration 033 is locked). No new LLM dispatcher or PostgREST helper (built in Plan 01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-intake-classifier-unsorted-handling/05-CONTEXT.md
@.planning/phases/05-intake-classifier-unsorted-handling/05-PATTERNS.md
@.planning/phases/05-intake-classifier-unsorted-handling/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the intake classifier poller (read editions → tier-1 events → classify → route → INSERT)</name>
  <read_first>
    - .planning/phases/05-intake-classifier-unsorted-handling/05-01-SUMMARY.md (the exact signatures of classify_intake_event, economy_map_insert_timeline_entry, economy_map_edition_already_emitted, and where the config floor is read)
    - docker/processor/agentpulse_processor.py lines 6969-7133 (surface_x_content_candidates — the canonical poller shape: guard `if not supabase`, read source rows, loop, per-item validity gate + insert, per-block try/except continue), 2838-2946 (extract_problems_multisource — log_pipeline_start/log_pipeline_end + log_llm_call wrapping), 884-886 (dedup existence-check convention), 4370-4374 (publish_newsletter sets status='published' + published_at), 3961-3969 (premium_source_posts event shape inside data_snapshot), 4331 (the unpublished status filter showing the lifecycle), 540-560 + 462-470 (config read idiom)
    - docker/processor/agentpulse_processor.py lines 477-503 (DeepSeek circuit breaker — _is_deepseek_available / _record_deepseek_failure; do NOT call directly, but understand failure semantics for D-05)
    - supabase/migrations/033_economy_map_schema.sql lines 148-160 (timeline_entries columns + NOT NULL constraints: block_slug, event_date, what_shifted, why_it_mattered are NOT NULL; source_url, source_edition_id, tag_confidence nullable)
  </read_first>
  <action>
    Add a poller function (e.g. `classify_intake_for_edition(edition: dict)` plus an orchestrator `classify_intake_poller()`) to `docker/processor/agentpulse_processor.py`, mirroring the `surface_x_content_candidates()` structure. Wrap the run with `log_pipeline_start(...)`/`log_pipeline_end(...)`. Steps:

    1. Guard: return early if `not supabase` or if `get_full_config().get("intake_classifier", {}).get("enabled", True)` is False.
    2. Read finalized editions: `supabase.table('newsletters').select(...).eq('status', 'published')` (D-02 — `'published'` is the ONLY finalized value; exclude draft/pending/preview/held). Select at minimum `id`, `published_at`, `created_at`, `data_snapshot`. Order newest-first and cap the batch (e.g. recent N editions) so a backlog can't blow the run.
    3. Fetch the allowed slug set: GET the seven slugs live from `economy_map.blocks` via an `Accept-Profile: economy_map` PostgREST read (the seven seeded slugs: identity-trust, memory-context, payments-settlement, autonomy-control, governance-accountability, psychology-disposition, regulation-legal). If the live fetch fails, fall back to the hard-coded seven-slug list. `'unsorted'` is always an allowed write target but is NOT in the classifier's offered label set.
    4. Per edition: idempotency skip (D-08) — call `economy_map_edition_already_emitted(str(edition['id']))`; if True, skip the whole edition.
    5. Derive `event_date` from the edition (D-04 caveat): use the `published_at` date (fall back to `created_at`) formatted as an ISO `YYYY-MM-DD` string — `premium_source_posts` events carry NO date and `event_date` is `DATE NOT NULL`.
    6. Extract tier-1 events (D-03): from `edition['data_snapshot']['premium_source_posts']`, keep entries where `tier == 1`. (Block-pipeline `max_source_tier` objects are NOT on published rows — do NOT use them.)
    7. Per tier-1 event, build the four prose fields (D-04 field mapping): `what_shifted` ← `title`, `why_it_mattered` ← `summary`, `source_url` ← `url`, `event_date` ← the derived edition date. Skip an event only if `title`/`summary` are empty (those columns are NOT NULL); otherwise it MUST produce a row.
    8. Classify + route (INTK-03, D-05, D-07): call `classify_intake_event(event, allowed_slugs)` inside try/except. On success, read `block_slug` + `tag_confidence`; set `target_slug = block_slug` IF `tag_confidence >= floor` AND `block_slug` is in the seven-slug allow-list, ELSE `target_slug = 'unsorted'`. Record `tag_confidence` even when below floor (flagged-not-dropped). On ANY exception (including circuit-break-driven failure), set `target_slug = 'unsorted'` and `tag_confidence = None` (NULL) — never skip the event (D-05). Do NOT persist the below-floor best-guess slug (D-07 — the schema has no column for it; only `tag_confidence` is recorded).
    9. INSERT (INTK-04): build the entry dict with `block_slug=target_slug`, `event_date`, `what_shifted`, `why_it_mattered`, `source_url`, `source_edition_id=str(edition['id'])`, `tag_confidence`, and call `economy_map_insert_timeline_entry(entry)`. Wrap each event's classify+insert in try/except so one bad event doesn't abort the edition — but a classify failure routes to 'unsorted' (step 8), it does not skip.
    10. Emit `log_llm_call("processor", "intake_classify", ...)` per classification for cost tracking, and `logger.info` progress lines under the `agentpulse` logger.

    Do NOT add a schema migration, a new LLM dispatcher, or a new PostgREST helper — reuse Plan 01's. Do NOT call `routed_llm_call` for classification (Plan 01's proxy path is mandatory for criterion 2).
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def classify_intake_for_edition\|def classify_intake_poller" docker/processor/agentpulse_processor.py` shows the poller function(s)
    - The poller reads `public.newsletters` with `.eq('status', 'published')` and does NOT read `'held'`/`'draft'`/`'pending'`/`'preview'` editions: `grep -n "eq('status', 'published')\|eq(\"status\", \"published\")" docker/processor/agentpulse_processor.py` matches inside the poller
    - The poller calls `economy_map_edition_already_emitted` before emitting (idempotency) and `economy_map_insert_timeline_entry` to write
    - The poller calls `classify_intake_event` and does NOT call `routed_llm_call` for classification (grep the poller body: `routed_llm_call` absent)
    - Tier-1 filter uses `premium_source_posts` and `tier == 1` (NOT `max_source_tier`): `grep -n "premium_source_posts" docker/processor/agentpulse_processor.py` matches in the poller; `max_source_tier` does NOT appear in the poller body
    - The INSERT dict sets `source_edition_id` to the stringified edition `id`
    - The error/below-floor paths set `block_slug = 'unsorted'` (NULL `tag_confidence` on error; recorded `tag_confidence` below floor): `grep -c "'unsorted'" docker/processor/agentpulse_processor.py` >= 1 inside the poller
    - `event_date` is derived from `published_at`/`created_at` (not from the event)
    - `python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"` exits 0
  </acceptance_criteria>
  <done>The poller reads only `published` editions, emits one row per tier-1 event with the D-04 field mapping + derived event_date, routes by the config floor with 'unsorted' for below-floor and NULL-confidence 'unsorted' on error, sets source_edition_id, and is idempotent per edition — all via Plan 01's proxy classifier + PostgREST helper.</done>
</task>

<task type="auto">
  <name>Task 2: Register the poller on the processor schedule</name>
  <read_first>
    - docker/processor/agentpulse_processor.py lines 9748-9811 (setup_scheduler — the schedule.every(...).do(...) registration block; the newsletter/publish lines ~9774-9777), 9353-9362 (scheduled_surface_x_candidates — the thin try/except wrapper convention the .do() points at)
    - .planning/phases/05-intake-classifier-unsorted-handling/05-01-SUMMARY.md (confirm the poller function name from Task 1)
  </read_first>
  <action>
    Add a thin wrapper `scheduled_classify_intake()` mirroring `scheduled_surface_x_candidates()` (lines 9353-9362): call the poller from Task 1 inside try/except, `logger.info` the result, `logger.error` on failure. Then add ONE `schedule.every(<interval>).do(scheduled_classify_intake)` line inside `setup_scheduler()`, placed near the newsletter/publish registration lines (~9774-9777) so it reads as part of the newsletter→intake flow. Interval is Claude's discretion (D — poller cadence); a 30-minute interval is a reasonable default given editions publish weekly and the idempotency skip makes frequent polling cheap. Do NOT register it inline in the newsletter service — it runs in the processor (D-01).
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def scheduled_classify_intake" docker/processor/agentpulse_processor.py` exists and the wrapper body has a try/except with logger.info on success + logger.error on failure
    - `grep -n "schedule.every(.*).do(scheduled_classify_intake)" docker/processor/agentpulse_processor.py` shows exactly one registration line, inside `setup_scheduler()`
    - The wrapper calls the Task 1 poller function
    - `python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"` exits 0
  </acceptance_criteria>
  <done>`scheduled_classify_intake()` exists as a thin try/except wrapper and is registered exactly once on the processor `schedule` inside `setup_scheduler()`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public.newsletters.data_snapshot → poller | event `title`/`summary`/`url` originate from scraped third-party sources; untrusted content the poller reads and forwards to the classifier and into the DB |
| poller (service_role) → economy_map.timeline_entries | service_role bypasses RLS by design; the write target slug must be constrained |
| poller → llm-proxy | classifier traffic governed/audited at the proxy (Plan 01 helper) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering / Elevation | classifier-returned block_slug written to DB | mitigate | Before INSERT, `target_slug` is accepted ONLY if `block_slug` is in the seven-slug allow-list AND `tag_confidence >= floor`; anything else (including an injected/unknown slug) routes to 'unsorted'. A prompt-injected slug cannot widen the write target beyond the seven seeded blocks or 'unsorted' |
| T-05-06 | Denial of Service (data loss) | event dropped on classifier failure | mitigate | D-05 fail-loud: any classify exception (incl. DeepSeek circuit break) routes the event to 'unsorted' with `tag_confidence = NULL` — the event is recorded, never silently lost; per-event try/except continues the loop without aborting the edition |
| T-05-07 | Tampering | source ingestion from abandoned drafts | mitigate | Only `status == 'published'` editions are read (D-02); held/draft/pending/preview editions never enter the timeline, so an unapproved draft cannot inject map entries |
| T-05-08 | Repudiation / Integrity | duplicate emission on re-run | mitigate | Pre-emit existence check on `source_edition_id` (D-08) skips an already-emitted edition; combined with the append-only trigger this keeps the ledger free of duplicate map rows |
| T-05-09 | Elevation of Privilege | service_role write surface | mitigate | INSERT goes only through Plan 01's `timeline_entries`-scoped helper; no UPDATE/DELETE helper exists so the append-only invariant cannot be bypassed by this poller |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"` passes.
- Poller reads only `status == 'published'`; uses `premium_source_posts` + `tier == 1`; never `max_source_tier`.
- Below-floor and error paths both write `'unsorted'` (error path with NULL confidence); `source_edition_id` set on every entry; idempotency check precedes emission.
- Exactly one `schedule.every(...).do(scheduled_classify_intake)` registration exists.
- Runtime (operator, post-deploy): rebuild `processor`; a newly published edition produces `timeline_entries` rows within the poll interval, each carrying `source_edition_id` joinable back to the newsletter row, and the proxy shows a `wallet_transactions`/log entry per classification (criterion 2 evidence from Plan 01's proxy path).
</verification>

<success_criteria>
- INTK-01: one timeline candidate emitted per tier-1 event of a `published` edition.
- INTK-03: `tag_confidence >= floor` → named block; below floor → `'unsorted'` (with confidence recorded); classifier error → `'unsorted'` with NULL confidence (D-05).
- INTK-04: every entry carries `source_edition_id` = the newsletter row id.
- D-08 idempotency: a re-polled edition is skipped.
- The poller is registered on the processor `schedule` (D-01).
</success_criteria>

<output>
Create `.planning/phases/05-intake-classifier-unsorted-handling/05-02-SUMMARY.md` when done.
</output>
