---
phase: 08-validation-sentinels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/processor/agentpulse_processor.py
  - tests/test_07_synthesis.py
autonomous: true
requirements: [VLDT-01, VLDT-02, VLDT-03, VLDT-04, VLDT-05]
must_haves:
  truths:
    - "A synthesized draft lands with a populated validator_report jsonb (tension_preserved, length_below_floor, structure_missing, maturity_jump, requires_attention)"
    - "A body missing skeleton headings now produces a draft (no raise) with structure_missing populated and requires_attention=true"
    - "tension_preserved=false when the live-tension section is absent, below char floor, equals the placeholder, or verbatim-echoes blocks.live_tension"
    - "length_below_floor=true when len(new)/len(prior_published) < 0.60; N/A (no flag) on cold-start"
    - "maturity_jump = absolute ordinal distance on nascent→emerging→contested→consolidating→mature; >1 sets requires_attention"
    - "A sentinel compute error logs loudly AND records a sentinel_errors note in validator_report — the draft still lands (never silent, never blocked)"
  artifacts:
    - path: "docker/processor/agentpulse_processor.py"
      provides: "run_sentinels helper + SYNTH_MATURITY_ORDER ordered sequence + synthesize_block wiring + insert payload extension; WR-02 raise removed"
      contains: "def run_sentinels"
    - path: "tests/test_07_synthesis.py"
      provides: "VLDT-04 sentinel tests replacing the WR-02 skeleton-raise test; insert-shape test updated for validator_report"
      contains: "run_sentinels"
  key_links:
    - from: "synthesize_block"
      to: "run_sentinels"
      via: "call between parse_synthesis_output and the INSERT"
      pattern: "run_sentinels\\("
    - from: "run_sentinels output"
      to: "economy_map_insert_block_body_version payload"
      via: "validator_report key passed at INSERT time"
      pattern: "validator_report"
---

<objective>
Compute the four deterministic validation sentinels (VLDT-01 tension preserved, VLDT-02 length floor, VLDT-03 maturity jump, VLDT-04 structure intact) plus a `requires_attention` rollup at synthesis time, write them atomically into the new draft's `validator_report` jsonb, and convert the Phase 7 WR-02 skeleton-missing hard gate into an annotating sentinel.

Purpose: Sentinels make editorial risk on a synthesized draft VISIBLE without ever blocking the draft from landing (VLDT-05 — silence is the enemy). Because `validator_report` is append-only (migration 033 trigger raises on any post-insert change), the report MUST be built before the INSERT.
Output: A new `run_sentinels(...)` pure-compute helper, an ordered maturity sequence, the WR-02 raise removed from `parse_synthesis_output`, `synthesize_block` wiring the sentinels between parse and INSERT, an extended insert payload, and the swapped/added tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-validation-sentinels/08-CONTEXT.md
@.planning/phases/08-validation-sentinels/08-PATTERNS.md

# Phase 7 code this plan modifies (read the cited line ranges, do not re-read whole file)
@docker/processor/agentpulse_processor.py
@tests/test_07_synthesis.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add run_sentinels helper + ordered maturity sequence, remove WR-02 raise</name>
  <files>docker/processor/agentpulse_processor.py</files>
  <read_first>
    - docker/processor/agentpulse_processor.py lines 3037-3048 (SYNTH_MATURITY_ENUM is a `set`; SYNTH_SKELETON_HEADINGS ordered list — live tension is index 2)
    - docker/processor/agentpulse_processor.py lines 3329-3354 (parse_synthesis_output — the WR-02 raise to remove is lines 3348-3353; keep empty-body/invalid-maturity/missing-maturity raises)
    - docker/processor/agentpulse_processor.py lines 3078-3080 (fail-loud log idiom: logger.error(..., exc_info=True))
    - docker/processor/agentpulse_processor.py lines 3152-3166 (fetch_current_block_body returns None on cold-start)
    - PATTERNS.md lines 26-92 (run_sentinels shape, inputs, fail-loud-but-never-block, requires_attention rollup) and lines 96-112 (exact raise block to remove)
  </read_first>
  <behavior>
    - Define SYNTH_MATURITY_ORDER as a module-level list ["nascent","emerging","contested","consolidating","mature"] (D-07; SYNTH_MATURITY_ENUM stays a set for membership checks — do NOT call .index() on a set). Place it next to SYNTH_MATURITY_ENUM (~line 3038).
    - Remove the WR-02 missing-headings raise from parse_synthesis_output (delete lines 3348-3353) and strip the "OR on a body_md missing any of the six required RNDR-02 skeleton sections" clause from its docstring (D-01). Empty-body, invalid-maturity, missing-maturity raises stay.
    - Add run_sentinels(body_md, prior_body_md, block, proposed_maturity) -> dict returning {tension_preserved: bool, length_below_floor: bool, structure_missing: [str], maturity_jump: int, requires_attention: bool}. Docstring cites D-03/D-04/D-05/D-06/D-07 and VLDT-01..05 (Phase 7 primitive docstring style, lines 3329-3338).
    - VLDT-04 structure: structure_missing = [h for h in SYNTH_SKELETON_HEADINGS if h.lower() not in body_md.lower()] (lift the idiom from old line 3351).
    - VLDT-03 maturity_jump: abs(SYNTH_MATURITY_ORDER.index(proposed_maturity) - SYNTH_MATURITY_ORDER.index(block["maturity"])).
    - VLDT-02 length: cold-start (prior_body_md is None or empty) -> length_below_floor=False (N/A, do not divide); else length_below_floor = len(body_md)/len(prior_body_md) < 0.60.
    - VLDT-01 tension_preserved: scan body_md for the "## The live tension" section (SYNTH_SKELETON_HEADINGS[2]) up to the next "##"; tension_preserved=False when that section is absent, OR its stripped body is below a small char floor, OR it equals the placeholder 'TBD — set via /map-tension', OR it is a verbatim echo of (block.get("live_tension") or "").strip(). Otherwise True.
    - requires_attention = (not tension_preserved) or length_below_floor or (maturity_jump > 1) or bool(structure_missing) (D-04).
    - VLDT-05 fail-loud-but-never-block: a sentinel compute error must NOT raise — wrap each sentinel (or the helper body) in try/except, logger.error(..., exc_info=True), record a "sentinel_errors" key in the report describing what failed, and leave the draft path intact (requires_attention should reflect the safe default so a failed check is visibly surfaced, not silently passed).
    - GATE-01: run_sentinels is pure compute — it issues NO PostgREST verb and touches no published row / blocks.maturity / current_body_version_id.
  </behavior>
  <action>Add SYNTH_MATURITY_ORDER (ordered list) per D-07 — keep SYNTH_MATURITY_ENUM as the set for membership. Remove the WR-02 raise block (processor lines 3348-3353) and its docstring clause per D-01, keeping the other three raises. Implement run_sentinels(body_md, prior_body_md, block, proposed_maturity) returning the D-03 report shape with the D-04 rollup, mirroring the Phase 7 primitive style (top-level def, decision-ID-citing docstring, returns a plain dict, no class). Implement VLDT-01..04 exactly per D-05/D-06/D-07 and the watch-outs (real placeholder 'TBD — set via /map-tension'; verbatim-echo of block live_tension; cold-start N/A for length; ordinal distance via the new ordered list). Wrap compute in try/except per VLDT-05: log loudly with exc_info=True and record a sentinel_errors note so the draft still lands with visible evidence — never raise, never silent-pass. Place run_sentinels near the other Phase 7 primitives (after parse_synthesis_output).</action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"</automated>
  </verify>
  <done>parse_synthesis_output no longer raises on missing headings (still raises on empty body / invalid / missing maturity); SYNTH_MATURITY_ORDER exists as an ordered list; run_sentinels returns the D-03 report with the D-04 rollup, implements VLDT-01..04, and never raises on compute error (records sentinel_errors instead). Python syntax check passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire sentinels into synthesize_block and extend the insert payload</name>
  <files>docker/processor/agentpulse_processor.py</files>
  <read_first>
    - docker/processor/agentpulse_processor.py lines 3425-3493 (synthesize_block; prior_body_md already in scope at line 3470; parse at 3473; INSERT at 3477-3482)
    - docker/processor/agentpulse_processor.py lines 3169-3196 (economy_map_insert_block_body_version — forwards row as json=row; docstring enumerates payload keys at 3174)
    - PATTERNS.md lines 116-176 (caller call-site extension, docstring update, GATE-01 invariant)
  </read_first>
  <behavior>
    - Inside synthesize_block, between parse_synthesis_output (line 3473) and the INSERT (line 3477), call run_sentinels(parsed["body_md"], prior_body_md, block, parsed["proposed_maturity"]) reusing the prior_body_md already fetched at line 3470 (do NOT add a second fetch_current_block_body call).
    - Add "validator_report": validator_report to the economy_map_insert_block_body_version payload dict so it is written atomically at INSERT (D-02 — append-only trigger forbids post-insert annotation).
    - Update economy_map_insert_block_body_version's docstring (line 3174) to add validator_report to its enumerated payload keys. No change to the function body (it already forwards json=row).
    - GATE-01: still writes ONLY the draft's payload; no new PostgREST verb; nothing touches blocks.maturity / current_body_version_id / a published row.
  </behavior>
  <action>In synthesize_block, insert the run_sentinels call between the parse and the INSERT, reusing prior_body_md from line 3470. Add the validator_report key to the insert payload dict (lines 3477-3482). Update the economy_map_insert_block_body_version docstring to list validator_report among its payload keys (per D-02; the function body forwards json=row so no body change). Preserve the existing BlockSynthesisError try/except wrapping and the GATE-01 draft-only invariant.</action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"</automated>
  </verify>
  <done>synthesize_block computes validator_report via run_sentinels before the INSERT and passes it in the payload; the insert docstring lists validator_report; no second prior-body fetch; GATE-01 preserved. Syntax check passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Swap WR-02 test for VLDT-04 sentinel tests; update insert-shape assertion</name>
  <files>tests/test_07_synthesis.py</files>
  <read_first>
    - tests/test_07_synthesis.py lines 309-359 (parse-test family; the test to DELETE is test_parse_output_raises_on_missing_skeleton at 323-332; keep empty/invalid/missing-maturity at 335-359)
    - tests/test_07_synthesis.py lines 365-390 (test_insert_block_body_version_shape — line 388 asserts the EXACT key set; add validator_report)
    - tests/test_07_synthesis.py lines 52, 63 (proc import; _FULL_SKELETON_BODY fixture)
    - PATTERNS.md lines 215-250 (D-01a swap + new VLDT-04 sentinel test pattern)
  </read_first>
  <behavior>
    - DELETE test_parse_output_raises_on_missing_skeleton (lines 323-332) — it asserts the opposite of D-01 behavior.
    - ADD a test asserting parse_synthesis_output no longer raises on a partial-skeleton body (body lands) (D-01).
    - ADD a VLDT-04 sentinel test: run_sentinels on a missing-heading body (prior_body=None, a block dict with maturity + live_tension) returns a populated structure_missing list and requires_attention is True (D-04).
    - ADD focused sentinel coverage: VLDT-01 tension_preserved=False for the placeholder/echo/absent-section cases; VLDT-02 length_below_floor=True at <0.60 and N/A (False) on cold-start (prior=None); VLDT-03 maturity_jump >1 sets requires_attention. (Use plain assert-style tests mirroring the parse family.)
    - UPDATE test_insert_block_body_version_shape: the exact-key-set assertion (line 388) must now include validator_report, and the payload row fixture must carry a validator_report dict. Keep the forbidden-columns and Content-Profile assertions intact.
    - Keep test_parse_output_raises_on_empty_body / _invalid_maturity / _missing_maturity unchanged (D-01a explicit).
  </behavior>
  <action>Replace the WR-02 raise test with (a) a no-raise-on-partial-skeleton test and (b) VLDT-04 + focused VLDT-01/02/03 run_sentinels tests per the PATTERNS.md pattern (lines 235-250). Finalize the validator_report key names to match Task 1's run_sentinels output. Update test_insert_block_body_version_shape so its exact-key-set assertion includes validator_report and its row fixture supplies one. Leave the three other parse-raise tests untouched.</action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_07_synthesis.py -q</automated>
  </verify>
  <done>The WR-02 skeleton-raise test is gone; new tests assert partial-skeleton bodies land and run_sentinels flags structure/tension/length/maturity correctly with the D-04 rollup; the insert-shape test accepts validator_report; full test_07_synthesis.py suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sonnet output → parse → sentinels | Model-generated body_md is untrusted text fed to deterministic string scans |
| processor → Supabase PostgREST (INSERT) | validator_report dict crosses into the DB at draft INSERT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | run_sentinels reading untrusted body_md | mitigate | Pure-compute string scans only; no eval, no shell, no SQL string-building; validator_report passed as a Python dict serialized by httpx (no interpolation) |
| T-08-02 | Denial of Service | a sentinel compute error aborting the draft | mitigate | VLDT-05 try/except: log loud + record sentinel_errors, never raise — draft always lands |
| T-08-03 | Elevation of Privilege | sentinel path mutating published state | accept→mitigate | GATE-01: run_sentinels issues no PostgREST verb; only the existing draft INSERT payload gains a key; no touch to blocks.maturity / current_body_version_id / published rows |
| T-08-04 | Information Disclosure | sentinel_errors leaking internals into validator_report | accept | Operator-only Telegram surface; error note is a short diagnostic string, no secrets |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies — all sentinel logic uses the stdlib + existing imports |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"` passes (mandatory pre-rebuild syntax check).
- `python3 -m pytest tests/test_07_synthesis.py -q` passes (parse-raise behavior changed, sentinels added, insert shape updated).
- grep confirms `run_sentinels(` is called inside synthesize_block before the INSERT and `validator_report` appears in the insert payload.
- No new PostgREST verb introduced (GATE-01); SYNTH_MATURITY_ENUM remains a set, SYNTH_MATURITY_ORDER is the new ordered list.
</verification>

<success_criteria>
- VLDT-01..05 satisfied: every synthesized draft lands with a populated validator_report; structure/tension/length/maturity flags compute per D-05/D-06/D-07; requires_attention rolls up per D-04; a sentinel error never blocks the draft (records sentinel_errors).
- The Phase 7 WR-02 hard gate is removed (D-01); the swapped test asserts the new annotate-not-block behavior (D-01a).
- The full test_07_synthesis.py suite is green.
</success_criteria>

<output>
Create `.planning/phases/08-validation-sentinels/08-01-SUMMARY.md` when done.
</output>
