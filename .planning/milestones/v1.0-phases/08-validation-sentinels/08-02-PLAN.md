---
phase: 08-validation-sentinels
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/gato_brain/gato_brain.py
autonomous: true
requirements: [VLDT-06]
must_haves:
  truths:
    - "/map-pending fetches each draft's validator_report (get_draft_versions select includes validator_report)"
    - "A draft with requires_attention=true renders a ⚠ REQUIRES ATTENTION headline followed by indented per-flag detail lines, serious flags first"
    - "Per-flag detail is concrete: maturity nascent→contested (+2), length 48% of prior (floor 60%), tension trivialized, missing headings: <list>"
    - "A clean draft (requires_attention falsy) renders a quiet ✓ clean line"
    - "Rendering is read-only by construction — no new write verb is introduced"
  artifacts:
    - path: "docker/gato_brain/gato_brain.py"
      provides: "get_draft_versions select extended to validator_report; handle_map_pending flag rendering per D-08"
      contains: "REQUIRES ATTENTION"
  key_links:
    - from: "get_draft_versions"
      to: "block_body_versions.validator_report"
      via: "select string includes validator_report"
      pattern: "validator_report"
    - from: "handle_map_pending"
      to: "draft validator_report"
      via: "per-draft read + flag formatting"
      pattern: "requires_attention"
---

<objective>
Surface the Phase 8 sentinel flags loudly on `/map-pending` (VLDT-06): each draft with `requires_attention=true` gets a `⚠ REQUIRES ATTENTION` headline plus an indented, concrete, serious-first per-flag detail list; a clean draft shows a quiet `✓ clean`. The rendering reads `validator_report` from each draft — which requires extending the upstream `get_draft_versions` select first.

Purpose: A flagged draft must be the VISIBLE outcome (silence is the enemy). The operator sees, on the existing approval inbox, exactly which editorial-integrity sentinels fired and why.
Output: An extended `get_draft_versions` select and a `handle_map_pending` per-draft flag renderer matching the pinned D-08 mockup. Read-only by construction stays intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-validation-sentinels/08-CONTEXT.md
@.planning/phases/08-validation-sentinels/08-PATTERNS.md

# Phase 6 renderers this plan modifies (read the cited line ranges)
@docker/gato_brain/gato_brain.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend get_draft_versions to select validator_report</name>
  <files>docker/gato_brain/gato_brain.py</files>
  <read_first>
    - docker/gato_brain/gato_brain.py lines 1655-1674 (get_draft_versions — select is "id,block_slug" at line 1665; fail-loud non-2xx guard 1669-1672)
    - docker/gato_brain/gato_brain.py lines 1582-1604 (_economy_map_get — returns httpx.Response with Accept-Profile: economy_map, GET-only by construction)
    - PATTERNS.md lines 207-211 (the upstream select extension — only change to the reader)
  </read_first>
  <action>Change the get_draft_versions select string from "id,block_slug" to "id,block_slug,validator_report" (gato_brain line 1665) so handle_map_pending can read each draft's flags. Leave the fail-loud non-2xx guard and Accept-Profile: economy_map read path untouched. This is the only reader change; no new verb, no Content-Profile (read-only by construction stays intact per D-09 / VLDT-06).</action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())" && grep -A6 'def get_draft_versions' docker/gato_brain/gato_brain.py | grep -q 'validator_report'</automated>
  </verify>
  <done>get_draft_versions selects validator_report; the GET-only read path and fail-loud guard are unchanged; syntax check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Render sentinel flags per draft in handle_map_pending (D-08)</name>
  <files>docker/gato_brain/gato_brain.py</files>
  <read_first>
    - docker/gato_brain/gato_brain.py lines 1773-1830 (handle_map_pending — per-draft rendering to extend is lines 1799-1803; line-list + "\n".join idiom)
    - docker/gato_brain/gato_brain.py lines 1548-1554 (MATURITY_STAGE map: nascent=1..mature=5 — gives ordinal positions for the maturity direction+delta display)
    - docker/gato_brain/gato_brain.py lines 1570-1579 (maturity_pill helper, available if a pill is wanted)
    - docker/gato_brain/gato_brain.py lines 1730-1770 (handle_map_status — sibling renderer pattern)
    - CONTEXT.md <specifics> (pinned mockup wording); PATTERNS.md lines 179-205 (D-08 extension detail)
  </read_first>
  <action>In handle_map_pending, for each draft v read report = v.get("validator_report") or {}. When report.get("requires_attention") is truthy, append a "⚠ REQUIRES ATTENTION" headline marker after the version line, then an INDENTED per-flag detail list ordered SERIOUS FIRST with concrete detail, matching the pinned mockup wording: structure ("missing headings: <list>" when structure_missing is non-empty), tension ("tension trivialized" when not tension_preserved), maturity (use MATURITY_STAGE ordinal positions to render direction+delta, e.g. "maturity jump nascent→contested (+2)"; emit only when maturity_jump > 1), length ("length 48% of prior (floor 60%)" computed from the flag's ratio when length_below_floor is true). Also surface any sentinel_errors note from the report (a check could not run) as an indented line so the failure is visible, not silent (VLDT-05). When requires_attention is falsy and there is no sentinel_errors, append a quiet "✓ clean" line. Document the chosen serious-first ordering in a comment citing D-08. Keep the existing version + /map-approve forward-contract line, the UNSORTED section, and the CHAR_BUDGET truncation net intact. No new write verb — render-only.</action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())" && grep -q 'REQUIRES ATTENTION' docker/gato_brain/gato_brain.py</automated>
  </verify>
  <done>handle_map_pending renders the ⚠ REQUIRES ATTENTION headline + serious-first indented per-flag detail for flagged drafts (concrete maturity/length/tension/structure wording per D-08), surfaces sentinel_errors, and shows ✓ clean for unflagged drafts; the version + /map-approve line, UNSORTED section, and CHAR_BUDGET net are preserved; no new write verb; syntax check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram operator → /map-pending | Operator-triggered read-only render; no input crosses into a write |
| gato_brain → Supabase PostgREST (GET) | validator_report read back from the draft row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-R1 | Elevation of Privilege | /map-pending render path acquiring a write verb | mitigate | Only the get_draft_versions select string changes + render formatting; httpx.get only, no Content-Profile, no new verb (read-only by construction, D-09) |
| T-08-R2 | Information Disclosure | sentinel_errors / flag text leaking internals | accept | Operator-only Telegram surface; flag text is short diagnostic copy, no secrets |
| T-08-R3 | Denial of Service | a large/malformed validator_report bloating the message | mitigate | Existing CHAR_BUDGET truncation net + downstream 4000-char split remain in force |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies — render uses stdlib + existing imports only |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())"` passes (mandatory pre-rebuild syntax check).
- grep confirms get_draft_versions selects validator_report and handle_map_pending emits "REQUIRES ATTENTION".
- Manual sanity: a draft row whose validator_report has requires_attention=true renders the ⚠ headline + concrete per-flag lines; a clean draft renders ✓ clean. (Verified against a crafted validator_report once Plan 01 is deployed; no DB write occurs.)
- No new PostgREST write verb / Content-Profile introduced (read-only by construction, VLDT-06 / D-09).
</verification>

<success_criteria>
- VLDT-06 satisfied: /map-pending surfaces every raised sentinel flag loudly with concrete per-flag detail (serious first) and a quiet ✓ clean for unflagged drafts, per the pinned D-08 mockup.
- The change is render-only and read-only by construction; the approval inbox's existing structure (version line, /map-approve forward contract, UNSORTED section, truncation net) is preserved.
</success_criteria>

<output>
Create `.planning/phases/08-validation-sentinels/08-02-SUMMARY.md` when done.
</output>
