---
phase: 09-gated-publishing-approval-commands
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - docker/gato_brain/gato_brain.py
  - tests/test_09_gated_publishing.py
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04, CMD-03, CMD-04]
must_haves:
  truths:
    - "Only the verified owner (access_tier == 'owner') can run /map-approve or /map-reject; a non-owner gets a typed refusal and the RPC is never called (D-02, SC5)"
    - "/map-status and /map-pending remain ungated and unchanged (D-02a)"
    - "/map-approve <uuid> calls economy_map.publish_block_version via PostgREST POST /rpc and returns a confirmation showing maturity old→new and the live block URL (CMD-03, GATE-02, SC2)"
    - "/map-reject <uuid> calls economy_map.reject_block_version via POST /rpc and returns a confirmation stating the draft's timeline entries return to the next synthesis (CMD-04, GATE-03)"
    - "A malformed or missing version_id arg returns a usage-hint typed error before any RPC call (D-04a, D-05 case b)"
    - "An 'already actioned / not in draft status' RPC raise surfaces as a distinct 'already published/rejected or doesn't exist' message (D-05 case c), never a double-publish (GATE-04 single-winner)"
    - "Synthesis only ever writes status='draft' (GATE-01) — asserted by an explicit verification test against the synthesis writer"
  artifacts:
    - path: "docker/gato_brain/gato_brain.py"
      provides: "owner-gated /map-approve + /map-reject write branches, RPC-POST helper, tier-threaded dispatch"
      contains: "/map-approve"
    - path: "tests/test_09_gated_publishing.py"
      provides: "owner-gate, UUID-validation, typed-error, and GATE-01 draft-only verification tests"
      contains: "def test_"
  key_links:
    - from: "docker/gato_brain/gato_brain.py handle_map_command"
      to: "economy_map.publish_block_version / reject_block_version"
      via: "RPC-POST helper to /rest/v1/rpc/<fn> with Content-Profile: economy_map"
      pattern: "rpc/(publish|reject)_block_version"
    - from: "docker/gato_brain/gato_brain.py /chat dispatch (~2264)"
      to: "handle_map_command"
      via: "access_tier threaded into the map dispatch"
      pattern: "handle_map_command\\(req\\.message,\\s*access_tier"
    - from: "docker/gato_brain/gato_brain.py write branches"
      to: "owner gate"
      via: "access_tier == 'owner' check in front of both write branches"
      pattern: "access_tier\\s*==\\s*['\"]owner['\"]"
---

<objective>
Add the two owner-gated write commands that close the autonomy boundary. Extend
`handle_map_command` (gato_brain ~1902) with `/map-approve <uuid>` and `/map-reject <uuid>`
branches, each gated on `access_tier == 'owner'` (D-02, reusing the existing owner identity),
each calling the Phase 2 atomic RPCs (`economy_map.publish_block_version` /
`reject_block_version`) through a NEW PostgREST RPC-POST helper (D-04). Thread the
`access_tier` (already computed at ~2126) into the dispatch at ~2264. Provide rich
confirmations and per-case typed errors (D-05). Add an explicit test that verifies GATE-01
(synthesis writes only `draft`), plus tests for the owner gate, UUID validation, and the
"already actioned" path.

This is the FIRST and ONLY write surface added to gato_brain's otherwise read-only `/map-*`
commands — exactly two gated RPC verbs, nothing more (D-04).

Purpose: Make `/map-approve` / `/map-reject` real, owner-only, fail-loud, and idempotent —
the human approval gate the whole project is designed around.
Output: modified `docker/gato_brain/gato_brain.py`; new `tests/test_09_gated_publishing.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-gated-publishing-approval-commands/09-CONTEXT.md
@.planning/phases/09-gated-publishing-approval-commands/09-PATTERNS.md
@.planning/phases/09-gated-publishing-approval-commands/09-01-SUMMARY.md
@docker/gato_brain/gato_brain.py
@tests/test_07_synthesis.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RPC-POST helper + owner-gated /map-approve and /map-reject branches + tier-threaded dispatch</name>
  <files>docker/gato_brain/gato_brain.py</files>
  <read_first>
    - docker/gato_brain/gato_brain.py `_economy_map_get` (1582-1604) — the GET helper to mirror: `apikey` + `Authorization: Bearer SUPABASE_KEY` (service_role), `Accept-Profile: economy_map`, timeout=10, fail-loud non-2xx. The new helper copies headers/timeout/fail-loud but uses `Content-Profile: economy_map` and the `/rpc/<fn>` endpoint.
    - docker/processor/agentpulse_processor.py `economy_map_insert_block_body_version` (3174-3203) — the WRITE POST style: `Content-Profile: economy_map`, `Content-Type: application/json`, fail-loud `if resp.status_code not in (...): raise RuntimeError(f"... failed ({resp.status_code}): {resp.text}")`. The RPC helper drops `Prefer: return=representation` (RPCs RETURN void → 200/204 empty body).
    - docker/gato_brain/gato_brain.py `handle_map_command` (1902-1923) — the dispatcher with `/map-status`, `/map-pending`, the "Unknown map command" fallthrough, and the top-level fail-loud `except Exception: return f"Command failed: {e}"` (D-05 case d) to layer typed cases in front of.
    - docker/gato_brain/gato_brain.py `get_blocks` (1635-1652) — the GET-by-slug pattern for resolving block name + current maturity for the confirmation message; and `get_draft_versions` (1655-1685) for reading a draft's `block_slug` / `proposed_maturity`.
    - docker/gato_brain/gato_brain.py `/chat` body (2124-2126) — `user = ensure_user(...)` then `access_tier = user.get("access_tier") or "free"`; and the `/map-` dispatch site (2263-2266) `if _msg_lower.startswith("/map-"): map_response = handle_map_command(req.message)`.
    - supabase/migrations/033_economy_map_schema.sql §9/§10 (256-345) — the RPC arg name `p_version_id`, RETURNS void, and the typed RAISE text `version % not found or not in draft status` (the string the helper must surface for D-05 case c).
    - docker/web/site/app.js line 411 — block page hash route `#/map/<slug>`; the live URL is `https://aiagentspulse.com/#/map/<slug>` (for the approve confirmation URL).
  </read_first>
  <behavior>
    - Owner gate: a non-owner (access_tier != 'owner') calling /map-approve or /map-reject gets a refusal string and NO RPC POST is issued (D-02, D-05 case a).
    - UUID validation: /map-approve or /map-reject with a missing arg, or an arg that is not a well-formed UUID, returns a usage-hint string and NO RPC POST is issued (D-04a, D-05 case b). Validate with uuid.UUID(arg) (or equivalent strict parse) before building the POST body.
    - Approve success: helper POSTs to /rest/v1/rpc/publish_block_version with body {"p_version_id": "<uuid>"} (parameterized JSON, never f-string-interpolated into the path/body); on 2xx returns a confirmation showing block name, maturity old→new, and the live URL https://aiagentspulse.com/#/map/<slug>, noting "renders within ~60s" (D-05 approve, D-03).
    - Reject success: helper POSTs to /rest/v1/rpc/reject_block_version with {"p_version_id": "<uuid>"}; on 2xx returns a confirmation stating the draft is superseded and its timeline entries return to the next synthesis (D-05 reject, GATE-03).
    - Already-actioned: when the RPC responds with the typed RAISE "version ... not found or not in draft status" (concurrent double-approve loser / wrong id / already published or rejected), the handler returns the distinct message "that draft was already published/rejected or doesn't exist" (D-05 case c) — never a double-publish (GATE-04 single-winner).
    - Any other failure: falls through to the existing fail-loud `Command failed: <e>` (D-05 case d).
    - /map-status and /map-pending behavior is unchanged and ungated (D-02a).
  </behavior>
  <action>
    Add a NEW RPC-POST helper (name/signature planner's discretion, e.g. `_economy_map_rpc`)
    near `_economy_map_get`: it `httpx.post`s to `f"{SUPABASE_URL}/rest/v1/rpc/{fn}"` with headers
    `apikey` + `Authorization: Bearer SUPABASE_KEY` + `Content-Type: application/json` +
    `Content-Profile: economy_map` (the WRITE profile — NOT Accept-Profile), `json={"p_version_id": version_id}`,
    timeout=10. RPCs RETURN void so accept 200/204; on any non-2xx raise
    `RuntimeError(f"economy_map rpc {fn} failed ({resp.status_code}): {resp.text}")` so the
    typed RAISE text is preserved in the exception for D-05 case-(c) matching. NEVER use
    supabase-py `.rpc()`/`.schema()`/`.in_()`. Do not interpolate `version_id` into the URL path.

    Extend `handle_map_command` to accept `access_tier` (change the signature to
    `handle_map_command(message: str, access_tier: str = "free")`) and add two branches:
    `cmd == "/map-approve"` → call a publish handler; `cmd == "/map-reject"` → call a reject
    handler (separate handlers or one parametrized helper — planner's call). In each write
    branch FIRST enforce the owner gate: `if access_tier != "owner": return <refusal>` (D-05
    case a), BEFORE any arg parsing or RPC call. Then parse `parts[1]` as the version_id arg;
    if missing or `uuid.UUID(arg)` raises ValueError, return the usage hint (D-05 case b). Keep
    `/map-status` and `/map-pending` ungated and untouched (D-02a). Wrap the RPC call so that an
    exception whose message contains `not found or not in draft status` returns the D-05 case-(c)
    string; let any other exception propagate to the existing top-level `except Exception` →
    `Command failed: <e>` (D-05 case d).

    For the approve confirmation, before/after the RPC: read the draft row (block_slug +
    proposed_maturity) and the current block row (name/slug + maturity) via `_economy_map_get`
    so the message can show maturity `<old>→<new>` and the URL `https://aiagentspulse.com/#/map/<slug>`.
    For reject, no extra lookup is required beyond the block_slug for naming. Resolve the block
    "name" from the existing blocks GET (slug-based; exact label wording is Claude's Discretion).

    Thread the tier into the dispatch: change the call site at ~2264 from
    `handle_map_command(req.message)` to `handle_map_command(req.message, access_tier)` (the
    `access_tier` local already exists at ~2126). Update the section comment so it no longer
    claims the surface is read-only (it now has exactly two gated write verbs — D-04).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())" && grep -q "/map-approve" docker/gato_brain/gato_brain.py && grep -q "/map-reject" docker/gato_brain/gato_brain.py && grep -Eq "handle_map_command\(req\.message,\s*access_tier" docker/gato_brain/gato_brain.py && grep -Eq "access_tier\s*==\s*[\"']owner[\"']|access_tier\s*!=\s*[\"']owner[\"']" docker/gato_brain/gato_brain.py && grep -Eq "rpc/(publish|reject)_block_version|rpc/\{" docker/gato_brain/gato_brain.py && grep -q "Content-Profile" docker/gato_brain/gato_brain.py && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())"` succeeds (syntax-clean before rebuild).
    - A new RPC-POST helper POSTs to `/rest/v1/rpc/<fn>` with `Content-Profile: economy_map` and body `{"p_version_id": <uuid>}`; raises RuntimeError with `resp.text` on non-2xx; uses no supabase-py `.rpc()`/`.schema()`.
    - `handle_map_command` signature accepts `access_tier`; both write branches return a refusal when `access_tier != "owner"` BEFORE any RPC call.
    - The dispatch at ~2264 passes `access_tier` into `handle_map_command`.
    - `/map-status` and `/map-pending` branches are byte-for-byte unchanged and remain ungated.
    - `version_id` is validated as a well-formed UUID before the RPC body is built; it is never interpolated into the request URL path.
    - The typed RAISE text `not found or not in draft status` maps to a distinct D-05 case-(c) message.
  </acceptance_criteria>
  <done>Two owner-gated write commands call the Phase 2 RPCs via a parameterized RPC-POST helper, with UUID validation, rich confirmations, and all five D-05 cases covered; read commands unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test suite — owner gate, UUID validation, typed errors, and GATE-01 draft-only verification</name>
  <files>tests/test_09_gated_publishing.py</files>
  <read_first>
    - tests/test_07_synthesis.py (lines 1-80, and `test_insert_block_body_version_shape` ~466-480) — the standalone test harness shape (module stubbing of `schedule`/`tweepy`/`resend`, `_FakeResponse`, monkeypatched POST stub) and the existing assertion that `economy_map_insert_block_body_version` OMITS `status` so the DB default `'draft'` applies — the GATE-01 source to assert against.
    - docker/gato_brain/gato_brain.py — the just-modified `handle_map_command`, the RPC-POST helper, and the owner gate, to test directly (stub `httpx.post`/`_economy_map_get` so no live network).
    - docker/processor/agentpulse_processor.py `economy_map_insert_block_body_version` (3174-3203) — the synthesis draft writer (GATE-01): it must POST without a `status` key and never target `blocks`/`published`.
    - tests/conftest.py — the cross-service import workaround (sys.path / module loading) used so gato_brain and processor import in the bare test env.
  </read_first>
  <behavior>
    - GATE-01 test: assert the synthesis writer (`economy_map_insert_block_body_version`) builds a POST body that OMITS `status` (so DB default 'draft' applies) and targets `block_body_versions` only — never `blocks` or a `status: 'published'`/`status: 'superseded'` write. Mirror test_07's `test_insert_block_body_version_shape` (capture the POSTed json via a stub).
    - Owner gate test: `handle_map_command("/map-approve <valid-uuid>", access_tier="free")` returns a refusal and the RPC-POST stub is NEVER called; same for `/map-reject`. With `access_tier="owner"` the RPC stub IS called.
    - UUID validation test: `/map-approve` with no arg, and with a non-UUID arg (e.g. "notauuid"), each returns a usage hint and the RPC stub is NEVER called (owner tier, to isolate validation from gating).
    - Already-actioned test: stub the RPC helper to raise an exception containing "not found or not in draft status"; assert the returned string is the distinct D-05 case-(c) "already published/rejected or doesn't exist" message, not a crash and not a success confirmation.
    - Approve confirmation test: stub the RPC (success) + the blocks/draft GET so the returned string contains the maturity transition and the `https://aiagentspulse.com/#/map/<slug>` URL.
  </behavior>
  <action>
    Create `tests/test_09_gated_publishing.py` as a standalone harness mirroring
    `tests/test_07_synthesis.py` (stub `schedule`/`tweepy`/`resend`, define a `_FakeResponse`,
    monkeypatch `httpx.post` and `_economy_map_get` so no network call occurs). Write `def test_*`
    functions covering: (1) GATE-01 — the synthesis writer omits `status` and never writes
    `blocks`/`published` (import `economy_map_insert_block_body_version` from the processor, capture
    its POST body via a stub, assert no `status` key and table == block_body_versions); (2) owner
    gate enforced before RPC for both verbs (refusal + RPC-not-called for non-owner; RPC-called for
    owner); (3) UUID validation (missing + malformed → usage hint, RPC not called); (4) already-actioned
    RPC raise → D-05 case-(c) message; (5) approve success confirmation contains maturity old→new and
    the block URL. Make the suite runnable standalone (`if __name__ == "__main__"` runner like
    test_07) AND collectible by pytest. Do NOT call the live database or proxy.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_09_gated_publishing.py -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_09_gated_publishing.py -v` passes (all tests green), no live network.
    - A test named for GATE-01 asserts the synthesis writer omits `status` (draft-only) and never targets `blocks`/`published` — making GATE-01 genuinely verified, not assumed.
    - A test asserts a non-owner caller of `/map-approve` AND `/map-reject` gets a refusal and the RPC-POST stub is never invoked.
    - A test asserts a missing arg and a malformed (non-UUID) arg each return a usage hint with no RPC call.
    - A test asserts the "not found or not in draft status" RPC raise maps to the distinct D-05 case-(c) message (not a crash, not a success).
    - A test asserts the approve success string contains the maturity `<old>→<new>` transition and the `#/map/<slug>` URL.
  </acceptance_criteria>
  <done>A standalone+pytest test suite verifies GATE-01 (draft-only synthesis), the owner gate, UUID validation, the already-actioned path, and the approve confirmation content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram user → gato_brain /chat → /map-* dispatch | Untrusted command text + user_id; the only thing distinguishing owner from non-owner is `ensure_user(...).access_tier`. This is the central control. |
| gato_brain → economy_map RPC (PostgREST) | gato_brain holds service_role; the RPC is `SECURITY DEFINER`. The owner gate must live in gato_brain BEFORE the RPC POST — the DB does not know who the Telegram caller is. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Elevation of privilege (broken access control) | /map-approve, /map-reject owner gate | mitigate | D-02: gate both write branches on `access_tier == 'owner'` BEFORE any RPC POST; close the PATTERNS.md wiring gap by threading `access_tier` (computed at ~2126) into `handle_map_command` at the ~2264 dispatch. Test asserts a non-owner caller never reaches the RPC. THIS IS THE CENTRAL THREAT. |
| T-09-07 | Injection / input validation | `<version_id>` arg → RPC body | mitigate | D-04a: validate the arg with `uuid.UUID(arg)` before building the POST body; malformed → typed usage hint (D-05 case b). The arg is passed as parameterized JSON `{"p_version_id": "<uuid>"}`, never f-string-interpolated into the URL path or body. |
| T-09-08 | Tampering / race (double-publish) | concurrent /map-approve | mitigate | Rely on the RPC's single-winner `WHERE status='draft' RETURNING`-empty property (033 Step 1); the loser's typed RAISE surfaces as D-05 case (c) "already actioned", never a second publish. Test covers the case-(c) mapping. |
| T-09-09 | Information disclosure (silent failure) | RPC-POST helper non-2xx | mitigate | Fail-loud: raise `RuntimeError(... resp.text)` on non-2xx so a failed write never reads as a benign no-op (the recurring silent-failure class); surfaces as D-05 case (d) `Command failed: <e>`. |
| T-09-10 | Spoofing | service_role key in RPC POST | accept | Reuses the existing `SUPABASE_KEY` service_role secret already present in gato_brain for reads; no new secret surface, no key broadening (D-02: no new env var/config). |
| T-09-SC | Tampering | npm/pip installs | accept | No new package installs in this phase (uses stdlib `uuid` + existing `httpx`); package-legitimacy gate not triggered. |
</threat_model>

<verification>
- gato_brain syntax-clean (`ast.parse`), `/map-approve` + `/map-reject` branches present, gated, and tier threaded into dispatch.
- `tests/test_09_gated_publishing.py` passes: GATE-01 draft-only, owner gate, UUID validation, already-actioned, approve confirmation.
- `/map-status` and `/map-pending` unchanged and ungated (D-02a).
- Scoped rebuild after merge: `cd /root/bitcoin_bot/docker && docker compose up -d --build gato_brain` (deploy step; behavior already validated by tests against the Plan 09-01 live RPC).
</verification>

<success_criteria>
- CMD-03/GATE-02: owner `/map-approve <uuid>` runs the atomic publish RPC and returns a confirmation with maturity old→new + live URL; the Plan 09-01 watermark amendment is live so `last_synthesized_at` advances correctly (SC2).
- CMD-04/GATE-03: owner `/map-reject <uuid>` supersedes the draft via the reject RPC and confirms the entries return to the next synthesis; no live page change.
- GATE-04: rejected drafts become `superseded` (terminal, never deleted) — guaranteed by the unchanged reject RPC; the double-approve loser surfaces as "already actioned".
- GATE-01: an explicit test verifies synthesis writes only `status='draft'`.
- SC5: both commands are owner-only and return a confirmation on success or a distinct typed error per D-05 case.
</success_criteria>

<output>
Create `.planning/phases/09-gated-publishing-approval-commands/09-02-SUMMARY.md` when done.
</output>
