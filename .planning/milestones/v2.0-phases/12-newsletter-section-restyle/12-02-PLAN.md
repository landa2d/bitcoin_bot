---
phase: 12-newsletter-section-restyle
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - docker/web/site/index.html
  - docker/web/site/app.js
autonomous: true
requirements: [TGL-01, TGL-02]
must_haves:
  truths:
    - "The Technical/Strategic toggle appears only inside the Newsletter list view and no longer occupies a global/shared position; it is hidden on reader/map/block/status/about routes"
    - "Switching mode still re-renders the same dual-mode content (setMode() logic unchanged; btn-technical/btn-strategic/mode-subtitle IDs preserved)"
    - "The list-row kicker reads EDITION #N · {date} (date appended via formatDate)"
    - "The single-article view shows its own magazine header (mono kicker + serif display title + mono byline) under the existing ← Back to Newsletter control; the PREVIEW banner uses the .preview-banner class not inline amber"
  artifacts:
    - path: "docker/web/site/index.html"
      provides: "Minimal D3 Newsletter header markup (tagline dropped, page-title headline, metadata line) with the relocated toggle markup + preserved IDs"
      contains: "id=\"btn-technical\""
    - path: "docker/web/site/app.js"
      provides: "showView() hero scoped to the list route; renderList() date-appended kicker; renderArticle() magazine header + .preview-banner"
      contains: "preview-banner"
  key_links:
    - from: "index.html .mode-toggle (btn-technical/btn-strategic)"
      to: "app.js setMode()"
      via: "onclick=\"setMode('technical'|'strategic')\" + getElementById IDs"
      pattern: "setMode\\('(technical|strategic)'\\)"
    - from: "app.js showView()"
      to: "the .hero block (now toggle host)"
      via: "hero shown only on the list route"
      pattern: "hero.*list"
    - from: "app.js renderList()"
      to: "EDITION #N · {date} kicker"
      via: "formatDate(n.published_at) appended to section-label"
      pattern: "section-label.*formatDate|formatDate.*published_at"
---

<objective>
Relocate the Technical/Strategic mode toggle so it lives ONLY inside the Newsletter section (TGL-01, D-01 — list-view only; the article view has no toggle of its own and inherits the persisted mode) and wire the markup that consumes Plan 01's styling: restructure the global `.hero` into the minimal D3 header (drop the WEEKLY INTELLIGENCE BRIEFING tagline, apply `.page-title` to the headline, keep the toggle markup with its load-bearing IDs), scope the hero/toggle to the `list` route in `showView()`, append the date to the `renderList()` kicker, and give the article view its own magazine header + the `.preview-banner` class in `renderArticle()`. The dual-mode content logic (`setMode()`, `getModeTitle/Content`) is UNCHANGED — only the control's DOM home, the kicker copy, and the article header markup move.

Purpose: Completes TGL-01 (structural relocation: the toggle ends up only in the Newsletter list because the hero that hosts it now renders only on the `list` route) and TGL-02 wiring (the preserved `.active` class + `#mode-subtitle` drive Plan 01's filled-accent pill + hint line). Delivers the markup side of criterion 3 (date-appended kicker, magazine header, token banner).

Output: A relocated, Newsletter-scoped toggle; a date-bearing list kicker; a magazine article header; and a class-based PREVIEW banner — all driving Plan 01's CSS, with `setMode()` requiring zero logic change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-newsletter-section-restyle/12-UI-SPEC.md
@.planning/phases/12-newsletter-section-restyle/12-PATTERNS.md
@.planning/phases/12-newsletter-section-restyle/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure index.html .hero into the minimal D3 header (toggle markup + IDs preserved)</name>
  <files>docker/web/site/index.html</files>
  <read_first>
    - docker/web/site/index.html lines 31-56 (the `.hero` block lines 33-42 + the `#list-view`/`#reader-view` containers — note `<article id="newsletter-content">` and the `← Back to Newsletter` backlink are load-bearing IDs, do NOT rename)
    - docker/web/site/style-base.css lines 82-97 (locked `.page-title` / `.eyebrow` classes to apply)
    - 12-UI-SPEC.md §"Component Contracts" A (DOM-home discretion resolved) and B (minimal header), §"What This Replaces" rows 1-3
    - 12-PATTERNS.md §2a (hero block current → target) — note the `id="hero-headline"`/`id="hero-date"` must stay reachable by `updateHero()`
  </read_first>
  <action>
    Restructure the `.hero` block (lines 33-42) into the minimal D3 header per UI-SPEC §B. Concrete edits:
    - DELETE the `<p class="hero-tagline">WEEKLY INTELLIGENCE BRIEFING</p>` line entirely (D-07 D3 has no tagline/eyebrow).
    - On the `<h1 ... id="hero-headline">` element, add the locked `.page-title` class (keep `id="hero-headline"` so `updateHero()` still writes into it). Result class list: `class="page-title hero-headline"` (or apply `page-title` and let Plan 01's `.hero-headline` rule match — keep both reachable).
    - Keep the `<p class="hero-date" id="hero-date">` metadata element (it receives `Latest: Edition #{N} · {date}` from `updateHero`); IDs unchanged.
    - PRESERVE the `.mode-toggle` block VERBATIM: the two `<button class="toggle-btn ..." id="btn-technical" onclick="setMode('technical')">Technical</button>` / `id="btn-strategic" onclick="setMode('strategic')">Strategic</button>` buttons and the `<p class="mode-subtitle" id="mode-subtitle">` line. Do NOT change the IDs, the `onclick` wiring, the class names, or the segment label text (`Technical`/`Strategic`) — Plan 01 styles these into the pill, and `setMode()` (app.js) targets these exact IDs.
    - Leave `#list-view`/`#newsletter-list` (lines 46-48) and `#reader-view`/`#newsletter-content`/`.backlink` (lines 51-56) untouched — their IDs are render sinks.
    - Do NOT touch the nav `<header>` shell, the map/block/status/about views, the subscribe section, the bottom bar, or the font `<link>`.
  </action>
  <verify>
    <automated>grep -q 'page-title' docker/web/site/index.html && grep -q 'id="btn-technical" onclick="setMode(.technical.)"' docker/web/site/index.html && grep -q 'id="btn-strategic"' docker/web/site/index.html && ! grep -q 'WEEKLY INTELLIGENCE BRIEFING' docker/web/site/index.html && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `index.html`: the `WEEKLY INTELLIGENCE BRIEFING` tagline line is gone (`grep -c 'WEEKLY INTELLIGENCE BRIEFING' docker/web/site/index.html` returns 0).
    - `index.html`: the headline `<h1>` carries the `page-title` class and still has `id="hero-headline"`.
    - `index.html`: the `.mode-toggle` block still contains `id="btn-technical"` with `onclick="setMode('technical')"`, `id="btn-strategic"` with `onclick="setMode('strategic')"`, and `id="mode-subtitle"` — all verbatim.
    - `index.html`: the toggle segment label text is still exactly `Technical` and `Strategic`.
    - `index.html`: `<article id="newsletter-content">` and the `class="backlink"` `← Back to Newsletter` control in `#reader-view` are unchanged.
  </acceptance_criteria>
  <done>The hero is the minimal D3 header (no tagline, page-title headline, metadata line) with the toggle markup preserved IDs-and-wiring intact, ready for showView() to scope it to the list route.</done>
</task>

<task type="auto">
  <name>Task 2: Scope the hero/toggle to the list route in showView() + correct the stale comment (TGL-01, D-01)</name>
  <files>docker/web/site/app.js</files>
  <read_first>
    - docker/web/site/app.js lines 68-104 (`setMode()` — KEEP VERBATIM; understand which IDs/classes it touches) and lines 138-160 (`showView()` — the hero/toggle visibility gating to extend)
    - 12-UI-SPEC.md §"Component Contracts" A ("DOM-home discretion resolved" — hero shows on the `list` route only; article carries its own header) and §"Interaction Contract"
    - 12-PATTERNS.md §1a (setMode stale-comment note at line 79) and §1c (showView target: scope hero to list route, preserve the defensive null-checks)
  </read_first>
  <action>
    Edit `showView()` so the `.hero` (now the toggle host) renders ONLY on the `list` route, satisfying TGL-01 structurally. Concrete edits:
    - The `var hero = document.querySelector('.hero'); if (hero) hero.style.display = ...` line currently shows the hero on every view except `about`. Change it so the hero shows only when `viewName === 'list'`: `hero.style.display = viewName === 'list' ? 'block' : 'none';`. PRESERVE the `if (hero)` defensive null-check (established pattern).
    - The existing `hideToggle`/`.mode-toggle`/`#mode-subtitle` display logic becomes redundant for hiding (the hero containing them is now hidden off-list), but keep the defensive `if (toggle)`/`if (subtitle)` guards; you may simplify `hideToggle` to `viewName !== 'list'` OR leave the toggle/subtitle display lines as belt-and-suspenders that resolve to `none` off-list — either is acceptable as long as the toggle is not shown on reader/map/block/status/about. Do not remove the null-checks.
    - Correct the stale comment at app.js line ~79 inside `setMode()` (`// Body class (drives CSS variables)`): it is no longer true post-Phase-11 (the body class only drives the dual-mode content re-render, not the palette). Update the comment text to reflect that the body class is a content-re-render selector only (per CONTEXT D-03). Do NOT change any `setMode()` behavior — comment-only edit there.
    - Do NOT change `setMode()`'s logic, `getModeTitle/Content`, the router, or any fetch/localStorage code.
  </action>
  <verify>
    <automated>grep -A24 'function showView' docker/web/site/app.js | grep -qE "hero\.style\.display = viewName === 'list'" && grep -A24 'function showView' docker/web/site/app.js | grep -q 'if (hero)' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `app.js`: in `showView()`, the `.hero` display line is `viewName === 'list' ? 'block' : 'none'` (hero shows only on the list route), and the `if (hero)` null-check is preserved.
    - `app.js`: the toggle is not displayed on `reader`/`map`/`block`/`status`/`about` views (hero hidden off-list; the `if (toggle)`/`if (subtitle)` guards remain).
    - `app.js`: the line-79 comment in `setMode()` no longer claims the body class "drives CSS variables" — it reflects content-re-render-only (D-03).
    - `app.js`: `setMode()` logic is otherwise byte-for-byte behaviorally unchanged (still toggles `.active`, writes `#mode-subtitle`, persists localStorage, sets `?mode=`, re-renders).
  </acceptance_criteria>
  <done>The hero (and the toggle it hosts) renders only on the Newsletter list route — TGL-01 satisfied structurally — with all defensive null-checks intact and setMode() behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add date to renderList() kicker + magazine header & .preview-banner in renderArticle()</name>
  <files>docker/web/site/app.js</files>
  <read_first>
    - docker/web/site/app.js lines 164-190 (`renderList()` — the `.section-label` kicker to date-append; the `· U+00B7` separator literal `' · '` already used on line 175) and lines 217-232 (`renderArticle()` — the magazine header + PREVIEW banner)
    - docker/web/site/app.js lines 11-26 (`MODES` const — `MODES[currentMode].label` yields `Technical`/`Strategic`), lines 335-353 (`escapeHtml`/`formatDate`)
    - docker/web/site/style-base.css lines 82-97 (`.page-title` / `.eyebrow` classes the magazine header reuses)
    - 12-UI-SPEC.md §"Component Contracts" C (list kicker copy `EDITION #{N} · {date}`) and D (magazine article header order: mono kicker `Edition #{N} · {Technical|Strategic}` → serif `.page-title` display title → mono byline `Edition #{N} · {date} · {Technical|Strategic}`; PREVIEW banner restyle), §"Copywriting Contract"
    - 12-PATTERNS.md §1d (renderList date-append) and §1e (renderArticle magazine header + banner)
  </read_first>
  <action>
    Two render-function markup changes (no fetch/content-logic change). Concrete edits:
    - In `renderList()`: change the `.section-label` template from `'EDITION #' + n.edition_number` to also append the date — `'EDITION #' + n.edition_number + ' · ' + formatDate(n.published_at)` (reuse the exact `' · '` U+00B7 separator literal already used at line 175). Keep the `.substring(0, 150)` excerpt truncation verbatim. Keep the `updateHero('AI Agents Pulse', 'Latest: Edition #' ...)` strings verbatim (locked copy). `getModeTitle/getModeContent` calls unchanged. The empty-state `No newsletters published yet.` copy is unchanged (Plan 01's serif rules restyle it; optionally drop the inline `color:var(--text-secondary);font-size:15px;` so it inherits serif — discretion).
    - In `renderArticle()`: the article now needs its own magazine header (the hero is list-only after Task 2). Prepend a magazine-header markup string to `#newsletter-content` (before `banner + rendered`), composed of: a mono kicker using the `.eyebrow` class with text `Edition #{N} · {Technical|Strategic}` (resolve the label via `MODES[currentMode].label`, NOT a hardcoded string); a serif display title using the `.page-title` class with `escapeHtml(title)`; and a mono byline line (`Edition #{N} · {date} · {Technical|Strategic}`) — use the `· ·` separator and `MODES[currentMode].label`. `escapeHtml()` every DB-derived string (title) exactly as the list rows do; `edition_number` is numeric. The `← Back to Newsletter` control is the static `.backlink` already in `#reader-view` (index.html) — the header sits under it; do NOT emit a back-link from the renderer.
    - Replace the inline-amber PREVIEW banner: change the `banner` string from the inline `<div style="background:#f59e0b;...">PREVIEW — NOT YET PUBLISHED</div>` to `<div class="preview-banner">PREVIEW — NOT YET PUBLISHED</div>` (Plan 01 defines `.preview-banner`). Keep the `status === 'preview'` gate and the exact copy `PREVIEW — NOT YET PUBLISHED`.
    - Keep `marked.parse(content)` (the only escapeHtml-bypass path — unchanged precedent) and the `updateHero(title, ...)` call (the non-list callers still rely on updateHero; the list-scoped hero just won't be visible on the reader route — harmless). Do NOT add any new unescaped interpolation of edition data into innerHTML beyond the escapeHtml'd kicker/title/byline.
  </action>
  <verify>
    <automated>grep -A14 'function renderList' docker/web/site/app.js | grep -qE "section-label.*edition_number.*\\\\u00b7.*formatDate|formatDate\(n\.published_at\)" && grep -q 'class="preview-banner"' docker/web/site/app.js && grep -A18 'function renderArticle' docker/web/site/app.js | grep -q 'page-title' && ! grep -q 'f59e0b' docker/web/site/app.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `app.js`: `renderList()` builds the `.section-label` kicker as `EDITION #{N} · {date}` — `formatDate(n.published_at)` is appended via the `' · '` separator; the `.substring(0, 150)` truncation is unchanged.
    - `app.js`: `renderArticle()` prepends a magazine header containing a `.eyebrow` mono kicker (`Edition #{N} · {Technical|Strategic}`), a `.page-title` serif display title (`escapeHtml(title)`), and a mono byline; the `{Technical|Strategic}` label is resolved via `MODES[currentMode].label` (not hardcoded).
    - `app.js`: the PREVIEW banner uses `class="preview-banner"` and the inline `#f59e0b` amber is gone (`grep -c 'f59e0b' docker/web/site/app.js` returns 0); copy is still `PREVIEW — NOT YET PUBLISHED`.
    - `app.js`: every DB-derived string in the new header is wrapped in `escapeHtml()`; `marked.parse(content)` is unchanged (no new unescaped edition-data innerHTML sink beyond the escaped header).
    - `app.js`: `getModeTitle()`/`getModeContent()` and all fetch/router/localStorage code are unchanged.
  </acceptance_criteria>
  <done>The list kicker shows the date; the article view has its own escaped magazine header (kicker + serif title + byline) under the back-control; the PREVIEW banner is the token-based `.preview-banner` — no new XSS sink introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase edition data → `#newsletter-content` innerHTML | Existing boundary (NOT new this phase). Edition title/content come from `newsletters` (Supabase) and are rendered into innerHTML. The pre-existing sinks are `escapeHtml()` for discrete strings and `marked.parse()` for the markdown body (the one accepted escapeHtml-bypass). This plan adds a magazine header that interpolates edition title/edition_number — it MUST reuse `escapeHtml()` so the boundary is not widened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering (XSS non-regression) | `renderArticle()` magazine header | mitigate | The new header interpolates `title` (DB string) and `edition_number` (numeric) into innerHTML. Mitigation: wrap `title` in `escapeHtml()` exactly as the existing list rows do (`escapeHtml(title)`); `MODES[currentMode].label` is a hardcoded constant (no DB input). NO new unescaped edition-data interpolation is added beyond the escaped header — the `marked.parse()` body path is unchanged, so the injection surface is NOT widened. Verified by the acceptance criterion requiring `escapeHtml()` on all DB-derived header strings. |
| T-12-04 | Tampering (XSS non-regression) | `renderList()` kicker date-append | mitigate | `formatDate(n.published_at)` returns a locale date string derived from a DB timestamp; it is concatenated into the `.section-label` template. The value is a formatted date (no markup); it sits in the same innerHTML the list already builds. No new attribute/href sink. The pre-existing escapeHtml on `title`/`excerpt` is unchanged. No widening of the sink. |
| T-12-05 | Tampering | `.preview-banner` swap | accept | Swapping an inline-styled `<div>` for a class-based `<div>` with the SAME static copy (`PREVIEW — NOT YET PUBLISHED`) removes inline style but adds no dynamic interpolation. No injection sink. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs — no `package.json` in the repo (vanilla-JS SPA, CDN `<script>` tags unchanged). No supply-chain surface added. |
</threat_model>

<verification>
- TGL-01: the toggle is structurally inside the Newsletter list — `showView()` renders the hero (toggle host) only on the `list` route; it is hidden on reader/map/block/status/about.
- TGL-02 wiring: `#btn-technical`/`#btn-strategic`/`#mode-subtitle` IDs + `onclick="setMode(...)"` preserved → `setMode()` needs zero logic change; the `.active` class drives Plan 01's filled-accent pill.
- XSS non-regression: the only DB→innerHTML paths are the pre-existing `escapeHtml()` (now also on the new header strings) and the unchanged `marked.parse()` body. No new unescaped interpolation, no new href/attribute sink.
- Static/source-level check; optional render check against the running `agentpulse-web` container (account for the `__SUPABASE_URL__` entrypoint substitution per the project memory note). NO prod deploy.
</verification>

<success_criteria>
- ROADMAP criterion 1 / TGL-01: the toggle appears only inside the Newsletter section (list view) and no longer occupies a global/shared position; switching mode re-renders the same dual-mode content.
- ROADMAP criterion 2 / TGL-02: the active mode shows a filled accent (Plan 01 pill via the preserved `.active`) and the hint line copy (`MODES[mode].subtitle`) renders below it.
- ROADMAP criterion 3 (markup side): the list kicker carries the date; the article view renders its own serif magazine header.
- `grep -c 'f59e0b' docker/web/site/app.js` returns 0; `grep -c 'WEEKLY INTELLIGENCE BRIEFING' docker/web/site/index.html` returns 0.
</success_criteria>

<output>
Create `.planning/phases/12-newsletter-section-restyle/12-02-SUMMARY.md` when done.
</output>
