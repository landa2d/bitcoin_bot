---
phase: 12
slug: newsletter-section-restyle
status: approved
shadcn_initialized: false
preset: none
created: 2026-06-04
reviewed_at: 2026-06-04
---

# Phase 12 — UI Design Contract

> Visual and interaction contract for the **Newsletter Section Restyle**: restyle the edition-list and single-article views onto the Phase 11 design-system shell and relocate the Technical/Strategic mode toggle so it lives **only** inside the Newsletter section. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **This phase REUSES the design system locked by Phase 11.** It does NOT re-derive tokens. The palette hexes, serif/mono type scale, weights (400/600), 4px spacing grid, radius set (3/7/8/10), and the mono-for-chrome reservation list are inherited verbatim from `11-UI-SPEC.md` + `style-base.css`. This contract only specifies how the **Newsletter list + article + relocated toggle + minimal header** consume those locked tokens. Every value below resolves to an existing `:root` token or a locked Phase 11 display class — no new tokens, no new palette, no second font.
>
> **Stack note:** Vanilla-JS single-page app — static `index.html` + hand-authored CSS using CSS custom properties, served by Caddy as the `agentpulse-web` container. Frontend lives in `docker/web/site/`. Not React/Tailwind/Next/shadcn. No `components.json` / `tailwind.config` / `package.json` exist (re-confirmed this phase). **shadcn gate resolved to N** (carried from Phase 11). Registry safety gate: **not applicable**.

---

## What This Phase Restyles (scope map — for the executor)

| Surface | Function / DOM | CONTEXT decision | What changes |
|---------|----------------|------------------|--------------|
| Mode toggle | `index.html` `.hero > .mode-toggle` → relocate into list view; `setMode()` `app.js:68` keeps the toggle markup IDs | D-01, D-02, D-03 / TGL-01, TGL-02 | Move out of global hero into Newsletter **list view only**; reform as segmented accent pill A1 + mono hint line. Content logic UNCHANGED. |
| Newsletter header | `index.html` `.hero` (tagline/headline/date) for the **list route** | D-07 | Replace WEEKLY INTELLIGENCE BRIEFING tagline + eyebrow with a minimal serif page-title + mono metadata line (D3). |
| Edition list rows | `renderList()` `app.js:164`; `.article-entry`/`.section-label`/`.entry-title`/`.entry-preview` in `style-shared.css` | D-04 / criterion 3, TYPE-01 | Restyled rows B1: mono kicker `EDITION #N · {date}`, serif title link, **serif** excerpt, `--line` dividers, tightened rhythm. Not cards. |
| Article reading view | `renderArticle()` `app.js:217`; `article` prose rules in `style-shared.css` | D-05, D-06 / criterion 3, TYPE-01 | Richer magazine C2: mono kicker, large serif display title, byline/metadata, emphasized lead, accent-soft blockquotes, styled code/pre. **Serif body prose** (no mono paragraphs). PREVIEW banner restyled, not rebuilt. |

**Out of scope (do NOT touch):** `getModeTitle()`/`getModeContent()` (`app.js:356/361`) content selection; any fetch/data/router logic; the map/block/status hero behavior on non-Newsletter routes (Phase 13 owns it — must not regress); `#/about` content (Phase 14); prod deploy.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (vanilla-JS SPA, hand-authored CSS variables — inherited from Phase 11) |
| Preset | not applicable |
| Component library | none |
| Icon library | none — Unicode glyphs already in the codebase: `·` (U+00B7) metadata separator, `←` (U+2190) back-control (Phase 11 shell), `↗` source links. No icon dependency added. |
| Font (reading/titles) | **Source Serif 4** (`var(--serif)` = `'Source Serif 4', Georgia, serif`) — list/article titles, excerpt, body prose, lead, blockquote text |
| Font (UI chrome only) | **IBM Plex Mono** (`var(--mono)` = `'IBM Plex Mono', ui-monospace, monospace`) — kicker, metadata line, toggle segment labels, hint line, byline, `code`/`pre`, table cells |
| Font loading | Inherited — the `index.html` `<head>` Google Fonts `<link>` (Source Serif 4 400/600 + italic 400, IBM Plex Mono 400/600) already loads everything this phase uses. **No font-loading change.** |

All tokens below reference the Phase 11 `:root` layer in `docker/web/site/style-base.css`. The legacy-token bridge (`--text-primary`, `--text-body`, `--text-secondary`, `--text-hint`, `--border`, `--code-bg`, `--pre-bg`, `--blockquote-bg`, `--toggle-bg`, `--toggle-inactive`) already maps onto the light palette; this phase may reference either the raw `--ink*/--line*/--accent*` tokens or the bridge aliases, but **prefer the raw Phase 11 tokens** for new Newsletter rules (the bridge aliases are slated for eventual retirement).

---

## Spacing Scale

Inherited verbatim from Phase 11 — no new tokens. Every value is a multiple of 4px.

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| `--space-xs` | 4px | Kicker→title gap; toggle pill inner padding; byline inline gaps |
| `--space-sm` | 8px | Title→excerpt gap; hint-line top margin; kicker→display-title gap (article) |
| `--space-md` | 16px | Toggle pill → list gap; row internal rhythm; blockquote padding; lead bottom margin |
| `--space-lg` | 24px | List-header bottom margin; between-row vertical padding (row block); article section padding |
| `--space-xl` | 32px | Minimal-header top padding (list view); article header → body gap |
| `--space-2xl` | 48px | Major break above subscribe section (existing, untouched) |
| `--space-3xl` | 64px | Page-bottom breathing room (inherited `main` region) |

**Tightened vertical rhythm (D-04, brief §6 "minimalist but not sparse"):** the current `.article-entry { margin-bottom: 20px }` (a non-grid value) is **replaced** by a `--line`-divided row using `--space-lg` (24px) vertical padding per row with a 1px `--line` divider between rows (border-bottom on each row, none on the last). This reads tighter and more deliberate than the loose 20px stacked margins while staying on the 4px grid.

**Spacing exceptions:** none. Chrome control paddings reuse the Phase 11 snapped literals where applicable (toggle button padding `8px 20px`). The retired `.toggle-btn { padding: 7px 16px }` / `.mode-subtitle { margin-top: 10px }` / `.article-entry { margin-bottom: 20px }` / `.entry-title { margin-bottom: 6px }` off-grid values are all replaced with 4px-grid values.

---

## Radius

Inherited verbatim from Phase 11 (3 / 7 / 8 / 10 set) — POLISH-01 / brief §6 "~7–10px, consistent." No radius outside this set.

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| `--radius` | 10px | The segmented toggle **pill wrapper**; the PREVIEW banner; `pre` code-block surface |
| `--radius-btn` | 8px | Each toggle **segment**; inline `code` may use `--radius-sm` (see below) |
| `--radius-sm` | 7px | Inline `code` chips; blockquote container |
| `--radius-dot` | 3px | Not used in Newsletter views (reserved for economy progress dots, Phase 13) |

**Discretion resolved (radius):** the segmented pill wrapper = `--radius` (10px); each active/inactive segment = `--radius-btn` (8px), nested inside the wrapper. Blockquote container and inline `code` = `--radius-sm` (7px); `pre` block = `--radius` (10px). This replaces the off-set legacy values (`.mode-toggle` 8px wrapper / `.toggle-btn` 6px / `code` 3px / `pre` 4px) with the locked 7/8/10 set.

---

## Typography

**Inherited two-family system (Phase 11): Source Serif 4 = everything you READ; IBM Plex Mono = UI chrome ONLY. Two weights total (400, 600).** No new sizes are introduced — every role below lands on a Phase 11 scale step. The single most important typographic rule this phase enforces is **TYPE-01 completion: no monospace body paragraphs** — the legacy `article p` / `.entry-preview` / `article ul,ol` / `article td` mono rules are all migrated to serif.

### Serif scale (consumed — Source Serif 4)

| Role | Size | Weight | Line Height | Where in this phase |
|------|------|--------|-------------|---------------------|
| Body / reading | 18px | 400 | 1.62 | Article body paragraphs (`article p`), list items (`article li`) — **migrated FROM mono** |
| List title | 20px | 600 | 1.25 | Edition-list `.entry-title` link (`letter-spacing:-.01em`). **Was 18px Georgia 400 → now 20px Source Serif 4 600**, consolidated to the locked list/card title step |
| List excerpt | 18px | 400 | 1.5 | `.entry-preview` — **migrated FROM mono 15px** to serif 18px; sits at the body step (line-height 1.5 keeps it tighter than full reading prose) |
| In-content H2 | 24px | 600 | 1.2 | `article h2` sub-sections — **was mono 15px uppercase 700 → now single serif heading at 600** (TYPE-03) |
| In-content H3 | 20px | 600 | 1.25 | `article h3` — same serif treatment as the list-title step; only margin differs from H2 (no separate weight/family) |
| Article lead (emphasized) | 20px | 400 | 1.45 | The **first paragraph** of the article body (D-05 emphasized lead) — serif, larger than body, `--ink` (not faint). One step up from body; NOT a new size (reuses the 20px list-title step at weight 400) |
| Page title (display) | `clamp(30px, 5vw, 46px)` | 600 | 1.12 | Minimal Newsletter header (D-07) **and** article magazine display title (D-05) — reuses the locked `.page-title` class (`letter-spacing:-.015em`) |

### Mono scale (consumed — IBM Plex Mono, chrome only)

| Role | Size | Weight | Where in this phase |
|------|------|--------|---------------------|
| Kicker (list + article) · Toggle hint line | 11px | 600 kicker / 400 hint | List row kicker `EDITION #N · {date}` and article kicker `Edition #N · {Technical\|Strategic}` reuse the locked `.eyebrow` step (11px/600, UPPERCASE, `letter-spacing:.2em`). The toggle **hint line** sits on the same 11px step at weight 400 (sentence-case, `--ink-faint`) — it is a label, not a kicker, so it is NOT uppercased |
| Toggle segment label | 12.5px | 600 active / 400 inactive | `Technical` / `Strategic` segment labels reuse the locked tab/button mono step (12.5px); active segment 600 + white, inactive 400 + `--ink-soft` |
| Metadata line · Byline · Code | 14px | 400 | Minimal-header metadata `Latest: Edition #N · {date}`; article byline/metadata `Edition #N · {date} · {Technical\|Strategic}`; inline `code` — all on the locked 14px metadata step at 400, `--ink-faint` (byline) / `--accent-ink` (code text) |

**Weight policy (exactly 2 weights, inherited):** 400 = serif body/excerpt/lead, mono metadata/byline/hint/code, inactive toggle segment. 600 = serif list-title/headings/display, mono kicker, active toggle segment. **No 500, no 700.** The legacy `article h2/h3 { font-weight:700 }`, `.toggle-btn.active { font-weight:700 }`, and `article th { font-weight:700 }` are all corrected to 600.

### TYPE-01 migration checklist (the load-bearing serif conversion)

These `style-shared.css` rules currently use `var(--mono)` on reading text and **MUST** convert to `var(--serif)` (the heart of success criterion 3):

| Rule | Current | Target |
|------|---------|--------|
| `.entry-preview` | `var(--mono)` 15px | `var(--serif)` 18px/400/1.5 |
| `article p` | `var(--mono)` 15px | `var(--serif)` 18px/400/1.62 |
| `article ul, article ol` | `var(--mono)` 15px | `var(--serif)` 18px/400/1.62 |
| `article li` | (inherits mono) | serif via the `ul/ol` rule |
| `article td` | `var(--mono)` 15px | `var(--serif)` 16px/400/1.45 (table cell — body data, reads as prose) |
| `article th` | `var(--mono)` 13px uppercase 700 | mono is ALLOWED on `th` (it is a chrome/label row) — keep `var(--mono)` but drop weight to 600; this is a header-label exception, not body copy |

Mono **stays** on: list/article kicker, metadata/byline, toggle labels + hint, inline `code` / `pre` (code is on the allowed-chrome list — brief §3, D-05), and table **header** cells (`th` = labels). Mono **never** appears on a reading paragraph, excerpt, list item, or table data cell after this phase.

---

## Color

A **single light-mode palette**, inherited verbatim from Phase 11 — **exactly ONE accent** (violet), **no mode-driven accent swap** (the old technical-teal / strategic-violet flip is already retired; one accent in both newsletter modes). Hex values are locked in `style-base.css :root`; this phase only assigns roles.

| Role | Value | Usage in this phase |
|------|-------|---------------------|
| Dominant (60%) — `--bg` | `#faf8f5` | Newsletter page background; the inactive toggle-pill track when on `--bg` |
| Surface (30%) — `--surface` | `#ffffff` | The toggle-pill wrapper fill; `pre` / `code` block surfaces (light); PREVIEW banner base |
| Ink — `--ink` | `#1a1916` | List title link (rest); article display title; lead paragraph; body `<strong>` |
| Ink soft — `--ink-soft` | `#55514a` | Article body paragraphs; excerpt text; inactive toggle segment label |
| Ink faint — `--ink-faint` | `#8a857c` | Mono kicker (when not accent), metadata line, byline, toggle hint line, table data |
| Line — `--line` | `#e7e2da` | **Row dividers** between edition-list rows; table cell borders; header bottom rule |
| Line strong — `--line-strong` | `#d8d2c7` | Toggle-pill outer border |
| Accent (10%) — `--accent` | `#5b3df5` | See reserved list |
| Accent soft — `--accent-soft` | `#efeaff` | Blockquote fill; PREVIEW banner soft fill (replacing the legacy amber `#f59e0b`) |
| Accent ink — `--accent-ink` | `#4a2fd6` | Link text; list-title link **hover**; inline `code` text; active-segment text is **white**, not accent-ink |
| Destructive | not applicable | Read-only public site — no destructive actions this phase |

**Accent reserved for (explicit list — this phase's accent budget, never "all interactive elements"):**
1. **Article body links** (`article a`) — `--accent-ink` text, subtle underline (see link treatment below).
2. **Active toggle segment fill** — solid `--accent` background, **white** text at weight 600 (the filled-accent requirement of TGL-02 / D-02).
3. **List-title link hover** — the `.entry-title` serif link darkens from `--ink` to `--accent-ink` on hover (color change only, no weight/underline change).
4. **Blockquote left border** — 3px `--accent` left stripe on `article blockquote` (D-05), fill `--accent-soft`.
5. **Inline `code` text** — `--accent-ink` on an `--surface`/`--line`-tinted chip (code is allowed accent chrome).
6. **Kicker (optional accent variant)** — the article/list kicker MAY render in `--accent-ink` (matching the locked `.eyebrow` class default) OR `--ink-faint`; **pick `--ink-faint` for the list-row kicker** (rationed accent — the row's focal point is the serif title, not the kicker) and `--accent-ink` for the single-article kicker (one accent kicker per reading screen is acceptable emphasis). This keeps the accent budget tight per the Phase 11 visual-hierarchy rule.

**Not allowed:** any second hue; the retired technical-teal `#00e5a0`; the legacy amber PREVIEW banner `#f59e0b` (replaced by `--accent-soft` fill + `--ink` text + an `--accent` left rule). The hint line and inactive segment never use accent. Mode-driven accent swapping stays gone — the toggle's active segment is `--accent` in **both** Technical and Strategic states (the segment that is filled changes; the fill color does not).

**Link underline treatment (discretion resolved):** article links render `--accent-ink` text with `text-decoration: underline; text-decoration-thickness: 1px; text-underline-offset: 2px;` and `text-decoration-color: var(--accent-soft)` at rest, deepening `text-decoration-color` to `--accent-ink` on hover. This is a restrained editorial underline (faint at rest, solid on hover) — it preserves the existing `article a { underline; offset 2px }` behavior but recolors it to the violet accent and softens the rest-state rule. The list-title link is **not** underlined (it relies on serif size + hover color shift).

**Contrast (inherited, re-stated for the new surfaces):** `--accent-ink #4a2fd6` on `--bg` ≈ 7.0:1 (links, hover). White on `--accent #5b3df5` ≈ 5.4:1 (active toggle segment — passes AA for the 12.5px/600 label). `--ink-soft #55514a` on `--bg` ≈ 7.5:1 (body/excerpt — AAA). `--ink-faint #8a857c` on `--bg` ≈ 3.3:1 — **chrome/metadata only** (kicker, byline, hint, table data), never a reading paragraph.

---

## Component Contracts (the prescriptive bit)

### A. Segmented accent pill — toggle (D-02, A1 / TGL-01, TGL-02)

- **Wrapper:** `display:inline-flex`, `background: var(--surface)`, `border: 1px solid var(--line-strong)`, `border-radius: var(--radius)` (10px), `padding: var(--space-xs)` (4px), `gap: 0`. One pill, two segments.
- **Segment (button):** `font-family: var(--mono)`, `font-size: 12.5px`, `padding: 8px 20px` (Phase 11 toggle literal), `border-radius: var(--radius-btn)` (8px), `border: none`, `cursor: pointer`.
- **Active segment:** `background: var(--accent)`, `color: #fff`, `font-weight: 600`. (Filled accent — TGL-02.)
- **Inactive segment:** `background: transparent`, `color: var(--ink-soft)`, `font-weight: 400`.
- **Hint line (TGL-02):** directly below the pill, `font-family: var(--mono)`, `font-size: 11px`, `font-weight: 400`, `color: var(--ink-faint)`, `margin-top: var(--space-sm)` (8px). Copy is mode-driven (see Copywriting). **Not uppercased** (it is a descriptive hint, not a label).
- **Placement (D-01 / TGL-01):** rendered at the top of the **list view only**, beneath the minimal header, **above** the edition rows. The single-article view has **no toggle**. The toggle is removed from the global `.hero`.
- **Wiring (D-03):** segment `onclick="setMode('technical')"` / `onclick="setMode('strategic')"` is preserved verbatim. `setMode()` keeps `getElementById('btn-technical'/'btn-strategic')` and `getElementById('mode-subtitle')` working — **keep those element IDs** on the relocated segments + hint line so `setMode()` needs no logic change (it already toggles `.active`, writes the subtitle text, persists localStorage, sets `?mode=`, toggles the body class, and re-renders). The `.active` class on the segment drives the filled-accent state.
- **DOM-home discretion resolved:** **render the pill + hint inside the list view's header block** (not the global `.hero`). Mechanism: repurpose the existing `.hero` block as a **list-scoped minimal header** (it is already hidden on `about` and map routes by `showView()` — extend that so the hero/header shows **only** on the `list` route, and the article view carries its own magazine header). This guarantees TGL-01 (toggle is structurally inside Newsletter) without regressing the map/status/block hero behavior Phase 13 owns. Keep the `btn-technical` / `btn-strategic` / `mode-subtitle` IDs on the moved elements.

### B. Minimal Newsletter header — list view (D-07, D3)

- **Page title:** reuse the locked `.page-title` class — serif `clamp(30px,5vw,46px)`/600, `--ink`. Copy: the existing site title string (`AI Agents Pulse` is the current `updateHero` value; render it as the serif display title). **Drop** the `WEEKLY INTELLIGENCE BRIEFING` tagline and any eyebrow above it (D-07 picks D3, not the eyebrow+title D1).
- **Metadata line:** beneath the title, `font-family: var(--mono)`, `font-size: 14px`, `font-weight: 400`, `color: var(--ink-faint)`. Copy: `Latest: Edition #{N} · {date}` (the existing `updateHero` second-arg string — keep it; it already uses the `·` U+00B7 separator).
- **Layout:** left-aligned or centered is discretion — **centered**, matching the Phase 11 `.page-title` display intent and the current `.hero { text-align:center }`. Top padding `--space-xl` (32px), title→metadata gap `--space-sm` (8px), metadata→toggle gap `--space-md` (16px).
- **The A1 toggle pill + hint sit directly beneath this header** (D-07), above the edition rows.

### C. Restyled edition-list rows — list view (D-04, B1)

Per row (preserve the `renderList()` `.article-entry` anatomy; restyle, don't rebuild):
- **Kicker:** `<div class="section-label">` → mono 11px/600 UPPERCASE, `letter-spacing:.2em`, `color: var(--ink-faint)`. **Copy must add the date:** `EDITION #{N} · {date}` (currently edition-number only — `renderList()` must append `· {formatDate(published_at)}`). The `·` separator is U+00B7.
- **Title:** `.entry-title` serif **20px/600**, `--ink`, `letter-spacing:-.01em`, `text-decoration:none`, `display:block`. Hover → `--accent-ink` (color only). Kicker→title gap `--space-xs` (4px).
- **Excerpt:** `.entry-preview` serif **18px/400/1.5**, `--ink-soft` (migrated from mono). Title→excerpt gap `--space-sm` (8px).
- **Row container:** `padding: var(--space-lg) 0` (24px top+bottom) with `border-bottom: 1px solid var(--line)`; `:last-child` drops the border. This is the tightened-rhythm divider treatment (D-04) — **rows, not cards** (no surface fill, no box border, no radius on the row).
- **Excerpt length (discretion resolved):** keep the existing ~150-char truncation in `renderList()` (`content.replace(...).substring(0,150)+'...'`). 150 chars at the serif 18px step yields ~2 lines on the 720px measure — enough to preview, short enough to keep rows tight. No change to the truncation logic; only the rendered font changes.

### D. Richer magazine article — reader view (D-05, D-06, C2)

`renderArticle()` output, in order, all within the **single serif-display system** (D-06: one display style, weights 400/600 only, no second family, no ornamentation):
- **Kicker (mono):** `Edition #{N} · {Technical|Strategic}` — mono 11px/600 UPPERCASE `.eyebrow` step, `--accent-ink` (one accent kicker per reading screen, per the Color reserved-list rationale). Sits under the Phase-11 `← Back to Newsletter` control already in `#reader-view`.
- **Display title (serif):** the edition title via the locked `.page-title` class (serif `clamp(30px,5vw,46px)`/600, `--ink`). Larger than the list title — this is the article's focal point. Kicker→title gap `--space-sm` (8px).
- **Byline / metadata line (mono, discretion resolved — format):** `Edition #{N} · {date} · {Technical|Strategic}` — mono 14px/400, `--ink-faint`. (No author name — AgentPulse editions are unbylined; "byline" here = the edition metadata line. Uses the `·` U+00B7 separator. This is the resolved byline format.) Title→byline gap `--space-xs` (4px); byline→body gap `--space-xl` (32px).
- **Lead paragraph (serif, emphasized):** the **first** body paragraph renders one step up — serif 20px/400/1.45, `--ink` (not `--ink-soft`). Achieved with a CSS rule scoped to `#newsletter-content > p:first-of-type` (or the first `<p>` after the banner) — no markdown change, no content-logic change. Remaining paragraphs are the 18px/400/1.62 body step.
- **Body prose (serif):** all `article p` / `li` at serif 18px/400/1.62, `--ink-soft`. `h2`/`h3` single serif heading (24px / 20px, 600, `--ink`). **No mono paragraphs** (TYPE-01).
- **Blockquotes (D-05):** `border-left: 3px solid var(--accent)`, `background: var(--accent-soft)`, `padding: var(--space-md)` (16px), `border-radius: var(--radius-sm)` (7px), serif body text `--ink`. (Recolors the existing teal-era rule to the violet accent.)
- **Code surfaces (D-05, allowed mono):** inline `code` — mono 14px, `--accent-ink` text, `background: var(--line)` (or a faint `--surface`/`--line` tint), `padding: 4px 8px` (4px-grid conformant), `border-radius: var(--radius-sm)` (7px). `pre` — mono, `background: var(--line)`, `padding: var(--space-md)` (16px), `border-radius: var(--radius)` (10px), `overflow-x:auto`; `pre code` has no own background/padding. Code is the **only** mono allowed in the article reading column.
- **Tables:** `th` keeps mono (label row) at 13px/600 UPPERCASE `--accent-ink`; `td` converts to serif 16px/400 `--ink-soft` (data reads as prose — TYPE-01).
- **PREVIEW banner (D-05, restyle not rebuild):** keep the `status === 'preview'` banner emission in `renderArticle()`. Restyle from the legacy inline amber (`background:#f59e0b;color:#000`) to: `background: var(--accent-soft)`, `color: var(--ink)`, `border-left: 3px solid var(--accent)`, `border-radius: var(--radius)` (10px), `padding: var(--space-sm) var(--space-md)`, mono 12.5px/600 UPPERCASE label. Copy unchanged: `PREVIEW — NOT YET PUBLISHED`.

---

## Visual Hierarchy

**List screen focal point:** the most-recent edition's serif **title** (20px/600, `--ink`) — the first `.entry-title` directly below the toggle pill. The minimal serif page-title above orients; the accent is rationed (active toggle segment, one optional accent kicker) so nothing out-shouts the lead headline.

**Article screen focal point:** the serif **display title** (`clamp(30px,5vw,46px)`/600) — the single largest element, under the mono kicker. The emphasized lead paragraph (20px serif) is the secondary draw. Mono chrome (kicker, byline) and the single accent (links, one kicker) support but never compete with the serif reading column. This mirrors the Phase 11 rule: one serif title leads each screen; mono + the single accent support.

---

## Interaction Contract

| Interaction | Behavior |
|-------------|----------|
| Click a toggle segment (list view) | `setMode('technical'\|'strategic')` — UNCHANGED logic: persists localStorage, sets `?mode=`, toggles `body.technical/.strategic` (content re-render only, NOT theme), re-renders the visible list. Active segment gains `.active` (filled accent), hint line swaps copy. |
| Mode persistence into article | The single-article view has **no toggle** (D-01). It renders whatever mode is currently selected (read from `currentMode` / localStorage). To switch mode while reading, the reader returns to the list — an accepted UX consequence (D-01). |
| Click an edition title (list) | Existing `href="#/edition/{N}"` hash navigation — unchanged. Title hover → `--accent-ink`. |
| `← Back to Newsletter` (article) | Inherited Phase 11 back-control in `#reader-view` — unchanged. |
| Mode transition | The existing 400ms `mode-transitioning` color/background transition is retained (it animates the segment fill + any re-rendered text). |

No new error surfaces. The existing empty-state and edition-not-found paths are restyled into serif/ink but their copy and logic are unchanged.

---

## Copywriting Contract

All strings locked. Mode-dependent copy is sourced verbatim from `MODES` in `app.js` (TGL-02 / locked in ROADMAP success criterion 2) — do not paraphrase.

| Element | Copy |
|---------|------|
| Toggle segment 1 label | `Technical` |
| Toggle segment 2 label | `Strategic` |
| Toggle hint — Technical active | `Architecture, code, implementation` |
| Toggle hint — Strategic active | `Markets, strategy, implications` |
| Minimal-header page title (list) | `AI Agents Pulse` *(preserve the existing `updateHero` site-title string; do not change wording)* |
| Minimal-header metadata (list) | `Latest: Edition #{N} · {date}` *(existing `updateHero` string — keep)* |
| List-row kicker | `EDITION #{N} · {date}` *(UPPERCASE; **add the date** — currently edition-number only)* |
| Article kicker | `Edition #{N} · {Technical\|Strategic}` |
| Article byline / metadata | `Edition #{N} · {date} · {Technical\|Strategic}` |
| PREVIEW banner | `PREVIEW — NOT YET PUBLISHED` *(unchanged — restyle only)* |
| Empty state (no editions) | `No newsletters published yet.` *(preserve verbatim — restyle into serif/`--ink-soft`; do not change wording)* |
| Edition-not-found | `Edition not found.` *(preserve verbatim — restyle into serif)* |
| Empty state body | None required beyond the line above. |
| Error state | No new error surfaces. Existing subscribe/unsubscribe messaging untouched (out of scope). |
| Destructive confirmation | Not applicable — read-only public site; no destructive actions this phase. |

**Metadata separator:** `·` (U+00B7) with surrounding spaces throughout — mono, `--ink-faint`. (Consistent with Phase 11.)

**Toggle label casing:** segment labels render as authored (`Technical` / `Strategic`, sentence-case) — these are the `MODES[*].label` values. The hint line is sentence-case (descriptive). Only the **kickers** are UPPERCASE (they are eyebrow/label chrome).

---

## What This Replaces (migration inventory — for the executor)

| Current (removed/replaced) | Where | Replaced by |
|----------------------------|-------|-------------|
| `.hero-tagline` `WEEKLY INTELLIGENCE BRIEFING` + `.hero-headline` Georgia 29px | `index.html` / `style-shared.css` | Minimal D3 header: locked `.page-title` serif display + mono 14px metadata line (D-07) |
| Global `.mode-toggle` + `.toggle-btn` (in `.hero`, 8px wrapper / 6px btn / 700 active) | `index.html` / `style-shared.css` | Relocated segmented accent pill A1 inside the **list view**: `--radius` wrapper + `--radius-btn` segments, active = filled `--accent`/white/600 (D-01, D-02) |
| `.mode-subtitle` mono 13px, `margin-top:10px` | `style-shared.css` | Hint line mono 11px/400 `--ink-faint`, `margin-top: var(--space-sm)` (8px) — same `#mode-subtitle` ID preserved |
| `.entry-title` Georgia 18px/400 | `style-shared.css` | Serif 20px/600 `--ink`, hover `--accent-ink` |
| `.entry-preview` mono 15px | `style-shared.css` | **Serif 18px/400/1.5** `--ink-soft` (TYPE-01) |
| `.section-label` mono 13px `--accent`, edition-number only | `style-shared.css` + `renderList()` | Mono 11px/600 `--ink-faint`, copy `EDITION #N · {date}` (B1) |
| `.article-entry { margin-bottom:20px }` (off-grid stacking) | `style-shared.css` | `--line`-divided rows, `padding: var(--space-lg) 0`, `:last-child` no border (D-04) |
| `article p` / `ul,ol` / `li` mono 15px | `style-shared.css` | Serif 18px/400/1.62 (TYPE-01) |
| `article h2` / `h3` mono 15px UPPERCASE 700 `--accent` | `style-shared.css` | Single serif heading 24px / 20px / 600 `--ink` (TYPE-03) |
| `article blockquote` (teal-era `--accent`) | `style-shared.css` | 3px `--accent` left border + `--accent-soft` fill + `--radius-sm` (D-05) |
| `article code` 3px / `pre` 4px radius | `style-shared.css` | `code` `--radius-sm` (7px) `--accent-ink`; `pre` `--radius` (10px) |
| `article td` mono 15px | `style-shared.css` | Serif 16px/400 `--ink-soft` (TYPE-01); `th` stays mono at 600 (label row) |
| PREVIEW banner inline amber `#f59e0b` | `renderArticle()` | `--accent-soft` fill + `--ink` text + 3px `--accent` left rule + `--radius` (D-05) |
| Global hero shown on list **and** as the data sink for all routes | `app.js` `showView()` / `updateHero()` | Hero/header scoped to the **list** route; article carries its own magazine header (DOM-home discretion resolved above); map/status/block hero behavior **unchanged** (Phase 13 owns it) |

**Deliberately untouched:** `getModeTitle()`/`getModeContent()` dual-mode selection; all fetch/router/localStorage logic in `setMode()`; the subscribe section + bottom bar; the map/block/status renderers and their hero usage; `#/about` (Phase 14); `style-map.css` (Phase 13).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| not applicable | none | not applicable — vanilla-JS SPA, no shadcn/component registry in use |

No third-party UI registries are declared or used. No registry vetting required. (Inherited from Phase 11 — re-confirmed: no `components.json` / `package.json` exists in the repo.)

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
