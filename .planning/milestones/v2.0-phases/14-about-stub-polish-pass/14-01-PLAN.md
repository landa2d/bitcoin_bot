---
phase: 14-about-stub-polish-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/web/site/index.html
  - docker/web/site/style-shared.css
autonomous: true
requirements: [ABOUT-01]
must_haves:
  truths:
    - "Visiting #/about shows the real What is AgentPulse page — eyebrow, title, page-sub, 3 prose paragraphs, and a 5-pill agent row — not the Phase-11 placeholder lede"
    - "The About copy is factually accurate: it says eight cooperating services and renders exactly the 5 content-agent pills (Processor, Analyst, Research, Newsletter, Gato), with Gato Brain / LLM Proxy / Web described in prose, not as pills"
    - "The agent-pills render as token-styled surface cards (serif/mono, light palette) with no literal hex color and no per-agent accent tint"
    - "The Back to Newsletter backlink is preserved verbatim and app.js is unchanged (route already wired)"
  artifacts:
    - path: "docker/web/site/index.html"
      provides: "Real About view markup in #about-view (eyebrow, page-title, page-sub, 3 paragraphs, .agent-row with 5 .agent-pill blocks, preserved backlink)"
      contains: "agent-pill"
    - path: "docker/web/site/style-shared.css"
      provides: "Net-new token-anchored .about p / .body-soft prose + .agent-row / .agent-pill / .an / .ad component CSS"
      contains: ".agent-pill"
  key_links:
    - from: "index.html #about-view"
      to: ".agent-row / .agent-pill CSS in style-shared.css"
      via: "shared class names"
      pattern: "agent-pill"
    - from: ".agent-pill"
      to: "style-base.css :root tokens"
      via: "var(--surface) / var(--line) / var(--radius-btn) / var(--accent-ink)"
      pattern: "var\\(--radius-btn\\)"
---

<objective>
Flesh out the already-wired `#/about` route's placeholder stub into the real "What is AgentPulse" page (ABOUT-01): replace the Phase-11 honesty-stub lede with the locked About structure (eyebrow -> page-title -> page-sub -> 3 reconciled prose paragraphs -> a 5-pill agent row), and add the net-new `.about p` / `.body-soft` / `.agent-row` / `.agent-pill` / `.an` / `.ad` CSS ported into the Phase-11 token system.

Purpose: Deliver the last content surface of the v2.0 redesign — a truthful, token-consistent About stub that completes the "minimalist but not sparse" feel without the deferred pipeline diagram (ABOUT-02).
Output: Updated `#about-view` markup in `index.html` + net-new token-anchored CSS in `style-shared.css`.

Scope fences (do NOT cross):
- Frontend-only. No backend / pipeline / Supabase / data / content-logic changes.
- Do NOT re-wire the `#/about` route — it is already wired in `app.js` (`getRoute`:134 / `showView`:146 / route->tab map:815 / `route()` case:845). `app.js` is read-only this plan.
- Do NOT add the pipeline diagram or richer About content (ABOUT-02 deferred); no dark mode (DARK-01 deferred).
- Do NOT touch the orphaned `style.css` / `style-builder.css` / `style-impact.css` (not in the live cascade).
- Do NOT deploy / rebuild the `agentpulse-web` container (D-06 — deploy is a separate operator-approved step). Verification is local / in-code only.

Editorial note: the About copy strings carried into this plan (from the UI-SPEC Copywriting Contract) are an operator-reviewable DRAFT corrected for accuracy. The operator MAY reword them at execution — editorial framing stays in human hands. The accuracy guardrails (D-02) are non-negotiable even if reworded: "eight cooperating services" must stay accurate; the pill roster is the 5 content agents only (Gato Brain / LLM Proxy / Web stay in prose, not pills); do NOT call the Processor an "orchestrator that routes work between agents" (routing is Gato Brain's job — the Processor is the background scheduler/monolith).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-about-stub-polish-pass/14-CONTEXT.md
@.planning/phases/14-about-stub-polish-pass/14-PATTERNS.md
@.planning/phases/14-about-stub-polish-pass/14-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add token-anchored About prose + agent-pill CSS to style-shared.css</name>
  <files>docker/web/site/style-shared.css</files>
  <read_first>
    - docker/web/site/style-shared.css — read `article p` (:597-604), `.block-body p` (:382-390), `article a` (:663-669), `article td` (:717-725), `.grid` (:243-248), `.card` (:253-266) and `.card:hover`/`.card:focus-visible` (:286-295), `.section-label` (:148-156), `.article-header .byline` (:637-643) — these are the exact analogs to copy token usage from
    - docker/web/site/style-base.css — the `:root` token layer (`--space-*`, `--radius-btn`, `--surface`, `--line`, `--ink`, `--ink-soft`, `--ink-faint`, `--accent-ink`, `--serif`, `--mono`) and the `.eyebrow` (:90-97) / `.page-title` (:82-88) / `.about-stub` rhythm (:232-248) display classes the markup reuses
    - .planning/phases/14-about-stub-polish-pass/14-UI-SPEC.md — §Component Inventory (the exact `.agent-row` / `.agent-pill` / `.an` / `.ad` target declarations) and §Typography / §Color (13px mono name @ `--accent-ink`, 14px serif `.ad` @ `--ink-soft`, `.about p` 18px serif, page-sub mono 14px `--ink-faint`)
    - .planning/phases/14-about-stub-polish-pass/14-PATTERNS.md — Pattern Assignments #1 (page-sub analog), #2 (`.about p` / `.body-soft`), #3 (`.agent-row`/`.agent-pill`/`.an`/`.ad`) with the explicit DIVERGENCES-from-`.card` checklist
  </read_first>
  <action>
    Append net-new component rules to `style-shared.css` (component layer; keep the new About + agent-pill rules grouped together — section placement is mechanical planner discretion per the hand-authored-CSS / cascade-order convention). Use ONLY `:root` tokens — no literal hex, no raw px radius, no off-grid spacing literal.

    `.about p` — port the `article p` serif-body pattern (:597-604): font-family var(--serif); font-size 18px; font-weight 400; color var(--ink); line-height 1.62; margin-bottom var(--space-md) (P1 primary text uses `--ink`; bottom margin lands on `var(--space-md)`, NOT the raw `16px` literal). Add a `.about p.body-soft` (or `.about .body-soft`) variant overriding color var(--ink-soft) for the P2/P3 secondary paragraphs per the UI-SPEC Color table.

    `.about-stub .page-sub` (page-sub mono metadata) — analog is `.article-header .byline` (:637-643): font-family var(--mono); font-size 14px; font-weight 400; color var(--ink-faint); margin var(--space-xs) 0 0.

    `.about a` (inline links, if About prose links) — copy `article a` (:663-669): color var(--accent-ink); text-decoration underline; text-decoration-thickness 1px; text-underline-offset 2px; text-decoration-color var(--accent-soft).

    `.agent-row` — grid container, copy the token structure from `.grid` (:243-248) but responsive-collapse: display grid; grid-template-columns repeat(auto-fit, minmax(150px, 1fr)); gap var(--space-md); margin var(--space-lg) 0.

    `.agent-pill` — surface card, port token usage from `.card` (:253-266) but apply the DIVERGENCES: border 1px solid var(--line); border-radius var(--radius-btn); background var(--surface); padding var(--space-md) var(--space-md); display flex; flex-direction column. DROP the `border-left: 3px solid var(--accent)` stripe, DROP `cursor: pointer`, DROP `transition`, DROP `text-decoration`/`color: inherit` link semantics. Do NOT port `.card:hover` or `.card:focus-visible` — pills are static, non-interactive informational cards (no hover lift, no link).

    `.agent-pill .an` (agent name, mono chrome): font-family var(--mono); font-size 13px; font-weight 600; color var(--accent-ink).

    `.agent-pill .ad` (one-line role, serif caption — analog `article td` :717-725 shrunk to 14px): font-family var(--serif); font-size 14px; font-weight 400; color var(--ink-soft); margin-top var(--space-xs); line-height 1.4.

    Single-accent discipline (COLOR-02 / Phase-13): all five `.an` names share the SAME `--accent-ink` — no `data-accent`, no per-agent tint. The agent-pill border + `.ad` stay neutral `--line` / `--ink-soft`. The `.agent-pill` `border-radius: var(--radius-btn)` is already on-token — Plan 02's radius grep gate validates it alongside the swept subscribe-form radii. Do NOT add any literal `px` radius or literal hex anywhere in these rules.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -qE "\.agent-pill[[:space:]]*\{" docker/web/site/style-shared.css && grep -qE "\.agent-row[[:space:]]*\{" docker/web/site/style-shared.css && grep -A12 -E "\.agent-pill[[:space:]]*\{" docker/web/site/style-shared.css | grep -q "var(--radius-btn)" && ! grep -A12 -E "\.agent-pill[[:space:]]*\{" docker/web/site/style-shared.css | grep -qiE "#[0-9a-f]{3,6}"</automated>
  </verify>
  <acceptance_criteria>
    - `style-shared.css` contains a `.agent-pill` rule with `border-radius: var(--radius-btn)`, `background: var(--surface)`, and `border: 1px solid var(--line)` — and no literal hex color inside the `.agent-pill` block.
    - `.agent-pill` has NO `border-left:` stripe, NO `cursor: pointer`, NO `transition`, and there is NO `.agent-pill:hover` / `.agent-pill:focus-visible` rule (`grep -c "agent-pill:hover" docker/web/site/style-shared.css` returns 0).
    - `.agent-pill .an` is `var(--mono)` / `13px` / `600` / `color: var(--accent-ink)`; `.agent-pill .ad` is `var(--serif)` / `14px` / `color: var(--ink-soft)`.
    - `.about p` is `var(--serif)` / `18px` / `line-height: 1.62` with `margin-bottom: var(--space-md)` (no raw `16px` literal); a `.body-soft` variant sets `color: var(--ink-soft)`.
    - No new `data-accent` is introduced by the agent-pill rules (single-accent discipline).
  </acceptance_criteria>
  <done>The net-new About prose + agent-pill CSS exists in style-shared.css, fully token-anchored (no literal hex, radius = var(--radius-btn), spacing = --space-* tokens), with single-accent `.an` names and static non-interactive pills.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the #about-view placeholder stub with the real About markup</name>
  <files>docker/web/site/index.html</files>
  <read_first>
    - docker/web/site/index.html — `#about-view` (:79-86): the current `.content-area about-stub` container, `.eyebrow`, `.page-title`, the `.about-lede` placeholder paragraph (with `<em>Full overview coming in Phase 14.</em>`), and the `Back to Newsletter` backlink (:84)
    - .planning/phases/14-about-stub-polish-pass/14-UI-SPEC.md — §Copywriting Contract (the exact draft strings: eyebrow `Behind the Briefing`, title `What is AgentPulse`, page-sub `A newsletter written by a multi-agent system`, the 3 prose paragraphs P1/P2/P3, and the 5-pill roster table) and §Component Inventory (the `.agent-row` > `.agent-pill` > `.an`/`.ad` markup shape)
    - .planning/phases/14-about-stub-polish-pass/14-PATTERNS.md — Pattern Assignment #1 (what to REUSE vs REPLACE in the `#about-view` markup)
    - .planning/phases/14-about-stub-polish-pass/14-CONTEXT.md — D-01/D-02/D-03 (structure + accuracy guardrails + pill roster decision)
  </read_first>
  <action>
    Inside `#about-view .content-area.about-stub` (index.html :79-86), REUSE the wrapper, the `.eyebrow` class, the `.page-title` class, and the backlink. Preserve the `&larr; Back to Newsletter` backlink line (:84) VERBATIM (NAV-03). REPLACE the current content — the title text `A multi-agent intelligence platform…` and the entire `.about-lede` placeholder paragraph including `Full overview coming in Phase 14.` — with the locked structure (use the UI-SPEC §Copywriting Contract draft strings; the operator may reword the prose + `.ad` roles, but the accuracy guardrails below are non-negotiable):

    1. Eyebrow: a `.eyebrow` paragraph reading `Behind the Briefing`.
    2. Page title: a `.page-title` h1 reading `What is AgentPulse`.
    3. Page-sub: a `.page-sub` paragraph reading `A newsletter written by a multi-agent system` (styled by the `.about-stub .page-sub` rule added in Task 1).
    4. Three prose paragraphs in order, inside a `.about` wrapper div so the Task-1 `.about p` / `.body-soft` CSS applies: P1 (primary, `--ink`); P2 with class `body-soft`; P3 with class `body-soft` (the single infra prose sentence covering Gato Brain / LLM Proxy / Web per D-03). Match the exact selector form Task 1 defined.
    5. An `.agent-row` div containing exactly 5 `.agent-pill` blocks, each an `.agent-pill` div with a `.an` child (name) and a `.ad` child (role), in this order: Processor, Analyst, Research, Newsletter, Gato — each `.ad` taken from the UI-SPEC roster table.

    Accuracy guardrails (D-02 — must hold even if the operator rewords): the copy must contain the phrase `eight cooperating services` (not "five agents"); render exactly 5 pills (Gato Brain / LLM Proxy / Web are prose-only in P3, NOT pills); the Processor `.ad` describes it as the background scheduler, NOT a routing orchestrator (routing is Gato Brain's job). The pills are static markup — no `onclick`, no `<a>`, no `href` (UI-SPEC Interaction Contract: informational, non-interactive).

    Do NOT touch `app.js` — the `#/about` route + active-tab wiring already exist. Editorial flag: surface in the SUMMARY that the prose + `.ad` strings are an operator-reviewable draft to finalize before the separate D-06 deploy; structure, 5-pill roster, and the accuracy bar are the locked contract.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -q "Behind the Briefing" docker/web/site/index.html && grep -q ">What is AgentPulse<" docker/web/site/index.html && grep -q "eight cooperating services" docker/web/site/index.html && [ "$(grep -c 'class=.agent-pill.' docker/web/site/index.html)" = "5" ] && grep -q "Back to Newsletter" docker/web/site/index.html && ! grep -q "Full overview coming in Phase 14" docker/web/site/index.html && git diff --quiet docker/web/site/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `#about-view` contains the eyebrow `Behind the Briefing`, the `.page-title` `What is AgentPulse`, the page-sub `A newsletter written by a multi-agent system`, exactly 3 prose paragraphs, and a `.agent-row` with exactly 5 `.agent-pill` blocks (`grep -c` returns 5).
    - The pill names appear in order: Processor, Analyst, Research, Newsletter, Gato — and none of `Gato Brain`, `LLM Proxy`, or `Web` appears as an `.agent-pill` (they are prose-only in P3).
    - The copy contains the literal phrase `eight cooperating services`; the placeholder `Full overview coming in Phase 14` is gone.
    - The `Back to Newsletter` backlink line is preserved verbatim (`grep -q "Back to Newsletter"` passes).
    - The agent-pills carry no `onclick`, no `<a>`, no `href` (static, non-interactive).
    - `app.js` is unchanged — `git diff --quiet docker/web/site/app.js` exits 0 (route already wired).
  </acceptance_criteria>
  <done>The `#about-view` renders the full token-styled About page (eyebrow, title, page-sub, 3 paragraphs, 5 agent-pills, preserved backlink), the copy is accurate ("eight cooperating services", 5 content-agent pills, infra layer in prose), the placeholder is removed, and app.js is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author -> static markup/CSS | This phase adds hand-authored static HTML + CSS only. No user input crosses any boundary on the About view: no form, no network fetch, no query param, no dynamic interpolation. |
| (existing) client -> subscribe API | Pre-existing boundary on the subscribe form (out of scope this plan — POLISH-01 touches its radii in Plan 02, not its behavior). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering (XSS) | `#about-view` About markup + agent-pills | accept | All About prose and agent-pill copy is STATIC author-written HTML in `index.html`, NOT rendered from user input, a network response, or `innerHTML`. No template-injection surface is introduced. The site's existing `escapeHtml` / `safeHttpUrl` model (used elsewhere for dynamic content) is unchanged and not exercised here because nothing dynamic is added. |
| T-14-02 | Information Disclosure | About copy accuracy | accept | The copy describes the system at a public marketing altitude (8 services, 5 content agents). No secrets, endpoints, keys, or internal-only data are exposed — this is intentionally public "What is AgentPulse" content. |
| T-14-SC | Tampering (supply chain) | npm/pip/cargo installs | accept (N/A) | No package-manager installs in this plan. Hand-authored CSS/HTML only, no new dependency, no build step. No legitimacy gate required. |

No `high`-severity threat is introduced by static CSS/markup edits on a read-only public page.
</threat_model>

<verification>
Local / in-code only (no container rebuild, no live deploy — D-06):

1. HTML structure: `grep -c 'class="agent-pill"' docker/web/site/index.html` returns `5`; eyebrow/title/page-sub/3 paragraphs present; placeholder removed; backlink preserved.
2. Accuracy: `grep -q "eight cooperating services" docker/web/site/index.html` passes; no `Gato Brain`/`LLM Proxy`/`Web` rendered as an `.agent-pill`.
3. CSS token discipline: the `.agent-pill` block uses `var(--radius-btn)` / `var(--surface)` / `var(--line)` and no literal hex; `.about p` is serif 18px with `var(--space-md)` bottom margin; single-accent `.an` uses `--accent-ink` with no `data-accent`.
4. No-regression: `git diff --quiet docker/web/site/app.js` exits 0 (route already wired, JS untouched).
5. (The full-cascade radius confirmation grep gate runs in Plan 02, validating the agent-pill `var(--radius-btn)` together with the swept subscribe-form radii.)
</verification>

<success_criteria>
- `#about-view` renders the real About page: eyebrow `Behind the Briefing`, title `What is AgentPulse`, page-sub, 3 reconciled prose paragraphs, and a 5-pill agent row (ABOUT-01, ROADMAP success criterion 1).
- Copy is factually accurate: "eight cooperating services", exactly 5 content-agent pills, Gato Brain / LLM Proxy / Web in prose (D-02/D-03).
- All net-new CSS is token-anchored (no literal hex, radius = `var(--radius-btn)`, spacing = `--space-*`), single-accent, serif-reading / mono-chrome split (TYPE-01/02, COLOR-02).
- `Back to Newsletter` preserved verbatim (NAV-03); `app.js` unchanged.
- No deploy / rebuild performed (D-06).
</success_criteria>

<output>
Create `.planning/phases/14-about-stub-polish-pass/14-01-SUMMARY.md` when done. Flag in the SUMMARY that the About prose + `.ad` role strings are an operator-reviewable draft to finalize before the separate D-06 deploy.
</output>
