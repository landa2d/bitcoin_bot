---
phase: 15-inventory-roster-reconciliation
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - .planning/phases/15-inventory-roster-reconciliation/15-APPROVAL.md
autonomous: false
requirements: [INV-01, INV-02, ROST-01]

must_haves:
  truths:
    - "The contract doc + the per-slug reconciliation plan are presented to the operator for review BEFORE any block is written (SC#4 — the 'read before writing, I approve' gate from EXECUTION_BRIEF §0)."
    - "The operator explicitly approves (or returns changes) — the gate blocks Phase 16 until approval is recorded."
    - "The phase boundary is proven held at the gate: no migration ≥043 applied, no app.js edit, no economy_map write (SC#4 precondition)."
  artifacts:
    - path: ".planning/phases/15-inventory-roster-reconciliation/15-APPROVAL.md"
      provides: "The operator-approval record gating Phase 16 (the read-before-write gate)"
      contains: "approved"
      min_lines: 15
  key_links:
    - from: ".planning/phases/15-inventory-roster-reconciliation/15-APPROVAL.md"
      to: ".planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md"
      via: "the gate presents the contract doc for review"
      pattern: "15-CONTRACT"
    - from: ".planning/phases/15-inventory-roster-reconciliation/15-APPROVAL.md"
      to: ".planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md"
      via: "the gate presents the reconciliation plan for review"
      pattern: "15-RECONCILIATION"
---

<objective>
Present the Phase 15 deliverables (the live contract doc + the per-slug reconciliation plan) to the operator and capture the "read before writing, I approve" decision (SC#4) before any Phase 16 write. The approval is the gate that the spine demands — no `economy_map` write happens until the contract and roster are operator-approved.

Purpose: Satisfy ROADMAP Phase 15 SC#4 and EXECUTION_BRIEF §0 ("Then propose the plan. I'll approve before you write.") and Memory's prod-cutover-discipline ("read before writing, I approve" — boundary-stop validated by operator).

Output: `15-APPROVAL.md` — the recorded operator decision, gating Phase 16. **This plan writes NOTHING to `economy_map`, applies NO migration, edits NO `app.js`.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/docs/EXECUTION_BRIEF.md
@.planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md
@.planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md
@.planning/phases/15-inventory-roster-reconciliation/15-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator-approval gate — present contract + reconciliation plan, record the read-before-write approval (SC#4)</name>
  <files>.planning/phases/15-inventory-roster-reconciliation/15-APPROVAL.md</files>

  <read_first>
  - .planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md (the live contract doc to present — INV-01, INV-02)
  - .planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md (the per-slug reconciliation plan to present — ROST-01)
  - .planning/ROADMAP.md (§Phase 15 — the 4 success criteria the operator checks against)
  - .planning/docs/EXECUTION_BRIEF.md (§0 the read-before-write gate; §5 the open items the operator confirms: cards-vs-prose deferred to Phase 17, first-publish-vs-rewrite per slug, the maturity mismatch)
  </read_first>

  <what-built>
  Two reconciliation documents are assembled from the live system (Plan 15-01):
  - `15-CONTRACT.md` — the live `economy_map` storage + serve contract (block columns + 3-tier CHECK, the 2 append-only triggers and exactly which tables they guard, the atomic `publish_block_version` RPC, the anon published-only RLS, the current hardcoded-`HUB_STORYLINE` serve path) and the verified maturity enum (5 members; `building` is not one; the operator-approved `building→emerging` remap applied at Phase-16 load time).
  - `15-RECONCILIATION.md` — the per-slug disposition for all 9 roster entries (hub first-publish via the D-04 Option-A hub accommodation; the 3 substrate blocks `building→emerging`; `negotiation-coordination` first-published as a new behavior block; the D-03 collision-free sort_order reshuffle to {1..8}; `regulation-legal` kept deferred/body-less at sort_order 8), plus the F-1/F-2/F-3 doc-consistency flags.

  Nothing was written to `economy_map`; no migration applied; no `app.js` edited. The phase boundary held.
  </what-built>

  <how-to-verify>
  1. Read `.planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md` — confirm the contract is documented from the live schema (line-cited to migrations 033/039 + app.js) and the maturity enum / `building→emerging` resolution reads correctly (ROADMAP SC#1, SC#2).
  2. Read `.planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md` — confirm every slug has an explicit disposition, the sort_order reshuffle is collision-free {1..8}, the hub accommodation is pinned to Option A, and the F-2 wording flag (substrate pills will render `emerging`, not `building`) is acknowledged (ROADMAP SC#3).
  3. Confirm the open items from EXECUTION_BRIEF §5 are handled: first-publish-vs-rewrite is decided per slug (all first-publish except regulation kept-deferred); cards-vs-prose-links is explicitly DEFERRED to Phase 17 (not decided here); the maturity mismatch was surfaced, not silently remapped.
  4. Confirm the phase boundary held — run: `git status --porcelain supabase/ docker/web/site/app.js` returns empty (no migration ≥043 applied, no app.js change), and there is no new file under `supabase/migrations/` numbered 043+.
  5. Approve (the read-before-write gate, SC#4), or return specific changes. On approval, the executor writes `15-APPROVAL.md` recording the decision; on changes, it loops back to Plan 15-01 to revise the docs before re-presenting.
  </how-to-verify>

  <action>
  Present the two documents per <how-to-verify> and capture the operator's decision. On approval, write `.planning/phases/15-inventory-roster-reconciliation/15-APPROVAL.md` containing: (a) the approval verdict (`approved` / `changes requested`) and the date; (b) a one-line confirmation that the operator reviewed `15-CONTRACT.md` and `15-RECONCILIATION.md` against ROADMAP Phase 15 SC 1-4; (c) an explicit acknowledgment of flag F-2 (the three substrate pills will render `emerging` (stage 2), not `building` — Phase-17 verification text should expect `emerging`); (d) the confirmation that the phase boundary held (no `economy_map` write, no migration ≥043, no app.js edit); (e) the gate statement that Phase 16 (content load + the D-03 reshuffle + the D-04 Option-A hub-tier migration) is cleared to proceed. **Write NOTHING to `economy_map` and apply NO migration in this task — the only file written is `15-APPROVAL.md` under `.planning/`.**
  </action>

  <resume-signal>Type "approved" to record the gate and clear Phase 16, or describe the specific changes needed to the contract / reconciliation docs (loops back to Plan 15-01).</resume-signal>

  <acceptance_criteria>
  - On approval, the record exists: `test -f .planning/phases/15-inventory-roster-reconciliation/15-APPROVAL.md`.
  - The record names both presented docs: `grep -q "15-CONTRACT" 15-APPROVAL.md` and `grep -q "15-RECONCILIATION" 15-APPROVAL.md`.
  - The verdict is recorded: `grep -Eiq "approved|changes requested" 15-APPROVAL.md`.
  - Flag F-2 acknowledged: `grep -q "emerging" 15-APPROVAL.md` (the substrate-pill wording note).
  - The phase boundary held at the gate (SC#4 precondition): `git status --porcelain supabase/ docker/web/site/app.js` is empty, AND `ls supabase/migrations/ | grep -E '^04[3-9]|^0[5-9][0-9]'` returns nothing (no migration ≥043 applied).
  </acceptance_criteria>

  <done>The operator has reviewed both docs against ROADMAP SC 1-4 and recorded an explicit verdict in `15-APPROVAL.md` (naming both docs, the verdict, the F-2 acknowledgment, and the boundary-held confirmation); no migration ≥043 exists and no app.js / economy_map write occurred — the read-before-write gate (SC#4) is satisfied and Phase 16 is cleared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Phase 16 write authorization | The gate IS the trust boundary control: the operator's explicit approval is the precondition for ANY `economy_map` write (Phase 16). No new technical boundary is crossed in Phase 15. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Repudiation / Tampering | Phase 16 writes proceeding without an operator-approved, documented contract+roster (the spine's "publishing is gated" inverted into "loading is ungated") | mitigate | The gate produces a written, dated approval record (`15-APPROVAL.md`) naming the exact docs reviewed — a non-repudiable read-before-write checkpoint. Phase 16 is blocked until this record exists. This is the in-phase realization of the spine and Memory's prod-cutover-discipline ("read before writing, I approve"). |
| T-15-06 | Tampering | A write (migration ≥043 / app.js edit / economy_map insert) sneaking into Phase 15 under the documentation cover | mitigate | The acceptance criteria assert `git status --porcelain supabase/ docker/web/site/app.js` is empty and no migration ≥043 exists AT THE GATE — a deterministic boundary-held check the operator can run before approving. Any write would fail the gate. |

**Severity assessment:** No high-severity finding. This plan is a checkpoint that records an approval and writes one `.planning/` file. The realistic model: the gate is itself the mitigating control (it enforces read-before-write); the boundary-held assertions make any accidental write loud at the gate. No package installs.
</threat_model>

<verification>
- `test -f .planning/phases/15-inventory-roster-reconciliation/15-APPROVAL.md` (after approval).
- `grep -q "15-CONTRACT" .../15-APPROVAL.md && grep -q "15-RECONCILIATION" .../15-APPROVAL.md` (both docs named).
- `grep -Eiq "approved|changes requested" .../15-APPROVAL.md` (verdict recorded).
- `git status --porcelain supabase/ docker/web/site/app.js` → empty; `ls supabase/migrations/ | grep -E '^04[3-9]'` → empty (boundary held — SC#4 precondition).
</verification>

<success_criteria>
- The contract doc + the reconciliation plan were presented to the operator and reviewed against ROADMAP Phase 15 SC 1-4 (SC#4 — the read-before-write gate).
- An explicit approval verdict is recorded in `15-APPROVAL.md`, naming both docs and acknowledging flag F-2.
- The phase boundary held at the gate: no migration ≥043, no app.js edit, no `economy_map` write — Phase 16 is cleared to proceed only after approval.
</success_criteria>

<output>
Create `.planning/phases/15-inventory-roster-reconciliation/15-02-SUMMARY.md` when done.
</output>
