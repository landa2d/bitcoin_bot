---
phase: 16-content-load-unpublished
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - .planning/phases/16-content-load-unpublished/16-LOAD-EVIDENCE.md
autonomous: false
requirements: [LOAD-01, LOAD-03]
must_haves:
  truths:
    - "All 8 canonical bodies are present in economy_map.block_body_versions as status='draft'"
    - "The anon-visible state (anon-key SELECT) is identical before and after the load — zero new published rows"
    - "Existing live rows for matching slugs are corrected via the canonical-body-rewrite path (new draft), never a raw UPDATE; no duplicate block rows"
    - "All economy_map access in the run uses direct PostgREST + Accept-Profile (no supabase-py .in_())"
  artifacts:
    - path: ".planning/phases/16-content-load-unpublished/16-LOAD-EVIDENCE.md"
      provides: "SC#1 before/after anon-perspective evidence + the loaded-draft inventory + idempotency note"
      contains: "before"
      min_lines: 25
  key_links:
    - from: "scripts/load_economy_map_content.py"
      to: "economy_map.block_body_versions (live)"
      via: "loader run after 043 is applied (FK targets exist)"
      pattern: "block_body_versions"
    - from: "16-LOAD-EVIDENCE.md"
      to: "anon-key SELECT on economy_map"
      via: "before/after published-row count comparison"
      pattern: "anon"
---

<objective>
Run the loader (Plan 02) against the LIVE DB — but only after migration `043` (Plan 01) is applied, so every `block_body_versions.block_slug` FK target (hub + negotiation) exists. Capture the SC#1 before/after anon-perspective evidence proving the load is invisible to visitors (the loaded drafts add zero published rows because anon RLS is `status='published'`), and record the loaded-draft inventory. Confirm the LOAD-03 correction posture (canonical-body-rewrite / no raw UPDATE / no duplicate rows) and the idempotent re-run behavior.

Purpose: This is where the content actually lands in the store (LOAD-01) and where SC#1 ("the live #/map is unchanged for visitors") is proven with a real before/after read (D-07), not RLS reasoning alone. Gated on 043 being live AND the loader existing — hence Wave 2.
Output: drafts in `economy_map.block_body_versions`; `.planning/phases/16-content-load-unpublished/16-LOAD-EVIDENCE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-content-load-unpublished/16-CONTEXT.md
@.planning/phases/16-content-load-unpublished/16-PATTERNS.md
@.planning/phases/16-content-load-unpublished/16-01-SUMMARY.md
@.planning/phases/16-content-load-unpublished/16-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Capture the SC#1 anon-perspective BEFORE snapshot, then run the loader against the live DB</name>
  <what-built>
    Plan 01 applied migration 043 live (hub + negotiation FK targets exist). Plan 02 authored the loader. This task captures the anon-visible baseline, then runs the loader to insert the 8 draft bodies — the FK targets now exist so the inserts succeed.
  </what-built>
  <how-to-verify>
    1. PRECONDITION: confirm 043 is live (per 16-01-SUMMARY: 9-row contiguous sort_order, agent-economy at 0, negotiation-coordination at 5). If not applied, STOP — the loader's FK inserts will fail.
    2. Capture the BEFORE snapshot using the ANON key (not service-role) via direct PostgREST + `Accept-Profile: economy_map`: count published bodies `GET /rest/v1/block_body_versions?status=eq.published&select=block_slug` and the anon-visible blocks `GET /rest/v1/blocks?select=slug,sort_order`. Record both in 16-LOAD-EVIDENCE.md under "BEFORE".
    3. Run the loader once against the live DB from the HOST (NOT a docker-exec — the loader is not copied into the processor container, which has no `/scripts/` path; it needs only SUPABASE_URL/SUPABASE_KEY + outbound HTTPS): `source /root/bitcoin_bot/config/.env && python3 /root/bitcoin_bot/scripts/load_economy_map_content.py` (the loader's DOCS_DIR defaults to `.planning/docs`; run from the repo root `/root/bitcoin_bot` so the default resolves, or pass the absolute fixture/docs path). Confirm it exits 0 and reports 8 drafts inserted (or skipped-if-already-open on re-run).
    4. Verify via service-role read: `GET /rest/v1/block_body_versions?status=eq.draft&select=block_slug` returns the 8 in-scope slugs, each exactly once (no duplicate block rows — the migration-041 UNIQUE-open-draft index backs this).
  </how-to-verify>
  <read_first>
    - scripts/load_economy_map_content.py — the run entrypoint, the DOCS_DIR default, and the HOST-SIDE run command in its module docstring (`source /root/bitcoin_bot/config/.env && python3 /root/bitcoin_bot/scripts/load_economy_map_content.py`)
    - .planning/phases/16-content-load-unpublished/16-01-SUMMARY.md — the applied-043 confirmation (the FK-targets-exist precondition)
    - .planning/phases/16-content-load-unpublished/16-02-SUMMARY.md — the loader HOST-SIDE run command + idempotency posture
    - docker/processor/agentpulse_processor.py lines 3088-3109 — the `_economy_map_get` read shape to model the anon snapshot SELECT (swap the service key for the anon key for the BEFORE/AFTER anon read)
    - .planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md lines 86-96 — the anon RLS boundary (anon sees blocks `USING (true)` but block_body_versions only `status='published'`) — the reason loaded drafts are invisible
  </read_first>
  <acceptance_criteria>
    - Behavior: the loader run (HOST-SIDE: `source /root/bitcoin_bot/config/.env && python3 /root/bitcoin_bot/scripts/load_economy_map_content.py`, NOT a docker-exec) exits 0 and a service-role `GET block_body_versions?status=eq.draft` returns the 8 in-scope slugs (agent-economy, identity-trust, memory-context, payments-settlement, autonomy-control, negotiation-coordination, governance-accountability, psychology-disposition), each present exactly once.
    - Source assertion: the loader run used direct PostgREST (no supabase-py `.in_()`) — confirmed by the Plan-02 loader source (`Content-Profile`/`Accept-Profile`, no `from supabase import`).
    - Evidence: a BEFORE anon-key published-body count is recorded in 16-LOAD-EVIDENCE.md before the loader ran.
    - The substrate trio (identity-trust/memory-context/payments-settlement) drafts carry `proposed_maturity='emerging'` (the building→emerging remap landed), verifiable via `GET block_body_versions?block_slug=eq.identity-trust&status=eq.draft&select=proposed_maturity`.
  </acceptance_criteria>
  <resume-signal>Type "loaded" once the loader exited 0, the 8 drafts are present (each once), and the BEFORE anon snapshot is recorded. Issues → describe them (e.g. an FK failure means 043 was not applied; a "can't open file" error means a docker-exec was used instead of the host-side command).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Capture the SC#1 AFTER snapshot + confirm zero visitor-facing change; record evidence + LOAD-03 posture</name>
  <what-built>
    The 8 drafts are loaded (Task 1). This task captures the anon-visible AFTER snapshot and proves it is identical to BEFORE — the load added zero published rows, so the live #/map is unchanged for visitors (SC#1 / D-07). It also records the LOAD-03 correction posture and the idempotent re-run check.
  </what-built>
  <how-to-verify>
    1. Capture the AFTER snapshot with the ANON key (same two PostgREST reads as BEFORE): published-body count and anon-visible blocks. Record under "AFTER" in 16-LOAD-EVIDENCE.md.
    2. Assert BEFORE == AFTER for the published-body count (zero new published rows) — the loaded drafts are invisible to anon (RLS `status='published'`). Note: the anon `blocks` count legitimately rose by 2 (hub + negotiation) from migration 043 — that is structure, not body content, and is expected; document it as such (anon sees block rows `USING (true)`, but those new rows have no published body, so #/map renders no new content).
    3. LOAD-03 posture: confirm NO raw UPDATE was issued against any append-only body column and NO duplicate block_body_versions draft exists per slug (the migration-041 UNIQUE-open-draft index is the structural backstop). Where an existing slug had a prior published body, the correction is a NEW draft (canonical-body-rewrite), to be published in Phase 18 — record which slugs (if any) already had a published body vs. which are first-publish.
    4. Idempotency check: re-run the loader once (HOST-SIDE: `source /root/bitcoin_bot/config/.env && python3 /root/bitcoin_bot/scripts/load_economy_map_content.py`); confirm it exits 0 and SKIPS all 8 (skip-if-open-draft) — no second draft per slug, no 23505 surfacing as a crash. Record the re-run result.
  </how-to-verify>
  <read_first>
    - .planning/phases/16-content-load-unpublished/16-LOAD-EVIDENCE.md — the BEFORE snapshot recorded in Task 1
    - .planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md — per-slug disposition (which slugs are first-publish vs body-rewrite) so the LOAD-03 posture record is accurate
    - .planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md lines 50-96 — the append-only trigger guard (no raw UPDATE on body columns) and the anon RLS read boundary
    - supabase/migrations/041_block_body_versions_unique_open_draft.sql — the one-open-draft UNIQUE index (the no-duplicate-draft backstop)
  </read_first>
  <acceptance_criteria>
    - Evidence: 16-LOAD-EVIDENCE.md shows the BEFORE and AFTER anon-key published-body counts are IDENTICAL (zero new published rows) — SC#1 proven by a real before/after read, not RLS reasoning alone.
    - Evidence: the document explicitly notes the anon `blocks` count rose by exactly 2 (hub + negotiation, structure from 043) and explains those rows render no new content because they have no published body.
    - Behavior: re-running the loader (HOST-SIDE command) exits 0 and inserts zero new drafts (all 8 skipped via skip-if-open-draft) — no duplicate draft per slug.
    - Source/record assertion: 16-LOAD-EVIDENCE.md records that corrections used the canonical-body-rewrite path (new draft) and NO raw UPDATE was issued on any append-only body column (LOAD-03).
  </acceptance_criteria>
  <resume-signal>Type "verified" once BEFORE==AFTER published counts are recorded, the +2 structural blocks delta is explained, the re-run skip is confirmed, and the LOAD-03 posture is documented. Issues → describe them.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader (service_role) → live economy_map | The live run writes draft bodies with the service-role key (bypasses RLS — the historical failure actor). Must stay status='draft'. |
| anon reader → economy_map (RLS) | The before/after anon snapshot reads as anon to prove the visitor-facing surface is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-RUN | Elevation of Privilege | service_role live draft write | accept | The loader omits `status` → rows default to `'draft'`, invisible to anon (RLS `status='published'`). SC#1 before/after anon read proves the privileged write adds zero published rows. |
| T-16-DUP | Tampering / integrity | duplicate or partial draft load | mitigate | Idempotent skip-if-open-draft + the migration-041 UNIQUE-open-draft index (raises 23505) guarantee at most one open draft per slug; the re-run check confirms no duplicate. A mid-batch transport failure is re-runnable to completion (D-04 validates-all-first, so no validation partial). |
| T-16-UPD | Tampering | raw UPDATE on append-only body | mitigate | LOAD-03 corrections go via canonical-body-rewrite (new draft → publish in Phase 18), never a raw UPDATE — the append-only trigger would reject a body UPDATE anyway; the evidence record asserts no raw UPDATE was issued. |
| T-16-VIS | Information disclosure | premature visitor exposure | mitigate | Anon RLS is `status='published'`; draft load is invisible until the Phase-18 publish RPC. The D-07 before/after anon read is the explicit proof, not reasoning alone. |
</threat_model>

<verification>
- 8 in-scope slugs present as draft in `block_body_versions`, each exactly once; substrate trio carry `proposed_maturity='emerging'`.
- 16-LOAD-EVIDENCE.md: BEFORE == AFTER anon published-body counts; the +2 anon blocks delta (structure) explained; re-run skip confirmed; LOAD-03 canonical-body-rewrite posture recorded.
- All economy_map access via direct PostgREST + Accept-Profile/Content-Profile (no `.in_()`).
- The loader was run HOST-SIDE (`source /root/bitcoin_bot/config/.env && python3 /root/bitcoin_bot/scripts/load_economy_map_content.py`), not via docker-exec.
</verification>

<success_criteria>
- LOAD-01: all 8 canonical bodies present in economy_map as unpublished drafts, frontmatter metadata truth, building→emerging applied — and the live #/map is unchanged for visitors (SC#1, proven by the before/after anon read).
- LOAD-03: existing rows corrected via canonical-body-rewrite (no raw UPDATE), no duplicate block rows, idempotent re-run confirmed.
- ROADMAP SC#1 and SC#4 satisfied for the load (SC#2 is Plan 02's negative test; SC#3 is the LOAD-03 posture recorded here).
</success_criteria>

<output>
Create `.planning/phases/16-content-load-unpublished/16-03-SUMMARY.md` when done. Reference 16-LOAD-EVIDENCE.md and record: the 8 loaded slugs + their proposed_maturity, the BEFORE==AFTER anon published count, the re-run skip result, the HOST-SIDE run command used, and the LOAD-03 per-slug first-publish-vs-rewrite disposition.
</output>
</content>
</invoke>
