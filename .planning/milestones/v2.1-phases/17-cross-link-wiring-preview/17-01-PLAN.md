---
phase: 17-cross-link-wiring-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/web/site/app.js
autonomous: true
requirements: [LINK-01, PREV-01, HUB-01]
must_haves:
  truths:
    - "With PREVIEW_ENABLED set + service_role read, an unpublished block page (e.g. #/map/identity-trust) renders its draft body via marked.parse"
    - "With PREVIEW_ENABLED set, the #/map hub renders the hub draft body intro (thesis + How-to-read framing) ABOVE the existing card grid"
    - "The hub does NOT render the Tier-1/Tier-2 prose block-list (it appears once, as the cards)"
    - "marked.parse'd draft block bodies emit the 15 in-body #/map/<slug> cross-links as real <a href> elements (LINK-01)"
    - "With the anon key + PREVIEW_ENABLED unset (production), #/map and every #/map/<slug> render byte-for-byte identically to pre-change (double-safe no-op)"
    - "Maturity pills render emerging / contested / nascent unchanged (no pill code touched)"
  artifacts:
    - path: "docker/web/site/app.js"
      provides: "PREVIEW_ENABLED flag; D-03 draft-fetch fallback in loadBlock + a hub-body fetch in loadHub/renderHub; D-06c prose-block-list trim"
      contains: "PREVIEW_ENABLED"
  key_links:
    - from: "docker/web/site/app.js loadBlock"
      to: "economy_map.block_body_versions (status='draft')"
      via: "sb.schema('economy_map') draft-fetch fallback, gated by PREVIEW_ENABLED"
      pattern: "block_body_versions[\\s\\S]*status[\\s\\S]*draft"
    - from: "docker/web/site/app.js renderHub"
      to: "marked.parse(trimmedHubBody)"
      via: "hub-intro render replacing the HUB_STORYLINE line, with HUB_STORYLINE fallback"
      pattern: "marked\\.parse"
---

<objective>
Add the content-scoped render path to `docker/web/site/app.js` that makes the Phase-16 loaded-but-unpublished bodies renderable on a local preview, WITHOUT changing the live published site for anon visitors and WITHOUT any new UI feature/route/component.

Three coupled edits, all on `app.js` (the only file touched), each reusing an existing in-file pattern (no net-new capability):
- **D-04** — a single module-scoped `PREVIEW_ENABLED` boolean read from a URL/hash param (the `getInitialMode()` idiom), gating the draft fetches. Ships DORMANT in prod.
- **D-03** — a read-only draft-fetch fallback in `loadBlock` (and a hub-body fetch in `loadHub`/`renderHub`): when `current_body_version_id` is NULL AND `PREVIEW_ENABLED`, fetch the latest `status='draft'` `block_body_versions` row for that slug and `marked.parse` it. No DB writes.
- **D-06a/c** — `renderHub` renders the hub draft body as the intro (replacing the `HUB_STORYLINE` constant, with graceful fallback to it), and code-trims the body's Tier-1/Tier-2 prose block-list so it is NOT duplicated by the cards (HUB-01).

Purpose: prove the loaded content is publish-ready (LINK-01 cross-links resolve, PREV-01 renders, HUB-01 single block list) before Phase 18 publishes — while the deployed `app.js` stays a byte-for-byte no-op (double-safe: no flag set AND published-only RLS).
Output: a modified `docker/web/site/app.js` whose new render path is dormant in production and active only behind the explicit preview flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-cross-link-wiring-preview/17-CONTEXT.md
@.planning/phases/17-cross-link-wiring-preview/17-PATTERNS.md
@.planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md
@.planning/docs/00-hub.md
@docker/web/site/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dormant PREVIEW_ENABLED flag (D-04)</name>
  <read_first>
    - docker/web/site/app.js lines 28-58 (the Phase-4 constants block + the getInitialMode() URL-param idiom — the established "read a flag off the URL" pattern to mirror)
    - .planning/phases/17-cross-link-wiring-preview/17-CONTEXT.md `<decisions>` D-04 (dormant-in-prod, double-safe rationale)
    - .planning/phases/17-cross-link-wiring-preview/17-PATTERNS.md §"D-04 — Dormant preview flag" (the getInitialMode analog at :49-58)
  </read_first>
  <action>
    Define a single module-scoped `const PREVIEW_ENABLED` near the other Phase-4 economy-map constants (around app.js:28-46, alongside `HUB_STORYLINE` / `MATURITY_STAGE`). Resolve it from a URL/hash param using the SAME idiom as `getInitialMode()` at app.js:49-58 — `new URL(window.location).searchParams.get('preview')` — set `PREVIEW_ENABLED = (the param value is '1' or 'true')`. It MUST default to `false` when the param is absent. This is a plain boolean read; do NOT add a new route, view, component, or `showView` branch. This single flag gates BOTH draft fetches added in Tasks 2 and 3 (block + hub). The double-safe property (D-04): in production the param is unset (flag false) AND published-only RLS independently returns no draft — either alone makes the new path a no-op.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node -e "const fs=require('fs');const s=fs.readFileSync('docker/web/site/app.js','utf8');const c=s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n');if(!/PREVIEW_ENABLED/.test(c)){console.error('FAIL: PREVIEW_ENABLED not defined');process.exit(1)};if(!/searchParams\.get\(\s*['\"]preview['\"]\s*\)/.test(c)){console.error('FAIL: preview param not read via searchParams');process.exit(1)};console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app.js` defines a module-scoped `PREVIEW_ENABLED` constant resolved from `new URL(window.location).searchParams.get('preview')`, defaulting to `false` when the param is absent.
    - Source: no new `case` in `route()`, no new `showView` argument, no new view div — the flag is a boolean read only.
    - Behavior: loading any route WITHOUT `?preview=1` leaves `PREVIEW_ENABLED` false (verifiable by the grep gate above plus reading the default branch).
  </acceptance_criteria>
  <done>`PREVIEW_ENABLED` exists as a module-scoped boolean read from the `preview` URL param via the existing `searchParams.get` idiom, defaulting to false; no new route/view/component added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the read-only draft-fetch fallback in loadBlock (D-03, LINK-01)</name>
  <read_first>
    - docker/web/site/app.js lines 505-560 (loadBlock — specifically the existing conditional published-body fetch at :535-543 to extend, and the graceful-degrade timeline posture at :531-533)
    - docker/web/site/app.js lines 562-605 (renderBlock — confirm :584-587 already hides the body section when bodyMd is falsy and marked.parse's it when present; NO render-side change needed)
    - .planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md §"Body storage" (block_body_versions columns: block_slug, body_md, status TEXT CHECK draft/published/superseded, created_at) and §"Migration-041 invariant" (at most ONE open draft per slug)
    - .planning/phases/17-cross-link-wiring-preview/17-PATTERNS.md §"D-03 — Read-only draft-fetch fallback" (the exact shape + the 3 planner notes) and §"supabase-js schema isolation (frontend) — NOT the Python httpx path"
  </read_first>
  <action>
    In `loadBlock`, AFTER the existing `if (blockRes.data.current_body_version_id) { ... }` published-body fetch (app.js:540-543), add a fallback: when `bodyMd` is still null AND `PREVIEW_ENABLED`, fetch the draft body for the slug. Use the frontend supabase-js idiom `sb.schema('economy_map').from('block_body_versions')` (which sets `Accept-Profile` automatically per D-16) — do NOT hand-build a PostgREST httpx call (that is the Phase-16 Python loader pattern; the browser uses supabase-js). Select `body_md`, filter `.eq('block_slug', slug).eq('status', 'draft')`, `.order('created_at', { ascending: false })`, `.limit(1)`. The `created_at` column is the confirmed append-ordering column (15-CONTRACT §"Body storage" — `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`); migration 041 already guarantees at most ONE open draft per slug, so `.limit(1)` returns exactly 0 or 1 row. Use `.limit(1)` + array result (NOT `.single()`) so zero drafts returns cleanly empty rather than erroring — assign `bodyMd = draftRes.data[0].body_md` only when `!draftRes.error && draftRes.data && draftRes.data.length`. Graceful-degrade: any error or empty result leaves `bodyMd = null`, matching the existing timeline (:531-533) and published-body (:542) posture — never throw. IMPORTANT: this `.eq('status','draft')` is the deliberate, flag-gated INVERSE of the D-17-forbidden defensive `.eq('status','published')` filter — it is functional scoping for the preview-only draft path and is only reachable behind `PREVIEW_ENABLED`; leave the existing published-body fetch's no-status-filter posture (D-17) unchanged. Do NOT modify `renderBlock` — it already conditionally renders `marked.parse(bodyMd)` at :584-587, which is where the 15 block→block `#/map/<slug>` cross-links become real `<a href>` elements (LINK-01).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node -e "const fs=require('fs');const s=fs.readFileSync('docker/web/site/app.js','utf8');const c=s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n');const ok=/PREVIEW_ENABLED/.test(c) && /block_body_versions/.test(c) && /eq\(\s*['\"]block_slug['\"]/.test(c) && /eq\(\s*['\"]status['\"]\s*,\s*['\"]draft['\"]\s*\)/.test(c) && /order\(\s*['\"]created_at['\"]/.test(c) && /limit\(\s*1\s*\)/.test(c);if(!ok){console.error('FAIL: draft-fetch fallback missing required filters');process.exit(1)};if(/\.single\(\)[\s\S]{0,40}status[\s\S]{0,10}draft/.test(c)){console.error('FAIL: draft path uses .single() not .limit(1)+array');process.exit(1)};console.log('PASS')"</automated>
    <automated>cd /root/bitcoin_bot && node -e "require('fs').readFileSync('docker/web/site/app.js','utf8');new Function(require('fs').readFileSync('docker/web/site/app.js','utf8').replace(/^/, 'var window={location:{hash:\x27\x27},supabase:{createClient:function(){return{}}}};var localStorage={getItem:function(){},setItem:function(){}};var document={getElementById:function(){return null},querySelector:function(){return null},body:{classList:{add:function(){},remove:function(){},toggle:function(){}}}};'));console.log('PASS: app.js parses')" 2>&1 | tail -1 || echo "SYNTAX-NOTE: parse-check is advisory; the real gate is the verification harness in 17-02"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app.js` `loadBlock` contains a draft-fetch fallback querying `block_body_versions` with `.eq('block_slug', slug)`, `.eq('status','draft')`, `.order('created_at', {ascending:false})`, `.limit(1)`, reached only when `!bodyMd && PREVIEW_ENABLED`.
    - Source: the draft path uses `sb.schema('economy_map')` supabase-js (NOT httpx/PostgREST hand-built headers).
    - Source: the draft path uses `.limit(1)` + array indexing, NOT `.single()` (zero-draft in prod returns empty cleanly).
    - Source: the pre-existing published-body fetch at :540-543 retains its no-`.eq('status','published')` posture (D-17 unchanged); `renderBlock`'s body section at :584-587 is unmodified.
    - Behavior (proven in 17-02): on the local service_role preview container with `?preview=1`, GET `#/map/identity-trust` renders a non-empty `marked.parse`'d body and the in-body cross-links appear as `<a href="#/map/...">`.
    - No-op proof (proven in 17-02): with the anon key and `PREVIEW_ENABLED` unset, `#/map/<slug>` renders identically to pre-change (body-less for unpublished slugs).
  </acceptance_criteria>
  <done>`loadBlock` has a flag-gated, RLS-safe draft-fetch fallback using supabase-js `.limit(1)` that leaves `bodyMd` null on any miss; `renderBlock` is untouched; production behavior is unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Render the hub draft body intro + trim the prose block-list in renderHub (D-06a/c, HUB-01)</name>
  <read_first>
    - docker/web/site/app.js lines 410-503 (loadHub query at :419-423 — already selects current_body_version_id; renderHub html assembly at :493-499 with the HUB_STORYLINE line at :496; the tier filters at :450-452 already exclude tier='hub')
    - .planning/docs/00-hub.md (the loaded hub body — the EXACT trim cut-points: KEEP L9-27 = the thesis paragraphs + `## How to read this map`; TRIM L31-55 = `## Tier 1 — The Substrate` / `## Tier 2 — The Behavior` prose block-list with the 7 `[Title →](#/map/<slug>)` links; OPTIONAL keep L58-60 = `## The thesis, restated`)
    - .planning/phases/17-cross-link-wiring-preview/17-PATTERNS.md §"D-06a/c — renderHub hub-intro render + prose block-list trim" (the exact html-assembly site + the suggested deterministic split on the literal headings)
    - .planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md §"Hub serve path (current)" (hub NOT DB-served today; renders escapeHtml'd HUB_STORYLINE; hub blocks row has tier='hub' excluded from grids — no hub card)
  </read_first>
  <action>
    Make `loadHub`/`renderHub` fetch and render the hub draft body as the intro, replacing the `HUB_STORYLINE` line, with graceful fallback to `HUB_STORYLINE`. (1) FETCH: in `loadHub` (or at the top of `renderHub`), when `PREVIEW_ENABLED`, fetch the hub draft body the SAME way as Task 2's D-03 fallback but for slug `'agent-economy'` (`.eq('block_slug','agent-economy').eq('status','draft').order('created_at',{ascending:false}).limit(1)`). Graceful-degrade to null on any error/empty. (2) TRIM (D-06c): before `marked.parse`, operate on the markdown STRING (simpler + safer than post-render DOM surgery). Split the hub body so it keeps the head — everything from the start through the end of the `## How to read this map` section — and DROPS the Tier-1/Tier-2 prose block-list. Use a deterministic cut on the literal heading `## Tier 1` (drop from that heading onward), and OPTIONALLY re-append the closing `## The thesis, restated` tail by splitting on that literal heading and keeping it (Claude's Discretion default keeps the restated thesis). Implement as a small pure helper, e.g. `trimHubBody(md)`, that returns the head (and optional restated-thesis tail) joined — match the literal headings `## Tier 1` and `## The thesis, restated`. Do NOT edit `00-hub.md` (the rejected variant) — the trim is in code so the loaded draft stays untouched and reversible. (3) RENDER: replace the `'<div class="hub-storyline">' + escapeHtml(HUB_STORYLINE) + '</div>'` line (app.js:496) with: when a trimmed hub body is available, `'<div class="hub-storyline">' + marked.parse(trimmedHubBody) + '</div>'`; otherwise fall back to the existing `escapeHtml(HUB_STORYLINE)` form (P15-D-04 graceful fallback pre-publish). Reuse the existing `marked.parse` (same path as :586 / :250) — no new capability. Do NOT touch the card grid: `renderTile` (:467) already emits `<a href="#/map/<slug>">` for hub→block click-through (LINK-01), and the three tier filters already exclude the `tier='hub'` row so no hub card is rendered. The result satisfies HUB-01: thesis + two-tier framing intro above the grid, block list appearing once (cards).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node -e "const fs=require('fs');const s=fs.readFileSync('docker/web/site/app.js','utf8');const c=s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n');if(!/agent-economy/.test(c)){console.error('FAIL: hub draft not fetched by slug agent-economy');process.exit(1)};if(!/marked\.parse/.test(c)){console.error('FAIL: marked.parse not used for hub intro');process.exit(1)};if(!/HUB_STORYLINE/.test(c)){console.error('FAIL: HUB_STORYLINE fallback removed (must remain as graceful fallback)');process.exit(1)};if(!/Tier 1/.test(c)){console.error('FAIL: trim cut-point literal heading \x27## Tier 1\x27 not referenced');process.exit(1)};console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `renderHub` (or a helper it calls) `marked.parse`'s a trimmed hub draft body for the intro and FALLS BACK to `escapeHtml(HUB_STORYLINE)` when no hub draft is available — `HUB_STORYLINE` is NOT deleted.
    - Source: the trim helper drops the markdown from the literal `## Tier 1` heading through the Tier-2 block-list (optionally re-appending `## The thesis, restated`), operating on the markdown string before `marked.parse`.
    - Source: the hub draft is fetched for slug `'agent-economy'` with `.eq('status','draft')` behind `PREVIEW_ENABLED`, via supabase-js `sb.schema('economy_map')`.
    - Source: `renderTile` (:467) and the tier filters (:450-452) are unchanged — no hub card, cards still emit `<a href="#/map/<slug>">`.
    - Behavior (proven in 17-02): on the preview container, `#/map` shows the thesis + "How to read this map" framing as the intro AND the card grid, with NO Tier-1/Tier-2 prose link list duplicated.
    - No-op proof (proven in 17-02): with the anon key and `PREVIEW_ENABLED` unset, `#/map` renders the existing `HUB_STORYLINE` text exactly as today (no hub draft fetched, no trim applied).
  </acceptance_criteria>
  <done>`renderHub` renders the trimmed hub draft body intro via `marked.parse` when the preview flag + draft are present and falls back to `HUB_STORYLINE` otherwise; the prose block-list is trimmed in code; the card grid is unchanged; production renders identically to today.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser anon client → economy_map (RLS) | the deployed app.js runs with the anon key; RLS `block_body_versions USING(status='published')` is the read boundary — drafts are invisible to anon |
| local preview container (service_role) → economy_map | the LOCAL-ONLY preview substitutes the service_role key (RLS bypass) to read drafts; blast radius is the operator's own browser (handled in 17-02) |
| draft markdown → marked.parse → DOM | untrusted-ish body markdown is rendered without a sanitizer (carried disposition T-04-03-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SVC-ROLE-LEAK | Elevation of Privilege / Info Disclosure | the service_role key substituted into the LOCAL preview container | mitigate | This plan introduces NO key value into `app.js` — lines 4-5 keep the `__SUPABASE_URL__` / `__SUPABASE_ANON_KEY__` placeholders (env-time substitution, never committed). The service_role substitution + the mandatory deployed-app.js `/diff` guard (service_role key never in tracked files / deployed image) is owned by Task 1 of plan 17-02. Acceptance criterion here: this plan's edits add no literal key. |
| T-17-XSS-MD | Tampering (XSS via markdown) | `marked.parse(bodyMd)` on draft block bodies + the hub intro | accept | Carried disposition T-04-03-01: same `marked.parse` path as the live `renderBlock`/`renderArticle`; compensating control = the operator publish gate (Phase 18 — only operator-approved content ever publishes). The preview is local-only (17-02), so any malicious draft's blast radius is the operator's own browser. No sanitizer added (matches existing precedent). |
| T-17-DRAFT-EXPOSE | Information Disclosure (unpublished content leaking to anon visitors) | the D-03 draft-fetch path shipping in the deployed app.js | mitigate | Double-safe (D-04): in prod the `PREVIEW_ENABLED` flag is unset (param absent → false) AND published-only RLS (`033:367-370`) independently returns no draft to the anon key. Either alone makes the path a no-op. Acceptance criteria require the flag to default false and the published-body fetch posture to remain unchanged; the no-op proof is verified on the deployed-key path in 17-02. |
</threat_model>

<verification>
- All three `app.js` grep/parse gates above pass (PREVIEW_ENABLED defined + defaulting false; draft-fetch with the right filters + `.limit(1)` not `.single()`; hub intro via `marked.parse` with `HUB_STORYLINE` fallback + the `## Tier 1` trim cut-point).
- `git diff docker/web/site/app.js` touches ONLY `app.js` and contains NO literal Supabase key value — lines 4-5 still hold the `__SUPABASE_URL__` / `__SUPABASE_ANON_KEY__` placeholders.
- No migration / schema / RLS / RPC / pipeline / proxy / agent-service file is modified (only `docker/web/site/app.js` appears in the diff).
- The behavioral + no-op + service_role-leak-guard proofs are executed in plan 17-02 against the running local preview container and the deployed-key path.
</verification>

<success_criteria>
- `docker/web/site/app.js` is the ONLY file modified.
- `PREVIEW_ENABLED` ships dormant (defaults false); the D-03 draft-fetch (block + hub) is gated behind it and uses supabase-js `.eq('status','draft').limit(1)`.
- `renderHub` renders the trimmed hub draft intro via `marked.parse` with graceful `HUB_STORYLINE` fallback; the Tier-1/Tier-2 prose block-list is trimmed in code (HUB-01: block list once, as cards).
- The deployed-key production path is a byte-for-byte no-op (double-safe); no DB write, no schema/RLS change; the service_role key value appears nowhere in `app.js`.
</success_criteria>

<output>
Create `.planning/phases/17-cross-link-wiring-preview/17-01-SUMMARY.md` when done.
</output>
