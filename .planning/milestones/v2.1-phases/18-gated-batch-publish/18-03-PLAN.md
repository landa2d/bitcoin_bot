---
phase: 18-gated-batch-publish
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - docker/web/entrypoint.sh
  - docker/web/Dockerfile
  - docker/docker-compose.yml
autonomous: false
requirements: [PUB-01]
must_haves:
  truths:
    - "The new app.js (from plan 18-01) is deployed live to prod via the scoped agentpulse-web rebuild FIRST, and verified as a visual no-op pre-publish (hub still HUB_STORYLINE, no new published blocks) before any content goes live (D-04)."
    - "On a single operator approval of the printed manifest, the publish batch flips all 8 in-scope drafts live (7 blocks first, hub last) via the existing publish_block_version RPC, halting loud on any failure (D-06/D-07/D-08)."
    - "Programmatic anon-key verification confirms post-publish: all 8 in-scope bodies status='published', the hub renders its published article at #/map, every #/map/<slug> cross-link resolves against PUBLISHED content, and the published-block count is 8 (+ hub) up from 2 (D-05)."
  artifacts:
    - path: "docker/web/entrypoint.sh"
      provides: "Scoped agentpulse-web build/serve context (touched only if the deploy requires it; otherwise unchanged)"
  key_links:
    - from: "scoped agentpulse-web rebuild (main tree)"
      to: "live prod docker/web/site/app.js"
      via: "docker compose up -d --build web (the web service builds the agentpulse-web container) from the absolute main-tree path"
      pattern: "agentpulse-web"
    - from: "operator-approved manifest"
      to: "economy_map.publish_block_version (8 drafts)"
      via: "scripts/publish_economy_map_batch.py run by the orchestrator after approval"
      pattern: "publish_block_version"
---

<objective>
Execute the gated go-live: ship the plan-18-01 `app.js` change to prod via the scoped `agentpulse-web` rebuild FIRST (proving it is a visual no-op pre-publish per D-04), then on a SINGLE operator approval run `scripts/publish_economy_map_batch.py` to flip all 8 in-scope drafts live (7 blocks first, hub last), then run `scripts/verify_economy_map_publish.py` to prove the published result from the anon perspective (D-05).

Purpose: This is the single clean cutover the whole v2.1 milestone built toward (PUB-01). Deploy-first means the renderer change is provably safe before any content flips live; the operator's manifest approval is the human gate; the publish RPC is atomic and idempotent; verification is programmatic.

CRITICAL EXECUTION CONSTRAINTS:
- This plan is NOT worktree-safe. The scoped `docker compose ... up -d --build web` (the web service builds the `agentpulse-web` container) MUST run from the absolute main-tree path `/root/bitcoin_bot` (MEMORY: scoped_rebuild_worktree_unsafe — a worktree executor would build stale code). Run no-worktree / sequential.
- The LIVE actions — the scoped rebuild AND the publish-batch RPC run — are owned by the ORCHESTRATOR (the in-chat operator approves the manifest; the orchestrator runs the live deploy + publish), NOT a worktree executor. The operator-approval checkpoint surfaces the manifest in-chat (mirrors the Phase-16 migration-apply + loader approval pattern, D-06).
- This is `autonomous: false`. Task 2 is the single blocking operator-approval gate.

Output: prod `#/map` renders the published hub article + 7 published block pillars + the 2 superseded blocks' new bodies; verification harness exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-gated-batch-publish/18-CONTEXT.md
@.planning/phases/18-gated-batch-publish/18-PATTERNS.md
@.planning/phases/18-gated-batch-publish/18-01-SUMMARY.md
@.planning/phases/18-gated-batch-publish/18-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy-first — scoped agentpulse-web rebuild from the main tree + service_role-leak pre-gate</name>
  <files>docker/web/entrypoint.sh, docker/web/Dockerfile, docker/docker-compose.yml (touched ONLY if the scoped rebuild requires it; expected diff is none — listed for ownership/serialization)</files>
  <read_first>
    - .planning/phases/18-gated-batch-publish/18-01-SUMMARY.md (the app.js change being shipped — confirm it landed and what the diff was)
    - docker/web/entrypoint.sh + docker/web/Dockerfile + docker/docker-compose.yml (the scoped web service build/serve context; the compose service key is `web` at docker-compose.yml line 358, container_name `agentpulse-web` at line 362; entrypoint.sh sed-substitutes __SUPABASE_URL__/__SUPABASE_ANON_KEY__ at container start — MEMORY: web_static_preview_substitution)
    - scripts/verify_economy_map_crosslinks.py :367-474 (run_svc_role_guard) and :345-351 (WEB_DEPLOY_PATH_FILES) — the --guard-only pre-deploy safety gate
    - CLAUDE.md (Build & Run Commands — the scoped rebuild command; MEMORY: deploy_scoped_and_approved — branch + diff + scoped, never a blind scripts/deploy.sh)
  </read_first>
  <action>
    BEFORE the rebuild, run the service_role-leak pre-gate `python3 scripts/verify_economy_map_crosslinks.py --guard-only` from `/root/bitcoin_bot` — it must exit 0 (placeholders intact in the tracked app.js; service_role key in NONE of the web-deploy-path files). If it fails, HALT (do not deploy). Then run the scoped rebuild from the ABSOLUTE main-tree path: `cd /root/bitcoin_bot/docker && docker compose up -d --build web` (the runnable compose SERVICE argument is `web` — the docker-compose.yml service key at line 358 — which builds the `agentpulse-web` container; scoped to the single web service, never a blanket `scripts/deploy.sh`, MEMORY: deploy_scoped_and_approved). Touch `docker/web/entrypoint.sh` / `Dockerfile` / `docker-compose.yml` ONLY if the rebuild requires it (the app.js change should need no build-config change — these files are listed as files_modified for ownership/serialization, but the expected diff is none). After the container is up, verify the deploy is a VISUAL NO-OP pre-publish (D-04): an anon read confirms the hub `agent-economy.current_body_version_id` is still NULL (so prod renders `HUB_STORYLINE`, not the published article) and the anon published-block count is still 2 (`identity-trust`, `governance-accountability`) — nothing newly visible. This must run from the main tree, orchestrator-owned; a worktree executor would build stale code (MEMORY: scoped_rebuild_worktree_unsafe).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 scripts/verify_economy_map_crosslinks.py --guard-only && docker compose -f docker/docker-compose.yml ps web --format '{{.State}}' | grep -q running && echo DEPLOY-UP-OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify_economy_map_crosslinks.py --guard-only` exits 0 (PLACEHOLDER-INTACT + service_role not in the web-deploy path) — the pre-deploy gate.
    - The `agentpulse-web` container is running after the scoped rebuild (`docker compose ps web` — the `web` service builds the `agentpulse-web` container — shows running; prints DEPLOY-UP-OK).
    - A pre-publish anon read confirms the hub `current_body_version_id` is still NULL and the anon published-block count is still 2 — the deploy is a visual no-op (D-04). (Running `python3 scripts/verify_economy_map_publish.py` here is EXPECTED to FAIL reporting count 2, NOT 8 — proving nothing is published yet; this confirms the pre-publish state, it does not gate this task.)
    - The rebuild ran from `/root/bitcoin_bot` (main tree), scoped to the `web` service (the `agentpulse-web` container) only — not a blanket deploy.
  </acceptance_criteria>
  <done>
    The plan-18-01 app.js is live in the running `agentpulse-web` container; the leak pre-gate passed; prod is a verified visual no-op pre-publish (hub still HUB_STORYLINE, count still 2). Ready for the gated publish.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Single operator-approval gate — review the publish manifest</name>
  <files>scripts/publish_economy_map_batch.py (run --dry-run to print the manifest; no file is modified by this task)</files>
  <action>
    The ORCHESTRATOR runs `python3 /root/bitcoin_bot/scripts/publish_economy_map_batch.py --dry-run` from the main tree and surfaces the printed manifest (slug -> version_id -> old_maturity->proposed_maturity, in publish order) in-chat for the SINGLE operator approval (D-06; mirrors the Phase-16 migration-apply + loader approval pattern). The --dry-run also exercises the D-08 pre-flight: if any of the 8 expected open drafts is missing the script HALTs (nonzero exit) BEFORE this approval and the gate does not proceed. Do NOT run the live publish until the operator types "approved". This task performs no DB write and no deploy.
  </action>
  <decision>Approve publishing the 8 in-scope economy_map drafts (hub agent-economy + 7 blocks) live, in ONE batch?</decision>
  <context>
    The deploy-first app.js change is live and verified as a visual no-op (Task 1). This is the single go-live gate (D-06): on approval the batch flips all 8 drafts to status='published' via the atomic publish_block_version RPC (7 blocks first, hub last — D-07). The 2 already-published blocks (identity-trust, governance-accountability) are superseded to their new Phase-16 bodies. regulation-legal stays deferred (excluded). This is the LAST gate of the v2.1 milestone — there is no separate operator manual live walk-through (the Phase-17 preview click-through already covered the content-identical drafts; post-publish verification is programmatic, D-05).
  </context>
  <how-to-verify>
    1. Orchestrator: from `/root/bitcoin_bot`, run `python3 scripts/publish_economy_map_batch.py --dry-run` and paste the manifest.
    2. Operator: confirm the manifest lists exactly 8 slugs (the 7 blocks then agent-economy last), each with a resolved version_id and an old->proposed maturity, and that regulation-legal is absent.
    3. Operator: confirm the pre-flight reported all 8 drafts resolved (no missing-draft halt).
  </how-to-verify>
  <resume-signal>Type "approved" to run the live publish batch, or describe the issue (the orchestrator halts and does not publish).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run the live publish batch + programmatic post-publish verification (D-05)</name>
  <files>scripts/publish_economy_map_batch.py, scripts/verify_economy_map_publish.py (RUN only — no source change; the live write target is economy_map via the RPC)</files>
  <read_first>
    - .planning/phases/18-gated-batch-publish/18-02-SUMMARY.md (the two scripts authored in plan 02 — their CLI shape, the --dry-run flag, the fail-loud/idempotent behavior)
    - scripts/publish_economy_map_batch.py (the batch script being run — the RPC loop, halt-and-report, idempotent re-run)
    - scripts/verify_economy_map_publish.py (the anon-key verification harness being run)
    - .planning/phases/18-gated-batch-publish/18-CONTEXT.md D-05/D-07/D-08 (the publish order, fail-loud recovery, and the four verification assertions)
  </read_first>
  <action>
    AFTER the Task-2 approval, the ORCHESTRATOR runs the live publish from the absolute main-tree path: `python3 /root/bitcoin_bot/scripts/publish_economy_map_batch.py` (no --dry-run). The script loops `publish_block_version` over the 8 in-scope drafts (7 blocks FIRST, hub LAST — D-07). If it HALTs mid-batch (fail-loud, D-08), report which slugs succeeded/failed, fix the cause, and RE-RUN the same command — the script is idempotent (already-published slugs skip, the halted remainder completes). Then run the programmatic verification `python3 /root/bitcoin_bot/scripts/verify_economy_map_publish.py` — it reads with the anon key (no service_role, no preview flag) and asserts (D-05): all 8 in-scope bodies are status='published', the hub `agent-economy` renders its published article (current_body_version_id non-null + body loads anon), every `#/map/<slug>` cross-link resolves against PUBLISHED content, and the anon-visible published-block count is 8 (+ hub) up from 2. It must exit 0. Finally confirm PUB-01's render surface on the live published set: the hub renders at `#/map` and each published block renders at `#/map/<slug>` with back arrow / title / subtitle / maturity pill / body (the v2.0 renderer already provides this surface; verification proves the bodies now resolve published). This live run is orchestrator-owned from the main tree (not a worktree executor).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 scripts/verify_economy_map_publish.py && echo PUBLISH-VERIFIED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/publish_economy_map_batch.py` (no --dry-run) ran to a `DONE: published=[...] skipped=[...]` summary covering all 8 in-scope slugs (published + skipped == 8); on any HALT it was re-run idempotently to completion.
    - `scripts/verify_economy_map_publish.py` exits 0 (prints PUBLISH-VERIFIED-OK) — all 8 in-scope bodies status='published' from the anon perspective, the hub published body resolves, every cross-link resolves against published content, and the published-block count is 8 (the 2 to 8 transition printed).
    - regulation-legal is NOT published (it was excluded from PUBLISH_ORDER; the count is 8 = hub + 7, not 9).
    - The hub renders at `#/map` (the published article, not HUB_STORYLINE) and each published block renders at `#/map/<slug>` with the full reading surface (back arrow / title / subtitle / maturity pill / body) — PUB-01 acceptance.
    - The pipeline / LLM proxy / agent services were untouched — only `economy_map` (via the RPC) and the scoped `agentpulse-web` container changed (D-03 / v2.1 constraint).
  </acceptance_criteria>
  <done>
    All 8 in-scope bodies are live published; the hub renders its published article at `#/map` and every published block renders at `#/map/<slug>` with the full surface; the anon-key verification harness exits 0 (count 2 to 8); regulation-legal stays deferred; no out-of-scope service changed. PUB-01 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator approval -> live publish (service_role) | The publish batch writes LIVE prod content under service_role; the single in-chat manifest approval (Task 2) is the human gate before any RPC fires. |
| repo main tree -> live prod (scoped rebuild) | The scoped agentpulse-web rebuild ships the tracked app.js into the live container; entrypoint.sh substitutes the anon-key placeholder at container start. |
| anon visitor -> economy_map (post-publish read) | Verification reads with the anon key to prove the published result from a real visitor's privilege level. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Tampering | over-publish (regulation-legal / wrong versions) | mitigate | PUBLISH_ORDER is the fixed 8-slug list (regulation-legal excluded); the manifest is reviewed at the single operator gate (Task 2) BEFORE any RPC; verification asserts count == 8 (not 9), catching any over-publish. |
| T-18-09 | Elevation of Privilege | service_role leak via the scoped deploy | mitigate | Task 1 runs `verify_economy_map_crosslinks.py --guard-only` BEFORE the rebuild — placeholders intact + service_role in NONE of the web-deploy-path files; the deployed image carries only the anon-key placeholder (substituted at container start). |
| T-18-10 | Information Disclosure | verification masking an RLS gap | mitigate | verify_economy_map_publish.py reads with SUPABASE_ANON_KEY only — a draft/superseded body that leaked to anon would surface as a count/resolution mismatch (fail-loud), not a false pass. |
| T-18-11 | Denial of Service | partial-pass / stuck batch on mid-publish failure | mitigate | Fail-loud halt-and-report (D-08): a non-idempotent failure HALTs reporting succeeded/remaining; the idempotent re-run completes the halted batch (already-published slugs skip). |
| T-18-12 | Tampering | out-of-scope service change in the deploy | mitigate | The rebuild is SCOPED to the `web` service / `agentpulse-web` container (never a blanket `scripts/deploy.sh`); files_modified is limited to the web-deploy artifacts; Task 3 acceptance asserts pipeline/proxy/agent services untouched (v2.1 constraint). |
| T-18-SC | Tampering | new package installs | accept | No new packages — Task 1/3 run already-authored scripts + docker compose; no pip install task; no Package Legitimacy Gate needed. |
</threat_model>

<verification>
- Task 1: `verify_economy_map_crosslinks.py --guard-only` exits 0; `agentpulse-web` container running; pre-publish anon read shows hub NULL + count 2 (visual no-op, D-04).
- Task 3: `verify_economy_map_publish.py` exits 0 — 8 published bodies + hub article + cross-links-against-published + count 2 to 8 (D-05).
- The publish batch DONE summary covers all 8 in-scope slugs; regulation-legal absent (count 8, not 9).
- No pipeline / proxy / agent-service change — only `economy_map` (RPC) + the scoped `agentpulse-web` container.
</verification>

<success_criteria>
- The plan-18-01 app.js is deployed via the scoped agentpulse-web rebuild from the main tree, verified a visual no-op pre-publish (D-04).
- On the single operator approval, the publish batch flips all 8 in-scope drafts live (7 blocks first, hub last) via the atomic RPC, fail-loud + idempotent (D-06/D-07/D-08).
- Programmatic anon verification exits 0: hub renders its published article at `#/map`, every published block renders at `#/map/<slug>` with the full surface, every cross-link resolves against published content, published-block count 2 to 8 (D-05). regulation-legal stays deferred. PUB-01 satisfied.
</success_criteria>

<output>
Create `.planning/phases/18-gated-batch-publish/18-03-SUMMARY.md` when done.
</output>
