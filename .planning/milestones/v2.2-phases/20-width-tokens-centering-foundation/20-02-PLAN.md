---
phase: 20-width-tokens-centering-foundation
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - docker/web/site/style-base.css
  - docker/web/site/style-shared.css
autonomous: false
requirements: [RHYTHM-01]
must_haves:
  truths:
    - "No surface uses a hardcoded color — every surface/text color themes from the existing :root token system (warm off-white + violet)."
    - "Section rhythm reads as a hierarchy: one full-strength rule (1px --line-strong) between MAJOR sections; hairline (0.5px --line) rules WITHIN sections."
    - "On a wide viewport, the whole surface is centered with no dead left band, reading copy holds ~60-70 chars, and nav edges align with content edges — verified holistically on the LIVE render, not inferred from source bytes."
  artifacts:
    - path: "docker/web/site/style-base.css"
      provides: "On-accent white aliased to a token (--on-accent / --btn-text) — no raw #fff on a surface; D-05 major-rule weight applied to chrome boundaries"
    - path: "docker/web/site/style-shared.css"
      provides: "D-05 section-rhythm rules: 1px --line-strong between major sections, 0.5px --line within; the .toggle-btn.active #fff aliased to a token"
  key_links:
    - from: ".subscribe / .toggle-btn.active text color"
      to: "the on-accent token"
      via: "color property"
      pattern: "color:\\s*var\\(--"
    - from: "major-section separators"
      to: "--line-strong token"
      via: "border-top"
      pattern: "border-top:\\s*1px solid var\\(--line-strong\\)"
---

<objective>
Establish the RHYTHM-01 baseline the whole site conforms to: token-only color (no hardcoded hex on a surface) and a deliberate section-rhythm hierarchy (1px `--line-strong` between MAJOR sections, 0.5px `--line` hairline WITHIN sections). RHYTHM-01 is applied site-wide but OWNED and VERIFIED HOLISTICALLY HERE — later phases conform to it rather than redefining it.

Purpose: A consistent, token-driven color + rhythm system is the shared visual baseline every downstream v2.2 phase builds on. RESEARCH §"RHYTHM-01 Color Audit" found the surface is already ~99% token-only — only THREE on-accent white literals on loaded routes (`style-base.css:36` is already a `--btn-text` token; `style-base.css:190` `.subscribe` and `style-shared.css:100` `.toggle-btn.active` are raw `#fff`). RESEARCH §"D-05 Section Rhythm" mapped the major-vs-within separators per route with concrete rule additions. This plan applies the recommended D-05 baseline and aliases the two raw `#fff` so the audit is literally 100% token-only.

Output: the two raw `#fff` aliased to an on-accent token; the D-05 section-rhythm rule bumps applied site-wide; and a holistic live-render verification (orchestrator-run, worktree-unsafe scoped `web` rebuild) confirming WIDTH-01 + RHYTHM-01 together on the actual rendered output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-width-tokens-centering-foundation/20-CONTEXT.md
@.planning/phases/20-width-tokens-centering-foundation/20-RESEARCH.md
@.planning/phases/20-width-tokens-centering-foundation/20-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alias the two raw on-accent #fff literals to a token (RHYTHM-01 token-only, D-05 discretion)</name>
  <files>docker/web/site/style-base.css, docker/web/site/style-shared.css</files>
  <read_first>
    - docker/web/site/style-base.css — read the `:root` token block (`:10-66`): note `--btn-text:#fff` at `:36` (already a token, consumed by `#subscribe-btn`) and the locked UI-SPEC literals `rgba(250,248,245,.86)` at `:114` and `#ddd2ff` at `:178` (these STAY — do not touch). Read the `.subscribe` rule (`:182-196`), which has `color:#fff` at `:190`.
    - docker/web/site/style-shared.css — read the `.toggle-btn.active` rule (`:98-102`), which has `color:#fff` at `:100`.
    - .planning/phases/20-width-tokens-centering-foundation/20-RESEARCH.md §"RHYTHM-01 Color Audit" (the complete literal set — only 3 on-accent whites on loaded routes; recommendation: alias the two raw `#fff` to `--btn-text` or a new `--on-accent:#fff`) and Open Question #2 (alias is the lowest-risk reading of "token-only verified holistically").
    - .planning/phases/20-width-tokens-centering-foundation/20-CONTEXT.md "Claude's Discretion" note on the stray `#fff` (tokenize or leave as intentional on-accent white) — resolve by aliasing per the research recommendation.
  </read_first>
  <action>
    Per RESEARCH §"RHYTHM-01 Color Audit" + Open Question #2 (recommended resolution: alias so the audit is literally 100% token-only), make the two raw on-accent-white literals reference a token. Add a clearly-named on-accent token to `style-base.css :root` — define `--on-accent:#fff` (semantic: text/icon color on top of a solid `--accent` fill) near the legacy-token bridge (around `:36`, beside `--btn-text`). Then replace the raw `color:#fff` at `style-base.css:190` (`.subscribe`) with `color: var(--on-accent)` and the raw `color:#fff` at `style-shared.css:100` (`.toggle-btn.active`) with `color: var(--on-accent)`. Leaving `--btn-text:#fff` as-is is fine (it is already a token); optionally point it at `var(--on-accent)` for consistency, but do NOT change `#subscribe-btn` behavior.

    Do NOT touch the two locked UI-SPEC literals (`style-base.css:114` translucent header bg, `style-base.css:178` active-tab border `#ddd2ff`) — they are documented locked literals out of RHYTHM-01's surface-color scope (RESEARCH audit). After this task, no `.subscribe`/`.toggle-btn.active` surface text uses a raw hex; every color on a loaded route flows from a token.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -Eq '\-\-on-accent:\s*#fff' docker/web/site/style-base.css && grep -vE '^\s*/?\*|^\s*//' docker/web/site/style-base.css | grep -A8 '\.subscribe' | grep -q 'color: *var(--on-accent)' && grep -vE '^\s*/?\*|^\s*//' docker/web/site/style-shared.css | grep -A4 '\.toggle-btn\.active' | grep -q 'color: *var(--on-accent)' && [ "$(grep -vE '^\s*/?\*|^\s*//' docker/web/site/style-shared.css | grep -c 'color: *#fff')" = "0" ] && grep -q '#ddd2ff' docker/web/site/style-base.css && grep -q 'rgba(250,248,245,.86)' docker/web/site/style-base.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: `style-base.css :root` contains `--on-accent:#fff` (a named on-accent token).
    - Source: `style-base.css` `.subscribe` rule uses `color: var(--on-accent)` (no raw `color:#fff` on the `.subscribe` surface).
    - Source: `style-shared.css` `.toggle-btn.active` uses `color: var(--on-accent)` and `style-shared.css` has zero non-comment `color:#fff` occurrences.
    - Source: the two locked UI-SPEC literals (`#ddd2ff` active-tab border, `rgba(250,248,245,.86)` header bg) are unchanged (still present).
    - Behavior: the Subscribe button wordmark and the active mode-toggle segment still render white-on-violet (the token resolves to `#fff`) — no visual regression, just tokenized.
  </acceptance_criteria>
  <done>The two raw on-accent `#fff` literals reference `--on-accent`; the audit is literally 100% token-only on loaded routes; the locked UI-SPEC literals are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the D-05 section-rhythm hierarchy — 1px --line-strong between major sections, 0.5px --line within (RHYTHM-01)</name>
  <files>docker/web/site/style-shared.css, docker/web/site/style-base.css</files>
  <read_first>
    - docker/web/site/style-shared.css — read the existing separator rules the research enumerated: `.content-area` top rule (`:130-134`, currently `0.5px solid var(--border)`), `.article-entry` bottom (`:139-142`, currently `1px solid var(--line)`), `#subscribe-section` top (`:744-748`, currently `0.5px solid var(--border)`), `.bottom-bar` top (`:839-846`, currently `0.5px solid var(--bar-border)`). Read the area around the `.tier-section` / `.tier-label` rules (the tier sections are emitted as `<section class="tier-section">` by `renderHub`/`tierSection` in app.js).
    - docker/web/site/style-base.css — read the `body > header` rule (`:111-118`): its `border-bottom` (`:117`) is `1px solid var(--line)` — already major WEIGHT (1px) but the lighter `--line` color. Leave it UNCHANGED in this task; whether the nav→content edge needs its border-COLOR bumped to `var(--line-strong)` for hierarchy consistency is a subtle visual call DEFERRED to the Task 3 holistic live-render check (a doubled rule with the `.content-area` top is possible where the nav meets content, so it must be judged on the real render). Do NOT touch the `position:sticky` rule itself.
    - .planning/phases/20-width-tokens-centering-foundation/20-RESEARCH.md §"D-05 Section Rhythm" — the "What already exists" table, the per-route major-vs-within mapping, and the "Concrete recommended additions" block (the exact rules to add/bump). Also Assumption A2 (0.5px hairlines are an established, accepted choice in this codebase) and Pitfall 1 (do not touch `body > header` sticky rule).
    - .planning/phases/20-width-tokens-centering-foundation/20-CONTEXT.md D-05 (1px `--line-strong` between MAJOR route sections; 0.5px `--line` for WITHIN-section separations; applied site-wide, owned + verified HERE).
  </read_first>
  <action>
    Per D-05 and RESEARCH §"D-05 Section Rhythm" "Concrete recommended additions", establish the rhythm hierarchy site-wide using the existing `--line-strong` (major) and `--line` (within) tokens. Apply these specific changes:

    MAJOR separators → 1px `--line-strong`:
    - Add a major top rule between adjacent map tier sections: `.tier-section + .tier-section { border-top: 1px solid var(--line-strong); padding-top: var(--space-xl); }` (the substrate↔behavior↔frame boundaries; today `.tier-label` has only top margin, no rule).
    - Bump `#subscribe-section` top border (`style-shared.css:747`) from `0.5px solid var(--border)` to `1px solid var(--line-strong)` (chrome→subscribe is a major boundary).
    - Bump `.bottom-bar` top border (`style-shared.css:844`) from `0.5px solid var(--bar-border)` to `1px solid var(--line-strong)` (footer is a major boundary).
    - Bump the `.content-area` top rule (`style-shared.css:132`) from `0.5px solid var(--border)` to `1px solid var(--line-strong)` (the hero↔body / chrome↔content major rule).

    WITHIN separators → 0.5px `--line`:
    - Demote the `.article-entry` row divider (`style-shared.css:141`) from `1px solid var(--line)` to `0.5px solid var(--line)` so list rows read as within-section hairlines (the `article td` bottom at `:730` is already `0.5px` — leave it; the `:last-child` border:none rule at `:144-146` stays).

    Do NOT touch the `body > header` sticky rule or its `position`/`top`/`z-index` (Pitfall 1) — only the separator rules above change. Do NOT add new sections, change grid counts, or do any Phase 21-23 work (scope guard). Keep the change to border weights/colors only; the `--line-strong`/`--line` tokens already exist (style-base.css:17-18) so no new color token is needed here.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -Eq '\.tier-section \+ \.tier-section\s*\{[^}]*border-top:\s*1px solid var\(--line-strong\)' docker/web/site/style-shared.css && grep -vE '^\s*/?\*|^\s*//' docker/web/site/style-shared.css | grep -A4 '#subscribe-section' | grep -q 'border-top: *1px solid var(--line-strong)' && grep -vE '^\s*/?\*|^\s*//' docker/web/site/style-shared.css | grep -A6 '\.bottom-bar' | grep -q 'border-top: *1px solid var(--line-strong)' && grep -vE '^\s*/?\*|^\s*//' docker/web/site/style-shared.css | grep -A3 '\.content-area' | grep -q 'border-top: *1px solid var(--line-strong)' && grep -vE '^\s*/?\*|^\s*//' docker/web/site/style-shared.css | grep -A3 '\.article-entry {' | grep -q 'border-bottom: *0.5px solid var(--line)' && grep -q 'position:sticky' docker/web/site/style-base.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: `style-shared.css` contains `.tier-section + .tier-section { border-top: 1px solid var(--line-strong); padding-top: var(--space-xl); }`.
    - Source: `#subscribe-section`, `.bottom-bar`, and `.content-area` top borders are each `1px solid var(--line-strong)` (the three chrome/major boundaries bumped from 0.5px).
    - Source: `.article-entry` bottom border is `0.5px solid var(--line)` (demoted from 1px to a within-section hairline).
    - Source: every separator uses a `--line-strong` (major) or `--line` (within) token — no raw px-color separators introduced.
    - Source: the `body > header { position:sticky ... }` rule is unchanged.
    - Behavior (verified holistically in Task 3): on the live render the section rhythm reads as a hierarchy — full-strength rules between major sections (hero↔body, tier↔tier, body↔subscribe↔footer), hairlines within (list rows, table rows).
  </acceptance_criteria>
  <done>The D-05 rhythm hierarchy is applied site-wide via the existing tokens: 1px `--line-strong` at the four major boundaries, 0.5px `--line` for the list-row hairline; the sticky-header rule is untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Holistic live-render verification of WIDTH-01 + RHYTHM-01 (orchestrator-run, worktree-unsafe)</name>
  <what-built>
    Plan 01 added the `--measure`/`--wide`/`--gutter` width tokens + `.prose`/`.wide` classes, widened the nav to `--wide`, retired the 720px `.container`, applied the per-route axis map, and wrapped the map prose intro. Plan 02 (Tasks 1-2) aliased the on-accent whites to `--on-accent` and applied the D-05 section-rhythm hierarchy. This checkpoint verifies WIDTH-01 + RHYTHM-01 together on the ACTUAL rendered output — not from source bytes (Phase-19 lesson, MEMORY [[feedback_verify_render_bugs_end_to_end]]: reproduce the rendered output; never infer a corpus clean from storage).
  </what-built>
  <how-to-verify>
    DEPLOY (orchestrator-owned, from the MAIN tree — worktree-unsafe; do NOT run from a worktree executor):
    1. Prod↔main drift check first; get operator approval before any deploy (per the standing v2.2 deploy discipline + MEMORY scoped/approved-deploys).
    2. Scoped rebuild ONLY the web service: `cd /root/bitcoin_bot/docker && docker compose up -d --build web` — use the SERVICE key `web`, NOT the `agentpulse-web` container_name (MEMORY: web compose service name). NO `--delete`.

    VERIFY ON THE LIVE SITE (load the real rendered pages on a wide ~1440px viewport):
    3. WIDTH-01 — no dead gutter: on the newsletter list (`#/`) and the map (`#/map`), content fills a centered ~1080px band with NO large empty band on the left. Both axes are centered, not pinned center-left.
    4. WIDTH-01 — reading line: open an edition (`#/<edition>`) and a block page (`#/map/<slug>`) — the body holds a ~60-70 char line (narrow centered prose), NOT a 1080px-wide wall of text.
    5. WIDTH-01 — one axis (D-02): the sticky nav's left/right edges line up with the wide content edges below it.
    6. WIDTH-01 — map split (D-03): on `#/map` the hub title/storyline read at a narrow prose width while the three tier card grids span the full wide band; the grid is still 2-col (3-col is Phase 21, must NOT have changed).
    7. RHYTHM-01 — token color: every surface themes off the off-white + violet system; the Subscribe button + active mode-toggle segment still render white-on-violet (no broken/black text from the alias).
    8. RHYTHM-01 — section rhythm: the rhythm reads as a hierarchy — full-strength rules between major sections (the nav bottom edge, hero↔body, tier↔tier on the map, body↔subscribe↔footer) all read at EQUIVALENT strength; hairlines within (list rows, table rows). If the nav bottom edge (`body > header` border-bottom, 1px `--line`) reads visibly LIGHTER than the other major boundaries (now `--line-strong`), the in-scope fix is to bump ONLY its `border-bottom` color to `var(--line-strong)` (never its weight, `position:sticky`, `top`, `z-index`, or `body >` scoping); conversely, if a DOUBLED rule appears where the nav meets a `.content-area` top, drop one of the two. Tune this on the live render — it is the subtle visual call this holistic checkpoint exists to make.
    9. Regression guards: the maturity pill does NOT overlap the nav on a block page (the `body > header` fix is intact); the mode toggle, tabs, ← Back links, and subscribe form all still work.
    10. Scope guard: no edition-header de-dup, no 3-col map, no excerpt change, no Signals tab appeared (those are Phases 21-23).
  </how-to-verify>
  <resume-signal>Operator types "approved" once the live render shows centered both-axes content with no dead gutter, a ~60-70 char reading line, aligned nav/content edges, token-only color, and the section-rhythm hierarchy — or describes the specific issue to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits two static CSS files (color-token aliasing + border-rule weights) and runs a scoped `web` rebuild + live verification. It adds NO new input handling, NO new data flow, NO auth/authz surface, NO new network calls, and NO user-controlled content-rendering path. The only pre-existing boundary — the deploy-time Supabase URL/key substitution and the anon-key CDN client — is untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Information Disclosure | scoped `web` rebuild / deploy | mitigate | Deploy is orchestrator-owned from the main tree, scoped to the `web` SERVICE key, NO `--delete`, with a prod↔main drift check + operator approval BEFORE any deploy (Task 3 steps 1-2). This prevents the worktree-stale-build and rsync-blast-radius failure modes (MEMORY: scoped/approved deploys, web compose service name). The plan is marked `autonomous: false`. |
| T-20-05 | Tampering | CSS color-token aliasing | accept | Aliasing two raw `#fff` to `--on-accent:#fff` is a value-preserving rename — it cannot change rendered color or introduce a sink. The acceptance criterion confirms the locked UI-SPEC literals are preserved and the white-on-violet pairing still renders. |
| T-20-06 | Elevation of Privilege | sticky-header scoping (`body > header`) | mitigate | Task 2 explicitly forbids touching the `body > header { position:sticky }` rule (read_first + acceptance criterion + live regression guard step 9) — the maturity-overlap fix is preserved; no bare `header` selector reintroduced. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — zero registry interaction, no `package.json` change, no new CDN `<script>`. The two existing CDN scripts are unchanged. Package Legitimacy Gate not applicable (RESEARCH §"Package Legitimacy Audit"). |

No new high or medium severity threats are introduced by this presentation-layer change. No existing mitigation is weakened: no inline event handlers added, the Supabase substitution path is untouched, the sticky-header scoping is preserved, and the deploy follows the scoped/approved/drift-checked discipline.
</threat_model>

<verification>
- Both raw on-accent `#fff` literals reference `--on-accent`; the color audit is literally 100% token-only on loaded routes; locked UI-SPEC literals preserved (Task 1 gate).
- The D-05 rhythm hierarchy is applied via existing tokens — 1px `--line-strong` at the four major boundaries, 0.5px `--line` for list-row hairlines; `body > header` untouched (Task 2 gate).
- Holistic live-render verification (orchestrator-run, worktree-unsafe scoped `web` rebuild) confirms WIDTH-01 + RHYTHM-01 on the ACTUAL rendered output: centered both-axes content, no dead gutter, ~60-70 char reading line, aligned nav/content edges, token color intact, section-rhythm hierarchy, no maturity/nav overlap, no Phase 21-23 scope creep (Task 3).
- Render verification reproduces the rendered pages — it does NOT infer a clean result from source bytes (Phase-19 lesson).
</verification>

<success_criteria>
RHYTHM-01 baseline is established and the phase is holistically verified:
- No surface uses a hardcoded color — every color themes from the `:root` token system (the two raw on-accent whites now reference `--on-accent`).
- Section rhythm reads as a hierarchy: 1px `--line-strong` between major sections, 0.5px `--line` within.
- On the LIVE wide-viewport render: no dead left gutter, both axes centered, ~60-70 char reading line, nav edges == content edges (WIDTH-01 confirmed end-to-end alongside RHYTHM-01).
- No scope creep into Phases 21-23; the maturity-overlap fix preserved.
</success_criteria>

<output>
Create `.planning/phases/20-width-tokens-centering-foundation/20-02-SUMMARY.md` when done.
</output>
