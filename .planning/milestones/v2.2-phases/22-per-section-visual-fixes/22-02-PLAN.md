---
phase: 22-per-section-visual-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/web/site/index.html
  - docker/web/site/style-shared.css
autonomous: true
requirements: [AGENTS-01]
must_haves:
  truths:
    - "The About `#about` section shows the pipeline agents (Processor / Analyst / Research / Newsletter) as an ORDERED, numbered 01-04 sequence in a left column (D-10)."
    - "The supporting layer is an UNORDERED bulleted (dot, not numbered) list in a right column — and Gato Brain is NOT silently dropped (Gato, Gato Brain, LLM proxy, web front end), keeping the v2.0 `eight cooperating services` accuracy bar consistent (D-12)."
    - "No orphaned single card on its own row (the 4+1 `.agent-row` defect is gone — replaced by the two-column `made-cols`)."
    - "`Nothing publishes without human approval.` renders as its own distinct violet `.approval` callout, and that clause is removed from intro prose P2 so it is not stated twice (D-11)."
    - "All net-new CSS is token-only (RHYTHM-01): the mockup's `--violet`/`--violet-soft`/`--line-soft`/`#2d2585` are mapped to `--accent`/`--accent-soft`/`--line`/`--accent-ink`; style-shared.css stays hex-free."
  artifacts:
    - path: "docker/web/site/index.html"
      provides: "#about section rewritten from the uniform 5-pill .agent-row to a .made-cols two-column block: numbered 01-04 pipeline (left), bulleted supporting layer (right), and a violet .approval callout; the approval clause pulled out of intro P2 (D-11)."
      contains: "made-cols"
    - path: "docker/web/site/style-shared.css"
      provides: "Net-new token-anchored CSS for .made-cols / .made-head / .agent / .idx / .dot / .name / .desc / .approval (ported from the .agent-row/.agent-pill analog), collapsing 2-col -> 1-col on mobile; no hardcoded hex."
      contains: ".made-cols"
  key_links:
    - from: "#about made-cols markup (index.html)"
      to: "the new .made-cols/.agent/.approval CSS (style-shared.css)"
      via: "shared class names"
      pattern: "made-cols"
    - from: ".approval callout text color/background"
      to: "the violet token system"
      via: "var(--accent-ink) text on var(--accent-soft) background"
      pattern: "var\\(--accent-soft\\)"
---

<objective>
AGENTS-01 — rewrite the About `#about` agent grid. Today the section (`index.html:131-152`) renders a uniform 5-pill `.agent-row`, which leaves Gato orphaned alone on row 2 (the 4+1 reads accidental). This plan replaces it with the mockup's pipeline-vs-supporting split: an ordered numbered 01-04 pipeline (Processor/Analyst/Research/Newsletter), an unordered bulleted supporting layer, and a distinct violet "human approval" callout (D-10) — the spine made visible.

Purpose: per the ROADMAP success criteria, the About section must read as an ordered pipeline + an unordered supporting layer with no orphaned card, and the "nothing publishes without human approval" line must be its own violet callout (the editorial-integrity spine, surfaced). Two coupled tasks: (1) the static `index.html` markup rewrite + the D-11 approval-clause de-dup; (2) the net-new token-anchored CSS ported from the existing `.agent-row`/`.agent-pill` analog, mapping every mockup palette token to the prod token system (RHYTHM-01). The supporting layer keeps Gato Brain (does NOT silently fold it into Gato) so the v2.0 "eight cooperating services" accuracy bar holds, and the final copy is surfaced for operator sign-off at deploy/verify (D-12).

Output: the `made-cols` markup + the de-duped intro prose in `index.html`, and the `.made-cols`/`.agent`/`.idx`/`.dot`/`.name`/`.desc`/`.approval` CSS in `style-shared.css`. Source-only — live render verified in 22-04. style-base.css is NOT modified (the `.approval` text color reuses the existing `--accent-ink` token, so no new token is needed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-per-section-visual-fixes/22-CONTEXT.md
@.planning/phases/22-per-section-visual-fixes/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite #about markup to the made-cols pipeline/supporting split + pull the approval clause from intro P2 (AGENTS-01 / D-10 / D-11 / D-12)</name>
  <files>docker/web/site/index.html</files>
  <read_first>
    - docker/web/site/index.html — the `#about` section (`:108-156`): the intro `.prose` block (eyebrow/title/page-sub + the three `.about` paragraphs at `:124-128`, where P1 `:125` says "eight cooperating services", P2 `:126` carries the "...nothing is published without human approval" clause to REMOVE, P3 `:127` prose-lists the supporting layer "Behind the five content agents..." which the new grid now carries), and the `.wide` block with the uniform 5-pill `.agent-row` (`:130-153`, the names + `.ad` descriptions to redistribute). Keep the `.prose`/`.wide` axis split (D-03 from Phase 20 — intro stays `.prose`, `made-cols` stays in `.wide`).
    - .planning/phases/22-per-section-visual-fixes/22-PATTERNS.md §"AGENTS-01" (`:137-204`) — the current-markup excerpt, the de-dup site (`:154-157`), the target `made-cols` shape (`:159-174`: LEFT numbered 01-04 pipeline, RIGHT bulleted supporting, `.approval` callout), and the §"Token Mapping" table + D-12 note (`:204`).
    - .planning/phases/22-per-section-visual-fixes/22-CONTEXT.md §"AGENTS-01" D-10 (made-cols layout locked; numbered pipeline + bulleted supporting + violet approval callout; Option B uniform-grid NOT taken), D-11 (pull approval clause from P2), D-12 (copy accuracy — keep Gato Brain, surface for operator review).
    - .planning/docs/REDESIGN_CC_BRIEF.md §"TASK 5" (`:125-134`) — the orphaned-Gato-card problem + the pipeline-as-real-sequence rationale for numbering (don't number the supporting layer).
  </read_first>
  <action>
    Replace the `.agent-row` block (`index.html:131-152`) with a `made-cols` two-column structure inside the existing `.wide` wrapper (`:130`). Use semantic, accessible markup — real elements, no click-handler pseudo-links (this is static informational content; A11Y full pass is Phase 25 but do not introduce a regression).

    LEFT column — heading `<p class="made-head">The pipeline · in order</p>` then four `.agent` rows, each `<span class="idx">01</span>` (01/02/03/04) + a wrapper holding `<p class="name">…</p>` and `<p class="desc">…</p>`: 01 Processor, 02 Analyst, 03 Research, 04 Newsletter. Reuse the existing `.ad` descriptions verbatim from the current pills (`:134,138,142,146`) as the `.desc` text. The numbering is justified because the pipeline genuinely is a sequence (brief TASK 5).

    RIGHT column — heading `<p class="made-head">The supporting layer</p>` then `.agent` rows that use `<span class="dot"></span>` (a bullet, NOT a number) instead of `.idx`: Gato (Telegram operator interface), Gato Brain (conversational middleware that routes operator commands), LLM proxy (governs budgets + per-agent wallets across every model call), web front end (serves the published site). Include Gato Brain as its own entry — do NOT silently fold it into Gato (D-12); the four supporting entries + the four pipeline agents = the "eight cooperating services" the intro P1 commits to. Then a `<div class="approval"><strong>Nothing publishes without human approval.</strong> Every edition is drafted by the system and shipped only after an operator signs off.</div>` callout.

    D-11 de-dup: in intro P2 (`:126`), REMOVE the trailing "— and nothing is published without human approval" clause (now carried by the `.approval` callout) so the line ends at "...is itself part of what we track." D-12 reconcile: P3 (`:127`) prose-enumerates the supporting layer the new grid now shows AND says "the five content agents" (now four numbered pipeline agents); trim P3 so it does not restate the supporting layer twice and does not contradict the new 4-item pipeline (either drop the redundant enumeration or recast as a one-line lead-in) — and SURFACE the final supporting-layer copy, the Gato-vs-Gato-Brain distinction, and the "eight cooperating services" count as the operator-reviewable D-12 point (recorded in the SUMMARY for sign-off at 22-04). Keep P1's "eight cooperating services" (now accurate: 4 pipeline + 4 supporting).

    Do NOT alter the eyebrow/title/page-sub, the `.prose`/`.wide` wrappers, the `#about-view`/`content-area`/`about-stub` classes (Phase-14 spacing), or any other section. Do NOT use raw inline `style=` hex colors. Confirm the HTML is well-formed (balanced tags).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -q 'class="made-cols"' docker/web/site/index.html && grep -q 'class="made-head"' docker/web/site/index.html && grep -Eq 'class="idx">0[1-4]' docker/web/site/index.html && grep -q 'class="dot"' docker/web/site/index.html && grep -q 'class="approval"' docker/web/site/index.html && grep -q 'Gato Brain' docker/web/site/index.html && ! grep -q 'class="agent-row"' docker/web/site/index.html && ! grep -q 'nothing is published without human approval' docker/web/site/index.html && [ "$(grep -oE '#[0-9a-fA-F]{3,6}' docker/web/site/index.html | wc -l)" -eq 0 ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: `#about` contains a `.made-cols` block with a LEFT `.made-head` "The pipeline · in order" + four `.agent` rows carrying `.idx` `01`/`02`/`03`/`04` for Processor/Analyst/Research/Newsletter (ordered, numbered).
    - Source: a RIGHT `.made-head` "The supporting layer" + `.agent` rows using `.dot` (bulleted, NOT numbered) for Gato, Gato Brain, LLM proxy, web front end — Gato Brain present (not folded into Gato, D-12).
    - Source: a `.approval` callout with `<strong>Nothing publishes without human approval.</strong>`; the old `.agent-row` is gone (no orphaned card).
    - Source: intro P2 no longer contains "nothing is published without human approval" (D-11 de-dup); P3 no longer double-states the supporting layer / "five content agents"; the SUMMARY records the D-12 copy-accuracy point for operator sign-off.
    - Source: no inline hex in `index.html` (`#[0-9a-fA-F]{3,6}` count == 0); the `.prose`/`.wide` axis split and the eyebrow/title/page-sub are unchanged.
  </acceptance_criteria>
  <done>The `#about` agent grid is the `made-cols` numbered-pipeline + bulleted-supporting split with a violet `.approval` callout; Gato Brain retained; the approval clause + supporting-layer prose de-duped from the intro (D-11/D-12); HTML well-formed; no inline hex.</done>
</task>

<task type="auto">
  <name>Task 2: Net-new token-anchored CSS for made-cols / agent / idx / dot / name / desc / approval (AGENTS-01 / RHYTHM-01)</name>
  <files>docker/web/site/style-shared.css</files>
  <read_first>
    - docker/web/site/style-shared.css — the `.agent-row`/`.agent-pill`/`.an`/`.ad` analog (`:921-958`, the token-anchored styling to PORT: `--mono` name, `--serif` desc, `--ink-*` colors, `--surface`/`--line` chrome, `--space-*` rhythm) and the existing mobile breakpoint `@media (max-width: 600px)` (`:962`, the prod mobile precedent to collapse `made-cols` 2-col -> 1-col).
    - .planning/phases/22-per-section-visual-fixes/22-PATTERNS.md §"AGENTS-01" CSS analog + FORM reference (`:176-204`) — the mockup `.made-cols`/`.agent`/`.idx`/`.dot`/`.name`/`.desc`/`.approval` rules to map to tokens — and §"Token Mapping" table (`:210-221`): `--violet`->`--accent`, `--violet-soft`->`--accent-soft`, `#2d2585`->`--accent-ink`, `--line-soft`->`--line`, raw px -> `--space-*`.
    - docker/web/site/style-base.css — the `:root` token layer (`:10-79`): `--accent` (`:19`), `--accent-soft` (`:20`), `--accent-ink` (`:21`), `--line`/`--line-strong` (`:17-18`), `--ink-soft`/`--ink-faint` (`:15-16`), `--mono`/`--serif` (`:53-54`), `--space-*` (`:57-63`), `--radius*` (`:66-69`). NOTE: `--violet`/`--violet-soft`/`--line-soft` do NOT exist — confirmed absent; never reference them.
    - .planning/phases/22-per-section-visual-fixes/22-CONTEXT.md §"Claude's Discretion" (exact class names + sizing) — the class names above are the chosen set; the `.approval` text color reuses `--accent-ink` (NO new token in style-base.css per "add a token only if needed").
  </read_first>
  <action>
    Append a clearly-commented block of net-new CSS (e.g. "Phase 22 AGENTS-01 — About made-cols pipeline/supporting grid") to `style-shared.css`, porting the token discipline from the `.agent-row`/`.agent-pill` analog and mapping EVERY mockup palette token per the Token Mapping table. Use ONLY `var(--…)` tokens and `--space-*` — NEVER a hex literal, NEVER `--violet`/`--violet-soft`/`--line-soft`.

    Write each rule body as COMPACT SINGLE-LINE CSS (selector `{` all-declarations `}` on one physical line) so the single-line `[^}]*` acceptance gates match (a multi-line rule body would silently miss the gate). Rules:
    - `.made-cols` — `display:grid; grid-template-columns:1fr 1fr; gap:0 var(--space-2xl); margin-top:var(--space-lg);` (the mockup's `48px` column gap -> `--space-2xl`).
    - `.made-head` — the mono section-label chrome matching `.tier-label` (`--mono`, 11px/600, uppercase, `letter-spacing:.08em`, `color:var(--ink-faint)`, a `--space-*` bottom margin).
    - `.agent` — `display:grid; grid-template-columns:26px 1fr; gap:0 var(--space-md); align-items:baseline; padding:var(--space-sm) 0; border-bottom:1px solid var(--line);` (mockup `--line-soft` -> `--line`).
    - `.agent .idx` — `font-family:var(--serif); font-size:14px; color:var(--ink-faint);`
    - `.agent .dot` — `width:6px; height:6px; border-radius:50%; background:var(--accent); display:inline-block;` (mockup `--violet` -> `--accent`).
    - `.agent .name` — `font-family:var(--mono); font-size:14px; font-weight:500; color:var(--ink); margin:0;`
    - `.agent .desc` — `font-size:14px; color:var(--ink-soft); margin:var(--space-xs) 0 0; line-height:1.5;`
    - `.approval` — `margin-top:var(--space-xl); padding:var(--space-md) var(--space-md); background:var(--accent-soft); border-radius:var(--radius); color:var(--accent-ink); line-height:1.55;` (mockup `--violet-soft` -> `--accent-soft`, `#2d2585` -> `--accent-ink`).

    Add a mobile collapse — `@media (max-width: 880px) { .made-cols { grid-template-columns: 1fr; } }` (mockup collapses at 880px; this is the responsive 2->1 the brief requires; the full responsive pass is Phase 25). Do NOT modify the existing `.agent-row` rules (now unused but harmless — leave them; cleanup is out of scope), the Phase-20 width axes, or any other rule. Do NOT add a token to `style-base.css`.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -Eq '\.made-cols *\{[^}]*grid-template-columns: *1fr 1fr' docker/web/site/style-shared.css && grep -Eq '\.agent \.dot *\{[^}]*background: *var\(--accent\)' docker/web/site/style-shared.css && grep -Eq '\.approval *\{[^}]*background: *var\(--accent-soft\)' docker/web/site/style-shared.css && grep -Eq '\.approval *\{[^}]*color: *var\(--accent-ink\)' docker/web/site/style-shared.css && grep -Eq 'max-width: *880px' docker/web/site/style-shared.css && ! grep -q -- '--violet' docker/web/site/style-shared.css && ! grep -q -- '--line-soft' docker/web/site/style-shared.css && [ "$(grep -oE '#[0-9a-fA-F]{3,6}' docker/web/site/style-shared.css | wc -l)" -eq 0 ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.made-cols` is a `1fr 1fr` grid (single-line rule); `.agent` is a `26px 1fr` row grid; `.made-head` matches the `.tier-label` mono chrome; `.idx`/`.dot`/`.name`/`.desc` ported from the `.agent-pill` token analog.
    - Source: `.agent .dot` background is `var(--accent)`; `.approval` background is `var(--accent-soft)` and text color is `var(--accent-ink)` (mockup tokens mapped per the table).
    - Source: a `@media (max-width: 880px)` rule collapses `.made-cols` to `1fr` (responsive 2->1).
    - Source (RHYTHM-01): style-shared.css references NO `--violet`/`--violet-soft`/`--line-soft` and contains ZERO hardcoded hex (`#[0-9a-fA-F]{3,6}` count == 0); style-base.css is unmodified (no new token).
    - Source: the rule bodies are compact single-line so the `[^}]*` gates match; the existing `.agent-row` rules + Phase-20 width axes are untouched.
  </acceptance_criteria>
  <done>The net-new `made-cols`/`agent`/`idx`/`dot`/`name`/`desc`/`approval` CSS is token-only (all mockup palette tokens mapped to `--accent*`/`--line`), collapses 2->1 at 880px, and style-shared.css stays hex-free; no new token added to style-base.css.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | `#about` is FULLY STATIC markup in `index.html` + static CSS in `style-shared.css`. No DB-derived data, no user input, no endpoint, no query, no `innerHTML` of dynamic content, no write verb. The copy is hand-authored literals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Tampering | static `#about` markup / CSS | accept | All content is hand-authored static literals — no DB string, no user input, no dynamic sink reaches this section. There is nothing to inject; `escapeHtml` is not in this path because no dynamic value crosses it. No new trust boundary. |
| T-22-02-02 | Information Disclosure | About copy accuracy (D-12) | mitigate | The supporting-layer copy keeps Gato Brain distinct (does not silently misrepresent the architecture) and is surfaced for operator sign-off at 22-04, preserving the v2.0 "eight cooperating services" accuracy bar. Editorial accuracy, not a security surface, but the spine ("nothing publishes without human approval") is the explicit content. |
| T-22-02-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs — no `package.json`, no new CDN `<script>`; existing CDN scripts unchanged. Package Legitimacy Gate not applicable. |

Net security delta: zero new attack surface. Fully static markup + token-only CSS; no input path, query, or write introduced. ASVS L1, no high-severity threat.
</threat_model>

<verification>
- `#about` renders the `made-cols` numbered-pipeline (01-04) + bulleted-supporting split with a violet `.approval` callout; Gato Brain retained; the orphaned `.agent-row` is gone (Task 1 gate).
- The approval clause is removed from intro P2 and the supporting-layer prose de-duped from P3; the D-12 copy point is recorded for operator sign-off (Task 1 acceptance).
- All net-new CSS is token-only — mockup palette tokens mapped to `--accent*`/`--line`, no `--violet*`/`--line-soft`, zero hex in style-shared.css; collapses 2->1 at 880px (Task 2 gate).
- Live confirmation (no orphaned card; ordered pipeline + bulleted supporting; violet callout) is owned by 22-04.
</verification>

<success_criteria>
AGENTS-01 ships at source:
- The About section shows an ordered numbered 01-04 pipeline (Processor/Analyst/Research/Newsletter) and an unordered bulleted supporting layer (Gato, Gato Brain, LLM proxy, web front end) — no orphaned single card.
- "Nothing publishes without human approval." is its own distinct violet callout; the clause is removed from the intro prose (stated once).
- All net-new CSS themes from the violet/off-white token system (no hardcoded color); style-base.css needs no new token.
- The Gato-vs-Gato-Brain wording + service count are surfaced for operator sign-off (D-12).
</success_criteria>

<output>
Create `.planning/phases/22-per-section-visual-fixes/22-02-SUMMARY.md` when done (include the D-12 copy-accuracy point for operator review).
</output>
