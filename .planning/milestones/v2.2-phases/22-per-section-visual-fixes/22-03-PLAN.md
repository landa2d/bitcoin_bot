---
phase: 22-per-section-visual-fixes
plan: 03
type: execute
wave: 2
depends_on: ["22-01", "22-02"]
files_modified:
  - docker/web/site/app.js
  - docker/web/site/style-shared.css
autonomous: true
requirements: [GRID-01, GRID-02]
must_haves:
  truths:
    - "The Agent Economy map tier grids render as a 3-column grid on desktop (GRID-01 / D-06), collapsing 3 -> 2 at <=880px -> 1 at <=600px."
    - "The three tier sections (SUBSTRATE / BEHAVIOR / FRAME) with their tier-label headings are KEPT — not flattened (D-05); the deferred `frame` block (regulation-legal) stays full-width spanning all 3 columns (D-08)."
    - "A maturity legend appears ONCE under the `The Agent Economy` heading, inside the `.prose` header, mirroring the REAL 5-segment `.maturity-pill` (data-stage=1 -> 1 filled + 4 empty) so the legend's scale equals the cards' scale (GRID-02 / D-07) — not the mockup's 3-bar sketch."
    - "Each block's filled-segment count comes from stored `economy_map` maturity via `MATURITY_STAGE[b.maturity]` (already true in code) — no schema change and no new query; the existing `sb.schema('economy_map')` read is untouched (D-09)."
    - "The legend CSS is token-only (RHYTHM-01): `--mono`/`--ink-faint` chrome + `--space-*`; style-shared.css stays hex-free."
  artifacts:
    - path: "docker/web/site/style-shared.css"
      provides: "`.grid` changed to repeat(3,1fr); the single @640px collapse replaced by 3->2 @<=880px and 1 @<=600px; `.card-deferred` full-width kept; net-new token-only `.legend`/`.legend-label` CSS."
      contains: "repeat(3, 1fr)"
    - path: "docker/web/site/app.js"
      provides: "renderHub emits the maturity legend ONCE inside the `.prose` header (after the page-title), reusing a real `.maturity-pill data-stage=\"1\"` 5-seg sample; the economy_map read and tier grids are otherwise unchanged."
      contains: "class=\"legend\""
  key_links:
    - from: "stored economy_map block.maturity"
      to: "the per-card filled-segment count"
      via: "MATURITY_STAGE[b.maturity] -> renderMaturityPill data-stage (unchanged)"
      pattern: "MATURITY_STAGE\\[b.maturity\\]"
    - from: "the legend sample pill"
      to: "the same .maturity-pill/.seg CSS the cards use"
      via: "reusing the .maturity-pill data-stage markup (scale equality = GRID-02)"
      pattern: "maturity-pill"
---

<objective>
GRID-01 / GRID-02 — the Agent Economy `#map` landing section. Today the tier card grids are 2-column (`style-shared.css:261`) with no maturity legend, so the per-block 5-segment bars read as decoration and the 7 blocks tile awkwardly. This plan makes the grids 3-column with a maturity legend under the heading.

Purpose: per the ROADMAP success criteria, the map must render 3-col on desktop (collapsing 3->2 at <=880px -> 1 at <=600px) with a maturity legend under the heading, and each block's filled-segment count must match its stored `economy_map` maturity. Two tasks: (1) the `.grid` column-count + responsive-breakpoint change (GRID-01) keeping the tier grouping (D-05) and the full-width deferred card (D-08); (2) a maturity legend inserted ONCE in `renderHub`'s `.prose` header that REUSES the real 5-segment `.maturity-pill` so its scale literally equals the cards' (GRID-02 / D-07). GRID-02's "fill matches stored value" is already true in code via `MATURITY_STAGE[b.maturity]` (D-09) — verification is read-only, with no schema change and no new economy_map query.

Output: the `.grid`/breakpoint/`.legend` CSS in `style-shared.css` and the legend markup in `renderHub` (`app.js`). Source-only — live render verified in 22-04. This plan is Wave 2 (it shares `app.js` with 22-01 and `style-shared.css` with 22-02; sequence after both to keep single-file ownership clean).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-per-section-visual-fixes/22-CONTEXT.md
@.planning/phases/22-per-section-visual-fixes/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier grid 2 -> 3 columns + responsive 3/2/1 breakpoints, keeping deferred full-width (GRID-01 / D-06 / D-08)</name>
  <files>docker/web/site/style-shared.css</files>
  <read_first>
    - docker/web/site/style-shared.css — the `.grid` base rule (`:261-266`, `grid-template-columns: repeat(2, 1fr)` to change to `repeat(3, 1fr)`), the single mobile collapse `@media (max-width: 640px) { .grid { grid-template-columns: 1fr; } }` (`:339-343`, to REPLACE with the 3/2/1 breakpoints), `.card-deferred { grid-column: 1 / -1; }` (`:317-319`, KEEP unchanged — D-08), and `.tier-section + .tier-section` (`:235`, the major-boundary rule — do NOT touch).
    - .planning/phases/22-per-section-visual-fixes/22-PATTERNS.md §"GRID-01" (`:55-85`) — the current-grid + current-collapse excerpts, the mockup target (`repeat(3,1fr)` desktop -> `repeat(2,1fr)` @880 -> `1fr` @600), the `behavior`-tier 4-blocks-wraps-3+1 note (acceptable), and the `.card-deferred` keep-full-width note.
    - .planning/phases/22-per-section-visual-fixes/22-CONTEXT.md §"GRID-01/02" D-06 (3-col + 3/2/1 breakpoints; planner's call where they live) + D-08 (deferred card stays full-width).
    - .planning/docs/REDESIGN_CC_BRIEF.md §"TASK 4" (`:106-121`) — 2->3 col, responsive 3->2(<=880)->1(<=600).
  </read_first>
  <action>
    Change the `.grid` base rule (`:263`) from `grid-template-columns: repeat(2, 1fr);` to `grid-template-columns: repeat(3, 1fr);` (GRID-01 desktop, D-06). The `.grid` class is used ONLY by the map tierSection (confirmed: the only `.grid` consumer is `app.js:757`), so this affects only the map.

    REPLACE the single `@media (max-width: 640px) { .grid { grid-template-columns: 1fr; } }` block (`:339-343`) with TWO breakpoint rules implementing the 3->2->1 collapse — write each as compact single-line CSS:
    `@media (max-width: 880px) { .grid { grid-template-columns: repeat(2, 1fr); } }` and `@media (max-width: 600px) { .grid { grid-template-columns: 1fr; } }`. (Consolidating the breakpoints here, replacing the old 640px rule, is the chosen placement per D-06 discretion.) At 1 column the `.card-deferred` full-width modifier is a no-op (every card already spans), as the existing comment notes.

    Leave `.card-deferred { grid-column: 1 / -1; }` UNCHANGED (D-08 — the `frame`/regulation-legal block spans all 3 columns as the map's closing frame; the `behavior` tier's 4 blocks naturally wrap 3+1, acceptable). Do NOT touch the deferred-detection JS, `.tier-section`/`.tier-label`, the card chrome, or the Phase-20 width axes.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -Eq 'grid-template-columns: *repeat\(3, *1fr\)' docker/web/site/style-shared.css && grep -q 'max-width: 880px' docker/web/site/style-shared.css && ! grep -q '640px' docker/web/site/style-shared.css && grep -q 'grid-column: 1 / -1' docker/web/site/style-shared.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.grid` base rule is `grid-template-columns: repeat(3, 1fr)` (3-col desktop).
    - Source: the old `@640px` collapse is gone (`! grep '640px'`), replaced by `@media (max-width: 880px)` -> `repeat(2, 1fr)` and `@media (max-width: 600px)` -> `1fr` (single-line rules).
    - Source: `.card-deferred { grid-column: 1 / -1; }` is unchanged (D-08); tier sections/labels + Phase-20 width axes untouched.
    - Behavior (verified live in 22-04): the map shows 3 columns on desktop, 2 at <=880px, 1 at <=600px; the deferred `frame` block spans full width.
  </acceptance_criteria>
  <done>The tier `.grid` is 3-col desktop with 3->2 (<=880px) -> 1 (<=600px) breakpoints replacing the old @640px rule; the deferred card stays full-width; tier grouping + width axes untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Maturity legend under the map heading, mirroring the real 5-seg pill + token-only .legend CSS (GRID-02 / D-07 / D-09)</name>
  <files>docker/web/site/app.js, docker/web/site/style-shared.css</files>
  <read_first>
    - docker/web/site/app.js — `renderMaturityPill(b, deferred)` (`:578-590`, the exact 5-`.seg` markup + `data-stage` the legend MIRRORS; `MATURITY_STAGE[b.maturity] || 1` is the fill source — D-09), the `renderHub` `.prose` header trio (`:795-800`: `<h1 class="page-title">The Agent Economy</h1>` + `subline` + `hubIntroHtml`, the insert site), `tierSection` (`:753-761`, the grids render OUTSIDE `.prose` — do NOT move the legend there), and the single `sb.schema('economy_map')` blocks read in `loadHub` (`:609-613`, UNCHANGED — the legend adds NO new fetch, D-09).
    - docker/web/site/style-shared.css — `.maturity-pill` + `.maturity-pill .seg` + the `data-stage` fill rules (`:192-212`, the CSS the legend's pill REUSES so its scale equals the cards), and `.tier-label` (`:217-225`, the `--mono`/`--ink-faint`/uppercase chrome the `.legend` labels match).
    - .planning/phases/22-per-section-visual-fixes/22-PATTERNS.md §"GRID-02" (`:89-133`) — the pill markup to mirror, the insert-site excerpt, the legend FORM (`Maturity  ■ □ □ □ □  nascent -> established`), the `.legend` CSS form (mono/uppercase, token-only), and D-09 (fill already from stored maturity).
    - .planning/phases/22-per-section-visual-fixes/22-CONTEXT.md §"GRID-01/02" D-07 (5-segment legend mirroring the real pill, placed ONCE under the page-title inside `.prose`, new class, token-only) + D-09 (read-only; no schema/query change).
  </read_first>
  <action>
    In `renderHub` (`app.js`), insert a maturity legend ONCE into the `.prose` header `html` string (`:795-800`), immediately AFTER the `<h1 class="page-title">The Agent Economy</h1>` and BEFORE `subline` (so it sits directly under the section heading per the brief), INSIDE the `.prose` div. Build the legend as a static string: a `<div class="legend">` containing `<span class="legend-label">Maturity</span>`, then a REAL sample pill `<div class="maturity-pill" data-stage="1" aria-hidden="true">` with exactly five `<span class="seg"></span>` children (data-stage="1" -> 1 filled + 4 empty = `■ □ □ □ □`, mirroring the cards), then `<span class="legend-label">nascent &rarr; established</span>`. Reuse the EXISTING `.maturity-pill`/`.seg` markup verbatim (do NOT invent a second bar system) — the legend's scale equality with the cards IS the GRID-02 requirement (D-07). The pill is decorative in the legend (`aria-hidden`); the surrounding labels carry the meaning. Do NOT change the `economy_map` read, `tierSection`, the grid column count, or the deferred logic, and do NOT add any new fetch/query (D-09 — the legend is a STATIC sample; per-card fill stays `MATURITY_STAGE[b.maturity]`, already correct).

    In `style-shared.css`, append net-new token-only CSS (clearly commented "Phase 22 GRID-02 — maturity legend"), written as compact single-line rules:
    - `.legend` — `display:flex; align-items:center; gap:var(--space-sm); margin-top:var(--space-sm); margin-bottom:var(--space-md);`
    - `.legend .legend-label` — the `.tier-label` mono chrome: `font-family:var(--mono); font-size:11px; font-weight:600; letter-spacing:.08em; text-transform:uppercase; color:var(--ink-faint);`
    Use ONLY `var(--…)` tokens — NO hex literal (RHYTHM-01). The legend's `.seg` segments inherit the existing `.maturity-pill .seg` rules (no new segment styling). Confirm `node --check` passes on app.js.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node --check docker/web/site/app.js && grep -q 'class="legend"' docker/web/site/app.js && grep -q 'data-stage="1"' docker/web/site/app.js && grep -q 'MATURITY_STAGE\[b.maturity\]' docker/web/site/app.js && grep -Eq '\.legend *\{[^}]*display: *flex' docker/web/site/style-shared.css && grep -Eq '\.legend \.legend-label *\{[^}]*font-family: *var\(--mono\)' docker/web/site/style-shared.css && [ "$(grep -oE '#[0-9a-fA-F]{3,6}' docker/web/site/style-shared.css | wc -l)" -eq 0 ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: `renderHub` emits a `<div class="legend">` ONCE inside the `.prose` header, after the page-title, with `Maturity` + a real `.maturity-pill data-stage="1"` 5-`.seg` sample + a `nascent -> established` label (mirrors the card pill — scale equality, D-07).
    - Source: the legend reuses the existing `.maturity-pill`/`.seg` markup (no second bar system); the per-card fill still derives from `MATURITY_STAGE[b.maturity]` (D-09); the legend is a STATIC sample — no new economy_map query/fetch added, the existing `sb.schema('economy_map')` read is unchanged; `node --check` passes.
    - Source: `.legend` + `.legend .legend-label` CSS is token-only (`--mono`/`--ink-faint`/`--space-*`), single-line so the gates match; style-shared.css stays hex-free (`#[0-9a-fA-F]{3,6}` count == 0).
    - Behavior (verified live in 22-04): the legend renders once under the heading, its filled+empty segments match the cards' scale; each block's pill fill equals its stored maturity (read-only check).
  </acceptance_criteria>
  <done>A single maturity legend sits under `The Agent Economy` heading inside `.prose`, mirroring the real 5-seg pill (scale equality = GRID-02); token-only `.legend` CSS, hex-free; per-card fill from stored maturity unchanged (no new query/schema change); `node --check` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored economy_map blocks -> map cards (existing) | The existing `renderHub` read of `sb.schema('economy_map').from('blocks')` already crosses here, with every DB string `escapeHtml`'d (`app.js:742-743`). This plan adds NO new read and NO new dynamic sink: the legend is a STATIC string (literal labels + a fixed `data-stage="1"` sample pill). The grid change is pure CSS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Tampering | maturity legend markup | accept | The legend is a fully STATIC string — fixed labels + a constant `data-stage="1"` sample pill, no DB value, no user input, no dynamic interpolation. Nothing to inject. |
| T-22-03-02 | Information Disclosure | economy_map read (D-09) | mitigate | The plan adds NO new economy_map query — the legend is a static `data-stage="1"` sample, and the existing single `sb.schema('economy_map')` blocks read (Accept-Profile auto, no defensive `.eq('status')` — RLS is the boundary) is unchanged. The positive `MATURITY_STAGE[b.maturity]` gate + the read-only live check (22-04 step 6) confirm the per-card fill derives from stored maturity, not a new fetch. (The supabase-py `.in_()` footgun is a backend concern; this supabase-js frontend is not in that code path.) |
| T-22-03-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs — no `package.json`, no new CDN `<script>`. Package Legitimacy Gate not applicable. |

Net security delta: zero new attack surface. A CSS column-count change + a static legend string; the existing escaped economy_map read is untouched (no new query). ASVS L1, no high-severity threat.
</threat_model>

<verification>
- The tier `.grid` is 3-col desktop with 3->2 (<=880px) -> 1 (<=600px) breakpoints; the deferred card stays full-width; tier grouping preserved (Task 1 gate).
- A single maturity legend mirroring the real 5-seg pill sits under the heading inside `.prose`; per-card fill still from stored `MATURITY_STAGE[b.maturity]`; no new economy_map query (the legend is static); legend CSS token-only + hex-free (Task 2 gate).
- Live confirmation (3 columns, visible legend with matching scale, pill fill == stored maturity per block) is owned by 22-04 — read-only, no schema change.
</verification>

<success_criteria>
GRID-01 + GRID-02 ship at source:
- The Agent Economy map renders 3-col on desktop (3->2 at <=880px -> 1 at <=600px); the three tier sections + the full-width deferred frame block are preserved.
- A maturity legend appears once under the heading, mirroring the real 5-segment pill so its scale equals the cards'.
- Each block's filled-segment count derives from stored `economy_map` maturity (already true in code; no schema change, no new query).
- All net-new CSS is token-only; style-shared.css stays hex-free.
</success_criteria>

<output>
Create `.planning/phases/22-per-section-visual-fixes/22-03-SUMMARY.md` when done.
</output>
