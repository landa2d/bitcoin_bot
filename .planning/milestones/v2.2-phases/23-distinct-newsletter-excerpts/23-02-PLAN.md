---
phase: 23-distinct-newsletter-excerpts
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified: []
autonomous: false
requirements: [EXCERPT-01]

must_haves:
  truths:
    - "SC#1 (live): on the deployed site editions 29 and 30 show DIFFERENT preview text in the archive list, in BOTH Technical and Strategic modes (D-08 mode-aware verified on the real rows, not just in code)"
    - "SC#2 (live): the list renders in the indexed-row format — number · title · one-line summary · date — clamped to 2 lines, with no leaked link URLs in any summary"
    - "SC#3: strip-at-render only — no newsletters schema change, no stored summary field, no content-pipeline change; every Supabase query byte-identical"
    - "Deploy discipline: scoped `docker compose up -d --build web` (SERVICE key web, NO --delete) from the main tree /root/bitcoin_bot/docker after a prod↔main drift check + operator approval; the entrypoint __SUPABASE_*__ sed-substitution intact (verify the SUBSTITUTED render, not raw source)"
    - "No regression: the hero (Latest: Edition #N), the Technical/Strategic mode toggle, and the #/edition/<n> deep links still work; the maturity pill does not overlap the nav"
  artifacts: []
  key_links:
    - from: "operator browser"
      to: "https://aiagentspulse.com (substituted /srv/app.js)"
      via: "scoped web container rebuild + live render check"
      pattern: "edition/2[9]|edition/30"
---

<objective>
Ship the Phase 23 excerpt changes to the live site and prove EXCERPT-01 on the
real data. This plan is ORCHESTRATOR-OWNED and WORKTREE-UNSAFE: the scoped web
rebuild must run from the absolute main tree (where config/.env + the deployed
container live), never from a git worktree.

Purpose: the acceptance proof for this phase is concrete and visual — editions
29 and 30 must show different preview text in the archive list in BOTH modes on
the deployed (substituted) render. A code-only pass is insufficient (the Phase
22 lesson: reproduce against the real ed 29/30 data on the live render).

Output: a deployed web container + operator-confirmed live verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-distinct-newsletter-excerpts/23-CONTEXT.md
@.planning/phases/23-distinct-newsletter-excerpts/23-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Pre-deploy gate — prod↔main drift check + /diff review + operator approval</name>
  <read_first>
    - /root/bitcoin_bot/.planning/STATE.md (the standing deploy discipline: branch + /diff per work group + prod↔main drift check + scoped web rebuild, NO --delete, operator approval; /root/bitcoin_bot IS prod — local scoped rebuilds)
    - /root/bitcoin_bot/.planning/phases/23-distinct-newsletter-excerpts/23-01-SUMMARY.md (what Plan 01 changed)
    - /root/bitcoin_bot/CLAUDE.md (deploy & web-entrypoint substitution discipline)
  </read_first>
  <decision>Approve the scoped `web` rebuild of the Phase 23 excerpt changes for the live site.</decision>
  <context>
    Show the operator: (1) a prod↔main drift check (is the deployed tree behind
    main for the web files?); (2) the `/diff` of docker/web/site/app.js +
    docker/web/site/style-shared.css for this phase (the new excerpt helpers,
    renderList rewrite, and `.row` CSS). Confirm the change is scoped to the two
    web files only — no schema/migration, no pipeline, no other service.
  </context>
  <options>
    <option id="approve-deploy">
      <name>Approve — proceed to scoped web rebuild</name>
      <pros>Ships EXCERPT-01; verifiable on live ed29/30</pros>
      <cons>None beyond the standard scoped web rebuild</cons>
    </option>
    <option id="hold">
      <name>Hold — adjust source first</name>
      <pros>Address any diff concern before deploy</pros>
      <cons>Phase stays unshipped</cons>
    </option>
  </options>
  <acceptance_criteria>
    - A prod↔main drift summary for the web files is presented.
    - The `/diff` of app.js + style-shared.css is shown and confirmed scoped to those two files (no migration, no other service, no `__SUPABASE_*__` placeholder change).
    - Operator selects approve-deploy before Task 2 runs.
  </acceptance_criteria>
  <resume-signal>Select: approve-deploy or hold</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scoped web rebuild from the main tree (worktree-unsafe)</name>
  <files>(deploy only — no source files modified)</files>
  <read_first>
    - /root/bitcoin_bot/.planning/STATE.md (web compose service name gotcha: use the SERVICE key `web`, NOT the container_name `agentpulse-web`; NO --delete; scoped rebuild ships the corrected app.js/style-shared.css via the entrypoint sed-substitution)
    - /root/bitcoin_bot/CLAUDE.md (build & run commands)
  </read_first>
  <action>
    From the ABSOLUTE main-tree path, run the scoped rebuild:
    `cd /root/bitcoin_bot/docker && docker compose up -d --build web`. Use the
    compose SERVICE key `web` (NOT the `agentpulse-web` container_name — the
    latter errors "no such service"). Do NOT pass `--delete`. Do NOT run a full
    `scripts/deploy.sh`. This MUST run on the main tree (not a worktree): the
    deployed container + config/.env live there, and the entrypoint sed-
    substitutes `__SUPABASE_URL__`/`__SUPABASE_ANON_KEY__` into /srv/app.js at
    container start. After `up`, confirm the web container is healthy/running.
  </action>
  <acceptance_criteria>
    - `cd /root/bitcoin_bot/docker && docker compose ps web` shows the web service Up/healthy after the rebuild.
    - The served /srv/app.js is the SUBSTITUTED copy (no literal `__SUPABASE_URL__` in the running container's served file) — e.g. `docker compose exec web grep -c "__SUPABASE_URL__" /srv/app.js` returns 0.
    - The served app.js contains the new row markup (`docker compose exec web grep -c 'class="row"' /srv/app.js` >= 1).
    - No `--delete` was used; only the `web` service was rebuilt.
  </acceptance_criteria>
  <verify>
    <automated>cd /root/bitcoin_bot/docker && docker compose ps web | grep -Eiq "up|running|healthy"</automated>
  </verify>
  <done>
    The web container is rebuilt + running from the main tree; the served
    /srv/app.js is substituted (no placeholder literal) and carries the new
    indexed-row render.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-render verification — SC#1 (ed29 ≠ ed30, both modes) + SC#2 indexed rows + no regression</name>
  <read_first>
    - /root/bitcoin_bot/.planning/phases/23-distinct-newsletter-excerpts/23-CONTEXT.md (the acceptance proof: ed29 vs ed30 must show DIFFERENT preview text in BOTH Technical and Strategic modes; the indexed-row format)
    - /root/bitcoin_bot/.planning/REQUIREMENTS.md (EXCERPT-01)
  </read_first>
  <what-built>
    The newsletter archive list now renders as indexed rows (number · title ·
    one-line summary · date) with a strip-at-render excerpt that skips the
    `## Read This, Skip the Rest` header + the recap intro and shows each
    edition's first distinct "This week…" sentence (2-line clamped), mode-aware.
  </what-built>
  <how-to-verify>
    On the LIVE site (the deployed/substituted render — hard-refresh to bust cache):
    1. Scroll to the newsletter archive list. Confirm rows render as
       number · title · summary · date (indexed-row format, SC#2).
    2. In TECHNICAL mode, read the summary lines for Edition 30 and Edition 29.
       They MUST be visibly DIFFERENT text (SC#1). Expected shape: ed30 ≈ "The
       payments thesis crossed into consumer scale: Block kicked off Cash App's
       phased stablecoin rollout…"; ed29 ≈ "This week it got specific.
       Researchers exposed BadHost…". Confirm NO `## Read This, Skip the Rest`
       text, NO "Last week…" recap, and NO leaked `(https://…)` URLs in any
       summary.
    3. Toggle to STRATEGIC mode. Confirm ed29 and ed30 summaries change (mode-
       aware, D-08) and are STILL different from each other (SC#1 in both modes).
    4. Confirm long summaries clamp to 2 lines with ellipsis (D-06) and the row
       layout does not break.
    5. No regression: the hero still shows "Latest: Edition #N · <date>"; the
       Technical/Strategic toggle still works; clicking a row opens
       `#/edition/<n>` (deep link preserved); the maturity pill in the map
       section does not overlap the nav.
  </how-to-verify>
  <acceptance_criteria>
    - Edition 29 and Edition 30 summaries are different in Technical mode (SC#1).
    - Edition 29 and Edition 30 summaries are different in Strategic mode (SC#1, D-08).
    - No `Read This, Skip the Rest` text, no recap "Last week…" opener, and no `(https://…)` link URL appears in any visible summary.
    - Rows render as number · title · summary · date and clamp to 2 lines (SC#2/D-06).
    - Hero, mode toggle, and `#/edition/<n>` deep links work; maturity pill does not overlap the nav (no regression).
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issues observed (which edition / which mode).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main tree → deployed web container | The scoped rebuild ships source from /root/bitcoin_bot (prod). A worktree build would ship stale code or lack config/.env. |
| operator review → live deploy | The diff + drift are reviewed and approved before the container is rebuilt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Tampering (deploy blast radius) | scoped web rebuild | mitigate | SERVICE key `web` only, NO `--delete`, NO full deploy.sh, run from the absolute main tree after a prod↔main drift check + operator approval (Task 1 gate). Acceptance asserts only `web` rebuilt, no `--delete`. |
| T-23-05 | Spoofing (false-positive verification) | live render check | mitigate | Verify the SUBSTITUTED /srv/app.js (entrypoint sed) on the live site with a hard refresh — assert no `__SUPABASE_URL__` literal in the served file — not raw source bytes; confirm SC#1 on the REAL editions 29 vs 30 in both modes (the Phase 22 reproduce-on-live lesson). |
| T-23-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No package installs in this phase — scoped rebuild of an existing image; no new dependencies. N/A. |
</threat_model>

<verification>
- Pre-deploy gate passed (drift + scoped /diff + operator approval).
- web container Up/healthy from the main tree; served /srv/app.js substituted
  (no `__SUPABASE_URL__` literal) and carrying the new row markup.
- Operator confirms SC#1 (ed29 ≠ ed30 in BOTH modes), SC#2 (indexed rows, 2-line
  clamp, no leaked URLs), and no regression on the live render.
</verification>

<success_criteria>
- EXCERPT-01 proven live: editions 29 and 30 show distinct preview text in the
  indexed-row format, in both Technical and Strategic modes, strip-at-render
  only (no schema/pipeline change).
- Deploy ran scoped (`web`, no `--delete`) from the main tree with operator
  approval; verified on the substituted render.
</success_criteria>

<output>
Create `.planning/phases/23-distinct-newsletter-excerpts/23-02-SUMMARY.md` when done.
</output>
