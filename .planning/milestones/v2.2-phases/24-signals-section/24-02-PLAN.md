---
phase: 24-signals-section
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/web/site/app.js
  - docker/web/site/style-shared.css
  - docker/web/site/index.html
autonomous: true
requirements: [SIGNAL-01, SIGNAL-02, SIGNAL-03, SIGNAL-04]
must_haves:
  truths:
    - "The #signals section renders tier-1 source rows newest-first, capped to ~15 (SIGNAL-01)"
    - "Each row is an external <a> (target=_blank, rel=noopener noreferrer, ↗ hover) showing date · headline · source domain (SIGNAL-02)"
    - "A 'View all' control reveals the rest inline up to ~50, no route (SIGNAL-01 / D-03)"
    - "A broken/missing feed path shows a LOUD diagnostic + console.error; a genuinely empty tier-1 week shows a benign quiet empty state (SIGNAL-04 / D-07)"
    - "The signals fetch is wired into ensureLandingDataLoaded (gated, not a premature fetch on the section)"
  artifacts:
    - path: "docker/web/site/app.js"
      provides: "fetchSignals() (3-way split), renderSignals(data, expanded), signalHost(url, fallback), view-all handler, ensureLandingDataLoaded wiring"
      contains: "function fetchSignals"
    - path: "docker/web/site/style-shared.css"
      provides: "Signal-row source-domain (.row .host) + view-all button styling (token-only)"
      contains: ".row .host"
    - path: "docker/web/site/index.html"
      provides: "Placeholder 'Coming soon.' removed from the #signals .about intro; #signals-list render target intact"
      contains: "id=\"signals-list\""
  key_links:
    - from: "ensureLandingDataLoaded"
      to: "fetchSignals"
      via: "Promise.all"
      pattern: "fetchSignals\\(\\)"
    - from: "fetchSignals"
      to: "public.signals_feed"
      via: "sb.from('signals_feed')"
      pattern: "from\\('signals_feed'\\)"
    - from: "renderSignals"
      to: "#signals-list"
      via: "innerHTML at the escaped sink"
      pattern: "getElementById\\('signals-list'\\)"
---

<objective>
Fill the existing static `#signals` shell (built in Phase 21) with the live tier-1 feed: add `fetchSignals()` / `renderSignals()` / `signalHost()` + a "View all" expand-in-place control to `app.js`, the source-domain + view-all styling to `style-shared.css`, and remove the "Coming soon." placeholder from `index.html`. Reuses the Phase 23 `.row` indexed-row family, `escapeHtml()`/`safeHttpUrl()`/`formatDate()` helpers, and the gated `ensureLandingDataLoaded()` load seam.

Purpose: SIGNAL-01 (capped feed + view-all), SIGNAL-02 (safe external links showing date · headline · domain), SIGNAL-03 (reachable as the `#signals` landing section — already wired into scroll-spy by Phase 21), and SIGNAL-04's frontend half (D-07 fail-loud: distinguish a broken feed from a benign empty week by HTTP response, not by emptiness).

Output: edited `app.js`, `style-shared.css`, `index.html` (source only — worktree-safe; no docker, no network). The interface contract with Plan 24-01 is the view name **`public.signals_feed`** exposing **`id, title, source_url, source, scraped_at`**. The live render proof (after the migration apply + scoped web rebuild) is Plan 24-03.

⚠ CRITICAL — `docker/web/site/app.js` contains a NUL sentinel byte (3 NUL bytes; the file is served raw, no transpile). All grep gates against it MUST use `grep -a` (text mode) or grep reports it as binary and silently matches nothing. A parse-time SyntaxError in app.js blanks the WHOLE SPA — `node --check` is a hard gate, and NO regex lookbehind is allowed (WebKit<16.4 crash — Phase 23 WR-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-signals-section/24-CONTEXT.md
@.planning/phases/24-signals-section/24-PATTERNS.md

# Analog source files (read before editing)
@docker/web/site/app.js
@docker/web/site/index.html
@docker/web/site/style-shared.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: app.js — fetchSignals (3-way split) + renderSignals + signalHost + view-all + wiring</name>
  <read_first>
    - docker/web/site/app.js (lines 451-458) — `ensureLandingDataLoaded()` Promise.all (currently `[loadList(), loadHub()]`) — the gated load seam to add `fetchSignals()` to.
    - docker/web/site/app.js (lines 502-526) — `loadList()` fetch shape (`sb.from(...).select(...).order(...)`); ⚠ it CONFLATES error+empty in one branch — `fetchSignals()` must NOT (D-07 deviation).
    - docker/web/site/app.js (lines 462-500) — `renderList()` `.row` builder: whole-`<a>`-is-the-click-target, `escapeHtml()` on every DB string, `formatDate()` on the date column.
    - docker/web/site/app.js (lines 665-684) — `escapeHtml()` (665), `safeHttpUrl()` (674), `formatDate()` (679) — reuse verbatim.
    - docker/web/site/app.js (lines 744, 978) — the `console.error('loadHub error:', error)` idiom to match for the loud branch.
    - docker/web/site/app.js (lines 1321-1326) — `LANDING_SECTION_IDS` already includes `'signals'` (scroll-spy reachability is Phase 21's; do NOT touch).
    - .planning/phases/24-signals-section/24-PATTERNS.md (§app.js) and 24-CONTEXT.md (D-02..D-07) — the authoritative render contract.
  </read_first>
  <action>
    Add four net-new functions to `app.js` (place them near the list functions, all `var`/`function`, ES3-universal — NO arrow functions in a way that breaks raw serving is fine since the file uses `function`; NO regex lookbehind; the `www.`-strip uses an anchored `^www\.` prefix replace).

    `signalHost(url, fallback)` (D-05): return `new URL(url).hostname` with a leading `www.` stripped via an anchored `^www\.` replace, inside try/catch; on throw return `fallback || ''`. (`new URL(...)` is already used at app.js:69/231.) This is the reader-facing source domain (e.g. `deepmind.google`, `news.ycombinator.com`).

    `fetchSignals()` (async; D-07 three-way split — the deliberate deviation from `loadList`): query `sb.from('signals_feed').select('id, title, source_url, source, scraped_at').order('scraped_at', { ascending: false }).order('id', { ascending: false }).limit(50)`. (Re-state `.order(...)` on the QUERY — PostgREST does not guarantee the view's internal ORDER BY. `50` is the D-03 hard ceiling.) Resolve `#signals-list`; if absent, return. Then split EXACTLY three ways: (a) `error` → `console.error('fetchSignals error:', error)` AND set `#signals-list` innerHTML to a LOUD diagnostic paragraph with class `signals-error` reading "Signals feed is temporarily unavailable." (this fires when the view/grant is missing — a missing view returns an HTTP error, not `200 []`); (b) `!data || data.length === 0` → set innerHTML to a quiet paragraph class `signals-empty` reading "No tier-1 signals this week." (benign — a genuinely thin week); (c) rows → stash `window.currentSignals = data` and call `renderSignals(data, false)`. Return the promise so it participates in the WR-01 settle gate (like `loadList`).

    `renderSignals(data, expanded)` (D-02/D-04/D-05/D-06): define module constant DEFAULT_VISIBLE = 15. Compute `var rows = expanded ? data : data.slice(0, DEFAULT_VISIBLE)`. Build HTML by mapping each row to a whole-`<a class="row signal-row">` whose `href` is `safeHttpUrl(row.source_url)` (skip the row — return '' in the map and filter — if `safeHttpUrl` returns null; D-02 already excludes nulls upstream, this is defense-in-depth), with `target="_blank"` and `rel="noopener noreferrer"`. Row anatomy: col1 `<span class="num">↗</span>` (the external-link affordance; reuses `.row .num` styling and colors to `--accent` on `.row:hover .num`); col2 `<span><p class="title">{escapeHtml(title)}</p><p class="host">{escapeHtml(signalHost(row.source_url, row.source))}</p></span>`; col3 `<span class="date">{escapeHtml(formatDate(row.scraped_at))}</span>`. After the rows, IF `!expanded && data.length > DEFAULT_VISIBLE`, append a `<button type="button" class="view-all" id="signals-view-all">View all signals (` + data.length + `)</button>`. Set `#signals-list` innerHTML once. Then `var btn = document.getElementById('signals-view-all'); if (btn) btn.addEventListener('click', function () { renderSignals(window.currentSignals, true); });` (D-03 expand-in-place — re-render the cached batch, NO route, NO re-query). Every DB-derived string passes through `escapeHtml()` at this innerHTML sink; `↗` and the static button label are literal.

    Wiring: in `ensureLandingDataLoaded()` change the `Promise.all([loadList(), loadHub()])` to `Promise.all([loadList(), loadHub(), fetchSignals()])` so the signals fetch runs exactly once when the landing is shown (the gated, non-premature fetch Phase 21 mandated — `#signals` must not fetch the RLS-blocked path before the landing renders). Do NOT add a new `<script>` tag or a second Supabase client.
  </action>
  <acceptance_criteria>
    - Parse-safe (a SyntaxError blanks the SPA): `node --check docker/web/site/app.js` exits 0.
    - No regex lookbehind (WR-01): `grep -ac '(?<' docker/web/site/app.js` returns `0`.
    - The three functions exist: `grep -a "function fetchSignals"`, `grep -a "function renderSignals"`, `grep -a "function signalHost"` each match (use `grep -a` — NUL sentinel).
    - Queries the view (contract with 24-01): `grep -a "from('signals_feed')" docker/web/site/app.js` matches; the select lists the 5 view columns and the query re-states `.order('scraped_at', { ascending: false })`.
    - Fail-loud 3-way split (D-07): all three present — `grep -a "console.error('fetchSignals" docker/web/site/app.js`, `grep -a "Signals feed is temporarily unavailable" docker/web/site/app.js`, `grep -a "No tier-1 signals this week" docker/web/site/app.js` each match.
    - Safe external link (D-06): `grep -a 'target="_blank"' docker/web/site/app.js` and `grep -a 'rel="noopener noreferrer"' docker/web/site/app.js` and `grep -a "safeHttpUrl(" docker/web/site/app.js` each match; the href is `safeHttpUrl(...)`-gated.
    - Date = scraped_at via the shared formatter (D-04): `grep -a "formatDate(.*scraped_at\|scraped_at" docker/web/site/app.js` confirms `scraped_at` is the date column (no `published_at` in the signals path).
    - Hostname escaped at sink (D-05): `grep -a "escapeHtml(signalHost" docker/web/site/app.js` matches (or escapeHtml wraps the host value at the sink).
    - View-all expand-in-place (D-03): `grep -a "signals-view-all" docker/web/site/app.js` matches and the handler re-renders `window.currentSignals` with `true` — `grep -a "renderSignals(window.currentSignals, true)" docker/web/site/app.js` matches; no `#/signals` route is added — `grep -a "#/signals" docker/web/site/app.js` returns nothing.
    - Gated wiring: `grep -a "fetchSignals()" docker/web/site/app.js` appears inside the `ensureLandingDataLoaded` Promise.all — `grep -a "Promise.all(\[loadList(), loadHub(), fetchSignals()\])" docker/web/site/app.js` matches.
  </acceptance_criteria>
  <verify>
    <automated>node --check docker/web/site/app.js && [ "$(grep -ac '(?<' docker/web/site/app.js)" = "0" ] && grep -aq "function fetchSignals" docker/web/site/app.js && grep -aq "from('signals_feed')" docker/web/site/app.js && grep -aq "console.error('fetchSignals" docker/web/site/app.js && grep -aq "Signals feed is temporarily unavailable" docker/web/site/app.js && grep -aq "No tier-1 signals this week" docker/web/site/app.js && grep -aq 'rel="noopener noreferrer"' docker/web/site/app.js && grep -aq "renderSignals(window.currentSignals, true)" docker/web/site/app.js && grep -aq "Promise.all(\[loadList(), loadHub(), fetchSignals()\])" docker/web/site/app.js && echo PASS</automated>
  </verify>
  <done>
    app.js parses clean, has `fetchSignals()` (3-way fail-loud split querying `signals_feed`), `renderSignals(data, expanded)` (escaped, `safeHttpUrl`-gated external `<a>` rows, date=`scraped_at`, source-domain via `signalHost`, capped at 15 with a view-all that re-renders the cached batch inline), `signalHost()` (lookbehind-free `^www\.` strip), and is wired into `ensureLandingDataLoaded`'s Promise.all. No new `<script>`, no second client, no `#/signals` route.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: style-shared.css source-domain + view-all styling; index.html placeholder removal</name>
  <read_first>
    - docker/web/site/style-shared.css (lines 185-267) — the Phase 23 `.row` / `.archive-label` family (token-only, zero hex). `.row .num` (221), `.row .title` (227), `.row .date` (249), `.row:hover .num` (219), `.row:focus-visible` (257), the 600px reflow (264). Reuse `.row` directly for signal rows; add ONLY the net-new `.row .host` + `.view-all`.
    - docker/web/site/index.html (lines 82-96) — the static `#signals` shell (Phase 21, LOCKED placement/order/anchor/copy). The only edit: the `.about` `<p>` (line 89) ends with " Coming soon." — drop those two words; keep `<div class="wide" id="signals-list"></div>` (line 93) exactly as-is (the render target).
    - .planning/phases/24-signals-section/24-PATTERNS.md (§style-shared.css, §index.html) — token-mapping gotcha: mockup tokens `--line-soft`/`--violet` do NOT exist in `:root`; use `--line`/`--accent`. ZERO hex.
  </read_first>
  <action>
    In `style-shared.css`, add two net-new, token-only rules near the `.row` family (do NOT alter the shared `.row` grid the newsletter list depends on): (1) `.row .host` — the source-domain sub-line under `.title`: `font-family: var(--mono)`, a small size (e.g. `12px`), `color: var(--ink-faint)`, `margin: 0`, no hex. (2) `.view-all` — the expand-in-place control: token-only (e.g. `font-family: var(--mono)`, small size, `color: var(--accent)`, a transparent/`var(--line)` border or plain text-button look, `background: none`, `cursor: pointer`, sensible padding via `var(--space-*)`); add a `:hover`/`:focus-visible` state using `var(--accent)` outline consistent with `.row:focus-visible`. Every value is a `var(--…)` token or a unitless/px primitive — ZERO hex. The `↗` arrow needs no new rule (it sits in `.row .num` and colors on `.row:hover .num`); optionally add a `.signal-row:hover .num` nudge only if needed, still token-only.

    In `index.html`, edit the `#signals` `.about` paragraph (line ~89) to remove the trailing " Coming soon." so the sentence ends at "…linked straight through." Leave the eyebrow / title / sub / `#signals-list` container untouched (Phase-21-LOCKED copy and the render target).
  </action>
  <acceptance_criteria>
    - Source-domain rule present: `grep -q "\.row \.host" docker/web/site/style-shared.css`.
    - View-all rule present: `grep -q "\.view-all" docker/web/site/style-shared.css`.
    - Token-only (no hex) in the net-new rules: the lines added for `.row .host` and `.view-all` contain no `#RRGGBB` literal — `grep -nE "#[0-9a-fA-F]{3,6}" docker/web/site/style-shared.css` shows no NEW hex within the added `.row .host` / `.view-all` blocks (existing file hex elsewhere is out of scope; the added rules use `var(--…)` only).
    - Placeholder removed: `grep -qi "Coming soon" docker/web/site/index.html` returns NON-zero (no match) — the words are gone.
    - Render target intact: `grep -q 'id="signals-list"' docker/web/site/index.html` matches; the `#signals` eyebrow/title/sub copy is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "\.row \.host" docker/web/site/style-shared.css && grep -q "\.view-all" docker/web/site/style-shared.css && ! grep -qi "Coming soon" docker/web/site/index.html && grep -q 'id="signals-list"' docker/web/site/index.html && echo PASS</automated>
  </verify>
  <done>
    `style-shared.css` has token-only `.row .host` (source-domain sub-line) and `.view-all` (expand control) rules with zero new hex; the signal rows reuse the existing `.row` family and `↗`-on-hover affordance. `index.html`'s `#signals` placeholder "Coming soon." is gone, the intro sentence and the `#signals-list` render target are intact, and the Phase-21-locked section copy/anchor/order is untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB-derived values → browser DOM (innerHTML) | `title`, `source_url`, `source` come from `source_posts` (scraped third-party content) and are rendered into `#signals-list` via `innerHTML` — untrusted content crossing into the DOM. |
| anon → PostgREST `signals_feed` (read) | The frontend reads the public view with the anon key; a missing/broken view returns an HTTP error the UI must surface, not swallow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Tampering (stored XSS) | `renderSignals` innerHTML sinks | mitigate | `escapeHtml()` on `title` and the derived hostname at every innerHTML sink (the established `escapeHtml` idiom). Verified by inspection + the escaped-host gate. |
| T-24-06 | Tampering (XSS via href) | `source_url` used as `href` | mitigate | `safeHttpUrl()` http(s)-only scheme allowlist BEFORE the value becomes an `href` (escapeHtml does NOT block `javascript:`/`data:`); rows failing the gate are skipped; `target="_blank" rel="noopener noreferrer"` prevents reverse-tabnabbing. |
| T-24-07 | Denial of Service / Repudiation | broken/missing view feed path | mitigate | Fail-loud 3-way split (D-07): `error`→loud diagnostic + `console.error`; `200 []`→benign empty; rows→render. A missing view/grant returns an HTTP error (not `200 []`), so the loud branch fires exactly when the migration is absent — never a silent empty feed. |
| T-24-08 | Availability | parse-time SyntaxError blanks the SPA | mitigate | Net-new JS stays `var`/`function`, ES3-universal, NO regex lookbehind (WR-01 — WebKit<16.4 crash); `node --check` is a blocking gate; the `^www\.` strip is an anchored prefix replace, not a lookbehind. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (reuses the CDN supabase client + existing helpers) — no new dependencies. |
</threat_model>

<verification>
- `node --check docker/web/site/app.js` passes (no SPA-blanking SyntaxError); zero regex lookbehind.
- All app.js grep gates use `grep -a` (NUL sentinel) and pass: the three functions exist, the view is queried, the 3-way fail-loud split is present, the external link is `safeHttpUrl`-gated with `target`/`rel`, the view-all re-renders the cached batch, and the fetch is wired into the gated Promise.all.
- CSS rules are token-only (zero new hex); the placeholder is removed and the render target intact.
- HIGH-severity XSS threats (T-24-05/06) are dispositioned `mitigate` with concrete `escapeHtml()`/`safeHttpUrl()` sinks — no HIGH threat unmitigated.
</verification>

<success_criteria>
- The `#signals` section will render tier-1 source rows newest-first, capped to ~15, each a safe external `<a>` (date · headline · source domain, `↗` hover), with a working "View all" inline expand and a fail-loud-vs-benign-empty distinction — pending the live render proof in Plan 24-03.
- The signals fetch is gated through `ensureLandingDataLoaded` (no premature fetch on the section).
- No new `<script>` tag, no second Supabase client, no `#/signals` route, no change to the Phase-21-locked section shell beyond removing the placeholder words.
</success_criteria>

<output>
Create `.planning/phases/24-signals-section/24-02-SUMMARY.md` when done.
</output>
