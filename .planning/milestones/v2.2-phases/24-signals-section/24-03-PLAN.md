---
phase: 24-signals-section
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified: []
autonomous: false
requirements: [SIGNAL-01, SIGNAL-02, SIGNAL-03, SIGNAL-04]
must_haves:
  truths:
    - "Live anon REST GET on public.signals_feed returns HTTP 200 (rows, or [] only on a genuinely thin tier-1 week) — NEVER 4xx (SIGNAL-04 live proof)"
    - "Live anon GET select=* on signals_feed returns exactly the 5 whitelisted keys; anon GET select=body → error; base source_posts leaks no sensitive rows to anon (D-01 live proof)"
    - "The deployed #signals section renders the live tier-1 feed newest-first, capped, view-all expands inline, rows open off-site safely, reachable via scroll-spy + #signals deep-link (SIGNAL-01/02/03 live proof)"
    - "A missing/broken view path produces the LOUD diagnostic, not a silent empty feed (SIGNAL-04 / D-07 fail-loud proof)"
  artifacts: []
  key_links:
    - from: "orchestrator (main tree)"
      to: "live Supabase project zxzaaqfowtqvmsbitqpu"
      via: "Supabase MCP apply_migration (NOT supabase db push)"
      pattern: "044_signals_anon_view"
    - from: "orchestrator (main tree)"
      to: "live web container"
      via: "scoped docker compose up -d --build web (NO --delete)"
      pattern: "compose up -d --build web"
---

<objective>
Take the Plan 24-01 migration and Plan 24-02 frontend live: (1) [BLOCKING] apply `044_signals_anon_view.sql` to the live Supabase project via the **Supabase MCP `apply_migration` tool** and prove the anon read path works AND is column/row-bounded; (2) the gated scoped `web` rebuild; (3) operator live-render verification of all four SIGNAL criteria + the fail-loud behavior + no Phase 21/22/23 regression.

Purpose: Close SIGNAL-01..04 with live proof. The migration apply is the schema-push gate that build/type checks cannot catch (the frontend has no compile dependency on the view, so without the live apply the feed would render the loud error or stay unverified). The over-exposure assertions are the security crux (D-01) verified against the LIVE view, not the source.

⚠ ORCHESTRATOR-OWNED / WORKTREE-UNSAFE (per CLAUDE.md + project memory): the migration apply (Supabase MCP, main tree) and the scoped `web` rebuild (main tree) MUST NOT be run by a worktree executor — a worktree executor would build stale code and/or apply against the wrong context. This entire plan is `autonomous: false`: the orchestrator/operator performs each step from `/root/bitcoin_bot`. This is NOT `supabase db push` CLI — it is the MCP `apply_migration` tool.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-signals-section/24-CONTEXT.md
@.planning/phases/24-signals-section/24-PATTERNS.md
@supabase/migrations/044_signals_anon_view.sql
@docker/web/site/app.js
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migration 044 live (Supabase MCP) + prove the bounded anon read path</name>
  <read_first>
    - supabase/migrations/044_signals_anon_view.sql (the Plan 24-01 output — the exact SQL to apply).
    - .planning/phases/24-signals-section/24-CONTEXT.md (D-01, D-07) and 24-PATTERNS.md (§migration 044 — the security-definer rationale and the live anon-read expectation).
    - CLAUDE.md (Supabase project ref `zxzaaqfowtqvmsbitqpu`; deploy discipline; "migration applied by the orchestrator from the main tree, worktree-unsafe").
    - config/.env — the anon key (`SUPABASE_ANON_KEY`) + `SUPABASE_URL` for the live REST assertions (do NOT print secrets into the SUMMARY).
  </read_first>
  <action>
    From the main tree (`/root/bitcoin_bot`), the orchestrator applies `supabase/migrations/044_signals_anon_view.sql` to the live Supabase project (ref `zxzaaqfowtqvmsbitqpu`) using the **Supabase MCP `apply_migration` tool** — NOT `supabase db push`, NOT a worktree executor. Pass the migration name `044_signals_anon_view` and the SQL body verbatim.

    Then prove the LIVE state with anon-key REST calls (anon key + URL from `config/.env`):
    - View reachable (the SIGNAL-04 / fail-loud-absence gate): `GET {SUPABASE_URL}/rest/v1/signals_feed?select=id&limit=1` with the anon `apikey`/`Authorization` headers returns HTTP **200** (NOT 4xx). A 4xx here means the view or grant is missing — STOP and fix before any deploy (this is exactly the state the frontend's loud branch is built to surface).
    - Column ceiling (D-01 live proof): `GET .../signals_feed?select=*&limit=1` returns objects whose keys are EXACTLY `id, title, source_url, source, scraped_at` — no `body`/`metadata`/`score`/`author`/`comment_count`/`tags`. And `GET .../signals_feed?select=body&limit=1` returns an error (column `body` does not exist on the view).
    - Row scope (D-01 live proof): the returned rows (if any) are tier-1 with non-null `source_url`, newest-first by `scraped_at`.
    - Base table still blocked: `GET .../source_posts?select=body&limit=1` with the anon key returns no sensitive rows (200 `[]` because RLS filters all rows, or a permission error — either proves anon cannot read the base `body`).

    Record the observed HTTP codes + the `select=*` key set in the SUMMARY (no secret values).
  </action>
  <acceptance_criteria>
    - The migration is applied to the live project via the MCP `apply_migration` tool (confirmed in the migrations list / no apply error).
    - Anon `GET /rest/v1/signals_feed?select=id&limit=1` → HTTP **200** (rows or `[]`), never 4xx. (This is the BLOCKING gate — frontend verification proceeds only after this passes.)
    - Anon `GET /rest/v1/signals_feed?select=*&limit=1` → response object keys are exactly the 5 whitelisted columns; `select=body` → error.
    - Anon `GET /rest/v1/source_posts?select=body&limit=1` → no sensitive `body` data returned (200 `[]` or permission error).
    - Expectation: tier-1 RSS posts exist, so the feed should return rows; a `200 []` is acceptable ONLY if the live DB genuinely has no tier-1 `source_url`-bearing rows this window — note + investigate if empty.
  </acceptance_criteria>
  <verify>
    <human-check>Orchestrator confirms (from main tree, anon key from config/.env): MCP apply_migration succeeded; anon GET on signals_feed returns 200; select=* shows only the 5 keys; select=body errors; base source_posts body unreadable by anon. HTTP codes recorded in the SUMMARY.</human-check>
  </verify>
  <done>
    `044_signals_anon_view.sql` is live; the anon key can read tier-1 source links through `public.signals_feed` (HTTP 200), bounded to exactly the 5 whitelisted columns / tier-1 rows, with the base table still anon-blocked. This BLOCKING gate has passed — frontend verification may proceed.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Pre-deploy gate + scoped web rebuild (main tree, worktree-unsafe)</name>
  <read_first>
    - .planning/STATE.md (deploy discipline: prod↔main drift check → branch → /diff per work group → scoped rebuild → operator approval).
    - docker/web/site/app.js, docker/web/site/style-shared.css, docker/web/site/index.html (the three changed files to /diff before rebuild).
    - CLAUDE.md (Build & Run: `cd /root/bitcoin_bot/docker && docker compose up -d --build web`); project memory "Web compose service name" — the SERVICE key is `web` (NOT `agentpulse-web`, the container_name).
  </read_first>
  <action>
    From the main tree only (`/root/bitcoin_bot/docker`): run the standard pre-deploy gate — prod↔main drift check, then a `/diff` of the three changed web files (`app.js`, `style-shared.css`, `index.html`) for operator review and approval. After approval, run the SCOPED rebuild `docker compose up -d --build web` — SERVICE key `web`, NO `--delete`, no full `scripts/deploy.sh`. Confirm the `web` container comes up healthy. Recall entrypoint.sh `sed`-substitutes the `__SUPABASE_*__` placeholders into `/srv/app.js` at container start, so live verification must read the SUBSTITUTED file, not the source.
  </action>
  <acceptance_criteria>
    - Operator approved the `/diff` of the three web files (recorded).
    - `docker compose up -d --build web` (service key `web`, no `--delete`) completed; the `web` container is healthy.
    - No other service was rebuilt or deleted (scoped deploy).
  </acceptance_criteria>
  <verify>
    <human-check>Orchestrator confirms operator-approved /diff + a clean scoped `web` rebuild (service key `web`, no --delete) from /root/bitcoin_bot/docker; container healthy.</human-check>
  </verify>
  <done>
    The Phase-24 frontend is deployed live via a scoped, operator-approved `web` rebuild from the main tree, with no `--delete` and no out-of-scope service touched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-render verification of SIGNAL-01..04 + fail-loud + no regression</name>
  <read_first>
    - .planning/REQUIREMENTS.md (SIGNAL-01..04, lines ~55-58) — the acceptance criteria to confirm on the live site.
    - .planning/phases/24-signals-section/24-CONTEXT.md (D-03 view-all, D-06 safe link, D-07 fail-loud) — the behaviors to verify.
    - Project memory "Web static-preview substitution" / "economy_map preview flag URL" — verify on the substituted live `/srv/app.js`, not the raw source.
  </read_first>
  <action>
    Operator verifies on the live site (`aiagentspulse.com`, the substituted build):
    1. SIGNAL-01: scroll to / deep-link `#signals` — the section shows tier-1 source rows newest-first, capped (~12-15 visible); a "View all signals (N)" control reveals the rest inline (up to ~50) with NO route change.
    2. SIGNAL-02: each row is an external `<a>` showing date · headline · source domain; clicking opens off-site in a new tab; confirm `target="_blank"` + `rel="noopener noreferrer"` and the `↗` hover affordance (right-click → inspect or hover).
    3. SIGNAL-03: `#signals` is reachable via the scroll-spy nav (highlights when scrolled into view) and via the `#signals` deep-link, consistent with the other landing sections.
    4. SIGNAL-04 / D-07 fail-loud: confirm the loud-vs-benign distinction — with the migration applied the feed renders; the loud branch ("Signals feed is temporarily unavailable" + a `console.error`) is the path that fires on a 4xx (e.g. the pre-apply state, or a deliberately-misnamed view in DevTools) — confirm it is NOT a silent empty render. A genuinely thin tier-1 week shows "No tier-1 signals this week."
    5. No regression: the newsletter list (Phase 23 indexed rows), the Agent-Economy 3-col grid (Phase 22), the About section, the mode toggle, subscribe form, scroll-spy, and deep links all still work; the maturity pill does not overlap the nav.
  </action>
  <acceptance_criteria>
    - SIGNAL-01: capped tier-1 feed newest-first + working inline "View all" (no route) — confirmed live.
    - SIGNAL-02: rows are external `<a>` with date · headline · domain, `target="_blank"` + `rel="noopener noreferrer"` + `↗` — confirmed live.
    - SIGNAL-03: `#signals` reachable via scroll-spy + deep-link — confirmed live.
    - SIGNAL-04: feed renders with the migration applied; fail-loud diagnostic + console.error fires on a broken path (not a silent empty feed); benign empty copy on a genuinely thin week — confirmed.
    - No Phase 21/22/23 regression observed.
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms all five live checks (SIGNAL-01/02/03/04 + no regression) on the substituted live build; issues described or "approved".</human-check>
  </verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
  <done>
    SIGNAL-01..04 are proven live: a capped, newest-first tier-1 Signals feed of safe external links with an inline view-all, reachable via scroll-spy/deep-link, fail-loud on a broken read path, with no regression to prior phases. Mark SIGNAL-01..04 satisfied in REQUIREMENTS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (public internet) → live `public.signals_feed` | The over-exposure ceiling is now verified against the LIVE view, not just the source SQL. |
| repo (main tree) → prod (live Supabase + web container) | The deploy boundary — migration apply + scoped rebuild; blast radius must stay scoped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09 | Information Disclosure | live view over-exposure | mitigate | Live assert: anon `select=*` returns ONLY the 5 keys; anon `select=body` on the view errors (column absent). Verified before sign-off. |
| T-24-10 | Elevation / Information Disclosure | live base-table exposure | mitigate | Live assert: anon `GET source_posts?select=body` returns no rows (RLS still blocks; migration did not touch the base table). |
| T-24-11 | Denial of Service / Repudiation | silent empty on missing migration | mitigate | The BLOCKING gate requires anon `signals_feed` GET → 200 before deploy; a 4xx (missing view/grant) is exactly what the frontend's loud branch surfaces — fail-loud confirmed, never a silent empty feed. |
| T-24-12 | Tampering | unscoped deploy blast radius | mitigate | Scoped `docker compose up -d --build web` only (service key `web`), NO `--delete`, no full deploy script; prod↔main drift check + `/diff` + operator approval first. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase. |
</threat_model>

<verification>
- [BLOCKING] migration 044 applied live via MCP `apply_migration`; anon `signals_feed` GET → 200; column/row ceiling and base-table block proven live (T-24-09/10).
- Scoped, operator-approved `web` rebuild from the main tree, no `--delete` (T-24-12).
- All four SIGNAL criteria + fail-loud + no-regression confirmed on the substituted live build.
- No HIGH threat left unmitigated; the fail-loud-on-missing-migration behavior is positively demonstrated (T-24-11).
</verification>

<success_criteria>
- The live `#signals` section shows the capped, newest-first tier-1 feed of safe external links with an inline view-all, reachable via scroll-spy + `#signals` deep-link.
- The anon read path is live AND bounded to the 5 whitelisted columns / tier-1 rows; the base `source_posts` table stays anon-blocked.
- A broken read path fails loud (diagnostic + console.error), never a silent empty feed; a genuinely thin week shows a benign empty state.
- SIGNAL-01, SIGNAL-02, SIGNAL-03, SIGNAL-04 marked satisfied in REQUIREMENTS after operator sign-off.
</success_criteria>

<output>
Create `.planning/phases/24-signals-section/24-03-SUMMARY.md` when done.
</output>
