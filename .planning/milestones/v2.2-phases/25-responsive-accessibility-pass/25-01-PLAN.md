---
phase: 25-responsive-accessibility-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/web/site/style-shared.css
  - docker/web/site/style-base.css
autonomous: true
requirements: [RESP-01, A11Y-01]
must_haves:
  truths:
    - "The subscribe email input shows a violet :focus-visible outline on keyboard focus (no bare outline:none on :focus) — D-02"
    - "Below 600px, archive rows and signal rows place the date as a mono kicker line ABOVE the headline, via ONE shared .row rule, with the left affordance column retained — D-07/D-08/D-09"
    - "The nav condense breakpoint is 600px (aligned with the row-stack/grid-collapse breakpoint), not 640px — D-05"
    - "Under prefers-reduced-motion, a canonical global reset suppresses all animation/transition/smooth-scroll, AND out-specifies the !important theme transition so theme toggles do not fade — D-10/D-11"
  artifacts:
    - path: "docker/web/site/style-shared.css"
      provides: "#subscribe-email:focus-visible violet outline (D-02); rewritten .row <=600px date-above-headline reflow (D-07/D-08/D-09)"
      contains: "#subscribe-email:focus-visible"
    - path: "docker/web/site/style-base.css"
      provides: "nav condense @media moved 640->600 (D-05); canonical prefers-reduced-motion global reset + D-11 out-specifying override (D-10/D-11)"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "docker/web/site/style-shared.css .row @media(max-width:600px)"
      to: "renderList rows AND renderSignals rows"
      via: "single shared .row rule (D-09 — no per-render JS change)"
      pattern: "span:not\\(.num\\):not\\(.date\\)"
    - from: "docker/web/site/style-base.css prefers-reduced-motion reset"
      to: "style-shared.css:32 .mode-transitioning * !important theme transition"
      via: "out-specifying .mode-transitioning.mode-transitioning * override (0,2,0 > 0,1,0)"
      pattern: "mode-transitioning\\.mode-transitioning"
---

<objective>
Land the four named CSS conformance fixes for the assembled single-scroll landing, strictly within the brief's RESP-01/A11Y-01 scope ceiling (D-01 — NO broad WCAG audit, NO new ARIA/landmarks/skip-link/contrast/alt-text). All edits are CSS-only in `style-shared.css` and `style-base.css`; `app.js` is NOT touched (D-09).

Four locked changes:
- D-02: give `#subscribe-email` a visible keyboard focus indicator (`:focus-visible` violet outline) matching the `.row`/`.view-all`/`.card` pattern.
- D-07/D-08/D-09: rewrite the shared `.row` <=600px reflow so the date is a mono kicker line ABOVE the headline (affordance column kept), once, for both archive and signal rows.
- D-05: move the nav condense breakpoint 640px -> 600px so nav condenses at the same width rows stack and grids collapse (D-04 two-tier system: mobile 600 / tablet 880).
- D-10/D-11: replace the scroll-only reduced-motion gate with the canonical global reset, AND make it win over the existing `!important` theme transition (the one cascade pitfall).

Purpose: Close the named responsive/a11y gaps so the holistic live-render verification (Plan 25-02) passes.
Output: Updated `style-shared.css` + `style-base.css` (source-only; deploy + live sign-off is Plan 25-02, orchestrator-owned).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-responsive-accessibility-pass/25-CONTEXT.md
@.planning/docs/REDESIGN_CC_BRIEF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-02 — visible keyboard focus on the subscribe email input</name>
  <files>docker/web/site/style-shared.css</files>
  <read_first>
    - docker/web/site/style-shared.css:905-925 — the current `#subscribe-email` block and the `#subscribe-email:focus { outline: none; border-color: var(--accent); }` regression being fixed.
    - docker/web/site/style-shared.css:256-261 — the canonical `.row:focus-visible` violet outline pattern (`outline: 2px solid var(--accent); outline-offset: 3px; border-radius: var(--radius-dot);`) to copy.
    - docker/web/site/style-shared.css:296-300 — the `.view-all:focus-visible` analog (same pattern), confirming the project-wide focus treatment.
  </read_first>
  <action>
    Fix the named A11Y-01 regression (D-02). The current `#subscribe-email:focus` rule swaps the outline for a border-color change (`outline: none; border-color: var(--accent);`) on the broad `:focus` pseudo-class, leaving keyboard users with NO visible focus ring. Replace that bare-`:focus` outline suppression with the standard `:focus-visible` violet-outline pattern used everywhere else.

    Convert the selector `#subscribe-email:focus` to `#subscribe-email:focus-visible` and replace its body with the canonical focus treatment: `outline: 2px solid var(--accent);` plus `outline-offset: 3px;` (use `var(--radius-sm)` for `border-radius` to match this input's existing corner radius rather than the row's `--radius-dot`). Keep the `border-color: var(--accent);` line if desired as a complementary cue, but the visible outline is the load-bearing change — do NOT leave any `outline: none` on a bare `:focus` for `#subscribe-email`. Token-only: zero new hex (RHYTHM-01). No `app.js` change.
  </action>
  <acceptance_criteria>
    - Source: `style-shared.css` contains a `#subscribe-email:focus-visible` rule whose body includes `outline: 2px solid var(--accent)` and `outline-offset: 3px`.
    - Source: there is NO `#subscribe-email:focus { ... outline: none ... }` left in the file (the bare-`:focus` outline suppression is gone).
    - Source: zero new hex literals introduced; the outline color is `var(--accent)`.
  </acceptance_criteria>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -A3 '#subscribe-email:focus-visible' docker/web/site/style-shared.css | grep -q 'outline: 2px solid var(--accent)' && ! grep -Pzo '#subscribe-email:focus\s*\{[^}]*outline:\s*none' docker/web/site/style-shared.css && echo PASS</automated>
  </verify>
  <done>The subscribe email input exposes a violet `:focus-visible` outline matching the `.row`/`.view-all`/`.card` pattern; no `outline:none` remains on its focus state.</done>
</task>

<task type="auto">
  <name>Task 2: D-07/D-08/D-09 — date-above-headline row reflow (shared .row rule, both row types)</name>
  <files>docker/web/site/style-shared.css</files>
  <read_first>
    - docker/web/site/style-shared.css:204-267 — the `.row` family: desktop grid `grid-template-columns: 56px 1fr auto` (:206), `.row .date` base rule (mono, :249-254), and the CURRENT <=600px reflow at :264-267 (`.row { grid-template-columns: 40px 1fr; }` + `.row .date { grid-column: 2; ... margin-top: var(--space-xs); }` — which drops the date BELOW the title, the behavior D-07 replaces).
    - docker/web/site/app.js:490-503 — `renderList` archive-row DOM: direct grid children are `<span class="num">`, an UNNAMED `<span>` wrapper (holds `.title` + optional `.sum`), and `<span class="date">`. (READ ONLY — app.js is not modified, D-09.)
    - docker/web/site/app.js:629-639 — `renderSignals` signal-row DOM: identical shape — `<span class="num">↗</span>`, an UNNAMED `<span>` wrapper (holds `.title` + `.host`), and `<span class="date">`. Confirms BOTH row types share the same three-child structure, so one shared rule covers both (D-09).
  </read_first>
  <action>
    Rewrite the existing `@media (max-width: 600px)` `.row` block (style-shared.css:264-267) so the date becomes a mono kicker line ABOVE the headline, landing ONCE on the shared `.row` rule that both archive (`renderList`) and signal (`renderSignals`) rows inherit (D-09 — single source of truth, NO per-render JS change, app.js untouched).

    The grid has exactly three direct children: `.num` (affordance), the unnamed content-wrapper `<span>` (title + sum/host), and `.date`. Replace the two current inner declarations with these (use literal `600px` in the `@media` condition — D-06, never `var(--bp)`):
    - `.row { grid-template-columns: 40px 1fr; align-items: start; }` — keep the affordance column (D-08); top-align for the stacked layout.
    - `.row .num { grid-column: 1; grid-row: 1 / span 2; }` — affordance stays in column 1 (D-08), spanning the kicker + content rows.
    - `.row .date { grid-column: 2; grid-row: 1; font-size: 11px; margin: 0 0 var(--space-xs); }` — the date is now the kicker on row 1 of the content column (it already inherits `font-family: var(--mono)` from `.row .date` at :250). REMOVE the old `margin-top` (it placed the date below the title).
    - `.row > span:not(.num):not(.date) { grid-column: 2; grid-row: 2; }` — pin the unnamed content wrapper (title + sum/host) to row 2 of the content column, BELOW the date kicker.

    Token-only; no new hex (RHYTHM-01). This is the only place the mobile date order changes (D-09).
  </action>
  <acceptance_criteria>
    - Source: the `@media (max-width: 600px)` block targeting `.row` assigns `.row .date` to `grid-row: 1` (kicker) and the unnamed wrapper `.row > span:not(.num):not(.date)` to `grid-row: 2` (content below).
    - Source: `.row .num` retains `grid-column: 1` at <=600px (affordance column NOT dropped — D-08).
    - Source: the old `.row .date { grid-column: 2; ... margin-top: ... }` below-title placement is gone (no `margin-top` on `.row .date` in the 600px block).
    - Source: the change lives on the shared `.row` rule only; `app.js` is unchanged (no diff to docker/web/site/app.js — D-09).
    - Source: the `@media` condition uses the literal `600px`, not a CSS custom property (D-06).
  </acceptance_criteria>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -q 'span:not(.num):not(.date)' docker/web/site/style-shared.css && grep -A5 'max-width: 600px' docker/web/site/style-shared.css | grep -q 'grid-row: 1' && git diff --quiet -- docker/web/site/app.js && echo PASS</automated>
  </verify>
  <done>Below 600px both archive and signal rows render the date as a mono kicker above the headline with the affordance column retained, driven by one shared `.row` rule; app.js untouched.</done>
</task>

<task type="auto">
  <name>Task 3: D-05 + D-10/D-11 — nav breakpoint to 600px and canonical reduced-motion reset that wins the cascade</name>
  <files>docker/web/site/style-base.css</files>
  <read_first>
    - docker/web/site/style-base.css:251-264 — the responsive nav rule: comment says "<=640px wrap tabs to scrollable row"; `@media (max-width:640px)` sets `.nav { flex-wrap: wrap; }` + `.tabs { order:3; flex-basis:100%; overflow-x:auto; }`. Only the breakpoint moves (D-05); the wrap-to-scrollable behavior is KEPT (D-12 — no hamburger, no relabel).
    - docker/web/site/style-base.css:287-312 — the Phase 21 scroll/motion block: `html { scroll-behavior: smooth; }` (:296) and the CURRENT scroll-only reduced-motion gate `@media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto; } }` (:308-312) — the block D-10 replaces.
    - docker/web/site/style-shared.css:26-33 — the theme transition `.mode-transitioning, .mode-transitioning *, .mode-transitioning *::before, .mode-transitioning *::after { transition: ... !important; }` (:32). Specificity `(0,1,0)` with `!important`. THIS is the rule the reset must beat (D-11).
    - docker/web/site/index.html:10-11 — CSS load order: `style-base.css` loads BEFORE `style-shared.css`. CONSEQUENCE: the reset (in style-base.css) is EARLIER in source order, so a bare `*` reset (specificity 0,0,0) loses to `.mode-transitioning *` (0,1,0) on BOTH specificity and source order — it MUST out-specify (D-11).
  </read_first>
  <action>
    Two edits in style-base.css.

    (A) D-05 nav breakpoint move. Change the responsive nav `@media (max-width:640px)` (style-base.css:254) to `@media (max-width:600px)`. Keep the rule body byte-identical (`.nav { flex-wrap: wrap; }` + the `.tabs` wrap-to-scrollable declarations) — D-12: no hamburger, no relabel, only the breakpoint moves to align with the 600px row-stack/grid-collapse line (D-04 two-tier: mobile 600 / tablet 880). Also update the rule's leading comment ("<=640px wrap tabs") to "<=600px" so no stale `640` remains in this block. Use the literal `600px` (D-06).

    (B) D-10 reduced-motion reset + D-11 cascade win. Replace the scroll-only gate at style-base.css:308-312 with the canonical global reset. Inside `@media (prefers-reduced-motion: reduce)`, add a `*, *::before, *::after` rule declaring `animation-duration: 0.01ms !important;`, `animation-iteration-count: 1 !important;`, `transition-duration: 0.01ms !important;`, and `scroll-behavior: auto !important;`. This single rule suppresses smooth-scroll (overrides the `html { scroll-behavior: smooth }` at :296), hover transitions, and any animation.

    Then, IN THE SAME `@media` block, add the D-11 out-specifying override (this is the one cascade pitfall, and it is an explicit acceptance criterion, not optional). The theme transition `.mode-transitioning *` (style-shared.css:32) is `(0,1,0) !important` AND loads later than style-base.css — so the bare `*` reset cannot suppress it. Add a selector list `.mode-transitioning.mode-transitioning, .mode-transitioning.mode-transitioning *, .mode-transitioning.mode-transitioning *::before, .mode-transitioning.mode-transitioning *::after { transition-duration: 0.01ms !important; }`. The doubled class gives specificity `(0,2,0)`, which beats `.mode-transitioning *` `(0,1,0)` regardless of source order. Include a comment naming the target rule (`out-specifies style-shared.css:32 .mode-transitioning * !important theme transition`) so it is not "simplified" away later.

    Token-only otherwise; the only literals are the canonical reset's `0.01ms` / `1` / `auto` motion values (RHYTHM-01 governs color, not motion-timing). No `app.js` change.
  </action>
  <acceptance_criteria>
    - Source: the responsive nav `@media` condition is `max-width:600px` (was 640); the `.nav { flex-wrap: wrap; }` + `.tabs` wrap-to-scrollable declarations are unchanged (D-12); no `640` remains in the nav block (D-05).
    - Source: a `@media (prefers-reduced-motion: reduce)` block contains a `*, *::before, *::after` rule with `transition-duration: 0.01ms !important`, `animation-duration: 0.01ms !important`, `animation-iteration-count: 1 !important`, and `scroll-behavior: auto !important` (D-10).
    - Source (D-11 cascade win — explicit): the same `@media` block ALSO contains a `.mode-transitioning.mode-transitioning` (doubled-class) selector list setting `transition-duration: 0.01ms !important`, i.e. specificity `(0,2,0)` that out-specifies the `(0,1,0)` `.mode-transitioning *` rule at style-shared.css:32. (Live proof that the theme fade is actually suppressed is Plan 25-02.)
    - Source: the old scroll-only `@media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto; } }` gate is replaced (not duplicated).
    - Source: `@media` conditions use literal px, no `var(--…)` (D-06).
  </acceptance_criteria>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -q 'max-width:600px' docker/web/site/style-base.css && ! grep -q '640' docker/web/site/style-base.css && grep -A8 'prefers-reduced-motion' docker/web/site/style-base.css | grep -q 'transition-duration: 0.01ms !important' && grep -q 'mode-transitioning.mode-transitioning' docker/web/site/style-base.css && echo PASS</automated>
  </verify>
  <done>Nav condenses at 600px (wrap-to-scrollable behavior unchanged); a canonical reduced-motion global reset is in place AND a doubled-class override out-specifies the `!important` theme transition so reduced-motion can suppress the theme fade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only authored rules; no new user input, no new data flow, no auth/network change. `app.js`, all queries, and all data sinks are untouched (D-09). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering / Information Disclosure | `style-shared.css`, `style-base.css` static rules | accept | No untrusted data is interpolated into CSS — every value is an author-written token, px, or motion-timing literal. No dynamic CSS, no `attr()`/`url()` of user data. |
| T-25-02 | Denial of Service (a11y) / Repudiation | reduced-motion reset + `:focus-visible` change | mitigate | Changes only motion TIMING and ADD a focus ring; they do not hide any security-relevant indicator. D-02 adds (not removes) a visible focus cue; the reduced-motion reset touches `*-duration`/`scroll-behavior` only — it does not affect the external-link `↗` affordance or the `rel="noopener noreferrer"` hardening (Phase 24). Verified in 25-02. |
| T-25-SC | Tampering (supply chain) | npm/pip/cargo installs | accept (N/A) | No package-manager installs in this phase — CSS-only edits, no dependency change, no `requirements.txt`/`package.json` delta. |

No HIGH-severity threats. The pseudo-link / navigation-bypass surface (Spoofing) is addressed in Plan 25-02 (the real-`<a>` audit); no markup changes here can introduce it (CSS-only).
</threat_model>

<verification>
- Task gates 1-3 pass (grep assertions above).
- `git diff --stat` shows ONLY `docker/web/site/style-shared.css` and `docker/web/site/style-base.css` changed — `app.js` has zero diff (D-09).
- No new hex literals in either file (RHYTHM-01); `@media` conditions use literal px (D-06).
- Scope ceiling honored (D-01): no new ARIA/landmarks/skip-link/contrast/alt-text rules added.
</verification>

<success_criteria>
- `#subscribe-email` has a violet `:focus-visible` outline; no `outline:none` on its focus (D-02).
- Below 600px both row types render date-above-headline via one shared `.row` rule, affordance kept (D-07/D-08/D-09).
- Nav condense breakpoint is 600px, wrap-to-scrollable behavior unchanged (D-05/D-12).
- Canonical reduced-motion reset present AND out-specifies the `!important` theme transition (D-10/D-11).
- Only the two CSS files changed; app.js untouched.
</success_criteria>

<output>
Create `.planning/phases/25-responsive-accessibility-pass/25-01-SUMMARY.md` when done.
</output>
