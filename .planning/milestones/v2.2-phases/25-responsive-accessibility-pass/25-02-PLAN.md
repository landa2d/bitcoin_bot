---
phase: 25-responsive-accessibility-pass
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified: []
autonomous: false
requirements: [RESP-01, A11Y-01]
must_haves:
  truths:
    - "At ~375px: map grid is 1-col, About agent-grid (.made-cols) is 1-col, nav is condensed to a scrollable tab row, and archive+signal rows show date-above-headline — D-03/D-04/D-05/D-07"
    - "At ~768px: the map card grid is 2-col (tablet tier) — D-04"
    - "At ~1280px: the map card grid is 3-col and rows render inline (56px 1fr auto) — D-03/D-04"
    - "Tabbing through nav, list rows, links, and the subscribe email input shows a visible violet :focus-visible outline; the scroll-spy nav is keyboard-operable — A11Y-01/D-02"
    - "With OS reduce-motion ON: toggling theme produces NO visible color fade (D-11 cascade win), hover lifts are suppressed, and the scroll-spy nav jumps rather than smooth-scrolls — D-10/D-11"
    - "Every link across the landing + touched detail routes is a real <a> (no click-handler <div> pseudo-links); view-all is correctly a <button> — A11Y-01"
  artifacts:
    - path: ".planning/phases/25-responsive-accessibility-pass/25-02-SUMMARY.md"
      provides: "Holistic live-render verification record + real-<a> audit result + operator sign-off"
  key_links:
    - from: "live container render (/srv substituted app.js)"
      to: "the Plan 25-01 CSS edits"
      via: "scoped docker compose up -d --build web from /root/bitcoin_bot/docker (orchestrator-owned, worktree-unsafe)"
      pattern: "docker compose .* web"
---

<objective>
Verify RESP-01 and A11Y-01 holistically on the LIVE render of the assembled single-scroll landing (+ the detail routes the milestone touched), after the Plan 25-01 CSS fixes ship. This is the phase's acceptance gate: per D-03 it VERIFIES the largely-already-built behavior and confirms the named gaps closed — it does not redo conforming surfaces, and it stays inside the D-01 scope ceiling (NO broad WCAG audit).

Three steps:
1. Source-level conformance audit (verify-only): grids reflow 3->2->1 (D-03/D-04), nav wrap-to-scrollable kept (D-12), and the real-`<a>` audit (Claude's Discretion) — every link a real `<a>`, no pseudo-link `<div>`, `view-all` correctly a `<button>`.
2. Operator-gated scoped deploy (orchestrator-owned, worktree-unsafe).
3. Live-render sign-off at the canonical viewports (~375/~768/~1280px) + keyboard focus + OS reduced-motion toggle (the D-11 cascade win proven live).

Purpose: Prove the whole redesigned surface holds up responsively and for keyboard/motion-sensitive users.
Output: `25-02-SUMMARY.md` recording the audit, deploy, and operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-responsive-accessibility-pass/25-CONTEXT.md
@.planning/docs/REDESIGN_CC_BRIEF.md
@.planning/phases/25-responsive-accessibility-pass/25-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source conformance audit — grids reflow, nav behavior, real-&lt;a&gt; links (verify-only)</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - docker/web/site/style-shared.css:391-396 — `.grid` desktop `repeat(3, 1fr)` (map + tier card grids), and :470-471 — `@media (max-width:880px){.grid{repeat(2,1fr)}}` + `@media (max-width:600px){.grid{1fr}}` (the 3->2->1 collapse, D-04 — VERIFY, do not change).
    - docker/web/site/style-shared.css:1123 + 1132 — `.made-cols` (About pipeline/supporting grid) `1fr 1fr` collapsing to `1fr` at `@media (max-width:880px)` (D-04 about-grid — VERIFY).
    - docker/web/site/style-base.css (post 25-01) :254 region — the nav condense `@media (max-width:600px)` wrap-to-scrollable rule (D-05/D-12 — VERIFY behavior kept).
    - docker/web/site/app.js:495 (`renderList` row `<a href="#/edition/...">`), :631 (`renderSignals` `<a ... class="row signal-row" target="_blank" rel="noopener noreferrer">`), :999 (block card `<a href="#/map/...">`), :642 (`view-all` is a `<button type="button">`). app.js contains NUL-byte sentinels — MUST grep with `grep -a` (plain grep treats it as binary; the Read tool renders NUL as a space). READ ONLY — no app.js change expected (D-01/D-09).
  </read_first>
  <action>
    Run the verify-only conformance audit; make NO code edits (if a genuine regression is found, record it for a `--gaps` follow-up rather than editing here — this plan's only artifact is the SUMMARY).

    (1) Grid reflow (D-03/D-04): confirm `.grid` is `repeat(3,1fr)` desktop and steps to 2-col `@880` and 1-col `@600`, and `.made-cols` collapses to 1-col `@880`. These are "already correct" per CONTEXT — verify present, do not redefine.

    (2) Nav condense (D-05/D-12): confirm the responsive nav rule is now `@media (max-width:600px)` and still does wrap-to-scrollable (`.nav{flex-wrap:wrap}` + `.tabs{order:3;flex-basis:100%;overflow-x:auto}`) — no hamburger, no relabel.

    (3) Real-`<a>` audit (Claude's Discretion / A11Y-01): using `grep -a` on app.js, confirm every navigational element is a real `<a>` — archive rows (`#/edition/...`), signal rows (external, `target="_blank" rel="noopener noreferrer"`), block cards (`#/map/...`), and the nav tabs. Confirm `view-all` is a `<button>` (an action, not navigation — correct as-is). Confirm NO click-handler `<div>`/`<span>`-as-link slipped in: scan `grep -a` for `addEventListener('click'` / `onclick=` attached to non-`<a>`/non-`<button>` navigational elements; the only legitimate click handler is the `view-all` button (`signals-view-all`, app.js:648) and the theme toggle. No JS change is expected (D-09).

    Record each check (pass/finding) in the SUMMARY.
  </action>
  <acceptance_criteria>
    - Source: `.grid` is `repeat(3, 1fr)` with `@media (max-width:880px)` -> `repeat(2,1fr)` and `@media (max-width:600px)` -> `1fr` present (D-04).
    - Source: `.made-cols` collapses to `1fr` at `@media (max-width:880px)` (D-04).
    - Source: the nav condense rule is `@media (max-width:600px)` and retains `flex-wrap:wrap` + `.tabs{...overflow-x:auto}` (D-05/D-12).
    - Source (real-`<a>` audit): `renderList`/`renderSignals`/block-card outputs are `<a>` elements; `view-all` is a `<button>`; no navigational click-handler `<div>` exists (only `signals-view-all` button + theme toggle carry click handlers). Audit run with `grep -a` (NUL-safe).
    - No file under docker/web/site is modified by this task (verify-only).
  </acceptance_criteria>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -q 'grid-template-columns: repeat(3, 1fr)' docker/web/site/style-shared.css && grep -q 'max-width: 600px) { .grid { grid-template-columns: 1fr' docker/web/site/style-shared.css && grep -q 'max-width: 880px) { .made-cols' docker/web/site/style-shared.css && grep -q 'max-width:600px' docker/web/site/style-base.css && grep -ac 'class="row' docker/web/site/app.js | grep -qv '^0$' && echo PASS</automated>
  </verify>
  <done>Source audit confirms grids reflow 3->2->1, nav condenses (wrap-to-scrollable) at 600px, and every link is a real `<a>` (view-all is a `<button>`, no pseudo-link divs); findings recorded in SUMMARY.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Operator-gated scoped web deploy (orchestrator-owned, worktree-unsafe)</name>
  <decision>Approve the scoped `web` rebuild that deploys the Plan 25-01 CSS fixes to the live container.</decision>
  <context>
    Deploy is ORCHESTRATOR-OWNED and worktree-unsafe — it must run from the absolute main tree, NOT a worktree executor (a worktree would build stale code). The exact, approved command is `cd /root/bitcoin_bot/docker && docker compose up -d --build web` — the service key is `web` (NOT the container_name `agentpulse-web`, which fails "no such service"). NO `rsync --delete`, no full `scripts/deploy.sh`, no `--remove-orphans`.

    Before approving, the operator/orchestrator performs the standing gate: prod<->main drift check, create a branch, and review `/diff` per work group (here: the two CSS files from 25-01). The container substitutes `__SUPABASE_*__` placeholders at start via `entrypoint.sh`, so verification (Task 3) must hit the running container, not a raw file open.
  </context>
  <options>
    <option id="approve-deploy">
      <name>Approve scoped web rebuild</name>
      <pros>Ships the 25-01 CSS fixes so RESP-01/A11Y-01 can be verified on the live render (the phase gate).</pros>
      <cons>Touches the live site; mitigated by scoped single-service build, no --delete, branch + /diff review first.</cons>
    </option>
    <option id="hold">
      <name>Hold — diff/drift concern</name>
      <pros>Pause if the prod<->main drift check or `/diff` surfaces anything unexpected.</pros>
      <cons>Phase cannot complete until deployed and verified live.</cons>
    </option>
  </options>
  <resume-signal>Type "approved" (orchestrator then runs `cd /root/bitcoin_bot/docker && docker compose up -d --build web` from the main tree) or describe the diff/drift concern.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-render sign-off — responsive reflow, focus visibility, reduced-motion (the acceptance gate)</name>
  <what-built>
    The Plan 25-01 CSS fixes (D-02 subscribe focus, D-05 nav breakpoint, D-07/D-08/D-09 row reflow, D-10/D-11 reduced-motion reset) are now live on the substituted `web` container. This is a `ui_phase` — the rendered result at the canonical viewports is the gate, not container-up.
  </what-built>
  <read_first>
    - .planning/phases/25-responsive-accessibility-pass/25-CONTEXT.md — the D-04/D-07/D-10/D-11 expected behaviors.
    - .planning/ROADMAP.md Phase 25 Success Criteria (1-4) — the holistic checklist.
  </read_first>
  <how-to-verify>
    On the LIVE site (running container; reduced-motion checks via OS setting), confirm:

    1. Responsive reflow at canonical viewports (RESP-01):
       - ~1280px (desktop): map card grid is 3-col; archive + signal rows render inline (number/↗ · title · date on one line).
       - ~768px (tablet): map card grid is 2-col.
       - ~375px (mobile): map card grid is 1-col; the About pipeline/supporting grid is 1-col; the nav has condensed to a single full-width horizontally-scrollable tab row (brand + subscribe stay on the top row, NO hamburger); archive AND signal rows now show the DATE as a mono kicker line ABOVE the headline, with the left affordance column (index number / ↗) still present.
       - Confirm the nav condenses at the SAME width the rows stack/grids collapse (~600px) — no in-between band where nav condensed but rows did not (D-05).

    2. Keyboard focus visibility (A11Y-01 / D-02):
       - Tab through nav tabs, archive rows, signal rows, links, and the SUBSCRIBE EMAIL input — each shows a visible violet outline. The subscribe email input specifically must now show the outline (the named regression D-02).
       - The scroll-spy nav is keyboard-operable (tab to a nav anchor, Enter navigates to the section).

    3. Reduced-motion (A11Y-01 / D-10/D-11) — enable the OS "reduce motion" setting, then:
       - Toggle the theme: there is NO visible color-fade transition (the D-11 cascade win — the reset out-specifies the `!important` theme transition). This is the load-bearing check; spot-verify it actually suppresses the fade, not merely that the rule exists.
       - Hover over rows/cards: no transition/lift animation.
       - Click a scroll-spy nav anchor: the page JUMPS to the section (no smooth-scroll).
  </how-to-verify>
  <resume-signal>Type "approved" if all three groups pass, or describe which viewport/focus/motion check failed (failures route to a `--gaps` follow-up).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered DOM link -> navigation | Navigational elements must be real `<a>` so href semantics (and the Phase 24 `safeHttpUrl` external-link gating + `rel="noopener noreferrer"`) govern navigation. A click-handler `<div>` would bypass that. Verify-only here; no markup change. |
| repo/worktree -> live container | The scoped `web` rebuild deploys source to prod; worktree-unsafe (stale-code risk) and live-site blast radius. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Spoofing / Tampering | navigational links (rows, cards, nav) | mitigate | Real-`<a>` audit (Task 1, `grep -a`) confirms no click-handler `<div>` pseudo-link bypasses real navigation or the Phase 24 `safeHttpUrl`/`rel=noopener` external-link hardening. Only legitimate click handlers are `view-all` (`<button>`) and the theme toggle. No JS change (D-09). |
| T-25-04 | Tampering / Denial of Service | scoped `web` deploy | mitigate | Single-service scoped build (`web` key), NO `--delete`/`--remove-orphans`, from the main tree only (worktree-unsafe), gated by prod<->main drift check + branch + `/diff` + operator approval (Task 2). Container substitutes `__SUPABASE_*__` at start (raw file open would be a dead SPA — verify on the running container). |
| T-25-05 | Denial of Service (a11y) | reduced-motion / focus changes | accept (verify) | The reduced-motion reset changes motion timing only; it does not hide the external-link `↗` affordance or focus rings. Confirmed in Task 3 (focus rings visible; `↗`/`rel=noopener` intact). |
| T-25-SC | Tampering (supply chain) | npm/pip/cargo installs | accept (N/A) | No package installs — verification + scoped rebuild of existing source only; no dependency delta. |

No HIGH-severity threats; surface is a CSS conformance pass with a gated single-service redeploy.
</threat_model>

<verification>
- Task 1 source audit gate passes (grids/nav/real-`<a>`).
- Task 2 deploy runs only after operator approval, from `/root/bitcoin_bot/docker`, service key `web`, no `--delete`.
- Task 3 operator sign-off confirms all four ROADMAP Success Criteria on the live render (reflow, focus, reduced-motion incl. the D-11 theme-fade suppression, real `<a>`).
- Scope ceiling honored (D-01): no broad-WCAG work performed.
</verification>

<success_criteria>
- Live render at ~375/~768/~1280px shows correct 3->2->1 grid reflow, 600px nav condense, and date-above-headline rows with affordance retained (RESP-01).
- Visible violet `:focus-visible` outline on nav/rows/links/subscribe-email; scroll-spy keyboard-operable (A11Y-01/D-02).
- OS reduced-motion suppresses the theme fade (D-11 proven live), hover transitions, and scroll-spy smooth-scroll (A11Y-01/D-10/D-11).
- Every link is a real `<a>`; no pseudo-link divs; view-all is a `<button>` (A11Y-01).
- Operator sign-off recorded.
</success_criteria>

<output>
Create `.planning/phases/25-responsive-accessibility-pass/25-02-SUMMARY.md` when done.
</output>
