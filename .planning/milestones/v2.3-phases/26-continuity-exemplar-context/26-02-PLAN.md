---
phase: 26-continuity-exemplar-context
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - tests/test_26_continuity_loader.py
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03, CTX-05]

must_haves:
  truths:
    - "A pytest suite imports the REAL load_edition_context via the conftest-preloaded newsletter_poller module and passes (D-16)"
    - "Tests prove operator_written filtering excludes edition 29 / block_v1 rows from the exemplar pool (D-01)"
    - "Tests prove the >=40-word non-header/non-list paragraph filter (D-05)"
    - "Tests prove the whole-corpus-empty path returns {previous_editions:[], exemplars:[], empty:true} + a WARNING and does not raise (CTX-03)"
    - "Tests prove the empty-operator-pool 'not scored' state is distinct from corpus-empty and from a real score:0 (D-02/D-03)"
  artifacts:
    - path: "tests/test_26_continuity_loader.py"
      provides: "deterministic fixture coverage of load_edition_context (the D-16 case set)"
      contains: "load_edition_context"
      min_lines: 60
  key_links:
    - from: "tests/test_26_continuity_loader.py"
      to: "nl.load_edition_context"
      via: "conftest-preloaded newsletter_poller import"
      pattern: "import newsletter_poller"
---

<objective>
Build the deterministic fixture/unit test for `load_edition_context` (D-15/D-16). This proves the loader's degrade paths — which are hard to reproduce against the live 32-edition corpus — without a live DB, by passing a Supabase stub. The test imports the REAL production function (never reimplements it) so a regression in production fails the test.

Purpose: Locks the contract (return shape, operator-written filtering, >=40-word/non-header/non-list filtering, the CTX-03 empty-corpus path, and the empty-operator-pool "not scored" path) before the live trigger spends real generation budget. Executor-owned and worktree-safe — pure Python against fixtures, no live DB, no rebuild.
Output: `tests/test_26_continuity_loader.py` that exits 0 under pytest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-continuity-exemplar-context/26-CONTEXT.md
@.planning/phases/26-continuity-exemplar-context/26-PATTERNS.md

# Test analogs + the function under test
@tests/test_19_smartquote.py
@tests/conftest.py
@docker/newsletter/newsletter_poller.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the deterministic fixture test for load_edition_context</name>
  <files>tests/test_26_continuity_loader.py</files>
  <read_first>
    - tests/test_19_smartquote.py:1-60 (the module-load pattern: import the REAL function via the conftest-preloaded module with a sys.path fallback; the `pytest.mark.parametrize` + `caplog` idioms; the "do NOT reimplement the transform" docstring rule)
    - tests/conftest.py:1-108 (the `_preload_poller` mechanism already registers `newsletter_poller` in sys.modules with the correct `schemas` — no extra wiring needed)
    - docker/newsletter/newsletter_poller.py (the `load_edition_context` built in 26-01 — its exact signature, return keys, and the "not scored" marker chosen there; the test asserts against the ACTUAL contract that was implemented)
    - .planning/phases/26-continuity-exemplar-context/26-CONTEXT.md (D-15/D-16 required cases; D-01/D-02/D-03/D-05)
    - .planning/phases/26-continuity-exemplar-context/26-PATTERNS.md (the test section — the Supabase stub chain `.table().select().eq().order().limit().execute()` and the D-16 case list)
  </read_first>
  <action>
Create `tests/test_26_continuity_loader.py`. Import the REAL `nl.load_edition_context` via the conftest-preloaded `newsletter_poller` module, mirroring test_19_smartquote.py:32-39 (the `NL_DIR` sys.path fallback as belt-and-suspenders). Do NOT reimplement the loader in the test — a copy could pass while production regresses.

Define a minimal Supabase stub exposing the fluent chain `.table(...).select(...).eq(...).order(...).limit(...).execute()` where `.execute()` returns an object whose `.data` is a fixture list of edition dicts. The stub must let each test inject any `.data` (queue per-call responses if the loader issues more than one query) so the degrade paths are reproducible without a live DB — this is why the 26-01 loader takes `supabase` as an explicit parameter. Each fixture edition dict carries `edition_number, title, title_impact, content_markdown, content_markdown_impact, data_snapshot` (with `operator_written` and `lead_theme`), and `published_at`.

Cover all D-16 cases (use `pytest.mark.parametrize` and `caplog` where it fits):
  1. **Return shape + theme sourcing (D-07)** — keys `previous_editions`, `exemplars`, `empty`, and the "not scored" marker are present; each `previous_editions` entry carries `{edition_number, title, primary_theme, opening_excerpt}` and `weeks_ago` is present iff `published_at` is non-null (assert it is OMITTED for a null-`published_at` fixture row — D-09). The fixture edition MUST set `data_snapshot={'lead_theme': 'test-theme', 'operator_written': 'true', 'primary_theme': None}` and the test MUST assert `entry['primary_theme'] == 'test-theme'` — proving the value is sourced from `data_snapshot.lead_theme`, NOT the always-null `data_snapshot.primary_theme` (D-07). Also include a fixture row whose `data_snapshot` has no `lead_theme` and assert its `primary_theme` is `None` (never derived/fabricated).
  2. **operator_written filtering (D-01)** — a fixture set including an `operator_written='true'` edition and a non-operator edition (an edition-29 analog: `operator_written` absent / `pipeline_version='block_v1'`) yields exemplars sourced ONLY from the operator-written edition.
  3. **>=40-word non-header/non-list filtering (D-05)** — fixture `content_markdown` containing a `## header` line, a `- list item`, a short (<40-word) paragraph, and a long (>=40-word) prose paragraph yields ONLY the long prose paragraph as an exemplar; headers, list items, and the short paragraph are excluded.
  4. **CTX-03 whole-corpus-empty** — stub `.data = []` → the loader returns exactly `{'previous_editions': [], 'exemplars': [], 'empty': True}`, `caplog` (WARNING) contains the literal `"continuity context empty"`, and the call does NOT raise (the generation-still-completes proxy).
  5. **Empty-operator-pool "not scored" (D-02/D-03)** — published rows exist but NONE are operator-written → `previous_editions` is non-empty, `empty` is False, and the exemplar marker is the distinguishable "not scored" state — assert it differs from both the corpus-empty state (case 4) and from a real `score:0` / a populated `exemplars` list (no any-published fallback).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_26_continuity_loader.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd /root/bitcoin_bot && python3 -m pytest tests/test_26_continuity_loader.py -v` exits 0 with at least 5 distinct test cases (the D-16 set).
    - The test imports `newsletter_poller` and calls `nl.load_edition_context`; it does NOT define its own `def load_edition_context` (grep the test file: zero local re-implementations).
    - A test asserts `caplog` contains the literal `"continuity context empty"` for the empty-corpus case.
    - A test asserts the empty-operator-pool marker is distinguishable from the corpus-empty state and from a populated exemplars list.
    - A test asserts `weeks_ago` is omitted for a fixture row with null `published_at`.
    - A test asserts `primary_theme == 'test-theme'` for a fixture whose `data_snapshot` has `lead_theme='test-theme'` (D-07: sourced from `lead_theme`, not the always-null `primary_theme`), and asserts `primary_theme is None` for a fixture with no `lead_theme`.
  </acceptance_criteria>
  <done>tests/test_26_continuity_loader.py exists, imports the REAL loader, covers all 5 D-16 cases (shape, operator_written filtering, >=40-word/non-header/non-list filtering, corpus-empty WARNING + explicit marker + no-raise, empty-operator-pool not-scored), and `pytest tests/test_26_continuity_loader.py` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> live Supabase | The test must NOT reach the live project; all reads come from an in-memory stub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-T1 | Information disclosure | test Supabase stub | mitigate | Tests use an in-memory stub returning fixture `.data`; no network, no credentials, never connect to the live project ref |
| T-26-T2 | Tampering (false pass) | test imports a copy of the loader | mitigate | The test imports the REAL `nl.load_edition_context` via the conftest preload (test_19 rule) — a production regression fails the suite; no local re-implementation |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — uses the existing pytest + conftest harness |
</threat_model>

<verification>
- `python3 -m pytest tests/test_26_continuity_loader.py -v` exits 0.
- The test file imports `newsletter_poller` (the real module) and contains no local `def load_edition_context`.
- All 5 D-16 cases are present and pass.
</verification>

<success_criteria>
- The loader's contract and both degrade paths are proven deterministically against fixtures, with the real production function, before any live generation budget is spent.
</success_criteria>

<output>
Create `.planning/phases/26-continuity-exemplar-context/26-02-SUMMARY.md` when done.
</output>
