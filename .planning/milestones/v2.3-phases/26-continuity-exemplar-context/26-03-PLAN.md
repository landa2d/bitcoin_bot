---
phase: 26-continuity-exemplar-context
plan: 03
type: execute
wave: 3
depends_on: [26-01, 26-02]
files_modified: []
autonomous: false
requirements: [CTX-04, CTX-05]
user_setup: []

must_haves:
  truths:
    - "data_snapshot.lead_theme is populated (operator-confirmed) on editions 25-28 and published_at is non-null on all 7 operator-written editions (25,26,27,28,30,31,32) (D-12/D-13)"
    - "A live generation against the 32-edition corpus populates the EDITORIAL CONTINUITY block in the prompt and the draft opening contains a bridge to a prior edition (CTX-04, D-17)"
    - "The live A/B-path Phase E returns voice_score.score > 0 with >=1 observation (no 'No exemplars provided' sentinel) (CTX-05, D-17)"
    - "The operator has read the generated opening bridge for prose quality — mechanism-correct does not auto-pass the human gate (D-18)"
  artifacts:
    - path: "live: newsletters A/B row (data_snapshot.voice_score)"
      provides: "Phase E real voice score from the :2269 A/B path on a fresh generation"
      contains: "voice_score"
    - path: "live: data_snapshot.lead_theme on editions 25-28"
      provides: "operator-confirmed editorial theme copy backfilled via MCP UPDATE"
      contains: "lead_theme"
  key_links:
    - from: "live newsletter generation (process_task)"
      to: "load_edition_context"
      via: "runtime setdefault injection -> EDITORIAL CONTINUITY prompt block"
      pattern: "EDITORIAL CONTINUITY"
    - from: "A/B generate_from_blocks (:2269)"
      to: "data_snapshot.voice_score"
      via: "Phase E real DeepSeek score"
      pattern: "voice_score"
---

<objective>
Prove Phase 26 end-to-end on the live system and complete the one optional data-hygiene backfill. Backfill `data_snapshot.lead_theme` on operator editions 25-28 (operator-confirmed editorial copy, applied via Supabase MCP UPDATE), then trigger one real generation against the live 32-edition corpus and verify the continuity context + exemplars + resurrected Phase E all fire.

Purpose: D-12/D-13 make the judge's bridge-verification angle text real for editions 25-28 and keep `weeks_ago` correct; D-17/D-18 are the live acceptance gate for CTX-04 + CTX-05. Both steps are worktree-unsafe and orchestrator/operator-owned on the main tree (a live-data MCP mutation + a scoped container rebuild).
Output: backfilled `lead_theme` on 4 editions, one fresh generation whose draft opens with a real cross-edition bridge and whose A/B Phase E score is > 0, and the operator's prose-quality read.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-continuity-exemplar-context/26-CONTEXT.md
@.planning/phases/26-continuity-exemplar-context/26-PATTERNS.md
@docs/audit/specs/07_continuity_and_exemplars.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Operator-confirmed lead_theme backfill on editions 25-28 (orchestrator-owned MCP UPDATE)</name>
  <files>(no code files — live data mutation via Supabase MCP on the main tree)</files>
  <read_first>
    - .planning/phases/26-continuity-exemplar-context/26-CONTEXT.md (D-12: which editions; D-13: operator-confirmation rule + MCP/worktree-unsafe discipline)
    - .planning/phases/26-continuity-exemplar-context/26-PATTERNS.md (the "Data-hygiene backfill (D-12/D-13) — NOT a code file" section)
    - Live DB facts: operator-written editions are 25,26,27,28,30,31,32; lead_theme is populated only on 30,31,32; null on 25-28; edition 29 is block_v1 (excluded)
  </read_first>
  <decision>Approve (or edit) the candidate `data_snapshot.lead_theme` strings derived for operator editions 25, 26, 27, 28 before they are written to the live DB.</decision>
  <context>
`lead_theme` is operator-authored editorial copy (NULL != intent — it must not be machine-fabricated and silently committed). The orchestrator first reads each of editions 25-28 via MCP (the `title` + the opening thesis of `content_markdown` after the `## Read This, Skip the Rest` header) and derives ONE short candidate `lead_theme` per edition from the opening thesis. The candidates are presented here for operator confirmation. Editions 30-32 already have `lead_theme` and are NOT touched.

On approval, the orchestrator applies the change via Supabase MCP `UPDATE` on each edition's `data_snapshot.lead_theme` (a live-data mutation on the main tree — worktree-unsafe, NOT a migration file; do NOT add anything under `supabase/migrations/`). It then verifies, via MCP select, that `data_snapshot.lead_theme` is non-empty on all 4 backfilled editions AND that `published_at` is non-null on all 7 operator-written editions (25,26,27,28,30,31,32) — so `weeks_ago` (D-09) stays correct. No executor runs raw SQL blindly.
  </context>
  <options>
    <option id="approve-candidates">
      <name>Approve the derived candidate themes as-is</name>
      <pros>Fastest; the candidates are drawn directly from each edition's own opening thesis</pros>
      <cons>Operator accepts machine-derived phrasing of their editorial copy</cons>
    </option>
    <option id="edit-then-apply">
      <name>Operator supplies / edits the theme string for one or more editions</name>
      <pros>Editorial copy stays fully in operator hands (matches the "framing in human hands" invariant)</pros>
      <cons>Requires the operator to write/adjust up to 4 short strings</cons>
    </option>
  </options>
  <verify>
    <automated>MISSING — live MCP verification: after apply, an MCP select confirms data_snapshot.lead_theme is non-empty on editions 25-28 and published_at is non-null on editions 25,26,27,28,30,31,32 (orchestrator runs this on the main tree; no executor SQL).</automated>
  </verify>
  <acceptance_criteria>
    - The candidate `lead_theme` for each of editions 25-28 was presented and operator-confirmed (or operator-edited) before any write.
    - After apply, `data_snapshot.lead_theme` is non-empty on editions 25, 26, 27, 28 (verified via MCP select).
    - `published_at` is non-null on all 7 operator-written editions (25,26,27,28,30,31,32).
    - No file was added under `supabase/migrations/` (this is a data mutation, not a migration).
  </acceptance_criteria>
  <resume-signal>Type "approved" to apply the shown candidates, or supply edited theme strings for the editions you want to change.</resume-signal>
  <done>Editions 25-28 have an operator-confirmed `data_snapshot.lead_theme`; all 7 operator editions have non-null `published_at`; applied via MCP on the main tree, no migration file created.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live generation trigger + end-to-end verification (orchestrator-owned, worktree-unsafe)</name>
  <files>(no code files — scoped rebuild + one live generation on the main tree)</files>
  <read_first>
    - .planning/phases/26-continuity-exemplar-context/26-CONTEXT.md (D-17 live-trigger acceptance, D-18 prose human gate, D-19 the live A/B path :2269)
    - docs/audit/specs/07_continuity_and_exemplars.md (Acceptance criteria 1-4 + the Human gate)
    - docker/newsletter/newsletter_poller.py:1182 (the `Injected editorial continuity into system prompt` log line) and :2269-2336 (the A/B path + the data_snapshot.voice_score write at :2329)
    - docker/newsletter/block_pipeline.py:673-679 (the `[BLOCK PIPELINE] Phase E: voice score = ...` log line)
    - MEMORY: scoped rebuild -> worktree-unsafe (cd to the absolute main-tree path); newsletter image drift (this rebuild also ships the Phase 19 corrected apostrophe guard)
  </read_first>
  <action>
Orchestrator-owned, on the main tree (worktree-unsafe — a scoped container rebuild against `/root/bitcoin_bot/docker`, run sequentially / no-worktree):
  1. Syntax-check both modified modules (`python3 -c "import ast; ast.parse(...)"`) then scoped-rebuild only the newsletter service: `cd /root/bitcoin_bot/docker && docker compose up -d --build newsletter`.
  2. Trigger ONE real `write_newsletter` generation against the live 32-published-edition corpus (via the existing newsletter trigger / queued agent_task — the operator's normal re-queue path) and watch the newsletter container logs.
  3. Verify (D-17, spec 07 acceptance 1-2): the logs show the loader line `Narrative context: N edition(s), M exemplar(s)` with M > 0 and the single-pass `Injected editorial continuity into system prompt (...)` line; the EDITORIAL CONTINUITY block is populated in the prompt; the generated draft's opening contains an explicit bridge to a prior edition. Because the live config is `enabled:false`/`ab_comparison:true`, Phase E runs through the A/B path (:2269) — confirm `[BLOCK PIPELINE] Phase E: voice score = ...` is > 0 and that the new A/B `newsletters` row's `data_snapshot.voice_score` has `score > 0` with >=1 observation (NOT the "No exemplars provided" / "not scored" marker). No need to flip `block_pipeline.enabled`.
  4. Confirm the prepass prompt includes the prior 3 angles as avoided themes (spec 07 acceptance 3).

Then the operator reads the generated opening bridge for PROSE QUALITY (D-18 / spec 07 human gate): the mechanism being correct does not make the prose good — framing stays in human hands, and the Monday publish gate is unchanged.
  </action>
  <verify>
    <automated>MISSING — live verification: orchestrator inspects the newsletter container logs for `Injected editorial continuity into system prompt`, `Narrative context: N edition(s), M exemplar(s)` (M>0), and `[BLOCK PIPELINE] Phase E: voice score =` (>0), and reads the fresh A/B newsletters row's data_snapshot.voice_score (score>0, >=1 observation). Worktree-unsafe; runs on the main tree.</automated>
  </verify>
  <what-built>The 26-01 loader + wiring deployed live (scoped newsletter rebuild) and exercised by one real generation against the 32-edition corpus.</what-built>
  <how-to-verify>
    1. Confirm the rebuild succeeded and the newsletter container is healthy.
    2. In the newsletter logs for the triggered generation, confirm: `Narrative context: N edition(s), M exemplar(s)` (M > 0); `Injected editorial continuity into system prompt`; `[BLOCK PIPELINE] Phase E: voice score = <n>` with n > 0.
    3. Open the freshly-generated A/B `newsletters` row and confirm `data_snapshot.voice_score.score > 0` with at least one observation (not the "not scored" marker).
    4. Read the draft's opening paragraph: does it contain a real, accurate one-sentence bridge to a prior edition? Is the prose good (D-18 — mechanism-correct is not enough)?
  </how-to-verify>
  <acceptance_criteria>
    - The newsletter service rebuilt cleanly on the main tree and a real generation completed.
    - Logs show the EDITORIAL CONTINUITY injection and a loader exemplar count > 0.
    - The fresh A/B row's `data_snapshot.voice_score.score` is > 0 with >=1 observation (CTX-05 proven live — no "No exemplars provided" sentinel).
    - The draft opening contains a bridge to a prior edition (CTX-04 proven live).
    - The operator has read the opening bridge for prose quality (D-18 human gate satisfied or issues recorded).
  </acceptance_criteria>
  <resume-signal>Type "approved" if the continuity block is populated, Phase E scored > 0, the opening bridge is present, and the prose reads well — or describe the issues (clumsy bridge, wrong edition reference, etc.).</resume-signal>
  <done>One live generation shows a populated EDITORIAL CONTINUITY block, a draft opening with a real cross-edition bridge, a Phase E `voice_score.score > 0` with >=1 observation on the A/B row, and the operator has read the bridge for prose quality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator -> live Supabase (editorial copy) | The D-13 MCP UPDATE mutates operator-authored `lead_theme` on live published rows |
| repo main tree -> live newsletter container | The scoped rebuild ships code into the running service; a stale worktree would ship wrong code |
| live exemplars -> live LLM prompt | The generation injects exemplar paragraphs into the real writer/judge prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Tampering | live MCP UPDATE on data_snapshot.lead_theme | mitigate | Candidate themes operator-confirmed before apply (D-13); applied on the main tree via MCP by the orchestrator, never blind executor SQL; `lead_theme` is operator-authored editorial copy |
| T-26-04 | Tampering | scoped rebuild against stale worktree code | mitigate | Rebuild runs on the absolute main-tree path `/root/bitcoin_bot/docker`, worktree-unsafe -> orchestrator-owned, sequential / no-worktree (MEMORY: scoped rebuild -> worktree-unsafe) |
| T-26-02 | Information disclosure | live exemplars injected into the live LLM prompt | mitigate | The 26-01 loader restricts exemplars to `operator_written` published editions; verify live that no held/unpublished/pipeline prose enters the prompt |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; the scoped rebuild ships the already-merged 26-01 code (note: this newsletter rebuild also ships the Phase 19 corrected apostrophe guard per the STATE carry-over) |
</threat_model>

<verification>
- MCP select: `data_snapshot.lead_theme` non-empty on editions 25-28; `published_at` non-null on 25,26,27,28,30,31,32.
- Newsletter logs (fresh generation): `Narrative context: N edition(s), M exemplar(s)` (M>0); `Injected editorial continuity into system prompt`; `[BLOCK PIPELINE] Phase E: voice score = <n>` (n>0).
- Fresh A/B `newsletters` row: `data_snapshot.voice_score.score > 0` with >=1 observation.
- Draft opening contains a cross-edition bridge; operator has read it for prose quality (D-18).
</verification>

<success_criteria>
- Editions 25-28 carry an operator-confirmed `lead_theme`; all 7 operator editions have non-null `published_at`.
- A live generation proves CTX-04 (continuity context reaches the writer) and CTX-05 (Phase E real score) end-to-end, without flipping `block_pipeline.enabled`.
- The operator has applied the unchanged human prose gate to the generated bridge.
</success_criteria>

<output>
Create `.planning/phases/26-continuity-exemplar-context/26-03-SUMMARY.md` when done.
</output>
