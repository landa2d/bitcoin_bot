---
phase: 27-eval-persistence-governed-agent
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/newsletter/edition_eval.py
  - tests/test_27_edition_eval.py
autonomous: true
requirements: [EVAL-02, EVAL-03]

must_haves:
  truths:
    - "edition_eval.write_eval_row writes an ok-row (eval_status='ok', verdict NOT NULL, error NULL) and an error-row (eval_status='error', verdict NULL, error NOT NULL) — a proxy 402/cap-hit is an error state, never a silent 0 score (D-09 a/b)"
    - "An inconsistent (eval_status/verdict/error) combination is rejected with a loud ValueError BEFORE any insert (Python-side mirror of the DB verdict-iff-ok CHECK, since the stub does not enforce CHECKs)"
    - "A failed insert logs ERROR with exc_info=True and re-raises — never a bare except, never swallowed (D-09 c)"
    - "All reads/writes use plain supabase-py .eq(); no .in_() anywhere (D-09 d / EVAL-03)"
    - "The fixture test imports the REAL edition_eval module (no re-implementation) and asserts against an in-memory Supabase stub, never the live DB"
  artifacts:
    - path: "docker/newsletter/edition_eval.py"
      provides: "write_eval_row() + read_evals_by_newsletter() + read_eval_trend() + LLM_PROXY_EVAL_KEY identity getter (no LLM call this phase)"
      contains: "def write_eval_row("
      min_lines: 70
    - path: "tests/test_27_edition_eval.py"
      provides: "deterministic fixture suite proving the D-09 fail-loud contract + .eq()-only"
      contains: "import edition_eval"
      min_lines: 90
  key_links:
    - from: "tests/test_27_edition_eval.py"
      to: "docker/newsletter/edition_eval.py"
      via: "import edition_eval as ee (the REAL production module, via sys.path insert)"
      pattern: "import edition_eval"
---

<objective>
Ship the fail-loud persistence helper `docker/newsletter/edition_eval.py` (`write_eval_row()` + a by-`newsletter_id` reader + a trend reader) and its deterministic fixture test `tests/test_27_edition_eval.py`. This is the surface Phases 28/29 import and call rather than re-implementing persistence (D-08) — it exists and is tested here even though the first real caller lands in Phase 28.

This plan is pure code + test — worktree-SAFE and autonomous. It wires NO caller into the live newsletter path (the helper just exists; do NOT touch newsletter_poller.py). It makes NO LLM calls this phase (the LLM_PROXY_EVAL_KEY identity getter is established for the Phase 28 judge but never invoked here).

Purpose: realize the milestone-wide fail-loud / no-silent-zero invariant (EVAL-02) and the no-`.in_()` rule (EVAL-03) in code, locked by a fixture test before any caller spends real budget.
Output: `docker/newsletter/edition_eval.py`, `tests/test_27_edition_eval.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Locked decisions — read D-08, D-09, D-10, D-15 in full
@.planning/phases/27-eval-persistence-governed-agent/27-CONTEXT.md
# Pattern map: the insert/.eq()/fail-loud-DIVERGENCE excerpts + the test stub shape
@.planning/phases/27-eval-persistence-governed-agent/27-PATTERNS.md

# The fixture-test pattern this test MUST mirror (real-module import + in-memory stub)
@tests/test_26_continuity_loader.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create docker/newsletter/edition_eval.py (write_eval_row + readers + identity getter)</name>
  <files>docker/newsletter/edition_eval.py</files>
  <read_first>
    - .planning/phases/27-eval-persistence-governed-agent/27-CONTEXT.md D-08, D-09 (the exact write contract), D-15 (identity separation), and the "Claude's Discretion" notes (signature/return shape are the planner's call)
    - .planning/phases/27-eval-persistence-governed-agent/27-PATTERNS.md "docker/newsletter/edition_eval.py" section — the insert-that-captures-id excerpt (newsletter_poller.py:1719-1724), the .eq()-only read excerpt (:2217-2227), the fail-loud DIVERGENCE excerpt (do NOT copy the :2044/:1723 fail-soft style), and the identity getter to DIVERGE FROM (:199, :206-218)
    - .planning/REQUIREMENTS.md lines 103-134 (the column contract the row payload must match exactly) and EVAL-02 / EVAL-03 (lines 51-52)
  </read_first>
  <behavior>
    - write_eval_row with eval_status='ok', verdict='passed' → builds a payload {eval_status:'ok', verdict:'passed', error:None, ...}, inserts, returns the inserted id (str)
    - write_eval_row with eval_status='error', error='proxy 402 cap hit', verdict=None → builds {eval_status:'error', verdict:None, error:'proxy 402 cap hit', sats_spent:0}, inserts, returns id — NOT a silent 0 score
    - write_eval_row with eval_status='error' but verdict='passed' (or eval_status='ok' but verdict=None) → raises ValueError BEFORE inserting (mirror of the DB verdict-iff-ok CHECK; the stub does not enforce CHECKs)
    - write_eval_row with eval_status not in ('ok','error') → raises ValueError
    - write_eval_row when supabase.insert(...).execute() raises → logs ERROR with exc_info=True (message names newsletter_id/layer/attempt) then re-raises the original exception (never swallowed)
    - read_evals_by_newsletter(supabase, newsletter_id) → chains .table('edition_evals').select(...).eq('newsletter_id', newsletter_id).order('attempt').execute(); returns .data or []
    - read_eval_trend(supabase, pipeline_version, limit=8) → chains .eq('pipeline_version', pipeline_version).order('edition_number', desc=True).limit(limit).execute(); returns .data or []
    - no method ever calls .in_()
  </behavior>
  <action>
    Create a NEW thin persistence module. Module-level: `import logging`, `import os`; `logger = logging.getLogger("newsletter-agent")` (reuse the newsletter logger name); a constant `LLM_PROXY_EVAL_KEY = os.getenv("LLM_PROXY_EVAL_KEY")` and `def _get_eval_api_key() -> str | None: return os.getenv("LLM_PROXY_EVAL_KEY") or None`, with a docstring noting this is the eval agent's OWN identity (D-15) for the Phase 28 judge's proxy calls — it must NOT reuse newsletter_poller._get_agent_api_key()/AGENT_NAME='newsletter'. It is NOT called this phase (no LLM calls in 27). Do NOT build an anthropic/OpenAI proxy client here.

    `write_eval_row(supabase, *, newsletter_id, edition_number, pipeline_version, attempt, layer, eval_status, verdict=None, error=None, deterministic_flags=None, judge_scores=None, judge_feedback=None, sats_spent=0, model_calls=None) -> str`. Take `supabase` as the FIRST positional param (mirrors load_edition_context) so the test passes the in-memory stub — do NOT use a module-global client. Validate fail-loud BEFORE the insert: raise ValueError if eval_status not in {'ok','error'}; raise ValueError unless (eval_status=='ok' and verdict is not None and error is None) or (eval_status=='error' and verdict is None and error is not None) — this is the Python mirror of edition_evals_verdict_iff_ok (the stub won't enforce the DB CHECK). Build the payload dict with every edition_evals column from REQUIREMENTS.md (default deterministic_flags→{}, judge_scores→{}, model_calls→[]). Insert via `supabase.table('edition_evals').insert(payload).execute()` and read back the id (`result.data[0]['id'] if result.data else None`, mirror :1721). DIVERGE from the analog's fail-soft handling: wrap the insert in `try/except Exception: logger.error("edition_evals write failed for newsletter_id=%s layer=%s attempt=%s", newsletter_id, layer, attempt, exc_info=True); raise` — never a bare except, never swallowed (D-09c). Never log the api key. Return the id.

    `read_evals_by_newsletter(supabase, newsletter_id) -> list[dict]`: `.table('edition_evals').select('*').eq('newsletter_id', newsletter_id).order('attempt').execute()`; return `result.data or []`. `read_eval_trend(supabase, pipeline_version, limit=8) -> list[dict]`: `.table('edition_evals').select('edition_number, pipeline_version, layer, attempt, eval_status, verdict, judge_scores, sats_spent, created_at').eq('pipeline_version', pipeline_version).order('edition_number', desc=True).limit(limit).execute()`; return `result.data or []`. EVAL-03: use ONLY `.eq()` — never `.in_()` anywhere in this module.
  </action>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('docker/newsletter/edition_eval.py').read())"` exits 0
    - `grep -c 'def write_eval_row(' docker/newsletter/edition_eval.py` returns 1
    - `grep -c '\.in_(' docker/newsletter/edition_eval.py` returns 0 (EVAL-03)
    - `grep -c 'exc_info=True' docker/newsletter/edition_eval.py` is ≥ 1 AND the file contains `raise` (fail-loud, D-09c)
    - file contains `os.getenv("LLM_PROXY_EVAL_KEY")` (identity separation, D-15) and does NOT contain `_get_agent_api_key` or `AGENT_NAME` (must not reuse newsletter identity)
    - file contains `read_evals_by_newsletter` and `read_eval_trend`
    - file does NOT import or reference `newsletter_poller` (no live-path wiring this phase)
  </acceptance_criteria>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('docker/newsletter/edition_eval.py').read())" && [ "$(grep -c 'def write_eval_row(' docker/newsletter/edition_eval.py)" = "1" ] && [ "$(grep -c '\.in_(' docker/newsletter/edition_eval.py)" = "0" ] && grep -q 'exc_info=True' docker/newsletter/edition_eval.py && grep -q 'LLM_PROXY_EVAL_KEY' docker/newsletter/edition_eval.py && [ "$(grep -c '_get_agent_api_key\|newsletter_poller' docker/newsletter/edition_eval.py)" = "0" ] && echo PASS</automated>
  </verify>
  <done>edition_eval.py exists with a fail-loud write_eval_row (validates verdict-iff-ok in Python, logs+raises on write failure, never a silent zero), .eq()-only readers, and a separate LLM_PROXY_EVAL_KEY identity getter — no LLM calls, no live-path wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_27_edition_eval.py (deterministic fixture suite)</name>
  <files>tests/test_27_edition_eval.py</files>
  <read_first>
    - tests/test_26_continuity_loader.py (the analog — mirror its real-module import via sys.path insert, the _StubResult/_StubQuery/StubSupabase in-memory stub, the caplog WARNING/ERROR assertion style, and one-behavior-per-test structure)
    - docker/newsletter/edition_eval.py (the module under test — Task 1's actual signatures)
    - .planning/phases/27-eval-persistence-governed-agent/27-PATTERNS.md "tests/test_27_edition_eval.py" section (the stub must additionally support .insert(payload) returning data=[{'id': <uuid>}] AND capture the inserted payload to assert the column contract; a raising-insert stub for the loud-raise test; a stub WITHOUT .in_() so accidental use raises AttributeError)
  </read_first>
  <action>
    Mirror test_26's header: insert `docker/newsletter` on sys.path then `import edition_eval as ee` (the REAL module — no re-implementation; edition_eval.py imports only stdlib and takes supabase as a parameter, so no conftest preload is needed, unlike the schemas-importing pollers). Build an in-memory Supabase stub extending test_26's shape: add an `.insert(payload)` method that captures the payload (e.g. appends to a list the test can read) and whose `.execute()` returns a `_StubResult(data=[{'id': '00000000-0000-0000-0000-000000000001'}])`; deliberately OMIT `.in_()` so any accidental `.in_()` raises AttributeError. Provide a separate "raising" stub whose `.insert(...).execute()` raises RuntimeError.

    Prove, one behavior per test (D-08/D-09):
    - ok-row write: `write_eval_row(stub, ..., layer='deterministic', eval_status='ok', verdict='passed')` returns the stub id; the CAPTURED payload has eval_status='ok', verdict='passed', error is None.
    - error-row write (the no-silent-zero proof): `write_eval_row(stub, ..., eval_status='error', error='proxy 402 cap hit', verdict=None)` — captured payload has eval_status='error', error='proxy 402 cap hit', verdict is None, and sats_spent==0 is NOT a verdict/score (assert verdict is None, not a numeric 0).
    - verdict-iff-ok rejection: `write_eval_row(stub, ..., eval_status='error', verdict='passed')` raises ValueError (pytest.raises); likewise eval_status='ok' with verdict=None raises ValueError; eval_status='bogus' raises ValueError. No insert is captured in these cases.
    - loud-raise-on-write-failure: with the raising stub, `write_eval_row(...)` re-raises (pytest.raises) AND an ERROR is logged (caplog.at_level(logging.ERROR); assert a record mentioning the failure / newsletter_id).
    - .eq()-only reads: `read_evals_by_newsletter(stub, 'nid')` and `read_eval_trend(stub, 'block_v1')` return the stub data list and never call `.in_()` (the stub omits it; a test asserting `not hasattr(stub_query, 'in_')` or that the chain executes without AttributeError documents the contract).
    - default JSONB shaping: an ok-row written without deterministic_flags/judge_scores/model_calls has those keys defaulted to {}/{}/[] in the captured payload.
  </action>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_27_edition_eval.py -q` exits 0 with ≥ 6 tests collected
    - `grep -c 'import edition_eval' tests/test_27_edition_eval.py` is ≥ 1 (REAL module import — no re-implementation)
    - test file contains `pytest.raises(ValueError)` (verdict-iff-ok proof) and a raising-insert test asserting re-raise
    - test file contains `caplog` (loud-log proof) and asserts verdict is None on the error-row (no-silent-zero proof)
    - the stub class in the test does NOT define an `in_` method (so accidental `.in_()` would raise AttributeError) — `grep -c 'def in_' tests/test_27_edition_eval.py` returns 0
  </acceptance_criteria>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_27_edition_eval.py -q && grep -q 'import edition_eval' tests/test_27_edition_eval.py && grep -q 'pytest.raises(ValueError)' tests/test_27_edition_eval.py && grep -q 'caplog' tests/test_27_edition_eval.py && [ "$(grep -c 'def in_' tests/test_27_edition_eval.py)" = "0" ] && echo PASS</automated>
  </verify>
  <done>The fixture suite imports the REAL edition_eval module and proves, against an in-memory stub (no live DB), the four D-09 fail-loud contract points (ok-row, error-row-not-zero, verdict-iff-ok ValueError, loud-raise-on-write-failure) plus .eq()-only reads — all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 28/29 caller → write_eval_row → DB | untrusted eval-result dicts cross into persisted governance telemetry |
| env (LLM_PROXY_EVAL_KEY) → module | the eval agent's own secret identity is read here (used in Phase 28) |
| test → in-memory stub | the test must never touch the live DB or network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Repudiation (silent data loss) | write_eval_row insert failure | mitigate | `except Exception: logger.error(..., exc_info=True); raise` — the eval result is never silently lost; the caller cannot continue as if the row landed. Test proves re-raise via pytest.raises + caplog (D-09c). |
| T-27-05 | Tampering (silent-zero) | a `0` masquerading as a real eval | mitigate | error-rows carry eval_status='error' + a reason + NULL verdict; write_eval_row raises ValueError on any verdict/status mismatch BEFORE insert. Test asserts verdict is None (not numeric 0) on the 402-cap error-row (D-09 a/b). |
| T-27-06 | Information Disclosure | logging the eval api key | mitigate | The module reads LLM_PROXY_EVAL_KEY but NEVER logs it; error log lines name only newsletter_id/layer/attempt. Acceptance criteria confirm no key in log format strings. |
| T-27-07 | Denial of Service (silent-failure bug) | supabase-py `.in_()` | mitigate | `.eq()`-only across the module; the test stub omits `.in_()` so any accidental use raises AttributeError. grep gate asserts zero `.in_(` (EVAL-03). |
| T-27-08 | Tampering | test re-implements the helper (a copy passes while prod regresses) | mitigate | The test imports the REAL `edition_eval` module (test_19_smartquote rule); grep asserts `import edition_eval`. |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package installs (uses already-present pytest + stdlib). No supply-chain surface. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_27_edition_eval.py -v` — all tests green.
- `python3 -m pytest tests/` — no regressions in the existing suite (the new module is import-isolated; takes supabase as a param, imports only stdlib).
- All Task 1 + Task 2 automated gates PASS.
</verification>

<success_criteria>
- `docker/newsletter/edition_eval.py` exists: fail-loud write_eval_row (verdict-iff-ok Python validation, log+raise on write failure, no silent zero), .eq()-only readers, separate LLM_PROXY_EVAL_KEY identity getter, no LLM calls, no live-path wiring (EVAL-02, EVAL-03).
- `tests/test_27_edition_eval.py` imports the REAL module and proves the D-09 contract against an in-memory stub — green.
- `grep -c '\.in_(' docker/newsletter/edition_eval.py` returns 0.
</success_criteria>

<output>
Create `.planning/phases/27-eval-persistence-governed-agent/27-02-SUMMARY.md` when done.
</output>
