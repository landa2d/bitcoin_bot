---
phase: 27-eval-persistence-governed-agent
plan: 03
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - supabase/migrations/045_edition_evals.sql
  - config/.env
autonomous: false
requirements: [EVAL-01, GOV-01, GOV-02]
user_setup:
  - service: supabase
    why: "Apply migration 045 to live Postgres after DDL review (project ref zxzaaqfowtqvmsbitqpu)"
    dashboard_config:
      - task: "Apply migration 045 via the Supabase MCP tool (mcp__claude_ai_Supabase__apply_migration) — NOT supabase db push, NOT from a worktree"
        location: "Supabase MCP (orchestrator/operator on the main tree)"

must_haves:
  truths:
    - "The minted ap_edition_eval_<hash> key's REAL bcrypt hash is substituted into SECTION 2 of 045 (the <bcrypt-hash> placeholder is gone) and committed as the audit record (D-13)"
    - "LLM_PROXY_EVAL_KEY (plaintext) is present in config/.env and ABSENT from docker/docker-compose.yml — the newsletter container reads it via the existing env_file (no compose change, honoring 'never in compose')"
    - "edition_evals table + the edition_eval agent_registry/agent_wallets_v2 rows exist live in Supabase (project zxzaaqfowtqvmsbitqpu) after MCP apply"
    - "One proxy call as edition_eval through llm-proxy:8200 SETTLES — agent_wallets_v2.balance_sats decrements from 25000 and a wallet/transaction row is recorded (governed-cycle proof, not container-up)"
  artifacts:
    - path: "supabase/migrations/045_edition_evals.sql"
      provides: "the committed file with the REAL bcrypt hash substituted (audit record)"
      contains: "$2b$"
  key_links:
    - from: "config/.env LLM_PROXY_EVAL_KEY (plaintext)"
      to: "agent_registry.api_key_hash (committed bcrypt hash)"
      via: "proxy bcrypt.checkpw authenticates the edition_eval agent (proxy.py:218)"
      pattern: "edition_eval"
---

<objective>
Realize the table + governed agent LIVE: the orchestrator/operator (1) mints the `edition_eval` proxy key + its bcrypt hash, (2) substitutes the REAL hash into SECTION 2 of `supabase/migrations/045_edition_evals.sql` and delivers the plaintext to `config/.env` as `LLM_PROXY_EVAL_KEY`, (3) MCP-applies migration 045 to live Supabase, then (4) verifies a settled proxy call as `edition_eval`.

EVERY task here is worktree-UNSAFE and orchestrator/operator-owned on the main tree (a worktree executor cannot mint keys, write secrets, or apply migrations) — the plan is `autonomous: false`. It runs AFTER 27-01 authored the SQL file (depends_on: 27-01; same file overlap forces wave 2). Do NOT use `supabase db push`; the migration is applied only via the Supabase MCP tool after DDL review.

Purpose: stand up the persistence + budget core in the live DB so Phases 28/29 can write through it and call the proxy as a governed agent (EVAL-01 live form, GOV-01, GOV-02). The migration file existing is NOT the table existing — and import/syntax checks pass without the apply, a false-positive this plan closes.
Output: a committed 045 with the real hash; `LLM_PROXY_EVAL_KEY` in config/.env; the live table + agent; a settled-call proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Locked decisions — read D-12, D-13, D-14, D-15 in full (key-mint sequencing + settled-call discipline)
@.planning/phases/27-eval-persistence-governed-agent/27-CONTEXT.md
# Authoritative seed values + the SQL-first / operator-applies rule
@.planning/REQUIREMENTS.md
# The committed-hash precedent
@supabase/migrations/029_rivalscope_agent.sql
# The file authored in 27-01 (the substitution target)
@supabase/migrations/045_edition_evals.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Task 1: Mint the edition_eval key, substitute the real bcrypt hash, deliver to config/.env</name>
  <files>supabase/migrations/045_edition_evals.sql, config/.env</files>
  <read_first>
    - .planning/phases/27-eval-persistence-governed-agent/27-CONTEXT.md D-12, D-13, D-15 (mint-first → substitute-hash → deliver-to-.env, then apply; the committed hash is the audit record)
    - supabase/migrations/029_rivalscope_agent.sql line 8 (the committed-hash precedent — format $2b$12$...)
    - docker/llm-proxy/proxy.py lines 210-232 (authenticate_agent: bcrypt.checkpw(api_key, key_hash) — confirms the key format the mint must produce and that a bcrypt hash of the plaintext is what api_key_hash stores)
    - config/.env (the existing LLM_PROXY_<SERVICE>_KEY entries — match their format; LLM_PROXY_EVAL_KEY is net-new)
  </read_first>
  <action>
    Orchestrator/operator on the MAIN tree (NOT a worktree). Mint a new proxy key for the agent named `edition_eval` in the format `ap_edition_eval_<random>` (matching the existing `ap_<agent>_<hash>` scheme; reuse the same generation mechanic 029's key was minted with — the researcher/orchestrator confirms the exact command). Compute its bcrypt hash with `bcrypt.hashpw(key.encode(), bcrypt.gensalt())` (cost 12, the $2b$12$ form the proxy validates via bcrypt.checkpw).

    Substitute the REAL bcrypt hash in place of the literal `<bcrypt-hash>` placeholder in SECTION 2 of supabase/migrations/045_edition_evals.sql (the committed hash is the audit record of the live key — D-13, 029 precedent). The PLAINTEXT key is NEVER written to the migration file, NEVER to docker-compose, NEVER logged.

    Append `LLM_PROXY_EVAL_KEY=<plaintext key>` to config/.env (gitignored; the newsletter container already loads config/.env via `env_file`, so the var reaches the container on its next rebuild — no docker-compose.yml change is needed or permitted, honoring GOV-02 "never in compose"). Do this BEFORE the MCP apply (Task 2).
  </action>
  <acceptance_criteria>
    - `grep -c '<bcrypt-hash>' supabase/migrations/045_edition_evals.sql` returns 0 (placeholder gone)
    - `grep -c '\$2b\$' supabase/migrations/045_edition_evals.sql` returns ≥ 1 (a real bcrypt hash is present)
    - `grep -c '^LLM_PROXY_EVAL_KEY=' config/.env` returns 1
    - `git check-ignore config/.env` prints `config/.env` (the plaintext key is not tracked)
    - `grep -c 'LLM_PROXY_EVAL_KEY' docker/docker-compose.yml` returns 0 (never in compose — GOV-02)
    - the plaintext key value does NOT appear anywhere in supabase/migrations/045_edition_evals.sql (only its bcrypt hash)
  </acceptance_criteria>
  <verify>
    <automated>cd /root/bitcoin_bot && [ "$(grep -c '<bcrypt-hash>' supabase/migrations/045_edition_evals.sql)" = "0" ] && [ "$(grep -c '\$2b\$' supabase/migrations/045_edition_evals.sql)" -ge "1" ] && [ "$(grep -c '^LLM_PROXY_EVAL_KEY=' config/.env)" = "1" ] && git check-ignore config/.env && [ "$(grep -c 'LLM_PROXY_EVAL_KEY' docker/docker-compose.yml)" = "0" ] && echo PASS</automated>
  </verify>
  <resume-signal>Type "minted" when the key is generated, the real hash is substituted into 045, and LLM_PROXY_EVAL_KEY is in config/.env (and confirmed absent from compose).</resume-signal>
  <done>The minted edition_eval key's bcrypt hash is committed in 045; the plaintext lives only in the gitignored config/.env; compose is untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Task 2: MCP-apply migration 045 to live Supabase</name>
  <files>supabase/migrations/045_edition_evals.sql</files>
  <read_first>
    - supabase/migrations/045_edition_evals.sql (the now-substituted file — DDL review BEFORE apply)
    - .planning/REQUIREMENTS.md "Out of Scope" (Applying schema before operator DDL approval is forbidden) + CLAUDE.md migration convention (MCP-applied, project ref zxzaaqfowtqvmsbitqpu)
    - .planning/phases/27-eval-persistence-governed-agent/27-CONTEXT.md "Current state" (043 is an unapplied carry-over and OUT OF SCOPE — apply ONLY 045; live list jumps 042→044)
  </read_first>
  <action>
    Orchestrator/operator on the MAIN tree. After DDL review of the substituted 045, apply it to live Supabase using the Supabase MCP tool `mcp__claude_ai_Supabase__apply_migration` (project ref `zxzaaqfowtqvmsbitqpu`), migration name `045_edition_evals`. Do NOT run `supabase db push`. Do NOT apply 043 (out of scope). Both sections are idempotent (CREATE TABLE IF NOT EXISTS + ON CONFLICT), so a re-apply is safe if needed.

    After apply, confirm via MCP (`list_tables` and/or a select) that: `edition_evals` exists with the verdict-iff-ok CHECK and the UNIQUE(newsletter_id, layer, attempt) constraint; `agent_registry` has the `edition_eval` row with `allowed_models` containing `claude-sonnet-4-6` and `is_active=TRUE`; `agent_wallets_v2` has the `edition_eval` row with `allow_negative=false`, `spending_cap_sats=5000`, `spending_cap_window='weekly'`, `uncapped=false`, `on_cap_behavior='reject'`, `balance_sats=25000` (the cap_or_uncapped CHECK is satisfied because 5000>0).
  </action>
  <acceptance_criteria>
    - MCP `list_tables` (schema public) includes `edition_evals`
    - MCP query `SELECT agent_name, allowed_models, is_active FROM agent_registry WHERE agent_name='edition_eval'` returns one row; allowed_models contains `claude-sonnet-4-6`; is_active true
    - MCP query `SELECT allow_negative, spending_cap_sats, spending_cap_window, uncapped, on_cap_behavior, balance_sats FROM agent_wallets_v2 WHERE agent_name='edition_eval'` returns `allow_negative=false, spending_cap_sats=5000, spending_cap_window='weekly', uncapped=false, on_cap_behavior='reject', balance_sats=25000`
    - MCP query confirming the `edition_evals_verdict_iff_ok` constraint exists on `edition_evals` (e.g. from information_schema.table_constraints / pg_constraint) returns it
  </acceptance_criteria>
  <verify>
    <human-check>Run the four MCP queries above (orchestrator/operator). All return the expected rows/constraints. This step is MCP-gated and cannot be a worktree automated check.</human-check>
  </verify>
  <resume-signal>Type "applied" when migration 045 is live and the four MCP confirmation queries pass.</resume-signal>
  <done>edition_evals + the governed edition_eval agent rows exist live in Supabase, applied via MCP (not db push), with the constraints + GOV-02 wallet values confirmed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Task 3: Verify a SETTLED proxy call as edition_eval (governed-cycle proof)</name>
  <files>supabase/migrations/045_edition_evals.sql</files>
  <read_first>
    - .planning/phases/27-eval-persistence-governed-agent/27-CONTEXT.md D-14 (verify via a settled proxy call, not container-up; the cap_or_uncapped CHECK is satisfied)
    - docker/llm-proxy/proxy.py lines 210-232 (authenticate_agent — the call authenticates by bcrypt.checkpw of the plaintext key against the committed hash) and the wallet reserve/settle path
    - .planning/phases/27-eval-persistence-governed-agent/27-PATTERNS.md "Proxy / agent-identity pattern" (Sonnet via /anthropic/v1/messages; the proxy attributes spend to the agent whose key authenticates)
  </read_first>
  <action>
    Orchestrator/operator on the MAIN tree. Make ONE minimal real model call through `llm-proxy:8200` authenticating as `edition_eval` with the plaintext `LLM_PROXY_EVAL_KEY` (e.g. a small Sonnet call via `/anthropic/v1/messages` with `model=claude-sonnet-4-6` and a 1-token prompt, OR a small DeepSeek call via `/v1/chat/completions` — either of the two allowed_models). Confirm the proxy authenticated the agent (no 401), the call returned, and it SETTLED on the governed wallet: `agent_wallets_v2.balance_sats` for `edition_eval` decremented from 25000, and a settled wallet/transaction record exists for the agent (the governed-cycle discipline — NOT merely "the container is up"). If the proxy returns 402 the cap is mis-seeded or the wallet is wrong — STOP and fix the seed (this is the SAFE-failure direction per D-02 but must not happen on the very first call against a 25000 balance / 5000 cap).
  </action>
  <acceptance_criteria>
    - the proxy call as edition_eval returns success (HTTP 200, no 401 auth error, no 402 cap error on the first call)
    - MCP query `SELECT balance_sats FROM agent_wallets_v2 WHERE agent_name='edition_eval'` returns a value STRICTLY LESS THAN 25000 (the call settled / debited)
    - a settled spend record for `edition_eval` exists (MCP query on the wallet transactions / agent_transactions table shows ≥ 1 row attributed to edition_eval with a positive sats amount)
    - the model used is one of the seeded allowed_models (claude-sonnet-4-6 or deepseek-chat) — the proxy did not reject on model allowlist
  </acceptance_criteria>
  <verify>
    <human-check>Orchestrator/operator runs the proxy call + the two MCP wallet queries. balance_sats < 25000 and a settled transaction row attributed to edition_eval. This is a live governed-cycle check; it cannot run in a worktree.</human-check>
  </verify>
  <resume-signal>Type "settled" when the edition_eval proxy call returned 200 and the wallet shows a settled debit (balance < 25000).</resume-signal>
  <done>The edition_eval agent authenticates through the proxy and a governed call settles against its hard-capped wallet — GOV-01/GOV-02 proven end-to-end, not just container-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plaintext key → config/.env | a net-new secret is created and stored; it must not leak to git/compose/logs |
| migration file → live DB | the substituted file becomes live schema + governance state via MCP apply |
| edition_eval key → llm-proxy | the minted key authenticates a budgeted identity at the proxy boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-09 | Information Disclosure | plaintext LLM_PROXY_EVAL_KEY leak | mitigate | Plaintext written ONLY to the gitignored config/.env; NEVER to the migration file (only its bcrypt hash), NEVER to docker-compose (the container reads it via env_file), NEVER logged. Acceptance criteria: `git check-ignore config/.env` passes; grep of compose + the migration for the key returns 0. |
| T-27-10 | Spoofing/Tampering | committed bcrypt hash in 045 | accept | One-way bcrypt (cost 12) of an INTERNAL proxy key; the committed hash is the audit record of which key is live (D-13, 029 precedent). It cannot be reversed to the plaintext; the plaintext is required to authenticate. Not a HIGH finding. |
| T-27-11 | Tampering | applying with the placeholder unsubstituted → dead/spoofable agent | mitigate | Task 1 substitutes the REAL hash BEFORE Task 2 applies; acceptance criteria assert the `<bcrypt-hash>` placeholder is gone and a `$2b$` hash is present; Task 3's settled call proves the real key authenticates (a placeholder hash would fail bcrypt.checkpw → 401). |
| T-27-12 | Elevation of Privilege / runaway budget | agent applied without a working cap | mitigate | GOV-02 wallet (allow_negative=false, 5000/weekly, reject-on-cap) confirmed live in Task 2; the cap_or_uncapped CHECK (034) holds (5000>0). Reject-on-cap is a SAFE failure (D-02): a tripped cap returns 402 → the eval escalates loudly, never silently passes. |
| T-27-13 | Tampering | wrong migration applied (043 carry-over) | mitigate | Task 2 applies ONLY 045 via MCP; 043 is explicitly out of scope (REQUIREMENTS Out of Scope). No `supabase db push` (which could sweep unapplied files). |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (key mint + MCP apply + a single proxy call). No supply-chain surface. |
</threat_model>

<verification>
- Task 1 automated gate PASS (placeholder gone, $2b$ present, key in .env, .env gitignored, key absent from compose).
- Task 2 MCP queries: edition_evals + the two agent rows live with the constraints + GOV-02 values.
- Task 3: a settled proxy call as edition_eval — balance_sats < 25000 + a settled transaction row (governed-cycle, not container-up).
- Phase-level: re-run plan 27-02's `python3 -m pytest tests/test_27_edition_eval.py` to confirm the helper still passes against its stub (the live table is not required for the unit test, but this confirms no drift).
</verification>

<success_criteria>
- 045 committed with the real bcrypt hash (placeholder gone); LLM_PROXY_EVAL_KEY in config/.env, absent from compose (GOV-02).
- edition_evals table + edition_eval agent_registry/agent_wallets_v2 rows live in Supabase via MCP apply (EVAL-01 live, GOV-02 live).
- A settled proxy call as edition_eval debits the hard-capped wallet (GOV-01 proven end-to-end).
</success_criteria>

<output>
Create `.planning/phases/27-eval-persistence-governed-agent/27-03-SUMMARY.md` when done.
</output>
