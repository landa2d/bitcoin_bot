---
phase: 28-layer-1-deterministic-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/newsletter/deterministic_gate.py
  - tests/test_28_deterministic_gate.py
autonomous: true
requirements: [GATE-01, GATE-04, GATE-05, GATE-08]
must_haves:
  truths:
    - "run_deterministic_gate(draft, fact_base, prior_edition, *, http_client=None, github_token=None) runs BOTH body versions (technical + impact) and returns a single flags object"
    - "The flags object has first-class top-level keys fabrication, unverified, mechanical, meta — unverified is NEVER folded into fabrication or pass (D-01)"
    - "A named study/benchmark not present in the fact base (ed-36 'MCP authentication', ed-34 'GroupMemBench') produces a fabrication flag via reused verify_draft tier1"
    - "A fake arXiv ID absent from the fact-base source text produces a fabrication flag (kind=arxiv); a real arXiv ID present in a source is clean (GATE-04)"
    - "An entity present only split-across-two-sources produces an entity_merge fabrication flag; a composite present verbatim in one single source is clean (GATE-05)"
    - "The gate trusts the handed fact_base, logs meta.fact_base_path ('blocks' vs 'input_data'), and verifies block_v1 against blocks and single-pass against input_data (GATE-08)"
    - "The gate emits flags only — no DB write, no LLM call, no status flip, no verdict computation (D-05)"
  artifacts:
    - path: "docker/newsletter/deterministic_gate.py"
      provides: "run_deterministic_gate orchestrator + arXiv membership + entity-merge refinement; imports verify_draft from verification (not rebuilt)"
      contains: "def run_deterministic_gate"
    - path: "tests/test_28_deterministic_gate.py"
      provides: "Golden-draft fixtures + GATE-01/04/05/08 unit tests importing the REAL module"
      contains: "import deterministic_gate as gate"
  key_links:
    - from: "docker/newsletter/deterministic_gate.py"
      to: "docker/newsletter/verification.py"
      via: "from verification import verify_draft, _extract_claims_from_prose, _ARXIV_ID, _STATISTIC"
      pattern: "from verification import"
    - from: "run_deterministic_gate return"
      to: "migration 045 deterministic_flags JSONB"
      via: "{fabrication, unverified, mechanical, meta} shape"
      pattern: "fabrication.*unverified.*mechanical"
---

<objective>
Build the fabrication + fact-base core of the no-LLM deterministic gate: the standalone module `docker/newsletter/deterministic_gate.py` exposing `run_deterministic_gate(draft, fact_base, prior_edition, *, http_client=None, github_token=None) -> flags`, plus the test scaffold and the GATE-01/04/05/08 unit tests. This plan establishes the module signature, the flags-object contract (the Phase 30 wiring surface), the reuse of the calibrated `verify_draft` engine, and the two net-new fabrication sub-checks (arXiv-ID membership, entity-merge per-source verbatim). Network checks (GATE-02/03) and mechanical checks (GATE-06/07) are added by Plans 02 and 03 on top of this skeleton.

Purpose: Lock the contract and the fabrication engine first (interface-first) so Plans 02/03 wire network + mechanical results into a stable orchestrator and flags shape. The arXiv-membership and entity-merge checks are genuinely net-new (the engine extracts-then-discards arXiv IDs and uses a flat fuzzy union that masks merges) — they are built thin ON TOP of the reused engine, never a rebuild.

Output: `deterministic_gate.py` (orchestrator + 2 net-new fabrication sub-checks, reusing verify_draft) and `tests/test_28_deterministic_gate.py` (import preamble, fixture builders, GATE-01/04/05/08 cases). Build-only, worktree-safe — no wiring, no DB write, no LLM, no container rebuild (D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-layer-1-deterministic-gate/28-CONTEXT.md
@.planning/phases/28-layer-1-deterministic-gate/28-RESEARCH.md
@.planning/phases/28-layer-1-deterministic-gate/28-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module scaffold, flags contract, both-version orchestrator, verify_draft reuse (GATE-01, GATE-08)</name>
  <files>docker/newsletter/deterministic_gate.py, tests/test_28_deterministic_gate.py</files>
  <read_first>
    - docker/newsletter/verification.py — verify_draft signature :483, return shape :699-719 (pull tier1_fabrications), _build_block_list dispatch on `.get('blocks')` :299/:315-316, _extract_claims_from_prose :141. Read so you reuse the engine verbatim and consume its real return keys.
    - .planning/phases/28-layer-1-deterministic-gate/28-RESEARCH.md — §"Recommended module signature + flags shape" (the exact `run_deterministic_gate` signature, the {fabrication, unverified, mechanical, meta} skeleton, the meta keys); §"TODO 2" (GATE-08 fact-base dispatch — gate TRUSTS the handed dict, logs the path, does NOT re-decide); Pitfall 3 (wrong fact base silently verifies).
    - .planning/phases/28-layer-1-deterministic-gate/28-PATTERNS.md — §"Layer 1 — Reuse the fabrication engine" (import list, the verify_draft return excerpt, the fact-base-dispatch-to-trust note); §"Output contract".
    - supabase/migrations/045_edition_evals.sql — `deterministic_flags JSONB ... {fabrication:[...], mechanical:[...]}` (:47): the column the flags object must fit; `unverified` is the extra first-class D-01 key.
    - tests/test_27_edition_eval.py :24-37 (import preamble), :43-75 (in-memory stub double shape) and tests/conftest.py — mirror the import-the-REAL-module preamble; verification.py imports only `re`/`typing` so the bare `from verification import` resolves via the sys.path.insert.
  </read_first>
  <action>
    Create `docker/newsletter/deterministic_gate.py`. At module top: `import os, re, logging`; `import httpx`; `from typing import Any`; `from verification import verify_draft, _extract_claims_from_prose, _ARXIV_ID, _STATISTIC`; `logger = logging.getLogger("newsletter")`. Define module constants now (consumed by later plans): `BODY_START_MARKER = "## Read This, Skip the Rest"` (verbatim per block_pipeline.py:661), the `READING_MODE_LABELS` seeded list (Plan 03 uses it — define the constant here so the contract is stable), `_GITHUB_URL = re.compile(r'github\.com/([A-Za-z0-9._-]+)/([A-Za-z0-9._-]+)', re.IGNORECASE)`, `_MD_LINK = re.compile(r'\[([^\]]+)\]\((https?://[^)\s]+)\)')`.

    Define the full signature `run_deterministic_gate(draft: dict, fact_base: dict, prior_edition: dict | None, *, http_client: httpx.Client | None = None, github_token: str | None = None) -> dict`. Include `http_client` and `github_token` in the signature NOW even though unused this plan (interface-first — Plan 02 uses them). Docstring states emit-only, no DB/LLM/action (D-05).

    Implement the orchestrator skeleton: defensively assert `isinstance(fact_base, dict)` (raise a clear ValueError if not — fail loud per the carry-forward 27-CONTEXT "an error is not evidence" posture). Compute `fact_base_path = "blocks" if fact_base.get("blocks") else "input_data"` and `logger.info` it (GATE-08 loud-path log; Pitfall 3). Iterate the two versions as `[("technical", draft.get("content_markdown",""), draft.get("title","")), ("impact", draft.get("content_markdown_impact",""), draft.get("title_impact",""))]` (GATE-01 runs BOTH versions — mirrors verify_draft tech+impact at poller:1756-1757). For each version with a non-empty body: call `verify_draft(body, fact_base)`, read `report["tier1_fabrications"]`, and append each as a `fabrication` entry `{"kind":"tier1_entity","value":<the entity value>,"version":<label>}` (GATE-04/05 base — the named-study fabrications like 'GroupMemBench'/'MCP authentication' surface here). Record `meta["tier1_count"][label] = report["summary"]["tier1_count"]`.

    Return `{"fabrication":[...], "unverified":[], "mechanical":[], "meta":{"fact_base_path":fact_base_path, "github_checked":0, "urls_checked":0, "github_token_present":bool(github_token or os.getenv("GITHUB_TOKEN")), "tier1_count":{"technical":0,"impact":0}}}`. The `unverified`/`mechanical` lists stay empty in this plan; network (Plan 02) and mechanical (Plan 03) populate them. NEVER fold unverified into fabrication (D-01).

    Create `tests/test_28_deterministic_gate.py`: copy the test_27 import preamble verbatim (sys.path.insert NL_DIR, `import deterministic_gate as gate` — the REAL module, never a re-implementation, per the test_19_smartquote rule). Add a `_make_draft(...)` fixture builder (returns the draft dict with title/title_impact/content_markdown/content_markdown_impact/pipeline_version), a single-pass `fact_base` builder (`{"premium_source_posts":[...], "section_b_emerging":[...], "clusters":[...]}`) and a block_v1 `fact_base` builder (`{"blocks":[{"description":...,"named_entities":[...]}], "tracked_entity_signals":[], "trending_tools":[], "predictions":[]}`). Add the `if __name__ == "__main__": sys.exit(pytest.main([__file__, "-v"]))` footer. Write GATE-01 tests (both bodies processed; flags object has all four top-level keys; unverified+mechanical empty here) and GATE-08 tests (a block_v1 fact_base sets meta.fact_base_path=="blocks"; single-pass sets "input_data"; both run verify_draft without error).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('docker/newsletter/deterministic_gate.py').read())" && python3 -m pytest tests/test_28_deterministic_gate.py -k "factbase or shape or both_version or gate01 or gate08" -x</automated>
  </verify>
  <acceptance_criteria>
    - `deterministic_gate.py` contains `def run_deterministic_gate(` with params `draft, fact_base, prior_edition, *, http_client=None, github_token=None`.
    - `grep -n "from verification import verify_draft" docker/newsletter/deterministic_gate.py` matches (engine reused, not rebuilt).
    - `grep -n 'BODY_START_MARKER = "## Read This, Skip the Rest"' docker/newsletter/deterministic_gate.py` matches verbatim.
    - The returned dict has exactly the top-level keys `fabrication`, `unverified`, `mechanical`, `meta` (assert in test).
    - A block_v1 fact_base yields `meta["fact_base_path"]=="blocks"`; a single-pass fact_base yields `"input_data"` (GATE-08).
    - A draft whose body names a study absent from the fact base produces a `fabrication` entry with `kind=="tier1_entity"` (GATE-04/05 base via verify_draft).
    - No DB call, no httpx call, no LLM call occurs in this task's code path (assert no network: tests pass with `http_client=None`).
    - `pytest tests/test_28_deterministic_gate.py -k "factbase or shape or both_version or gate01 or gate08" -x` exits 0.
  </acceptance_criteria>
  <done>The module imports the calibrated engine, exposes the full `run_deterministic_gate` signature with the stable flags-object contract, runs both body versions, trusts+logs the fact-base path (GATE-08), and surfaces tier1 study/benchmark fabrications — all proven by passing tests that import the real module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: arXiv-ID membership (GATE-04) + entity-merge per-source verbatim refinement (GATE-05)</name>
  <files>docker/newsletter/deterministic_gate.py, tests/test_28_deterministic_gate.py</files>
  <read_first>
    - docker/newsletter/verification.py — :287-289 (arXiv IDs are extracted-then-DISCARDED today; there is NO membership test — this is the GATE-04 gap to close); :499-514 (the flat-union fuzzy `be in entity.lower()` match that masks entity-merge — the GATE-05 gap); _build_block_list :299-480 (the per-source field accessors to mirror PER-ITEM instead of unioning); _ARXIV_ID :109; _extract_claims_from_prose :141 (entity extraction to reuse).
    - .planning/phases/28-layer-1-deterministic-gate/28-RESEARCH.md — §"TODO 1" (the exact 4-step entity-merge refinement; per-source raw text strings; composite-entity criterion `len(words) >= 2` + owner/repo tokens; flag iff full composite NOT verbatim in at least one single source); §"Pitfall 1" (arXiv membership: finditer body, flag any ID not verbatim in concatenated fact-base text — the ed-36 fixture); §"Pitfall 2".
    - .planning/phases/28-layer-1-deterministic-gate/28-PATTERNS.md — §"Net-New" table rows for arXiv membership and per-source entity-merge; the verification.py:287-289 and :499-514 excerpts confirming the gaps.
  </read_first>
  <action>
    Add a helper `_fact_base_source_texts(fact_base: dict) -> list[str]` that returns a PER-SOURCE (NOT unioned) list of raw text strings, mirroring the field accessors `_build_block_list` reads but keeping each item separate: for the block_v1 path each block contributes `description + " " + " ".join(named_entities)`; for single-pass each `premium_source_posts` post contributes `title + " " + summary + " " + source_display`, each `section_b_emerging`/`clusters` item contributes `theme + " " + description`, plus tracked-entity-signal/tool/prediction text per item. This is the provenance the flat engine union lacks (verification.py:309). Do NOT touch `_build_block_list`, the `_STOP_WORDS` list, or the tier classifier (FP-regression risk).

    Implement `_check_arxiv_membership(body, source_texts, version) -> list[dict]` (GATE-04, per D-04 reuse + the net-new membership step): run `_ARXIV_ID.finditer(body)`; for each matched ID, if the ID string does NOT appear verbatim in the concatenated `source_texts`, emit a `fabrication` entry `{"kind":"arxiv","id":<id>,"version":version,"detail":"arXiv ID not present in fact base"}`. A real arXiv ID present in a source text is clean. This closes the extract-then-discard gap (verification.py:287-289).

    Implement `_check_entity_merge(body, source_texts, version) -> list[dict]` (GATE-05, the net-new per-source verbatim refinement, NOT a rebuild): get entities via `_extract_claims_from_prose(body)["entities"]`; restrict to COMPOSITE entities — those with `len(entity.split()) >= 2` — plus any `owner/repo` token captured by `_GITHUB_URL` in the body. For each composite, flag `fabrication` `{"kind":"entity_merge","entity":<entity>,"version":version}` iff the full composite string does NOT appear (case-insensitive) within at least ONE single `source_texts` string. (Appearing only split-across separate sources == a fabricated merge.) Single-word entities are left entirely to the reused verify_draft tier1 — do not re-flag them here.

    Wire both checks into `run_deterministic_gate`: build `source_texts` once, and inside the per-version loop call both and extend `fabrication`. Keep them additive to the Task-1 tier1 fabrication entries.

    Add tests: the ed-36 invented "MCP authentication" study (tier1 fabrication), ed-34 "GroupMemBench" (tier1 fabrication), a fake arXiv ID `2605.99999` absent from the fact base → `kind=="arxiv"` fabrication, a real arXiv ID present in a `premium_source_posts` summary → clean, an entity-merge case where source A names org "Acme" and source B names "widgets" but neither contains "Acme widgets" verbatim → `kind=="entity_merge"` fabrication, and a composite present verbatim in one source → clean.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_28_deterministic_gate.py -k "arxiv or merge or groupmem or mcp_auth or study" -x</automated>
  </verify>
  <acceptance_criteria>
    - A fake arXiv ID not in any fact-base source text produces a `fabrication` entry with `kind=="arxiv"` (GATE-04).
    - A real arXiv ID present verbatim in a source text produces NO arxiv fabrication flag.
    - An invented benchmark/study name ("GroupMemBench", "MCP authentication") absent from the fact base produces a fabrication flag.
    - A composite entity present only split-across two separate sources produces a `fabrication` entry with `kind=="entity_merge"` (GATE-05).
    - A composite entity present verbatim within a SINGLE source string produces NO entity_merge flag.
    - `grep -n "_build_block_list\|_STOP_WORDS" docker/newsletter/deterministic_gate.py` shows NO redefinition of these (engine not rebuilt — only imported/reused).
    - `pytest tests/test_28_deterministic_gate.py -k "arxiv or merge or groupmem or mcp_auth or study" -x` exits 0.
  </acceptance_criteria>
  <done>The two net-new fabrication sub-checks (arXiv membership, entity-merge per-source verbatim) close the documented engine gaps and fire on the golden offenders, without touching the calibrated stop-list or tier classifier — proven by passing tests on real and fabricated fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-generated draft prose → gate regex/extraction | The draft body is untrusted model output; entity/arXiv/stat extraction runs over it |
| fact_base dict → verify_draft | Caller-supplied in-memory dict; wrong shape must fail loud, not silently verify against the wrong base (GATE-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Denial of Service | ReDoS via adversarial draft text on `_GITHUB_URL`/`_MD_LINK`/reused regexes | mitigate | New regexes use simple bounded character classes (no nested quantifiers); reused engine regexes are already linear — assert no catastrophic-backtracking pattern is added |
| T-28-02 | Tampering (log injection) | `logger.info` on draft-derived content | mitigate | Log only counts, the fact_base_path label, and entity/ID values — never raw draft prose blocks at INFO |
| T-28-03 | Spoofing/Tampering | Wrong fact_base silently verifies (GATE-08) | mitigate | Defensive `isinstance(fact_base, dict)` assert + loud `meta.fact_base_path` log so a Phase-30 wrong-base wiring bug surfaces, never silently passes |
| T-28-SC | Tampering | pip installs | accept | Zero new packages this phase (httpx/pytest/verification all already present per RESEARCH Package Legitimacy Audit) — nothing to vet; no install task exists |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('docker/newsletter/deterministic_gate.py').read())"` passes (syntax gate, CLAUDE.md convention).
- `python3 -m pytest tests/test_28_deterministic_gate.py -x` green for all GATE-01/04/05/08 cases authored this plan.
- No regression: `python3 -m pytest tests/test_26_continuity_loader.py tests/test_27_edition_eval.py` still green (engine reuse must not break the existing suite).
- Source assertion: the module imports `verify_draft`/`_ARXIV_ID`/`_STATISTIC`/`_extract_claims_from_prose` from `verification` and does NOT redefine `_build_block_list` or `_STOP_WORDS`.
</verification>

<success_criteria>
- `run_deterministic_gate` exists with the full signature (including the unused-this-plan `http_client`/`github_token` seams), runs both body versions, and returns `{fabrication, unverified, mechanical, meta}` with `unverified` first-class.
- Tier-1 study/benchmark fabrications, fake arXiv IDs, and entity-merges are flagged; real ones are clean.
- GATE-08 fact-base path is trusted, logged, and correct for both block_v1 and single-pass shapes.
- Emit-only: no DB write, no LLM call, no network call, no verdict computed (D-05).
- All GATE-01/04/05/08 tests pass importing the REAL module; existing suites stay green.
</success_criteria>

<output>
Create `.planning/phases/28-layer-1-deterministic-gate/28-01-SUMMARY.md` when done.
</output>
