---
phase: 30-sequencer-wiring-hold-action-activation-gate
plan: 03
type: execute
wave: 2
depends_on: [30-02]
files_modified:
  - docker/newsletter/newsletter_poller.py
autonomous: true
requirements: [WIRE-01, WIRE-02, WIRE-03, WIRE-04, WIRE-06]
must_haves:
  truths:
    - "The eval runs at the primary save point (save_newsletter) and the block_v1 A/B save point, only when enabled=true"
    - "A held_fabrication or held_voice verdict on the PRIMARY draft flips status='held' + do_not_publish=true + reason ONLY when enforce=true; otherwise it surfaces a would-have-held alert with no flip"
    - "A passed verdict flips nothing — the edition proceeds to the unchanged Monday human gate (no auto-publish)"
    - "The block_v1 A/B eval is telemetry-only — it writes edition_evals rows but never flips publish state"
    - "do_not_publish has exactly one canonical home: the A/B row's data_snapshot.do_not_publish JSONB flag is moved to the top-level column"
  artifacts:
    - path: "docker/newsletter/newsletter_poller.py"
      provides: "primary + block_v1 eval invocations, enforce-gated status flip, escalation alert, D-02 reconciliation"
      contains: "run_edition_eval("
  key_links:
    - from: "docker/newsletter/newsletter_poller.py:save_newsletter"
      to: "run_edition_eval + newsletters.status/do_not_publish"
      via: "enforce-gated UPDATE on the primary row_id"
      pattern: "do_not_publish"
---

<objective>
Wire the Plan 30-02 orchestrator into the two generation save points and act on the returned verdict — the invasive, consequential step, all gated behind `enabled` (invocation) and `enforce` (auto-hold), so it ships dormant and rollback-safe (D-15/D-03).

1. PRIMARY draft (`save_newsletter`, the single shared insert reached by both single-pass and block-primary paths, D-13): after the existing Phase-D block, if `enabled` → build the governed client + one real `httpx.Client`, fetch the prior published edition, call `run_edition_eval` on the primary draft's fact base, then act on the verdict — `held_fabrication`/`held_voice` → operator escalation alert AND (only when `enforce=true`) flip the primary `row_id` to `status='held'` + `do_not_publish=true` + `do_not_publish_reason`; `passed` → no flip (unchanged Monday human gate, WIRE-04); `escalated` → already alerted by the orchestrator, no flip (fail-open, D-12). Under `enforce=false` (report-only) a held verdict surfaces a "would-have-held" alert with NO status flip (D-15).
2. block_v1 A/B draft (`bp_row` insert, ~line 2700): reconcile `data_snapshot.do_not_publish` → the new top-level `do_not_publish` column (D-02, one canonical home) and run `run_edition_eval` on the `blocks_data` fact base as TELEMETRY-ONLY — persist edition_evals rows, never flip publish state (the A/B row is already always-held, D-14).

Purpose: the verdict actually holds a fabricating/voice-failing edition (when armed), and both drafts are fully evaluated for A/B trend completeness — with the human gate untouched on a pass.
Output: the two wired save points + one canonical do_not_publish home.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-CONTEXT.md
@.planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-PATTERNS.md
@.planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the PRIMARY draft eval + enforce-gated hold action into save_newsletter</name>
  <files>docker/newsletter/newsletter_poller.py</files>
  <read_first>
    - docker/newsletter/newsletter_poller.py lines 1706-1812 (save_newsletter: the row insert at 1720 binding `row_id`; the Phase-D block 1726-1793 building `verification_input` by fact-base branch and the write-back UPDATE at 1778-1780; the `return row_id` at 1812)
    - docker/newsletter/newsletter_poller.py lines 2344-2371 (process_task: `input_data['narrative_context']` is the load_edition_context output = the run_layer2 `prior_context`)
    - docker/newsletter/newsletter_poller.py (the Plan 30-02 helpers: `_read_edition_eval_config`, `_build_eval_llm_client`, `_alert_operator`, `_fetch_prior_published_edition`, `run_edition_eval`)
    - .planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-PATTERNS.md (Analog A fact-base branch, Analog C row_id capture, Analog D status-flip UPDATE, Analog F httpx.Client injection)
  </read_first>
  <action>
    Inside `save_newsletter`, immediately AFTER the Phase-D verification block (after line ~1793, before the local-file writes), add the primary eval. Read `cfg = _read_edition_eval_config()`; if `not cfg.get('enabled', False)` do nothing (rollback-safe, D-15/D-03) and fall through. When enabled: reuse the fact-base already selected by the Phase-D branch — bind `fact_base = verification_input` and `pipeline_version = 'block_v1' if (blocks_data and blocks_data.get('blocks')) else 'single_pass'` (D-13: this is whatever primary draft save_newsletter inserted). Build `draft` as the dict `{'title': title, 'title_impact': title_impact, 'content_markdown': content_markdown, 'content_markdown_impact': content_markdown_impact, 'pipeline_version': pipeline_version}`. Bind `prior_context = input_data.get('narrative_context') or {}`; `prior_edition = _fetch_prior_published_edition()`; `llm_client = _build_eval_llm_client()`. Open ONE `with httpx.Client(timeout=15.0) as hc:` and call `run_edition_eval(supabase, draft, fact_base, prior_context, prior_edition, pipeline_version=pipeline_version, newsletter_id=row_id, edition=edition, config=cfg, llm_client=llm_client, http_client=hc, github_token=os.getenv('GITHUB_TOKEN'))`, binding the result to `verdict_obj` — the SAME `hc` reaches both eval modules (D-08). Then act on `verdict_obj['verdict']` on the PRIMARY `row_id` (D-09/D-10/D-11/D-13): when the verdict is `held_fabrication` or `held_voice`, build an operator escalation message from labels/counts only (edition number, verdict, and `verdict_obj['reason']` which already carries fabrication categories/counts for D-09 or the failing judge dimensions + feedback for held_voice per D-10 — never raw draft prose); if `cfg.get('enforce', False)` is true → call `_alert_operator` with an "[EVAL HELD]" prefix AND issue `supabase.table('newsletters').update({'status':'held','do_not_publish':True,'do_not_publish_reason':verdict_obj['reason']}).eq('id', row_id).execute()`; else (report-only) → call `_alert_operator` with an "[EVAL would-have-held]" prefix and perform NO status flip (D-15). When the verdict is `passed` → do nothing (unchanged Monday human gate, WIRE-04). When the verdict is `escalated` → do nothing here (run_edition_eval already alerted the operator, D-12). Wrap this whole primary-eval block in its own try/except that logs an ERROR and continues (an eval/action failure must never break the save path — the row is already inserted). Skip the flip when `row_id is None` (insert failed). Keep the existing `return row_id`. Syntax-check with ast before finishing.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/newsletter/newsletter_poller.py').read())" && grep -q "run_edition_eval(" docker/newsletter/newsletter_poller.py && grep -q "with httpx.Client(timeout=15.0) as hc" docker/newsletter/newsletter_poller.py && grep -q "'do_not_publish': True" docker/newsletter/newsletter_poller.py && grep -q "get('enforce'" docker/newsletter/newsletter_poller.py && python3 -m pytest tests/test_30_orchestration.py -q && echo GATE-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('docker/newsletter/newsletter_poller.py').read())"` exits 0.
    - `save_newsletter` calls `run_edition_eval(` after the Phase-D block, inside a `with httpx.Client(timeout=15.0) as hc:` passing `http_client=hc` (D-08 live re-check active).
    - The status flip `update({'status':'held','do_not_publish':True,'do_not_publish_reason':...})` is nested under an `if cfg.get('enforce'...)` branch — report-only (`enforce=false`) performs NO flip (D-15).
    - The `passed` verdict path performs no UPDATE (WIRE-04, never auto-publish).
    - The primary eval is guarded by `cfg.get('enabled', ...)` (rollback-safe, D-03/D-15) and the whole block is in a try/except-continue.
    - `pytest tests/test_30_orchestration.py -q` still green (helper unchanged).
  </acceptance_criteria>
  <done>The primary draft is evaluated at save time; a held verdict escalates + (when armed) holds via do_not_publish; a pass leaves the human gate untouched; all gated by enabled/enforce.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the A/B do_not_publish home + telemetry-only block_v1 eval (D-02/D-14)</name>
  <files>docker/newsletter/newsletter_poller.py</files>
  <read_first>
    - docker/newsletter/newsletter_poller.py lines 2643-2705 (the A/B block path: `ab_verification_input` fact base, the `bp_row` build with `data_snapshot.do_not_publish=True` at ~2690, the `insert(bp_row).execute()` at ~2700)
    - .planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-PATTERNS.md (Analog H: D-02 reconciliation target + D-14 telemetry-only rule)
  </read_first>
  <action>
    In the A/B block path: (a) D-02 reconciliation — move `do_not_publish` OUT of `bp_row['data_snapshot']` and set it as a TOP-LEVEL `bp_row` key `"do_not_publish": True` (the new migration-046 column, the single canonical home). Delete the `"do_not_publish": True` entry inside the `data_snapshot` dict so it is not a second source of truth; keep `ab_comparison` / `pipeline_version` / `angle` / etc. in `data_snapshot`. The bp_row stays `status='held'` — its hold state is unchanged; only its HOME moves. (b) D-14 telemetry-only eval — change the insert to capture the id: `insert_res = supabase.table('newsletters').insert(bp_row).execute()` then `bp_row_id = insert_res.data[0]['id'] if insert_res.data else None`. Guarded by `_read_edition_eval_config().get('enabled', False)` and `bp_row_id is not None`, build `bp_draft` as the dict `{'title': bp_row['title'], 'title_impact': bp_row['title_impact'], 'content_markdown': bp_row['content_markdown'], 'content_markdown_impact': bp_row['content_markdown_impact'], 'pipeline_version': 'block_v1'}`, reuse `ab_verification_input` as the fact base, open ONE `with httpx.Client(timeout=15.0) as hc:` and call `run_edition_eval(supabase, bp_draft, ab_verification_input, input_data.get('narrative_context') or {}, _fetch_prior_published_edition(), pipeline_version='block_v1', newsletter_id=bp_row_id, edition=edition, config=_read_edition_eval_config(), llm_client=_build_eval_llm_client(), http_client=hc, github_token=os.getenv('GITHUB_TOKEN'))`. IGNORE the returned verdict entirely for publish state — NO status flip and NO would-have-held alert on the block row (it is an always-held shadow; telemetry rows only, D-14). This runs inside the existing A/B try/except-continue block (non-blocking, already at ~2704). Syntax-check with ast.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/newsletter/newsletter_poller.py').read())" && grep -q '"do_not_publish": True' docker/newsletter/newsletter_poller.py && [ "$(grep -c 'run_edition_eval(' docker/newsletter/newsletter_poller.py)" -ge 2 ] && grep -q "bp_row_id" docker/newsletter/newsletter_poller.py && python3 -m pytest tests/test_30_orchestration.py -q && echo GATE-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('docker/newsletter/newsletter_poller.py').read())"` exits 0.
    - `bp_row` has a TOP-LEVEL `"do_not_publish": True` key and NO `do_not_publish` key remaining inside `data_snapshot` (grep the bp_row/data_snapshot region — one canonical home, D-02).
    - `run_edition_eval(` is called at least twice in the file (primary in Task 1 + the block telemetry call here).
    - The block-path `run_edition_eval` return value is not used to flip status and there is no `_alert_operator` call in the block path (telemetry-only, D-14).
    - `bp_row_id` is captured from the insert result and passed as `newsletter_id=` to the telemetry eval.
    - `pytest tests/test_30_orchestration.py -q` still green.
  </acceptance_criteria>
  <done>do_not_publish has one canonical home (the column) and both drafts are fully evaluated; the block eval is telemetry-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval verdict → newsletters.status/do_not_publish UPDATE | Untrusted verdict flips publish state; gated by enforce, only on the primary row |
| verdict reason/feedback → operator alert text | Built from draft/verdict data (untrusted for log-injection) |
| block_v1 shadow row → publish state | Telemetry-only; must never influence what ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-HOLD | Elevation of Privilege | enforce-gated status flip | mitigate | The `status='held'`+`do_not_publish=true` UPDATE fires only inside the `enforce=true` branch and only on the primary `row_id` (D-13/D-15); report-only surfaces a would-have-held alert with no flip |
| T-30-AUTOPUB | Elevation of Privilege | pass verdict | mitigate | A `passed` verdict performs NO UPDATE — the edition proceeds to the unchanged Monday human gate; the eval never auto-publishes (WIRE-04) |
| T-30-DRIFT | Tampering | do_not_publish dual-home | mitigate | The A/B JSONB flag is moved to the top-level column (D-02); grep proves no `do_not_publish` remains inside `data_snapshot` — one canonical home, no read/write drift |
| T-30-LOG | Information disclosure / log-injection | escalation alert text | mitigate | Alert + do_not_publish_reason built from labels/counts/failing-dimension names plus, for held_voice, per-dimension scores + a single-line bounded `judge_feedback` excerpt (judge critique output, NOT raw draft prose) |
| T-30-SHADOW | Elevation of Privilege | block_v1 telemetry eval | mitigate | The block eval's verdict is discarded for publish state; no status flip / no alert on the shadow row (D-14) |
| T-30-SC | Tampering | package installs | accept | No npm/pip/cargo installs this phase — all modules pre-built; httpx already imported. |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('docker/newsletter/newsletter_poller.py').read())"` exits 0.
- `run_edition_eval(` appears ≥2× (primary + block telemetry); both inside `with httpx.Client(...)` (D-08).
- The held-status UPDATE is nested under `enforce`; a pass performs no UPDATE (WIRE-04).
- `do_not_publish` is top-level on bp_row and absent from data_snapshot (D-02).
- `pytest tests/test_30_orchestration.py -q` green (no helper regression).
</verification>

<success_criteria>
- The eval is invoked at both save points (WIRE-01) and, when armed, holds a fabricating/voice-failing edition (WIRE-02/WIRE-03).
- A pass never auto-publishes (WIRE-04); auto-hold is gated by enforce, invocation by enabled (WIRE-06).
- Hold state has one canonical home (D-02); both drafts fully evaluated, block telemetry-only (D-14).
</success_criteria>

<output>
Create `.planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-03-SUMMARY.md` when done.
</output>
