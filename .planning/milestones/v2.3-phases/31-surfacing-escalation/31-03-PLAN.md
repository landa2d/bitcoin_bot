---
phase: 31-surfacing-escalation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/gato_brain/gato_brain.py
  - docker/gato/inject-gato-brain.mjs
  - tests/test_31_newsletter_eval_handler.py
autonomous: true
requirements: [SURF-03]
must_haves:
  truths:
    - "/newsletter_eval (no args) renders the latest newsletter that HAS edition_evals rows (any status); if none exist anywhere it says so explicitly (D-09)"
    - "/newsletter_eval <edition#> renders that specific edition's eval (D-09)"
    - "The main view shows per-dimension score lines for ALL dims; for FAILING dims it adds the judge's quoted evidence + before/after exemplar excerpt-bounded to ~300 chars each; passing dims stay score-only; mechanical Layer-1 flags are listed even on a passed verdict (D-10, P29 D-12)"
    - "/newsletter_eval trend renders the last ~8 editions, one line each: edition #, pipeline version, final verdict, attempts used, flag counts (D-11, mirrors read_eval_trend)"
    - "The whole handler is owner-gated on access_tier == 'owner' — it quotes pre-publication draft prose that must not leak to a non-owner chat (D-12)"
    - "All edition_evals reads are local .eq()-only selects (no .in_(); the /newsletter_preview handler's .in_() is the anti-pattern)"
    - "/newsletter_eval matches a NEW isNewsletterEval regex in inject-gato-brain.mjs and is OR'd into isGatoBrainCommand so it is not a dead command over Telegram"
  artifacts:
    - path: "docker/gato_brain/gato_brain.py"
      provides: "/newsletter_eval direct-dispatch handler (owner-gated) + local .eq()-only readers + detail/trend formatters"
      contains: "startswith(\"/newsletter_eval\")"
    - path: "docker/gato/inject-gato-brain.mjs"
      provides: "isNewsletterEval regex added to the isGatoBrainCommand allowlist"
      contains: "isNewsletterEval"
    - path: "tests/test_31_newsletter_eval_handler.py"
      provides: "handler coverage: owner-gate refusal, no-args latest-with-rows, edition# arg, trend, D-10 failing-dim evidence bounded, no-eval-anywhere message"
      contains: "def test_"
  key_links:
    - from: "gato_brain /chat dispatch"
      to: "/newsletter_eval handler"
      via: "startswith prefix before the intent router, access_tier threaded"
      pattern: "startswith\\(\"/newsletter_eval\"\\)"
    - from: "inject-gato-brain.mjs isGatoBrainCommand"
      to: "gato_brain /chat"
      via: "isNewsletterEval OR-chain forwarding"
      pattern: "isNewsletterEval"
---

<objective>
SURF-03: add a live `/newsletter_eval` (+ `trend`) Gato command. A direct-dispatch handler in `gato_brain` (owner-gated, before the intent router) renders the current edition's eval — per-dimension scores for all dims plus bounded evidence + before/after voice exemplars for FAILING dims — and `/newsletter_eval trend` renders recent editions' verdicts. The command is added to the `isGatoBrainCommand` allowlist in `inject-gato-brain.mjs` (the gato rebuild that makes it live is plan 31-04, worktree-unsafe/orchestrator-owned) so it is not a dead command over Telegram (the Phase 9 `/map-*` lesson).

Purpose: give the operator an on-demand deep view of any edition's eval, including the mechanical Layer-1 flags on a passed verdict (P29 D-12). Because the view quotes unpublished draft prose (judge evidence + exemplars), it is owner-gated (D-12) — the autonomy boundary applied to a read surface.
Output: the `/newsletter_eval` handler + local `.eq()`-only readers + detail/trend formatters in `gato_brain.py`; the `isNewsletterEval` allowlist entry in `inject-gato-brain.mjs`; a unit test suite. The deploy + live Telegram round-trip is plan 31-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-surfacing-escalation/31-CONTEXT.md
@.planning/phases/31-surfacing-escalation/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the /newsletter_eval owner-gated handler + local .eq()-only readers + detail/trend formatters</name>
  <read_first>
    - docker/gato_brain/gato_brain.py (READ the `/chat` dispatch chain ~:2600-2760: `access_tier` computed at :2608-2609; the `/newsletter_preview` direct-dispatch shape at :2708-2734 to COPY — but its `.in_()` at :2712 is the ANTI-pattern; the `/map-` branch at :2750 showing `access_tier` threaded into `handle_map_command`; the module-global `supabase` client)
    - docker/gato_brain/gato_brain.py (READ `handle_map_command` :2366-2406 and the per-verb gate `if access_tier != "owner":` at ~:2037 — the owner-gating pattern to apply to the WHOLE handler, D-12)
    - docker/newsletter/edition_eval.py (READ `read_evals_by_newsletter` :186-195 and `read_eval_trend` :198-217 — the .eq()-only semantics to MIRROR locally; NOT importable across containers)
    - docker/newsletter/judge_loop.py (READ ~:55-171 — the `judge_scores` shape `{ "technical": {dim: {"score","evidence","exemplar_before","exemplar_after"}}, "impact": {...} }`, the five `_DIMENSIONS`, the fail-below thresholds continuity=4/clickbait=3/repeated_subtopics=3/specificity=3/hedging + `hedging_filler_hits_max=3`, and the continuity=="n/a" exclusion — to decide which dims are FAILING for the bounded-evidence render, D-10)
    - supabase/migrations/045_edition_evals.sql (READ columns: `edition_number`, `pipeline_version`, `deterministic_flags` `{fabrication,unverified,mechanical,...}`, `verdict`, `attempt`, `layer`, `UNIQUE(newsletter_id,layer,attempt)`)
    - tests/test_09_gated_publishing.py (READ ~:56-130 — the harness that imports `gato_brain` in a bare env with stubs; template for the new test)
    - tests/test_27_edition_eval.py (READ ~:54-146 — the StubSupabase double whose `_StubQuery` lacks an in-list method, structurally proving `.eq()`-only)
  </read_first>
  <action>
    Add a new direct-dispatch branch in the `/chat` handler BEFORE the intent router, near the `/newsletter_preview` branch (~:2708): `if _msg_lower.startswith("/newsletter_eval"):`. OWNER-GATE the whole handler first — `if access_tier != "owner": return ChatResponse(response="/newsletter_eval is owner-only ...", session_id="", intent="NEWSLETTER_COMMAND", metadata={})` (D-12; thread `access_tier` exactly like the `/map-` branch). Parse args from `req.message`: `trend` token -> trend view; a bare integer token -> that `<edition#>`; no arg -> latest-with-rows. Add local module-level readers (mirroring `edition_eval.py`, `.eq()`-only, `supabase` as first param, `return result.data or []`): `_eval_read_by_edition(supabase, edition_number)` = `.eq("edition_number", N).order("pipeline_version").order("attempt")`; `_eval_read_latest_with_rows(supabase)` = read the most recent edition_evals row (`.select("edition_number").order("edition_number", desc=True).limit(1)`) then read that edition's rows (used by no-args, D-09); `_eval_read_trend(supabase, pipeline_version, limit=8)` = `.eq("pipeline_version", pv).order("edition_number", desc=True).limit(8)` mirroring `read_eval_trend` (D-11). NEVER `.in_()`. Add two pure formatters. `_format_eval_detail(eval_rows) -> str` (D-10): for the edition, render per-pipeline_version — the verdict, then a score line for EACH of the 5 dims (`dim: technical=<s> impact=<s>`); for a FAILING dim (min(technical,impact) score below the local threshold constant mirrored from judge_loop DEFAULT_CONFIG; continuity=="n/a" excluded) additionally render the judge's `evidence` and `exemplar_before`/`exemplar_after`, each bounded to ~300 chars (`[:300]`); list the deterministic `mechanical` flags even when the verdict is `passed` (P29 D-12); passing dims stay score-only. `_format_eval_trend(rows) -> str` (D-11): one line per edition — `#<edition> <pipeline_version> <verdict> attempts=<n> fab=<n>/unv=<n>/mech=<n>`. If `_eval_read_latest_with_rows` finds nothing anywhere, return an explicit "No eval has run for any edition yet." message (D-09). Return `ChatResponse(response=..., session_id="", intent="NEWSLETTER_COMMAND", metadata={})`; the downstream 4000-char splitter handles overflow. Wrap the handler body in try/except that logs a labeled ERROR and returns a human-readable "eval view failed: <e>" string (fail-loud, mirror `handle_map_command`). Log labels/counts only — never the raw evidence/exemplar prose or any key (T-30-LOG).
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())" && python3 -m pytest tests/test_31_newsletter_eval_handler.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'startswith("/newsletter_eval")' docker/gato_brain/gato_brain.py` returns a match placed BEFORE the intent-router dispatch (before the `/x-` / `/map-` branches or in the same pre-router block)
    - The handler returns an owner-only refusal when `access_tier != "owner"` (a test with a non-owner tier asserts the refusal string and that NO eval row read occurred)
    - `grep -n "def _eval_read_by_edition\|def _eval_read_latest_with_rows\|def _eval_read_trend" docker/gato_brain/gato_brain.py` shows all three readers; none contain `.in_(` (grep their regions)
    - Detail-formatter tests (owner tier, StubSupabase seeded rows): a `held_voice` edition renders per-dim score lines for all 5 dims AND, for a failing dim, `evidence`/`exemplar_before`/`exemplar_after` each truncated to <=300 chars; a `passed` edition renders score-only dims BUT still lists the `mechanical` flags (D-10/P29 D-12)
    - Trend-formatter test: `/newsletter_eval trend` renders <=8 lines each containing edition #, pipeline_version, verdict, attempts, and fab/unv/mech counts (D-11)
    - No-args with an empty edition_evals stub returns the explicit "No eval has run for any edition yet." message (D-09)
    - An edition# arg (`/newsletter_eval 103`) reads via `_eval_read_by_edition(supabase, 103)` `.eq("edition_number"` (test asserts the path; an accidental `.in_` would raise AttributeError against the stub)
  </acceptance_criteria>
  <done>The owner-gated `/newsletter_eval` handler with `.eq()`-only readers and D-09/D-10/D-11 detail+trend formatters exists and is proven by unit tests, including the owner-gate refusal and the mechanical-on-passed rule.</done>
</task>

<task type="auto">
  <name>Task 2: Allowlist /newsletter_eval in inject-gato-brain.mjs (new regex + OR-chain)</name>
  <read_first>
    - docker/gato/inject-gato-brain.mjs (READ :106-152 — the `isXCommand`/`isMapCommand`/`isNewsletterPreview` regexes at :107-111, the `isGatoBrainCommand` OR-chain at :112, the forward-to-gato-brain fetch, and the fall-through safety at :142-152; the help text block that lists `/newsletter_*` commands)
  </read_first>
  <action>
    Add a NEW regex line beside `isNewsletterPreview` (~:111): `const isNewsletterEval = text && /^\/newsletter_eval\b/i.test(text.trim());` — a distinct pattern is required because `/^\/newsletter_preview\b/` does NOT match `/newsletter_eval` (D-Discretion note). Add `|| isNewsletterEval` to the `isGatoBrainCommand` OR-chain at :112 so the command forwards to `gato_brain:8100/chat` instead of falling through to OpenClaw (the Phase 9 `/map-*` dead-command landmine). Optionally add a `/newsletter_eval — Eval scores & voice exemplars (owner)` line to the help text block that already lists the `/newsletter_*` commands. The fall-through safety at :142-152 (a gato-brain command that returns nothing gets a fixed reply, never falls through) already covers the new command — no change needed there. Do NOT run the gato rebuild here — that is the worktree-unsafe, orchestrator-owned step in plan 31-04.
  </action>
  <verify>
    <automated>node --check docker/gato/inject-gato-brain.mjs 2>/dev/null || node -e "require('fs').readFileSync('docker/gato/inject-gato-brain.mjs','utf8')"; grep -c "isNewsletterEval" docker/gato/inject-gato-brain.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isNewsletterEval" docker/gato/inject-gato-brain.mjs` returns >= 2 (the regex declaration + the OR-chain use)
    - `grep -n "newsletter_eval" docker/gato/inject-gato-brain.mjs` shows the regex `/^\/newsletter_eval\b/i`
    - The `isGatoBrainCommand` line at ~:112 now includes `|| isNewsletterEval`
    - The file still parses (the injected template is valid JS — `grep` shows the OR-chain and no stray unmatched braces around the edit)
  </acceptance_criteria>
  <done>`/newsletter_eval` matches a dedicated `isNewsletterEval` regex and is OR'd into `isGatoBrainCommand`; the command will forward to gato_brain once gato is rebuilt in 31-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram user (any chat) → gato → gato_brain /chat | untrusted caller; /chat already requires the X-Gato-Secret HMAC |
| edition_evals (DB) → /newsletter_eval response | the view quotes UNPUBLISHED draft prose (judge evidence + exemplars) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Information disclosure / Elevation | /newsletter_eval reading pre-publication draft prose | mitigate | Owner-gate the WHOLE handler on `access_tier == "owner"` (D-12), returning an owner-only refusal to any non-owner BEFORE any eval read — proven by a non-owner-tier unit test |
| T-31-08 | Information disclosure | handler ERROR logs | mitigate | Labels/counts only in logs; never the evidence/exemplar prose or a key (T-30-LOG); evidence/exemplars appear ONLY in the owner-gated response, bounded to ~300 chars |
| T-31-09 | Denial of service | malformed `<edition#>` arg | mitigate | Integer-parse the arg with validation; a top-level try/except returns a human-readable failure string (mirror `handle_map_command`) rather than raising |
| T-31-10 | Tampering (silent failure) | edition_evals reads | mitigate | `.eq()`-only local readers; the `.in_()` anti-pattern (`/newsletter_preview` :2712) is not copied and is structurally excluded by the StubSupabase test |
| T-31-SC | Tampering | package installs / gato rebuild | accept | No npm/pip/cargo installs this plan; the gato rebuild (31-04) reuses the existing OpenClaw image + deps — no new dependency introduced; RESEARCH package-legitimacy gate N/A |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())"` passes.
- `python3 -m pytest tests/test_31_newsletter_eval_handler.py -q` green.
- `grep -c "isNewsletterEval" docker/gato/inject-gato-brain.mjs` >= 2.
- `python3 -m pytest tests/test_09_gated_publishing.py -q` still green (shared gato_brain import harness unaffected).
</verification>

<success_criteria>
- `/newsletter_eval` (no-args / `<edition#>` / `trend`) renders per D-09/D-10/D-11, owner-gated per D-12, `.eq()`-only.
- `isNewsletterEval` added to the `isGatoBrainCommand` allowlist.
- Deploy (gato_brain + gato scoped rebuild) and the live Telegram round-trip are DEFERRED to plan 31-04 (worktree-unsafe, orchestrator-owned).
</success_criteria>

<output>
Create `.planning/phases/31-surfacing-escalation/31-03-SUMMARY.md` when done.
</output>
