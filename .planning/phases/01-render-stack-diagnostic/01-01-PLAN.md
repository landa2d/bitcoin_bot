---
id: 01-01-render-stack-findings
title: Render-Stack Findings (describe-only diagnostic)
phase: 01-render-stack-diagnostic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md
  - .planning/docs/economy-map-build-spec-v2.md
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04]
tags: [diagnostic, docs-only, render-stack]

must_haves:
  truths:
    - "Findings report names the service/container/framework serving aiagentspulse.com and pinpoints the HTML emission point (DIAG-01)"
    - "Findings report documents how a new edition page reaches production end-to-end — file write path, cache invalidation if any, deploy trigger (DIAG-02)"
    - "Findings report contains an explicit reuse-vs-sibling-route recommendation with rationale (DIAG-03)"
    - "Build spec v2 §6 is annotated to reference the findings doc before any renderer work begins (DIAG-04)"
    - "Zero application code changes were made — only files under .planning/ touched (diagnostic-only confirmed)"
  artifacts:
    - path: ".planning/phases/01-render-stack-diagnostic/01-FINDINGS.md"
      provides: "Canonical Phase 1 findings document"
      contains: "## Stack / ## Publish Mechanism / ## Block-Page Publish Path Recommendation / ## Known Unknowns / ## Implications for Phase 4"
    - path: ".planning/docs/economy-map-build-spec-v2.md"
      provides: "Build spec §6 with by-reference annotation to 01-FINDINGS.md (per D-01, DIAG-04)"
      contains: "01-FINDINGS.md"
  key_links:
    - from: ".planning/phases/01-render-stack-diagnostic/01-FINDINGS.md"
      to: ".planning/docs/economy-map-build-spec-v2.md §6"
      via: "explicit citation in findings doc"
      pattern: "economy-map-build-spec-v2.md"
    - from: ".planning/docs/economy-map-build-spec-v2.md §6"
      to: ".planning/phases/01-render-stack-diagnostic/01-FINDINGS.md"
      via: "by-reference annotation appended to §6"
      pattern: "01-FINDINGS.md"
---

<objective>
Produce the canonical Phase 1 findings document (`01-FINDINGS.md`) describing how
`aiagentspulse.com` is served and how new pages reach production, then annotate
build spec v2 §6 to reference it. This is a **describe-only** diagnostic — no
application code changes, no live probes, no Supabase queries, no shell scripts
that hit the network. The findings are derived entirely from reading the
existing codebase artifacts under `docker/web/`, `scripts/deploy.sh`,
`supabase/migrations/`, and the planning docs.

Purpose: Phase 4 (Hub/Block/Status Renderer) cannot be planned in detail until
the publish path is named and the known unknowns are flagged. This plan closes
that gap and unblocks downstream phases — particularly Phase 4 (which assumes
publish-path reuse) and Phase 2 (which owns the `economy_map` schema and will
validate the anon-key / `Accept-Profile` path live).

Output: One findings document plus a by-reference annotation appended to build
spec v2 §6. Every locked decision in 01-CONTEXT.md (D-01 through D-07) is
honored. The deferred items (live probes, deep-link/SEO improvements,
prerendering) MUST NOT appear in the findings doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-render-stack-diagnostic/01-CONTEXT.md
@.planning/docs/economy-map-build-spec-v2.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the render-stack source artifacts (no writes yet)</name>
  <files>
    (read-only task — no files modified)
  </files>
  <read_first>
    - .planning/phases/01-render-stack-diagnostic/01-CONTEXT.md (locked decisions D-01..D-07; in particular D-03 forbids live probes and D-06 forbids file-diff sketches)
    - .planning/docs/economy-map-build-spec-v2.md §2 (Phase 0 prompt — "do not build anything") and §6 (Renderer contract this phase informs)
    - .planning/REQUIREMENTS.md (DIAG-01..04 — the four requirements this plan must satisfy)
    - .planning/ROADMAP.md (Phase 1 success criteria — five items)
    - docker/web/Dockerfile (Caddy 2-alpine base, COPY site/, entrypoint.sh)
    - docker/web/Caddyfile (domain binding aiagentspulse.com, SPA fallback try_files {path} /index.html, CSP `connect-src https://*.supabase.co`)
    - docker/web/entrypoint.sh (sed-based runtime injection of SUPABASE_URL / SUPABASE_ANON_KEY into /srv/app.js)
    - docker/web/site/app.js (314 lines — hash router getRoute()/route(), supabase-js client, list + reader views, hashchange listener)
    - docker/web/site/index.html (77 lines — shell; loads style-shared.css, supabase-js CDN, marked CDN, app.js)
    - scripts/deploy.sh (rsync-to-Hetzner, auto-detect docker/web/ changes, rebuild + restart web service)
    - .planning/codebase/INTEGRATIONS.md §"Deployment Target" (domain, Caddy reverse proxy, port mapping)
    - .planning/codebase/ARCHITECTURE.md §"User-Facing Layer" (Web position; Caddy static serve on port 443)
    - supabase/migrations/ (list the directory; confirm whether any eu_ai_act schema migration exists — finding to flag if absent)
  </read_first>
  <action>
    Read every file listed under read_first. Do NOT write or modify anything in this task. Do NOT run live probes (no curl, no supabase-py, no PostgREST requests). The goal is to internalize the exact text/behavior of each artifact so Tasks 2–6 can cite line ranges and exact directives precisely.

    Track these specific facts for use in later tasks (capture them mentally / via Read tool only, not in any new file):
    - The Dockerfile base image (`caddy:2-alpine`) and that `site/` is COPY'd into `/srv/`.
    - The exact Caddyfile directives: `root * /srv`, `file_server`, `encode gzip`, `try_files {path} /index.html`, the full `Content-Security-Policy` header value.
    - The two sed substitutions in `entrypoint.sh` (`__SUPABASE_URL__`, `__SUPABASE_ANON_KEY__` → real values from container env).
    - The hash routes already implemented in `app.js`: `#/edition/N` and `#/unsubscribe`; the `getRoute()` + `route()` + `window.addEventListener('hashchange', route)` pattern; the `sb = window.supabase.createClient(...)` client construction.
    - The supabase-js client is loaded from `https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2` (CDN, not bundled).
    - `scripts/deploy.sh` map_service rule `map_service 'docker/web/'  web` which triggers `docker compose build web && docker compose up -d web` over SSH.
    - Whether `supabase/migrations/` contains an `eu_ai_act` schema migration — if absent, the "follow the eu_ai_act pattern" guidance from PROJECT.md is a pattern-from-spec, not an in-tree precedent; flag this in §"Known Unknowns".
  </action>
  <verify>
    <automated>test -f /root/bitcoin_bot/docker/web/Caddyfile && test -f /root/bitcoin_bot/docker/web/site/app.js && test -f /root/bitcoin_bot/scripts/deploy.sh && test -f /root/bitcoin_bot/.planning/docs/economy-map-build-spec-v2.md && test -f /root/bitcoin_bot/.planning/phases/01-render-stack-diagnostic/01-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - All eleven read_first files exist and have been read in this task's turn.
    - No new files created; `ls .planning/phases/01-render-stack-diagnostic/` returns the same set as before the task (CONTEXT + DISCUSSION-LOG only).
    - `git status --porcelain` shows no new tracked or untracked files added by this task.
  </acceptance_criteria>
  <done>
    All source artifacts are in context. Executor has the exact text of the Caddyfile directives, the entrypoint sed lines, the app.js hash-router shape, the deploy.sh service map, and knows whether supabase/migrations/ contains an eu_ai_act precedent. No files have been written.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 01-FINDINGS.md scaffold and write §"Stack" (DIAG-01)</name>
  <files>.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md</files>
  <read_first>
    - .planning/phases/01-render-stack-diagnostic/01-CONTEXT.md (D-07: file lives here; "Claude's Discretion" allows mirroring build spec §6 structure)
    - .planning/docs/economy-map-build-spec-v2.md §6 (Renderer contract — the destination shape this findings doc informs)
    - .planning/REQUIREMENTS.md (DIAG-01 phrasing: service / container / framework / HTML emission point)
    - docker/web/Dockerfile, docker/web/Caddyfile, docker/web/entrypoint.sh, docker/web/site/index.html, docker/web/site/app.js (text being summarized)
  </read_first>
  <action>
    Create `.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` with the following top-of-file structure (use the Write tool, not heredoc):

    1. Top matter (front matter is optional; if used, keep it minimal: `phase: 01-render-stack-diagnostic`, `requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04]`, `status: complete`).
    2. Title: `# Phase 1 — Render-Stack Findings`.
    3. A 2–4 line opening paragraph stating: (a) this is the canonical Phase 1 findings doc, (b) it informs build spec v2 §6 by reference (per D-01), (c) it is **describe-only** — no live probes were run.
    4. Section `## 1. Stack (DIAG-01)` covering all of the following, with exact file-path citations:
       - **Service / container:** the `web` service in `docker/docker-compose.yml`, built from `docker/web/Dockerfile`. Base image `caddy:2-alpine`. The static site is baked into the image at build time via `COPY site/ /srv/` — quote the COPY line.
       - **Framework / web server:** Caddy 2 (alpine). Quote the relevant `docker/web/Caddyfile` directives: `root * /srv`, `file_server`, `encode gzip`, and the SPA fallback `try_files {path} /index.html`. Note the domain binding (`aiagentspulse.com {...}`) and the internal `:8080 /health` endpoint.
       - **HTML emission point:** the single shell `docker/web/site/index.html` (77 lines) is served by `file_server`. All page content is rendered into the DOM by `docker/web/site/app.js` after page load via DOM manipulation (e.g., `document.getElementById('newsletter-list').innerHTML = ...`). This is a **client-rendered SPA** — there is no server-rendered HTML emission. The `<script src="/app.js">` tag at the bottom of `index.html` is the entry point.
       - **Runtime config injection:** `docker/web/entrypoint.sh` performs two `sed` substitutions on `/srv/app.js` at container start: `__SUPABASE_URL__` and `__SUPABASE_ANON_KEY__` are replaced with the values from container env. After substitution, `caddy run` starts the server. Quote the two sed lines.
       - **Client-side data path:** the SPA loads `https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2` from CDN and constructs an anon-key client via `sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY)`. All page data is queried from Supabase at view-render time (e.g., `sb.from('newsletters').select(...)`).
       - **Routing model:** `docker/web/site/app.js` implements a hash router: `getRoute()` parses `window.location.hash`, matches `#/edition/<N>` and `#/unsubscribe`, defaults to the list view; `window.addEventListener('hashchange', route)` re-dispatches on hash changes. Caddy's `try_files {path} /index.html` makes deep hash routes deliver `index.html` unconditionally — the hash itself is client-only and never reaches Caddy.

    Do NOT include implementation plans for Phase 4 in this section (that belongs to §5 per D-06). Do NOT include live-probe results (D-03). Cite line ranges (e.g., "Caddyfile lines 6–20") where useful so future readers can find the source.

    Keep section concise — 25–60 lines of prose+code-blocks total for §1.
  </action>
  <verify>
    <automated>test -f /root/bitcoin_bot/.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md &amp;&amp; grep -c '^## 1. Stack' /root/bitcoin_bot/.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` exists.
    - Contains the heading `# Phase 1 — Render-Stack Findings` (exact wording).
    - Contains the heading `## 1. Stack (DIAG-01)` (exact wording, including the requirement-ID parenthetical).
    - `grep -c 'docker/web/Dockerfile' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1.
    - `grep -c 'docker/web/Caddyfile' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1.
    - `grep -c 'docker/web/entrypoint.sh' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1.
    - `grep -c 'docker/web/site/app.js' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1.
    - `grep -c 'try_files {path} /index.html' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1 (quotes the SPA fallback directive).
    - `grep -c 'caddy:2-alpine' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1.
    - `grep -c 'supabase-js' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1.
    - `grep -cE 'createClient' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1.
    - Section contains the word "SPA" or "client-rendered" or "client-side" to make the rendering model explicit.
  </acceptance_criteria>
  <done>
    Findings doc exists with §1 covering service, container, framework, HTML emission point, runtime config injection, client-side data path, and hash-router routing model — all with exact file-path citations and quoted directives where relevant. No Phase 4 implementation prescriptions in this section.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write 01-FINDINGS.md §"Publish Mechanism" (DIAG-02)</name>
  <files>.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md</files>
  <read_first>
    - .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md (the in-progress doc — read to confirm §1 is intact before appending)
    - .planning/phases/01-render-stack-diagnostic/01-CONTEXT.md §"Specific Ideas" (the "no publish-page step" point — most important conceptual finding) and D-05 (publish-path = DB rows → SPA reads on next load)
    - .planning/REQUIREMENTS.md (DIAG-02 phrasing: file write path, cache invalidation if any, deploy trigger)
    - docker/web/site/app.js (`loadList()` calls `sb.from('newsletters').select('*').in('status', ['published', 'preview']).order('edition_number', { ascending: false })` — this is the entire "publish discovery" path; `loadEdition(N)` is the per-edition read)
    - scripts/deploy.sh (lines 65–141: map_service 'docker/web/' → web; `docker compose build web && docker compose up -d web` over SSH)
    - docker/web/Caddyfile (no cache directives — confirm absence)
    - docker/web/Dockerfile (site is baked at image build time — only the shell/app.js change with a rebuild; content is always live from Supabase)
  </read_first>
  <action>
    Append section `## 2. Publish Mechanism (DIAG-02)` to `01-FINDINGS.md`. The section must cover the end-to-end path a "new edition page" travels to production. State explicitly:

    - **There is no per-page publish step.** This is the most important conceptual finding (per CONTEXT.md §"Specific Ideas"). New edition "pages" are not files emitted to disk; they are **rows inserted/updated in Supabase `newsletters` table with `status='published'`**. The SPA reads `sb.from('newsletters').select('*').in('status', ['published', 'preview']).order('edition_number', { ascending: false })` on every page load and renders whatever it finds. Quote the relevant lines from `loadList()` and `loadEdition()` in `app.js`.
    - **File write path:** none. The act of publishing an edition is a DB write performed by the newsletter pipeline (`docker/newsletter/`), not a write to `docker/web/site/`. The static SPA shell (`index.html`, `app.js`, `style-shared.css`) only changes when SPA code itself is modified.
    - **Cache invalidation:** none required by the SPA pattern. The Caddyfile contains no cache-control directives; the client fetches Supabase data on every navigation. (Note: Supabase + CDN caching at the network layer exists but is out-of-scope here.)
    - **Deploy trigger (for SPA shell changes):** `scripts/deploy.sh` detects modifications under `docker/web/` via its `map_service 'docker/web/' web` rule, then SSHes to the Hetzner host and runs `docker compose build web && docker compose up -d web`. This rebuilds the Caddy image with the new `site/` baked in and restarts the container. **This deploy trigger is only relevant when the SPA shell itself changes — adding new editions to the live site requires zero deploy actions.**
    - **Implication for Phase 9 (`/map-approve`):** the autonomy-boundary "publish" action for a block body is not a file write or container rebuild; it is the atomic Postgres transaction defined in build spec v2 §3.2 (flip `block_body_versions.status` to `published`, supersede prior, update `blocks.current_body_version_id`). The next SPA page load reads the new state. This is the same pattern as edition publishing — note this explicitly.

    Keep section to 25–50 lines.
  </action>
  <verify>
    <automated>grep -c '^## 2. Publish Mechanism' /root/bitcoin_bot/.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File contains the heading `## 2. Publish Mechanism (DIAG-02)`.
    - `grep -cE '(no per-page publish|no publish step|no traditional publish|no file write|no per-page publish step)' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1 — the "no publish-page step" finding is stated explicitly.
    - `grep -c 'scripts/deploy.sh' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1.
    - `grep -cE 'docker compose (build|up).*web' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1.
    - `grep -cE 'newsletters' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1 (the DB table is named).
    - Section references either Phase 9 or `/map-approve` to make the atomic-publish implication explicit (so future Phase 9 planners can find it).
    - Previous heading `## 1. Stack (DIAG-01)` still present (`grep -c '^## 1. Stack' …` returns 1).
  </acceptance_criteria>
  <done>
    §2 documents the end-to-end publish path with the explicit finding that there is no per-page publish step; the file write path is "none — DB writes only"; cache invalidation is "none"; and the deploy trigger is named with its exact docker compose commands. The atomic-publish implication for Phase 9 is flagged.
  </done>
</task>

<task type="auto">
  <name>Task 4: Write 01-FINDINGS.md §"Block-Page Publish Path Recommendation" (DIAG-03)</name>
  <files>.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md</files>
  <read_first>
    - .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md (in-progress; §1 and §2 must remain intact)
    - .planning/phases/01-render-stack-diagnostic/01-CONTEXT.md D-05 (the bare recommendation: existing path is fully reusable via new hash routes in `docker/web/site/app.js`) and D-06 (do NOT include Phase 4 file-diff sketch — name the path, flag the known unknowns)
    - .planning/REQUIREMENTS.md (DIAG-03 — preferred reuse vs. sibling-route call, with rationale)
    - docker/web/Caddyfile (`try_files {path} /index.html` means deep hash routes work out of the box)
    - docker/web/site/app.js (existing hash-router pattern: adding `#/map`, `#/map/<slug>`, `#/status` is a localized extension)
  </read_first>
  <action>
    Append section `## 3. Block-Page Publish Path Recommendation (DIAG-03)` to `01-FINDINGS.md`. The section must:

    1. State the **bare recommendation** up front (one or two sentences): **the existing publish path is fully reusable** for block, hub, and status pages. No sibling route is needed.
    2. Name the **path** (per D-06 — name the path, do NOT prescribe Phase 4's implementation):
       - New hash routes added to the existing `docker/web/site/app.js` hash router: `#/map`, `#/map/<slug>`, `#/status` (plus whatever Phase 4 chooses for the hub URL — `#/map` is the spec's primary).
       - Queries against the `economy_map` schema via the same `supabase-js` client already wired (`sb`), using its `.schema('economy_map')` method (which sets `Accept-Profile: economy_map` on the underlying PostgREST request — this is the supabase-js equivalent of the direct-PostgREST pattern mandated for backend services).
       - **No Caddy changes**: `try_files {path} /index.html` already routes every URL to the SPA shell; hash routes are processed client-side. Quote the directive.
       - **No new container**: the existing `web` service serves the new pages.
       - **No new deploy path**: `scripts/deploy.sh`'s existing `map_service 'docker/web/' web` rule handles SPA-shell changes when Phase 4 modifies `app.js` / `index.html`.
    3. State the **rationale**:
       - The SPA-only pattern means the "publish step" is already a DB write (per §2). Block bodies become rows in `economy_map.block_body_versions`; the SPA reads them on the next load. This is identical to how editions work — same architectural shape, different table.
       - A sibling route (e.g., a separate Caddy site, a server-rendered service, or static-file generation) would introduce new infra without solving anything the SPA pattern doesn't already solve.
       - Substance over design (per build spec §8) — v1 does not need crawlable URLs / SEO / SSR. v2 design pass can revisit.
    4. **Explicitly out of scope for this recommendation** (per D-06): a Phase 4 file-diff sketch, exact code changes to `app.js`, new HTML container div IDs, CSS structure. State that Phase 4's `discuss-phase` and `plan-phase` own those decisions.

    Keep section to 25–40 lines. The recommendation is short by design.
  </action>
  <verify>
    <automated>grep -c '^## 3. Block-Page Publish Path Recommendation' /root/bitcoin_bot/.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File contains heading `## 3. Block-Page Publish Path Recommendation (DIAG-03)`.
    - Section contains a clear "reuse the existing publish path" statement (`grep -cE 'reuse|reusable' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1 within the doc; the recommendation wording is unambiguous).
    - Section names the three new hash routes — `grep -c '#/map' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1 AND `grep -c '#/status' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1.
    - Section references `Accept-Profile` or `.schema('economy_map')` so the schema-isolation mechanism is named (`grep -cE "Accept-Profile|\\.schema\\('economy_map'\\)" .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1).
    - Section states "no sibling route" / "no new Caddy route" / "no new container" or equivalent — making the negative recommendation explicit.
    - Section does NOT contain a code-block diff of `app.js` (per D-06): `grep -cE '^\+|^-' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md | head -1` does not show diff-style lines outside other contexts. Reviewer-judgable: the section names the path, does not sketch Phase 4 implementation.
    - Previous headings (`## 1. Stack`, `## 2. Publish Mechanism`) still present.
  </acceptance_criteria>
  <done>
    §3 makes the explicit reuse recommendation with named path components (hash routes, `.schema('economy_map')` via supabase-js, no Caddy changes, no new container, no deploy-path changes) and rationale — without sketching Phase 4's implementation.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write 01-FINDINGS.md §"Known Unknowns" (flag, do not validate)</name>
  <files>.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md</files>
  <read_first>
    - .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md (in-progress)
    - .planning/phases/01-render-stack-diagnostic/01-CONTEXT.md D-04 (the three known unknowns: anon-role read of non-public schema via Accept-Profile; CSP coverage for schema-isolated PostgREST; hash-route deep-link/SEO behavior) and D-03 ("describe-only — Phase 2 validates the read path live")
    - .planning/phases/01-render-stack-diagnostic/01-CONTEXT.md §"Specific Ideas" (Supabase exposed-schemas allowlist is a Phase 2 prerequisite, not a Phase 1 blocker)
    - .planning/phases/01-render-stack-diagnostic/01-CONTEXT.md §"Deferred Ideas" (live probes deferred to Phase 2; deep-link/SEO out of v1; static-route prerendering out of scope)
    - docker/web/Caddyfile (CSP value: `connect-src https://*.supabase.co` — likely covers schema-isolated PostgREST because same host, but unverified)
    - supabase/migrations/ (whether an `eu_ai_act` migration exists in-tree — if not, "follow eu_ai_act pattern" is pattern-from-spec, not in-tree precedent; flag this)
  </read_first>
  <action>
    Append section `## 4. Known Unknowns (flagged for Phase 2)` to `01-FINDINGS.md`. The section MUST list the three items from D-04 plus the additional finding about `eu_ai_act` precedent and the Supabase exposed-schemas allowlist prerequisite. Each item:
    - Names the unknown precisely (one short sentence).
    - States what would resolve it (one line) — Phase 2 owns this.
    - States the current best guess and rationale (one or two lines).
    - Is explicitly marked **Phase 2 will validate** (or, for the allowlist item, **Phase 2 prerequisite**).

    The four unknowns to list (use these subsection headings exactly):

    `### 4.1 Anon-role read of non-public schema via Accept-Profile`
    Whether the Supabase anon-role API key can read tables in a non-`public` schema (e.g., `economy_map`) when the supabase-js client calls `.schema('economy_map').from(...)` (which sets `Accept-Profile: economy_map` on the PostgREST request). The spec asserts this works because `eu_ai_act` uses the same pattern, but live behavior with the browser anon key is unverified. **Phase 2 will validate** with the real schema and seed data.

    `### 4.2 Caddy CSP coverage for schema-isolated PostgREST`
    Whether the existing CSP `connect-src https://*.supabase.co` (quote the directive verbatim) covers PostgREST calls to the schema-isolated endpoint. Likely yes — supabase-js routes through the same `*.supabase.co` host regardless of `Accept-Profile` — but unverified from the browser context. **Phase 2 will validate** when block-page renderer probes are run.

    `### 4.3 Hash-route deep-link / SEO behavior for #/map/<slug>`
    Hash-routed deep links (e.g., `aiagentspulse.com/#/map/payments-settlement`) work for the SPA but are not crawlable by search engines or rich-link previews (the hash never reaches the server). Acceptable for v1 per build spec §8 (substance over design). **Flagged for Phase 4** so the renderer planner is aware; **out of v1 scope** per the deferred-ideas section of CONTEXT.md — revisit only if a v2 design pass requires it.

    `### 4.4 In-tree precedent for the eu_ai_act isolation pattern`
    The build spec, PROJECT.md, and CONTEXT.md all reference `eu_ai_act` as the precedent for schema isolation via `Accept-Profile`. A scan of `supabase/migrations/` (001–032 as of this writing) shows **no `eu_ai_act` migration in this repo**. The pattern exists in spec form (CLAUDE.md / PROJECT.md), but Phase 2 will be the first time it lands as actual SQL in `supabase/migrations/`. **Phase 2 will establish the in-tree precedent** alongside `economy_map`.

    Add a final subsection `### 4.5 Supabase exposed-schemas allowlist (Phase 2 prerequisite)`:
    Per CONTEXT.md §"Specific Ideas", a non-`public` schema must be on Supabase's PostgREST exposed-schemas allowlist for browser-side reads to succeed even with a valid `Accept-Profile` header. This is a one-time settings change (project config in Supabase Dashboard). **Phase 2 prerequisite**, not a Phase 1 blocker, but called out here so it isn't forgotten.

    Do NOT propose probes, scripts, or validation steps in this section — that work is Phase 2's, per D-03.

    Keep section to 40–80 lines.
  </action>
  <verify>
    <automated>grep -c '^## 4. Known Unknowns' /root/bitcoin_bot/.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File contains heading `## 4. Known Unknowns (flagged for Phase 2)`.
    - File contains all five subsection headings: `### 4.1 Anon-role read`, `### 4.2 Caddy CSP coverage`, `### 4.3 Hash-route deep-link`, `### 4.4 In-tree precedent`, `### 4.5 Supabase exposed-schemas allowlist` (each `grep -c '^### 4\\.[1-5]' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns 5).
    - Section references "Phase 2" at least 3 times — `grep -c 'Phase 2' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 3 (the unknowns are explicitly deferred).
    - Section references `Accept-Profile` at least once — `grep -c 'Accept-Profile' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1.
    - Section does NOT propose live probes, scripts, or curl commands: `grep -ciE '(^|[^[:alnum:]])(curl|probe |test live|run live)' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns 0 (no probe imperatives outside of "Phase 2 will validate").
    - Section quotes the Caddy CSP `connect-src https://*.supabase.co` directive somewhere (`grep -c "connect-src" .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1).
    - Previous headings 1, 2, 3 still present.
  </acceptance_criteria>
  <done>
    §4 lists all four CONTEXT-mandated unknowns plus the eu_ai_act in-tree precedent finding and the exposed-schemas allowlist prerequisite. Every item is flagged "Phase 2 will validate" (or "Phase 2 prerequisite") — no live probes proposed in this phase.
  </done>
</task>

<task type="auto">
  <name>Task 6: Write 01-FINDINGS.md §"Implications for Phase 4" (bridge to renderer phase)</name>
  <files>.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md</files>
  <read_first>
    - .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md (in-progress — sections 1–4 must remain intact)
    - .planning/phases/01-render-stack-diagnostic/01-CONTEXT.md D-06 (do NOT prescribe Phase 4 implementation; bridge by reference)
    - .planning/ROADMAP.md §"Phase 4: Hub, Block, and Status Renderer" (the downstream consumer)
    - .planning/docs/economy-map-build-spec-v2.md §6 (Renderer contract this findings doc informs)
    - .planning/REQUIREMENTS.md (RNDR-05: "Block pages reuse the existing aiagentspulse.com publish path (per Phase 0)")
  </read_first>
  <action>
    Append section `## 5. Implications for Phase 4 (bridge — not a design)` to `01-FINDINGS.md`. The section's purpose is to hand off cleanly to Phase 4 without prescribing its implementation (per D-06). Cover:

    - **What Phase 4 can assume (locked by this phase):**
      - Block, hub, and status pages are added as new hash routes in `docker/web/site/app.js` (no new Caddy site, no new container, no SSR service).
      - Data is fetched via `supabase-js` `.schema('economy_map').from(...)` against the existing anon-key client.
      - Re-render on timeline-entry insert (RNDR-06 in REQUIREMENTS.md) is solved by the SPA's "read on next page load" pattern — the spec's "live data" requirement maps to "the next user navigation queries Supabase and gets fresh data." If Phase 4 wants real-time push (e.g., Supabase Realtime subscriptions), that's an additive choice within the same architecture, not a deviation from this finding.
      - The deploy trigger for shipping the new SPA code is unchanged: `scripts/deploy.sh` already detects `docker/web/` changes.
    - **What Phase 4 owns (NOT locked here):**
      - Exact code changes to `app.js` (new view functions, new `getRoute()` branches, new `route()` cases).
      - DOM container structure in `index.html` (new `<div id="map-view">`, `<div id="block-view">`, `<div id="status-view">`).
      - CSS / token integration (depends on Phase 3 — Design Tokens — landing first).
      - Whether to use Supabase Realtime for instant re-render vs. relying on next-navigation reads.
      - Hub URL choice — build spec offers `/` or `/map`. Phase 4 picks based on whether the current edition-list home page is retained.
    - **What Phase 4 must check first:**
      - Phase 2 must have landed `economy_map` schema with the exposed-schemas allowlist updated, otherwise queries return empty. Confirm this is true before Phase 4 implementation.
      - Phase 3 (Design Tokens) ships the maturity pill component — Phase 4 consumes it, doesn't build it.
    - **One-line bridge to build spec §6**: "See `.planning/docs/economy-map-build-spec-v2.md` §6 for the renderer contract this findings doc informs; that section now references this document by name (per D-01)."

    Keep section to 30–50 lines. End the doc with a final line like `*Phase 1 findings complete. Diagnostic-only — zero application code changes.*`
  </action>
  <verify>
    <automated>grep -c '^## 5. Implications for Phase 4' /root/bitcoin_bot/.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File contains heading `## 5. Implications for Phase 4 (bridge — not a design)`.
    - Section references both "Phase 2" and "Phase 3" at least once — Phase 4's prerequisites are named (`grep -c 'Phase 2' …` returns ≥ 4, `grep -c 'Phase 3' …` returns ≥ 1).
    - Section references the build spec by path: `grep -c 'economy-map-build-spec-v2.md' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1.
    - Section references RNDR-05 or RNDR-06 (the renderer requirements that depend on this finding) — `grep -cE 'RNDR-0[56]' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1.
    - Section does NOT contain `<div id="…">` markup or specific app.js code-snippets that prescribe Phase 4's implementation — the section lists boundaries (what Phase 4 owns) without code. (Reviewer-judgable; the section names DOM containers as Phase 4-owned territory but does not write the code.)
    - Final closing line about diagnostic-only / zero application code changes is present.
    - Previous headings 1–4 still present (`grep -cE '^## [1-5]\\. ' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns 5).
  </acceptance_criteria>
  <done>
    §5 cleanly bridges to Phase 4 — naming what Phase 4 can assume, what it owns, and what its prerequisites are — without sketching Phase 4's code. The doc ends with an explicit "diagnostic-only" closing line. All five sections (DIAG-01 through bridge) are present.
  </done>
</task>

<task type="auto">
  <name>Task 7: Annotate build spec v2 §6 to reference 01-FINDINGS.md (DIAG-04)</name>
  <files>.planning/docs/economy-map-build-spec-v2.md</files>
  <read_first>
    - .planning/docs/economy-map-build-spec-v2.md (lines around §6 — the "Renderer contract" section, currently starting at line 231 with "## 6. Renderer contract (shape confirmed in Phase 0)")
    - .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md (the finished findings doc — must exist before this task runs)
    - .planning/phases/01-render-stack-diagnostic/01-CONTEXT.md D-01 (annotate §6 by reference, do NOT duplicate the findings content into the spec)
    - .planning/REQUIREMENTS.md DIAG-04 ("Section 6 of the build spec is filled with the findings before any renderer work begins")
  </read_first>
  <action>
    Add a single annotation block to `.planning/docs/economy-map-build-spec-v2.md` §6. The annotation is **by reference** — do NOT copy the findings doc's contents into the spec. Specifically:

    Use the Edit tool (not Write — preserve the rest of the spec verbatim) to insert the following annotation block immediately after the existing line `## 6. Renderer contract (shape confirmed in Phase 0)` (currently line 231) and before the existing first paragraph (`Given a block slug, assemble: ...`):

    ```
    > **Phase 1 findings (2026-05-26):** This section's "shape confirmed in Phase 0"
    > clause is fulfilled by `.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md`.
    > Findings confirm: block pages reuse the existing publish path via new hash routes
    > in `docker/web/site/app.js`; no sibling route is needed; the publish-path for
    > block bodies is a DB write to `economy_map.block_body_versions` (same pattern as
    > newsletter editions). See `01-FINDINGS.md` §3 for the recommendation and §4 for
    > known unknowns deferred to Phase 2.
    ```

    Do not modify any other part of the spec. Do not edit §10 (decisions table). Do not add new top-level sections. Use the Edit tool's exact-string matching to ensure only the targeted insertion happens.
  </action>
  <verify>
    <automated>grep -c '01-FINDINGS.md' /root/bitcoin_bot/.planning/docs/economy-map-build-spec-v2.md | grep -qE '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '01-FINDINGS.md' .planning/docs/economy-map-build-spec-v2.md` returns ≥ 1 (the annotation exists).
    - `grep -c '01-render-stack-diagnostic' .planning/docs/economy-map-build-spec-v2.md` returns ≥ 1 (the findings path is referenced).
    - The annotation is in §6: `grep -n '01-FINDINGS.md' .planning/docs/economy-map-build-spec-v2.md` returns a line number greater than the line of `## 6. Renderer contract` and less than the line of `## 7. Control surface`. (Manual check during execution; if the Edit tool placed the insertion correctly this is satisfied by construction.)
    - The annotation is **by reference** — the spec does NOT now contain duplicated phrases from `01-FINDINGS.md` §1's stack description or §2's publish mechanism. (Lightweight check: `grep -c 'caddy:2-alpine' .planning/docs/economy-map-build-spec-v2.md` returns 0 — the spec did not absorb stack details; only the reference annotation was added.)
    - The unrelated sections of the spec are unchanged. `wc -l .planning/docs/economy-map-build-spec-v2.md` shows a line-count increase consistent with adding ~7 lines (≤ 346 — original was 346 lines).
    - §10 (Open decisions) is untouched: `grep -c '^## 10. Open decisions' .planning/docs/economy-map-build-spec-v2.md` returns 1, and the four decision lines under it (Negotiation, Regulation, Synthesis thresholds, Evolution order) are unchanged.
  </acceptance_criteria>
  <done>
    Build spec v2 §6 has a by-reference annotation pointing to `01-FINDINGS.md` §§3 and 4. The rest of the spec is unchanged. DIAG-04 satisfied without duplicating content (per D-01).
  </done>
</task>

<task type="auto">
  <name>Task 8: Verify zero application code changes (success criterion 5)</name>
  <files>
    (verification-only task — no files modified)
  </files>
  <read_first>
    - .planning/phases/01-render-stack-diagnostic/01-CONTEXT.md D-03 ("describe-only — no live probes" — by extension, no application code changes)
    - .planning/ROADMAP.md Phase 1 success criterion 5 ("Zero application code changes were made during this phase")
  </read_first>
  <action>
    Run a git-diff verification that proves only files under `.planning/` were touched during this plan. The phase's diagnostic-only constraint (D-03, ROADMAP success #5) is the most important success gate — this task closes it.

    Run, in order:
    1. `git status --porcelain` and capture the output.
    2. `git diff --name-only HEAD` and capture the output.
    3. For each changed file, confirm it starts with `.planning/`. The only legitimate changes are:
       - `.planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` (newly created)
       - `.planning/docs/economy-map-build-spec-v2.md` (annotated)
       - Plan / state / roadmap files updated by the planner/executor harness itself (these all live under `.planning/`).
    4. If ANY file outside `.planning/` is modified, this task FAILS and the executor must revert it before proceeding. Specifically, no changes are allowed under `docker/`, `supabase/`, `scripts/`, `tests/`, `config/`, or any top-level source directory.

    Do NOT modify any files in this task. The task's job is to assert the invariant, not to enforce a fix; if the invariant is violated, surface it as an execution failure and stop.

    If the verification passes, append a single closing sentence to `01-FINDINGS.md` confirming the check (e.g., `*Verified: git diff --name-only shows only files under .planning/ — zero application code changes.*`). This is a one-line addition, not a new section.
  </action>
  <verify>
    <automated>test -z "$(cd /root/bitcoin_bot && git diff --name-only HEAD | grep -v '^.planning/')" &amp;&amp; test -z "$(cd /root/bitcoin_bot && git status --porcelain | awk '{print $2}' | grep -v '^.planning/')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd /root/bitcoin_bot && git diff --name-only HEAD | grep -v '^.planning/'` returns empty (no committed changes outside `.planning/`).
    - `cd /root/bitcoin_bot && git status --porcelain | awk '{print $2}' | grep -v '^.planning/'` returns empty (no unstaged changes outside `.planning/`). Note: the working tree may include the two pre-existing modifications listed in the initial git status (`.claude/settings.local.json`, `config/agentpulse-config.json`) — those predate this phase. The acceptance criterion targets changes introduced by this phase. If those two files appear, confirm via `git log -1 --format=%H -- <path>` that they were last touched before Phase 1's start (commit `36894de` or earlier) and treat them as pre-existing noise, not Phase 1 changes. For a strict gate, run `git diff --name-only 36894de | grep -v '^.planning/'` and confirm empty.
    - The strict-gate check `cd /root/bitcoin_bot && git diff --name-only 36894de | grep -v '^.planning/'` returns empty.
    - `01-FINDINGS.md` contains the closing verification line (`grep -c 'Verified: git diff' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 1).
  </acceptance_criteria>
  <done>
    Diagnostic-only constraint verified by git. No files outside `.planning/` were touched by this phase. The findings doc records the verification result in its closing line. Phase 1 success criterion 5 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This phase is documentation-only. No new code is shipped, no new network endpoints opened, no new auth flows added, no new user input parsed. The two existing trust boundaries (browser → Supabase via anon key, operator → Telegram bot) are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-NA | (N/A) | Phase 1 deliverable (`01-FINDINGS.md`, build-spec annotation) | accept | No new attack surface; phase is documentation-only with zero application code changes (D-03, ROADMAP success #5, verified by Task 8). No new secrets, no new network access, no new auth flows, no new user input. Existing CSP / RLS / anon-key behavior is unchanged. |
| T-01-DOC | Tampering | `.planning/docs/economy-map-build-spec-v2.md` (canonical source-of-truth doc edited in Task 7) | mitigate | Edit is by-reference only (per D-01) and limited to a single annotation block at the top of §6. Task 7 acceptance criteria explicitly check that other sections (especially §10 Open decisions) are unchanged. The full file remains in git history for audit; revert is `git checkout HEAD -- .planning/docs/economy-map-build-spec-v2.md`. |

This phase does not introduce new packages, services, schemas, endpoints, or auth flows. The package-legitimacy gate is therefore inapplicable here (see RESEARCH.md `## Package Legitimacy Audit` — none required when no installs occur).
</threat_model>

<verification>
Per-task `<verify>` blocks cover the bulk of the work. Phase-level checks:

- `test -f .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` (the canonical artifact exists).
- `grep -cE '^## [1-5]\\. ' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns 5 (all five major sections present).
- `grep -c '01-FINDINGS.md' .planning/docs/economy-map-build-spec-v2.md` returns ≥ 1 (build spec §6 references the findings doc).
- `git diff --name-only 36894de | grep -v '^.planning/'` returns empty (zero application code changes — diagnostic-only confirmed).
- Each DIAG requirement appears at least once in the findings doc as a parenthetical heading marker: `grep -cE 'DIAG-0[1-4]' .planning/phases/01-render-stack-diagnostic/01-FINDINGS.md` returns ≥ 4.
</verification>

<success_criteria>
Phase 1 is complete when **all five** ROADMAP success criteria are satisfied:

1. ✓ Findings report names the service/container/framework responsible for serving `aiagentspulse.com` and pinpoints the HTML emission point (DIAG-01) — Task 2.
2. ✓ The current publish mechanism for a new edition page is documented end-to-end (file write path, cache invalidation if any, deploy trigger) (DIAG-02) — Task 3.
3. ✓ A clear recommendation exists on whether block pages reuse the existing publish path (preferred) or require a sibling route, with rationale (DIAG-03) — Task 4.
4. ✓ Section 6 of the build spec is filled with the findings before any renderer work begins (DIAG-04) — Task 7 (by-reference annotation per D-01).
5. ✓ Zero application code changes were made during this phase (diagnostic-only confirmed) — Task 8 (git verification).
</success_criteria>

<output>
Create `.planning/phases/01-render-stack-diagnostic/01-01-SUMMARY.md` when done. The summary should:
- Confirm all five success criteria are satisfied.
- Link to the canonical artifact (`01-FINDINGS.md`).
- Note the build-spec annotation location (§6, by reference).
- List the five known unknowns that Phase 2 must address (4.1 anon-role read, 4.2 CSP coverage, 4.3 hash-route SEO, 4.4 eu_ai_act in-tree precedent, 4.5 exposed-schemas allowlist).
- State the diagnostic-only invariant was held (zero application code changes; git verified).
</output>
