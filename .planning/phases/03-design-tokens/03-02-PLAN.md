---
phase: 03-design-tokens
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - docker/web/site/tokens-preview.html
autonomous: true
requirements:
  - TOKN-01
  - TOKN-02
  - TOKN-03
must_haves:
  truths:
    - "A standalone preview page exists at docker/web/site/tokens-preview.html"
    - "Preview renders 4 accents × 2 modes = 8 swatches showing pinned base hex + on-dark variant"
    - "Preview renders 4 accents × 5 maturity stages = 20 pill renders (single source of truth verified)"
    - "Preview renders 3 timeline-entry samples (normal, source-null, long-text wrap)"
    - "Preview includes a body-mode toggle identical to edition pages so operator can flip technical/strategic"
    - "Preview loads /style-shared.css AND /style-map.css and no other site CSS"
    - "Preview does NOT execute app.js or load Supabase credentials"
    - "Preview is served as a static file by Caddy file_server (matches before SPA try_files fallback per Phase 1 §1)"
  artifacts:
    - path: "docker/web/site/tokens-preview.html"
      provides: "Standalone deployable verification artifact for ROADMAP SC#1"
      contains: "data-stage=\"5\""
      min_lines: 80
  key_links:
    - from: "tokens-preview.html mode-toggle buttons"
      to: "inline setMode() script (no app.js dependency)"
      via: "onclick handlers + document.body.classList swap"
      pattern: "onclick=\"setMode\\("
    - from: "tokens-preview.html <link> tags"
      to: "/style-shared.css + /style-map.css"
      via: "root-relative href"
      pattern: "<link rel=\"stylesheet\" href=\"/style-(shared|map)\\.css\">"
---

<objective>
Create `docker/web/site/tokens-preview.html` — a standalone deployable HTML file that is served as a static file by Caddy (bypassing the SPA `try_files` fallback) and renders every visual surface this phase ships: the 8 accent swatches, the 20 maturity pills, and the 3 timeline entries. The preview includes a mode-toggle identical to edition pages so the operator can visually confirm the on-dark variants work in technical mode.

Purpose: This page is the verification artifact for ROADMAP SC#1 ("render correctly in a standalone preview"). Per CONTEXT.md D-11 and `<specifics>`, the operator must be able to spot-check it on the live `aiagentspulse.com/tokens-preview.html` after Plan 03-03 rebuilds the `web` container. It deliberately bypasses the SPA — no Supabase, no `app.js`, no runtime substitution.

Output: `docker/web/site/tokens-preview.html`. No other file is modified by this plan.

Depends on Plan 03-01 (style-map.css contract) — this preview consumes the CSS classes and data-attributes that 03-01 defines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-design-tokens/03-CONTEXT.md
@.planning/phases/03-design-tokens/03-PATTERNS.md
@.planning/phases/03-design-tokens/03-01-PLAN.md
@docker/web/site/index.html
@docker/web/site/style-shared.css
@docker/web/Caddyfile
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author tokens-preview.html — head, body shell, mode toggle, inline setMode</name>
  <files>docker/web/site/tokens-preview.html</files>
  <read_first>
    - docker/web/site/index.html (full 77 lines — copy head structure lines 1-9 and mode-toggle markup lines 25-28 verbatim; strip everything related to hero/nav/SPA wiring)
    - docker/web/site/style-shared.css (lines 7-49 to understand body.technical/body.strategic variable cascade; lines 159-184 .mode-toggle styles consumed by the preview)
    - docker/web/Caddyfile (lines 6-13 — confirms file_server matches static .html before try_files fallback, so /tokens-preview.html serves directly without SPA wiring)
    - .planning/phases/03-design-tokens/03-CONTEXT.md (D-11 default, "Other Claude discretions" empty-source + ↗ glyph rules)
    - .planning/phases/03-design-tokens/03-PATTERNS.md (§2 — tokens-preview.html pattern assignment + minimal inline setMode skeleton)
  </read_first>
  <behavior>
    - Document opens with `<!DOCTYPE html>` and starts with `<body class="technical">` to default to dark mode (matches edition pages' default).
    - `<head>` loads exactly two stylesheets in this order: `/style-shared.css`, then `/style-map.css`.
    - `<head>` does NOT load `app.js`, `marked.min.js`, the Supabase CDN script, or any third-party JS.
    - Body wraps content in `<div class="container">` so the inherited 720px max-width and padding from `style-shared.css` apply (TOKN-04 inheritance verification).
    - A `.mode-toggle` is present using the exact markup from `index.html` lines 25-28 (two `.toggle-btn` buttons with `onclick="setMode('technical')"` / `onclick="setMode('strategic')"`).
    - An inline `<script>` defines a minimal `setMode(mode)` function: swaps `document.body.classList` between `technical` and `strategic` and toggles the `.active` class on the two buttons. No localStorage, no URL params, no re-render hooks.
    - The page title is `AgentPulse — Design Tokens Preview` (distinct from the SPA shell title).
    - Document contains zero references to `__SUPABASE_URL__` or `__SUPABASE_ANON_KEY__` (the entrypoint.sh substitution never touches this file, but a defensive grep confirms no placeholder leaks).
  </behavior>
  <action>
Create `docker/web/site/tokens-preview.html`. Mirror the head structure from `index.html` lines 1-9 but replace the title and add the `<link>` for `/style-map.css` (consumed in this preview):

```
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AgentPulse — Design Tokens Preview</title>
    <link rel="stylesheet" href="/style-shared.css">
    <link rel="stylesheet" href="/style-map.css">
    <style>
        /* Preview-only layout helpers — not part of the shipped token surface. */
        .preview-section { margin: 40px 0; }
        .preview-section h2 { font-family: Georgia, serif; font-size: 20px; margin-bottom: 16px; }
        .preview-section h3 { font-family: 'Courier New', monospace; font-size: 13px; letter-spacing: 1.5px; text-transform: uppercase; color: var(--text-secondary); margin: 24px 0 8px 0; }
        .swatch-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; }
        .swatch { padding: 16px; border: 1px solid var(--border); border-radius: 6px; }
        .swatch-tile { width: 100%; height: 48px; border-radius: 4px; margin-bottom: 8px; }
        .swatch-label { font-family: 'Courier New', monospace; font-size: 12px; color: var(--text-secondary); }
        .pill-grid { display: grid; grid-template-columns: 80px repeat(5, 1fr); gap: 12px; align-items: center; }
        .pill-grid .pill-row-label { font-family: 'Courier New', monospace; font-size: 13px; color: var(--text-secondary); }
        .pill-grid .pill-col-label { font-family: 'Courier New', monospace; font-size: 11px; color: var(--text-hint); text-align: center; text-transform: uppercase; }
    </style>
</head>
<body class="technical">
  <div class="container">
    <header style="padding: 24px 0;">
      <h1 style="font-family: Georgia, serif; font-size: 28px; margin-bottom: 16px;">Design Tokens Preview</h1>
      <p style="font-family: 'Courier New', monospace; font-size: 14px; color: var(--text-secondary); margin-bottom: 16px;">
        Phase 3 deliverable — TOKN-01..04 verification surface. Toggle modes to verify on-dark contrast variants.
      </p>
      <div class="mode-toggle">
        <button class="toggle-btn active" id="btn-technical" onclick="setMode('technical')">Technical</button>
        <button class="toggle-btn" id="btn-strategic" onclick="setMode('strategic')">Strategic</button>
      </div>
    </header>

    <!-- Preview sections (added by Task 2) -->
    <main id="preview-main"></main>
  </div>

  <script>
    /* Minimal setMode — body-class swap only. No localStorage, no URL param,
       no re-render. The CSS variable cascade in style-shared.css + style-map.css
       handles every visible change automatically via `body.technical [data-accent]`
       and `body.strategic [data-accent]` rules. */
    function setMode(mode) {
      if (mode !== 'technical' && mode !== 'strategic') return;
      document.body.classList.remove('technical', 'strategic');
      document.body.classList.add(mode);
      document.getElementById('btn-technical').classList.toggle('active', mode === 'technical');
      document.getElementById('btn-strategic').classList.toggle('active', mode === 'strategic');
    }
  </script>
</body>
</html>
```

Stop after writing the shell — the next task populates `<main id="preview-main">` with the swatch grid, the pill grid, and the timeline samples.

DO NOT load `app.js`. DO NOT load the Supabase CDN. DO NOT include `__SUPABASE_URL__` or `__SUPABASE_ANON_KEY__` placeholders — this page does not need them and the entrypoint.sh substitution does not touch this file.
  </action>
  <verify>
    <automated>test -f docker/web/site/tokens-preview.html && grep -F '<link rel="stylesheet" href="/style-shared.css">' docker/web/site/tokens-preview.html && grep -F '<link rel="stylesheet" href="/style-map.css">' docker/web/site/tokens-preview.html && grep -F 'AgentPulse — Design Tokens Preview' docker/web/site/tokens-preview.html && grep -F '<body class="technical">' docker/web/site/tokens-preview.html && grep -F 'onclick="setMode(' docker/web/site/tokens-preview.html && grep -F 'function setMode(mode)' docker/web/site/tokens-preview.html && ! grep -F '__SUPABASE_URL__' docker/web/site/tokens-preview.html && ! grep -F '/app.js' docker/web/site/tokens-preview.html && ! grep -F 'supabase-js' docker/web/site/tokens-preview.html</automated>
  </verify>
  <acceptance_criteria>
    - `docker/web/site/tokens-preview.html` exists
    - File contains `<link rel="stylesheet" href="/style-shared.css">`
    - File contains `<link rel="stylesheet" href="/style-map.css">`
    - File contains `<title>AgentPulse — Design Tokens Preview</title>`
    - File contains `<body class="technical">` (defaults to technical mode like edition pages)
    - File contains two buttons with `onclick="setMode('technical')"` and `onclick="setMode('strategic')"`
    - File contains an inline `<script>` defining `function setMode(mode)`
    - File contains ZERO references to `__SUPABASE_URL__`, `__SUPABASE_ANON_KEY__`, `/app.js`, or `supabase-js` (security gate — no SPA wiring leak)
    - File contains `<main id="preview-main">` empty container (populated by Task 2)
  </acceptance_criteria>
  <done>
    Shell of `tokens-preview.html` exists with stylesheets, mode toggle, inline setMode, and empty `<main id="preview-main">` container. All acceptance assertions pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Populate preview with 8 swatches, 20 pills, and 3 timeline entries</name>
  <files>docker/web/site/tokens-preview.html</files>
  <read_first>
    - docker/web/site/tokens-preview.html (Task 1 output — must contain the shell + empty `<main id="preview-main">`)
    - docker/web/site/style-map.css (Plan 03-01 output — confirm the .maturity-pill markup contract and .timeline-entry markup contract before emitting them)
    - .planning/phases/03-design-tokens/03-CONTEXT.md (D-11 — the three required preview sections with edge cases)
    - .planning/phases/03-design-tokens/03-PATTERNS.md (§2 required-sections checklist)
  </read_first>
  <behavior>
    - The `<main id="preview-main">` section is replaced/populated with three sub-sections, in order: Swatches, Maturity Pills, Timeline Entries.
    - Swatches section renders 4 swatches (one per accent: teal, purple, coral, gray). Each swatch is a `[data-accent="..."]` wrapper containing a colored tile filled with `var(--accent-tier)` and a label printing both the base hex and the on-dark hex. Since the preview honors mode-toggle, switching modes flips the visible tile color via the cascade — both modes are observable.
    - Maturity Pills section renders a 4-row × 5-column grid: rows are accents (teal, purple, coral, gray), columns are stages 1-5. Each cell renders one full `.maturity-pill` markup per D-10: `<div class="maturity-pill" data-accent="..." data-stage="..." aria-label="..."><span class="seg"></span>×5</div>`. Total: 20 pill instances.
    - Timeline Entries section renders three `<article class="timeline-entry">` samples wrapped in a `<div data-accent="teal">` so `--accent-tier` resolves (the source link tints).
      - Sample 1 (normal): has `data-source="..."` and a `<a class="timeline-source">source ↗</a>` element.
      - Sample 2 (source-null): no `data-source` attribute, no `<a class="timeline-source">` element — empty-source rule covers it.
      - Sample 3 (long-text wrap): why-it-mattered runs ~3 lines so wrap behavior is visible; source link stays right-anchored.
    - The literal `↗` glyph (Unicode U+2197) appears in the rendered HTML (not `&#8599;` or an SVG).
  </behavior>
  <action>
Edit `tokens-preview.html` to populate the `<main id="preview-main">` block. Replace the empty `<main id="preview-main"></main>` with this content (preserving everything before and after it in the file):

```
    <main id="preview-main">
      <!-- ═══════ SECTION 1: Tier Accent Swatches ═══════ -->
      <section class="preview-section">
        <h2>Tier Accent Swatches</h2>
        <p style="font-family: 'Courier New', monospace; font-size: 13px; color: var(--text-secondary); margin-bottom: 16px;">
          Four accents × two modes. Toggle Technical/Strategic above to flip between pinned-base hex (light) and on-dark variants (dark).
        </p>
        <div class="swatch-grid">
          <div class="swatch" data-accent="teal">
            <div class="swatch-tile" style="background: var(--accent-tier);"></div>
            <div class="swatch-label">teal — substrate<br>base #0F6E56 / on-dark #4FCBA8</div>
          </div>
          <div class="swatch" data-accent="purple">
            <div class="swatch-tile" style="background: var(--accent-tier);"></div>
            <div class="swatch-label">purple — behavior<br>base #534AB7 / on-dark #9D95E8</div>
          </div>
          <div class="swatch" data-accent="coral">
            <div class="swatch-tile" style="background: var(--accent-tier);"></div>
            <div class="swatch-label">coral — psychology<br>base #993C1D / on-dark #E89072</div>
          </div>
          <div class="swatch" data-accent="gray">
            <div class="swatch-tile" style="background: var(--accent-tier);"></div>
            <div class="swatch-label">gray — frame<br>base #5F5E5A / on-dark #B0AEA8</div>
          </div>
        </div>
      </section>

      <!-- ═══════ SECTION 2: Maturity Pills (4 accents × 5 stages = 20) ═══════ -->
      <section class="preview-section">
        <h2>Maturity Pills — 4 accents × 5 stages</h2>
        <p style="font-family: 'Courier New', monospace; font-size: 13px; color: var(--text-secondary); margin-bottom: 16px;">
          Single source of truth across hub / block / status (TOKN-02). Left-to-right fill keyed off <code>data-stage</code>.
        </p>
        <div class="pill-grid">
          <span></span>
          <span class="pill-col-label">nascent (1)</span>
          <span class="pill-col-label">emerging (2)</span>
          <span class="pill-col-label">contested (3)</span>
          <span class="pill-col-label">consolidating (4)</span>
          <span class="pill-col-label">mature (5)</span>

          <span class="pill-row-label">teal</span>
          <div class="maturity-pill" data-accent="teal" data-stage="1" aria-label="Maturity: nascent (1 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="teal" data-stage="2" aria-label="Maturity: emerging (2 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="teal" data-stage="3" aria-label="Maturity: contested (3 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="teal" data-stage="4" aria-label="Maturity: consolidating (4 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="teal" data-stage="5" aria-label="Maturity: mature (5 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>

          <span class="pill-row-label">purple</span>
          <div class="maturity-pill" data-accent="purple" data-stage="1" aria-label="Maturity: nascent (1 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="purple" data-stage="2" aria-label="Maturity: emerging (2 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="purple" data-stage="3" aria-label="Maturity: contested (3 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="purple" data-stage="4" aria-label="Maturity: consolidating (4 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="purple" data-stage="5" aria-label="Maturity: mature (5 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>

          <span class="pill-row-label">coral</span>
          <div class="maturity-pill" data-accent="coral" data-stage="1" aria-label="Maturity: nascent (1 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="coral" data-stage="2" aria-label="Maturity: emerging (2 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="coral" data-stage="3" aria-label="Maturity: contested (3 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="coral" data-stage="4" aria-label="Maturity: consolidating (4 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="coral" data-stage="5" aria-label="Maturity: mature (5 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>

          <span class="pill-row-label">gray</span>
          <div class="maturity-pill" data-accent="gray" data-stage="1" aria-label="Maturity: nascent (1 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="gray" data-stage="2" aria-label="Maturity: emerging (2 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="gray" data-stage="3" aria-label="Maturity: contested (3 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="gray" data-stage="4" aria-label="Maturity: consolidating (4 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
          <div class="maturity-pill" data-accent="gray" data-stage="5" aria-label="Maturity: mature (5 of 5)"><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span><span class="seg"></span></div>
        </div>
      </section>

      <!-- ═══════ SECTION 3: Timeline Entries (3 samples — normal, source-null, long-text) ═══════ -->
      <section class="preview-section" data-accent="teal">
        <h2>Timeline Entries — TOKN-03 format</h2>
        <p style="font-family: 'Courier New', monospace; font-size: 13px; color: var(--text-secondary); margin-bottom: 16px;">
          Fixed two-line format: <code>&lt;event_date&gt; · &lt;what_shifted&gt;</code> then <code>&lt;why_it_mattered&gt; [source ↗]</code>. Wrapped in <code>data-accent="teal"</code> so source-link tints.
        </p>

        <h3>Normal (source present)</h3>
        <article class="timeline-entry" data-source="https://example.com/post">
          <div class="timeline-line1">
            <time class="timeline-date">2026-05-15</time>
            <span class="timeline-sep">·</span>
            <span class="timeline-what">Anthropic publishes Constitutional AI v2 framework</span>
          </div>
          <div class="timeline-line2">
            <span class="timeline-why">Shifts the alignment conversation from oversight to training-time constraints.</span>
            <a class="timeline-source" href="https://example.com/post">source ↗</a>
          </div>
        </article>

        <h3>Source-null (empty-source graceful degradation)</h3>
        <article class="timeline-entry">
          <div class="timeline-line1">
            <time class="timeline-date">2026-05-12</time>
            <span class="timeline-sep">·</span>
            <span class="timeline-what">Operator marks a draft as superseded without external reference</span>
          </div>
          <div class="timeline-line2">
            <span class="timeline-why">No source URL — why-it-mattered occupies the full second line, no [source ↗] link rendered.</span>
          </div>
        </article>

        <h3>Long-text wrap (why-it-mattered exceeds one line)</h3>
        <article class="timeline-entry" data-source="https://example.com/long">
          <div class="timeline-line1">
            <time class="timeline-date">2026-05-08</time>
            <span class="timeline-sep">·</span>
            <span class="timeline-what">Coordinated bid-ask behavior observed across three independent agent runtimes simultaneously</span>
          </div>
          <div class="timeline-line2">
            <span class="timeline-why">This is the first concrete signal that negotiation-coordination might graduate from a section inside payments-settlement to its own block, but the sample size remains small and the pattern could be coincidental — operator should watch for similar signals across the next four weeks before drawing conclusions or queuing a backfill of relevant payments entries into the new block.</span>
            <a class="timeline-source" href="https://example.com/long">source ↗</a>
          </div>
        </article>
      </section>
    </main>
```

Use the Edit tool (NOT Write) to replace just the `<main id="preview-main"></main>` line — preserve the header, the inline `<style>`, and the `<script>` exactly as Task 1 wrote them.

The literal `↗` glyph (U+2197) must appear in the markup as a literal character, not as `&#8599;` or `&#x2197;` (per CONTEXT.md "Other Claude discretions" — prefer the literal).
  </action>
  <verify>
    <automated>grep -c 'data-stage="5"' docker/web/site/tokens-preview.html | (read n; [ "$n" -eq 4 ]) && grep -c 'data-stage="3"' docker/web/site/tokens-preview.html | (read n; [ "$n" -eq 4 ]) && grep -c 'class="maturity-pill"' docker/web/site/tokens-preview.html | (read n; [ "$n" -eq 20 ]) && grep -c 'class="timeline-entry"' docker/web/site/tokens-preview.html | (read n; [ "$n" -eq 3 ]) && grep -c 'data-accent="teal"' docker/web/site/tokens-preview.html | (read n; [ "$n" -ge 6 ]) && grep -c 'data-accent="purple"' docker/web/site/tokens-preview.html | (read n; [ "$n" -ge 6 ]) && grep -c 'data-accent="coral"' docker/web/site/tokens-preview.html | (read n; [ "$n" -ge 6 ]) && grep -c 'data-accent="gray"' docker/web/site/tokens-preview.html | (read n; [ "$n" -ge 6 ]) && grep -c 'source ↗' docker/web/site/tokens-preview.html | (read n; [ "$n" -ge 4 ]) && python3 -c "html=open('docker/web/site/tokens-preview.html').read(); n=html.count('class=\"timeline-source\"'); assert n==2, f'expected 2 timeline-source links (normal + long-text), found {n}'"</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 20 `class="maturity-pill"` instances (4 accents × 5 stages)
    - File contains exactly 4 `data-stage="1"` instances, 4 of `="2"`, 4 of `="3"`, 4 of `="4"`, 4 of `="5"` (one per accent per stage)
    - File contains exactly 3 `class="timeline-entry"` instances (normal, source-null, long-text)
    - File contains exactly 2 `class="timeline-source"` instances (only normal + long-text — source-null sample omits the link element per D-11 default)
    - File contains the literal `↗` glyph (U+2197) — appears in the rendered text `source ↗` of both surviving source links
    - File contains 4 `data-accent="teal"` (swatch + 5 pills + timeline section wrapper = 7, ≥ 6 minimum)
    - File contains `data-source="https://example.com/post"` (normal sample) and `data-source="https://example.com/long"` (long-text sample); the source-null sample's `<article class="timeline-entry">` has NO `data-source` attribute
    - File still contains ZERO references to `__SUPABASE_URL__`, `/app.js`, or `supabase-js`
  </acceptance_criteria>
  <done>
    `tokens-preview.html` contains the three populated sections (8 swatches, 20 pills, 3 timeline entries) inside `<main id="preview-main">`. All acceptance assertions pass. Ready for Plan 03-03 to deploy the `web` container so the file becomes reachable at `aiagentspulse.com/tokens-preview.html`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caddy → browser (public) | `tokens-preview.html` is reachable by any public visitor at `aiagentspulse.com/tokens-preview.html` |
| Preview ↔ SPA shell | The preview must not load `app.js` or accidentally inherit Supabase credential placeholders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | Public reachability of preview file | accept | The preview contains no secrets — only hex values, sample timeline text, and CSS classes. Nothing operator-private or credential-bearing. The page is intentionally public per D-11 ("deployable so operator can review on the real Caddy stack"). |
| T-03-05 | Tampering | Inline `<script>` for setMode | mitigate | CSP at `docker/web/Caddyfile` already permits `script-src 'self' 'unsafe-inline'` for the existing edition pages. The inline setMode mirrors that pattern. Script does ONE thing — body class swap — with no DOM injection, no fetch, no eval. Acceptance criteria gate it via `grep -F 'function setMode(mode)'` exact match. |
| T-03-06 | Spoofing | Sample external links (`https://example.com/...`) | mitigate | Sample URLs use `example.com` (reserved domain per IANA RFC 2606) — they are demonstrably non-routable to real third-party content. No risk of inadvertent advertisement of a real domain. |
| T-03-07 | Information Disclosure | SPA-wiring placeholder leak | mitigate | Verify command grep -F `__SUPABASE_URL__` must return zero matches. The entrypoint.sh `sed` only touches `app.js`, but the acceptance assertion is defensive — if any contributor copy-pasted from `index.html` and accidentally pulled in Supabase wiring, the gate would block. |
| T-03-SC | Tampering | No package installs in this plan | accept | This plan writes a single static HTML file; no npm/pip/cargo installs. Package legitimacy gate not applicable. |
</threat_model>

<verification>
- `docker/web/site/tokens-preview.html` exists and is valid HTML5 (opens with `<!DOCTYPE html>`)
- Loads exactly `/style-shared.css` and `/style-map.css` (no other CSS, no JS dependencies beyond inline setMode)
- Body defaults to `class="technical"`
- Renders 8 swatches (4 accents × 2 modes via toggle), 20 pills (4 accents × 5 stages), 3 timeline entries
- Contains no `__SUPABASE_URL__` / `__SUPABASE_ANON_KEY__` placeholders
- Contains no `<script src="/app.js">` or Supabase CDN scripts
- File serves directly at `/tokens-preview.html` once the `web` container is rebuilt (Plan 03-03)
</verification>

<success_criteria>
- ROADMAP SC#1: standalone preview exists that renders the four tier accent colors correctly across both modes — verified once Plan 03-03 deploys
- TOKN-01: the 4 swatches × 2 modes visibly show the pinned hex values render
- TOKN-02: the 20 pill instances exercise every (accent × stage) combination from the same `.maturity-pill` component — the single-source-of-truth claim is testable
- TOKN-03: the 3 timeline-entry samples cover the format's edge cases (normal, empty-source, long-text wrap)
</success_criteria>

<output>
Create `.planning/phases/03-design-tokens/03-02-SUMMARY.md` when done, recording:
- File line count of `tokens-preview.html`
- Confirmation that the file contains zero SPA-wiring placeholders (`grep -c __SUPABASE_URL__` returns 0)
- Confirmation that the 20-pill grid + 3-entry timeline are present
- Note that final visual verification waits on Plan 03-03 deployment
</output>
