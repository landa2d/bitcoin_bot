---
phase: 04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified: []
autonomous: false
requirements: [D-01, D-05, D-08]
must_haves:
  truths:
    - "D-08: caps land FIRST — migration 034 is applied to live Supabase (project zxzaaqfowtqvmsbitqpu) BEFORE the new proxy.py is deployed"
    - "D-08: proxy.py goes live SECOND — llm-proxy is redeployed only after the migration is confirmed applied"
    - "D-05: enforcement is verified THIRD — the canary (04.1-CANARY.md) passes (reject case + gato downgrade case) BEFORE any other service deploys"
    - "D-01: live agent_wallets_v2 reflects analyst=28000, processor=1000, research=5000, newsletter=2000, gato=50000 with zero null-and-not-uncapped rows"
    - "the governance unit is atomic: ordering + the canary gate are how the non-transactional (DB-change + code-deploy) boundary is made all-or-nothing"
  artifacts:
    - path: ".planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-02-CANARY-RESULTS.md"
      provides: "recorded evidence that the canary gate passed (or failed + rollback taken) against live prod"
  key_links:
    - from: "supabase/migrations/034_governance_caps_and_oncap_behavior.sql"
      to: "live Supabase project zxzaaqfowtqvmsbitqpu"
      via: "Supabase MCP apply_migration"
      pattern: "agent_wallets_v2"
    - from: "docker/llm-proxy/proxy.py (edited in Plan 01)"
      to: "prod llm-proxy container (agentpulse-llm-proxy)"
      via: "scripts/deploy.sh --force-rebuild llm-proxy"
      pattern: "llm-proxy"
    - from: "04.1-CANARY.md"
      to: "live governance enforcement (reject + downgrade)"
      via: "operator-run canary checklist"
      pattern: "governance_error|downgrade"
---

<objective>
Ship the governance migration + proxy as ONE atomic unit to production, in the fixed D-08 order: (1) apply migration 034 to live Supabase, (2) deploy the new proxy.py, (3) run the canary gate and confirm enforcement BEFORE any other service deploys. Atomicity across the non-transactional DB-change + code-deploy boundary is enforced by this ordering plus the canary gate (D-05/D-08).

This plan MUTATES PRODUCTION. Every task is `autonomous: false` — operator approval/observation is required per the operator's standing deploy-safety rule (scoped + approved, never blind) and the canary gate.

Purpose: cut governance over from file-based (governance_config.json) to DB-based (agent_wallets_v2) with ZERO behavior change (D-01 exact caps + gato downgrade preserved) and fail-loud guarantees — the structural fix for "the wallet bug all over again."
Output: live prod with migration 034 applied + new proxy running + a recorded canary-results artifact proving enforcement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-CONTEXT.md
@.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-CANARY.md
@.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-01-SUMMARY.md
@supabase/migrations/034_governance_caps_and_oncap_behavior.sql
@scripts/deploy.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Apply migration 034 to live Supabase (caps land FIRST — D-08 step 1)</name>
  <files>supabase/migrations/034_governance_caps_and_oncap_behavior.sql</files>
  <read_first>
    - supabase/migrations/034_governance_caps_and_oncap_behavior.sql (the exact SQL to apply — read it fully before applying)
    - .planning/phases/04.1-.../04.1-CONTEXT.md (D-01 exact values; D-08 ordering — caps before proxy)
    - .planning/phases/04.1-.../04.1-PATTERNS.md (audit facts: analyst=1000 must become 28000; processor/research/newsletter/gato null; rivalscope/lab_*/code_* null → uncapped sweep)
  </read_first>
  <what-built>
Migration 034 (written + reviewed in Plan 01) is applied to the live Supabase project `zxzaaqfowtqvmsbitqpu` via the Supabase MCP `apply_migration` tool (migration name `034_governance_caps_and_oncap_behavior`). This adds the uncapped/on_cap_behavior/downgrade_map columns, extends the governance_events event_type CHECK, backfills the exact D-01 caps (correcting analyst 1000→28000; populating processor/research/newsletter/gato), sweeps every other null-cap agent to uncapped=TRUE, and installs the structural `(cap>0) OR uncapped` CHECK. The migration is idempotent (ADD COLUMN IF NOT EXISTS + guarded ALTER + explicit UPDATE) so re-applying is safe. This is D-08 step 1: caps MUST land before the proxy goes live, because the new proxy reads the live table (not the file) and would hard-reject any agent missing a cap.
  </what-built>
  <how-to-verify>
1. Before applying: run the Supabase MCP `list_tables` / a SELECT to capture the pre-migration state of `agent_wallets_v2` (record analyst=1000 and the null-cap agents) into the results artifact.
2. Apply migration 034 via the Supabase MCP `apply_migration` tool against project `zxzaaqfowtqvmsbitqpu` (NOT the local CLI — this is the remote prod project; there is no in-code runner).
3. Run the 04.1-CANARY.md §1 DB-state SQL against live: assert `SELECT agent_name, spending_cap_sats, spending_cap_window, on_cap_behavior, uncapped, downgrade_map FROM agent_wallets_v2 WHERE agent_name IN ('analyst','processor','research','newsletter','gato')` returns exactly 28000/daily, 1000/daily, 5000/weekly, 2000/weekly, 50000/daily(downgrade, sonnet→deepseek map).
4. Assert the D-03 invariant: `SELECT count(*) FROM agent_wallets_v2 WHERE spending_cap_sats IS NULL AND uncapped = FALSE` returns 0.
5. Assert the CHECK extension: confirm `governance_events` accepts the new fail-loud event_type (e.g. attempt a harmless test insert in a transaction that is rolled back, or inspect the constraint definition).
DO NOT deploy proxy.py yet — that is Task 2. If any assertion fails, STOP and do not proceed to Task 2.
  </how-to-verify>
  <acceptance_criteria>
    - Migration 034 is recorded as applied in `supabase_migrations.schema_migrations` for project zxzaaqfowtqvmsbitqpu.
    - `SELECT spending_cap_sats FROM agent_wallets_v2 WHERE agent_name='analyst'` returns 28000 (was 1000).
    - processor=1000/daily, research=5000/weekly, newsletter=2000/weekly all present.
    - gato: spending_cap_sats=50000, on_cap_behavior='downgrade', downgrade_map maps claude-sonnet-4-20250514→deepseek-chat.
    - `SELECT count(*) FROM agent_wallets_v2 WHERE spending_cap_sats IS NULL AND uncapped=FALSE` = 0.
    - governance_events CHECK includes the fail-loud event_type from migration 034.
    - proxy.py has NOT been deployed yet (D-08 ordering preserved).
  </acceptance_criteria>
  <resume-signal>Type "caps applied and verified" once the live DB-state assertions all pass, or "abort" with the failing assertion.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Deploy the new proxy.py to prod (proxy goes live SECOND — D-08 step 2)</name>
  <files>docker/llm-proxy/proxy.py</files>
  <read_first>
    - scripts/deploy.sh (the scoped --force-rebuild <service> mechanism lines 124-149; the rsync excludes 48-59; NOTE the service-map gap lines 96-104 does NOT list llm-proxy)
    - docker/docker-compose.yml (the compose service is literally named `llm-proxy`, container `agentpulse-llm-proxy`, port 8200 — confirmed)
    - .planning/phases/04.1-.../04.1-01-SUMMARY.md (the downgrade/cross-provider decision the canary must observe)
    - .planning/phases/04.1-.../04.1-CONTEXT.md (D-06: never `--force-rebuild all`; D-08: proxy only after caps)
  </read_first>
  <what-built>
The edited proxy.py (Plan 01) is deployed to prod and only the `llm-proxy` service is rebuilt/restarted. This is the file→DB governance cutover. LOAD-BEARING DEPLOY GAP (from PATTERNS): `scripts/deploy.sh`'s `map_service` block does NOT auto-map `docker/llm-proxy/` to any service, so an auto-detected deploy would NOT rebuild the proxy. The compose service IS named `llm-proxy`, so the correct scoped invocation is `--force-rebuild llm-proxy` (which sets NEED_REBUILD["llm-proxy"]=1 and runs `docker compose build llm-proxy && docker compose up -d llm-proxy`). Do NOT use `--force-rebuild all` (D-06).
  </what-built>
  <how-to-verify>
1. `DEPLOY_HOST=<prod> scripts/deploy.sh --dry-run` first — inspect the rsync delta and confirm it carries the new `docker/llm-proxy/proxy.py` and does NOT delete `governance_config.json` (the excludes from commit 5f6fadc + the file now being tracked protect it). Confirm no unexpected `--delete` targets. (Note: --dry-run previews FILE changes only and exits before the service-map/build stage.)
2. Scoped deploy of ONLY the proxy: `DEPLOY_HOST=<prod> scripts/deploy.sh --force-rebuild llm-proxy`. This rsyncs the dirs then builds+ups only `llm-proxy`. Confirm `docker compose ps` shows `agentpulse-llm-proxy` healthy/up.
3. Confirm the running proxy is the new code: it must be reading from agent_wallets_v2 and must contain NO governance_config.json reference (the deployed file matches the repo edit). A quick liveness probe on :8200 (health/metrics endpoint) returns 200.
DO NOT run the canary's enforcement tests yet beyond liveness — the full enforcement gate is Task 3. If the deploy fails or the container is unhealthy, roll back the proxy to the prior image and STOP.
  </how-to-verify>
  <acceptance_criteria>
    - `scripts/deploy.sh --dry-run` shows the proxy.py change and NO deletion of governance_config.json and no unexpected --delete targets.
    - Deploy uses `--force-rebuild llm-proxy` (the named compose service), NOT `--force-rebuild all`.
    - `docker compose ps` on prod shows `agentpulse-llm-proxy` up/healthy after deploy.
    - The proxy :8200 liveness probe returns 200.
    - Migration 034 was already applied (Task 1) BEFORE this deploy — D-08 ordering preserved.
    - No other service was rebuilt in this task.
  </acceptance_criteria>
  <resume-signal>Type "proxy deployed and healthy" once agentpulse-llm-proxy is up on the new code, or "rollback" with the failure detail.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Run the canary gate — verify enforcement THIRD (D-05) before any other deploy</name>
  <files>.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-02-CANARY-RESULTS.md</files>
  <read_first>
    - .planning/phases/04.1-.../04.1-CANARY.md (the full canary checklist authored in Plan 01 — execute it verbatim)
    - .planning/phases/04.1-.../04.1-01-SUMMARY.md (the fail-loud event_type string + the gato cross-provider downgrade decision the canary must observe)
    - docker/llm-proxy/proxy.py (the 429 governance_error response shape; the api-key scheme; WALLET_CACHE_TTL=15s / GOV_EVENT_COOLDOWN=300s timing)
  </read_first>
  <what-built>
The canary gate (04.1-CANARY.md) is executed against live prod and the evidence is recorded in `04.1-02-CANARY-RESULTS.md`. This is the D-05 gate: enforcement MUST be proven (a capped agent rejects over-limit, gato downgrades instead of rejecting, unknown agent rejects, under-cap calls still return 200) BEFORE any other drifted service deploys (Plan 03). This is the moment the atomic governance unit is declared committed-or-rolled-back.
  </what-built>
  <how-to-verify>
Execute every numbered step in 04.1-CANARY.md against live prod and record actual outputs into 04.1-02-CANARY-RESULTS.md:
1. DB-state proof (re-confirm the five caps + zero ambiguous rows — already done in Task 1, re-cite).
2. FAIL-LOUD REJECT: drive a capped agent over its window → confirm 429 governance_error AND a governance_event row for the chosen event_type. Plus the unknown-agent negative test → reject (not allow). Mind the 15s wallet cache and 300s event dedup when reading results.
3. DOWNGRADE: drive gato over 50000/daily with a sonnet request → confirm a real downgrade to deepseek (served-by-deepseek OR typed redirect + model_downgrade event, per the Plan 01 decision), explicitly NOT a reject and NOT a silent sonnet pass-through. Confirm the model_downgrade governance_event row.
4. ZERO-BEHAVIOR-CHANGE: a normal under-cap call for each of the five agents returns 200.
5. GATE: if ALL pass → record PASS, the governance unit is committed, Plan 03 may proceed. If ANY fail → record FAIL, roll back the proxy to the prior image (the migration is additive/idempotent and may stay; document the rollback), and STOP — do NOT proceed to Plan 03.
  </how-to-verify>
  <acceptance_criteria>
    - 04.1-02-CANARY-RESULTS.md exists with actual command outputs (not placeholders).
    - Reject canary: a capped agent's over-limit call returned 429 governance_error AND a matching governance_events row exists.
    - Unknown-agent negative test returned a reject, not allow.
    - Downgrade canary: gato's over-cap sonnet request produced a downgrade to deepseek (or the documented typed redirect) + a model_downgrade governance_event — NOT a reject, NOT a silent sonnet pass-through.
    - Zero-behavior-change: each of the five agents' under-cap call returned 200.
    - A binary PASS/FAIL verdict is recorded; on FAIL, the rollback action is documented and Plan 03 is NOT started.
  </acceptance_criteria>
  <resume-signal>Type "canary PASS — governance committed" to authorize Plan 03, or "canary FAIL — rolled back" with the failing case.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration author → live Supabase (zxzaaqfowtqvmsbitqpu) | apply_migration mutates per-agent governance config on prod. |
| deploy operator → prod llm-proxy container | scripts/deploy.sh rsync + scoped rebuild swaps the running governance code. |
| agent → live llm-proxy:8200 | post-cutover, every agent call is governed by the DB caps; the canary probes this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-042-01 | Tampering/Integrity | partial migration — caps applied but proxy not, or proxy deployed before caps | mitigate | Strict D-08 ordering via wave/depends_on + three blocking checkpoints: Task 1 (caps) must verify before Task 2 (proxy) before Task 3 (canary). Each STOPs on failure. |
| T-042-02 | Elevation of Privilege | proxy goes live while some agent still null-and-not-uncapped → that agent serves ungoverned OR hard-rejects unexpectedly | mitigate | Task 1 asserts `count(*) WHERE cap IS NULL AND uncapped=FALSE = 0` before Task 2 deploys; the new proxy fail-loud rejects rather than serving ungoverned if the invariant is somehow violated. |
| T-042-03 | Tampering | deploy.sh --delete removes governance_config.json prod depends on (legacy hazard) | mitigate | --dry-run inspected first (Task 2 step 1); file is now tracked + excludes from commit 5f6fadc protect it; D-09 keeps it as audit trail. |
| T-042-04 | Repudiation | enforcement claimed but never actually verified on live | mitigate | D-05 canary gate (Task 3) records actual reject + downgrade outputs in 04.1-02-CANARY-RESULTS.md; build/import checks alone are an explicit false-positive (governance reads the live table). |
| T-042-05 | DoS | wrong scoped deploy (`--force-rebuild all`) restarts unrelated services / amplifies blast radius | mitigate | D-06: only `--force-rebuild llm-proxy`; the `all` branch is forbidden; Task 2 acceptance asserts no other service rebuilt. |
| T-042-SC | Tampering | npm/pip/cargo installs | accept | No package installs — this plan applies SQL and redeploys existing code. No new dependencies. |
</threat_model>

<verification>
- Migration 034 applied to live project zxzaaqfowtqvmsbitqpu and confirmed in schema_migrations.
- Live agent_wallets_v2 shows the five exact D-01 caps + gato downgrade_map + zero null-and-not-uncapped rows.
- agentpulse-llm-proxy redeployed via scoped `--force-rebuild llm-proxy` and healthy; no other service rebuilt.
- 04.1-02-CANARY-RESULTS.md records PASS on: reject canary, unknown-agent reject, gato downgrade, zero-behavior-change under-cap 200s.
- D-08 ordering (caps → proxy → canary) demonstrably preserved by the three sequential blocking checkpoints.
</verification>

<success_criteria>
- The governance unit is committed atomically: caps applied first, proxy deployed second, enforcement verified third — all-or-nothing via ordering + the canary gate (D-05/D-08).
- Zero behavior change for already-correct agents; gato downgrade preserved; fail-loud proven live.
- The canary PASS verdict authorizes Plan 03; a FAIL triggers documented proxy rollback and halts the phase.
</success_criteria>

<output>
Create `.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-02-SUMMARY.md` when done, referencing 04.1-02-CANARY-RESULTS.md and recording the PASS/FAIL verdict + the deploy invocation actually used.
</output>
