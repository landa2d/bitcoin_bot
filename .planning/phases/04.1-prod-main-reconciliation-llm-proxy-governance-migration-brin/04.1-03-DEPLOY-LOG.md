# 04.1-03 — Staged Per-Service Deploy Log (prod)

**Date:** 2026-05-28
**Host:** prod (compose project `/root/bitcoin_bot/docker`; on-box rebuilds, not deploy.sh rsync)
**Precondition:** Plan 02 canary PASS (accepted) — governance unit committed.

## Drift inventory (Task 1)

| Service | Running image | Latest commit to dir | Action |
|---------|--------------|----------------------|--------|
| gato_brain | 2026-04-27 | 2026-04-30 (`adb0f4b`) | rebuild |
| analyst | 2026-04-27 | 2026-04-30 (`adb0f4b`) | rebuild |
| research | 2026-04-30 | 2026-05-15 (`842f444`) | rebuild |
| gato | 2026-03-24 | 2026-04-10 (`f23055f`) | **carved out** — operator maintenance window (user-facing, heavy OpenClaw build) |
| newsletter | 2026-05-24 | 2026-05-24 | skip — current |
| processor | 2026-05-24 | 2026-05-15 | skip — current |
| llm-proxy | — | — | done (Plan 02) |
| lab-data-provider | — | — | excluded (D-07) |

## Governance pre-flight (post-034, against live agent_wallets_v2)

All in-scope services authenticate under governance-valid identities (none will hit `cap_missing`):
- gato_brain → `gato` (50000/daily, downgrade) ✓
- analyst → `analyst` (28000/daily) ✓
- research → `research` (5000/weekly) ✓
- gato → `gato` (carve-out) ✓

## Staged rebuilds (one at a time, governed-cycle verify, halt on cap_missing/failure)

Each rebuilt locally (`docker compose up -d --build [--no-deps] <svc>`). `cap_missing` across ALL rebuilds = **0** (verified in DB) — governance never misfired.

### gato_brain — ✓ deployed (clean)
- Rebuilt 11:36, healthy on new image. All LLM clients initialized **via proxy** (Claude + DeepSeek + corpus-probe) under identity `gato` (governance-valid). No errors, 0 cap_missing.
- Request-driven (FastAPI) — no autonomous poll to exercise; verified by clean startup + proxy init + zero cap_missing.
- Note: `docker compose up gato_brain` also recreated `llm-proxy` (depends_on). Harmless (proxy restarted on same new image). Subsequent rebuilds used `--no-deps` to stay scoped.

### analyst — ✓ deployed + governed cycle verified
- Rebuilt 11:37 (`--no-deps`), healthy on new image. DeepSeek client via proxy as `analyst`.
- **Governed poll cycle confirmed:** 2× `POST llm-proxy/v1/chat/completions → 200`, 2 settled `llm_call` transactions, 0 cap_missing.
- Pre-existing non-blocking error (unrelated to this phase): `Failed to expire overdue predictions: column predictions.title does not exist` — latent schema mismatch in analyst's prediction-expiry code. **Follow-up.**

### research — ✓ deployed + governed cycle verified (after fixing a pre-existing drift bug)
- Rebuilt 11:38 (`--no-deps`), healthy. Initial poll loop FAILED: `claim_research_task → 404` / `relation "research_queue" does not exist`. **Halted per instruction** and diagnosed.
- Root cause (PRE-EXISTING, since the 2026-04-01 `fix_function_search_paths` hardening migration; old image hit it too — NOT a regression): `claim_research_task(integer)` had `search_path=""`, breaking its unqualified `research_queue` reference. RPC + table both exist.
- Fix: **migration 035** (`ALTER FUNCTION public.claim_research_task(integer) SET search_path = pg_catalog, public`) — committed `be7c567`, applied to prod (operator-approved). Function is not SECURITY DEFINER, so safe; no logic change.
- After fix: `claim_research_task → 200`, research claimed a task ("Security Trust") and ran. **Governed cycle confirmed:** 2× `POST llm-proxy/anthropic/v1/messages → 200` (Claude Sonnet as `research`), 2 settled `llm_call` transactions, 0 cap_missing.
- Two pre-existing non-blocking observations (unrelated to governance cutover): stale research-trigger files cause `Permission denied` on the file-fallback path (DB path works); one `POST /v1/proxy/pay → 500` on the agent-to-agent payment endpoint. **Follow-ups.**

### gato — CARVED OUT (not deployed)
- Operator maintenance window (user-facing Telegram bot, heavy OpenClaw/pnpm build). Identity `gato` is governance-valid, so a future rebuild won't hit cap_missing. **Phase stays OPEN until gato ships.**

## Status: Plan 03 PARTIAL — phase remains open

- Deployed + verified: gato_brain, analyst, research (+ llm-proxy from Plan 02).
- Already current (no deploy): newsletter, processor. Excluded: lab-data-provider (D-07).
- **Outstanding for phase completion:** deploy `gato`; then final reconcile check (prod == main) + resolve the source drift todo (Plan 03 Task 3).
- **Follow-ups surfaced (separate from this phase, all pre-existing):** (1) `predictions.title` missing column in analyst; (2) stale research-trigger file permissions; (3) `/v1/proxy/pay` 500; (4) optional: harden the 5 soft caps via `allow_negative=false`.

