---
phase: 04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin
plan: 03
type: execute
wave: 3
depends_on: ["04.1-02"]
files_modified: []
autonomous: false
requirements: [D-06, D-07]
must_haves:
  truths:
    - "D-06: each drifted service group (gato_brain, newsletter, processor, analyst, research, gato) is deployed ONE GROUP AT A TIME via scoped --force-rebuild <svc>, never --force-rebuild all"
    - "D-06: docker compose ps health is confirmed between each group before the next deploys; per-group rollback awareness"
    - "D-07: lab-data-provider is NOT deployed in this phase (excluded)"
    - "after this plan, prod == main with no untracked drift and a clean deploy baseline before Phase 5"
  artifacts:
    - path: ".planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-03-DEPLOY-LOG.md"
      provides: "per-group deploy record (dry-run delta, services rebuilt, post-deploy health, rollback notes)"
  key_links:
    - from: "local main working tree (docker/<svc>/)"
      to: "prod /opt/bitcoin_bot/docker/<svc>/"
      via: "scripts/deploy.sh --force-rebuild <svc> (scoped, per-group)"
      pattern: "force-rebuild"
    - from: "deploy scope"
      to: "lab-data-provider exclusion"
      via: "never named in any --force-rebuild invocation (D-07)"
      pattern: "lab-data-provider"
---

<objective>
With the governance unit committed (Plan 02 canary PASS), deploy the remaining accumulated prod↔main code drift ONE SERVICE GROUP AT A TIME, each via a scoped `scripts/deploy.sh --force-rebuild <svc>` gated by a `--dry-run` first, confirming `docker compose ps` health between groups (D-06). `lab-data-provider` is explicitly excluded (D-07). The outcome is prod == main with a clean, reconciled deploy baseline before Phase 5 feature work begins.

This plan MUTATES PRODUCTION across multiple services — every task is `autonomous: false` (operator approval/observation per the scoped+approved deploy rule). NEVER `--force-rebuild all`.

Drift inventory (from the folded todo audit, all committed local-ahead): gato_brain (code engine + gato_brain.py), newsletter (block pipeline + poller), processor, analyst, research, gato (Dockerfile/entrypoint), plus volume-mounted config/x_source_accounts.json + skills/* + templates/*.

Purpose: close the prod↔main gap safely so the next phase ships onto a known-good baseline; resolve the source todo.
Output: prod reconciled to main; 04.1-03-DEPLOY-LOG.md recording each group's deploy + health.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-CONTEXT.md
@.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-02-SUMMARY.md
@.planning/todos/pending/2026-05-28-reconcile-prod-main-drift-and-close-the-gap.md
@scripts/deploy.sh
@docker/docker-compose.yml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Confirm canary PASS + capture the full prod↔main dry-run delta</name>
  <files>.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-03-DEPLOY-LOG.md</files>
  <read_first>
    - .planning/phases/04.1-.../04.1-02-SUMMARY.md (Plan 02 — the canary verdict; this plan runs ONLY if PASS)
    - .planning/todos/pending/2026-05-28-reconcile-prod-main-drift-and-close-the-gap.md (the classified drift inventory — which services are behind main)
    - scripts/deploy.sh (the --dry-run path lines 61-84; rsync excludes 48-59; the service-map 96-104; the config/templates/skills restart fan-out 114-122)
    - .planning/phases/04.1-.../04.1-CONTEXT.md (D-06 staged, D-07 lab-data-provider excluded)
  </read_first>
  <what-built>
A precondition gate + a full rsync dry-run inventory. First confirm Plan 02 recorded a canary PASS (if FAIL, this plan must NOT run — the governance unit was rolled back). Then run `DEPLOY_HOST=<prod> scripts/deploy.sh --dry-run` to capture the complete file delta between local main and prod across docker/, config/, skills/, templates/. This is the map of exactly what each subsequent scoped deploy will change, so the operator approves with full visibility (no blind deploy). The dry-run syncs ALL dirs and exits before the build stage, so it previews file changes only — not which services rebuild.
  </what-built>
  <how-to-verify>
1. Open 04.1-02-SUMMARY.md and confirm the verdict is "canary PASS — governance committed". If FAIL, STOP this plan.
2. `DEPLOY_HOST=<prod> scripts/deploy.sh --dry-run` and capture the full output into 04.1-03-DEPLOY-LOG.md.
3. Review the delta and classify it against the known drift inventory: expect changes under docker/gato_brain/, docker/newsletter/, docker/processor/, docker/analyst/, docker/research/, docker/gato/, config/x_source_accounts.json, skills/*, templates/*. Flag ANY unexpected `--delete` target or any change touching docker/llm-proxy/ (should already be deployed) or docker/lab-data-provider/ (must be excluded — D-07).
4. Confirm the delta does NOT propose deleting governance_config.json or any prod-only file the operator wants kept.
  </how-to-verify>
  <acceptance_criteria>
    - 04.1-02-SUMMARY.md verdict is canary PASS (else this plan halts).
    - 04.1-03-DEPLOY-LOG.md contains the full `--dry-run` delta output.
    - The delta is classified per the drift inventory; no unexpected `--delete` targets.
    - No change is staged under docker/lab-data-provider/ scope for deploy (D-07).
    - governance_config.json is not slated for deletion.
  </acceptance_criteria>
  <resume-signal>Type "canary PASS confirmed, dry-run reviewed" to authorize staged deploys, or "halt" with the concern.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Deploy drifted services ONE GROUP AT A TIME, health-checking between each (D-06)</name>
  <files>.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-03-DEPLOY-LOG.md</files>
  <read_first>
    - scripts/deploy.sh (the scoped `--force-rebuild <service>` mechanism lines 124-149 — single named service sets NEED_REBUILD[svc]=1 then `docker compose build <svc> && docker compose up -d <svc>` + `docker compose ps`; the FORBIDDEN `all` branch lines 126-129)
    - docker/docker-compose.yml (exact compose service names: gato, gato_brain, analyst, newsletter, research, processor, web, lab-data-provider — verify each name before invoking)
    - .planning/phases/04.1-.../04.1-03-DEPLOY-LOG.md (Task 1's dry-run delta — deploy only what changed)
    - .planning/phases/04.1-.../04.1-CONTEXT.md (D-06 one-group-at-a-time + per-group rollback; D-07 exclude lab-data-provider)
  </read_first>
  <what-built>
The accumulated drift is shipped to prod, deployed in scoped per-service groups in this order, each as its own approved step with a health check before the next: (1) gato_brain, (2) newsletter, (3) processor, (4) analyst, (5) research, (6) gato. Each group is `DEPLOY_HOST=<prod> scripts/deploy.sh --force-rebuild <svc>` (the named compose service), which rsyncs the dirs then builds+ups ONLY that service and prints `docker compose ps`. NEVER `--force-rebuild all` (D-06). `lab-data-provider` is never named (D-07). Note the deploy.sh config/templates/skills fan-out (lines 114-122) will mark dependent services for restart when those dirs change — the operator should be aware that a config change can restart the listed services; sequence so this does not surprise a still-pending group.
  </what-built>
  <how-to-verify>
For EACH group in order (gato_brain → newsletter → processor → analyst → research → gato):
1. Run `DEPLOY_HOST=<prod> scripts/deploy.sh --force-rebuild <svc>` for exactly that one service name.
2. Confirm the post-deploy `docker compose ps` shows that container up/healthy (and that no unintended service was rebuilt beyond the config/templates fan-out the operator expects).
3. Do a lightweight functional sanity check appropriate to the service (e.g. gato_brain: a Telegram command round-trips; newsletter/processor/analyst/research: the poller/scheduler logs show a clean startup with no import/crash errors; gato: container is up and connected). Record the result in 04.1-03-DEPLOY-LOG.md.
4. ONLY proceed to the next group if the current one is healthy. If a group is unhealthy, roll it back (redeploy the prior image / revert the rsync for that dir) and STOP — record the failure; do not cascade.
NEVER run `--force-rebuild all`. NEVER deploy lab-data-provider (D-07).
After all six groups are healthy, run a final `scripts/deploy.sh --dry-run` and confirm an empty/clean delta (prod == main for the in-scope dirs, modulo the deliberately-excluded lab-data-provider).
  </how-to-verify>
  <acceptance_criteria>
    - Each of gato_brain, newsletter, processor, analyst, research, gato was deployed via its own `--force-rebuild <svc>` invocation (recorded in the log), not in a single batch and never via `all`.
    - `docker compose ps` health + a functional sanity check is recorded per group before the next group deployed.
    - lab-data-provider was NOT deployed (its name appears in no invocation) — D-07.
    - A final `scripts/deploy.sh --dry-run` shows a clean/empty delta for in-scope dirs (prod == main).
    - Any group failure is recorded with the rollback action and the sequence was halted (no cascade).
  </acceptance_criteria>
  <resume-signal>Type "all groups deployed and healthy" once the final dry-run is clean, or "stopped at <svc>" with the failure + rollback taken.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close the source todo + update ROADMAP/STATE to record the reconciled baseline</name>
  <files>.planning/todos/pending/2026-05-28-reconcile-prod-main-drift-and-close-the-gap.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/todos/pending/2026-05-28-reconcile-prod-main-drift-and-close-the-gap.md (the todo this phase executed — mark resolved per CONTEXT "Folded Todos")
    - .planning/phases/04.1-.../04.1-03-DEPLOY-LOG.md (the deploy evidence to cite)
    - .planning/ROADMAP.md (the Phase 04.1 entry to mark complete)
    - .planning/STATE.md (current position to advance)
    - .planning/phases/04.1-.../04.1-CONTEXT.md ("Mark the todo resolved when this phase completes")
  </read_first>
  <action>
Record the reconciled baseline now that prod == main. (1) Resolve the source todo: move `.planning/todos/pending/2026-05-28-reconcile-prod-main-drift-and-close-the-gap.md` to `.planning/todos/completed/` (the repo's resolved-todo convention — see the existing `.planning/todos/completed/cr-01-source-url-scheme-validation.md`), adding a `**Status: resolved (Phase 04.1, <date>)**` header and a one-line pointer to 04.1-03-DEPLOY-LOG.md. (If for some reason it stays in pending, the resolved header + deploy-log pointer are still required.) Note the two consciously-deferred items it surfaced remain open: lab-data-provider deploy decision (D-07) and the rivalscope negative-balance anomaly. (2) ROADMAP.md: do NOT mark Phase 04.1 complete here if the GSD phase-completion flow owns that — instead ensure the Phase 04.1 plan list reflects the three plans (01 governance build, 02 governance cutover, 03 staged deploy) and note the reconciled-baseline outcome. (3) STATE.md: advance the position note to reflect that prod is reconciled to main and the clean deploy baseline exists before Phase 5. Keep edits factual and minimal; do not invent metrics.
  </action>
  <verify>
    <automated>test -f .planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-03-DEPLOY-LOG.md && { ! test -f .planning/todos/pending/2026-05-28-reconcile-prod-main-drift-and-close-the-gap.md || grep -qi "resolved" .planning/todos/pending/2026-05-28-reconcile-prod-main-drift-and-close-the-gap.md; } && grep -rq "04.1-03-DEPLOY-LOG" .planning/todos/ && echo TODO_AND_ROADMAP_UPDATED</automated>
  </verify>
  <acceptance_criteria>
    - The source todo is marked resolved (moved to done/ or a resolved header added) with a pointer to 04.1-03-DEPLOY-LOG.md.
    - The two deferred items (lab-data-provider deploy, rivalscope negative balance) are explicitly noted as still-open, not silently dropped.
    - ROADMAP.md Phase 04.1 plan list reflects the three plans and the reconciled-baseline outcome.
    - STATE.md notes prod == main / clean deploy baseline before Phase 5.
  </acceptance_criteria>
  <done>The source drift todo is resolved with deploy evidence, deferred items are carried forward, and ROADMAP/STATE record the reconciled prod↔main baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local main working tree → prod /opt/bitcoin_bot | scripts/deploy.sh rsync --delete --checksum syncs source; blast radius is the dir set. |
| deploy operator → each prod service container | per-group rebuild/restart swaps running code one service at a time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-043-01 | DoS / availability | bare `--force-rebuild all` restarts everything at once, amplifying blast radius (the operator's named hazard) | mitigate | D-06: scoped `--force-rebuild <svc>` per group only; the `all` branch is forbidden; Task 2 deploys one named service at a time with health gate between. |
| T-043-02 | Tampering | rsync --delete removes a prod file not in the repo (e.g. governance_config.json, prod-only state) | mitigate | --dry-run reviewed first (Task 1) to surface every --delete target before any sync; excludes from commit 5f6fadc protect artifacts; governance_config.json now tracked. |
| T-043-03 | Integrity | unintended lab-data-provider deploy adds/alters a prod container (D-07 violation) | mitigate | D-07: lab-data-provider is named in NO invocation; Task 1 flags any delta in its scope; Task 2 acceptance asserts it was never deployed. |
| T-043-04 | Availability | a drifted service crash-loops post-deploy and cascades to the next group | mitigate | per-group health check + functional sanity before the next group; on failure, roll back that group and HALT (no cascade) — D-06 per-group rollback awareness. |
| T-043-05 | Repudiation | deploy claimed but state unverified | mitigate | 04.1-03-DEPLOY-LOG.md records each invocation, docker compose ps, sanity result, and a final clean dry-run proving prod == main. |
| T-043-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — services rebuild from their existing committed Dockerfiles/requirements; no dependency additions in this phase. |
</threat_model>

<verification>
- Plan 02 canary PASS confirmed before any deploy in this plan.
- Full prod↔main dry-run delta captured and classified; no unexpected --delete; lab-data-provider excluded.
- Each of gato_brain/newsletter/processor/analyst/research/gato deployed via its own scoped `--force-rebuild <svc>`, health-checked between groups; never `--force-rebuild all`.
- Final `--dry-run` shows a clean delta — prod == main for in-scope dirs.
- Source todo resolved with deploy evidence; deferred items carried forward; ROADMAP/STATE updated.
</verification>

<success_criteria>
- Production matches main with no untracked drift (clean final dry-run), achieved via staged, approved, scoped deploys — never a blind full deploy (D-06).
- lab-data-provider was not deployed (D-07).
- A clean, reconciled deploy baseline exists before Phase 5; the source todo is closed.
</success_criteria>

<output>
Create `.planning/phases/04.1-prod-main-reconciliation-llm-proxy-governance-migration-brin/04.1-03-SUMMARY.md` when done, referencing 04.1-03-DEPLOY-LOG.md and stating the final dry-run result (prod == main) + the two carried-forward deferred items.
</output>
