---
phase: 05-intake-classifier-unsorted-handling
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - tests/test_05_intake.py
autonomous: true
requirements: [INTK-05]
must_haves:
  truths:
    - "Attempting to UPDATE a timeline_entries content column fails (append-only enforced by the Phase 2 trigger — INTK-05)"
    - "Attempting to DELETE a timeline_entries row fails (append-only enforced)"
    - "A below-floor classification routes the entry to 'unsorted' while recording tag_confidence"
    - "A classifier-error path routes the entry to 'unsorted' with tag_confidence = NULL (never dropped)"
    - "A live classifier call carries proxy-routing evidence (X-Proxy-Request-Id header / wallet_transactions increment), proving ROADMAP criterion 2 routing per classification (machine-verified, skips without secrets)"
  artifacts:
    - path: "tests/test_05_intake.py"
      provides: "Append-only proof test + below-floor routing test + error-path NULL-confidence test + live proxy-routing evidence test (SC-2)"
      contains: "append_only"
  key_links:
    - from: "tests/test_05_intake.py"
      to: "economy_map.timeline_entries append-only trigger"
      via: "INSERT via Plan 01 helper, then UPDATE/DELETE expected to raise"
      pattern: "timeline_entries"
    - from: "tests/test_05_intake.py (Tests B/C)"
      to: "classify_intake_for_edition (Plan 02 route decision)"
      via: "monkeypatch classify_intake_event, then call the Plan 02 poller and assert the routed block_slug/tag_confidence"
      pattern: "classify_intake_for_edition"
    - from: "tests/test_05_intake.py (Test D)"
      to: "http://llm-proxy:8200 proxy-routing evidence"
      via: "live classify_intake_event call asserts X-Proxy-Request-Id / wallet_transactions increment (skips without secrets)"
      pattern: "X-Proxy-Request-Id"
---

<objective>
Prove the spine guarantees Phase 5 must uphold, via an automated test file:

1. INTK-05 / ROADMAP criterion 5 (append-only): the Phase 2 migration-033 trigger still rejects UPDATE and DELETE on `economy_map.timeline_entries` content columns. The trigger already EXISTS (Phase 2) — Phase 5 only proves it still holds; this is a TEST, not a schema change.
2. The confidence routing contract (ROADMAP criterion 3 + D-05): a below-floor classification lands in `'unsorted'` with `tag_confidence` recorded, and a classifier-error path lands in `'unsorted'` with `tag_confidence = NULL` (never silently dropped). This is the **route decision built by Plan 02's `classify_intake_for_edition`** — Tests B/C call that poller directly (see the dependency note below).
3. ROADMAP criterion 2 (proxy routing, machine-verified): a live classification routed through `http://llm-proxy:8200` carries proxy-side routing evidence per call — promoting SC-2 from operator-verified-only to automated. This is the **Plan 01 `classify_intake_event`** proxy path.

Purpose: criterion 5 is a hard acceptance gate and the project's append-only invariant is the structural backbone of "no silent degradation." Criterion 3 / D-05 is the "silence is the enemy" guarantee. Criterion 2 is the "all LLM calls route through the proxy" governance rule (the RivalScope lesson). All three must be machine-verifiable.

**Dependency note (resolves checker Warning 2 — Option B, the honest dependency):** Tests B/C validate the routing DECISION (floor comparison + error→'unsorted'), and that decision lives ENTIRELY inside Plan 02's `classify_intake_for_edition` — Plan 01's `classify_intake_event` only returns the raw `{block_slug, tag_confidence}` and *raises* on failure (it has no routing or 'unsorted' fallback). There is no pure routing helper to test in isolation. Therefore this plan honestly depends on BOTH Plan 01 and Plan 02 and runs in wave 3 (after both). Tests B/C import and call `classify_intake_for_edition` (Plan 02); Test D imports and calls `classify_intake_event` (Plan 01). Both functions exist before this plan runs, so import/collection cannot fail.

Output: One test file `tests/test_05_intake.py`. No production-code changes (those are Plans 01/02). No schema migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-intake-classifier-unsorted-handling/05-CONTEXT.md
@.planning/phases/05-intake-classifier-unsorted-handling/05-PATTERNS.md
@.planning/phases/05-intake-classifier-unsorted-handling/05-01-SUMMARY.md
@.planning/phases/05-intake-classifier-unsorted-handling/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the append-only proof + confidence-routing + proxy-routing-evidence tests</name>
  <read_first>
    - .planning/phases/05-intake-classifier-unsorted-handling/05-01-SUMMARY.md (exact signatures of economy_map_insert_timeline_entry, economy_map_edition_already_emitted, classify_intake_event — Test A imports the insert helper; Test D imports/exercises classify_intake_event against the live proxy)
    - .planning/phases/05-intake-classifier-unsorted-handling/05-02-SUMMARY.md (exact signature of classify_intake_for_edition / the poller route function — Tests B/C import and call THIS function, not classify_intake_event, because the route decision lives here)
    - tests/test_phase2_integration.py lines 25-46, 68-80 (the env-load + sys.path.insert + `import agentpulse_processor as proc` + `proc.init_clients()` live-Supabase harness to mirror)
    - tests/conftest.py lines 1-60 (the module-loading workaround; do not break it)
    - tests/test_migrations.py lines 19-53 (migration-text + class TestX / def test_* style to mirror)
    - tests/test_llm_proxy.py (if present: the existing skip-if-no-secrets convention — mirror its `pytest.mark.skipif`/`pytest.skip` gate for the live-proxy Test D)
    - supabase/migrations/033_economy_map_schema.sql lines 213-253 (the timeline_entries_append_only() trigger — raises 'timeline_entries is append-only (DELETE not permitted)' on DELETE and 'timeline_entries.<col> is append-only' on content UPDATE), 372-386 (anon RLS hides 'unsorted'; service_role bypasses RLS)
    - docker/llm-proxy/proxy.py lines 870-979 (non-streaming path: stamps X-Proxy-Request-Id + X-Proxy-Agent response headers and records a wallet_transactions row per call — the criterion-2 evidence Test D asserts)
  </read_first>
  <action>
    Create `tests/test_05_intake.py` mirroring the `tests/test_phase2_integration.py` harness (env-load from `config/.env`, `sys.path.insert` for the processor dir, `import agentpulse_processor as proc`, `proc.init_clients()`), using a `class TestX:` with `def test_*` methods (or flat `def test_*()`), per CLAUDE.md conventions. Tests that require live Supabase OR the live proxy must skip cleanly (pytest skip) when their required secrets are absent, so the file imports and the suite runs in CI without secrets.

    The ONLY functions this test file may import from the processor are: `economy_map_insert_timeline_entry` (Plan 01 — Test A), `classify_intake_event` (Plan 01 — Test D), and `classify_intake_for_edition` (Plan 02 — Tests B/C). Tests B/C MUST import and exercise `classify_intake_for_edition` (the route decision lives there); they must NOT attempt to re-implement routing locally.

    Test A — append-only INSERT→UPDATE→DELETE (INTK-05, criterion 5): skip cleanly when `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` are absent. INSERT a throwaway `timeline_entries` row via Plan 01's `economy_map_insert_timeline_entry` helper (use a recognizable test `source_edition_id`, e.g. a `test-intk05-<uuid>` value, `block_slug='unsorted'`, valid NOT-NULL fields). Then attempt to UPDATE its `what_shifted` via a direct PostgREST PATCH (`Content-Profile: economy_map`) and assert the call FAILS (the migration-033 trigger raises; assert a 4xx status or that the helper raises). Then attempt a DELETE (PostgREST DELETE with the profile header) and assert it FAILS too. The append-only trigger blocks both even for the service_role key. (Cleanup is unnecessary precisely because DELETE is blocked — note this in a comment; the row is a recognizable test marker.)

    Test B — below-floor routing (criterion 3, Plan 02 route decision): monkeypatch `proc.classify_intake_event` to return a deterministic low-confidence result (`{'block_slug': <a valid named slug>, 'tag_confidence': <below the config floor>}`), then call Plan 02's `classify_intake_for_edition` with a minimal synthetic published-edition dict carrying one tier-1 `premium_source_posts` event (valid NOT-NULL `title`/`summary`). Assert the entry the poller produces for that event has `block_slug == 'unsorted'` and a recorded (non-NULL) `tag_confidence` equal to the stubbed value. Drive this deterministically via the monkeypatch — do NOT depend on live LLM output. If `classify_intake_for_edition` writes to the DB rather than returning the routed entry, either (a) capture the entry by monkeypatching `proc.economy_map_insert_timeline_entry` to record the dict it was handed (preferred — keeps the test offline and asserts on the exact INSERT payload), or (b) skip cleanly without live Supabase and assert the persisted row. Prefer the monkeypatch-the-insert approach so Test B runs in CI without secrets.

    Test C — error-path NULL confidence (D-05, Plan 02 route decision): monkeypatch `proc.classify_intake_event` to raise, then call `classify_intake_for_edition` with the same synthetic edition. Assert the poller still produces an entry for the event with `block_slug == 'unsorted'` and `tag_confidence` NULL/None — proving an event is never dropped on classifier failure. Capture the entry the same way as Test B (monkeypatch `proc.economy_map_insert_timeline_entry` to record the payload), so the test runs offline.

    Test D — proxy-routing evidence (criterion 2 / SC-2, Plan 01 classifier; resolves checker Warning 1): skip cleanly when `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` OR the proxy/processor-agent-key secrets are absent (match the project's existing skip-if-no-secrets convention from tests/test_llm_proxy.py / test_phase2_integration.py). When live, call Plan 01's `classify_intake_event(event, allowed_slugs)` against the REAL `http://llm-proxy:8200` for one well-formed synthetic event and the seven allowed slugs, then assert proxy-routing evidence fired for THAT classification — choose whichever is directly observable from the Plan 01 path:
      - Preferred (direct): assert the proxy response carried an `X-Proxy-Request-Id` response header (proves the call traversed the proxy). If Plan 01's `classify_intake_event` does not surface response headers to its caller, assert via the durable substitute below.
      - Durable substitute (DB evidence): read the processor agent's `wallet_transactions` count via PostgREST immediately before and after the `classify_intake_event` call and assert it incremented by exactly 1 for the processor agent — the per-call wallet row the proxy records (proxy.py:870-979) is the durable, machine-verifiable proxy-routing evidence.
    Do NOT assert on a log line (non-durable). This test promotes SC-2 from operator-verified-only to machine-verifiable: it proves the classifier call is actually routed through the proxy per classification, not merely that the code shape POSTs to a proxy URL.

    Do NOT modify any production file. Do NOT add or edit any `supabase/migrations/*.sql`. Do NOT use supabase-py `.schema()`/`.in_()` against `economy_map` in the test — use direct PostgREST (the same header convention as Plan 01's helper) for the UPDATE/DELETE attempts and any `wallet_transactions`/`timeline_entries` reads.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_05_intake.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_05_intake.py` imports cleanly: `python3 -c "import ast; ast.parse(open('tests/test_05_intake.py').read())"` exits 0
    - `python3 -m pytest tests/test_05_intake.py -v` collects the tests with no import/collection error (tests requiring live Supabase or the live proxy skip cleanly when their secrets are absent; they do not error)
    - A test asserts an UPDATE of `what_shifted` on a `timeline_entries` row FAILS (4xx / raises) and a DELETE FAILS (4xx / raises) — INTK-05 / criterion 5
    - Tests B/C import and call `classify_intake_for_edition` (Plan 02) — NOT a local re-implementation of routing: `grep -c "classify_intake_for_edition" tests/test_05_intake.py` >= 1
    - A test asserts a below-floor classification (stubbed via `classify_intake_event` monkeypatch) yields `block_slug == 'unsorted'` with a recorded (non-NULL) `tag_confidence`
    - A test asserts a classifier-error (stubbed `classify_intake_event` raises) yields `block_slug == 'unsorted'` with `tag_confidence` None/NULL (D-05)
    - Test D exercises the LIVE proxy via Plan 01's `classify_intake_event` and asserts proxy-routing evidence — either an `X-Proxy-Request-Id` response header OR a `wallet_transactions` increment of 1 for the processor agent — and skips cleanly when secrets are absent: `grep -c "X-Proxy-Request-Id\|wallet_transactions" tests/test_05_intake.py` >= 1 and `grep -c "skip" tests/test_05_intake.py` >= 1
    - The test uses direct PostgREST with the `economy_map` profile header for the UPDATE/DELETE attempts; `grep -c "\.schema(" tests/test_05_intake.py` and `grep -c "\.in_(" tests/test_05_intake.py` return 0
  </acceptance_criteria>
  <done>`tests/test_05_intake.py` exists and passes (or cleanly skips on absent secrets): it proves UPDATE+DELETE on `timeline_entries` fail (INTK-05); below-floor → 'unsorted' with recorded confidence and classifier-error → 'unsorted' with NULL confidence via Plan 02's `classify_intake_for_edition` (D-05); and a live classification through the proxy carries `X-Proxy-Request-Id` / a `wallet_transactions` increment (criterion 2 / SC-2 machine-verified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test (service_role) → economy_map.timeline_entries | the test exercises the service_role write/UPDATE/DELETE path precisely to prove the append-only trigger binds even service_role |
| test → llm-proxy (:8200) | Test D drives a real classification through the proxy to assert the per-call routing evidence (auth + wallet row) actually fires |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | append-only invariant could silently regress | mitigate | This test is itself the mitigation — it asserts UPDATE and DELETE on `timeline_entries` content fail at the trigger, catching any future "simplify to RLS" regression (the migration explicitly warns against that) |
| T-05-11 | Information Disclosure | test secrets / DB write in CI | accept | Live-Supabase/live-proxy tests skip cleanly when `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`/proxy secrets are absent; the test inserts only a recognizable throwaway marker row that the append-only trigger then refuses to delete — acceptable, low-value, isolated to the test marker |
| T-05-12 | Spoofing / Bypass | classifier-routing regression could silently revert to a direct SDK call | mitigate | Test D asserts durable proxy-side evidence (X-Proxy-Request-Id header / wallet_transactions increment) per classification — a future change that bypasses the proxy would fail this test, enforcing ROADMAP criterion 2 (the RivalScope/governance lesson) as a machine check rather than operator review |
</threat_model>

<verification>
- `python3 -m pytest tests/test_05_intake.py -v` passes locally with secrets; collects + skips cleanly without secrets.
- Append-only: UPDATE and DELETE on `timeline_entries` both fail.
- Below-floor → 'unsorted' (confidence recorded) and error → 'unsorted' (confidence NULL), both via Plan 02's `classify_intake_for_edition`.
- Proxy routing (criterion 2): a live `classify_intake_event` call carries `X-Proxy-Request-Id` / a per-call `wallet_transactions` increment.
</verification>

<success_criteria>
- INTK-05 / criterion 5 proven: UPDATE and DELETE on `economy_map.timeline_entries` are rejected by the existing trigger.
- Criterion 3 proven (via Plan 02's poller): below-floor routes to 'unsorted' with recorded confidence.
- D-05 proven (via Plan 02's poller): classifier-error routes to 'unsorted' with NULL confidence.
- Criterion 2 / SC-2 proven by machine check: a live classification through the proxy carries durable proxy-routing evidence (header or wallet_transactions increment), skipping cleanly without secrets.
- No production or migration file changed.
</success_criteria>

<output>
Create `.planning/phases/05-intake-classifier-unsorted-handling/05-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
