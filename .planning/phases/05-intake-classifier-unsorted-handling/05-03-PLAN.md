---
phase: 05-intake-classifier-unsorted-handling
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/test_05_intake.py
autonomous: true
requirements: [INTK-05]
must_haves:
  truths:
    - "Attempting to UPDATE a timeline_entries content column fails (append-only enforced by the Phase 2 trigger — INTK-05)"
    - "Attempting to DELETE a timeline_entries row fails (append-only enforced)"
    - "A below-floor classification routes the entry to 'unsorted' while recording tag_confidence"
    - "A classifier-error path routes the entry to 'unsorted' with tag_confidence = NULL (never dropped)"
  artifacts:
    - path: "tests/test_05_intake.py"
      provides: "Append-only proof test + below-floor routing test + error-path NULL-confidence test"
      contains: "append_only"
  key_links:
    - from: "tests/test_05_intake.py"
      to: "economy_map.timeline_entries append-only trigger"
      via: "INSERT via Plan 01 helper, then UPDATE/DELETE expected to raise"
      pattern: "timeline_entries"
---

<objective>
Prove the two spine guarantees Phase 5 must uphold, via an automated test file:

1. INTK-05 / ROADMAP criterion 5 (append-only): the Phase 2 migration-033 trigger still rejects UPDATE and DELETE on `economy_map.timeline_entries` content columns. The trigger already EXISTS (Phase 2) — Phase 5 only proves it still holds; this is a TEST, not a schema change.
2. The confidence routing contract (ROADMAP criterion 3 + D-05): a below-floor classification lands in `'unsorted'` with `tag_confidence` recorded, and a classifier-error path lands in `'unsorted'` with `tag_confidence = NULL` (never silently dropped).

Purpose: criterion 5 is a hard acceptance gate and the project's append-only invariant is the structural backbone of "no silent degradation." Criterion 3 / D-05 is the "silence is the enemy" guarantee. Both must be machine-verifiable.

Output: One test file `tests/test_05_intake.py`. No production-code changes (those are Plans 01/02). No schema migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-intake-classifier-unsorted-handling/05-CONTEXT.md
@.planning/phases/05-intake-classifier-unsorted-handling/05-PATTERNS.md
@.planning/phases/05-intake-classifier-unsorted-handling/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the append-only proof + confidence-routing tests</name>
  <read_first>
    - .planning/phases/05-intake-classifier-unsorted-handling/05-01-SUMMARY.md (exact signatures of economy_map_insert_timeline_entry, economy_map_edition_already_emitted, classify_intake_event — the test imports/exercises these from the processor module)
    - tests/test_phase2_integration.py lines 25-46, 68-80 (the env-load + sys.path.insert + `import agentpulse_processor as proc` + `proc.init_clients()` live-Supabase harness to mirror)
    - tests/conftest.py lines 1-60 (the module-loading workaround; do not break it)
    - tests/test_migrations.py lines 19-53 (migration-text + class TestX / def test_* style to mirror)
    - supabase/migrations/033_economy_map_schema.sql lines 213-253 (the timeline_entries_append_only() trigger — raises 'timeline_entries is append-only (DELETE not permitted)' on DELETE and 'timeline_entries.<col> is append-only' on content UPDATE), 372-386 (anon RLS hides 'unsorted'; service_role bypasses RLS)
  </read_first>
  <action>
    Create `tests/test_05_intake.py` mirroring the `tests/test_phase2_integration.py` harness (env-load from `config/.env`, `sys.path.insert` for the processor dir, `import agentpulse_processor as proc`, `proc.init_clients()`), using a `class TestX:` with `def test_*` methods (or flat `def test_*()`), per CLAUDE.md conventions. Tests that require live Supabase must skip cleanly (pytest skip) when `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` are absent, so the file imports and the suite runs in CI without secrets.

    Test A — append-only INSERT→UPDATE→DELETE (INTK-05, criterion 5): INSERT a throwaway `timeline_entries` row via Plan 01's `economy_map_insert_timeline_entry` helper (use a recognizable test `source_edition_id`, e.g. a `test-intk05-<uuid>` value, `block_slug='unsorted'`, valid NOT-NULL fields). Then attempt to UPDATE its `what_shifted` via a direct PostgREST PATCH (`Content-Profile: economy_map`) and assert the call FAILS (the migration-033 trigger raises; assert a 4xx status or that the helper raises). Then attempt a DELETE (PostgREST DELETE with the profile header) and assert it FAILS too. The append-only trigger blocks both even for the service_role key. (Cleanup is unnecessary precisely because DELETE is blocked — note this in a comment; the row is a recognizable test marker.)

    Test B — below-floor routing (criterion 3): construct a deliberately-ambiguous event and exercise the classification→route decision with a `tag_confidence` below the configured floor, asserting the resulting `block_slug == 'unsorted'` and that `tag_confidence` is recorded (non-NULL). If exercising the real classifier is non-deterministic, drive the routing logic directly by stubbing/monkeypatching `classify_intake_event` to return a low-confidence result, then assert the poller (or the route decision) writes `'unsorted'` with the recorded confidence. Prefer testing the route decision deterministically over depending on live LLM output.

    Test C — error-path NULL confidence (D-05): monkeypatch `classify_intake_event` to raise, then assert the route decision writes `block_slug == 'unsorted'` with `tag_confidence` NULL/None — proving an event is never dropped on classifier failure.

    Do NOT modify any production file. Do NOT add or edit any `supabase/migrations/*.sql`. Do NOT use supabase-py `.schema()`/`.in_()` against `economy_map` in the test — use direct PostgREST (the same header convention as Plan 01's helper) for the UPDATE/DELETE attempts.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_05_intake.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_05_intake.py` imports cleanly: `python3 -c "import ast; ast.parse(open('tests/test_05_intake.py').read())"` exits 0
    - `python3 -m pytest tests/test_05_intake.py -v` collects the tests with no import/collection error (tests requiring live Supabase skip cleanly when secrets are absent; they do not error)
    - A test asserts an UPDATE of `what_shifted` on a `timeline_entries` row FAILS (4xx / raises) and a DELETE FAILS (4xx / raises) — INTK-05 / criterion 5
    - A test asserts a below-floor classification yields `block_slug == 'unsorted'` with a recorded (non-NULL) `tag_confidence`
    - A test asserts a classifier-error yields `block_slug == 'unsorted'` with `tag_confidence` None/NULL (D-05)
    - The test uses direct PostgREST with the `economy_map` profile header for the UPDATE/DELETE attempts; `grep -c "\.schema(" tests/test_05_intake.py` and `grep -c "\.in_(" tests/test_05_intake.py` return 0
  </acceptance_criteria>
  <done>`tests/test_05_intake.py` exists and passes (or cleanly skips on absent secrets): it proves UPDATE+DELETE on `timeline_entries` fail (INTK-05), below-floor → 'unsorted' with recorded confidence, and classifier-error → 'unsorted' with NULL confidence (D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test (service_role) → economy_map.timeline_entries | the test exercises the service_role write/UPDATE/DELETE path precisely to prove the append-only trigger binds even service_role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | append-only invariant could silently regress | mitigate | This test is itself the mitigation — it asserts UPDATE and DELETE on `timeline_entries` content fail at the trigger, catching any future "simplify to RLS" regression (the migration explicitly warns against that) |
| T-05-11 | Information Disclosure | test secrets / DB write in CI | accept | Live-Supabase tests skip cleanly when `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` are absent; the test inserts only a recognizable throwaway marker row that the append-only trigger then refuses to delete — acceptable, low-value, isolated to the test marker |
</threat_model>

<verification>
- `python3 -m pytest tests/test_05_intake.py -v` passes locally with secrets; collects + skips cleanly without secrets.
- Append-only: UPDATE and DELETE on `timeline_entries` both fail.
- Below-floor → 'unsorted' (confidence recorded); error → 'unsorted' (confidence NULL).
</verification>

<success_criteria>
- INTK-05 / criterion 5 proven: UPDATE and DELETE on `economy_map.timeline_entries` are rejected by the existing trigger.
- Criterion 3 proven: below-floor routes to 'unsorted' with recorded confidence.
- D-05 proven: classifier-error routes to 'unsorted' with NULL confidence.
- No production or migration file changed.
</success_criteria>

<output>
Create `.planning/phases/05-intake-classifier-unsorted-handling/05-03-SUMMARY.md` when done.
</output>
