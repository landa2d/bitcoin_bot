---
phase: 06-telegram-read-only-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/gato_brain/gato_brain.py
autonomous: true
requirements: [CMD-01, CMD-02]
must_haves:
  truths:
    - "Operator runs /map-status and gets a single message with SUBSTRATE/BEHAVIOR/FRAME tier headers and all seven block slugs, each with a 5-segment maturity pill + word label, ·N new (unabsorbed), and ·N draft (omitted when zero)"
    - "/map-status ends with an 'unsorted: N awaiting' footer line"
    - "Operator runs /map-pending and gets a message listing each draft's full version UUID with a pre-filled /map-approve line, and each unsorted entry's full entry UUID with conf: and a pre-filled /map-assign line"
    - "/map-pending shows explicit empty-state words when nothing is pending and nothing is unsorted (not silence)"
    - "Both commands route through the same /x-* dispatch ladder (handle_map_command) before the intent router — no parallel infrastructure"
    - "No /map-* code path can mutate economy_map: the economy_map access wrapper exposes only GET methods, with zero httpx.post/patch/delete and zero Content-Profile header"
    - "A read failure (non-2xx) raises rather than rendering as an empty/zero state"
  artifacts:
    - path: "docker/gato_brain/gato_brain.py"
      provides: "Read-only economy_map GET wrapper, maturity pill text renderer, handle_map_status(), handle_map_pending(), handle_map_command() dispatcher, /map- routing branch"
      contains: "def handle_map_command"
  key_links:
    - from: "handle_map_command"
      to: "handle_map_status / handle_map_pending"
      via: "command dispatch on /map-status, /map-pending"
      pattern: "handle_map_(status|pending)"
    - from: "/map- routing branch in /chat handler"
      to: "handle_map_command"
      via: "_msg_lower.startswith('/map-') before intent_router.route"
      pattern: "startswith\\(\"/map-\"\\)"
    - from: "economy_map GET wrapper"
      to: "economy_map.blocks / block_body_versions / timeline_entries"
      via: "httpx.get with Accept-Profile: economy_map"
      pattern: "Accept-Profile.*economy_map"
---

<objective>
Add two READ-ONLY Telegram commands — `/map-status` (CMD-01) and `/map-pending` (CMD-02) — to `docker/gato_brain/gato_brain.py`, dispatched in the same `handle_x_command()` pattern (D-10), reading `economy_map` via a GET-only PostgREST wrapper that is read-only by construction (D-09).

Purpose: Give the operator situational awareness of the economy_map (block maturity, unabsorbed entry backlog, pending drafts, unsorted assignment queue) entirely via Telegram, before any write commands exist (Phases 9/10). This is the operator-facing control surface for the autonomy boundary — surfacing what is waiting for human approval (fail-loud, "silence is the enemy").

Output: New symbols in `gato_brain.py` — a read-only `economy_map` GET wrapper (`get_blocks()`, `get_draft_versions()`, `get_unsorted_entries()`, plus per-block unabsorbed counts), a `maturity_pill()` text renderer, `handle_map_status() -> str`, `handle_map_pending() -> str`, `handle_map_command(message) -> str`, and a `/map-` routing branch. No new files, no migrations, no writes to economy_map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-telegram-read-only-scaffolding/06-CONTEXT.md
@.planning/phases/06-telegram-read-only-scaffolding/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read-only economy_map GET wrapper + maturity pill text renderer</name>
  <files>docker/gato_brain/gato_brain.py</files>
  <read_first>
    - docker/gato_brain/gato_brain.py — lines 19 (import httpx), 37-38 (SUPABASE_URL, SUPABASE_KEY = os.getenv("SUPABASE_SERVICE_KEY") or os.getenv("SUPABASE_KEY")), 72 (logger = logging.getLogger("gato-brain")) — the globals the wrapper reuses (the file being modified)
    - docker/processor/agentpulse_processor.py — lines 600-660 (economy_map_edition_already_emitted, economy_map_emitted_event_keys) and 3043-3070 (_fetch_economy_map_block_slugs) — the proven Accept-Profile GET + raise-on-non-2xx shape to PORT
    - docker/processor/agentpulse_processor.py — lines 573-597 (economy_map_insert_timeline_entry) — the WRITE anti-example (Content-Profile + httpx.post) the wrapper must NOT mirror
    - docker/web/site/app.js — lines 38 (MATURITY_STAGE), 41 (TIER_LABELS), 357-362 (renderMaturityPill) — the maturity-to-stage map + 5-segment markup to mirror in Python text
    - supabase/migrations/033_economy_map_schema.sql — lines 46-52 (maturity ENUM order), 65-77 (blocks columns incl. last_synthesized_at), 95-110 (block_body_versions + partial draft index), 148-161 (timeline_entries columns + index), 372-376 (anon RLS hides 'unsorted' -> service_role read required)
  </read_first>
  <action>
    Add a GET-only read-only economy_map access surface to gato_brain.py, ported from the processor helpers' shape. Implement as a small set of module-level functions (or a single namespace object) exposing ONLY read methods: get_blocks() does GET /rest/v1/blocks with select=slug,tier,sort_order,maturity,last_synthesized_at and order=sort_order.asc; get_draft_versions() does GET /rest/v1/block_body_versions with status=eq.draft and select=id,block_slug (uses the partial draft index 033:109-110); get_unsorted_entries() does GET /rest/v1/timeline_entries with block_slug=eq.unsorted and select=id,what_shifted,tag_confidence,created_at,event_date and order=created_at.desc; plus a per-block unabsorbed counter (D-05). Every call sets headers apikey and Authorization: Bearer both equal to SUPABASE_KEY (the service key; 033 anon RLS hides unsorted and non-published drafts, so service_role is mandatory here) and Accept-Profile: economy_map, with timeout=10. Use only httpx.get. Zero httpx.post/patch/delete and zero Content-Profile header anywhere in this wrapper (D-09 / criterion-4 read-only-by-construction). Raise RuntimeError on any non-2xx status (fail-loud read — a read failure must never render as empty/zero, which would silently hide drafts/unsorted; mirror the processor raise-on-non-2xx rationale). Pass all slug/uuid filter values through the params= dict — never f-string-interpolate values into the query path.

    For D-05 "unabsorbed" counts: for each block, count timeline_entries for that block_slug newer than the block's last_synthesized_at; when last_synthesized_at IS NULL count ALL the block's entries. Use the created_at column for the newer-than comparison (D-05 default: absorption is about ingestion time; consistent with Phase 7 SYNT-01 windowing). Implement via PostgREST filters per block, e.g. block_slug=eq.<slug> and created_at=gt.<last_synthesized_at> with select=id (or no created_at filter when null) — pass values through params=. Use the Prefer: count=exact header with the Content-Range response, OR a select+len count; whichever is clearer for the 7 low-volume blocks (Claude's discretion per CONTEXT — separate GETs are acceptable).

    Add a maturity_pill(maturity: str) -> str text renderer mirroring the web pill: MATURITY_STAGE map nascent=1, emerging=2, contested=3, consolidating=4, mature=5 (identical to 033 enum order + app.js:38); stage = MATURITY_STAGE.get(maturity, 1) (mirrors app.js || 1 guard for bad enum). Pill = stage filled glyphs + (5-stage) empty glyphs + space + the maturity word, e.g. a 2-filled 3-empty pill followed by "emerging". Use the glyphs filled/empty; if you have evidence they do not render in Telegram monospace, substitute monospace-safe glyphs and note the substitution in the SUMMARY — the locked contract is the 5-segment fill + word label (D-02), not the exact glyph.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; t=ast.parse(open('docker/gato_brain/gato_brain.py').read()); names={n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)}; assert 'maturity_pill' in names, 'maturity_pill missing'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())" exits 0
    - gato_brain.py contains a maturity_pill function and read functions get_blocks, get_draft_versions, get_unsorted_entries (or equivalently-named GET methods on a read-only namespace)
    - Every economy_map GET in the new wrapper sets header Accept-Profile: economy_map — grep -c 'Accept-Profile' docker/gato_brain/gato_brain.py is >= 1
    - The new wrapper contains NO Content-Profile, no httpx.post, no httpx.patch, no httpx.delete (verified by Task 4 gate; structurally absent here)
    - maturity_pill("emerging") returns a 5-glyph fill (2 filled, 3 empty) followed by the word emerging; maturity_pill("garbage") returns a stage-1 pill (1 filled) without raising
  </acceptance_criteria>
  <done>A GET-only economy_map wrapper and a Python maturity-pill renderer exist in gato_brain.py; the wrapper uses Accept-Profile GETs with service_role headers, raises on non-2xx, and exposes no write surface.</done>
</task>

<task type="auto">
  <name>Task 2: handle_map_status() and handle_map_pending() renderers</name>
  <files>docker/gato_brain/gato_brain.py</files>
  <read_first>
    - docker/gato_brain/gato_brain.py — lines 1205-1300 (_handle_x_plan()) — the in-file analog for read-rows -> build lines[] -> "\n".join(...) with empty-state early returns and inline _format_* closures (the file being modified)
    - docker/gato_brain/gato_brain.py — the Task 1 wrapper functions + maturity_pill() just added (consume these; do NOT use supabase.table(...) for economy_map — that client is public-schema only)
    - docker/web/site/app.js — lines 41 (TIER_LABELS), 425-438 (tierSection / renderStatus tier grouping) — mirror SUBSTRATE/BEHAVIOR/FRAME grouping in Python
    - .planning/phases/06-telegram-read-only-scaffolding/06-CONTEXT.md — D-01 (status layout preview), D-03 (count format), D-06 (unsorted footer), D-07 (pending layout + full UUIDs preview), D-08/D-08a (pre-filled write lines + explicit empty states)
  </read_first>
  <action>
    Implement handle_map_status() -> str (CMD-01, per D-01/D-02/D-03/D-06): call the Task-1 wrapper for blocks, per-block draft counts (from get_draft_versions() grouped by block_slug), per-block unabsorbed counts (D-05), and the unsorted total. Group the seven blocks by tier into SUBSTRATE / BEHAVIOR / FRAME (uppercase TIER_LABELS, hardcoded), ordered within each tier by sort_order. Render each block line as the left-padded slug, then maturity_pill(), then "·N new" and optionally "·N draft" — always show "·N new" even at zero; OMIT the "·N draft" segment when the draft count is zero (D-03). Wrap the whole block-table body in a Markdown code fence (triple backtick) so the slug/pill/count columns align in monospace (D-01); left-pad slugs to a fixed width for column alignment. Append a footer line "unsorted: N awaiting" where N = count of timeline_entries WHERE block_slug='unsorted' (D-06). Build via lines = [...] then return "\n".join(lines).

    Implement handle_map_pending() -> str (CMD-02, per D-07/D-08/D-08a): produce two sections. DRAFTS AWAITING APPROVAL — one entry per draft from get_draft_versions(), grouped/labeled by block_slug, each showing the FULL raw block_body_versions.id UUID verbatim as "version: <uuid>" plus a pre-filled forward-contract line "/map-approve <uuid>" (D-08; show plainly as the forward contract — the command lands in Phase 9, no need to build it). UNSORTED AWAITING ASSIGNMENT — one entry per get_unsorted_entries() row, showing a short what_shifted snippet, "conf:<tag_confidence>", the FULL raw timeline_entries.id UUID verbatim as "entry: <uuid>", and a pre-filled "/map-assign <uuid> <slug>" line (D-08). Use FULL UUIDs, stateless — do NOT introduce any daily_index-style ephemeral short-index (D-07; that pattern is the explicit stale-mismatch anti-example). Explicit empty states (D-08a, fail-loud): when no drafts emit "Nothing awaiting approval." in words; when no unsorted entries emit "Nothing awaiting assignment." Both sections always render their header plus either rows or the explicit empty line. Keep lines compact; both messages are expected to fit one Telegram message (the downstream Gato/Node layer handles the 4000-char split + Markdown/plain-text fallback — do NOT build an in-file splitter, but a CHAR_BUDGET-style truncation safety net like _handle_x_plan at :1242 is available if a list grows large).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; t=ast.parse(open('docker/gato_brain/gato_brain.py').read()); names={n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)}; assert {'handle_map_status','handle_map_pending'} <= names, names; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())" exits 0
    - gato_brain.py defines def handle_map_status( and def handle_map_pending(
    - handle_map_status source contains the literal tier headers SUBSTRATE, BEHAVIOR, FRAME and emits an unsorted: footer token — grep -c 'SUBSTRATE' docker/gato_brain/gato_brain.py >= 1 and grep -c 'unsorted:' docker/gato_brain/gato_brain.py >= 1
    - handle_map_pending source contains DRAFTS AWAITING APPROVAL, UNSORTED AWAITING ASSIGNMENT, the explicit empty strings "Nothing awaiting approval" and "Nothing awaiting assignment", and the forward-contract tokens /map-approve and /map-assign
    - Neither handler contains the token daily_index (D-07: no ephemeral index) — grep -c 'daily_index' over the new handlers is 0
    - Neither handler uses supabase.table( for economy_map data (economy_map reads go through the Task-1 GET wrapper only) — verified by inspection in Task 4 gate
  </acceptance_criteria>
  <done>Both read+render handlers exist, mirror the operator-approved layouts (tier-grouped status with pill/counts/footer; pending with full UUIDs + pre-filled write lines + explicit empty states), and source economy_map data exclusively through the GET wrapper.</done>
</task>

<task type="auto">
  <name>Task 3: handle_map_command() dispatcher + /map- routing branch</name>
  <files>docker/gato_brain/gato_brain.py</files>
  <read_first>
    - docker/gato_brain/gato_brain.py — lines 1488-1525 (handle_x_command()) — the sync def -> str dispatcher contract to clone (split cmd/args, one top-level try/except -> logger.error + return string, unknown-command help branch) (the file being modified)
    - docker/gato_brain/gato_brain.py — lines 1856-1858 (the /x- routing branch + ChatResponse(...) shape) and 1924 (intent_router.route — the branch MUST be placed before this) — where to wire the /map- branch
    - docker/gato_brain/gato_brain.py — line 92 (class ChatResponse) — the response model fields (response, session_id, intent, metadata)
  </read_first>
  <action>
    Add def handle_map_command(message: str) -> str: mirroring handle_x_command exactly (D-10): SYNC def returning a string; split message into cmd/args; one top-level try/except Exception that logs via logger.error("Map command failed: ...") and returns "Command failed: <e>" (fail-loud, human-readable). Dispatch: cmd == "/map-status" -> handle_map_status(); cmd == "/map-pending" -> handle_map_pending(); else return an "Unknown map command: <cmd>" help string listing "Available: /map-status, /map-pending". Use the module-level "gato-brain" logger already in scope.

    Wire the route in the /chat request handler ladder beside /x-, /code*, /cto and BEFORE intent_router.route (D-10 / criterion 3 — no parallel infrastructure). Add an if _msg_lower.startswith("/map-"): branch (re-using the already-computed _msg_lower), call handle_map_command(req.message) with the RAW (not lowered) message, and return ChatResponse(response=<map_response>, session_id="", intent="MAP_COMMAND", metadata={}) — identical shape to the /x- branch at :1856-1858. Place it inside the same prefix-dispatch ladder so map commands skip session/intent logic entirely, exactly like /x-*.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; src=open('docker/gato_brain/gato_brain.py').read(); ast.parse(src); names={n.name for n in ast.walk(ast.parse(src)) if isinstance(n,ast.FunctionDef)}; assert 'handle_map_command' in names; assert '/map-' in src and 'MAP_COMMAND' in src; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())" exits 0
    - gato_brain.py defines def handle_map_command( and it dispatches both /map-status and /map-pending (source contains both literals)
    - The /chat handler contains startswith("/map-") and returns a ChatResponse with intent="MAP_COMMAND"
    - The /map- branch appears at a line number BEFORE the intent_router.route call — grep -n confirms the /map- branch line index is less than the intent_router.route line index
    - handle_map_command is synchronous (def, not async def) and wraps dispatch in a single try/except Exception
  </acceptance_criteria>
  <done>/map-status and /map-pending route through handle_map_command in the same prefix-dispatch ladder as /x-*, before the intent router, returning a ChatResponse with intent MAP_COMMAND — no parallel infrastructure.</done>
</task>

<task type="auto">
  <name>Task 4: Read-only-by-construction code-review gate + syntax check (criterion 4 / D-09 evidence)</name>
  <files>docker/gato_brain/gato_brain.py</files>
  <read_first>
    - docker/gato_brain/gato_brain.py — the wrapper + handlers + dispatcher added in Tasks 1-3 (the file being modified)
    - docker/processor/agentpulse_processor.py — lines 573-597 (economy_map_insert_timeline_entry: Content-Profile + httpx.post) — the write signature the gate must prove is ABSENT from the /map-* path
    - .planning/phases/06-telegram-read-only-scaffolding/06-CONTEXT.md — D-09 (GET-only wrapper + code-review gate confirming zero write verbs)
  </read_first>
  <action>
    Perform and record the criterion-4 / D-09 read-only-by-construction review over the new /map-* code surface (the wrapper, both handlers, the dispatcher). Confirm structurally that the economy_map access surface added in this plan contains: zero httpx.post, zero httpx.patch, zero httpx.delete, zero Content-Profile header, and zero supabase.table(...)/.schema(...)/.in_(...) calls against economy_map (those would be silent-failure or write paths). The ONLY economy_map verb used is httpx.get with Accept-Profile: economy_map. Run the grep-based evidence checks in <verify> over the file and record the counts in the SUMMARY as the criterion-4 evidence (note: existing FastAPI @app.post route decorators elsewhere in the file are unrelated to economy_map and are fine; the gate targets httpx write verbs and the Content-Profile write header specifically). If ANY httpx write verb or Content-Profile header is found, fix it (remove the write path) before completing — the wrapper must be read-only by construction, not by convention. Finally, syntax-check the whole file (project convention before any rebuild).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())" && test "$(grep -c 'httpx.post\|httpx.patch\|httpx.delete\|Content-Profile' docker/gato_brain/gato_brain.py)" = "0" && test "$(grep -c 'Accept-Profile' docker/gato_brain/gato_brain.py)" -ge "1" && echo "read-only-by-construction OK"</automated>
  </verify>
  <acceptance_criteria>
    - python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())" exits 0
    - grep -c 'httpx.post\|httpx.patch\|httpx.delete\|Content-Profile' docker/gato_brain/gato_brain.py returns 0 (no write verbs, no write header in the file's economy_map surface)
    - grep -c 'Accept-Profile' docker/gato_brain/gato_brain.py returns >= 1 (read header present)
    - The SUMMARY records the criterion-4 evidence: the grep counts above + a one-line statement that the economy_map wrapper exposes only GET methods
  </acceptance_criteria>
  <done>The /map-* economy_map surface is verified read-only by construction (GET-only, Accept-Profile only, no httpx write verbs, no Content-Profile); the file syntax-checks clean and the criterion-4 evidence is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram user -> Gato -> Gato Brain /chat | Operator commands cross here; /map-* is dispatched in the same gated ladder as /x-* (no broader exposure) |
| Gato Brain -> Supabase PostgREST (economy_map) | gato_brain holds the service_role key (bypasses RLS); the read-only wrapper is the only economy_map touch and is GET-only by construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | economy_map access wrapper (a future edit reintroducing a write path through the same wrapper) | mitigate | GET-only wrapper (no insert/update/delete method exists); Task 4 code-review gate asserts grep count 0 for httpx.post/patch/delete + Content-Profile |
| T-06-02 | Elevation of Privilege | /map-* dispatch exposing map data to non-operator Telegram users | mitigate | /map-* routed in the exact same prefix ladder as /x-* (same operator trust boundary); no new endpoint, no auth change — inherits the existing /chat gating |
| T-06-03 | Information Disclosure | unsorted entries (anon RLS hides them) read via service_role | mitigate | service_role read path is used ONLY inside the gated /map-* handlers; wrapper is not widened to other callers; data surfaced only to the operator command surface |
| T-06-04 | Injection | block_slug / UUID values rendered into PostgREST query params | mitigate | All filter values passed via httpx params= dict (URL-encoded), never f-string-interpolated into the query path |
| T-06-05 | Denial of Service / silent-data-loss | non-2xx read silently rendering as empty/zero (hiding drafts or unsorted backlog) | mitigate | Wrapper raises RuntimeError on non-2xx (fail-loud); dispatcher catches and returns "Command failed: <e>" rather than a false-empty state |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (httpx already in gato_brain); Package Legitimacy Gate N/A |
</threat_model>

<verification>
Phase-level checks after this plan:
- `python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())"` exits 0
- `grep -c 'httpx.post\|httpx.patch\|httpx.delete\|Content-Profile' docker/gato_brain/gato_brain.py` == 0 (read-only by construction, criterion 4)
- `grep -c 'Accept-Profile' docker/gato_brain/gato_brain.py` >= 1
- `grep -c 'handle_map_command\|handle_map_status\|handle_map_pending\|MAP_COMMAND' docker/gato_brain/gato_brain.py` >= 4
- The `/map-` branch line index < the `intent_router.route` line index (routes before the intent router)
</verification>

<success_criteria>
- ROADMAP criterion 1 (CMD-01): `/map-status` returns one message with SUBSTRATE/BEHAVIOR/FRAME tier headers, seven block slugs each with a 5-segment maturity pill + word label, `·N new` unabsorbed count, `·N draft` (omitted at zero), and an `unsorted: N awaiting` footer.
- ROADMAP criterion 2 (CMD-02): `/map-pending` returns drafts-awaiting-approval (full version UUID + `/map-approve` line) and unsorted-awaiting-assignment (full entry UUID + `conf:` + `/map-assign` line), with explicit empty states.
- ROADMAP criterion 3: both commands dispatch through `handle_map_command` in the same `/x-*` ladder before the intent router — no parallel infrastructure.
- ROADMAP criterion 4: read-only verified by code review — the economy_map wrapper is GET-only by construction (no httpx write verb, no Content-Profile), confirmed by the Task 4 gate.
</success_criteria>

<output>
Create `.planning/phases/06-telegram-read-only-scaffolding/06-01-SUMMARY.md` when done.
</output>
