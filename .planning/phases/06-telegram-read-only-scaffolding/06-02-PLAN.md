---
phase: 06-telegram-read-only-scaffolding
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - docker/gato_brain/gato_brain.py
autonomous: false
requirements: [CMD-01, CMD-02]
must_haves:
  truths:
    - "After a scoped gato_brain rebuild, /map-status returns a live message with the seven seeded blocks grouped under SUBSTRATE/BEHAVIOR/FRAME, real maturity pills, real ·N new / ·N draft counts, and the unsorted footer"
    - "After the rebuild, /map-pending returns live drafts (or the explicit empty state) and live unsorted entries (or the explicit empty state) with real UUIDs"
    - "The deploy is a scoped, operator-approved rebuild (docker compose up -d --build --no-deps gato_brain), not a blind full deploy"
  artifacts:
    - path: "docker/gato_brain/gato_brain.py"
      provides: "Verified-live /map-status and /map-pending against the running stack"
      contains: "def handle_map_command"
  key_links:
    - from: "Telegram /map-status,/map-pending"
      to: "running gato_brain container"
      via: "Gato -> Gato Brain /chat dispatch"
      pattern: "MAP_COMMAND"
---

<objective>
Bring the Plan 06-01 read-only commands live with a SCOPED, operator-approved rebuild of the gato_brain service, then smoke-test `/map-status` and `/map-pending` against the running stack with real economy_map data.

Purpose: Prove CMD-01 / CMD-02 work end-to-end (Telegram -> Gato -> Gato Brain -> economy_map PostgREST) on the actual seeded seven-block data, and surface the live message shape to the operator for sign-off. The operator's deploy preference is scoped per-service rebuilds with per-step approval and a verified governed cycle — never a blind full deploy.

Output: A running gato_brain with `/map-*` live, plus recorded smoke-test evidence (operator-confirmed `/map-status` and `/map-pending` output) in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-telegram-read-only-scaffolding/06-CONTEXT.md
@.planning/phases/06-telegram-read-only-scaffolding/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight syntax check before rebuild</name>
  <files>docker/gato_brain/gato_brain.py</files>
  <read_first>
    - docker/gato_brain/gato_brain.py — the file modified by Plan 06-01 (confirm the /map-* symbols are present before rebuilding)
    - CLAUDE.md — Build & Run Commands (syntax-check Python before rebuild; scoped rebuild command)
  </read_first>
  <action>
    Re-run the project's mandatory pre-rebuild syntax check on gato_brain.py and confirm the Plan 06-01 symbols are present (handle_map_command, handle_map_status, handle_map_pending, the /map- routing branch, MAP_COMMAND). Do NOT rebuild yet — that is gated behind the Task 2 checkpoint. If the syntax check fails or any symbol is missing, STOP and report (do not proceed to the rebuild checkpoint).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())" && test "$(grep -c 'handle_map_command\|handle_map_status\|handle_map_pending\|MAP_COMMAND' docker/gato_brain/gato_brain.py)" -ge "4" && echo "pre-flight OK"</automated>
  </verify>
  <acceptance_criteria>
    - python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())" exits 0
    - grep -c 'handle_map_command\|handle_map_status\|handle_map_pending\|MAP_COMMAND' docker/gato_brain/gato_brain.py returns >= 4
  </acceptance_criteria>
  <done>gato_brain.py syntax-checks clean and contains all Plan 06-01 /map-* symbols; ready for the gated rebuild.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator-approved scoped rebuild + live /map-status & /map-pending smoke check</name>
  <what-built>
    Plan 06-01 added read-only `/map-status` (CMD-01) and `/map-pending` (CMD-02) to gato_brain.py, dispatched in the /x-* ladder, reading economy_map via a GET-only wrapper. This task brings them live and verifies them against the running stack. The rebuild is SCOPED (gato_brain only, --no-deps) per the operator's deploy-cutover-discipline preference — no full deploy, no other services touched.
  </what-built>
  <how-to-verify>
    1. Approve the scoped rebuild. Claude runs exactly:
       `cd /root/bitcoin_bot/docker && docker compose up -d --build --no-deps gato_brain`
       (scoped, single-service; no rsync, no full scripts/deploy.sh — per memory feedback-deploy-scoped-and-approved).
    2. Confirm gato_brain came up healthy:
       `docker compose -f /root/bitcoin_bot/docker/docker-compose.yml logs --tail 40 gato_brain` shows uvicorn started, no traceback.
    3. From Telegram (operator account), send `/map-status`. Expect ONE message: a monospace block grouped under SUBSTRATE / BEHAVIOR / FRAME, all seven seeded slugs, each with a 5-segment maturity pill + word label and `·N new` (and `·N draft` only when non-zero), then a footer `unsorted: N awaiting`. Maturity must match the live `economy_map.blocks.maturity` values.
    4. From Telegram, send `/map-pending`. Expect a `DRAFTS AWAITING APPROVAL` section (full version UUIDs + `/map-approve <uuid>` lines, or the explicit "Nothing awaiting approval." line) and an `UNSORTED AWAITING ASSIGNMENT` section (full entry UUIDs + `conf:` + `/map-assign <uuid> <slug>` lines, or the explicit "Nothing awaiting assignment." line).
    5. Confirm the maturity glyphs render legibly in your Telegram client (desktop and/or mobile). If they do not render cleanly, report the substitution needed (the 5-segment + word-label contract is locked, not the exact glyph — D-02).
    6. Optionally, send a non-existent map command (e.g. `/map-bogus`) and confirm it returns the "Unknown map command" help string (not a crash, not the intent-router path).
  </how-to-verify>
  <resume-signal>Type "approved" with the observed /map-status and /map-pending output (or paste/describe any rendering issue, e.g. glyph substitution needed, or a count that disagrees with the DB).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> repo -> prod gato_brain container | /root/bitcoin_bot IS prod; the rebuild crosses the repo->prod boundary and must be scoped + approved |
| Running gato_brain -> Supabase economy_map | Live service_role GET reads against seeded data; read-only by construction (verified in Plan 06-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | blind/over-broad deploy blast radius (rsync --delete, full deploy.sh) | mitigate | Scoped single-service rebuild only (docker compose up -d --build --no-deps gato_brain); blocking operator checkpoint before the command runs (memory: deploy-scoped-and-approved, prod-cutover-discipline) |
| T-06-07 | Information Disclosure | live /map-pending exposes unsorted/draft data | accept | Surfaced only to the operator via the gated /map-* command (same trust boundary as /x-*); intended operator-only visibility |
| T-06-08 | Denial of Service | a bad rebuild takes gato_brain down | mitigate | Pre-flight syntax check (Task 1) + log/health verification step before declaring success; scoped to gato_brain so other services are unaffected |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase; rebuild uses existing requirements.txt |
</threat_model>

<verification>
- gato_brain container is up and healthy after the scoped rebuild (logs show uvicorn started, no traceback)
- Operator confirms live `/map-status` matches the locked layout (tier groups, seven slugs, pills, counts, unsorted footer)
- Operator confirms live `/map-pending` matches the locked layout (full UUIDs + pre-filled write lines, or explicit empty states)
- The deploy was the scoped `docker compose up -d --build --no-deps gato_brain` command, operator-approved before execution
</verification>

<success_criteria>
- ROADMAP criteria 1 & 2 confirmed LIVE against the running stack with real seeded data, operator-signed-off.
- The cutover honored the operator's scoped + approved deploy discipline (no blind full deploy).
- Any glyph substitution or count discrepancy surfaced to the operator and recorded.
</success_criteria>

<output>
Create `.planning/phases/06-telegram-read-only-scaffolding/06-02-SUMMARY.md` when done, including the operator-confirmed live `/map-status` and `/map-pending` output as criterion-1/2 evidence.
</output>
