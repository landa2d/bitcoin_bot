---
phase: 07-synthesis-loop-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/agentpulse-config.json
  - config/economy_map/synth_identity.md
  - docker/processor/agentpulse_processor.py
  - tests/test_07_synthesis.py
autonomous: true
requirements: [SYNT-01, SYNT-03, SYNT-04, SYNT-05, SYNT-06]
must_haves:
  truths:
    - "Editing config/economy_map/synth_identity.md on disk changes the next synthesis call's system prompt with no service restart (mtime hot-reload)"
    - "A missing or empty synth_identity.md causes the loader to return None and the caller to skip the cycle (fail-loud, never voiceless synthesis)"
    - "The eligibility predicate marks a block eligible at >= N=5 new entries since last_synthesized_at, OR >= T=30 days with >= 1 new entry, and never when a draft already exists"
    - "Recency counts entries by created_at > last_synthesized_at (D-02 watermark column, D-04 created_at recency; NULL watermark => all entries / cold-start), never by event_date"
    - "Input assembly passes concrete entry content (event_date, what_shifted, why_it_mattered, source_url) ordered by event_date newest-first, never bare cluster labels"
    - "When unabsorbed entries exceed the cap, the most-recent up to the cap are kept and the omitted count is logged AND noted in the prompt (never a silent drop)"
    - "The single editorial call targets {LLM_PROXY_URL}/anthropic/v1/messages with the agent key and model claude-sonnet-4-20250514; no direct api.anthropic.com call and no routed_llm_call usage"
    - "Output parse validates proposed_maturity against the five-value enum and raises (caller skips, never defaults) on invalid/missing/empty body_md"
  artifacts:
    - path: "config/economy_map/synth_identity.md"
      provides: "Working-default editorial voice system prompt (operator-controlled, hot-reloaded)"
      min_lines: 15
    - path: "config/agentpulse-config.json"
      provides: "synthesis config block (enabled, N, T_days, synthesis_model, max_input_entries, max_input_tokens, output_max_tokens, temperature)"
      contains: "\"synthesis\""
    - path: "docker/processor/agentpulse_processor.py"
      provides: "load_synth_identity, synthesis Sonnet call, output parser, economy_map block/entry/draft/body reads, economy_map_insert_block_body_version, eligibility predicate, input assembly"
      contains: "def load_synth_identity"
    - path: "tests/test_07_synthesis.py"
      provides: "Unit harness covering identity reload, eligibility, assembly+cap, Sonnet routing, output validation, draft INSERT shape"
      contains: "/anthropic/v1/messages"
  key_links:
    - from: "docker/processor/agentpulse_processor.py synthesis call"
      to: "{LLM_PROXY_URL}/anthropic/v1/messages"
      via: "httpx.post with Authorization Bearer + x-api-key agent key"
      pattern: "anthropic/v1/messages"
    - from: "load_synth_identity"
      to: "config/economy_map/synth_identity.md"
      via: "Path.stat().st_mtime cached read"
      pattern: "st_mtime"
    - from: "economy_map_insert_block_body_version"
      to: "economy_map.block_body_versions"
      via: "PostgREST POST with Content-Profile: economy_map"
      pattern: "Content-Profile"
---

<objective>
Build the standalone, independently-testable building blocks of the per-block synthesis loop: the hot-reloadable editorial identity loader, the config threshold block, the economy_map read/insert helpers, the eligibility predicate, the input-assembly+cap logic, the single Sonnet call via the proxy's Anthropic route, and the output parser/validator — plus the Wave-0 test harness that exercises them.

Purpose: Phase 7's autonomous editorial core is composition of primitives that already exist as Phase-5 analogs. Isolating the pure functions (parse, eligibility, assembly, identity load) and the I/O helpers (reads, insert, Sonnet call) in this plan lets them be unit-tested with stubs before the scheduled orchestrator (Plan 02) wires them together. This honors SYNT-01 (trigger), SYNT-03 (assembly), SYNT-04 (single proxy-routed Sonnet call), SYNT-05 (mtime identity), SYNT-06 (body_md + proposed_maturity output).

Output: A `synthesis` config block, a working-default `synth_identity.md`, ~7 new top-level functions in the processor, and `tests/test_07_synthesis.py`.

CRITICAL ROUTING CORRECTION (RESEARCH supersedes CONTEXT D-01/D-10): the Sonnet call is a raw `httpx.post` to `{LLM_PROXY_URL}/anthropic/v1/messages` with an Anthropic Messages body — NOT `routed_llm_call()` (no Anthropic branch) and NOT `/v1/chat/completions` (its Sonnet base_url is api.anthropic.com, which has no /chat/completions route). This is still proxy-routed (D-10 satisfied), needs no new dependency, and mirrors the in-processor `classify_intake_event` transport.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-synthesis-loop-core/07-CONTEXT.md
@.planning/phases/07-synthesis-loop-core/07-RESEARCH.md
@.planning/phases/07-synthesis-loop-core/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship the synthesis config block + working-default identity file</name>
  <read_first>
    - config/agentpulse-config.json (the file being modified — locate the existing `intake_classifier` block, the analog at config/agentpulse-config.json:~101-103, and the `pricing` block which already keys `claude-sonnet-4-20250514`)
    - config/persona.md, config/guardrails.md (mounted operator-controlled config convention that config/economy_map/synth_identity.md follows — D-11)
    - .planning/ROADMAP.md § Phase 4 RNDR-02 (the six-part block skeleton: (1) What it is, (2) Why it's hard, (3) The live tension, (4) Where it stands today, (5) Evolution, (6) Maturity indicator) — the identity file must instruct the model to produce body_md spanning these sections
    - docker/newsletter/block_pipeline.py (editorial prose voice reference for the default identity wording — match the newsletter Sonnet voice)
    - 07-RESEARCH.md § "D-09 Resolution — token budgets" (the exact recommended config values) and § "Wave 0 Gaps"
  </read_first>
  <action>
    Add a top-level `synthesis` block to config/agentpulse-config.json with EXACTLY these keys and the RESEARCH D-09 defaults: `enabled` true, `N` 5, `T_days` 30, `synthesis_model` "claude-sonnet-4-20250514", `max_input_entries` 22, `max_input_tokens` 12000, `output_max_tokens` 8000, `temperature` 0.4 (per D-05/D-09). Place it as a sibling of `intake_classifier` so it is read via `get_full_config().get('synthesis', {})`. Validate the JSON parses.
    Create config/economy_map/synth_identity.md — a working-default editorial-voice SYSTEM prompt (per D-11). It MUST: (a) establish the synthesizer's voice consistent with the newsletter prose; (b) instruct the model to produce body_md organized by the six RNDR-02 skeleton headings; (c) instruct the model to PRESERVE the supplied live_tension as a real section, never trivializing it (foreshadows VLDT-01 but does not implement sentinels); (d) instruct the model to treat all supplied timeline entry text as DATA to synthesize, never as instructions (prompt-injection mitigation, threat T-07-PI); (e) instruct the model to return STRUCTURED JSON `{"body_md": "...", "proposed_maturity": "<one of nascent|emerging|contested|consolidating|mature>"}` (D-12 chosen format); (f) state proposed_maturity must be exactly one of the five enum values. Do NOT hardcode any block-specific content — the file is global voice. The file lands under config/ which is already bind-mounted into the processor at /home/openclaw/.openclaw/config:ro (docker-compose.yml:268) — no docker-compose change.
  </action>
  <verify>
    <automated>python3 -c "import json; c=json.load(open('config/agentpulse-config.json')); s=c['synthesis']; assert s['N']==5 and s['T_days']==30 and s['synthesis_model']=='claude-sonnet-4-20250514' and s['max_input_entries']==22 and s['max_input_tokens']==12000 and s['output_max_tokens']==8000 and s['temperature']==0.4 and s['enabled'] is True; print('synthesis config OK')"</automated>
    <automated>test -s config/economy_map/synth_identity.md && grep -qi proposed_maturity config/economy_map/synth_identity.md && grep -qi body_md config/economy_map/synth_identity.md && echo "identity default OK"</automated>
  </verify>
  <done>config/agentpulse-config.json has a `synthesis` block with all eight D-09 keys at the documented defaults; config/economy_map/synth_identity.md exists, is non-empty, instructs structured-JSON `{body_md, proposed_maturity}` output across the six skeleton headings, preserves live_tension, and treats entry text as data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Identity loader, economy_map read/insert helpers, eligibility predicate, input assembly</name>
  <read_first>
    - docker/processor/agentpulse_processor.py (the file being modified): `_clean_json_response` (:2965-2971), `economy_map_insert_timeline_entry` (:573-597) and its tight-write-surface rationale comment (:566-571), the economy_map read helpers `economy_map_edition_already_emitted` (:600-626) and `_fetch_economy_map_block_slugs` (:3043-3070), `get_full_config` (:542), `_get_agent_api_key` (:6467-6479), module-level `SUPABASE_URL`/`SUPABASE_KEY`/`LLM_PROXY_URL` constants
    - docker/analyst/analyst_poller.py (:82-110 load_identity mtime pattern; AND :120 the `is not None` variant — Phase 7 MUST use `is not None`, not truthy, so an edited file re-checks correctly)
    - supabase/migrations/033_economy_map_schema.sql (exact columns: blocks.slug/maturity/live_tension/last_synthesized_at/current_body_version_id; block_body_versions.id/block_slug/body_md/proposed_maturity/synthesized_from_through/status default 'draft'; timeline_entries.event_date/created_at/what_shifted/why_it_mattered/source_url/block_slug; maturity enum order nascent->emerging->contested->consolidating->mature; partial index idx_block_body_versions_status on status='draft')
    - 07-RESEARCH.md § "Pattern 4" (load_synth_identity snippet), § "Eligibility predicate (D-05/D-06)", § "Parse + validate output (D-12)", § "Pitfall 4/5"
    - 07-PATTERNS.md sections (b), (c), (d) (exact read/insert/parse adaptations with line refs)
  </read_first>
  <action>
    Add these top-level functions to docker/processor/agentpulse_processor.py, snake_case, near their analogs, broad `except` + `logger.error(..., exc_info=True)` where I/O is involved:
    1. Module globals `SYNTH_IDENTITY_PATH = Path("/home/openclaw/.openclaw/config/economy_map/synth_identity.md")`, `_synth_identity_cache: str | None = None`, `_synth_identity_mtime: float = 0.0`. `load_synth_identity() -> str | None`: mtime-cached read using `is not None` cache check (NOT truthy); returns None and `logger.error` (fail-loud) on missing OR empty-after-strip; otherwise caches and returns stripped text (D-11/SYNT-05).
    2. economy_map READ helpers via `httpx.get` to `{SUPABASE_URL}/rest/v1/<table>` with headers `apikey`/`Authorization: Bearer {SUPABASE_KEY}`/`Accept-Profile: economy_map`, raising RuntimeError on non-2xx (so read-failure is never mistaken for no-draft): (a) fetch all blocks selecting `slug,maturity,live_tension,last_synthesized_at,current_body_version_id`; (b) `block_has_open_draft(slug) -> bool` querying `block_body_versions?block_slug=eq.<slug>&status=eq.draft&select=id&limit=1` (D-03); (c) fetch timeline entries selecting `event_date,what_shifted,why_it_mattered,source_url,created_at` for a slug, with the `created_at=gt.<watermark>` filter (watermark = `blocks.last_synthesized_at`, the single watermark column for both N-count and T-clock per D-02) OMITTED when watermark is NULL (cold-start = all, D-06); the recency filter compares `created_at` (NOT `event_date`) per D-04 — event_date is for prompt ordering only; pass last_synthesized_at through as the raw ISO string PostgREST returned, never reconstructed (Pitfall 4); (d) fetch the current published body_md by `block_body_versions?id=eq.<current_body_version_id>&select=body_md` (None when current_body_version_id is NULL — cold-start D-08).
    3. `economy_map_insert_block_body_version(row) -> dict` — a SECOND purpose-scoped insert helper (NOT a generic writer; honor the :566-571 tight-write-surface rationale, threat T-07-WS). `httpx.post` to `{SUPABASE_URL}/rest/v1/block_body_versions` with `Content-Profile: economy_map`, `Prefer: return=representation`; payload keys are exactly `block_slug`, `body_md`, `proposed_maturity`, `synthesized_from_through`; OMIT `status` (DB default 'draft', D-13); raise on non-2xx.
    4. `is_block_eligible(block, new_entries, has_draft, N, T_days) -> bool` implementing D-02/D-04/D-05/D-06 exactly: has_draft => False; recency by len(new_entries) where new = `created_at > last_synthesized_at` (D-04 recency-by-created_at against the D-02 watermark column; NULL => all); eligible when n>=N, OR (n>=1 AND age>=T_days) where age uses now(timezone.utc) minus last_synthesized_at (or minus the EARLIEST new-entry created_at on cold-start, D-06).
    5. `assemble_synthesis_input(block, entries, prior_body_md, cfg) -> dict` returning the assembled prompt string + metadata: order entries by `event_date` DESC newest-first (D-07/RNDR-07); include concrete `event_date`/`what_shifted`/`why_it_mattered`/`source_url` per entry (never cluster labels, SYNT-03); include live_tension (placeholder OK, D-08) + current maturity + the six skeleton headings; cold-start (prior_body_md None) omits the prior-body section (D-08). CAP (D-09): if len(entries) > cfg max_input_entries OR estimated tokens (len(text)//4) > cfg max_input_tokens, keep the most-recent entries up to the cap, compute `omitted_count = total - included`, `logger.warning(...)`, AND append an in-prompt note line declaring the omitted count — never a silent drop.
    6. `parse_synthesis_output(text) -> dict` — `json.loads(_clean_json_response(text))`, validate non-empty `body_md` and `proposed_maturity` in the five-value enum set, raise ValueError (caller logs + skips, never defaults) on invalid/missing (D-12/SYNT-06).
    7. `synthesis_sonnet_call(system_prompt, user_prompt, cfg) -> str` — single `httpx.post` to `{LLM_PROXY_URL}/anthropic/v1/messages` with headers `Authorization: Bearer {_get_agent_api_key()}`, `x-api-key: {_get_agent_api_key()}` (send BOTH per RESEARCH A1; Bearer is read first by proxy `_extract_api_key`), `anthropic-version: 2023-06-01`, `Content-Type: application/json`; body keys `model` (cfg synthesis_model, default claude-sonnet-4-20250514), `system`, `max_tokens` (cfg output_max_tokens), `temperature` (cfg temperature), `messages` (single user role); `timeout=120`; raise RuntimeError on non-2xx; return `resp.json()["content"][0]["text"]` (Anthropic shape, NOT choices[0].message.content). Do NOT import or call routed_llm_call here.
    Syntax-check after editing: `python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"`.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read()); print('processor parses')"</automated>
    <automated>grep -v '^#' docker/processor/agentpulse_processor.py | grep -c 'anthropic/v1/messages' | { read n; [ "$n" -ge 1 ] && echo "anthropic route present ($n)"; }</automated>
    <automated>grep -q "def load_synth_identity" docker/processor/agentpulse_processor.py && grep -q "def economy_map_insert_block_body_version" docker/processor/agentpulse_processor.py && grep -q "def is_block_eligible" docker/processor/agentpulse_processor.py && grep -q "def assemble_synthesis_input" docker/processor/agentpulse_processor.py && grep -q "def parse_synthesis_output" docker/processor/agentpulse_processor.py && echo "all helpers defined"</automated>
    <automated>! grep -n "routed_llm_call" docker/processor/agentpulse_processor.py | grep -i synth</automated>
  </verify>
  <acceptance_criteria>
    - `load_synth_identity()` returns None (and logs) for a missing file and for an empty-after-strip file; returns cached text without re-read while mtime is unchanged; re-reads after mtime changes (uses `is not None` cache guard, verified by Task 3 tmp-file test).
    - The Sonnet call URL ends with `/anthropic/v1/messages`, carries the agent key in BOTH Authorization Bearer and x-api-key, sets `model` to claude-sonnet-4-20250514, and raises on a non-2xx response (SYNT-04 / D-10).
    - No occurrence of `routed_llm_call` in any synthesis function; no string `api.anthropic.com` anywhere in the new code.
    - `is_block_eligible` returns False when has_draft is True regardless of counts (D-03); True at n>=5; True at n>=1 with age>=30d; cold-start (NULL watermark) uses earliest new-entry created_at for the age clock (D-06).
    - `assemble_synthesis_input` orders by event_date desc, includes the four concrete entry fields, caps at max_input_entries keeping newest, and records a non-zero omitted_count with a logger.warning + in-prompt note when over the cap (D-09).
    - `parse_synthesis_output` raises ValueError on empty body_md, on a maturity outside the five-value enum, and on missing proposed_maturity (D-12/SYNT-06).
    - `economy_map_insert_block_body_version` POSTs to /block_body_versions with Content-Profile: economy_map and omits the `status` key (defaults draft, D-13); it never writes to blocks or the published row.
    - `python3 -c "import ast; ast.parse(...)"` passes.
  </acceptance_criteria>
  <done>All seven helper functions exist, parse, route Sonnet via /anthropic/v1/messages, and enforce the fail-loud rules (None identity, invalid maturity, over-cap note) — verified by the Task 3 unit suite.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 unit harness covering identity reload, eligibility, assembly+cap, routing, validation, insert shape</name>
  <read_first>
    - tests/test_05a_intake_classifier.py (the full harness to mirror: module stubs for schedule/tweepy/resend/markdown before `import agentpulse_processor` :28-38; OPENCLAW_DATA_DIR + sys.path.insert :40-43; priming proc._model_config_cache from repo config :48-49; `_FakeResponse` :65-75; `_install_proxy_stub` monkeypatching proc._get_agent_api_key + proc.httpx.post and capturing url/headers/body :78-101)
    - tests/conftest.py (module-loading workarounds already in place)
    - docker/processor/agentpulse_processor.py (the functions under test, added in Task 2)
    - 07-RESEARCH.md § "Validation Architecture" → "Phase Requirements → Test Map" and "Test harness pattern to mirror" (the Anthropic-shaped _FakeResponse: json() returns {"content":[{"type":"text","text": ...}]})
    - 07-PATTERNS.md § "tests/test_07_synthesis.py" (assertion list)
  </read_first>
  <action>
    Create tests/test_07_synthesis.py mirroring the test_05a harness. Add a `_FakeResponse` whose `.json()` returns the Anthropic Messages shape `{"content":[{"type":"text","text": <stringified JSON {body_md, proposed_maturity}>}]}` and `.status_code`. Monkeypatch `proc._get_agent_api_key` to a fake `ap_processor_test` key, `proc.httpx.post` (capturing url/headers/body for both the Sonnet call and the INSERT), and `proc.httpx.get` (returning canned economy_map rows for the read helpers). Write `def test_*` functions (also runnable standalone via a `_run_all()` __main__ block) covering:
    - SYNT-04: the Sonnet call captured url endswith `/anthropic/v1/messages`; headers include the agent key in Authorization and x-api-key; body["model"] == "claude-sonnet-4-20250514"; a stubbed non-2xx response makes the call raise.
    - SYNT-05/D-11: `load_synth_identity()` against a tmp file returns its text; returns None for a missing path and for an empty file; re-reads after the tmp file's mtime changes (monkeypatch SYNTH_IDENTITY_PATH to the tmp path).
    - SYNT-01/D-03/D-05/D-06: `is_block_eligible` truth table — has_draft True => False; n>=5 => True; n in [1..4] with age<30d => False, with age>=30d => True; cold-start NULL watermark uses earliest entry created_at.
    - SYNT-03/D-07/D-09: `assemble_synthesis_input` orders entries event_date desc; over-cap input keeps newest `max_input_entries`, sets omitted_count>0, and the assembled prompt contains the omitted-count note; under-cap sets omitted_count 0.
    - SYNT-06/D-12: `parse_synthesis_output` returns the dict for valid JSON; raises on empty body_md, invalid maturity, and missing proposed_maturity.
    - D-13: calling `economy_map_insert_block_body_version({...})` captures a POST whose url endswith `/block_body_versions`, headers include `Content-Profile: economy_map`, and the body has NO `status` key and never references blocks/published.
    Run standalone and under pytest.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 tests/test_07_synthesis.py</automated>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_07_synthesis.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tests/test_07_synthesis.py` exits 0 and prints per-test pass lines (standalone mode).
    - `python3 -m pytest tests/test_07_synthesis.py -q` is green with no skips on the six requirement groups.
    - The Sonnet-routing test asserts the literal substring `/anthropic/v1/messages` in the captured URL and fails if the code ever reverts to `/v1/chat/completions` or `routed_llm_call`.
    - The cap test asserts both a logged warning path (omitted_count>0) and the in-prompt note substring — proving no silent drop (D-09).
    - The identity test asserts None on missing AND empty file (fail-loud, D-11).
  </acceptance_criteria>
  <done>tests/test_07_synthesis.py passes standalone and under pytest, asserting the /anthropic/v1/messages routing, the eligibility truth table, the cap-with-note behavior, the maturity-enum validation, and the Content-Profile draft INSERT shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk (config volume) → processor system prompt | synth_identity.md content is read off the mounted config volume and injected as the LLM system prompt |
| timeline_entries (DB) → synthesis prompt | classifier/operator-sourced entry text is embedded into the user prompt |
| processor → llm-proxy (`/anthropic`) | agent key crosses to the proxy which holds the upstream Anthropic key |
| processor → economy_map (PostgREST, service_role) | the draft INSERT crosses into the append-only schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-PI | Tampering | synth_identity.md / timeline entry text → system+user prompt | mitigate | synth_identity.md is operator-controlled config (same trust tier as persona.md); entries are classifier/operator-sourced, not anonymous input; identity file instructs the model to treat entry text as DATA not instructions; output is `draft` only and human-gated in Phase 9 — low blast radius |
| T-07-PATH | Information Disclosure | synth_identity.md file read | accept | path is a fixed module constant under the read-only mount (no user-supplied path component); `:ro` bind mount; no traversal surface |
| T-07-AVAIL | Denial of Service | missing/empty identity (voiceless synthesis) | mitigate | load_synth_identity returns None → caller logs loudly and skips the cycle (fail-loud D-11); never synthesizes voiceless |
| T-07-MV | Tampering / Input Validation | LLM-returned proposed_maturity | mitigate | parse_synthesis_output validates against the five-value enum and raises (skip block, never default) — DB enum + NOT NULL is the backstop (D-12) |
| T-07-WS | Elevation of Privilege | service_role write surface | mitigate | a SECOND purpose-scoped `economy_map_insert_block_body_version` (no generic schema-agnostic writer); payload omits `status` and never targets blocks/published (D-13) |
| T-07-DROP | Repudiation / Info loss | over-cap entry omission | mitigate | D-09: keep newest up to cap, logger.warning + in-prompt omitted-count note — never silent |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — Phase 7 installs no packages (httpx/schedule/supabase/stdlib already present); no install task, no slopcheck checkpoint required |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"` passes (syntax gate before any rebuild).
- `python3 tests/test_07_synthesis.py` and `python3 -m pytest tests/test_07_synthesis.py tests/test_05a_intake_classifier.py -q` are green.
- `python3 -c "import json; json.load(open('config/agentpulse-config.json'))"` parses; the `synthesis` block carries all eight D-09 keys.
- Code review: no `routed_llm_call` in any synthesis function; no `api.anthropic.com` literal; the only economy_map write is the purpose-scoped draft INSERT (no blocks/published mutation).
</verification>

<success_criteria>
- The synthesis config block and working-default synth_identity.md exist with the documented keys/structure.
- load_synth_identity hot-reloads on mtime and fails loud (None) on missing/empty.
- The eligibility predicate, input assembly (with fail-loud cap), Sonnet call (via /anthropic/v1/messages), and output validator are implemented and unit-tested.
- All Phase-7 unit tests pass standalone and under pytest.
</success_criteria>

<output>
Create `.planning/phases/07-synthesis-loop-core/07-01-SUMMARY.md` when done.
</output>
