---
phase: 07-synthesis-loop-core
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - docker/processor/agentpulse_processor.py
  - tests/test_07_synthesis.py
autonomous: true
requirements: [SYNT-01, SYNT-02, SYNT-03, SYNT-04, SYNT-05, SYNT-06]
must_haves:
  truths:
    - "A scheduled processor job (registered via the schedule library) runs the synthesis cycle autonomously, iterating all seven blocks"
    - "For each block the loop evaluates eligibility (no-draft guard + N/T predicate), and when eligible assembles input, makes ONE Sonnet call via the proxy, parses+validates output, and INSERTs exactly one block_body_versions row with status draft"
    - "An ineligible block is skipped; a per-block error (read/call/parse) is logged and skipped without aborting the whole cycle"
    - "Every successful synthesis writes one new draft row with populated body_md and a valid proposed_maturity, and synthesized_from_through set to the run wall-clock timestamp"
    - "Nothing about the live published row, blocks.maturity, or blocks.current_body_version_id changes (GATE-01 draft-only invariant upheld)"
    - "A missing/empty synth_identity.md aborts the cycle loudly (no voiceless synthesis); a missing agent key aborts the run loudly (no silent no-op)"
    - "The poller records a pipeline run via log_pipeline_start/log_pipeline_end with totals (eligible/synthesized/skipped/failed)"
  artifacts:
    - path: "docker/processor/agentpulse_processor.py"
      provides: "synthesize_block, synthesize_blocks_poller orchestrator, scheduled_synthesize_blocks wrapper, schedule registration in main()"
      contains: "def synthesize_blocks_poller"
    - path: "tests/test_07_synthesis.py"
      provides: "End-to-end poller test (eligible block => one draft INSERT, draft-only invariant, fail-loud identity/key paths)"
      contains: "synthesize_blocks_poller"
  key_links:
    - from: "main() schedule registration"
      to: "scheduled_synthesize_blocks"
      via: "schedule.every(<cadence>).do(scheduled_synthesize_blocks)"
      pattern: "do\\(scheduled_synthesize_blocks\\)"
    - from: "synthesize_blocks_poller"
      to: "economy_map_insert_block_body_version"
      via: "one draft INSERT per eligible+synthesized block"
      pattern: "economy_map_insert_block_body_version"
    - from: "synthesize_block"
      to: "synthesis_sonnet_call"
      via: "single editorial call per eligible block"
      pattern: "synthesis_sonnet_call"
---

<objective>
Wire the Plan-01 primitives into the autonomous synthesis loop: a per-block `synthesize_block` (eligibility → assemble → ONE Sonnet call → parse/validate → draft INSERT), an orchestrating `synthesize_blocks_poller` that iterates the seven blocks fail-loud, a thin `scheduled_synthesize_blocks` wrapper, and its registration on the processor `schedule`. End-to-end tests prove the draft-only invariant (GATE-01) and the fail-loud guards.

Purpose: This is the autonomous editorial spine turning on. It completes SYNT-01/02 (trigger eval on a schedule), SYNT-03/04 (assemble + single proxy Sonnet call), and SYNT-05/06 (hot-reloaded identity → body_md + proposed_maturity), and it lands the artifact Phase 8 (sentinels) and Phase 9 (/map-approve) consume — a `status='draft'` block_body_versions row — WITHOUT touching the published row or blocks.* (the autonomy boundary; GATE-01 must not be violated here).

Output: the synthesis poller + scheduled job in the processor, plus end-to-end poller tests appended to tests/test_07_synthesis.py.

This plan depends on 07-01 (the helpers it composes). Both plans modify agentpulse_processor.py and tests/test_07_synthesis.py, so 07-02 runs in Wave 2 after 07-01 (file-overlap → sequential).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-synthesis-loop-core/07-CONTEXT.md
@.planning/phases/07-synthesis-loop-core/07-RESEARCH.md
@.planning/phases/07-synthesis-loop-core/07-PATTERNS.md
@.planning/phases/07-synthesis-loop-core/07-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: synthesize_block + synthesize_blocks_poller orchestrator (fail-loud, draft-only)</name>
  <read_first>
    - docker/processor/agentpulse_processor.py (the file being modified): the Plan-01 helpers (load_synth_identity, the economy_map block/draft/entries/body reads, economy_map_insert_block_body_version, is_block_eligible, assemble_synthesis_input, parse_synthesis_output, synthesis_sonnet_call); the structural analog `classify_intake_poller` (:3234-3298) for guard/config/key-check/per-block-try-except/run-logging; `classify_intake_for_edition` (:3073-3233) for the per-item processing shape; `log_pipeline_start`/`log_pipeline_end` (:3252, :3293-3296); `get_full_config` (:542); `_get_agent_api_key` (:6467-6479)
    - 07-RESEARCH.md § "System Architecture Diagram" and "Recommended Project Structure" (the synthesize_block / synthesize_blocks_poller shape), § "Common Pitfalls" 2/3/5
    - 07-PATTERNS.md § (e) "Orchestrator poller" (guard + per-block try/except + run logging, mirroring :3242-3296)
    - .planning/REQUIREMENTS.md GATE-01 (the draft-only invariant this loop must not violate)
  </read_first>
  <action>
    Add two top-level functions to docker/processor/agentpulse_processor.py:
    1. `synthesize_block(block, cfg, identity_text) -> dict` — for ONE block: read its open-draft flag (block_has_open_draft) and its new entries (created_at > last_synthesized_at, NULL => all); compute eligibility via `is_block_eligible(...)` using `N`/`T_days` from cfg; if ineligible return a `{skipped: 'ineligible'}`-style result (no call); if eligible: fetch prior published body_md (None on cold-start), `assemble_synthesis_input(...)`, make the single `synthesis_sonnet_call(identity_text, prompt, cfg)`, `parse_synthesis_output(...)`, then `economy_map_insert_block_body_version({block_slug, body_md, proposed_maturity, synthesized_from_through})` where `synthesized_from_through = datetime.now(timezone.utc).isoformat()` (the RUN timestamp, not the newest entry's date — Pitfall 5). Return a structured result (slug, status, version id, omitted_count). NEVER write blocks.maturity / blocks.current_body_version_id / a published row (GATE-01).
    2. `synthesize_blocks_poller() -> dict` — orchestrator mirroring classify_intake_poller: guard `if not supabase: return {'error': ...}`; read `cfg = get_full_config().get('synthesis', {})`; if `not cfg.get('enabled', True)` log + return `{disabled: True}`; `identity_text = load_synth_identity()` — if None, `logger.error` + (run logged failed) + return (fail-loud D-11, never voiceless); if `not _get_agent_api_key()` log + `log_pipeline_end(run_id, 'failed', ...)` + return (fail-loud missing key, never silent no-op); `run_id = log_pipeline_start('synthesize_blocks')`; fetch all seven blocks; iterate each in its own try/except (one bad block never aborts the cycle — mirror :3278-3291), accumulating totals {eligible, synthesized, skipped, failed}; `log_pipeline_end(run_id, 'completed', totals)`; return totals. Iterate all 7 blocks (low volume).
    Syntax-check: `python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"`.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read()); print('parses')"</automated>
    <automated>grep -q "def synthesize_block" docker/processor/agentpulse_processor.py && grep -q "def synthesize_blocks_poller" docker/processor/agentpulse_processor.py && echo "poller defined"</automated>
    <automated>grep -q "log_pipeline_start('synthesize_blocks')" docker/processor/agentpulse_processor.py && echo "run logging wired"</automated>
    <automated>grep -A40 "def synthesize_block" docker/processor/agentpulse_processor.py | grep -Eq "current_body_version_id\s*=|blocks.*maturity\s*=" && echo "WARN: possible blocks mutation" || echo "no blocks.* write in synthesize_block"</automated>
  </verify>
  <acceptance_criteria>
    - `synthesize_block` makes at most ONE synthesis_sonnet_call per invocation, and only when the block is eligible (no-draft guard + N/T predicate); ineligible blocks return a skip result with zero calls (SYNT-01/D-03).
    - On success it calls economy_map_insert_block_body_version exactly once with `synthesized_from_through` equal to the run wall-clock ISO timestamp (Pitfall 5), and makes no write to blocks or any published row (GATE-01).
    - `synthesize_blocks_poller` returns `{disabled: True}` when synthesis.enabled is false; aborts loudly (logged + run marked failed/returned) when load_synth_identity() is None (D-11) or the agent key is missing (fail-loud); never proceeds voiceless or keyless.
    - A per-block exception is caught, logged, counted as failed, and the loop continues to the remaining blocks (mirror :3278-3291) — verified by the Task 2 test stubbing one block read to raise.
    - The poller wraps its run in log_pipeline_start('synthesize_blocks') / log_pipeline_end with totals.
    - `python3 -c "import ast; ast.parse(...)"` passes.
  </acceptance_criteria>
  <done>synthesize_block and synthesize_blocks_poller exist, compose the Plan-01 helpers, enforce the no-draft + N/T eligibility and the fail-loud identity/key guards, write draft-only (no blocks/published mutation), and record a pipeline run.</done>
</task>

<task type="auto">
  <name>Task 2: Scheduled wrapper + registration; end-to-end poller tests (draft-only + fail-loud)</name>
  <read_first>
    - docker/processor/agentpulse_processor.py (the file being modified): `scheduled_classify_intake` (:9834-9845) thin try/except wrapper; the schedule registration block in main() (:10238-10297, intake registered :10261-10263); the Friday newsletter slots to avoid (:10255-10258)
    - tests/test_07_synthesis.py (the file being modified — the Plan-01 harness, _FakeResponse Anthropic shape, _install_proxy_stub, the httpx.get/post capture stubs)
    - tests/test_05a_intake_classifier.py (poller-level test shape: stub reads, invoke poller, assert writes)
    - 07-RESEARCH.md § "Pattern 5: scheduled poller orchestration" and § "Open Questions" 2 (cadence: daily or every 6h, avoid Friday newsletter slots)
    - .planning/REQUIREMENTS.md GATE-01
  </read_first>
  <action>
    1. Add `scheduled_synthesize_blocks()` — a thin try/except wrapper around `synthesize_blocks_poller()` that logs the result and logs+swallows exceptions (clone scheduled_classify_intake at :9834).
    2. Register it in main() near :10263 with `schedule.every(...).do(scheduled_synthesize_blocks)` at a cadence that does NOT collide with the Friday 08:30/11:00/12:00 newsletter slots and is not the same minute as the 30-min intake job — recommend a daily slot or every 6 hours (SYNT-02, Claude's discretion). Document the chosen cadence in a code comment citing SYNT-02.
    3. Append end-to-end tests to tests/test_07_synthesis.py driving `synthesize_blocks_poller` with stubbed `proc.httpx.get` (canned blocks/entries/draft/body rows) and stubbed `proc.httpx.post` (capturing the Sonnet call + the INSERT), and `load_synth_identity`/`_get_agent_api_key` stubbed:
       - eligible cold-start block (NULL watermark, >=5 entries, no draft) => exactly ONE Sonnet POST to /anthropic/v1/messages AND exactly ONE INSERT POST to /block_body_versions with status omitted; assert NO POST/PATCH targets /blocks and no published-row write (GATE-01 draft-only invariant).
       - block with an existing open draft => zero Sonnet calls, zero INSERTs (D-03 guard).
       - load_synth_identity() returns None => poller aborts loudly, zero Sonnet calls (D-11).
       - missing agent key => poller aborts loudly, zero Sonnet calls.
       - one block's read raises => that block counted failed, remaining blocks still processed (per-block isolation).
    Syntax-check the processor, then run the suite.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read()); print('parses')"</automated>
    <automated>grep -q "def scheduled_synthesize_blocks" docker/processor/agentpulse_processor.py && grep -Eq "do\(scheduled_synthesize_blocks\)" docker/processor/agentpulse_processor.py && echo "scheduled + registered"</automated>
    <automated>cd /root/bitcoin_bot && python3 tests/test_07_synthesis.py</automated>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_07_synthesis.py tests/test_05a_intake_classifier.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `scheduled_synthesize_blocks` exists and is registered exactly once in main() via `schedule.every(...).do(scheduled_synthesize_blocks)` with a cadence comment citing SYNT-02 and avoiding the Friday newsletter slots.
    - The eligible-cold-start test asserts exactly one POST to /anthropic/v1/messages and exactly one POST to /block_body_versions (status key absent), and asserts zero requests target /blocks or mutate a published row — proving GATE-01.
    - The open-draft test asserts zero Sonnet calls and zero INSERTs (D-03).
    - The None-identity and missing-key tests assert the poller returns early with zero Sonnet calls (fail-loud).
    - The raising-block test asserts the cycle still processes the other blocks (per-block try/except isolation).
    - `python3 -m pytest tests/test_07_synthesis.py tests/test_05a_intake_classifier.py -q` is green (no regression to the Phase-5 intake test).
  </acceptance_criteria>
  <done>The synthesis poller is scheduled in the processor and the end-to-end tests prove: one eligible block => one draft row (no blocks/published mutation), the no-draft guard, the fail-loud identity/key aborts, and per-block error isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schedule loop → autonomous LLM spend | the scheduled job triggers Sonnet (priciest model) calls without human prompting |
| processor → economy_map (service_role) | the per-block draft INSERT crosses into the append-only schema |
| synthesis output → published surface | the draft must NOT reach the live published row/blocks.* (autonomy boundary, GATE-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-GATE | Tampering / Elevation | draft-only invariant (GATE-01) | mitigate | synthesize_block writes ONLY a status=draft block_body_versions row via the purpose-scoped insert helper; never touches blocks.maturity/current_body_version_id or a published row; the append-only DB trigger (033 §8) backstops pinned columns; an end-to-end test asserts zero /blocks writes per cycle |
| T-07-COST | Denial of Service / cost | autonomous Sonnet spend | mitigate | per-block no-draft guard caps calls to <=7/cycle; proxy wallet reserve/settle + governance enforce budget; eligibility predicate prevents re-synthesizing already-drafted blocks |
| T-07-AVAIL | Denial of Service | voiceless/keyless run | mitigate | poller aborts loudly (logged, run marked failed) when identity is None (D-11) or the agent key is missing — never a silent no-op (fail-loud governance, MEMORY) |
| T-07-ISO | Availability | one bad block aborting the cycle | mitigate | per-block try/except so a single read/call/parse failure is logged + counted + skipped, not fatal to the cycle |
| T-07-DUP | Tampering / Info | duplicate drafts piling up | mitigate | D-03 no-draft eligibility guard (one in-flight draft per block); dovetails with Phase-9 reject re-eligibility |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — Phase 7 installs no packages; no install task, no slopcheck checkpoint required |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"` passes.
- `python3 tests/test_07_synthesis.py` and `python3 -m pytest tests/test_07_synthesis.py tests/test_05a_intake_classifier.py -q` are green.
- Code review: synthesize_block writes only the draft row (no blocks/published mutation) — GATE-01 upheld; the poller fails loud on None identity and missing key; per-block try/except isolates failures; the scheduled job is registered once and avoids the newsletter slots.
- (Deploy is out of scope for this plan — rebuild/smoke is handled by the operator-approved processor rebuild per CLAUDE.md `docker compose up -d --build processor`, after the syntax gate and green tests.)
</verification>

<success_criteria>
- A scheduled `synthesize_blocks` job runs the autonomous per-block loop on the processor schedule (SYNT-02).
- Eligible blocks produce exactly one draft block_body_versions row each (body_md + valid proposed_maturity), with the published row and blocks.* untouched (GATE-01 / SYNT-01..06).
- Fail-loud guards (None identity, missing key) and per-block error isolation are proven by end-to-end tests.
- The full synthesis test suite and the Phase-5 intake suite pass together.
</success_criteria>

<output>
Create `.planning/phases/07-synthesis-loop-core/07-02-SUMMARY.md` when done.
</output>
