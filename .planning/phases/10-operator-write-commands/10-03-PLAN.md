---
phase: 10-operator-write-commands
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - docker/processor/agentpulse_processor.py
autonomous: false
requirements: [CMD-07]

must_haves:
  truths:
    - "A pending synth-request is drained within the poll interval and runs synthesize_block bypassing N/T eligibility (SC3, D-01)"
    - "The forced synth NEVER bypasses the open-draft invariant — block_has_open_draft stays the guard (D-02)"
    - "On success the request row is marked done with the resulting version_id; on failure it is marked failed with the error — queryable, never a silent drop (D-03)"
    - "A lost open-draft race surfaces as a logged benign skip (23505), never a fail-loud abort (D-07)"
  artifacts:
    - path: "docker/processor/agentpulse_processor.py"
      provides: "synth-request drain poller, scheduler registration, force-synth bypass, 23505 benign-skip"
      contains: "synth_request"
  key_links:
    - from: "scheduled drain wrapper"
      to: "economy_map.synth_requests"
      via: "GET status=eq.pending then synthesize_block force then UPDATE status done/failed"
      pattern: "synth_request"
    - from: "synthesize_block INSERT"
      to: "uq_block_body_versions_one_open_draft"
      via: "23505 unique-violation caught as logged benign skip"
      pattern: "23505"
---

<objective>
Add the processor-side cross-service synth mechanism for `/map-synth` (CMD-07): a short-interval `schedule` drain job that reads pending `economy_map.synth_requests`, runs `synthesize_block` bypassing the N/T eligibility thresholds (D-01) but NEVER the open-draft invariant (D-02), and writes the request's terminal `done`/`failed` status fail-loud (D-03); plus a `23505` benign-skip around the draft INSERT in `synthesize_block` (D-07).

Purpose: the processor is a pure `schedule` loop with no HTTP surface, so the only cross-service trigger is DB-polling (D-01). This closes the SC3 loop: gato_brain enqueues (Plan 02), the processor drains and produces a `draft`.
Output: processor.py with the drain poller, scheduler registration, force-synth bypass, and 23505 handling, rebuilt scoped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-operator-write-commands/10-CONTEXT.md
@.planning/phases/10-operator-write-commands/10-PATTERNS.md
@docker/processor/agentpulse_processor.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add a force bypass to synthesize_block + 23505 benign-skip around the draft INSERT (D-01/D-02/D-07)</name>
  <files>docker/processor/agentpulse_processor.py</files>
  <read_first>
    - docker/processor/agentpulse_processor.py:3587-3663 (`synthesize_block` — the eligibility short-circuit at 3619 `if not is_block_eligible(...)`, the post-eligibility try block 3631-3654, and the `economy_map_insert_block_body_version(...)` call at 3646 wrapped in `raise BlockSynthesisError(...)`)
    - docker/processor/agentpulse_processor.py:3124-3173 (`block_has_open_draft` — the cheap fast-path that MUST stay as the open-draft guard, D-02)
    - docker/processor/agentpulse_processor.py:3174-3204 (`economy_map_insert_block_body_version` — the INSERT that the unique index rejects on a lost race; currently RAISEs `RuntimeError(... resp.text)` on non-(200,201), and PostgREST returns 23505/409 + "duplicate key" in resp.text)
    - .planning/phases/10-operator-write-commands/10-PATTERNS.md (§"Synth-request drain poller" point 3 — the force=True option; §"The 23505 site + benign-skip")
  </read_first>
  <action>
    In `docker/processor/agentpulse_processor.py`:
    1. Add a `force: bool = False` parameter to `synthesize_block(block, cfg, identity_text, force=False)`. When `force=True`, skip ONLY the `is_block_eligible(...)` short-circuit branch at 3619 (the N/T predicate — D-01 "ignore N and T") while STILL computing `has_draft = block_has_open_draft(slug)` and STILL honoring it: if `has_draft` is True even under force, return the skip result (the open-draft invariant is NEVER bypassed — D-02; gato_brain already refuses up front, this is the defense-in-depth backstop). Concretely: under force, bypass the eligibility predicate but keep the `block_has_open_draft` check as a hard guard. Default callers (the autonomous `synthesize_blocks_poller`) pass nothing so `force=False` and behavior is identical.
    2. Add `23505` benign-skip handling around the `economy_map_insert_block_body_version(...)` call at ~3646: catch the `RuntimeError` raised by the INSERT helper, detect the `"23505"` / `"duplicate key"` marker in its message, and convert it to a logged benign skip (`logger.info` "race lost, open draft already exists, skipping" and return `{'slug': slug, 'status': 'skipped', 'reason': 'race-lost-open-draft'}`), NEVER a fail-loud abort (D-07). A non-23505 RuntimeError still raises `BlockSynthesisError` as today. This is the structural backstop the `/map-synth` + scheduled-poller concurrency now requires (paired with migration 041's unique index). NOTE: `'skipped'` here is an in-memory return value of `synthesize_block` (a Python dict the drain poller inspects in Task 2) — it is NOT a `synth_requests.status` value and is never written to the DB as a status.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())" && grep -q "def synthesize_block(block: dict, cfg: dict, identity_text: str, force" docker/processor/agentpulse_processor.py && grep -q "23505" docker/processor/agentpulse_processor.py && grep -q "race-lost-open-draft" docker/processor/agentpulse_processor.py && echo BYPASS_AND_SKIP_OK</automated>
  </verify>
  <acceptance_criteria>
    - `synthesize_block` accepts a `force: bool = False` parameter; default behavior is byte-for-byte the existing autonomous path.
    - Under `force=True`, the `is_block_eligible` N/T predicate is bypassed but `block_has_open_draft` is still checked and still returns a skip when a draft exists (D-02).
    - The `economy_map_insert_block_body_version` call is wrapped to detect a `23505`/`duplicate key` violation and return `{'status': 'skipped', 'reason': 'race-lost-open-draft'}` with a logged info line — never a raised abort (D-07).
    - The `'skipped'` value is a `synthesize_block` in-memory return only; it is NOT written to `synth_requests.status` (which migration 040's CHECK pins to {pending, processing, done, failed}).
    - Non-23505 INSERT failures still raise `BlockSynthesisError` (fail-loud preserved).
    - `python3 -c "import ast; ast.parse(...)"` passes.
  </acceptance_criteria>
  <done>synthesize_block has a force bypass that respects the open-draft guard, and the draft INSERT converts a 23505 race-loss to a logged benign skip.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the synth-request drain poller (fail-loud, per-request isolation, queryable terminal status)</name>
  <files>docker/processor/agentpulse_processor.py</files>
  <read_first>
    - docker/processor/agentpulse_processor.py:3666-3759 (`synthesize_blocks_poller` — the EXACT fail-loud skeleton to mirror: `if not supabase` guard, config-enable check, `log_pipeline_start` FIRST, `load_synth_identity()` None then fail-loud, `_get_agent_api_key()` missing then fail-loud, per-item try/except isolation, `log_pipeline_end`)
    - docker/processor/agentpulse_processor.py:3088 region and 3174-3204 (the `_economy_map_get`-style read helper and the direct-PostgREST writer idiom for reading pending requests and UPDATEing the request row's status)
    - docker/processor/agentpulse_processor.py:573-610 (`economy_map_insert_timeline_entry` — the `Content-Profile: economy_map` + PostgREST write idiom for the request-row status UPDATE, if a helper is needed)
    - supabase/migrations/040_operator_write_commands_schema.sql (the `synth_requests` table — its `status` CHECK constraint accepts ONLY {pending, processing, done, failed}, plus `version_id` and `error` columns; Plan 01 Task 1)
    - .planning/phases/10-operator-write-commands/10-PATTERNS.md (§"Synth-request drain poller" — the four drain-poller adaptations)
  </read_first>
  <action>
    Add a new function `synth_request_drain_poller() -> dict` to `docker/processor/agentpulse_processor.py`, mirroring `synthesize_blocks_poller`'s fail-loud skeleton EXACTLY, differing only as the drain requires:
    1. Same preamble: `if not supabase` guard; `cfg = get_full_config().get('synthesis', {})` + config-enable check; `run_id = log_pipeline_start('synth_request_drain')` FIRST; `load_synth_identity()` None then fail-loud (`log_pipeline_end(run_id, 'failed', ...)`, never synthesize voiceless); `_get_agent_api_key()` missing then fail-loud. A failed forced-synth must be queryable, never a silent drop (D-03 — the "wallet bug" lesson).
    2. Read PENDING requests via the direct-PostgREST read idiom: GET `economy_map.synth_requests` filtered `status=eq.pending`, `order=created_at.asc`, selecting id + block_slug. (Direct PostgREST + `Accept-Profile: economy_map` only — NEVER supabase-py `.in_()`/`.schema()`/`.rpc()`.)
    3. For each request, in its OWN try/except (per-request isolation, mirroring the per-block isolation at 3737): UPDATE the row to `status='processing'`; fetch the target block by slug (filter `fetch_economy_map_blocks()` to the slug or a single-slug GET); call `synthesize_block(block, cfg, identity_text, force=True)` (bypass eligibility, D-01; the open-draft guard + 23505 backstop from Task 1 stay intact, D-02/D-07). The request row's `status` is constrained by migration 040's CHECK to EXACTLY one of {`pending`, `processing`, `done`, `failed`} — NEVER write any other value (writing e.g. `'skipped'` would itself raise a 23514 CHECK violation and make the failed forced-synth invisible, directly violating the D-03 fail-loud-governance contract). Map the three outcomes explicitly onto that closed set:
       - **Genuinely synthesized** (`synthesize_block` returns a `'synthesized'` result): UPDATE the row to `status='done'` with the resulting `version_id` and `updated_at`. Reserve `'done'` for this case ONLY.
       - **Skipped synthesis — open draft present / lost 23505 race** (`synthesize_block` returns the `{'status': 'skipped', 'reason': 'race-lost-open-draft'}` in-memory result, OR a `block_has_open_draft` skip): UPDATE the row to `status='failed'` with a non-null, explanatory, operator-queryable `error` string, e.g. `error='open draft already exists — no synthesis performed; approve or reject the existing draft via /map-pending'`. Do NOT invent a `'skipped'` status value and do NOT leave the request `processing` or `pending` — the skip lands as a terminal `failed` with an explanatory error so the operator can see exactly why no new draft appeared (D-03: queryable, never silent).
       - **Genuine synthesis error** (the per-request try/except catches an exception): UPDATE the row to `status='failed'` with `error=str(e)`, `logger.error(..., exc_info=True)`, and continue the loop.
    4. `log_pipeline_end(run_id, 'completed', totals)` at the end; on an outer failure, `log_pipeline_end(run_id, 'failed', {'error': str(e)})`.
    Use the project's existing direct-PostgREST write idiom (`Content-Profile: economy_map`, service_role headers, fail-loud on non-2xx) for the request-row status UPDATEs; add a small purpose-scoped writer if needed (mirror `economy_map_insert_*`). The exact `done`/`failed` column semantics follow migration 040's `synth_requests` shape; every status write MUST be a member of {pending, processing, done, failed}.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())" && grep -q "def synth_request_drain_poller" docker/processor/agentpulse_processor.py && grep -q "log_pipeline_start('synth_request_drain')" docker/processor/agentpulse_processor.py && grep -q "force=True" docker/processor/agentpulse_processor.py && grep -q "status=eq.pending" docker/processor/agentpulse_processor.py && echo DRAIN_POLLER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `synth_request_drain_poller()` exists and reuses the exact fail-loud preamble (`if not supabase`, config-enable, `log_pipeline_start` FIRST, `load_synth_identity` None then fail-loud, `_get_agent_api_key()` missing then fail-loud).
    - Reads pending requests via direct PostgREST (`status=eq.pending`, `Accept-Profile: economy_map`), NEVER supabase-py.
    - Calls `synthesize_block(..., force=True)` so N/T are bypassed (D-01) while the open-draft guard/23505 backstop stay intact (D-02/D-07).
    - Every `synth_requests.status` value written is a member of {`pending`, `processing`, `done`, `failed`} — NO status value outside that set is ever written (no `'skipped'`/`'queued'`/etc.), so no write can hit migration 040's CHECK constraint (which would make a failure invisible, violating D-03).
    - A genuinely-synthesized draft sets `status='done'` with the resulting `version_id`; `'done'` is used for no other outcome.
    - The open-draft / lost-race "skipped synthesis" case lands as `status='failed'` with a non-null, explanatory `error` string (the operator can query why no draft appeared) — never `processing`, never `pending`, never a silent drop (D-03).
    - A genuine synthesis exception sets `status='failed'` with `error=str(e)`, per-request isolated.
    - `python3 -c "import ast; ast.parse(...)"` passes.
  </acceptance_criteria>
  <done>The drain poller exists, fail-loud, draining pending requests with force-bypass and writing a queryable terminal status (done with version_id, or failed with an explanatory error) per request — only ever within {pending, processing, done, failed}.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Register the drain scheduler wrapper + schedule.every(...).seconds; rebuild processor</name>
  <files>docker/processor/agentpulse_processor.py</files>
  <read_first>
    - docker/processor/agentpulse_processor.py:10594 region (`scheduled_synthesize_blocks` — the thin try/except wrapper analog) and 10580 (`scheduled_classify_intake`)
    - docker/processor/agentpulse_processor.py:10999-11064 (the `schedule.every(...)` registration block — short-interval examples: `.every(30).minutes`, `.every(5).minutes`, `.every(10).minutes`)
    - .planning/phases/10-operator-write-commands/10-PATTERNS.md (§"Scheduler registration")
    - CLAUDE.md (Build & Run — `cd /root/bitcoin_bot/docker && docker compose up -d --build processor`)
  </read_first>
  <action>
    In `docker/processor/agentpulse_processor.py`:
    1. Add a thin scheduler wrapper `scheduled_synth_request_drain()` mirroring `scheduled_synthesize_blocks` (try/except that calls `synth_request_drain_poller()`, logs the result, and logs any exception — never lets the scheduler thread die).
    2. Register it in the `schedule.every(...)` block (~10999-11064) with a short interval: `schedule.every(<30-60>).seconds.do(scheduled_synth_request_drain)` (`schedule` supports `.seconds`). The exact cadence is Claude's Discretion per D-01 (30-60s); pick one and ensure it matches the "~Ns" wording the gato_brain ack uses (Plan 02 Task 2 cites this). D-01 permits reusing an existing tick, but a dedicated short job is the cleanest match to the drain intent.
    3. Rebuild ONLY the processor via the scoped pattern: `cd /root/bitcoin_bot/docker && docker compose up -d --build processor` (Plan 01's migrations — the `synth_requests` table — must be live first). Confirm the container is healthy and the drain job fires on its interval (check the processor log).
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())" && grep -q "def scheduled_synth_request_drain" docker/processor/agentpulse_processor.py && grep -Eq "schedule.every\([0-9]+\).seconds.do\(scheduled_synth_request_drain\)" docker/processor/agentpulse_processor.py && echo SCHEDULER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scheduled_synth_request_drain()` wrapper exists and swallows exceptions (logs, never propagates to the scheduler thread).
    - A `schedule.every(<n>).seconds.do(scheduled_synth_request_drain)` line is registered with n in 30-60.
    - The chosen cadence matches the "~Ns" wording in gato_brain's `/map-synth` ack (Plan 02).
    - Processor rebuilt via scoped `docker compose up -d --build processor`, healthy, and the drain job is observed firing in the log.
    - `python3 -c "import ast; ast.parse(...)"` passes.
  </acceptance_criteria>
  <done>The drain wrapper is registered on a short `schedule` interval and the processor is rebuilt and running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synth_requests queue → synthesis | Operator-enqueued requests drive a Sonnet synthesis call bypassing the autonomous N/T gate |
| service_role → economy_map DB | Drain writes (request status, draft INSERT) execute as service_role, bypassing RLS — enforcement is the trigger + unique index, never RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Elevation of Privilege | force-synth bypass | mitigate | `force=True` bypasses ONLY the N/T eligibility predicate (D-01). `block_has_open_draft` stays a hard guard and the human-approval gate (draft-then-approve) is untouched — "ignore N and T" never reaches the publish path. |
| T-10-12 | Denial of Service | concurrent drain + autonomous poller | mitigate | `uq_block_body_versions_one_open_draft` (migration 041) + the 23505 benign-skip (Task 1) make the check-then-act race safe: the loser logs a skip, never a crash or a second open draft. |
| T-10-13 | Repudiation | failed forced synth | mitigate | Every request gets a terminal `done`/`failed` status with `version_id`/`error` and a `pipeline_runs` row (`log_pipeline_start`/`end`) — a failed forced-synth is queryable, never a silent drop (D-03, fail-loud governance). The open-draft skip lands as `failed` with an explanatory error (NOT a 'skipped' value that would violate migration 040's CHECK and itself become invisible). |
| T-10-14 | Tampering | request-row status writes | mitigate | Direct PostgREST with `Content-Profile: economy_map`, parameterized JSON, fail-loud on non-2xx; NEVER supabase-py `.in_()`/`.schema()`/`.rpc()` (silent-failure rule). Every status write is a member of migration 040's CHECK set {pending, processing, done, failed} so no write can be rejected as a constraint violation. |
| T-10-SC | Tampering | npm/pip installs | mitigate | No new package installs in this plan (uses existing `httpx`, `schedule`); slopcheck N/A. |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(...)"` passes after each task.
- `synthesize_block` force bypass respects the open-draft guard; 23505 → logged skip.
- Drain poller is fail-loud, per-request isolated, marks queryable terminal status — only ever within {pending, processing, done, failed}; the open-draft skip lands as `failed` with an explanatory error.
- Scheduler wrapper registered on a 30-60s interval; processor rebuilt scoped and healthy.
- End-to-end (end-of-phase): `/map-synth <slug>` enqueues a request; within the interval the processor drains it and a `draft` appears in `/map-pending`; the request row reads `done` with the version_id. A second `/map-synth` while a draft is open is refused (gato_brain) and the backstop holds (no duplicate draft); were one to slip through, the lost-race request reads `failed` with the explanatory open-draft error.
</verification>

<success_criteria>
- CMD-07 end-to-end: a forced synth-request is drained and produces a `draft` within the call/poll window, ignoring N/T but never the open-draft invariant (SC3).
- A failed or skipped forced-synth is queryable via the request row (`status='failed'` + explanatory `error`) + pipeline_runs (D-03).
</success_criteria>

<output>
Create `.planning/phases/10-operator-write-commands/10-03-SUMMARY.md` when done.
</output>
</output>
