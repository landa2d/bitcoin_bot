---
phase: 11-design-system-nav-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/web/site/style-base.css
  - docker/web/site/style-shared.css
  - docker/web/site/index.html
autonomous: true
requirements: [COLOR-01, COLOR-02, TYPE-01, TYPE-02, TYPE-03]
user_setup: []

must_haves:
  truths:
    - "A single light-mode palette (warm off-white bg, surfaces, ink scale, one violet accent) is defined via CSS variables under :root and applied site-wide, replacing the dark map theme (COLOR-01)."
    - "Only one accent (#5b3df5 violet) exists in the token layer; no second brand color or per-tier accent is defined in style-base.css (COLOR-02)."
    - "Body and reading text render in Source Serif 4 with no body-level monospace; Courier New is no longer the body font (TYPE-01)."
    - "IBM Plex Mono is available as the --mono token reserved for UI chrome only; a single ~18px/~1.62 serif body style is the base (TYPE-02, TYPE-03)."
    - "The dark body.technical / body.strategic var blocks no longer exist in style-shared.css, so the new :root palette is not overridden (D-04 hard constraint)."
  artifacts:
    - path: "docker/web/site/style-base.css"
      provides: ":root light palette + spacing/radius tokens + serif body typography + display/eyebrow type classes"
      min_lines: 60
      contains: ":root"
    - path: "docker/web/site/index.html"
      provides: "Google-Fonts <link> + style-base.css linked first in <head>"
      contains: "style-base.css"
    - path: "docker/web/site/style-shared.css"
      provides: "dark var blocks deleted + Courier New body font removed"
  key_links:
    - from: "docker/web/site/index.html"
      to: "docker/web/site/style-base.css"
      via: "<link rel=stylesheet> loaded before style-shared.css"
      pattern: "style-base\\.css"
    - from: "docker/web/site/index.html"
      to: "fonts.googleapis.com"
      via: "Google Fonts <link> for Source Serif 4 + IBM Plex Mono weights 400;600"
      pattern: "Source\\+Serif\\+4"
---

<objective>
Establish the design-system foundation for the v2.0 redesign: a new `style-base.css` holding the single light-mode `:root` CSS-variable palette (COLOR-01/02), the Source Serif 4 / IBM Plex Mono typography system with serif 18px/1.62 body and the display/eyebrow type classes (TYPE-01/02/03), and the spacing/radius token set. Load it first in `index.html`, wire the Google-Fonts `<link>`, and — the hard, non-optional constraint — retire the dark `body.technical`/`body.strategic` var blocks and the body-level `Courier New` font in `style-shared.css` so the new palette and serif body actually take effect.

Purpose: Every later v2.0 phase (12 Newsletter, 13 Agent Economy grid, 14 About+polish) restyles on top of these tokens. Without retiring the dark mode-scoped var blocks, their higher specificity overrides `:root` and COLOR-01/TYPE-01 never apply — this is the highest-risk edit of the phase.
Output: New `docker/web/site/style-base.css`; modified `index.html` (`<head>` fonts + stylesheet order); modified `style-shared.css` (dark blocks + Courier body deleted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-design-system-nav-shell/11-UI-SPEC.md
@.planning/phases/11-design-system-nav-shell/11-PATTERNS.md
@.planning/phases/11-design-system-nav-shell/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author style-base.css — :root tokens + serif typography + display/eyebrow classes</name>
  <files>docker/web/site/style-base.css</files>
  <read_first>
    - .planning/phases/11-design-system-nav-shell/11-UI-SPEC.md (§Color for the 11 palette hexes + the exact variable names; §Typography for the serif 4-size / mono 3-size scale and the 400/600 weight policy; §Spacing for --space-xs..3xl values; §Radius for --radius / --radius-sm / --radius-btn / --radius-dot)
    - .planning/phases/11-design-system-nav-shell/11-PATTERNS.md (§"style-base.css (NEW)" — the :root intent block at lines 49-57, the body serif rule, and the .eyebrow / .page-title class intent at line 104)
    - docker/web/site/style-shared.css (lines 1-57 — the retired body.technical var block whose pattern the new :root replaces; read so you reproduce the cascade-correct structure)
  </read_first>
  <action>
    Create new file docker/web/site/style-base.css. Author a single `:root{}` block with the COLOR-01 light palette using these exact token names and hex values from UI-SPEC §Color: --bg:#faf8f5, --surface:#ffffff, --ink:#1a1916, --ink-soft:#55514a, --ink-faint:#8a857c, --line:#e7e2da, --line-strong:#d8d2c7, --accent:#5b3df5, --accent-soft:#efeaff, --accent-ink:#4a2fd6. Add the two font-stack tokens: --serif:'Source Serif 4', Georgia, serif and --mono:'IBM Plex Mono', ui-monospace, monospace. ONE accent only (COLOR-02) — do not introduce any teal, coral, gray, or per-tier accent token. In the same :root, add the spacing tokens from UI-SPEC §Spacing (--space-xs:4px, --space-sm:8px, --space-md:16px, --space-lg:24px, --space-xl:32px, --space-2xl:48px, --space-3xl:64px) and the radius tokens from UI-SPEC §Radius (--radius:10px, --radius-sm:7px, --radius-btn:8px, --radius-dot:3px).
    Add a `body{}` rule setting font-family:var(--serif); font-size:18px; line-height:1.62; background:var(--bg); color:var(--ink) — this is the TYPE-01/TYPE-03 serif body base (18px / 1.62 per UI-SPEC §Typography "Base body").
    Add the display/eyebrow type classes per D-02 (ship the CSS only; do NOT restructure any hero DOM): `.page-title` = font-family:var(--serif); font-size:clamp(30px,5vw,46px); font-weight:600; line-height:1.12; letter-spacing:-.015em. `.eyebrow` = font-family:var(--mono); font-size:11px; font-weight:600; text-transform:uppercase; letter-spacing:.2em; color:var(--accent-ink). Use weights 400 and 600 ONLY across every rule (UI-SPEC weight policy — 500 and 700 are removed). Hand-authored CSS, no build step (PATTERNS cross-cutting convention #1).
    Do NOT add nav-shell / tab / back-control styles here — those belong to Plan 02 in the same file; this task delivers tokens + body typography + display classes.
  </action>
  <verify>
    <automated>grep -q ":root" docker/web/site/style-base.css && grep -q "\-\-accent:#5b3df5" docker/web/site/style-base.css && grep -q "Source Serif 4" docker/web/site/style-base.css && grep -q "font-size:18px\|font-size: 18px" docker/web/site/style-base.css && grep -qv "00e5a0\|7c3aed\|Courier" docker/web/site/style-base.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docker/web/site/style-base.css` contains a `:root{` block including the literal substrings `--accent:#5b3df5`, `--accent-soft:#efeaff`, `--accent-ink:#4a2fd6`, `--bg:#faf8f5`, `--ink:#1a1916`, `--line-strong:#d8d2c7` (whitespace after the colon permitted).
    - File contains `--space-xs`, `--space-3xl`, `--radius`, `--radius-dot` and the `--serif` + `--mono` font-stack tokens.
    - File contains a `body` rule with `font-family:var(--serif)` (or `var(--serif)` in the font-family declaration) and `font-size:18px` and `line-height:1.62`.
    - File contains `.page-title` with `clamp(30px, 5vw, 46px)` (spaces optional) and `.eyebrow` with `font-family:var(--mono)` and `font-size:11px`.
    - File contains NO `00e5a0`, NO `7c3aed`, NO `Courier`, and NO per-tier accent token (no second hue).
  </acceptance_criteria>
  <done>style-base.css exists with the single light :root palette, spacing/radius tokens, serif 18px/1.62 body, and the .page-title / .eyebrow display classes; one accent only; weights limited to 400/600.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Google Fonts + load style-base.css first in index.html &lt;head&gt;</name>
  <files>docker/web/site/index.html</files>
  <read_first>
    - .planning/phases/11-design-system-nav-shell/11-UI-SPEC.md (§Font Loading — the locked Google Fonts <link> block, weights 400 & 600 only for both families; the rationale that CSP already whitelists these origins)
    - .planning/phases/11-design-system-nav-shell/11-PATTERNS.md (§"index.html" Edit 1, lines 121-132 — current <head> at lines 3-9, insert font <link> + style-base.css FIRST before style-shared.css)
    - docker/web/site/index.html (lines 1-9 — current <head>: title at line 6, style-shared.css at line 7, style-map.css at line 8)
    - docker/web/Caddyfile (line 19 — confirm CSP already whitelists fonts.googleapis.com / fonts.gstatic.com; do NOT edit this file)
  </read_first>
  <action>
    Edit docker/web/site/index.html `<head>` (lines 3-9). After the `<title>` (line 6) and before the existing `<link rel="stylesheet" href="/style-shared.css">` (line 7), insert: (a) the two preconnect links and the Google Fonts stylesheet `<link>` exactly as locked in UI-SPEC §Font Loading — `https://fonts.googleapis.com/css2?family=Source+Serif+4:ital,opsz,wght@0,8..60,400;0,8..60,600;1,8..60,400&family=IBM+Plex+Mono:wght@400;600&display=swap` (weights 400 & 600 ONLY — do NOT use the mockup's 400;500;600;700); then (b) `<link rel="stylesheet" href="/style-base.css">` so style-base.css loads FIRST, before style-shared.css and style-map.css. Load order is the cascade control (D-04 / PATTERNS convention #1) — style-base.css must precede the other two so its :root and serif body win.
    Do NOT edit the CSP — Caddyfile line 19 already whitelists `style-src ... https://fonts.googleapis.com` and `font-src https://fonts.gstatic.com` (UI-SPEC Font Loading rationale; zero infra change). Touch nothing else in this file in this plan — the nav/hero/back-link edits belong to Plan 02.
  </action>
  <verify>
    <automated>grep -q "Source+Serif+4" docker/web/site/index.html && grep -q "IBM+Plex+Mono:wght@400;600" docker/web/site/index.html && grep -q "style-base.css" docker/web/site/index.html && ! grep -q "400;500;600;700" docker/web/site/index.html && awk '/style-base.css/{b=NR} /style-shared.css/{s=NR} END{exit !(b>0 && b<s)}' docker/web/site/index.html && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` `<head>` contains the Google Fonts `<link>` whose href includes `family=Source+Serif+4` and `family=IBM+Plex+Mono:wght@400;600` and `display=swap`.
    - The font `<link>` does NOT request weights `500` or `700` (no `400;500;600;700` substring anywhere).
    - `index.html` contains `<link rel="stylesheet" href="/style-base.css">` and that line appears BEFORE the `style-shared.css` link line (cascade order).
    - Two `preconnect` links to fonts.googleapis.com and fonts.gstatic.com are present.
    - `docker/web/Caddyfile` is unchanged (no CSP edit).
  </acceptance_criteria>
  <done>The Google Fonts <link> (400;600 only) and style-base.css are in the <head>, with style-base.css loaded before style-shared.css; CSP untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Retire dark var blocks + Courier body in style-shared.css (hard constraint D-04)</name>
  <files>docker/web/site/style-shared.css</files>
  <read_first>
    - .planning/phases/11-design-system-nav-shell/11-PATTERNS.md (§"style-shared.css" Retire 1/2/3, lines 239-262 — exact line targets: dual dark palettes 7-49, Courier body line 52, .top-nav 77-83 + .back-link 243-250; the "Preserve" note: keep the * reset and .mode-transitioning)
    - .planning/phases/11-design-system-nav-shell/11-UI-SPEC.md (§"What This Replaces" — the migration inventory confirming the dark dual-palette + Courier + .top-nav + back-link rows are retired here)
    - docker/web/site/style-shared.css (lines 1-57 — the body.technical block 7-27, body.strategic block 29-49, body rule 51-57 with the Courier font on line 52 and min-height:100vh on line 56 to preserve)
  </read_first>
  <action>
    Edit docker/web/site/style-shared.css for three deletions (these are NOT optional — body.technical/.strategic specificity overrides :root so COLOR-01/TYPE-01 never take effect unless removed; per D-04 hard constraint):
    Retire 1 — delete the ENTIRE `body.technical { ... }` block (lines 7-27) AND the ENTIRE `body.strategic { ... }` block (lines 29-49). These are the teal `#00e5a0` / strategic `#7c3aed` dual-accent dark mode-flip.
    Retire 2 — in the `body { ... }` rule (lines 51-57), delete ONLY the `font-family: 'Courier New', monospace;` declaration (line 52). Keep the rest of the rule's properties (PATTERNS notes `min-height:100vh` at line 56 must survive; background/color/line-height also stay — style-base.css's body rule, loaded first but lower in source order is overridden, so leaving these harmless is fine, but you MUST remove the Courier font-family so it does not cascade as the page default per Retire 2).
    Retire 3 — delete the `.top-nav` family rules and the `.back-link` rule (PATTERNS cites .top-nav at 77-83 and .back-link at 243-250) since style-base.css's sticky header + back-control (Plan 02) supersede them; deletion is cleaner than leaving dead selectors.
    PRESERVE: the `*` reset (line 3) and `.mode-transitioning` (lines 68-73). Do NOT chase every other Courier/dark var reference elsewhere in this file — smallest-blast-radius rule (D-01/D-04); those component rules are owned by Phases 12-13 and may render rough locally between phases (acceptable, batch-deploy). Do NOT touch style-map.css at all this phase.
  </action>
  <verify>
    <automated>grep -vq "body.technical {" docker/web/site/style-shared.css && grep -vq "body.strategic {" docker/web/site/style-shared.css && grep -vq "Courier New" docker/web/site/style-shared.css && grep -q "mode-transitioning" docker/web/site/style-shared.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `style-shared.css` no longer contains the string `body.technical {` or `body.strategic {`.
    - `style-shared.css` no longer contains the string `Courier New` (verifies BOTH the body-level Courier and the .back-link Courier are gone, since .back-link is deleted).
    - `style-shared.css` no longer contains a `.top-nav` rule or a `.back-link` rule (grep `.top-nav` and `.back-link` selectors absent).
    - `style-shared.css` STILL contains the `* {` reset and `.mode-transitioning` (preserved).
    - No edits were made to `docker/web/site/style-map.css`.
  </acceptance_criteria>
  <done>The dual dark body.technical/.strategic var blocks, the body-level Courier New font, and the old .top-nav/.back-link rules are deleted from style-shared.css; the * reset and .mode-transitioning are preserved; style-map.css untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → static assets (Caddy) | The only boundary; all assets are static, served read-only. No user input crosses into the app in this plan. |
| browser → fonts.googleapis.com / fonts.gstatic.com | New external origin for font CSS/woff2 — already permitted by the existing CSP (Caddyfile:19). No new origin introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | Google Fonts <link> (third-party origin) | accept | Fonts are static, public, content-less assets already whitelisted by the existing CSP. No PII or app data sent beyond the standard font request the CSP already permits. No new origin added; no CSP change. Low/none. |
| T-11-02 | Tampering | style-base.css / style-shared.css edits | accept | CSS-only token + typography changes; no script, no data flow, no new DOM sinks. Read-only public restyle. No injection surface introduced. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan (vanilla-JS SPA, no build step, no new dependencies). Package legitimacy gate not applicable. |

No `high`-severity threats. This is a read-only public static-site restyle: no new user inputs, no auth, no backend/data-flow changes, CSP unchanged, no new external origin, no new innerHTML/inline-handler XSS sinks beyond the existing codebase pattern.
</threat_model>

<verification>
Local-only (D-01 batch deploy — NO production deploy this phase):
1. Syntax: CSS has no compiler; visually confirm style-base.css parses by loading the page (no FOUC/black-screen). index.html is valid HTML (the <link> insertions are well-formed).
2. Load the site locally (open docker/web/site/index.html via the local agentpulse-web container or a static server) and confirm: the page background is warm off-white (#faf8f5), body text renders in a serif (Source Serif 4), and NO black/dark background appears (proves the dark body.technical block is gone and :root won).
3. grep gates from each task's <verify> all return PASS.
</verification>

<success_criteria>
- ROADMAP success criteria 3 (typography) and 4 (color) are locally TRUE: light-mode palette applied site-wide via :root; serif body with no body-level monospace; one violet accent in the token layer.
- COLOR-01, COLOR-02, TYPE-01, TYPE-02, TYPE-03 satisfied at the token/foundation level.
- The dark mode-scoped var blocks and body-level Courier are deleted (D-04 hard constraint met).
- No production deploy performed (D-01).
</success_criteria>

<output>
Create `.planning/phases/11-design-system-nav-shell/11-01-SUMMARY.md` when done.
</output>
