---
phase: 11
slug: design-system-nav-shell
status: approved
shadcn_initialized: false
preset: none
created: 2026-06-04
reviewed_at: 2026-06-04
---

# Phase 11 — UI Design Contract

> Visual and interaction contract for the v2.0 Frontend Redesign foundation: the single light-mode CSS-variable palette, the Source Serif 4 / IBM Plex Mono typography system, and the persistent stateful 3-tab nav shell with back-arrow. Every later phase (12 Newsletter, 13 Agent Economy grid, 14 About + polish) restyles on top of this shell and inherits these tokens. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Stack note:** This is NOT a React/Tailwind/Next/shadcn project. It is a vanilla-JS single-page app — static `index.html` + hand-written CSS using CSS custom properties, served by Caddy as the `agentpulse-web` Docker container. Frontend lives entirely in `docker/web/site/`. The "design system" this phase establishes is a set of plain CSS variables, not a component-library theme. shadcn gate resolved to N. No `components.json` / `tailwind.config` / `package.json` exist (confirmed). Registry safety gate: **not applicable**.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (vanilla JS SPA, hand-authored CSS variables) |
| Preset | not applicable |
| Component library | none |
| Icon library | none — use Unicode glyphs already in the codebase (`←` U+2190 for back, `·` U+00B7 as metadata separator, `↗` for source links). Do not add an icon dependency. |
| Font (reading/titles) | **Source Serif 4** (`'Source Serif 4', Georgia, serif`) |
| Font (UI chrome only) | **IBM Plex Mono** (`'IBM Plex Mono', ui-monospace, monospace`) |
| Font loading | **Google Fonts via `<link>` in `index.html` `<head>`** (matches mockup; see Font Loading note below). |

### Font Loading (locked)

Use the Google Fonts stylesheet link, identical to the mockup except trimmed to the **two weights this contract actually uses (400, 600)** for both families:

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Source+Serif+4:ital,opsz,wght@0,8..60,400;0,8..60,600;1,8..60,400&family=IBM+Plex+Mono:wght@400;600&display=swap" rel="stylesheet">
```

**Rationale (load-bearing — do not change to self-hosting without a CSP edit):** The existing `docker/web/Caddyfile` Content-Security-Policy already whitelists `style-src ... https://fonts.googleapis.com` and `font-src https://fonts.gstatic.com`. Google Fonts therefore works with zero infra change. Self-hosting woff2 would require editing the CSP `font-src` to `'self'` and is explicitly out of scope for this UI pass. **Weights to load (exactly 2 per family):** Source Serif 4 — 400, 600 (+ italic 400); IBM Plex Mono — 400, 600. Weights 500 and 700 are deliberately NOT requested — see the weight policy in Typography.

---

## Spacing Scale

Source: mockup nav/section/card padding values + REDESIGN_BRIEF §6 ("tighten loose vertical gaps; minimalist but not sparse") + POLISH-01. **Every spacing value in this contract is a multiple of 4px** — chrome micro-paddings that the mockup expressed as non-multiples (nav `14px`, tab `7px/13px`, Subscribe `9px`, toggle `7px`) are snapped to the nearest 4px step below. There are no spacing exceptions.

Declared tokens (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| `--space-xs` | 4px | Progress-dot gaps, inline icon gaps, toggle inner padding |
| `--space-sm` | 8px | Compact element spacing, brand gap, tab gap → title gaps |
| `--space-md` | 16px | Default element gap; grid gaps; nav side-padding step; card-internal gap |
| `--space-lg` | 24px | Section side padding (`main`/`.nav` horizontal padding), card padding |
| `--space-xl` | 32px | Block/section vertical breaks, section-label top margin |
| `--space-2xl` | 48px | Major section breaks (e.g. above About note, large eyebrow top margin) |
| `--space-3xl` | 64px | Page-bottom breathing room (`main` bottom padding region) |

Chrome control paddings (literal values, all snapped to multiples of 4 — kept as literals rather than tokens because they are one-off control sizing, but they remain fully 4px-conformant):

| Element | Padding | Notes |
|---------|---------|-------|
| Nav vertical padding | `12px` (horizontal uses `--space-lg` 24px) | Snapped from mockup `14px`. Use `16px` only if the sticky bar reads too tight against the mockup. |
| Tab padding | `8px 12px` | Snapped from mockup `7px 13px`. |
| Subscribe button padding | `8px 16px` | Snapped from mockup `9px 16px`. |
| Toggle button padding | `8px 20px` | Snapped from mockup `7px 20px`. |
| Hairline borders | `1px` | Border width, not spacing — exempt from the 4px rule by definition. |

---

## Radius

Source: REDESIGN_BRIEF §6 + POLISH-01 ("~7–10px, consistent") + mockup `--radius:10px`.

| Token | Value | Usage |
|-------|-------|-------|
| `--radius` | 10px | Cards, the mode-toggle wrapper, dashed note boxes — the default "card" radius |
| `--radius-sm` | 7px | Tabs (`7px`), small inline pills, tags |
| `--radius-btn` | 8px | Subscribe button, toggle buttons |
| `--radius-dot` | 3px | Progress-dot pills (`3px`) |

Convention: nothing on the site uses a radius outside the 3 / 7 / 8 / 10px set. No fully-rounded (`50%`) shapes except the 9px brand `--dot`.

---

## Typography

**Two-family system per the brief — each family uses ≤4 sizes (serif: 4, mono: 3); 2 weights total (400, 600) shared across both families.** The brief mandates two type families with distinct jobs, so the contract gives EACH family a tight, defensible scale and verifies them per-family (not by counting globally).

- **Source Serif 4 = everything you READ** (body paragraphs + every title/heading).
- **IBM Plex Mono = UI chrome ONLY** (eyebrow/label, metadata like "Edition # · date", tab labels, brand wordmark, buttons, tags, code, section labels).
- **No monospace body paragraphs anywhere** (TYPE-01). A **single serif heading treatment** — the old second monospace heading style (`Courier New` uppercase accent `<h2>`/`<h3>`) is **removed** (TYPE-03).

Base body: **18px / line-height 1.62** serif (TYPE-03 target "~18px / ~1.6").

### Serif scale (4 sizes — Source Serif 4)

| Role | Size | Weight | Line Height | Notes |
|------|------|--------|-------------|-------|
| Body | 18px | 400 | 1.62 | Reading text, article/about paragraphs. `body { font-size:18px; line-height:1.62 }`. |
| List/card title | 20px | 600 | 1.25 | Single shared title size for the edition list `h3` AND economy-map card titles. `letter-spacing:-.01em`. (Consolidates the former 19–22px range to one value.) |
| Heading (in-content H2) | 24px | 600 | 1.2 | Single serif heading style for article/block sub-sections; only `margin` varies. The former 700 "emphasis" H2 is the SAME serif treatment at weight 600 — not a new size or weight. |
| Page title (display) | `clamp(30px, 5vw, 46px)` | 600 | 1.12 | Centered hero/section title; `letter-spacing:-.015em`. |

### Mono scale (3 sizes — IBM Plex Mono, chrome only)

| Role | Size | Weight | Notes |
|------|------|--------|-------|
| Eyebrow / kicker · Section label | 11px | 600 | UPPERCASE, `letter-spacing:.18–.22em`, accent or `--ink-faint`. Both the kicker and the mono group/section label live on this step. |
| Tab label · Button/Subscribe · Back-control | 12.5px | 400 (600 when active/primary) | Tab `letter-spacing:.02em`; Subscribe/button `letter-spacing:.06em` UPPERCASE; back-control "← Back to [section]". Rest state 400; the active tab, the Subscribe button, and any primary button render at 600. Back-control rest state is 400, → `--accent-ink` on hover (color change, not weight). |
| Brand wordmark · Metadata / page-sub · Code | 14px | 600 brand / 400 metadata & code | Brand `letter-spacing:.08em`, `--ink`, weight 600. Metadata ("Edition # · date", "synthesized … · block N of 8") and inline `code` sit on the same 14px step at weight 400, `--ink-faint`. (Metadata moved up from the former 13px to land on this consolidated step.) |

**Weight policy (exactly 2 weights total, shared across both families):**
- **400** — serif body; mono metadata; mono code; mono tab/back-control rest state.
- **600** — serif headings, list/card titles, page-title display; mono active tab; mono brand wordmark; mono Subscribe/primary buttons; mono eyebrow & section labels.
- **500 is removed** (former tab/back rest state now uses 400). **700 is retired** (the former article-H2 "emphasis" is the same serif heading at 600, scaled by margin — not a new weight). The Google Fonts `<link>` requests only 400 & 600 for both families accordingly.

---

## Color

A **single light-mode palette** as CSS variables, applied site-wide, **replacing the dark map theme** (COLOR-01). Hex values are locked verbatim from the REDESIGN_BRIEF §4 and mockup `:root`. **Exactly ONE accent** (violet) — no second brand color (COLOR-02). The old dual-accent system (technical teal `#00e5a0` / strategic `#7c3aed`) and the four-way tier-accent palette (teal/purple/coral/gray in `style-map.css`) are **retired** for this shell.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) — `--bg` | `#faf8f5` | Warm off-white page background; sticky-header translucent base. |
| Surface (30%) — `--surface` | `#ffffff` | Cards, agent pills, raised panels. |
| Ink (text) — `--ink` | `#1a1916` | Primary reading text + titles. |
| Ink soft — `--ink-soft` | `#55514a` | Secondary body, descriptions, back-control rest state. |
| Ink faint — `--ink-faint` | `#8a857c` | Metadata, page-sub, mono section/group labels, empty dots context. |
| Line — `--line` | `#e7e2da` | Hairline dividers, default card border, header bottom border. |
| Line strong — `--line-strong` | `#d8d2c7` | Empty progress-dot fill, toggle border, stronger separators. |
| Accent (10%) — `--accent` | `#5b3df5` | See reserved list. |
| Accent soft — `--accent-soft` | `#efeaff` | Active-tab background fill only. |
| Accent ink — `--accent-ink` | `#4a2fd6` | Accessible accent for text/links + active-tab text + hover-darkened accent. |
| Destructive | not applicable | No destructive actions in this phase (read-only public site). |

**Accent reserved for (explicit list — never "all interactive elements"):**
1. **Links** (anchor text in articles/about) — rendered in `--accent-ink` for contrast.
2. **The active nav tab** — `--accent-soft` background + `--accent-ink` text + `#ddd2ff` border.
3. **Card left-border accent** — 3px `--accent` stripe on economy-map cards (deepens to `--accent-ink` on hover). *(Establish the token here; the grid that consumes it is Phase 13.)*
4. **Progress dots (filled state)** — `--accent` fill on maturity/progress dots.
5. **Subscribe button** — solid `--accent` fill, white text, `--accent-ink` on hover.
6. **Eyebrow / kicker label** — accent-colored uppercase mono eyebrow.
7. **Brand dot** — the 9px `--accent` circle beside the wordmark.

Not allowed: any second hue, any per-tier accent color, the retired teal. Mode-driven accent swapping (the old technical/strategic palette flip) is gone — one accent in both newsletter modes.

**Contrast notes (accessibility):** `--accent-ink #4a2fd6` on `--bg #faf8f5` ≈ 7.0:1 (AA/AAA body text) — use it for link/active-tab text. `--accent #5b3df5` on `#fff` ≈ 5.4:1 — fine for the white-on-accent Subscribe button and as a non-text fill (borders/dots). `--ink #1a1916` on `--bg` ≈ 15:1. `--ink-faint #8a857c` on `--bg` ≈ 3.3:1 — **chrome/metadata only, never body copy** (consistent with TYPE-02 keeping metadata in mono chrome). All hex values are taken from the brief and treated as locked, not defaults.

---

## Visual Hierarchy

**Primary focal point of the main Newsletter-list screen:** the most-recent edition headline — the first edition's serif title (20px/600) in the edition list, the first element directly below the sticky nav. The eye lands there first; the centered page-title display sits above as orientation, and the accent is rationed (eyebrow, active tab, Subscribe) so nothing competes with that lead headline. Each later screen inherits the same rule: one serif title leads, mono chrome and the single accent support but never out-shout it.

---

## Nav Shell Contract (NAV-01..04)

The persistent shell is the load-bearing deliverable of this phase. Specifics:

**Structure (present on EVERY page / every hash route):**
- Sticky `<header>` (`position:sticky; top:0; z-index:50`) with translucent warm-white background `rgba(250,248,245,.86)` + `backdrop-filter: saturate(140%) blur(10px)` + `1px solid --line` bottom border. Keep the blur light.
- Left: **brand** — `--dot` (9px accent circle) + `AGENTPULSE` wordmark (IBM Plex Mono 14px/600). Clicking the brand returns to the Newsletter home (`#/`).
- Center: **three tabs** in exact order and copy: `Newsletter` · `Agent Economy` · `What is AgentPulse`.
- Right: **Subscribe** button (solid accent). Reuses the **existing subscribe flow** — it must invoke the current `scrollToSubscribe()` path (do not build a new subscribe mechanism; NAV-01).

**Active-state mechanism (NAV-02):**
- Active tab styling = `--accent-soft` bg + `--accent-ink` text + `#ddd2ff` border + weight 600 (class `.active` / `aria-current="page"`).
- Active state is derived from the **current hash route**, not from which link was clicked, so deep links and nested pages light the right tab. Mapping (hooks into the existing `getRoute()` in `app.js`):

  | Hash route | View | Active tab |
  |-----------|------|-----------|
  | `#/` (and empty) | edition list | **Newsletter** |
  | `#/edition/{n}` | single edition (nested) | **Newsletter** |
  | `#/map` | economy hub | **Agent Economy** |
  | `#/map/{slug}` | single block (nested) | **Agent Economy** |
  | `#/status` | maturity snapshot (nested under map) | **Agent Economy** |
  | `#/about` *(route added in Phase 14; tab ships now)* | about stub | **What is AgentPulse** |
  | `#/unsubscribe` | unsubscribe utility | none active (utility page) |

  Implement as a single function that maps route→section and toggles `.active` on the matching tab inside `route()` (called on load and on every `hashchange`). This mirrors the mockup's `parent` map but is driven by the real router.

**Back-control (NAV-03):**
- Every **nested** page shows a top-left back control: copy pattern **`← Back to [section]`**.
  - Single edition (`#/edition/{n}`) → `← Back to Newsletter`.
  - Single block (`#/map/{slug}`) and status (`#/status`) → `← Back to the map` (the Agent Economy section).
- Styling: IBM Plex Mono 12.5px weight 400, `--ink-soft` → `--accent-ink` on hover, no underline. (Replaces the current `← All editions` / `← Map` back-links.)

**One-click reachability + Map retirement (NAV-04):**
- From any section a reader reaches any other in one click via the always-present tabs.
- The old plain `<a class="nav-map-link">Map</a>` text link in `.nav-left` is **removed** — its destination is now the **Agent Economy** tab.

---

## Copywriting Contract

All copy below is locked verbatim — these strings are reused by Phases 12–14 and must not drift.

| Element | Copy |
|---------|------|
| Nav tab 1 | `Newsletter` |
| Nav tab 2 | `Agent Economy` |
| Nav tab 3 | `What is AgentPulse` |
| Brand wordmark | `AGENTPULSE` |
| Primary CTA (Subscribe button) | `SUBSCRIBE` (uppercase, reuses existing subscribe flow) |
| Back control — from a single edition | `← Back to Newsletter` |
| Back control — from a single block / status | `← Back to the map` |
| Back-control pattern (general) | `← Back to [section]` |
| Empty state heading (no editions) | *(preserve existing)* `No newsletters published yet.` — restyle into the new serif/ink system; do not change wording. |
| Empty state body | None required beyond the line above for this phase. |
| Error state | No new error surfaces introduced by the shell. Existing subscribe/unsubscribe status messaging is untouched (out of scope — content/flow unchanged). |
| Destructive confirmation | Not applicable — read-only public site; the shell introduces no destructive actions. |

**On the single-word CTA `SUBSCRIBE`:** intentionally kept as the standard newsletter subscribe verb. It is unambiguous in context (top-right of a newsletter site's nav, beside the Newsletter tab) and reuses the existing subscribe flow verbatim — expanding it to a verb+noun phrase ("Subscribe to the newsletter") would add nav-bar width for no clarity gain and would diverge from the brief/mockup wording. The single-word label is deliberate, not an omission.

Metadata separator: use `·` (U+00B7) with surrounding spaces, e.g. `Edition #29 · June 1, 2026` — IBM Plex Mono, `--ink-faint`.

---

## What This Replaces (migration inventory — for the executor)

So later phases inherit a clean base, this phase explicitly retires:

| Current (being removed/replaced) | Where | Replaced by |
|----------------------------------|-------|-------------|
| Dark `--bg:#0a0a0f`, white text, dual `body.technical` / `body.strategic` palettes | `style-shared.css` | Single light-mode `:root` palette above |
| `--accent:#00e5a0` (teal) + `--accent:#7c3aed` (strategic violet) mode-flip | `style-shared.css` | One `--accent:#5b3df5` (no mode flip) |
| Tier accents teal/purple/coral/gray + `--accent-tier` cascade + on-dark variants | `style-map.css` | One accent; per-card 3px `--accent` left-border (Phase 13) |
| `font-family:'Courier New', monospace` on body, paragraphs, list items, tables | `style-shared.css` | Source Serif 4 body; Courier removed entirely |
| `Georgia, serif` headings + second uppercase-mono `<h2>/<h3>` heading treatment | `style-shared.css` / `style-map.css` | Single Source Serif 4 heading style |
| `.nav-map-link` "Map" text link | `index.html`, `style-map.css` | Agent Economy nav tab |
| `.top-nav` (brand + subscribe only, no tabs) | `index.html` / `style-shared.css` | Sticky 3-tab shell with active state + blur |
| `← All editions` / `← Map` back-links | `index.html` / `style-shared.css` | `← Back to [section]` control |

Out of scope this phase (deliberately untouched): the mode-toggle relocation (Phase 12), the economy grid render (Phase 13), the About route/page (Phase 14), all backend/data/subscribe-flow logic, dark mode (DARK-01, deferred).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| not applicable | none | not applicable — vanilla-JS SPA, no shadcn/component registry in use |

No third-party UI registries are declared or used. No registry vetting required.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
