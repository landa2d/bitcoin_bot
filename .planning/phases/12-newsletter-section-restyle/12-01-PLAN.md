---
phase: 12-newsletter-section-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/web/site/style-shared.css
autonomous: true
requirements: [TGL-02]
must_haves:
  truths:
    - "The edition list and single-article views render in Source Serif 4 with no monospace body paragraphs (article p/li/td and .entry-preview are serif)"
    - "Article h2/h3 use a single serif heading style at weight 600 (no mono uppercase heading)"
    - "The mode toggle's active segment is a filled --accent pill with white weight-600 text, and a mono hint line styling exists below it"
    - "Blockquotes use a 3px --accent left border + --accent-soft fill; the PREVIEW banner uses --accent-soft (not legacy amber)"
  artifacts:
    - path: "docker/web/site/style-shared.css"
      provides: "Serif prose rules (TYPE-01), B1 list rows, A1 segmented pill, minimal-header rules, .preview-banner class"
      contains: ".preview-banner"
  key_links:
    - from: "style-shared.css .toggle-btn.active"
      to: "var(--accent) fill + #fff text + weight 600"
      via: "active-segment CSS rule (driven by .active class setMode toggles)"
      pattern: "toggle-btn\\.active"
    - from: "style-shared.css article p / .entry-preview"
      to: "var(--serif)"
      via: "TYPE-01 font-family migration"
      pattern: "var\\(--serif\\)"
---

<objective>
Restyle every Newsletter CSS rule in `docker/web/site/style-shared.css` onto the Phase 11 light-palette / serif design system: migrate all reading text (`article p/ul/ol/li/td`, `.entry-preview`) from `var(--mono)` to `var(--serif)` (TYPE-01 / ROADMAP success criterion 3), reform the mode toggle into the segmented accent pill A1 with a mono hint line (TGL-02 styling), restyle the edition-list rows (B1), the minimal Newsletter header (D3), the magazine article surfaces (D-05: serif headings, accent blockquotes, code chips, emphasized lead), and add a token-based `.preview-banner` class. This plan touches ONLY `style-shared.css` — markup/JS wiring lands in Plan 02.

Purpose: This is the load-bearing CSS pass. After it, the Newsletter list + article read cleanly in serif/light with no monospace body paragraphs (criterion 3), and the toggle pill + hint line + banner all consume Phase 11 tokens so Plan 02 only has to relocate markup that the styling already exists for.

Output: A fully restyled `style-shared.css` Newsletter rule set; all targets resolve to existing `:root` tokens / locked display classes in `style-base.css` (no new tokens, no second font, weights 400/600 only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-newsletter-section-restyle/12-UI-SPEC.md
@.planning/phases/12-newsletter-section-restyle/12-PATTERNS.md
@docker/web/site/style-base.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: TYPE-01 serif prose migration + magazine article surfaces (article rules)</name>
  <files>docker/web/site/style-shared.css</files>
  <read_first>
    - docker/web/site/style-shared.css lines 160-270 (the current `article` prose rules being migrated)
    - docker/web/site/style-base.css lines 10-66 (the `:root` tokens these targets consume — raw `--ink*/--line*/--accent*/--space-*/--radius*`)
    - 12-UI-SPEC.md §"Typography" (the serif scale table + "TYPE-01 migration checklist") and §"Component Contracts" D (magazine article: blockquote, code/pre, lead, table)
    - 12-PATTERNS.md §3d (current → target table for every `article` rule, with the new lead-paragraph rule)
  </read_first>
  <action>
    Migrate the `article` prose rules to the serif system per the UI-SPEC TYPE-01 checklist, preferring raw `--ink*/--line*/--accent*` tokens over bridge aliases. Concrete targets:
    - `article p`: change `var(--mono)` → `var(--serif)`, size 18px, weight 400, line-height 1.62, color `var(--ink-soft)`.
    - `article ul, article ol`: `var(--mono)` → `var(--serif)`, 18px/400/1.62, `var(--ink-soft)`; `article li` inherits serif via this rule.
    - `article h2`: from mono 15px UPPERCASE 700 `--accent` → serif (`var(--serif)`) 24px/600/1.2, `var(--ink)`, REMOVE `text-transform:uppercase` and the `letter-spacing` (single serif heading, TYPE-03).
    - `article h3`: from mono 15px UPPERCASE 700 `--accent` → serif 20px/600/1.25, `var(--ink)`, remove uppercase + letter-spacing; keep only its distinct margins.
    - `article strong`: color → `var(--ink)`.
    - `article blockquote`: `border-left: 3px solid var(--accent)`, `background: var(--accent-soft)`, `padding: var(--space-md)`, `border-radius: var(--radius-sm)`, text color `var(--ink)`, font-family `var(--serif)`.
    - `article a`: `color: var(--accent-ink)`, `text-decoration: underline`, `text-decoration-thickness: 1px`, `text-underline-offset: 2px`, `text-decoration-color: var(--accent-soft)` at rest; add `article a:hover { text-decoration-color: var(--accent-ink); }`.
    - `article code`: mono 14px, `color: var(--accent-ink)`, `background: var(--line)`, `padding: 4px 8px`, `border-radius: var(--radius-sm)`.
    - `article pre`: `background: var(--line)`, `padding: var(--space-md)`, `border-radius: var(--radius)`, keep `overflow-x: auto`; leave `article pre code` (no own bg/padding) unchanged.
    - `article table`: leave container; `article th` keeps `var(--mono)` but weight 600 (NOT 700), 13px UPPERCASE, color `var(--accent-ink)`; `article td` → `var(--serif)` 16px/400, color `var(--ink-soft)`, line-height 1.45; keep `article tr:last-child td` unchanged.
    - ADD a new rule `#newsletter-content > p:first-of-type { font-size: 20px; line-height: 1.45; color: var(--ink); }` (the emphasized lead — serif via the `article p` family; one step up from body; no markdown change).
    Mono MUST remain only on `article th` (label row) and inline `code`/`pre`. Do NOT touch `getModeContent`/markdown logic — this is CSS only.
  </action>
  <verify>
    <automated>grep -nE 'article (p|ul|ol|td)\b' docker/web/site/style-shared.css >/dev/null && ! grep -E -A4 'article (p|ul, article ol|td)' docker/web/site/style-shared.css | grep -q 'var(--mono)' && grep -q 'p:first-of-type' docker/web/site/style-shared.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `style-shared.css`: the `article p` rule uses `font-family: var(--serif)` (not `var(--mono)`), `font-size: 18px`, `line-height: 1.62`, `color: var(--ink-soft)`.
    - `style-shared.css`: the `article ul, article ol` rule uses `var(--serif)` 18px; `article td` uses `var(--serif)` 16px `var(--ink-soft)`.
    - `style-shared.css`: `article h2` is `var(--serif)` 24px/600 with NO `text-transform: uppercase`; `article h3` is `var(--serif)` 20px/600.
    - `style-shared.css`: `article th` retains `var(--mono)` at `font-weight: 600` (the only mono in the article reading column besides `code`/`pre`).
    - `style-shared.css`: `article blockquote` has `border-left: 3px solid var(--accent)` + `background: var(--accent-soft)` + `border-radius: var(--radius-sm)`.
    - `style-shared.css`: `article code` uses `border-radius: var(--radius-sm)` and `color: var(--accent-ink)`; `article pre` uses `border-radius: var(--radius)`.
    - `style-shared.css`: a `#newsletter-content > p:first-of-type` rule exists setting `font-size: 20px` and `color: var(--ink)`.
    - Running `grep -cE 'article (p|ul, article ol|td)[^{]*\{[^}]*var\(--mono\)' docker/web/site/style-shared.css` returns 0 (no mono on reading text — note `th` is excluded as it is a label row).
  </acceptance_criteria>
  <done>article p/ul/ol/li/td and the lead read in serif; h2/h3 are a single serif heading at 600; blockquote/code/pre/a/th restyled to the accent token system; mono remains only on th + code/pre.</done>
</task>

<task type="auto">
  <name>Task 2: B1 list rows + D3 minimal-header rules (.article-entry / .section-label / .entry-title / .entry-preview / .hero*)</name>
  <files>docker/web/site/style-shared.css</files>
  <read_first>
    - docker/web/site/style-shared.css lines 40-71 (current `.hero` / `.hero-tagline` / `.hero-headline` / `.hero-date`) and lines 119-155 (current `.article-entry` / `.section-label` / `.entry-title` / `.entry-preview`)
    - docker/web/site/style-base.css lines 82-97 (locked `.page-title` / `.eyebrow` display classes — REUSE, do not redefine) and lines 52-65 (space + radius tokens)
    - 12-UI-SPEC.md §"Component Contracts" B (minimal Newsletter header) and C (restyled edition-list rows B1), §"What This Replaces" rows 1 and 4-7
    - 12-PATTERNS.md §3a (hero text rules) and §3c (B1 rows)
  </read_first>
  <action>
    Restyle the list rows and the minimal-header text rules; prefer raw tokens. Concrete targets:
    - `.entry-preview`: `var(--mono)` 15px → `var(--serif)` 18px, weight 400, line-height 1.5, color `var(--ink-soft)`. (This is TYPE-01 list-line-item #1.)
    - `.entry-title`: `Georgia, serif` 18px/400 → `var(--serif)` 20px/600, color `var(--ink)`, `letter-spacing: -.01em`, keep `text-decoration: none` + `display: block`; bottom gap `var(--space-sm)`. Change `.entry-title:hover` color to `var(--accent-ink)`.
    - `.section-label` (the list-row kicker): `var(--mono)` 13px/400 `--accent` → `var(--mono)` 11px/600, color `var(--ink-faint)` (rationed accent — NOT `--accent`), `letter-spacing: .2em`, keep UPPERCASE, `margin-bottom: var(--space-xs)`.
    - `.article-entry`: REMOVE `margin-bottom: 20px`; use `padding: var(--space-lg) 0` + `border-bottom: 1px solid var(--line)`; add `.article-entry:last-child { border-bottom: none; }`. Rows, not cards (no surface fill, no box border, no radius).
    - `.hero`: keep `text-align: center`; change padding from `40px 0 24px` to `var(--space-xl) 0 0` (32px top).
    - `.hero-tagline`: DELETE the rule entirely (the markup is dropped in Plan 02; D-07 picks D3 not the eyebrow variant).
    - `.hero-headline`: restyle to match the locked `.page-title` look — `font-family: var(--serif)`, `font-size: clamp(30px, 5vw, 46px)`, weight 600, line-height 1.12, `letter-spacing: -.015em`, color `var(--ink)`; remove the old `Georgia` + `29px` values. (Plan 02 may instead apply the `.page-title` class to the element; this rule must not fight it.)
    - `.hero-date`: `var(--mono)` 14px/400, color `var(--ink-faint)`; replace the off-grid `margin-bottom: 28px` with `var(--space-md)` (16px, header→toggle gap); title→date gap is handled by `.hero-headline` margin set to `var(--space-sm)` (8px).
  </action>
  <verify>
    <automated>grep -A3 '\.entry-preview' docker/web/site/style-shared.css | grep -q 'var(--serif)' && grep -A3 '\.article-entry:last-child' docker/web/site/style-shared.css | grep -q 'border-bottom: none' && ! grep -q 'hero-tagline' docker/web/site/style-shared.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `style-shared.css`: the `.entry-preview` rule uses `var(--serif)`, `font-size: 18px`, `line-height: 1.5`, `color: var(--ink-soft)` (migrated FROM mono).
    - `style-shared.css`: the `.entry-title` rule uses `var(--serif)`, `font-size: 20px`, `font-weight: 600`, `color: var(--ink)`; `.entry-title:hover` color is `var(--accent-ink)`.
    - `style-shared.css`: the `.section-label` rule is `var(--mono)` 11px/600 with `color: var(--ink-faint)` (NOT `--accent`) and `letter-spacing: .2em`.
    - `style-shared.css`: `.article-entry` has `padding: var(--space-lg) 0` + `border-bottom: 1px solid var(--line)`, and an `.article-entry:last-child { border-bottom: none; }` rule exists; the `margin-bottom: 20px` is gone.
    - `style-shared.css`: the `.hero-tagline` rule no longer exists (`grep -c 'hero-tagline' docker/web/site/style-shared.css` returns 0).
    - `style-shared.css`: `.hero-date` is `var(--mono)` 14px `var(--ink-faint)`; `.hero` top padding is `var(--space-xl)`.
  </acceptance_criteria>
  <done>List rows are `--line`-divided serif B1 rows with a rationed `--ink-faint` kicker; the minimal D3 header text rules (page-title-matched headline + mono metadata) replace the old tagline/Georgia headline.</done>
</task>

<task type="auto">
  <name>Task 3: A1 segmented accent pill + hint line + .preview-banner + mobile reconciliation</name>
  <files>docker/web/site/style-shared.css</files>
  <read_first>
    - docker/web/site/style-shared.css lines 73-109 (current `.mode-toggle` / `.toggle-btn` / `.toggle-btn.active` / `.mode-subtitle`) and lines 399-424 (the mobile `@media` block, esp. line 409 `.toggle-btn { padding: 7px 16px }`)
    - docker/web/site/style-base.css lines 52-65 (space + radius tokens) and lines 19-21 (`--accent` / `--accent-soft` / `--accent-ink`)
    - 12-UI-SPEC.md §"Component Contracts" A (segmented accent pill — wrapper/segment/active/inactive/hint exact values), §"Component Contracts" D PREVIEW-banner spec, §"Spacing" (the retired `7px 16px` / `10px` flags)
    - 12-PATTERNS.md §3b (pill current → target) and §1e (banner restyle target)
  </read_first>
  <action>
    Reform the toggle into the A1 segmented accent pill, restyle the hint line, add the `.preview-banner` class, and reconcile the off-grid mobile override. The element IDs/classes are unchanged (Plan 02 preserves `#btn-technical`/`#btn-strategic`/`#mode-subtitle` and the `.mode-toggle`/`.toggle-btn` class names so `setMode()` keeps working). Concrete targets:
    - `.mode-toggle` (pill wrapper): `display: inline-flex`, `background: var(--surface)`, `border: 1px solid var(--line-strong)`, `border-radius: var(--radius)` (10px), `padding: var(--space-xs)` (4px), `gap: 0`.
    - `.toggle-btn` (segment): `font-family: var(--mono)`, `font-size: 12.5px`, `font-weight: 400`, `padding: 8px 20px`, `border-radius: var(--radius-btn)` (8px), `border: none`, `background: transparent`, `color: var(--ink-soft)`, `cursor: pointer`; keep a `transition`.
    - `.toggle-btn.active`: `background: var(--accent)`, `color: #fff`, `font-weight: 600` (corrected from 700).
    - `.mode-subtitle` (hint line): `font-family: var(--mono)`, `font-size: 11px`, `font-weight: 400`, `color: var(--ink-faint)`, `margin-top: var(--space-sm)` (8px); REMOVE `letter-spacing` and any `text-transform` (it is a sentence-case descriptive hint, not uppercased).
    - ADD `.preview-banner`: `background: var(--accent-soft)`, `color: var(--ink)`, `border-left: 3px solid var(--accent)`, `border-radius: var(--radius)` (10px), `padding: var(--space-sm) var(--space-md)`, `font-family: var(--mono)`, `font-size: 12.5px`, `font-weight: 600`, `text-transform: uppercase`, `margin-bottom: var(--space-md)`. (Plan 02 swaps the inline amber `<div>` for this class.)
    - Mobile `@media (max-width: 600px)` line ~409: reconcile the `.toggle-btn { font-size: 14px; padding: 7px 16px; }` off-grid override — either drop the override entirely (the 12.5px/`8px 20px` desktop values are already compact) or re-snap to `padding: var(--space-sm) var(--space-md)` (8px 16px). Do NOT leave the off-grid `7px 16px`. Also drop the now-dead `.hero-headline { font-size: 25px }` mobile override if it conflicts with the clamp() (the `clamp(30px,5vw,46px)` already scales down) — keep the `@media` block otherwise intact.
  </action>
  <verify>
    <automated>grep -A4 '\.toggle-btn\.active' docker/web/site/style-shared.css | grep -qE 'font-weight: *600' && grep -q '\.preview-banner' docker/web/site/style-shared.css && ! grep -q '7px 16px' docker/web/site/style-shared.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `style-shared.css`: `.mode-toggle` uses `background: var(--surface)`, `border: 1px solid var(--line-strong)`, `border-radius: var(--radius)`, `padding: var(--space-xs)`.
    - `style-shared.css`: `.toggle-btn` is `var(--mono)` `font-size: 12.5px` with `border-radius: var(--radius-btn)`; `.toggle-btn.active` is `background: var(--accent)`, `color: #fff`, `font-weight: 600` (NOT 700).
    - `style-shared.css`: `.mode-subtitle` is `var(--mono)` 11px/400 `var(--ink-faint)` with `margin-top: var(--space-sm)` and no `letter-spacing`/`text-transform`.
    - `style-shared.css`: a `.preview-banner` rule exists with `background: var(--accent-soft)`, `border-left: 3px solid var(--accent)`, `border-radius: var(--radius)`.
    - `grep -c '7px 16px' docker/web/site/style-shared.css` returns 0 (the off-grid mobile toggle override is reconciled).
  </acceptance_criteria>
  <done>The toggle renders as a segmented pill with a filled `--accent`/white/600 active segment + an 11px mono `--ink-faint` hint line; `.preview-banner` exists token-based; no off-grid `7px 16px` remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits only `style-shared.css` — a static CSS file served by the read-only `agentpulse-web` Caddy container. No inputs, no auth, no network calls, no data writes, no innerHTML sinks are introduced or modified. CSS cannot create an injection sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information disclosure | CSS token consumption | accept | CSS only references existing `:root` tokens; no secret/data is exposed in a stylesheet. No PII, low-value public static asset. |
| T-12-02 | Tampering (XSS non-regression) | `.preview-banner` / article prose classes | accept | This plan adds CSS classes ONLY; it introduces ZERO new innerHTML interpolation. The existing `renderArticle()` `marked.parse()` sink (the only escapeHtml-bypass path) is unchanged here — Plan 02 swaps the banner markup but adds no new unescaped edition-data interpolation. Confirmed: no new injection sink in CSS. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — no `package.json` exists in the repo (vanilla-JS SPA, CDN `<script>` tags only, unchanged). No supply-chain surface added. |
</threat_model>

<verification>
- All targets resolve to `:root` tokens present in `style-base.css` lines 10-66 or the locked `.page-title`/`.eyebrow` classes (lines 82-97) — no new tokens, no second font, weights 400/600 only.
- No monospace remains on reading text: `article p/ul/ol/li/td` and `.entry-preview` are `var(--serif)`; mono survives only on kicker/metadata/hint/toggle-labels (other files), `code`/`pre`, and `article th`.
- `style-base.css` is NOT modified by this plan (read-only reference).
- Static/source-level check; optional render check against the running `agentpulse-web` container (account for the `__SUPABASE_URL__` entrypoint substitution per the project memory note). NO prod deploy.
</verification>

<success_criteria>
- ROADMAP criterion 3 (CSS side): edition list + article render in serif + light palette with no monospace body paragraphs.
- TGL-02 (styling side): active toggle segment is a filled `--accent` pill with white/600 text; a mono hint-line style exists below it.
- D-04/D-05/D-06/D-07 CSS targets met: B1 `--line`-divided rows, magazine article surfaces (serif headings, accent blockquotes, code chips, emphasized lead), minimal D3 header text rules.
- `grep -cE 'article (p|ul, article ol|td)[^{]*\{[^}]*var\(--mono\)' docker/web/site/style-shared.css` returns 0.
</success_criteria>

<output>
Create `.planning/phases/12-newsletter-section-restyle/12-01-SUMMARY.md` when done.
</output>
