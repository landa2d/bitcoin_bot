---
phase: 13-agent-economy-grid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/web/site/app.js
  - docker/web/site/style-shared.css
  - docker/web/site/style-map.css
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, MAP-04]

must_haves:
  truths:
    - "The Agent Economy hub renders as a 2-column card grid on desktop and 1 column on mobile, not a vertical stack (MAP-01, ROADMAP crit 1)"
    - "Each normal block is a bordered card with a serif 20px/600 title, a serif 18px/400 one-line description, progress dots, a 3px accent left-border, and a hover lift (MAP-02, ROADMAP crit 2)"
    - "Cards stay grouped under the SUBSTRATE / BEHAVIOR / FRAME mono section labels from the live economy_map.blocks tier data (MAP-03, ROADMAP crit 3)"
    - "A block with current_body_version_id null spans the full grid width, shows a · DEFERRED tag, and shows all-empty progress dots (MAP-04, D-04/D-04a, ROADMAP crit 4)"
    - "Tiers are distinguished only by their mono section label — no per-tier color survives; the single --accent violet is the only accent on cards and filled dots (D-05, COLOR-02)"
    - "The hub shows a serif 'The Agent Economy' page-title, an optional mono 'updated {date}' sub-line, and the existing HUB_STORYLINE as a serif one-line frame, rendered inside #map-view .content-area (D-06)"
  artifacts:
    - path: "docker/web/site/style-shared.css"
      provides: "Light-system card grid + card + DEFERRED + recolored maturity-pill + tier-label + hub-storyline rules (de-darkened from style-map.css)"
      contains: "grid-template-columns: repeat(2, 1fr)"
    - path: "docker/web/site/app.js"
      provides: "renderHub grid + renderTile card with DEFERRED branch + recolored renderMaturityPill + in-content hub header"
      contains: "current_body_version_id"
    - path: "docker/web/site/style-map.css"
      provides: "Per-tier hex + [data-accent]/--accent-tier cascade + dark .block-tile/.tier-label rules removed (delete-and-fold disposition)"
  key_links:
    - from: "app.js renderTile"
      to: ".card / .card-deferred rule in style-shared.css"
      via: "class name emitted by renderTile consumed by the new grid CSS"
      pattern: "class=\"card"
    - from: "app.js renderMaturityPill"
      to: ".maturity-pill .seg fill rule"
      via: "data-stage attribute keys the --accent fill; data-stage=0 = all empty"
      pattern: "data-stage"
    - from: "renderHub grid wrapper"
      to: "#map-view .content-area"
      via: "innerHTML write of header + grouped grid"
      pattern: "map-view"
---

<objective>
Re-render the Agent Economy hub (`renderHub()` / `renderTile()` / `renderMaturityPill()`) as a responsive 2-column grouped card grid on the Phase 11 light/serif design system, with the full-width DEFERRED treatment, and de-dark the shared `style-map.css` rules these views consume (cards, tier labels, progress pill, hub storyline). This is the headline MAP-01..04 work plus the D-05 single-accent collapse and the D-06 hub header.

Purpose: Replace the long dark vertical scroll with a tight scannable grid so related blocks sit together with minimal scrolling (ROADMAP goal, brief §68), on the same shell Phase 12 applied to the newsletter — and retire the per-tier color machinery (COLOR-02 / D-05). This plan also owns the `style-map.css` **delete-and-fold** disposition decision for the rules the hub shares with block/status, so Plan 02 builds on a de-darkened base.
Output: A grid hub + DEFERRED cards + recolored dots + a serif hub header, with the dark per-tier cascade deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-agent-economy-grid/13-CONTEXT.md
@.planning/phases/13-agent-economy-grid/13-PATTERNS.md
@.planning/phases/13-agent-economy-grid/13-UI-SPEC.md
@docker/web/site/app.js
@docker/web/site/style-map.css
@docker/web/site/style-shared.css
@docker/web/site/style-base.css
@docker/web/site/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-dark the shared CSS (pill recolor, tier-label, hub-storyline, retire per-tier cascade) — delete-and-fold disposition</name>
  <files>docker/web/site/style-shared.css, docker/web/site/style-map.css</files>
  <read_first>
    - docker/web/site/style-map.css (the dark theme being retired — read the per-tier hexes `:18`–`:31`, the `body.technical|strategic [data-accent]` cascade `:33`–`:43`, `.maturity-pill .seg` + stage fills `:55`–`:75`, `.tier-label` `:172`–`:187`)
    - docker/web/site/style-base.css `:root` (`:10`–`66`) — the canonical light tokens to consume: `--surface`, `--ink*`, `--line`, `--line-strong`, `--accent`, `--accent-ink`, `--serif`, `--mono`, `--space-*`, `--radius`, `--radius-dot`
    - docker/web/site/style-shared.css `.section-label` (`:148`–`:156`), `.entry-title` (`:158`–`:167`), `.entry-preview` (`:173`–`:179`) — the EXACT Phase 12 property blocks the card title/desc and section label copy from
    - .planning/phases/13-agent-economy-grid/13-UI-SPEC.md §Color (the "Empty dots = DEFERRED visual" + accent reserved list) and §"What This Replaces" migration table
    - .planning/phases/13-agent-economy-grid/13-PATTERNS.md §"renderMaturityPill" + §"Shared Patterns / CSS-DISPOSITION" + §"Single-Accent / Per-Tier Retirement"
  </read_first>
  <action>
    Adopt the delete-and-fold disposition (PATTERNS §CSS-DISPOSITION option 1): the de-darkened map rules move into `style-shared.css` (joining the Phase 12 newsletter rules), `style-map.css` shrinks. In `style-map.css`, DELETE outright (not lighten): the per-tier base/on-dark hex `:root` block (`--accent-teal-base` etc., lines :18–:31), the entire `body.technical|strategic [data-accent]{--accent-tier:…}` cascade (lines :33–:43), the dead `.nav-map-link` rule (:160–:169, already superseded by the Phase 11 tab), and the dark `.tier-label` (:172–:187). Migrate the de-darkened `.maturity-pill` rules into `style-shared.css`: keep the 5-segment structure and the `[data-stage="N"] .seg:nth-child(-n+N)` fill selectors, but change the empty `.seg` background from `transparent` to `var(--line-strong)`, remove the dark `border: 1px solid var(--border)` (set border to none or `--line-strong`), set `border-radius` from `2px` to `var(--radius-dot)` (3px), and change every fill `var(--accent-tier)` to `var(--accent)` (background AND border-color on the stage selectors). Per UI-SPEC §4, `data-stage="0"` matches none of the `:nth-child` selectors so all segments fall through to the `--line-strong` empty default — that is the DEFERRED empty-dots state by construction (do not add a `[data-stage="0"]` rule). Inter-segment `.maturity-pill{gap}` may stay 2px or widen to 4px (UI-SPEC §Spacing — planner discretion; pick one). Add a `.tier-label` rule in `style-shared.css` matching the `.section-label` property block (mono `var(--mono)` 11px/600, `var(--ink-faint)`, `text-transform:uppercase`, `letter-spacing:.18em` per UI-SPEC) with `margin: var(--space-xl) 0 12px` and a `.tier-label:first-of-type{margin-top: var(--space-sm)}` reduced-top rule (UI-SPEC §Spacing). Add the orphan `.hub-storyline` rule (NO rule exists today anywhere per PATTERNS §"No Analog Found"): `font-family: var(--serif); font-size:18px; font-weight:400; color: var(--ink-soft); line-height:1.55; margin-bottom: var(--space-md)`. Do NOT touch any Phase 12 newsletter rules (`.article-entry`, `article *`, `.hero*`) — they are out of scope (Phase 12 done). Do NOT delete the timeline-entry / block-* / status-* / show-all rules from `style-map.css` in THIS task — those de-dark in Plan 02; leave them in place for now (they still cascade onto a not-yet-restyled block/status view, which is acceptable until Plan 02 lands them).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node -e "const fs=require('fs');const m=fs.readFileSync('docker/web/site/style-map.css','utf8');const s=fs.readFileSync('docker/web/site/style-shared.css','utf8');const fail=[];if(/--accent-teal-base|--accent-purple-base|--accent-coral-base|--accent-gray-base/.test(m))fail.push('per-tier hex still in style-map.css');if(/\[data-accent=/.test(m))fail.push('[data-accent] cascade still in style-map.css');if(/\.nav-map-link/.test(m))fail.push('dead .nav-map-link still in style-map.css');if(!/repeat\(|grid-template-columns/.test(s)){/*grid lands in Task2 file too*/}if(!/\.hub-storyline/.test(s))fail.push('.hub-storyline rule missing from style-shared.css');if(!/\.tier-label/.test(s))fail.push('.tier-label rule missing from style-shared.css');if(/var\(--accent-tier\)/.test(s.match(/\.maturity-pill[\s\S]*?(?=\n\.[a-z])/i)?.[0]||''))fail.push('maturity-pill still uses --accent-tier in style-shared.css');if(fail.length){console.error('FAIL:\n'+fail.join('\n'));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `style-map.css` contains no `--accent-teal-base` / `--accent-purple-base` / `--accent-coral-base` / `--accent-gray-base` declaration
    - `style-map.css` contains no `[data-accent=` selector and no `.nav-map-link` rule
    - `style-shared.css` contains a `.maturity-pill .seg` rule whose empty background is `var(--line-strong)` and whose fill (`[data-stage="N"]`) selectors use `var(--accent)` (not `var(--accent-tier)`)
    - `style-shared.css` `.maturity-pill .seg` `border-radius` is `var(--radius-dot)` (or `3px`)
    - `style-shared.css` contains a `.hub-storyline` rule with `font-family: var(--serif)` and `color: var(--ink-soft)`
    - `style-shared.css` contains a `.tier-label` rule using `var(--mono)` and `var(--ink-faint)` with `text-transform: uppercase`
    - No `'Courier New'`, `Georgia` literal, or `transparent` empty-seg background remains in the migrated `.maturity-pill` / `.tier-label` / `.hub-storyline` rules
  </acceptance_criteria>
  <done>The progress pill, tier labels, and hub storyline render on the light single-accent system; the per-tier color cascade and dead nav-map-link are deleted; the new grid/card rules can be added in Task 2 against this de-darkened base.</done>
</task>

<task type="auto">
  <name>Task 2: Card grid + DEFERRED CSS (style-shared.css) — MAP-01, MAP-02, MAP-04</name>
  <files>docker/web/site/style-shared.css, docker/web/site/style-map.css</files>
  <read_first>
    - docker/web/site/style-shared.css `.entry-title` (`:158`), `.entry-preview` (`:173`), `.section-label` (`:148`) — copy these property blocks verbatim into the card title/description rules
    - docker/web/site/style-map.css `.block-tile` (`:194`–`:226`) — the dark card rule being replaced (delete it here as part of the fold)
    - .planning/phases/13-agent-economy-grid/13-UI-SPEC.md §"Component Inventory" items 1 (Card), 2 (Grid + tier sections), 3 (DEFERRED card variant) and §Spacing (resolved card padding `20px 20px 16px`, grid `gap:16px`, `margin-top:8px`, internal `gap:8px`) and §Radius (`--radius` 10px card corners)
    - .planning/phases/13-agent-economy-grid/13-PATTERNS.md §"No Analog Found" (the grid + `· DEFERRED` tag + full-width variant are the only structurally-novel pieces — use the mockup's `.grid`/`.card.span2` mechanism as intent, NOT copied class names)
    - .planning/docs/agentpulse-redesign-mockup.html — `.grid` (`repeat(2,1fr)`), `.card`, `.card.span2{grid-column:1/-1}`, `@media(max-width:640px){.grid{grid-template-columns:1fr}}` — INTENT reference only
  </read_first>
  <action>
    In `style-shared.css`, delete the dark `.block-tile` rules (folded out of `style-map.css` — remove them there too). Add the grid + card rules per UI-SPEC §Component Inventory. Grid wrapper (`.grid` child div is the recommended structure per UI-SPEC §2 — the executor in Task 3 emits it; planner discretion allows applying `display:grid` to `.tier-section` instead, pick the child-div form): `display:grid; grid-template-columns: repeat(2, 1fr); gap: var(--space-md); margin-top: var(--space-sm)`. Card rule (`.card`): `background: var(--surface); border: 1px solid var(--line); border-left: 3px solid var(--accent); border-radius: var(--radius); padding: 20px 20px 16px; display:flex; flex-direction:column; gap: var(--space-sm); text-decoration:none; color:inherit; cursor:pointer; transition:.18s ease`. Card title (`.card .tile-title` or `.tile-title`) copies the `.entry-title` property block (serif 20px/600, `var(--ink)`, `letter-spacing:-.01em`); card description (`.tile-subtitle`) copies `.entry-preview` (serif 18px/400, `var(--ink-soft)`, line-height 1.5) plus `flex:1` so the dots bottom-align. Hover (MAP-02 "subtle hover lift"): `.card:hover{ transform: translateY(-3px); box-shadow: 0 8px 24px rgba(26,25,22,.07); border-left-color: var(--accent-ink) }`. Focus ring (a11y, the dark theme had none): `.card:focus-visible{ outline: 2px solid var(--accent-ink); outline-offset: 2px }`. DEFERRED full-width variant: name the modifier `.card-deferred` and give it `grid-column: 1 / -1` (the mockup's `.card.span2` mechanism — intent, not copied name); keep all other card chrome identical (the 3px accent stripe stays per UI-SPEC §3). DEFERRED tag (`· DEFERRED`): a `.deferred-tag` mono chrome rule — `font-family: var(--mono); font-size:11px; font-weight:600; text-transform:uppercase; letter-spacing:.12em; color: var(--ink-faint)`. Recommended dots row: a `.card-dots-row{ display:flex; align-items:center; justify-content:space-between; gap: var(--space-sm) }` so a deferred card reads "empty dots · DEFERRED" (UI-SPEC §3 recommended placement; planner discretion on exact placement). Mobile collapse (MAP-01): `@media (max-width: 640px){ .grid{ grid-template-columns: 1fr } }` — at 1 column the full-width modifier is a no-op (every card already spans). Use ONLY the four locked serif sizes (18/20/24/clamp) and the radius set (3/7/8/10); declare no new tokens (UI-SPEC §Token inheritance).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node -e "const fs=require('fs');const s=fs.readFileSync('docker/web/site/style-shared.css','utf8');const m=fs.readFileSync('docker/web/site/style-map.css','utf8');const fail=[];if(!/grid-template-columns:\s*repeat\(2,\s*1fr\)/.test(s))fail.push('2-col grid rule missing');if(!/grid-column:\s*1\s*\/\s*-1/.test(s))fail.push('full-width DEFERRED grid-column missing');if(!/@media[^}]*max-width:\s*640px[\s\S]*?grid-template-columns:\s*1fr/.test(s))fail.push('mobile 1-col collapse missing');if(!/translateY\(-3px\)/.test(s))fail.push('hover lift transform missing');if(!/border-left:\s*3px solid var\(--accent\)/.test(s))fail.push('3px accent left-border missing');if(/\.block-tile\b/.test(m))fail.push('.block-tile still in style-map.css (not folded out)');if(/[Cc]ourier New|Georgia,/.test(s.match(/\.tile-title[\s\S]*?\}/)?.[0]||''))fail.push('card title not serif');if(fail.length){console.error('FAIL:\n'+fail.join('\n'));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `style-shared.css` contains a grid rule with `grid-template-columns: repeat(2, 1fr)` and `gap: var(--space-md)` (or `16px`)
    - `style-shared.css` contains a card rule with `border-left: 3px solid var(--accent)`, `border-radius: var(--radius)` (or `10px`), and `background: var(--surface)`
    - `style-shared.css` card hover rule contains `transform: translateY(-3px)` and `border-left-color: var(--accent-ink)`
    - `style-shared.css` contains a full-width modifier with `grid-column: 1 / -1`
    - `style-shared.css` contains `@media (max-width: 640px)` collapsing the grid to `grid-template-columns: 1fr`
    - `style-shared.css` contains a `.deferred-tag` (or equivalently named) rule using `var(--mono)` 11px/600 `var(--ink-faint)`
    - `style-map.css` contains no `.block-tile` rule (folded out)
    - card title rule uses `var(--serif)` (no `'Courier New'` / `Georgia` literal)
  </acceptance_criteria>
  <done>The 2-column grid, bordered cards with serif title/description/3px accent stripe/hover lift, full-width DEFERRED variant, DEFERRED tag chrome, and mobile 1-col collapse all exist as light-system rules; the dark `.block-tile` is gone.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: renderHub grid + DEFERRED branch + recolored pill + in-content hub header (app.js) — MAP-01..04, D-04/D-05/D-06</name>
  <files>docker/web/site/app.js</files>
  <read_first>
    - docker/web/site/app.js `renderMaturityPill()` (`:391`–`:396`), `loadHub()` (`:403`–`:428`), `renderHub()` (`:430`–`:480`), `renderTile()` (`:447`–`:453`), `tierSection()` (`:462`–`:468`) — the exact current functions being changed
    - docker/web/site/app.js `showView()` (`:159`–`:163`) — confirms `.hero` is scoped to the `list` route, so the hub header must render inside `#map-view .content-area`, NOT via `updateHero()`
    - docker/web/site/app.js constants `HUB_STORYLINE` (`:32`), `TIER_LABELS` (`:41`), `MATURITY_STAGE` (`:38`) — operator-owned copy, do NOT change wording
    - docker/web/site/app.js `renderList()` row template (`:186`–`:190`) — the escapeHtml + section-label/title/desc discipline to mirror (PATTERNS §renderHub analog)
    - .planning/phases/13-agent-economy-grid/13-UI-SPEC.md §"Component Inventory" items 2, 3, 5 (Hub header) and §Copywriting (hub title "The Agent Economy", DEFERRED tag "· DEFERRED", omit date when all null)
    - .planning/phases/13-agent-economy-grid/13-PATTERNS.md §renderHub, §renderMaturityPill, §"Security Invariants"
  </read_first>
  <action>
    Change `renderMaturityPill(b)` to `renderMaturityPill(b, deferred)`: drop the `data-accent="…"` attribute from the emitted markup (the cascade it fed is deleted per D-05); when `deferred` is truthy emit `data-stage="0"` and `aria-label="Maturity: deferred (not yet synthesized)"`, otherwise keep the existing `MATURITY_STAGE[b.maturity] || 1` stage and aria-label. Keep the five `<span class="seg">` children verbatim. In `renderTile(b)`: derive `var deferred = !b.current_body_version_id;` (the column is already in the `loadHub` select at `:415` — do NOT add a `.eq('status',…)` filter, RLS is the boundary per D-17 / CLAUDE.md). Emit `<a href="#/map/{encodeURIComponent(slug)}" class="card">` (drop `data-accent`), adding the `card-deferred` class when `deferred` (so `class="card card-deferred"`). Keep the whole-anchor click target (CLAUDE.md D-14 — the `<a>` IS the card). Inside: `<h3 class="tile-title">{escapeHtml(title)}</h3>` then `<p class="tile-subtitle">{escapeHtml(subtitle)}</p>` then the dots row: for a normal card just `renderMaturityPill(b, false)`; for a deferred card wrap the pill + tag in the `.card-dots-row` flex (`renderMaturityPill(b, true)` + `<span class="deferred-tag">· DEFERRED</span>`) per UI-SPEC §3 recommended placement. Keep every DB string `escapeHtml`'d (security invariant). In `tierSection(label, blocks)`: keep the `<section class="tier-section">` + `<h2 class="tier-label">{label}</h2>` (MAP-03 grouping is already correct — do not re-derive), but wrap the joined tiles in a `<div class="grid">…</div>` so the label sits ABOVE the grid (UI-SPEC §2). In `renderHub(data)`: remove the `updateHero(HUB_STORYLINE, dateText)` call (the `.hero` is list-scoped now — PATTERNS gotcha 2); keep the `latest`/`dateText` computation (`:434`–`:435`) and reuse it for the in-content sub-line, omitting the stamp entirely when `latest` is null (UI-SPEC §Copywriting). Build the `#map-view .content-area` html in this order: `<h1 class="page-title">The Agent Economy</h1>` (reuse the Phase 11 class verbatim — D-06), then the mono sub-line ONLY when `latest` exists (`<p class="hero-date">updated {formatDate(latest)}</p>` reusing the existing `.hero-date` treatment, 14px mono `--ink-faint` per UI-SPEC §Typography hub header; do not add a new class if `.hero-date` is list-scoped — use a plain mono sub-line element styled in Task 2/inline-free, planner discretion to add a `.hub-subline` rule), then the existing `<div class="hub-storyline">{escapeHtml(HUB_STORYLINE)}</div>`, then the three `tierSection(...)` calls. Do NOT emit an `.eyebrow` kicker (UI-SPEC §5: recommended OFF). Restyle the hub empty-state at `:420`–`:421`: change the inline `color:var(--text-secondary);font-size:15px` to a serif `--ink-soft` treatment (e.g. `class="entry-preview"` or `style="color:var(--ink-soft)"`), copy wording "Map data unavailable." unchanged.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "print('skip-py')" >/dev/null 2>&1; node --check docker/web/site/app.js && node -e "const fs=require('fs');const a=fs.readFileSync('docker/web/site/app.js','utf8');const fail=[];const tile=a.match(/function renderTile[\s\S]*?\n    }/)?.[0]||'';if(/data-accent/.test(tile))fail.push('renderTile still emits data-accent');if(!/!b\.current_body_version_id|current_body_version_id\s*\?|deferred/.test(tile))fail.push('renderTile has no DEFERRED branch on current_body_version_id');if(!/class=\\\"card/.test(a)&&!/class=\"card/.test(a))fail.push('renderTile does not emit .card');if(!/class=\"grid\"|class=\\\"grid\\\"/.test(a)&&!/<div class=.grid/.test(a))fail.push('grid wrapper div not emitted');if(!/The Agent Economy/.test(a))fail.push('hub page-title copy missing');if(!/· DEFERRED|\\u00b7 DEFERRED|deferred-tag/.test(a))fail.push('DEFERRED tag not emitted');const pill=a.match(/function renderMaturityPill[\s\S]*?\n}/)?.[0]||'';if(/data-accent/.test(pill))fail.push('renderMaturityPill still emits data-accent');if(!/deferred/.test(pill))fail.push('renderMaturityPill has no deferred override');const _strip=function(s){return s.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/[^\n]*/g,'')};if(/\.eq\(.status./.test(_strip(a.match(/async function loadHub[\s\S]*?renderHub\(data\);\n}/)?.[0]||'')))fail.push('forbidden status filter added to loadHub');if(fail.length){console.error('FAIL:\n'+fail.join('\n'));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app.js` passes `node --check` (no syntax error)
    - `renderTile` emits `class="card"` for normal blocks and adds `card-deferred` when `!b.current_body_version_id` is true
    - `renderTile` no longer emits a `data-accent` attribute
    - `renderMaturityPill` accepts a deferred flag, emits `data-stage="0"` when deferred, and emits no `data-accent`
    - `renderHub` wraps each tier's tiles in a `<div class="grid">` with the `<h2 class="tier-label">` above it
    - `renderHub` writes `<h1 class="page-title">The Agent Economy</h1>` into `#map-view .content-area` and no longer calls `updateHero()` for the hub title
    - `renderHub` renders the `updated {date}` mono sub-line only when a block has a non-null `last_synthesized_at`
    - The `· DEFERRED` tag (U+00B7 middot) is emitted for deferred cards
    - `loadHub` has NO `.eq('status', …)` filter added (RLS is the boundary)
    - Every DB string in `renderTile` / hub header is wrapped in `escapeHtml()`
  </acceptance_criteria>
  <done>The hub renders as a grouped 2-col grid of light cards with serif headers, the 5 bodyless blocks render as full-width DEFERRED cards with empty dots + the DEFERRED tag, the 2 blocks with bodies render as normal cards with filled accent dots, and the hub shows the serif "The Agent Economy" header + storyline — verifiable in a browser at `#/map`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase (anon, RLS) → app.js render | `economy_map.blocks` rows (title, subtitle, slug, accent, maturity, current_body_version_id) cross into the DOM. RLS is the read boundary — only published/visible rows reach the client. |
| Browser DOM ← rendered HTML | Block string fields are interpolated into `innerHTML` in `renderTile` / hub header. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering (XSS) | `renderTile` / hub header interpolating `title`, `subtitle`, `slug`, `HUB_STORYLINE` | mitigate | Preserve the existing `escapeHtml()` on every DB string and `encodeURIComponent()` on the slug href — unchanged from current code. No raw `innerHTML` of an unescaped field is introduced. The grid/header markup adds only static class names + escaped values. |
| T-13-02 | Tampering (data boundary) | `loadHub` query | mitigate | No new query params, no `.eq('status',…)` / defensive status filter added — RLS stays the sole read boundary (D-17 / CLAUDE.md). DEFERRED is derived in JS from `current_body_version_id` already in the result set, not via a query filter. |
| T-13-03 | Information disclosure | `current_body_version_id` exposed to client | accept | The column is already selected by the existing `loadHub` (read-only metadata, no secret); using it as a null-check changes nothing about exposure. No PII, no secret. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — vanilla-JS, no build step, no new dependency. Supabase-js + marked are CDN-loaded and unchanged. |

**No new high-severity threat is introduced** if the existing `escapeHtml` sanitization and the RLS-only read boundary are preserved unchanged — which this plan mandates.
</threat_model>

<verification>
- `node --check docker/web/site/app.js` passes.
- Load the site locally (substituted preview per the `web-static-preview-substitution` memory — serving raw `docker/web/site` crashes app.js at `createClient('__SUPABASE_URL__')`; use the entrypoint.sh-substituted temp copy). Navigate to `#/map`:
  - Desktop: SUBSTRATE / BEHAVIOR / FRAME labels, 2-column grid, `identity-trust` + `governance-accountability` as normal cards with filled accent dots, the other 5 as full-width DEFERRED rows with empty dots + `· DEFERRED`.
  - Narrow the viewport ≤640px: the grid collapses to 1 column.
  - Hover a card: it lifts (-3px) and the left stripe deepens to `--accent-ink`.
  - The header reads "The Agent Economy" in serif; the storyline reads below it.
- grep confirms no `--accent-tier` / `[data-accent]` / per-tier hex survives in `style-map.css`; no `data-accent` emitted by `renderTile`/`renderMaturityPill`.
</verification>

<success_criteria>
- MAP-01: hub is a 2-col desktop / 1-col mobile grid with ~16px gaps (ROADMAP crit 1).
- MAP-02: each normal card has a serif title, one-line description, progress dots, a 3px accent left-border, and a hover lift (ROADMAP crit 2).
- MAP-03: cards stay grouped under the canonical SUBSTRATE/BEHAVIOR/FRAME labels from live `economy_map.blocks` (ROADMAP crit 3).
- MAP-04: the 5 bodyless blocks span full grid width with `· DEFERRED` + empty dots (ROADMAP crit 4, D-04/D-04a).
- D-05: only `--accent` violet appears on cards/dots; per-tier color machinery deleted (COLOR-02).
- D-06: serif "The Agent Economy" page-title + optional mono date sub-line + serif storyline, rendered in `#map-view .content-area`.
</success_criteria>

<output>
Create `.planning/phases/13-agent-economy-grid/13-01-SUMMARY.md` when done.
</output>
