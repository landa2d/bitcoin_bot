---
phase: 13-agent-economy-grid
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - docker/web/site/app.js
  - docker/web/site/style-shared.css
  - docker/web/site/style-map.css
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, MAP-04]

must_haves:
  truths:
    - "Clicking a card from the new hub grid opens a block reading view on the light/serif system — no dark background, no Courier New, no per-tier color (D-01, D-03)"
    - "The block body markdown prose renders in serif 18px/1.62 --ink-soft with in-content links in --accent-ink; no monospace body paragraphs anywhere (D-03, TYPE-01)"
    - "The block view keeps its existing 4-part structure (header / live-tension card / markdown body / Evolution timeline) — restyled, not redesigned; the live_tension placeholder gate is preserved (D-03)"
    - "The block H1 is a serif 24px/600 reading-view title that reads smaller than the hub display page-title; the inline pill is the recolored single-accent dots (D-03, D-06)"
    - "The deep-link-only #/status view renders cleanly on the light system (de-dark pass only, no layout redesign) — strip dark bg / Courier / per-tier accent (D-02)"
    - "Every block/status accent (left stripes, filled dots, links) uses only the single --accent / --accent-ink violet; no per-tier color survives (D-05, COLOR-02)"
  artifacts:
    - path: "docker/web/site/style-shared.css"
      provides: "De-darkened block-detail rules (.block-body p/li/h2, .block-tension, .block-header, .evolution, .timeline-*, .timeline-show-all) + status rules (.status-row/.status-title/.status-subtitle/.status-synth) on the light/serif system"
      contains: ".block-body p"
    - path: "docker/web/site/style-map.css"
      provides: "Remaining dark block/status/timeline rules removed (delete-and-fold completes; file deletable or emptied of dark theme)"
    - path: "docker/web/site/app.js"
      provides: "renderBlock + renderStatusRow drop dead data-accent; pill calls use the recolored single-accent renderMaturityPill"
      contains: "block-header"
  key_links:
    - from: "app.js renderBlock body section"
      to: ".block-body p/li/h2 serif rules"
      via: "marked.parse output wrapped in <section class=block-body> styled by scoped serif rules"
      pattern: "block-body"
    - from: "app.js renderStatusRow"
      to: ".status-row light rules"
      via: "class name consumed by de-darkened status CSS, stripe = --accent"
      pattern: "status-row"
---

<objective>
Apply the D-03 restrained system pass to the per-block reading view (`renderBlock()`) and the D-02 light de-dark pass to the deep-link-only `#/status` view (`renderStatus()`), completing the de-darkening of every `style-map.css` consumer view. This pairs with the Plan 01 hub the way Phase 12 paired the newsletter list with the article: clicking a card must land on a clean light reading page, not a dark/Courier page.

Purpose: D-01 requires BOTH the hub AND the block detail restyled (batch-deploy means any view left on the dark theme ships looking broken). The block view adopts the design system (serif body prose via the Phase 12 TYPE-01 migration, light surfaces, single-accent dots) but KEEPS its structure — no second magazine layer (that stays Newsletter-only). The status view is cheap insurance for a clean deep-link. This plan finishes the `style-map.css` delete-and-fold started in Plan 01.
Output: A restyled block reading view + a de-darkened status view; `style-map.css` emptied of the dark theme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-agent-economy-grid/13-CONTEXT.md
@.planning/phases/13-agent-economy-grid/13-PATTERNS.md
@.planning/phases/13-agent-economy-grid/13-UI-SPEC.md
@.planning/phases/13-agent-economy-grid/13-01-SUMMARY.md
@docker/web/site/app.js
@docker/web/site/style-map.css
@docker/web/site/style-shared.css
@docker/web/site/style-base.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Block-detail restrained system pass CSS (style-shared.css) — D-03, TYPE-01</name>
  <files>docker/web/site/style-shared.css, docker/web/site/style-map.css</files>
  <read_first>
    - docker/web/site/style-shared.css `article p` (`:210`–`:217`), `article h2` (`:190`–`:198`), `article a` (`:276`–`:286`), `article blockquote` (`:266`–`:274`) — the EXACT Phase 12 serif-prose property blocks the block-body migration replicates (PATTERNS §renderBlock — the strongest analog)
    - docker/web/site/style-map.css `.block-header` (`:233`–`:243`), `.block-tension` (`:249`–`:258`), `.block-body` + `.block-body h2` + `.block-body p` (`:264`–`:277`), `.evolution` + `.evolution > h2` (`:283`–`:295`), `.timeline-*` (`:96`–`:148`), `.timeline-show-all` (`:300`–`:315`) — the dark rules being de-darkened/folded
    - .planning/phases/13-agent-economy-grid/13-UI-SPEC.md §"Component Inventory" item 6 (Block detail), §Typography "Block detail" table, §"What This Replaces" rows for block-header/tension/body/evolution/show-all
    - .planning/phases/13-agent-economy-grid/13-PATTERNS.md §renderBlock + §"Serif-Prose Migration" + §"Security Invariants"
  </read_first>
  <action>
    Complete the delete-and-fold: move the de-darkened block rules into `style-shared.css` and DELETE the dark originals from `style-map.css`. CRITICAL mechanism (PATTERNS gotcha 3): the markdown body is wrapped in `<section class="block-body">` (NOT `<article>`), so the Phase 12 `article p` rules do NOT reach it. Choose option (a) — scope NEW `.block-body p/ul/ol/li/h2/a` rules (keeps the magazine layer Newsletter-only per D-03; do NOT add `.block-body` to the `article` selector list). `.block-body p, .block-body li`: copy the `article p` block verbatim (serif `var(--serif)` 18px/400 `var(--ink-soft)` line-height 1.62, margin-bottom 16px). `.block-body h2`: copy `article h2` (serif 24px/600/1.2 `var(--ink)`) — replaces the dark Georgia 21px. `.block-body a`: copy `article a` (`var(--accent-ink)` underline). `.block-body` wrapper keeps `margin-bottom: var(--space-2xl)`. `.block-header`: keep the flex baseline/space-between layout + `margin-bottom: var(--space-lg)`; `.block-header h1` → serif `var(--serif)` 24px/600 line-height 1.2 `var(--ink)` (Phase 11 heading step — reads smaller than the hub `.page-title` display hero, D-06; replaces inherited dark Georgia 29px). `.block-tension`: light surface — copy the `article blockquote` stripe/surface treatment (`border-left: 3px solid var(--accent)`, `background: var(--accent-soft)` per the Phase 12 blockquote analog, or `--surface` — UI-SPEC §6 allows either), serif italic 18px (body step) line-height 1.6 `var(--ink-soft)`, `padding: var(--space-md) var(--space-lg)`, `margin-bottom: var(--space-xl)`. `.evolution > h2` ("EVOLUTION"): mono section-label step — `var(--mono)` 11px/600 uppercase `letter-spacing:.18em` `var(--ink-faint)` (replaces dark Courier 13px). Timeline entries (`.timeline-entry`, `.timeline-line1`, `.timeline-what`, `.timeline-why`): de-dark to serif `what`/`why` at 18px/400 `var(--ink-soft)` body step; `.timeline-date`/`.timeline-source`/`.timeline-sep` stay mono chrome (`.timeline-source` → `var(--accent-ink)`, NOT `--accent-tier`; the `var(--accent-tier, var(--accent))` fallback at `:133`/`:309` must become plain `var(--accent-ink)`/`var(--accent)`). `.timeline-show-all`: de-dark — mono `var(--mono)` 12.5px, `border-radius: var(--radius-btn)` (8px, from 4px), `border: 1px solid var(--line)`, `color: var(--accent-ink)`, `background: transparent`, hover `background: var(--surface)` (replacing the dark `rgba(255,255,255,.02)`). No `'Courier New'`, no `Georgia` literal, no `--accent-tier`, no dark `rgba(255,255,255,…)` survives in any folded rule. Keep all timeline-entry structural markup contracts (the `:not([data-source])` defensive rule may move too). Do NOT touch Phase 12 `article *` rules.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node -e "const fs=require('fs');const s=fs.readFileSync('docker/web/site/style-shared.css','utf8');const m=fs.readFileSync('docker/web/site/style-map.css','utf8');const fail=[];if(!/\.block-body p/.test(s))fail.push('.block-body p serif rule missing from style-shared.css');const bb=s.match(/\.block-body p[\s\S]*?\}/)?.[0]||'';if(!/var\(--serif\)/.test(bb))fail.push('.block-body p is not serif');if(!/\.block-header h1/.test(s))fail.push('.block-header h1 rule missing');if(/Courier New|Georgia,/.test(s.match(/\.block-[\s\S]*?\.status|\.timeline[\s\S]*$/)?.[0]||s))fail.push('Courier/Georgia literal remains in folded block rules');if(/var\(--accent-tier\)/.test(s))fail.push('--accent-tier still referenced in style-shared.css');if(/\.block-tile|\.block-header|\.block-tension|\.block-body|\.timeline-show-all/.test(m))fail.push('dark block rules still in style-map.css (not folded)');if(!/var\(--radius-btn\)|border-radius:\s*8px/.test(s.match(/\.timeline-show-all[\s\S]*?\}/)?.[0]||''))fail.push('show-all button radius not in-set');if(fail.length){console.error('FAIL:\n'+fail.join('\n'));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `style-shared.css` contains a `.block-body p` (and `.block-body li`) rule using `var(--serif)`, 18px, `var(--ink-soft)`, line-height 1.62
    - `style-shared.css` contains a `.block-body h2` rule at serif 24px/600 and a `.block-body a` rule at `var(--accent-ink)`
    - `style-shared.css` contains a `.block-header h1` rule at serif 24px/600 (not 29px/Georgia)
    - `style-shared.css` `.block-tension` uses `border-left: 3px solid var(--accent)` and serif italic body text
    - `style-shared.css` `.evolution > h2` uses `var(--mono)` 11px/600 uppercase
    - `style-shared.css` `.timeline-show-all` uses `var(--radius-btn)` (or `8px`), `var(--line)` border, and `var(--accent-ink)` text
    - No `'Courier New'`, `Georgia` literal, `--accent-tier`, or dark `rgba(255,255,255,...)` survives in the folded block/timeline rules
    - `style-map.css` contains no `.block-tile` / `.block-header` / `.block-tension` / `.block-body` / `.timeline-show-all` rule
  </acceptance_criteria>
  <done>The block reading view's header, tension card, markdown body prose, Evolution heading, timeline entries, and show-all button all render on the light/serif single-accent system; the dark block rules are deleted from `style-map.css`.</done>
</task>

<task type="auto">
  <name>Task 2: Status view de-dark CSS + complete style-map.css disposition (style-shared.css / style-map.css) — D-02</name>
  <files>docker/web/site/style-shared.css, docker/web/site/style-map.css, docker/web/site/index.html</files>
  <read_first>
    - docker/web/site/style-map.css `.status-row` + `.status-title` + `.status-subtitle` + `.status-synth` (`:317`–`:348`) — the dark status rules being de-darkened
    - docker/web/site/style-shared.css `.entry-preview` (`:173`) / `article p` (`:210`) — the serif body property pattern the status title/subtitle land on
    - docker/web/site/index.html cascade order (`:10`–`:12`) — `style-map.css` is `<link>`ed LAST; if the file is emptied/deleted the `<link>` must be removed
    - .planning/phases/13-agent-economy-grid/13-UI-SPEC.md §"Component Inventory" item 7 (Status view), §Typography "Status" table, §"What This Replaces" status-row row
    - .planning/phases/13-agent-economy-grid/13-PATTERNS.md §renderStatus + §"Shared Patterns / CSS-DISPOSITION" (the delete-and-fold end-state)
  </read_first>
  <action>
    Fold the de-darkened status rules into `style-shared.css` and DELETE the dark originals from `style-map.css`. `.status-row`: keep the flex layout + `padding: 12px var(--space-lg) 12px var(--space-lg)` (preserve the `12px 20px 12px 24px` rhythm — the asymmetric left padding accommodates the stripe), `border-left: 3px solid var(--accent)` (was `--accent-tier`), `margin-bottom: var(--space-sm)`; the row sits on `var(--bg)` (no dark surface). `.status-title` → serif `var(--serif)` 18px/400 `var(--ink)` (body step; was Georgia 17px — the 1px change is imperceptible and lands on the locked body step). `.status-subtitle` → serif `var(--serif)` 18px/400 `var(--ink-soft)` (TYPE-01: descriptive text becomes serif; was Courier 14px), keep `flex:1`. `.status-synth` → mono `var(--mono)` 14px `var(--ink-faint)` (metadata stays mono chrome; was Courier 13px), keep `flex-shrink:0`. The `.tier-label` is shared with the hub and was already migrated in Plan 01 — do not redefine it. Now COMPLETE the disposition: after Plan 01 + this plan's folds, `style-map.css` should contain only the (already-de-darkened-in-Plan-01) `.maturity-pill` if it was left there, or be empty of dark theme. Adopt delete-and-fold fully: if every remaining rule has been folded into `style-shared.css`, DELETE `docker/web/site/style-map.css` and remove its `<link rel="stylesheet" href="/style-map.css">` line from `index.html` (`:12`). If a small residue is cleaner to keep (planner discretion), the file may remain but MUST contain nothing dark/Courier/Georgia/per-tier — verify it is empty of those. Restyle the status empty-state at `app.js:652` is a JS-touch (Task 3); here only the CSS/markup disposition. Confirm the cascade still resolves: `style-base.css` (tokens) → `style-shared.css` (all component rules including the folded map/block/status) is the final two-`<link>` topology matching the Phase 12 "all component rules live in style-shared.css" end-state.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node -e "const fs=require('fs');const s=fs.readFileSync('docker/web/site/style-shared.css','utf8');const html=fs.readFileSync('docker/web/site/index.html','utf8');const mapExists=fs.existsSync('docker/web/site/style-map.css');const fail=[];if(!/\.status-row/.test(s))fail.push('.status-row not folded into style-shared.css');const sr=s.match(/\.status-row\s*\{[\s\S]*?\}/)?.[0]||'';if(!/border-left:\s*3px solid var\(--accent\)/.test(sr))fail.push('.status-row stripe not --accent');const st=s.match(/\.status-title[\s\S]*?\}/)?.[0]||'';if(!/var\(--serif\)/.test(st))fail.push('.status-title not serif');const sy=s.match(/\.status-synth[\s\S]*?\}/)?.[0]||'';if(!/var\(--mono\)/.test(sy))fail.push('.status-synth not mono');if(mapExists){const m=fs.readFileSync('docker/web/site/style-map.css','utf8');if(/Courier New|Georgia,|--accent-tier|\[data-accent|rgba\(255,\s*255,\s*255/.test(m))fail.push('style-map.css still contains dark/Courier/per-tier rules');}else{if(/style-map\.css/.test(html))fail.push('style-map.css deleted but <link> still in index.html');}if(fail.length){console.error('FAIL:\n'+fail.join('\n'));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `style-shared.css` contains a `.status-row` rule with `border-left: 3px solid var(--accent)` (not `--accent-tier`)
    - `style-shared.css` `.status-title` uses `var(--serif)` 18px `var(--ink)`; `.status-subtitle` uses `var(--serif)` `var(--ink-soft)`; `.status-synth` uses `var(--mono)` 14px `var(--ink-faint)`
    - Either `style-map.css` is deleted AND its `<link>` removed from `index.html`, OR `style-map.css` contains no `'Courier New'` / `Georgia` literal / `--accent-tier` / `[data-accent]` / dark `rgba(255,255,255,...)`
    - If `style-map.css` is deleted, `index.html` no longer references `/style-map.css`
  </acceptance_criteria>
  <done>The deep-link `#/status` view renders cleanly on the light system (serif titles/subtitles, mono synth timestamps, single-accent stripe); the dark theme is fully retired — the site loads `style-base.css` + `style-shared.css` only (or a residue-free `style-map.css`).</done>
</task>

<task type="auto">
  <name>Task 3: renderBlock + renderStatusRow JS cleanup — drop dead data-accent, single-accent pill, restyle empty states (app.js) — D-03/D-02/D-05</name>
  <files>docker/web/site/app.js</files>
  <read_first>
    - docker/web/site/app.js `renderBlock()` (`:540`–`:582`) — the 4-part structure to KEEP; the `data-accent` on `.block-header` (`:545`) to drop; the `live_tension` placeholder gate (`:553`) to PRESERVE verbatim
    - docker/web/site/app.js `renderTimelineEntries()` (`:587`–`:621`) — the `safeHttpUrl` + escapeHtml security model to PRESERVE; the empty-state at `:591` to restyle
    - docker/web/site/app.js `renderStatus()` / `renderStatusRow()` (`:672`–`:692`) — the `data-accent` on `.status-row` (`:686`) to drop; the empty-state at `:652` to restyle
    - docker/web/site/app.js `renderMaturityPill` — the recolored single-accent signature from Plan 01 (Task 3) that `renderBlock`/`renderStatusRow` now call
    - .planning/phases/13-agent-economy-grid/13-UI-SPEC.md §"Component Inventory" items 4 (dots), 6 (block detail), 7 (status), §Copywriting (preserve all empty-state wording)
    - .planning/phases/13-agent-economy-grid/13-PATTERNS.md §"Security Invariants" (keep escapeHtml + safeHttpUrl + marked.parse as the sole bypass; never add status filters)
  </read_first>
  <action>
    In `renderBlock()`: drop the `data-accent="…"` attribute from the `<header class="block-header">` markup (`:545`) — the cascade it fed is deleted (D-05). Keep the four-part composition exactly: header (`<h1>` + `renderMaturityPill(block)` — the recolored single-accent dots from Plan 01), the `live_tension !== LIVE_TENSION_PLACEHOLDER` gate (`:553`) UNCHANGED (tension hidden on all 7 blocks today), the `marked.parse(bodyMd)` body wrapped in `<section class="block-body">` UNCHANGED (the sole escape-bypass, same precedent as renderArticle — do not change to raw innerHTML of any other field), and the Evolution section UNCHANGED. Do NOT add a mono kicker / display-title / lead / blockquote magazine layer (Newsletter-only, D-03). In `renderTimelineEntries()`: keep `safeHttpUrl()` gating + `escapeHtml()` on every field verbatim (security invariant); restyle the empty-state at `:591` from inline `color:var(--text-secondary)` to a serif `--ink-soft` treatment (e.g. inline `style="color:var(--ink-soft)"` or a class), wording "No timeline entries yet." UNCHANGED. In `renderStatusRow()`: drop the `data-accent="…"` attribute from the `<div class="status-row">` markup (`:686`); the pill call (`renderMaturityPill(b)`) now resolves the single-accent dots; keep every `escapeHtml()` and the `synthText` computation. Restyle the status empty-state at `:652` (`color:var(--text-secondary);font-size:15px`) to serif `--ink-soft`, wording "Status data unavailable." UNCHANGED. Restyle the block-not-found state at `:502` (`color:var(--text-secondary)`) to serif `--ink-soft`, wording "Block not found." UNCHANGED. Do NOT add any `.eq('status',…)` filter to `loadBlock` / `loadStatus` (RLS is the boundary, D-17). Do NOT change `HUB_STORYLINE` / `TIER_LABELS` / `STATUS_PAGE_HEADER` / `LIVE_TENSION_PLACEHOLDER` wording (operator-owned constants).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node --check docker/web/site/app.js && node -e "const fs=require('fs');const a=fs.readFileSync('docker/web/site/app.js','utf8');const fail=[];const rb=a.match(/function renderBlock[\s\S]*?innerHTML = headerHtml[^;]*;/)?.[0]||'';if(/data-accent/.test(rb))fail.push('renderBlock still emits data-accent');if(!/live_tension !== LIVE_TENSION_PLACEHOLDER/.test(rb))fail.push('live_tension placeholder gate not preserved');if(!/marked\.parse\(bodyMd\)/.test(rb))fail.push('marked.parse body path changed');const rs=a.match(/function renderStatusRow[\s\S]*?\n    }/)?.[0]||'';if(/data-accent/.test(rs))fail.push('renderStatusRow still emits data-accent');const rte=a.match(/function renderTimelineEntries[\s\S]*?\}\)\.join/)?.[0]||'';if(!/safeHttpUrl\(e\.source_url\)/.test(rte))fail.push('safeHttpUrl gating removed from timeline');if(/var\(--text-secondary\)/.test(a.match(/Block not found|No timeline entries yet|Status data unavailable/)?'':'')) {} ;const lb=a.match(/async function loadBlock[\s\S]*?renderBlock\(/)?.[0]||'';const ls=a.match(/async function loadStatus[\s\S]*?renderStatus\(data\);/)?.[0]||'';if(/\.eq\(.status./.test(lb)||/\.eq\(.status./.test(ls))fail.push('forbidden status filter added');if(fail.length){console.error('FAIL:\n'+fail.join('\n'));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app.js` passes `node --check`
    - `renderBlock` no longer emits `data-accent` on `.block-header`
    - `renderBlock` preserves the `live_tension !== LIVE_TENSION_PLACEHOLDER` gate and the `marked.parse(bodyMd)` body path unchanged
    - `renderBlock` emits no mono-kicker / display-title / lead / blockquote magazine markup
    - `renderStatusRow` no longer emits `data-accent` on `.status-row`
    - `renderTimelineEntries` still calls `safeHttpUrl(e.source_url)` and `escapeHtml` on every field
    - The empty-states ("Block not found.", "No timeline entries yet.", "Status data unavailable.") are restyled to `--ink-soft` serif with wording unchanged
    - No `.eq('status', …)` filter added to `loadBlock` or `loadStatus`
  </acceptance_criteria>
  <done>The block reading view and status rows render single-accent dots with no dead `data-accent`, the security model (escapeHtml / safeHttpUrl / marked.parse-only-bypass) is intact, and the empty-states are restyled — verifiable by clicking `identity-trust` from the hub (light reading view) and deep-linking `#/status`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase (anon, RLS) → app.js render | `economy_map.blocks` + `timeline_entries` rows (title, live_tension, body markdown, what_shifted, why_it_mattered, source_url) cross into the block/status DOM. RLS is the read boundary. |
| Browser DOM ← rendered HTML | Block/timeline string fields are interpolated into `innerHTML`; the markdown body is the one sanctioned escape-bypass via `marked.parse`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering (XSS) | `renderBlock` title / live_tension; `renderStatusRow` title/subtitle/synth | mitigate | Preserve the existing `escapeHtml()` on every DB string — unchanged. Dropping `data-accent` removes markup, never adds an unescaped field. |
| T-13-05 | Tampering (XSS via markdown) | `marked.parse(bodyMd)` in `.block-body` | accept | This is the single existing escape-bypass (same precedent as `renderArticle`, prior threat T-04-03-01). The Phase 9 operator-approval publish gate is the compensating control. This plan does NOT widen the bypass — `bodyMd` is the only field passed to `marked.parse`; no other field switches to raw innerHTML. |
| T-13-06 | Tampering (XSS via URL) | timeline `source_url` | mitigate | Preserve `safeHttpUrl()` scheme-gating + `escapeHtml()` on the source link (existing CR-01 control) — unchanged. A `javascript:`/`data:` URL is dropped (no href). |
| T-13-07 | Tampering (data boundary) | `loadBlock` / `loadStatus` queries | mitigate | No `.eq('status',…)` / defensive filter added — RLS stays the read boundary (D-17 / CLAUDE.md). Block visibility is unchanged. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs — vanilla-JS, no build step, no new dependency. |

**No new high-severity threat is introduced** — the existing `escapeHtml` / `safeHttpUrl` / `marked.parse`-only-bypass sanitization model and the RLS-only read boundary are preserved unchanged.
</threat_model>

<verification>
- `node --check docker/web/site/app.js` passes.
- grep confirms `style-map.css` is deleted (and its `<link>` removed) OR contains no dark/Courier/Georgia/per-tier/`[data-accent]` rules; `style-shared.css` holds the folded block/status rules; no `--accent-tier` anywhere.
- Load the site locally (substituted preview — see the `web-static-preview-substitution` memory; serving raw `docker/web/site` crashes on `__SUPABASE_URL__`). From the hub `#/map`:
  - Click `identity-trust` (has a body): the reading view renders serif H1 24px, single-accent dots, serif body prose, `--accent-ink` links, a de-darkened Evolution timeline — no dark bg, no Courier.
  - Click a DEFERRED block (e.g. `memory-context`): header + empty dots + Evolution render cleanly (no body, no tension — gated).
  - Deep-link `#/status`: serif status titles, mono synth timestamps, single-accent stripes, no dark surface.
</verification>

<success_criteria>
- D-01/D-03: the block reading view is restyled onto the light/serif system (serif body prose, light surfaces, single-accent dots replacing the pill), structure preserved (header / tension / body / Evolution), no second magazine layer.
- D-02: `#/status` de-darked (serif titles/subtitles, mono synth, single-accent stripe) — no layout redesign.
- D-05/COLOR-02: every block/status accent is the single `--accent`/`--accent-ink`; no per-tier color or `data-accent` survives.
- TYPE-01: no monospace body paragraphs in the block reading view.
- The `style-map.css` dark theme is fully retired (delete-and-fold complete).
</success_criteria>

<output>
Create `.planning/phases/13-agent-economy-grid/13-02-SUMMARY.md` when done.
</output>
