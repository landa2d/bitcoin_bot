---
phase: 14-about-stub-polish-pass
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - docker/web/site/style-shared.css
autonomous: true
requirements: [POLISH-01]
must_haves:
  truths:
    - "Every border-radius in the live cascade (style-shared.css + style-base.css) is a radius token (var(--radius)/--radius-sm/--radius-btn/--radius-dot) or the single 50% brand dot — no raw px radius remains (D-05 confirmation gate)"
    - "The three subscribe-form 6px radii are snapped: email input -> var(--radius-sm), submit button + secondary button -> var(--radius-btn)"
    - "The named loose/off-grid spacing literals (subscribe-section, content-area, card, tier-label, label margins, article rhythms) are re-anchored onto --space-* tokens — no off-grid spacing literal remains on those swept surfaces"
    - "The sweep is CSS-only: no markup, no JS, no IDs/classes that app.js or setMode() depend on are changed"
  artifacts:
    - path: "docker/web/site/style-shared.css"
      provides: "Radius normalization (3 x 6px -> token) + vertical-rhythm re-anchoring (off-grid literals -> --space-* tokens) across the swept surfaces"
      contains: "var(--radius-btn)"
  key_links:
    - from: "style-shared.css swept declarations"
      to: "style-base.css :root tokens"
      via: "var(--radius-*) and var(--space-*) references replacing literals"
      pattern: "var\\(--space-"
---

<objective>
Apply the POLISH-01 site-wide consistency sweep on the live cascade (POLISH-01): (a) snap the three off-token `6px` subscribe-form radii to role-appropriate radius tokens, then run the D-05 confirmation grep gate over the full live cascade; (b) re-anchor the named loose / off-grid vertical-rhythm literals onto the existing `--space-*` tokens (denser / more editorial, but never cramped — "minimalist but not sparse").

Purpose: Complete the v2.0 redesign so the full site (Newsletter, Agent Economy, About) reads as one coherent system — single accent, one serif heading style, consistent spacing on the 4px grid, every radius in the locked 3/7/8/10 set (ROADMAP success criteria 2 + 3).
Output: Edited declarations in `style-shared.css` (radius + spacing), validated by the confirmation grep gate.

This plan runs in Wave 2 (after Plan 01) so the radius confirmation gate validates the whole live cascade in one pass — including the net-new `.agent-pill` `var(--radius-btn)` from Plan 01 together with the swept subscribe-form radii. Plans 01 and 02 both write `style-shared.css`; sequencing them across waves prevents any parallel write to that file.

Scope fences (do NOT cross):
- Frontend-only, CSS-only. No backend / pipeline / Supabase / data / JS / markup changes. The sweep touches no IDs/classes that `app.js` or `setMode()` depend on.
- Do NOT touch the orphaned `style.css` / `style-builder.css` / `style-impact.css` (NOT in the live cascade — no `<link>` loads them; their `6px` radii at `style.css:126/136` are explicitly OUT of scope).
- Do NOT change `style-base.css` token values, and do NOT touch the Phase-11 already-conformant chrome paddings (`.tab 8px 12px`, `.subscribe 8px 16px`, `.nav 12px var(--space-lg)`).
- `0.5px` hairline border widths are exempt (border widths, not spacing — same exemption Phase 11 took for `1px`); optional `0.5px`->`1px` is out of required scope.
- Do NOT deploy / rebuild the `agentpulse-web` container (D-06). Verification is local / in-code only.

Magnitude note (D-04): the direction (tighten, one step down where loose) is locked; the exact targets below are the UI-SPEC's resolved magnitudes. Land each literal exactly on the token named in the table — do not re-litigate the magnitude.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-about-stub-polish-pass/14-CONTEXT.md
@.planning/phases/14-about-stub-polish-pass/14-PATTERNS.md
@.planning/phases/14-about-stub-polish-pass/14-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snap the three subscribe-form 6px radii to tokens + pass the D-05 confirmation gate</name>
  <files>docker/web/site/style-shared.css</files>
  <read_first>
    - docker/web/site/style-shared.css — `#subscribe-email` (:759-769, radius at :765), `#subscribe-btn` (:801-813, radius at :808), `.btn-subscribe-secondary` (:844-854, radius at :852) — the three `6px` literals to snap
    - docker/web/site/style-base.css — `.tab` `border-radius:var(--radius-sm)` (:163), `.subscribe` `border-radius:var(--radius-btn)` (:186), the `--radius`/`--radius-sm`/`--radius-btn`/`--radius-dot` token defs (:65) and the `.dot` `border-radius:50%` (:140) — the role-correct radius targets + the one allowed non-token radius
    - .planning/phases/14-about-stub-polish-pass/14-UI-SPEC.md — §Radius "POLISH-01 radius sweep (D-05)" table (the exact element->token mapping) + the confirmation-gate definition
    - .planning/phases/14-about-stub-polish-pass/14-PATTERNS.md — Pattern Assignment #4 (the 3-row radius table + the confirmation grep gate + the list of already-tokenized radii)
  </read_first>
  <action>
    Replace exactly three `border-radius: 6px;` declarations in `style-shared.css`, each with its role-appropriate token (no other change to those rules):

    - `#subscribe-email` (line ~765): `6px` -> `var(--radius-sm)` (7px — inputs/small pills radius at 7px sitewide, matching `.tab`).
    - `#subscribe-btn` (line ~808): `6px` -> `var(--radius-btn)` (8px — buttons radius at 8px sitewide, matching `.subscribe`).
    - `.btn-subscribe-secondary` (line ~852): `6px` -> `var(--radius-btn)` (8px — buttons radius at 8px sitewide).

    Do NOT touch any other `border-radius` in the cascade — the others are already tokenized (`style-shared.css:80/90/119/203/257/515/658/679/685` and `style-base.css:163/186`). Do NOT edit `style-base.css`. Do NOT touch the orphaned `style.css` 6px radii (not in cascade).

    Then run the D-05 confirmation gate: `grep -n border-radius docker/web/site/style-shared.css docker/web/site/style-base.css` and confirm every value is `var(--radius)` / `var(--radius-sm)` / `var(--radius-btn)` / `var(--radius-dot)` or the single `50%` brand dot (`style-base.css:140`). No raw `px` radius may remain in the live cascade. This gate also validates the net-new `.agent-pill` `var(--radius-btn)` added in Plan 01 — confirm it is present and tokenized.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && ! grep -nE "border-radius:[[:space:]]*[0-9]+px" docker/web/site/style-shared.css docker/web/site/style-base.css && grep -A6 "#subscribe-email" docker/web/site/style-shared.css | grep -q "border-radius: var(--radius-sm)" && grep -A8 "#subscribe-btn {" docker/web/site/style-shared.css | grep -q "border-radius: var(--radius-btn)" && grep -A9 "\.btn-subscribe-secondary" docker/web/site/style-shared.css | grep -q "border-radius: var(--radius-btn)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "border-radius:[[:space:]]*[0-9]+px" docker/web/site/style-shared.css docker/web/site/style-base.css` returns NOTHING — no raw px radius remains in the live cascade (D-05 confirmation gate).
    - `grep -n border-radius docker/web/site/style-shared.css docker/web/site/style-base.css` shows every value is `var(--radius)` / `var(--radius-sm)` / `var(--radius-btn)` / `var(--radius-dot)` or the single `50%` brand dot.
    - `#subscribe-email` radius is `var(--radius-sm)`; `#subscribe-btn` and `.btn-subscribe-secondary` radii are `var(--radius-btn)`.
    - The net-new `.agent-pill` radius (from Plan 01) is present and `var(--radius-btn)` — validated by the same gate.
    - `style-base.css` is unchanged (`git diff --quiet docker/web/site/style-base.css` exits 0); the orphaned `style.css` 6px radii are untouched.
  </acceptance_criteria>
  <done>All three subscribe-form radii are snapped to role tokens, and the D-05 confirmation grep gate passes over the full live cascade — no raw px radius remains; the agent-pill radius is tokenized.</done>
</task>

<task type="auto">
  <name>Task 2: Re-anchor the named loose/off-grid spacing literals onto --space-* tokens</name>
  <files>docker/web/site/style-shared.css</files>
  <read_first>
    - docker/web/site/style-shared.css — the exact current declarations to re-anchor: `.content-area` (:130-134: `padding: 0 0 32px` + `padding-top: 20px`), `.tier-label` (:223: `margin: var(--space-xl) 0 12px`), `.card` (:258: `padding: 20px 20px 16px`), in-content H2 (:406: `margin: 32px 0 12px`), `article h3` (:584: `margin-bottom: 12px`), article title (:593: `margin-top: 20px`), `article blockquote` (:656: `margin: 20px 0`), `#subscribe-section` (:733-734: `padding: 40px 0`), `#subscribe-email` (:768: `margin-bottom: 12px`), `.subscribe-status` (:821: `margin-top: 12px`)
    - docker/web/site/style-base.css — the `--space-xs:4 / --space-sm:8 / --space-md:16 / --space-lg:24 / --space-xl:32 / --space-2xl:48 / --space-3xl:64` token defs and canonical consumers (`.article-entry padding: var(--space-lg) 0`)
    - .planning/phases/14-about-stub-polish-pass/14-UI-SPEC.md — §Spacing Scale "POLISH-01 vertical-rhythm sweep (D-04)" table (the exact surface -> target-token magnitudes) + the hairline-exemption + already-conformant notes
    - .planning/phases/14-about-stub-polish-pass/14-PATTERNS.md — Pattern Assignment #5 (the off-grid/loose literals table with exact current state + target tokens, and the do-NOT-touch list)
  </read_first>
  <action>
    Re-anchor each named literal to the token in the UI-SPEC §Spacing table (magnitudes are locked — land exactly on the named token, do not re-litigate). Each is a straight literal->token swap, no structural change:

    - `#subscribe-section` padding (:734): `40px 0` -> `var(--space-xl) 0` (32px).
    - `.content-area` (:131,:133): `padding: 0 0 32px` -> `padding: 0 0 var(--space-xl)`; `padding-top: 20px` -> `padding-top: var(--space-lg)` (32 / 24).
    - `.card` padding (:258): `20px 20px 16px` -> `var(--space-lg) var(--space-lg) var(--space-md)` (24/24/16).
    - `.tier-label` margin (:223): `var(--space-xl) 0 12px` -> `var(--space-xl) 0 var(--space-sm)` (32/0/8).
    - In-content H2 margin (:406): `32px 0 12px` -> `var(--space-xl) 0 var(--space-sm)` (32/0/8).
    - `article h3` margin-bottom (:584): `12px` -> `var(--space-sm)` (8px).
    - Article title margin-top (:593): `20px` -> `var(--space-lg)` (24px).
    - `article blockquote` margin (:656): `20px 0` -> `var(--space-lg) 0` (24px — consistency over micro-tighten; keep the reading-view generous).
    - `#subscribe-email` margin-bottom (:768): `12px` -> `var(--space-sm)` (8px).
    - `.subscribe-status` margin-top (:821): `12px` -> `var(--space-sm)` (8px).

    Do NOT touch the `0.5px` hairline border widths (`.content-area:132`, `#subscribe-section:736`, `.bottom-bar`, `#subscribe-email:764`) — border widths, exempt. Do NOT touch the Phase-11 already-conformant chrome paddings (`.tab 8px 12px`, `.subscribe 8px 16px`, `.nav 12px var(--space-lg)`). Do NOT edit `style-base.css`. The net-new About/agent-pill spacing from Plan 01 is already on-token (`.agent-row gap var(--space-md)` / `margin var(--space-lg) 0`, `.about p margin-bottom var(--space-md)`) — no further change.

    Line numbers will shift slightly after Task 1's radius edits; locate each declaration by its selector + the current literal value named above, not by absolute line number alone.
  </action>
  <verify>
    <automated>! grep -nE "padding:[[:space:]]*40px" docker/web/site/style-shared.css && ! grep -nE "padding:[[:space:]]*20px 20px 16px" docker/web/site/style-shared.css && ! grep -nE "padding-top:[[:space:]]*20px" docker/web/site/style-shared.css && grep -A4 "#subscribe-section {" docker/web/site/style-shared.css | grep -q "var(--space-xl) 0" && grep -A6 "\.card {" docker/web/site/style-shared.css | grep -q "var(--space-lg) var(--space-lg) var(--space-md)" && git diff --quiet docker/web/site/app.js docker/web/site/index.html docker/web/site/style-base.css</automated>
  </verify>
  <acceptance_criteria>
    - No off-grid literal remains on the swept surfaces: `grep -nE "padding:[[:space:]]*40px" docker/web/site/style-shared.css` and `grep -nE "20px 20px 16px" docker/web/site/style-shared.css` and `grep -nE "padding-top:[[:space:]]*20px" docker/web/site/style-shared.css` all return nothing.
    - `#subscribe-section` padding is `var(--space-xl) 0`; `.card` padding is `var(--space-lg) var(--space-lg) var(--space-md)`; `.tier-label` and in-content H2 bottom margins are `var(--space-sm)`.
    - The hairline `0.5px` border widths are untouched; the Phase-11 chrome paddings (`8px 12px` tabs, `8px 16px` subscribe) are untouched.
    - CSS-only: `git diff --quiet docker/web/site/app.js docker/web/site/index.html docker/web/site/style-base.css` exits 0 (no JS, no markup, no token-file change from this task).
  </acceptance_criteria>
  <done>The named loose/off-grid spacing literals are re-anchored onto --space-* tokens (denser but not cramped); no off-grid spacing literal remains on the swept surfaces; the sweep is CSS-only with app.js / index.html / style-base.css untouched by this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author -> static CSS | This plan edits only hand-authored CSS declaration values (radius + spacing literals -> tokens). No user input, no network fetch, no dynamic interpolation, no markup or JS behavior is touched. |
| (existing) client -> subscribe API | Pre-existing boundary on the subscribe form. This plan changes only the form's `border-radius` / margin values — not its behavior, validation, IDs, or submit logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | subscribe-form CSS (radius/margin) | accept | The sweep edits only presentation values (`border-radius`, `padding`, `margin`) on existing rules. No ID/class that `app.js` or `setMode()` depends on is renamed or removed; the `git diff --quiet app.js index.html` gate proves no behavioral surface changed. No `innerHTML`, no input handling, no auth touched. |
| T-14-04 | Denial of Service / regression | live cascade integrity | accept | CSS-only literal->token swaps cannot break parsing (tokens are pre-defined in `style-base.css :root`); the confirmation grep gate proves no stray raw value remains. No build step exists to fail. |
| T-14-SC | Tampering (supply chain) | npm/pip/cargo installs | accept (N/A) | No package-manager installs. Hand-authored CSS edits only, no new dependency, no build step. No legitimacy gate required. |

No `high`-severity threat is introduced by CSS value normalization on a read-only public page.
</threat_model>

<verification>
Local / in-code only (no container rebuild, no live deploy — D-06):

1. Radius D-05 gate: `grep -nE "border-radius:[[:space:]]*[0-9]+px" docker/web/site/style-shared.css docker/web/site/style-base.css` returns nothing; every `border-radius` is a `var(--radius*)` token or the one `50%` brand dot. The agent-pill `var(--radius-btn)` (Plan 01) is included and tokenized.
2. Spacing gate: the named off-grid literals (`40px`, `20px 20px 16px`, `padding-top: 20px`, the `12px` label margins) are gone from the swept surfaces and replaced by `--space-*` tokens.
3. CSS-only / no-regression: `git diff --quiet docker/web/site/app.js docker/web/site/index.html docker/web/site/style-base.css` exits 0 (this plan changed only `style-shared.css`); the orphaned `style.css` is untouched.
4. Coherence (ROADMAP success criterion 3): after the sweep, every radius is in the 3/7/8/10 set and every swept gap is on the 4px grid — the full site reads as one system.
</verification>

<success_criteria>
- Radii are consistent (~7-10px) across cards, toggle, and buttons site-wide; the three subscribe-form 6px outliers are snapped (input 7px / buttons 8px) and the D-05 confirmation gate passes (POLISH-01, ROADMAP success criterion 2).
- Vertical rhythm is tightened and token-anchored (denser / more editorial, not sparse, never cramped) across the named surfaces (D-04).
- The full site (Newsletter, Agent Economy, About) reads as one coherent system — single accent, one serif heading style, consistent spacing on the 4px grid, every radius in the 3/7/8/10 set (ROADMAP success criterion 3).
- CSS-only: no JS, no markup, no IDs/classes `app.js`/`setMode()` depend on; `style-base.css` and the orphaned sheets untouched.
- No deploy / rebuild performed (D-06).
</success_criteria>

<output>
Create `.planning/phases/14-about-stub-polish-pass/14-02-SUMMARY.md` when done. Note in the SUMMARY that the radius D-05 confirmation gate passed over the full live cascade (including the Plan-01 agent-pill), and that the live deploy + accumulated 11/12/13/14 browser-UAT verification remains a SEPARATE operator-approved post-phase step (D-06).
</output>
