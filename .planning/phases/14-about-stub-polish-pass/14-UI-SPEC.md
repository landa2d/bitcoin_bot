---
phase: 14
slug: about-stub-polish-pass
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-05
---

# Phase 14 — UI Design Contract

> Visual and interaction contract for the **final v2.0 phase**: ABOUT-01 (flesh out the `#/about` stub into the real "What is AgentPulse" page) + POLISH-01 (site-wide vertical-rhythm tightening + radius normalization). Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Design system is LOCKED by Phase 11 — this contract REUSES it verbatim and does not re-derive it.** Palette hexes, the single-accent reservation list (COLOR-02), the serif/mono type scale (weights 400/600 only), the 4px-grid spacing tokens (`--space-xs..3xl`), and the radius token set (3/7/8/10) all come from `11-UI-SPEC.md` + `style-base.css :root`. The only NET-NEW design surface this phase introduces is the **agent-pill component** (ported from the mockup into the Phase-11 token system) and a tightened-but-token-anchored vertical rhythm. Everything else is reuse.
>
> **Stack note:** Not a React/Tailwind/Next/shadcn project. Vanilla-JS SPA — static `index.html` + hand-authored CSS custom properties, served by Caddy as the `agentpulse-web` container. Frontend lives in `docker/web/site/`. The live cascade is exactly two stylesheets, loaded in order: `style-base.css` (tokens + display classes, first → cascade control) then `style-shared.css` (component rules, where About + agent-pill + the polish sweep land). `components.json` / `tailwind.config` / `package.json` confirmed absent. shadcn gate → N. Registry safety: **not applicable**.
>
> **Scope guardrails (from 14-CONTEXT.md):** Frontend-only. This phase does NOT touch backend/pipeline/Supabase/data, does NOT add the pipeline diagram (ABOUT-02, deferred), does NOT add dark mode (DARK-01, deferred), does NOT re-wire the `#/about` route (already wired in `app.js`), and does NOT deploy to the live site (D-06 — deploy is a separate operator-approved post-phase step). The orphaned legacy stylesheets `style.css` / `style-builder.css` / `style-impact.css` are NOT in the live cascade (no `<link>` references them — confirmed) and are explicitly out of scope for the radius sweep.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (vanilla-JS SPA, hand-authored CSS variables) — **inherited from Phase 11, unchanged** |
| Preset | not applicable |
| Component library | none |
| Icon library | none — Unicode glyphs already in the codebase (`←` U+2190 back-control, `·` U+00B7 metadata separator). Do not add an icon dependency. |
| Font (reading/titles) | **Source Serif 4** (`var(--serif)` → `'Source Serif 4', Georgia, serif`) |
| Font (UI chrome only) | **IBM Plex Mono** (`var(--mono)` → `'IBM Plex Mono', ui-monospace, monospace`) |
| Font loading | already loaded via the trimmed Google-Fonts `<link>` in `index.html` (weights 400/600 + serif italic 400). **No font change this phase** — the agent-pill name uses mono 600, descriptions use serif/mono 400, both already loaded. |

No new tokens, no new fonts, no new weights. The agent-pill, the About prose, and every tightened gap reference existing `:root` tokens from `style-base.css`.

---

## Spacing Scale

**Locked from Phase 11 — reused verbatim. Every value below is a multiple of 4px.** The token set is unchanged; POLISH-01 (D-04) does NOT add tokens — it **re-anchors loose/off-grid literals onto these existing tokens** and tightens vertical gaps within them.

| Token | Value | Usage |
|-------|-------|-------|
| `--space-xs` | 4px | Progress-dot gaps, agent-pill name→desc gap, inline icon gaps |
| `--space-sm` | 8px | Compact element spacing, brand gap, title→date gaps, pill grid label margin |
| `--space-md` | 16px | Default element gap; **agent-row grid gap**; card-internal gap; about paragraph bottom margin |
| `--space-lg` | 24px | Section side padding; card padding; agent-pill internal padding region |
| `--space-xl` | 32px | Section vertical breaks; section-label top margin |
| `--space-2xl` | 48px | Major section breaks |
| `--space-3xl` | 64px | Page-bottom breathing room |

### POLISH-01 vertical-rhythm sweep (D-04 — magnitude is this contract's call, token-anchored)

Direction (locked by D-04): **denser / more editorial, but minimalist-NOT-sparse — never cramped.** Magnitude is delegated to this UI-SPEC. The sweep snaps the live-cascade off-grid literals to the nearest token **one step tighter** where it reads loose, and confirms no off-grid spacing remains. Concrete re-anchors (executor target — all in `style-shared.css` unless noted):

| Surface | Current (off-grid / loose) | Target (token) | Rationale |
|---------|---------------------------|----------------|-----------|
| `#subscribe-section` padding | `40px 0` (line ~733) | `var(--space-xl) 0` → **32px** | 40px is between `--space-xl`/`--space-2xl`; step down to 32px tightens the largest loose block-break. |
| `.content-area` padding | `0 0 32px` + `padding-top: 20px` (lines ~130–134) | `0 0 var(--space-xl)` + `padding-top: var(--space-lg)` → bottom **32px**, top **24px** | 20px is off-grid → 24px (nearest token, still airy under the divider). |
| `.card` padding | `20px 20px 16px` (line ~258) | `var(--space-lg) var(--space-lg) var(--space-md)` → **24/24/16** | 20px off-grid → 24px; keeps card density consistent with section padding. |
| `.tier-label` margin | `32px 0 12px` (line ~223) | `var(--space-xl) 0 var(--space-sm)` → **32/0/8** | 12px label→grid gap off-grid → 8px tightens the group header. |
| `.section-label` family (mono labels) | bottom margins at `12px` literals (e.g. lines ~406, ~768, ~821) | `var(--space-sm)` → **8px** | Label-to-content gaps land on 8px consistently. |
| `.card` title `margin: 20px 0` / list `20px 0` (lines ~593, ~656) | `20px` literals | `var(--space-lg) 0` → **24px** | Off-grid → token; consistency over micro-tightening here (these are reading-view rhythms, keep generous). |

**Net-new About spacing (agent-pill block):** `.agent-row` grid `gap: var(--space-md)` (16px); `margin: var(--space-lg) 0` (24px above/below the row); `.about p` bottom margin `var(--space-md)` (16px); the eyebrow→title→page-sub→prose stack reuses the `.about-stub` rhythm already present (`padding-top: var(--space-2xl)`, `.page-title margin-top: var(--space-md)`).

**Hairline note:** existing `0.5px` borders (`.content-area`, `#subscribe-section`, `.bottom-bar`, subscribe input) are border *widths*, not spacing — exempt from the 4px rule by definition (same exemption Phase 11 took for `1px`). The sweep does NOT need to touch them; if the executor normalizes `0.5px`→`1px` for crispness it is optional and out of POLISH-01's required scope.

**Exceptions:** none — every spacing value lands on a 4px-grid token after the sweep. The chrome control paddings locked in Phase 11 (`8px 12px` tabs, `8px 16px` subscribe, `8px 20px` toggle, `12px` nav vertical) are already 4px-conformant and are NOT changed.

---

## Radius

**Locked from Phase 11 — the 3 / 7 / 8 / 10 set. Reused verbatim. POLISH-01 (D-05) does NOT add radii — it snaps the only three off-token literals onto this set and confirms none remain.**

| Token | Value | Usage |
|-------|-------|-------|
| `--radius` | 10px | Cards, mode-toggle wrapper, dashed note boxes — default card radius |
| `--radius-sm` | 7px | Tabs, small inline pills, tags, **subscribe email input (after sweep)** |
| `--radius-btn` | 8px | Subscribe/toggle/primary buttons, **agent-pill (net-new)**, **subscribe submit + secondary buttons (after sweep)** |
| `--radius-dot` | 3px | Progress-dot pills |

### POLISH-01 radius sweep (D-05 — locked targets)

The only off-token radii **in the live cascade** are three hardcoded `6px` values in the subscribe form, all in `style-shared.css`. Snap each to the role-appropriate token so inputs/buttons radius identically to the rest of the site:

| Element | Selector | Line | Current | Target token | Reason |
|---------|----------|------|---------|--------------|--------|
| Subscribe email **input** | `#subscribe-email` | ~765 | `6px` | `var(--radius-sm)` → **7px** | Inputs radius at 7px sitewide (matches tab/pill convention). |
| Subscribe submit **button** | `#subscribe-btn` | ~808 | `6px` | `var(--radius-btn)` → **8px** | Buttons radius at 8px sitewide. |
| Secondary **button** (footer bar) | `.btn-subscribe-secondary` | ~852 | `6px` | `var(--radius-btn)` → **8px** | Buttons radius at 8px sitewide. |

**Net-new agent-pill radius:** `var(--radius-btn)` → **8px** (matches the mockup's `border-radius:8px` on `.agent-pill`, which already lands on the locked token — no reconciliation needed).

**Confirmation gate (D-05):** after the snap, the executor must `grep -n border-radius docker/web/site/style-shared.css docker/web/site/style-base.css` and verify **every** value is `var(--radius)` / `var(--radius-sm)` / `var(--radius-btn)` / `var(--radius-dot)` (or the one `50%` brand dot). No raw px radius may remain in the live cascade. (The orphaned `style.css` 6px radii at lines 126/136 are NOT in the cascade — no `<link>` loads them — and are out of scope; do NOT edit them.)

Convention (unchanged): nothing on the site uses a radius outside the 3/7/8/10 set; no `50%` shapes except the 9px brand `--dot`.

---

## Typography

**Locked from Phase 11 — reused verbatim. ≤4 serif sizes, ≤3 mono sizes, exactly 2 weights total (400, 600).** This phase adds NO new size, weight, or family. The About page reuses the existing serif body + the `.page-title` / `.eyebrow` display classes; the agent-pill reuses one mono step + one serif/mono description step.

### Serif scale (Source Serif 4 — everything you READ)

| Role | Size | Weight | Line Height | Usage this phase |
|------|------|--------|-------------|------------------|
| Body | 18px | 400 | 1.62 | About prose paragraphs (`.about p`). Serif, no monospace body (TYPE-01). |
| List/card title | 20px | 600 | 1.25 | (not introduced on About; agent-pill name is mono, see below) |
| Heading (in-content H2) | 24px | 600 | 1.2 | not used on the stub (no sub-sections this phase — diagram is ABOUT-02, deferred) |
| Page title (display) | `clamp(30px, 5vw, 46px)` | 600 | 1.12 | About `.page-title` "What is AgentPulse" — reuses the existing `.page-title` class verbatim. |

### Mono scale (IBM Plex Mono — UI chrome ONLY)

| Role | Size | Weight | Usage this phase |
|------|------|--------|------------------|
| Eyebrow / kicker · section label | 11px | 600 | About eyebrow "Behind the Briefing" — reuses `.eyebrow` class (accent-ink, UPPERCASE, `.2em`). |
| Page-sub / metadata | 14px | 400 | About page-sub "A newsletter written by a multi-agent system" — mono 14px `--ink-faint` (matches the locked metadata/page-sub step). |
| **Agent-pill name (`.an`)** | **13px** | **600** | Net-new. Agent name in mono 600 `--accent-ink` — chrome label, NOT body. Sits on the existing mono chrome family (13px is within the locked 11–14px mono band; ported from mockup `.an` 13px). |

**Agent-pill description (`.ad`):** the mockup renders `.ad` at serif 13.5px `--ink-soft`. **Reconciliation:** keep it as a short serif descriptor (one line, `line-height:1.4`, `--ink-soft`) — it is reading content, so serif is correct (TYPE-01). Size **13.5px** is below the 18px body step but is acceptable as a *compact card-caption* role analogous to the 16px table-cell serif (`article td`) and the 13px mono metadata already in the system; it is NOT a body paragraph. If the checker flags the half-pixel, round to **14px** (no behavioral change). Both land within the established reading-caption band.

**Weight policy (unchanged, exactly 2):** 400 (serif body, mono page-sub, serif pill description) and 600 (page-title, eyebrow, mono pill name). 500 and 700 remain retired. No new Google-Fonts weight requested.

---

## Color

**Locked from Phase 11 — the single light-mode palette, ONE violet accent (COLOR-01/COLOR-02). Reused verbatim. This phase introduces NO new hue and adds NO accent surface beyond the reserved list.**

| Role | Value | Usage this phase |
|------|-------|------------------|
| Dominant (60%) — `--bg` | `#faf8f5` | About page background (inherited body bg). |
| Surface (30%) — `--surface` | `#ffffff` | **Agent-pill background** (raised panel on the warm bg — the pill is a surface, COLOR-aligned). |
| Ink (text) — `--ink` | `#1a1916` | About prose primary text (`.about p`). |
| Ink soft — `--ink-soft` | `#55514a` | Secondary About prose (`.body-soft` paragraphs), agent-pill description (`.ad`), back-control rest. |
| Ink faint — `--ink-faint` | `#8a857c` | About page-sub (mono metadata), the infra prose sentence if rendered as a caption. |
| Line — `--line` | `#e7e2da` | **Agent-pill border** (1px hairline). |
| Line strong — `--line-strong` | `#d8d2c7` | (no new use this phase) |
| Accent (10%) — `--accent` | `#5b3df5` | Brand dot, Subscribe button (inherited chrome) — no NEW accent fill added on About. |
| Accent soft — `--accent-soft` | `#efeaff` | Active "What is AgentPulse" tab fill (inherited nav chrome). |
| Accent ink — `--accent-ink` | `#4a2fd6` | **Agent-pill name (`.an`)** + any inline links in About prose + active-tab text. |
| Destructive | not applicable | Read-only public site — no destructive actions this phase. |

**Accent reserved for (inherited from Phase 11 — NO additions):**
1. Links (anchor text in About prose) — `--accent-ink`.
2. The active nav tab ("What is AgentPulse" when on `#/about`) — `--accent-soft` bg + `--accent-ink` text + `#ddd2ff` border.
3. Card left-border accent (economy grid — not on About).
4. Progress dots filled state (not on About).
5. Subscribe button — solid `--accent` fill.
6. Eyebrow / kicker label — `--accent-ink` ("Behind the Briefing").
7. Brand dot.
8. **Agent-pill name text (`.an`) — `--accent-ink`.** This is the ONE net-new accent surface this phase adds. Justification: the pill name functions as a kicker/label (mono, like reserved item 6), NOT as a second brand color or a per-agent palette. All five pills use the same single `--accent-ink` — no per-agent tinting (consistent with the Phase-13 single-accent / no-`data-accent` discipline). The agent-pill description and border stay neutral ink/line.

**Not allowed (unchanged):** any second hue, any per-agent/per-pill accent color, the retired teal, mode-driven accent swapping. The agent-row must read as one accent at one job (label-mono), echoing the economy grid's single-accent rule.

**Contrast (inherited):** `--accent-ink #4a2fd6` on `--surface #ffffff` ≈ 6.6:1 (AA) — fine for the mono pill name. `--ink-soft #55514a` on `#ffffff` ≈ 8:1 — fine for the serif pill description. `--ink #1a1916` on `--bg` ≈ 15:1 for About body. `--ink-faint` used only for mono page-sub / caption chrome, never for a reading paragraph.

---

## Component Inventory (net-new this phase)

The **only net-new component** is the agent-pill block. It is ported from the mockup's dark/standalone styles (`agentpulse-redesign-mockup.html:189–198`) into the Phase-11 token system — **not copied verbatim**. Target file: `style-shared.css` (component layer), in or adjacent to the About rules; mechanical section placement is planner discretion per the hand-authored-CSS / cascade-order convention.

### `.agent-row` (grid container)
- `display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: var(--space-md);` (16px tight gap — matches the economy-grid gap rhythm)
- `margin: var(--space-lg) 0;` (24px above/below)
- Mobile (`≤640px`): collapses to a single column automatically via `auto-fit`/`minmax` (no extra media query required; the existing `@media(max-width:640px)` grid override pattern is available if a hard 1-col is preferred).

### `.agent-pill` (surface card)
- `border: 1px solid var(--line); border-radius: var(--radius-btn); background: var(--surface); padding: var(--space-md) var(--space-md);` (16px — denser than the mockup's `14px 16px`, snapped to grid per D-04)
- No left-accent stripe (that treatment is reserved for the economy grid's MAP-02 cards — keep the About pills visually distinct from economy cards so the two sections don't read as the same component).
- No hover lift required (static informational pills, not links).

### `.agent-pill .an` (agent name)
- `font-family: var(--mono); font-size: 13px; font-weight: 600; color: var(--accent-ink);`

### `.agent-pill .ad` (one-line role)
- `font-family: var(--serif); font-size: 13.5px; color: var(--ink-soft); margin-top: var(--space-xs); line-height: 1.4;`

**Porting checklist (executor):** (1) every color is a `:root` token, no literal hex; (2) radius is `var(--radius-btn)`, not the mockup's `8px` literal; (3) gaps/padding/margins are `--space-*` tokens, not the mockup's `12px/14px/16px/24px` literals; (4) name is mono-chrome, description is serif-reading (TYPE-01/02); (5) no `data-accent` / per-pill tint.

---

## Copywriting Contract

**ABOUT-01 content (D-01/D-02/D-03).** Structure adopts the mockup's full About shape; **copy is reconciled to the REAL 8-service AgentPulse system — NOT the mockup's inaccurate 5-agent wording.** The strings below are a **draft in the mockup's spirit, corrected for accuracy** — per D-02/D-03 and the project's "editorial framing in human hands" constraint, the **operator reviews/edits this copy in the plan or at execution**; it is editorially owned, not auto-final. Capture the structure and the accuracy bar as the contract; treat the exact wording as operator-reviewable.

### Eyebrow → Title → Page-sub
| Element | Copy (draft, operator-reviewable) |
|---------|-----------------------------------|
| Eyebrow (mono, accent) | `Behind the Briefing` |
| Page title (serif display) | `What is AgentPulse` |
| Page-sub (mono, ink-faint) | `A newsletter written by a multi-agent system` |

(All three match the mockup verbatim — they are already accurate; the page-sub is true to the real system.)

### Prose paragraphs (3 — reconciled to the real 8-service system, D-02)
| # | Copy (draft, operator-reviewable) |
|---|-----------------------------------|
| P1 (intro, `.about p`) | `AgentPulse is a weekly intelligence briefing on the AI agent economy — its economics, governance, and trust infrastructure. Each edition is produced by eight cooperating services that ingest the week's signals, synthesize findings, and draft the briefing.` |
| P2 (`body-soft`) | `Specialized agents each own one job and hand work between each other through a background scheduler. Every model call is metered against a per-agent wallet, so the cost of producing an edition is itself part of what we track — and nothing is published without human approval.` |
| P3 (`body-soft`, infra sentence — D-03) | `Behind the five content agents sits a supporting layer: a conversational middleware that routes operator commands, an LLM proxy that governs budgets and wallets across every model call, and a web front end that serves what you are reading now.` |

P3 is the **single accurate prose sentence covering the supporting layer** required by D-03 (Gato Brain routing/middleware, LLM Proxy budget/wallet governance, Web) — it keeps the pill grid to the five content agents while staying truthful about the full system.

### Agent-pill roster (5 content agents — D-03; one accurate one-line role each)
Render exactly these five pills, in this order. Roles are reconciled to the actual codebase (CLAUDE.md service map), corrected from the mockup's simplifications:

| `.an` (name) | `.ad` (one-line role — draft, operator-reviewable) |
|--------------|---------------------------------------------------|
| `Processor` | `Background scheduler — scrapes sources, runs the pipelines, and posts.` |
| `Analyst` | `Scores and clusters incoming signals into findings.` |
| `Research` | `Deepens context on the week's tier-1 stories.` |
| `Newsletter` | `Synthesizes the dual-mode (Technical / Strategic) editions.` |
| `Gato` | `Telegram operator interface and coding surface.` |

**Accuracy guardrails (D-02 — must hold even if the operator rewords):**
- Do NOT call the Processor a "deterministic orchestrator that routes work between agents" (mockup's P-name for it) — in the real system it is the background scheduler/monolith; command *routing* is Gato Brain's job. Keep these distinct.
- The roster is the **5 content-producing agents** only; Gato Brain, LLM Proxy, and Web appear **as prose (P3)**, NOT as pills (D-03) — adding them as pills would turn the stub into an architecture doc.
- "eight cooperating services" must stay accurate (5 content agents + Gato Brain + LLM Proxy + Web = 8) — do not regress to the mockup's "five agents" framing.

### Standard copy slots
| Element | Copy |
|---------|------|
| Primary CTA (inherited nav) | `SUBSCRIBE` (unchanged — Phase 11 locked; reuses `scrollToSubscribe()`) |
| Back control (About → home) | `← Back to Newsletter` (already present at `index.html:84`; preserve verbatim, NAV-03) |
| Empty state | not applicable — the About stub is static prose with no data fetch / no empty state. |
| Error state | not applicable — no network fetch on the About view; nothing can fail to load. |
| Destructive confirmation | not applicable — read-only public site, no destructive actions. |

**Content replaces** the current placeholder stub (`index.html:79–86`): the lede `A multi-agent intelligence platform…` + `<em>Full overview coming in Phase 14.</em>` placeholder is **removed** and replaced by the eyebrow → title → page-sub → 3 paragraphs → agent-row structure above. The existing `.about-stub` container, `.eyebrow`, `.page-title`, and `← Back to Newsletter` backlink are reused.

---

## Interaction & Routing Contract

**No new routing, no new JS behavior.** The `#/about` route is already fully wired in `app.js` (`getRoute()` :134, `showView()` :146, route→tab map :815, `route()` case :845, with `window.scrollTo(0,0)`). Phase 14 fills `#about-view .about-stub` content only.

- The "What is AgentPulse" tab already lights active on `#/about` via the route-derived `setActiveTab()` (NAV-02) — unchanged.
- The agent-pills are **static, non-interactive** (informational) — no hover state, no click target, no link. This keeps them visually distinct from the economy-grid cards (which ARE clickable links) and avoids implying navigation that doesn't exist.
- Back-control behavior unchanged (`← Back to Newsletter`).
- The POLISH-01 sweep is CSS-only — it touches no markup behavior, no JS, no IDs/classes that `app.js` or `setMode()` depend on.

---

## Visual Hierarchy

**About view focal point:** the serif `.page-title` "What is AgentPulse" — the largest type on the page, centered below the sticky nav, with the accent eyebrow above it as orientation. The eye lands on the title, then reads down through the page-sub (mono, quiet) into the three serif paragraphs; the agent-row is a scannable supporting grid (mono names rationed to the single accent-ink), and the infra sentence closes the picture. The single accent is rationed exactly as Phase 11 dictates — eyebrow + pill names + the active tab — so nothing competes with the title. One serif title leads; mono chrome and the single accent support but never out-shout it (same rule every v2.0 screen inherits).

**Site-wide after POLISH-01:** the full site (Newsletter, Agent Economy, About) reads as one coherent system — single violet accent, one serif heading style, consistent spacing on the 4px grid, and every radius in the 3/7/8/10 set (success criterion 3). The tightening is uniform (sections, cards, subscribe, chrome) so no surface reads looser than its neighbors, and none reads cramped.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| not applicable | none | not applicable — vanilla-JS SPA, no shadcn/component registry in use |

No third-party UI registries are declared or used. No registry vetting required. The agent-pill is hand-authored CSS ported from the project's own mockup, not pulled from any registry.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
