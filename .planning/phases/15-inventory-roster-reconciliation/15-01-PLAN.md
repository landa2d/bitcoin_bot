---
phase: 15-inventory-roster-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md
  - .planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md
autonomous: true
requirements: [INV-01, INV-02, ROST-01]

must_haves:
  truths:
    - "A reader can see exactly how a block is stored (columns, constraints), how the append-only trigger behaves, and how the atomic publish RPC works — from the contract doc, without reading code (SC#1)."
    - "The live maturity enum's 5 members are documented and the docs' `building` value is shown to be a non-member, with the operator-approved `building→emerging` resolution stated (SC#2)."
    - "Every slug in the reconciled roster (hub + 8 block-level entries) has a written first-publish / body-rewrite / new-block / kept-deferred disposition (SC#3)."
    - "The D-03 sort_order before/after map is present and resolves to a collision-free contiguous {1..8} (SC#3)."
    - "The D-04 hub accommodation is pinned (Option A: relax tier CHECK + 'hub' sentinel tier) as a Phase-16 instruction, not executed here (SC#3)."
  artifacts:
    - path: ".planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md"
      provides: "Live economy_map storage + serve contract + verified maturity enum (INV-01 + INV-02)"
      contains: "publish_block_version"
      min_lines: 60
    - path: ".planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md"
      provides: "Per-slug roster disposition + D-04 hub pin + D-03 collision-free reshuffle map (ROST-01)"
      contains: "negotiation-coordination"
      min_lines: 50
  key_links:
    - from: ".planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md"
      to: "supabase/migrations/033_economy_map_schema.sql"
      via: "every documented fact cites its migration line/section"
      pattern: "033"
    - from: ".planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md"
      to: ".planning/phases/15-inventory-roster-reconciliation/15-CONTEXT.md"
      via: "each disposition cites the D-NN decision it implements"
      pattern: "D-0[1-4]"
---

<objective>
Assemble the two reader-facing reconciliation deliverables for Phase 15: (1) `15-CONTRACT.md` — the live `economy_map` storage + serve contract documented from the running schema (INV-01) with the maturity enum verified (INV-02); and (2) `15-RECONCILIATION.md` — the per-slug roster disposition resolving the docs-vs-live diff (ROST-01), including the pinned D-04 hub accommodation and the D-03 collision-free sort_order reshuffle map.

Purpose: No write to `economy_map` may happen until the contract and the roster are locked and documented from the live system (not assumed). These two documents ARE the lock — Phase 16/18 execute from them.

Output: Two `.planning/` documents. **This plan writes NOTHING to `economy_map`, applies NO migration, edits NO `app.js`.** Every fact in the contract doc is sourced from in-tree migration SQL + `app.js`; every disposition cites its locked CONTEXT decision (D-01..D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-inventory-roster-reconciliation/15-CONTEXT.md
@.planning/phases/15-inventory-roster-reconciliation/15-RESEARCH.md
@.planning/phases/15-inventory-roster-reconciliation/15-VALIDATION.md
@.planning/docs/EXECUTION_BRIEF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 15-CONTRACT.md — live storage + serve contract + verified maturity enum (INV-01, INV-02)</name>
  <files>.planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md</files>

  <read_first>
  - .planning/phases/15-inventory-roster-reconciliation/15-RESEARCH.md (§INV-01 and §INV-02 — the facts are already extracted there; this task assembles them into a reader-facing contract doc, it does NOT re-derive)
  - supabase/migrations/033_economy_map_schema.sql (THE core contract: §3 enum lines 46-52, §4 `blocks` columns lines 65-81 incl. tier CHECK line 68, §5 `block_body_versions` lines 94-110, §7 `timeline_entries`, §8 append-only triggers lines ~177-253, §11 RLS lines 356-386, §13 seed lines 402-415)
  - supabase/migrations/039_publish_block_version_watermark_null_guard.sql (the CURRENT publish RPC body — atomic 4-step, SECURITY DEFINER, service_role-only GRANT line 82)
  - supabase/migrations/038_publish_block_version_watermark.sql (watermark introduction — note 039 supersedes it as the body to document)
  - supabase/migrations/041_block_body_versions_unique_open_draft.sql (UNIQUE one-open-draft-per-slug index)
  - docker/web/site/app.js (serve path: `HUB_STORYLINE` line 32 hardcoded + escapeHtml'd at line 496; `MATURITY_STAGE` line 38; `renderMaturityPill` unknown→stage 1 at line 396; block body `marked.parse` line 586)
  - .planning/phases/15-inventory-roster-reconciliation/15-CONTEXT.md (D-01 maturity remap rationale; D-05/D-06 the spine)
  </read_first>

  <action>
  Write `.planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md` as a reader-facing contract document. Every stated fact MUST cite its source as `(033:LINE)` / `(039:LINE)` / `(app.js:LINE)` so a reader can verify it. Assemble — do not paste full file bodies; quote only the specific identifiers/signatures/values named below.

  Required sections (use these exact headings):

  1. `## Block data contract (economy_map.blocks)` — table of the 8 load-relevant columns with type + constraint: `slug` (TEXT NOT NULL UNIQUE), `tier` (TEXT NOT NULL CHECK in substrate/behavior/frame — cite 033:68), `title`, `subtitle` (both NOT NULL), `sort_order` (INTEGER NOT NULL UNIQUE — a PLAIN updatable column, NO append-only trigger), `maturity` (the ENUM, NOT NULL DEFAULT nascent), `current_body_version_id` (FK → block_body_versions, nullable), `accent` (CHECK teal/purple/coral/gray). State the key fact: `blocks` has NO append-only trigger, so `sort_order` is a permitted plain UPDATE.

  2. `## Body storage (economy_map.block_body_versions)` — the body lives here, not in `blocks`: `block_slug` (FK → blocks(slug) ON DELETE RESTRICT, pinned), `body_md` (NOT NULL, pinned), `status` (TEXT CHECK draft/published/superseded, lifecycle-mutable), `proposed_maturity` (the ENUM, NOT NULL, PINNED at insert — so the D-01 remap MUST produce a valid member at INSERT time, cite 033:100). State the migration-041 invariant: at most ONE open draft per `block_slug` (UNIQUE partial index where status='draft').

  3. `## Append-only triggers — what they guard` — there are exactly 2 triggers (cite: `grep -c "CREATE TRIGGER" 033` = 2): on `block_body_versions` (BEFORE UPDATE OR DELETE — DELETE always raises; UPDATE raises if body_md/proposed_maturity/block_slug/synthesized_from_through/validator_report change; `status` and `published_at` ARE allowed to change so the RPC can flip draft→published) and on `timeline_entries` (fully immutable). State explicitly: NO trigger on `economy_map.blocks`. Explain WHY triggers not RLS: service_role bypasses RLS; only a BEFORE trigger binds it (cite 033 §8 comment, ~166-175).

  4. `## Atomic publish RPC (publish_block_version)` — signature `economy_map.publish_block_version(p_version_id uuid) RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path = economy_map, public`; service_role-only (REVOKE PUBLIC / GRANT service_role, cite 039:82); current authoritative body = migration **039** (038 added the watermark, 039 added the COALESCE null-guard). Document the atomic 4 steps: (1) UPDATE the draft → published + published_at, `AND status='draft' RETURNING` = single-winner race property; (2) RAISE EXCEPTION if not found/not draft (fail-loud); (3) supersede the prior published version for that slug; (4) UPDATE blocks: point current_body_version_id at the new version, sync maturity from proposed_maturity, advance last_synthesized_at via COALESCE. State that all 4 are one transaction.

  5. `## Read boundary (RLS — what anon sees)` — anon SELECT on `blocks` = USING(true) (sees all rows incl. deferred); on `block_body_versions` = USING(status='published') (cite 033:370 — this is why a Phase-16 unpublished load is invisible to visitors until the Phase-18 RPC runs); on `timeline_entries` = block_slug <> 'unsorted'.

  6. `## Hub serve path (current)` — `#/map` renders the hardcoded `HUB_STORYLINE` JS constant (app.js:32) escapeHtml'd as plain text at app.js:496 — the hub is NOT DB-served today. Block bodies render via `marked.parse(bodyMd)` at app.js:586, RLS-gated to published. (The D-04 accommodation that gives the hub a DB-served home is documented in 15-RECONCILIATION.md, not here.)

  7. `## Maturity enum (INV-02)` — the 5 members verbatim in order: `nascent, emerging, contested, consolidating, mature` (cite 033:46-52). State explicitly: **`building` is NOT a member** — a `block_body_versions` insert with `proposed_maturity='building'` is rejected loud by the enum. Document `MATURITY_STAGE` (app.js:38) maps emerging→2, and `renderMaturityPill` falls back unknown→stage 1 (app.js:396) — the silent mis-fill that the D-01 remap prevents. State the INV-02 resolution: the docs' `building` (on substrate slugs 1/2/3) is an operator-approved remap to `emerging` applied at LOAD time (Phase 16), no ALTER TYPE, no app.js change; `contested`/`nascent` pass through unchanged. Note flag F-2: ROADMAP SC#2 / Phase-17 wording still says pills show `building` — after D-01 the substrate trio renders `emerging` (stage 2); the verification text should read `emerging`.

  Close with a one-line statement that this contract is documented from the in-tree migration SQL + app.js (the authoritative source per CONTEXT D-05), no live SELECT issued, no write performed.
  </action>

  <verify>
    <automated>test -f .planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md && grep -q "publish_block_version" .planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md && grep -q "nascent" .planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md && grep -qi "building" .planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md && echo CONTRACT_OK</automated>
  </verify>

  <acceptance_criteria>
  - File exists: `test -f .planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md`
  - The 7 required section headings are present: `grep -c '^## ' 15-CONTRACT.md` is >= 7 AND all of these match: `grep -q "Block data contract"`, `grep -q "Append-only triggers"`, `grep -q "publish RPC\|publish_block_version"`, `grep -q "RLS\|anon"`, `grep -q "Hub serve path"`, `grep -q "Maturity enum"` (run each on the file).
  - The enum 5 members are all named: `grep -q "nascent" && grep -q "emerging" && grep -q "contested" && grep -q "consolidating" && grep -q "mature"` on 15-CONTRACT.md.
  - The `building`-not-a-member fact is stated: `grep -iq "building" 15-CONTRACT.md` AND the doc states the `building→emerging` remap (`grep -q "emerging" 15-CONTRACT.md`).
  - The documented tier-CHECK fact matches live source — the live assertion still holds: `grep -n "CHECK (tier IN ('substrate','behavior','frame'))" supabase/migrations/033_economy_map_schema.sql` returns line 68 (proves the contract doc states a fact that is true of the live SQL).
  - The documented "2 append-only triggers, none on blocks" fact matches live source: `grep -v '^--' supabase/migrations/033_economy_map_schema.sql | grep -c "CREATE TRIGGER"` equals 2, and `grep -n "TRIGGER.*ON economy_map.blocks" supabase/migrations/033_economy_map_schema.sql` returns nothing.
  - The documented publish-RPC service_role grant matches live source: `grep -n "GRANT EXECUTE ON FUNCTION economy_map.publish_block_version" supabase/migrations/039_publish_block_version_watermark_null_guard.sql` returns line 82.
  - The documented anon published-only RLS matches live source: `grep -n "USING (status = 'published')" supabase/migrations/033_economy_map_schema.sql` returns line 370.
  - No write occurred: `git status --porcelain supabase/ docker/web/site/app.js` is empty (no migration, no app.js change).
  </acceptance_criteria>

  <done>15-CONTRACT.md exists with all 7 sections, every fact line-cited to in-tree source; the live grep assertions for tier CHECK / trigger count / RPC grant / RLS still match the source files (proving the doc states true facts); no migration or app.js change in git status.</done>
</task>

<task type="auto">
  <name>Task 2: Write 15-RECONCILIATION.md — per-slug roster disposition + D-04 hub pin + D-03 collision-free reshuffle (ROST-01)</name>
  <files>.planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md</files>

  <read_first>
  - .planning/phases/15-inventory-roster-reconciliation/15-CONTEXT.md (§specifics — the reconciled roster table orders 0-8 is the target; D-01..D-04 are the locked decisions each disposition cites)
  - .planning/phases/15-inventory-roster-reconciliation/15-RESEARCH.md (§ROST-01 — the live seed facts + the doc-vs-context cross-check table + F-1/F-2/F-3 flags + the before/after sort_order collision-free proof; §D-04 — the pinned Option A recommendation + the rejected B/C and WHY)
  - supabase/migrations/033_economy_map_schema.sql (seed lines 402-415: regulation-legal seeded frame@7, negotiation-coordination NOT seeded, `ON CONFLICT (slug) DO NOTHING` line 415; tier CHECK line 68 that Option A relaxes)
  - .planning/docs/00-hub.md … 07-psychology-disposition.md (the YAML frontmatter — slug/tier/order/maturity — is the metadata source of truth; confirm the 8 frontmatters match the roster: hub has no tier/maturity; 01/02/03 carry `maturity: building`; negotiation=order 5, governance=order 6, psychology=order 7)
  - .planning/docs/EXECUTION_BRIEF.md (§0 read-before-write gate; §1 canonical roster; §5 open items)
  </read_first>

  <action>
  Write `.planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md` as the per-slug reconciliation plan that Phase 16+ executes (documented HERE, executed THERE). Every disposition MUST cite the D-NN decision it implements. Assemble from the sources in read_first — do not paste full file bodies.

  Required sections (use these exact headings):

  1. `## Reconciled roster — per-slug disposition` — a table with one row for ALL 9 block-level entries (hub + 8) with columns: order | slug | tier | maturity (loaded) | disposition | decision. The exact values (copy from CONTEXT §specifics):
     - 0 | `agent-economy` (hub) | hub (DB-served home per D-04) | — | first-publish (DB + marked.parse, RPC-gated) | D-04
     - 1 | `identity-trust` | substrate | `emerging` (was `building`) | first-publish | D-01
     - 2 | `memory-context` | substrate | `emerging` (was `building`) | first-publish | D-01
     - 3 | `payments-settlement` | substrate | `emerging` (was `building`) | first-publish | D-01
     - 4 | `autonomy-control` | behavior | `contested` | first-publish | —
     - 5 | `negotiation-coordination` | behavior | `nascent` | **new block** (graduated, was a section in Payments) | D-03
     - 6 | `governance-accountability` | behavior | `contested` | first-publish (sort_order 5→6) | D-03
     - 7 | `psychology-disposition` | behavior | `nascent` | first-publish (sort_order 6→7) | D-03
     - 8 | `regulation-legal` | frame | (unchanged) | **kept deferred** — body-less, sort_order 7→8 | D-02
     State the result line: 8 published bodies (hub + 7 doc blocks) + regulation-legal retained as a deferred frame card = 9 block-level entries, 3 tiers.

  2. `## Tier model decision (D-02)` — the tier model STAYS at 3 (`substrate`/`behavior`/`frame`); it does NOT collapse to the docs' 2. `regulation-legal` is kept as a deferred frame slot (unpublished/body-less, renders as a DEFERRED card), fed later by the EU AI Act tracker (a future milestone). Cite D-02. State the only permitted touch to its row is the structural sort_order 7→8 bump (per D-03) — no content/publish write.

  3. `## D-03 sort_order reshuffle — collision-free map` — the before/after table for all 8 block rows: identity-trust 1→1, memory-context 2→2, payments-settlement 3→3, autonomy-control 4→4, negotiation-coordination (absent)→5 INSERT, governance-accountability 5→6, psychology-disposition 6→7, regulation-legal 7→8. State the final ordered set = {1,2,3,4,5,6,7,8} (8 distinct, contiguous, no gaps/dups — collision-free), and that frame (regulation=8) sorts strictly after the highest behavior (psychology=7). Add the execution note (for Phase 16, NOT executed here): because sort_order is UNIQUE, the existing rows must move highest-first (regulation 7→8, then psychology 6→7, then governance 5→6) THEN insert negotiation at 5 — or run inside one txn with a deferrable unique constraint. State this is a Phase-16 write detail; Phase 15 only documents that it is collision-free and permitted (blocks has no append-only trigger).

  4. `## D-04 hub schema accommodation (pinned for Phase 16)` — state the problem: hub `agent-economy` has `type: hub`, no tier; `blocks.tier` is NOT NULL CHECK(substrate/behavior/frame) and `block_body_versions.block_slug` FKs `blocks(slug)`, so the hub body cannot live in block_body_versions without a blocks row, which needs a valid tier. Pin the chosen accommodation = **Option A: relax the tier CHECK to add a `'hub'` sentinel tier** (a clean, separately-reviewable new migration in Phase 16). State WHY Option A over B (nullable tier — rejected: drops NOT NULL for every block row, opens a silent tier-less-block surface; violates fail-loud) and over C (dedicated hub table + parallel RPC — rejected: net-new FK/RLS/RPC/trigger, violates D-04's "reuse publish_block_version unchanged"). State the consequences that make Option A safe: the hub becomes an ordinary blocks row (slug=agent-economy, tier=hub, sort_order=0, accent=gray); its body lives in block_body_versions under the existing FK; `publish_block_version` works UNCHANGED (slug-generic); `renderHub`'s three `tier === 'substrate'|'behavior'|'frame'` filters naturally EXCLUDE the hub row from all grids (no card duplication); render reuses `marked.parse` (app.js:586) with a graceful fallback to the existing HUB_STORYLINE when no published hub body exists. Note the existing XSS-via-markdown disposition (T-04-03-01, compensating control = operator publish gate) carries over unchanged because the hub publishes through the same gated RPC. Cite D-04. **State explicitly: this DDL is NOT applied in Phase 15 — it is the instruction for Phase 16.**

  5. `## Doc-vs-live divergence flags` — list the three flags from RESEARCH §ROST-01: F-1 (expected/benign — doc frontmatter `maturity: building` on slugs 1/2/3 vs loaded `emerging`; the Phase-16 loader does the substitution, NOT a doc edit); F-2 (ROADMAP SC#2 / Phase-17 wording lag — says pills show `building`; after D-01 the substrate trio renders `emerging`; Phase-17 verification text should read `emerging`); F-3 (no `08-regulation-legal.md` doc — intentional per D-02; the frame slot stays deferred). State each is a documentation-consistency note, not a code defect.

  Close with a one-line statement: this reconciliation is the operator-approval target — no `economy_map` write happens until it is approved (D-05); Phase 16 executes the load/reshuffle, Phase 16's hub-tier migration applies Option A.
  </action>

  <verify>
    <automated>test -f .planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md && grep -q "negotiation-coordination" .planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md && grep -q "regulation-legal" .planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md && grep -q "agent-economy" .planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md && echo RECONCILIATION_OK</automated>
  </verify>

  <acceptance_criteria>
  - File exists: `test -f .planning/phases/15-inventory-roster-reconciliation/15-RECONCILIATION.md`
  - All 9 roster slugs appear in the disposition: each of `agent-economy`, `identity-trust`, `memory-context`, `payments-settlement`, `autonomy-control`, `negotiation-coordination`, `governance-accountability`, `psychology-disposition`, `regulation-legal` matches `grep -q <slug> 15-RECONCILIATION.md`.
  - The 5 required section headings are present: `grep -q "per-slug disposition"`, `grep -q "Tier model decision"`, `grep -q "sort_order reshuffle"`, `grep -q "hub schema accommodation\|D-04"`, `grep -q "divergence flags\|F-1\|F-2\|F-3"` (run each on the file).
  - Every locked decision is cited: `grep -q "D-01" && grep -q "D-02" && grep -q "D-03" && grep -q "D-04"` on 15-RECONCILIATION.md.
  - The collision-free final set is asserted: the doc contains the contiguous set `grep -Eq "1.?2.?3.?4.?5.?6.?7.?8" 15-RECONCILIATION.md` AND names the regulation 7→8, psychology 6→7, governance 5→6 moves (`grep -q "7→8\|7 → 8" && grep -q "6→7\|6 → 7" && grep -q "5→6\|5 → 6"`).
  - Option A is pinned and B/C rejected: `grep -q "Option A" && grep -iq "relax" && grep -q "hub" ` (sentinel tier) AND `grep -q "Option B\|Option C"` (rejection noted).
  - The documented seed facts match live source (proves the disposition is grounded in reality): `grep -n "regulation-legal" supabase/migrations/033_economy_map_schema.sql` shows it seeded at sort_order 7; `grep -c "negotiation-coordination" supabase/migrations/033_economy_map_schema.sql` equals 0 (NOT seeded); `grep -n "ON CONFLICT (slug) DO NOTHING" supabase/migrations/033_economy_map_schema.sql` returns line 415.
  - Frontmatter source-of-truth holds: `grep -A8 '^---' .planning/docs/05-negotiation-coordination.md | grep -q "order: 5"` and the substrate docs carry building: `grep -q "maturity: building" .planning/docs/01-identity-trust.md`.
  - No write occurred: `git status --porcelain supabase/ docker/web/site/app.js` is empty (no migration applied, no app.js edit).
  </acceptance_criteria>

  <done>15-RECONCILIATION.md exists with all 5 sections, a 9-slug disposition table, the collision-free {1..8} reshuffle map, the pinned Option-A hub accommodation (with B/C rejection rationale), and the F-1/F-2/F-3 flags — every disposition citing its D-NN decision; the live seed grep assertions still match the source; no migration or app.js change in git status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none net-new this phase) | Phase 15 is no-write/no-new-code documentation. No new trust boundary is crossed. The boundaries documented below are the EXISTING ones carried into Phase 16/18 — they are recorded here so the reconciliation preserves the existing controls. |
| anon browser → PostgREST (`economy_map`) | Existing: anon reads only `status='published'` bodies via RLS. Documented in 15-CONTRACT.md; unchanged. |
| service_role agent → `economy_map` tables | Existing: service_role bypasses RLS; immutability of content is bound by BEFORE UPDATE/DELETE triggers, not RLS. Documented; unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering / Elevation | Stored XSS via block/hub markdown body (`marked.parse`, app.js:586) | accept (existing control documented) | No net-new threat in Phase 15 (no body is loaded or published here). The existing disposition T-04-03-01 (compensating control = operator publish gate via `publish_block_version`) is documented in 15-CONTRACT.md/RECONCILIATION.md and carries over to the hub unchanged (D-04 routes the hub through the same gated RPC). |
| T-15-02 | Tampering | service_role mutating immutable content (`block_body_versions`/`timeline_entries`) | accept (existing control documented) | Documented in 15-CONTRACT.md: BEFORE UPDATE/DELETE triggers bind service_role (RLS cannot). Phase 15 writes nothing, so introduces no new mutation path. The D-03 reshuffle (Phase 16) touches only `blocks.sort_order` — a column with NO append-only trigger, by design — so it does not weaken the immutability control. |
| T-15-03 | Tampering | Hub schema accommodation choice (Option A vs B) — a tier-less/NULL block slipping past constraints | mitigate | The reconciliation doc PINS Option A (keep `tier NOT NULL`, enumerate an explicit `'hub'` value) over Option B (nullable tier). This is the documented decision that PREVENTS a silent tier-less-block surface (fail-loud preserved). Recorded in 15-RECONCILIATION.md §D-04; applied as a migration in Phase 16, not here. |
| T-15-04 | Elevation | search_path hijack of the SECURITY DEFINER publish RPC | accept (existing control documented) | Documented in 15-CONTRACT.md: `SET search_path = economy_map, public` is pinned on `publish_block_version` (039:37). Phase 15 does not modify the RPC; the hub reuses it unchanged (D-04). |

**Severity assessment:** No high-severity finding. Phase 15 is a no-write documentation phase — the realistic threat model is "no net-new threat introduced; the documented dispositions preserve the existing controls (RLS, append-only triggers, pinned search_path, operator publish gate), and the pinned Option-A hub accommodation actively preserves the fail-loud `tier NOT NULL` invariant for Phase 16." This is an explicit low-severity model, acceptable for a documentation/reconciliation phase. No package installs (no `Package Legitimacy Audit` applies — RESEARCH confirms none).
</threat_model>

<verification>
Run the live-source assertions from 15-VALIDATION.md — each proves the contract doc states facts that match the live SQL/app.js, and each is <1s deterministic:

- `grep -n "CHECK (tier IN ('substrate','behavior','frame'))" supabase/migrations/033_economy_map_schema.sql` → line 68 (INV-01 3-tier contract).
- `grep -v '^--' supabase/migrations/033_economy_map_schema.sql | grep -c "CREATE TRIGGER"` → 2; `grep -n "TRIGGER.*ON economy_map.blocks" supabase/migrations/033_economy_map_schema.sql` → empty (INV-01 triggers guard content tables only).
- `grep -n "GRANT EXECUTE ON FUNCTION economy_map.publish_block_version" supabase/migrations/039_publish_block_version_watermark_null_guard.sql` → line 82 (INV-01 service_role-only RPC).
- `grep -n "USING (status = 'published')" supabase/migrations/033_economy_map_schema.sql` → line 370 (INV-01 anon published-only).
- `grep -n "CREATE TYPE economy_map.maturity" supabase/migrations/033_economy_map_schema.sql` → line 46; the 5 members present, no `building` member (INV-02).
- `grep -n "regulation-legal\|negotiation-coordination\|ON CONFLICT (slug) DO NOTHING" supabase/migrations/033_economy_map_schema.sql` → reg seeded @7, neg absent, ON CONFLICT @415 (ROST-01).
- `git status --porcelain supabase/ docker/web/site/app.js` → empty (no write, no migration, no app.js change — the phase boundary held).
</verification>

<success_criteria>
- `15-CONTRACT.md` exists and documents the block data contract, append-only trigger behavior, atomic publish RPC, RLS read boundary, hub serve path, and the verified maturity enum — every fact line-cited to in-tree source (SC#1, SC#2 = INV-01, INV-02).
- `15-RECONCILIATION.md` exists with a per-slug disposition for all 9 roster entries, the collision-free {1..8} reshuffle map, the pinned Option-A hub accommodation, and the F-1/F-2/F-3 flags — each disposition citing its D-NN decision (SC#3 = ROST-01).
- All `<automated>` and `<acceptance_criteria>` grep assertions pass against the live source files (the documented facts are true of the live schema).
- `git status` shows NO migration applied, NO `app.js` edit, NO `economy_map` write — the phase boundary held (precondition for SC#4, satisfied in Plan 02).
</success_criteria>

<output>
Create `.planning/phases/15-inventory-roster-reconciliation/15-01-SUMMARY.md` when done.
</output>
