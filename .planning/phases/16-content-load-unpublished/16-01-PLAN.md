---
phase: 16-content-load-unpublished
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql
autonomous: false
requirements: [LOAD-01]
must_haves:
  truths:
    - "The economy_map.blocks tier CHECK admits the 'hub' sentinel tier"
    - "A hub blocks row (slug='agent-economy', tier='hub', sort_order=0) exists in the live DB"
    - "A negotiation-coordination blocks row (tier='behavior', sort_order=5) exists in the live DB"
    - "After 043, sort_order across all 8 blocks is contiguous {0..8} with no collision"
  artifacts:
    - path: "supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql"
      provides: "All blocks-row structure: tier-CHECK relax + hub/negotiation INSERT + collision-free sort_order reshuffle"
      contains: "blocks_tier_check"
      min_lines: 40
  key_links:
    - from: "supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql"
      to: "economy_map.blocks"
      via: "ALTER TABLE + INSERT + UPDATE (no append-only trigger on blocks)"
      pattern: "INSERT INTO economy_map\\.blocks"
---

<objective>
Write migration `043` — the single atomic schema artifact that owns ALL `blocks`-row structure for the content load: (a) relax the `blocks` tier CHECK to admit a `'hub'` sentinel tier, (b) INSERT the hub `blocks` row (`agent-economy`), (c) INSERT the `negotiation-coordination` `blocks` row, and (d) reshuffle `sort_order` highest-first so the `UNIQUE(sort_order)` constraint never transiently collides. Then have the ORCHESTRATOR apply it to the live DB via the Supabase MCP tool (NOT a worktree executor, NOT `supabase db push`) — because every `block_body_versions.block_slug` FK target the loader will write (hub + negotiation) must exist before the loader runs (Plan 03).

Purpose: The loader (Plan 02/03) inserts BODIES ONLY and creates zero `blocks` rows. This migration is the structural prerequisite — its hub + negotiation FK targets are what make the loader's draft inserts succeed. `blocks` has no append-only trigger, so these are permitted plain writes (D-02).
Output: `supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql`, applied live via MCP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-content-load-unpublished/16-CONTEXT.md
@.planning/phases/16-content-load-unpublished/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 043 (tier-CHECK relax + hub/negotiation INSERT + collision-free sort_order reshuffle)</name>
  <files>supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql</files>
  <read_first>
    - supabase/migrations/033_economy_map_schema.sql — the `blocks` table def (lines 65-78: tier CHECK at :68, accent CHECK at :71, sort_order UNIQUE at :72, live_tension NOT NULL at :73, maturity DEFAULT at :74) and the §13 seed (lines 402-414) showing the LIVE sort_order assignments: identity-trust=1, memory-context=2, payments-settlement=3, autonomy-control=4, governance-accountability=5, psychology-disposition=6, regulation-legal=7
    - supabase/migrations/027_x_content_candidates_narrative_type.sql — the exact DROP CONSTRAINT IF EXISTS / ADD CONSTRAINT CHECK-relax idiom to copy (lines 4-6)
    - supabase/migrations/041_block_body_versions_unique_open_draft.sql — the loud multi-line `--` header style (lines 1-18) and the own-approved-track convention
    - .planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md — confirms `blocks` has NO append-only trigger (sort_order is a permitted plain UPDATE); accent CHECK is `('teal','purple','coral','gray')`; live_tension placeholder is `'TBD — set via /map-tension'`
    - .planning/phases/16-content-load-unpublished/16-PATTERNS.md — the migration analog assignments and the (a)/(b)/(c)/(d) SQL excerpts
  </read_first>
  <action>
    Create migration `043` as ONE atomic transaction. Open with a loud multi-line `--` header (copy 041's style): state Phase 16, that it lands D-02/D-04 (hub-tier accommodation + negotiation block + collision-free reshuffle), the atomicity guarantee, and WHY the reshuffle runs highest-first.

    (a) **Tier-CHECK relax:** `ALTER TABLE economy_map.blocks DROP CONSTRAINT IF EXISTS blocks_tier_check;` then `ALTER TABLE economy_map.blocks ADD CONSTRAINT blocks_tier_check CHECK (tier IN ('substrate','behavior','frame','hub'));` (Postgres auto-named the original inline CHECK `blocks_tier_check`).

    (b) **Collision-free sort_order reshuffle — MUST run highest-first and BEFORE the INSERT,** because `sort_order` is UNIQUE: `UPDATE economy_map.blocks SET sort_order = 8 WHERE slug = 'regulation-legal';` (live 7→8), then `UPDATE economy_map.blocks SET sort_order = 7 WHERE slug = 'psychology-disposition';` (live 6→7), then `UPDATE economy_map.blocks SET sort_order = 6 WHERE slug = 'governance-accountability';` (live 5→6). This vacates slot 5 for negotiation.

    (c) **INSERT the two new blocks rows** with the explicit 8-column list `(slug, tier, title, subtitle, accent, sort_order, live_tension, maturity)`, `::economy_map.maturity` cast on the maturity literal, and `ON CONFLICT (slug) DO NOTHING` (so re-run never clobbers operator copy):
      - hub: `('agent-economy', 'hub', 'The Agent Economy', 'Capability is solved. Trust and coordination are not.', 'gray', 0, 'TBD — set via /map-tension', 'nascent'::economy_map.maturity)` — title/subtitle taken verbatim from `.planning/docs/00-hub.md` frontmatter; accent='gray' per D-04 reconciliation example; sort_order=0.
      - negotiation: `('negotiation-coordination', 'behavior', 'Negotiation & Coordination', 'The market between strangers that doesn''t exist yet.', 'coral', 5, 'TBD — set via /map-tension', 'nascent'::economy_map.maturity)` — title/subtitle verbatim from `.planning/docs/05-negotiation-coordination.md`; accent='coral' (behavior tier uses purple/coral; psychology already uses coral, but accent has no UNIQUE constraint so this is permitted — pick 'coral' to match the behavior palette); sort_order=5; note the doubled single-quote `''` to SQL-escape the apostrophe in "doesn't".

    Order the statements: header → (a) tier relax → (b) reshuffle (highest-first) → (c) INSERT. Use `ON CONFLICT (slug) DO NOTHING` and `IF EXISTS` / `IF NOT EXISTS` guards so the migration is idempotent on replay. Do NOT touch `block_body_versions` (bodies are the loader's job, D-01). Do NOT add any body content to this SQL.
  </action>
  <verify>
    <automated>grep -q "blocks_tier_check" supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql && grep -q "'hub'" supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql && grep -q "agent-economy" supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql && grep -q "negotiation-coordination" supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql` exists and contains an `ALTER TABLE economy_map.blocks ... ADD CONSTRAINT blocks_tier_check CHECK (tier IN ('substrate','behavior','frame','hub'))`.
    - The file inserts `slug='agent-economy'` with `tier='hub'` and `sort_order=0`, and `slug='negotiation-coordination'` with `tier='behavior'` and `sort_order=5`.
    - The three `UPDATE ... SET sort_order` statements (regulation-legal→8, psychology-disposition→7, governance-accountability→6) appear BEFORE the INSERT and run highest-target-first.
    - Source assertion: the file contains NO `INSERT INTO economy_map.block_body_versions` and NO `body_md` (bodies are the loader's job).
    - The INSERT uses `ON CONFLICT (slug) DO NOTHING` and the maturity literal is cast `::economy_map.maturity`.
  </acceptance_criteria>
  <done>Migration 043 SQL written, idempotent, owns only blocks-row structure (tier relax + 2 INSERTs + highest-first reshuffle), no body content.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] Orchestrator applies migration 043 to the live DB via Supabase MCP</name>
  <what-built>
    Migration `043` (Task 1) is written but NOT yet applied. This project applies `economy_map` migrations via the Supabase MCP tool, run by the ORCHESTRATOR — never from a worktree executor and never via a `supabase db push` shell command (prod-cutover discipline / "orchestrator owns the live migration apply via MCP"). This task is the live apply.
  </what-built>
  <how-to-verify>
    1. ORCHESTRATOR applies `supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql` to project ref `zxzaaqfowtqvmsbitqpu` via the Supabase MCP `apply_migration` tool.
    2. Confirm the hub + negotiation rows landed and sort_order is contiguous: run (via MCP or service-role PostgREST with `Accept-Profile: economy_map`) `SELECT slug, tier, sort_order FROM economy_map.blocks ORDER BY sort_order;` — expect exactly 9 rows with sort_order `0,1,2,3,4,5,6,7,8` and slug='agent-economy' at 0 (tier='hub'), slug='negotiation-coordination' at 5 (tier='behavior').
    3. Confirm the tier CHECK now admits 'hub' (the agent-economy INSERT would have failed otherwise — its presence is the proof).
  </how-to-verify>
  <read_first>
    - supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql — the SQL being applied
    - .planning/phases/16-content-load-unpublished/16-CONTEXT.md — D-03 (orchestrator applies via MCP, loader runs second) and the run-order lock
  </read_first>
  <acceptance_criteria>
    - Behavior: a service-role / MCP `SELECT slug, tier, sort_order FROM economy_map.blocks ORDER BY sort_order` returns 9 rows with sort_order 0..8 contiguous, agent-economy at 0 (tier='hub'), negotiation-coordination at 5.
    - The migration applied without error (the agent-economy row's existence proves the tier CHECK admits 'hub').
    - This task is operator/orchestrator-owned (autonomous: false) — NOT applied by a worktree executor, NOT via `supabase db push`.
  </acceptance_criteria>
  <resume-signal>Type "applied" once 043 is live and the 9-row contiguous sort_order SELECT is confirmed. This unblocks Plan 03 (the loader run, whose FK targets are these rows).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration author → live DB | Static SQL written by an executor, applied by the orchestrator via MCP. No runtime user input flows into this SQL. |
| service_role → economy_map.blocks | The MCP apply runs with elevated privilege; `blocks` has no append-only trigger, so these writes are structurally permitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | migration 043 INSERT/UPDATE values | mitigate | All values are STATIC literals authored in-repo (title/subtitle copied verbatim from the `.md` frontmatter, no runtime concatenation). The apostrophe in the negotiation subtitle is SQL-escaped as `''`. No operator/visitor input reaches this SQL. |
| T-16-02 | Elevation of Privilege | sort_order reshuffle on blocks | accept | `blocks` has NO append-only trigger (15-CONTRACT confirms) — these plain UPDATEs are permitted by design; the privileged write is structure-only and adds zero published rows. |
| T-16-03 | Denial of Service | UNIQUE(sort_order) transient collision | mitigate | Reshuffle runs highest-first (regulation→8, psych→7, gov→6) BEFORE inserting negotiation at the vacated 5 — the UNIQUE constraint never transiently collides; `ON CONFLICT (slug) DO NOTHING` makes replay safe. |
| T-16-04 | Tampering | accidental body write in the migration | mitigate | The migration touches ONLY `economy_map.blocks`; acceptance criteria assert NO `block_body_versions` / `body_md` appears in the SQL (bodies are the loader's narrow write surface, D-01). |
</threat_model>

<verification>
- `043` SQL exists, contains the tier-CHECK relax admitting 'hub', the hub + negotiation INSERTs, and the highest-first reshuffle.
- After the orchestrator MCP apply: `economy_map.blocks` has 9 rows, sort_order 0..8 contiguous, agent-economy (hub) at 0, negotiation-coordination (behavior) at 5.
- No `block_body_versions` / `body_md` content in the migration.
</verification>

<success_criteria>
- Migration 043 written as one atomic, idempotent transaction owning only blocks-row structure.
- Orchestrator has applied 043 live via MCP; the 9-row contiguous-sort_order SELECT confirms the hub + negotiation FK targets exist (unblocking the Plan 03 loader run).
- Partially satisfies ROADMAP SC#1 (in-scope metadata structure present) — the load itself is Plan 02/03.
</success_criteria>

<output>
Create `.planning/phases/16-content-load-unpublished/16-01-SUMMARY.md` when done. Record: the final sort_order map (0..8), the applied-via-MCP confirmation, and the verified 9-row SELECT result so Plan 03 can rely on the FK targets.
</output>
