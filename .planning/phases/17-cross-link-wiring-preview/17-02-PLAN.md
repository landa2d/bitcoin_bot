---
phase: 17-cross-link-wiring-preview
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - scripts/verify_economy_map_crosslinks.py
autonomous: false
requirements: [LINK-01, PREV-01, HUB-01]
must_haves:
  truths:
    - "A local agentpulse-web container runs on this branch with SUPABASE_ANON_KEY set to the service_role key, exposing the unpublished drafts to the preview only"
    - "The deployed app.js retains the anon-key placeholder — the service_role key never enters tracked files or the deployed image"
    - "A fail-loud harness extracts every #/map/<slug> href from the hub + 7 draft bodies and asserts each target slug exists in the live blocks roster; exits non-zero on any miss"
    - "The operator manually clicks through hub->each of the 7 cards and the in-body block->block cross-links on the local preview and confirms each resolves + the pills show emerging/contested/nascent"
    - "The public live #/map is confirmed unchanged for anon visitors throughout (drafts never visible without the service_role + flag)"
  artifacts:
    - path: "scripts/verify_economy_map_crosslinks.py"
      provides: "fail-loud #/map/<slug> href extraction + in-roster assertion over the hub + 7 draft bodies"
      contains: "Accept-Profile"
  key_links:
    - from: "scripts/verify_economy_map_crosslinks.py"
      to: "economy_map.block_body_versions (status=draft) + economy_map.blocks roster"
      via: "direct PostgREST + Accept-Profile (Python-service idiom), extract #/map/<slug>, assert in roster"
      pattern: "Accept-Profile"
    - from: "local agentpulse-web container"
      to: "economy_map drafts (service_role read)"
      via: "entrypoint.sh sed-substitution of SUPABASE_ANON_KEY to the service_role key"
      pattern: "SUPABASE_ANON_KEY"
---

<objective>
Stand up the LOCAL-ONLY elevated preview (D-01/D-02), prove the service_role key never ships (the mandatory diff guard), run the fail-loud cross-link verification harness (D-05), and gate on the operator's manual click-through of the full 22-instance cross-link set on the preview (PREV-01 / LINK-01 / HUB-01). This is the last gate before Phase 18 publishes.

Built on plan 17-01's app.js render path. Three pieces:
- D-01/D-02 — run a LOCAL agentpulse-web container on this branch with SUPABASE_ANON_KEY set to the service_role key (riding the EXISTING docker/web/entrypoint.sh:3-4 sed-substitution, NOT modified), so the local preview alone sees the status=draft bodies. The deployed container is NOT rebuilt and keeps the anon key.
- D-02 guard (mandatory, T-SVC-ROLE-LEAK mitigation) — diff-confirm the DEPLOYED app.js keeps the __SUPABASE_ANON_KEY__ placeholder and that the service_role key value appears in NO tracked file (only the local run env carries it).
- D-05 — a fail-loud harness that programmatically extracts every #/map/<slug> href from the hub + 7 draft bodies and asserts each target slug exists in the live blocks roster (non-zero exit on any miss — guards future content drift), PLUS the operator manual click-through.

Purpose: prove the loaded content is publish-ready end-to-end (LINK-01 resolves, PREV-01 renders on a non-published preview, HUB-01 single block list) with the live published site unchanged — satisfying all 4 Phase-17 success criteria before any publish.
Output: scripts/verify_economy_map_crosslinks.py (the fail-loud harness) + a recorded preview-verification result + the operator approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-cross-link-wiring-preview/17-CONTEXT.md
@.planning/phases/17-cross-link-wiring-preview/17-PATTERNS.md
@.planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md
@.planning/phases/16-content-load-unpublished/16-CONTEXT.md
@docker/web/entrypoint.sh
@docker/web/site/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the fail-loud cross-link verification harness (D-05) + run the deployed-app.js service_role guard (D-02)</name>
  <read_first>
    - .planning/phases/17-cross-link-wiring-preview/17-CONTEXT.md decisions D-05 (exhaustive + fail-loud) and specifics (the 22-instance inventory to 7 distinct in-roster target slugs; none to regulation-legal)
    - .planning/phases/17-cross-link-wiring-preview/17-PATTERNS.md sections "Fail-loud cross-link verification (D-05)" and "supabase-js schema isolation ... NOT the Python httpx path" (this HARNESS is a Python service so it uses direct PostgREST + Accept-Profile, the inverse of the frontend; never supabase-py .in_ array filter)
    - .planning/phases/15-inventory-roster-reconciliation/15-CONTRACT.md sections "Body storage" + "Block data contract" (block_body_versions: block_slug/body_md/status; blocks.slug is the roster) + "Read boundary (RLS)" (service_role bypasses RLS; anon sees only published)
    - scripts/load_economy_map_content.py (the Phase-16 loader — REUSE its direct-PostgREST + Accept-Profile request idiom and its config/.env key loading; the harness reads bodies the same way, with the service_role key)
    - docker/web/site/app.js lines 4-5 (the __SUPABASE_URL__ / __SUPABASE_ANON_KEY__ placeholders the D-02 guard asserts are unchanged)
  </read_first>
  <action>
    Create scripts/verify_economy_map_crosslinks.py — a standalone fail-loud harness. Mirror the Phase-16 loader's direct-PostgREST + Accept-Profile: economy_map request idiom from scripts/load_economy_map_content.py (per CLAUDE.md never use the supabase-py .in_ array filter; this is the Python-service path, NOT supabase-js). The script: (1) reads the live economy_map.blocks roster via PostgREST — the set of slug values that exist (the in-roster target set); (2) reads every status=draft block_body_versions row for the hub agent-economy + the 7 block slugs (identity-trust, memory-context, payments-settlement, autonomy-control, negotiation-coordination, governance-accountability, psychology-disposition) using the service_role key from config/.env (drafts are RLS-invisible to anon, so the harness MUST use the service-role key — the same key the loader used; do NOT hardcode any key); (3) extracts every #/map/<slug> href from each body's markdown via regex over the raw markdown (this matches what marked.parse emits — a [text](#/map/<slug>) link becomes an a-href to #/map/<slug>); (4) FAIL-LOUD: for every extracted target slug, assert it is in the live blocks roster — collect ALL misses, print each source_slug to missing_target, and sys.exit nonzero if ANY miss (guards future content drift; per MEMORY fail_loud_governance — halt loudly, never silently pass); (5) on full success print the count summary (expect 22 instances to 7 distinct in-roster targets, none to regulation-legal) and exit zero. ALSO run the mandatory D-02 deployed-app.js guard (the T-SVC-ROLE-LEAK mitigation): confirm via git grep / reading the tracked docker/web/site/app.js that lines 4-5 STILL hold the __SUPABASE_URL__ / __SUPABASE_ANON_KEY__ placeholders (NOT a substituted key), and that the service_role key VALUE appears in NO tracked file (git grep for the key's distinguishing prefix returns nothing) — record the guard result in the SUMMARY.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('scripts/verify_economy_map_crosslinks.py').read())" && echo PARSE-PASS</automated>
    <automated>cd /root/bitcoin_bot && grep -q "Accept-Profile" scripts/verify_economy_map_crosslinks.py && grep -q "sys.exit" scripts/verify_economy_map_crosslinks.py && echo IDIOM-PASS</automated>
    <automated>cd /root/bitcoin_bot && grep -c "in_" scripts/verify_economy_map_crosslinks.py | grep -qx 0 && echo NO-IN-FILTER-PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: scripts/verify_economy_map_crosslinks.py reads economy_map via direct PostgREST with an Accept-Profile: economy_map header (the Python-service idiom), NOT supabase-js and NOT the supabase-py .in_ array filter.
    - Source: the harness reads draft bodies for the hub agent-economy + all 7 block slugs using the service_role key loaded from config/.env (no hardcoded key).
    - Behavior: running the harness against the live DB extracts the #/map/<slug> targets and asserts each is in the live blocks roster; it exits 0 today (all 22 instances resolve to the 7 in-roster targets) and exits non-zero if any target slug is missing from the roster (deliberately injecting an off-roster target makes it exit non-zero).
    - Guard (D-02 / T-SVC-ROLE-LEAK): git-tracked docker/web/site/app.js lines 4-5 still hold the __SUPABASE_URL__ / __SUPABASE_ANON_KEY__ placeholders; git grep for the service_role key prefix returns nothing in tracked files. Recorded in the SUMMARY.
  </acceptance_criteria>
  <done>scripts/verify_economy_map_crosslinks.py exists, uses direct PostgREST + Accept-Profile + service_role read, extracts every #/map/<slug> target and fail-louds (non-zero exit) on any off-roster target; the deployed-app.js placeholder guard is run and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Build + run the local elevated preview container (D-01/D-02) and run the fail-loud harness against it</name>
  <read_first>
    - docker/web/entrypoint.sh (the :3-4 sed-substitution mechanism — used UNCHANGED; the local container sets SUPABASE_ANON_KEY to the service_role key for that container only)
    - docker/web/Dockerfile + docker/web/Caddyfile + docker/docker-compose.yml (the web service build/serve context; the local container is built+run on this branch)
    - .planning/phases/17-cross-link-wiring-preview/17-CONTEXT.md decisions D-01 (local-private elevated preview), D-02 (service_role substituted into the LOCAL container only; mandatory diff guard), D-04 (PREVIEW_ENABLED flag set only on the local container)
    - .planning/phases/17-cross-link-wiring-preview/17-PATTERNS.md "D-02 substitution rides the EXISTING mechanism" — including the MEMORY gotcha reference_web_static_preview_substitution (serve the SUBSTITUTED container, NEVER the raw docker/web/site dir, or app.js crashes at module-load on the unsubstituted createClient placeholder)
    - .planning/phases/17-cross-link-wiring-preview/17-01-SUMMARY.md (what the app.js render path + the PREVIEW_ENABLED param look like, from plan 17-01)
    - MEMORY reference_scoped_rebuild_worktree_unsafe (a docker compose --build task cds to the absolute main-tree path; run no-worktree/sequential)
  </read_first>
  <action>
    Build and run the LOCAL agentpulse-web container on this branch with the app.js from plan 17-01, configured for the elevated preview (D-01/D-02): set the container's SUPABASE_ANON_KEY env to the service_role key (read from config/.env) so the existing docker/web/entrypoint.sh:3-4 sed substitutes it into app.js at container start — DO NOT edit entrypoint.sh. Per the MEMORY gotcha reference_web_static_preview_substitution, the preview MUST be the substituted CONTAINER (or a sed'd temp copy), NEVER the raw docker/web/site dir (the unsubstituted __SUPABASE_URL__ crashes app.js at module-load). Per reference_scoped_rebuild_worktree_unsafe, run this build from the absolute main-tree path (no worktree). The deployed/production container is NOT rebuilt and NOT touched — only a throwaway LOCAL container. Open the preview with the D-04 flag active (the ?preview=1 param) so the draft-fetch path turns on. Then RUN the Task-1 harness (scripts/verify_economy_map_crosslinks.py) against the live DB and confirm exit 0. Re-confirm the D-02 guard at this point: the deployed app.js is unchanged (the service_role key lives only in the local container's run env, never in a tracked file or the deployed image). Capture: the local preview URL/port, the harness exit code + summary, and the guard result, into the SUMMARY.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && docker ps --format "{{.Names}} {{.Status}}" | grep -i web && echo CONTAINER-UP</automated>
    <automated>cd /root/bitcoin_bot && python3 scripts/verify_economy_map_crosslinks.py && echo HARNESS-GREEN</automated>
    <automated>cd /root/bitcoin_bot && git grep -nE "__SUPABASE_ANON_KEY__" -- docker/web/site/app.js && echo PLACEHOLDER-INTACT</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: a LOCAL agentpulse-web container is running (docker ps shows it) built from this branch's app.js; the deployed/prod container is not rebuilt.
    - Behavior: the container's SUPABASE_ANON_KEY env is the service_role key, substituted into app.js by the UNCHANGED entrypoint.sh:3-4 sed; docker/web/entrypoint.sh has zero diff.
    - Behavior: the preview is reached via the substituted container (not the raw site dir) with ?preview=1; the fail-loud harness run against the live DB exits 0 (HARNESS-GREEN).
    - Guard (D-02): git-tracked docker/web/site/app.js lines 4-5 still hold __SUPABASE_URL__ / __SUPABASE_ANON_KEY__ (PLACEHOLDER-INTACT); the service_role key value is in no tracked file and not in the deployed image.
  </acceptance_criteria>
  <done>A local elevated preview container runs from this branch's app.js with the service_role key substituted via the unchanged entrypoint.sh; the harness exits 0 against it; the deployed app.js retains the anon-key placeholder and the service_role key ships nowhere.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Operator manual click-through of the full cross-link set on the local preview (PREV-01, LINK-01, HUB-01)</name>
  <what-built>
    The LOCAL elevated agentpulse-web preview container (Task 2) renders the Phase-16 loaded-but-unpublished hub + 7 block bodies via the plan-17-01 app.js render path (PREVIEW_ENABLED + the D-03 draft-fetch + the D-06 hub intro/trim). The automated fail-loud cross-link harness (Tasks 1-2) has already asserted every #/map/<slug> target is in-roster. This checkpoint is the operator's eyeball pass on the rendered pages — the human half of D-05, the last gate before Phase 18 publishes. The 22-instance cross-link inventory (7 hub->block + 15 block->block) is in 17-CONTEXT.md specifics.
  </what-built>
  <how-to-verify>
    1. Open the LOCAL preview at the #/map landing with the preview flag (e.g. http://localhost:PORT/#/map?preview=1 — use the exact URL/port recorded in the Task-2 SUMMARY).
    2. HUB-01: confirm the hub renders the thesis + the "How to read this map" two-tier framing as the intro ABOVE the card grid, and that the Tier-1/Tier-2 prose block-list is NOT present (the block list appears ONCE, as the cards).
    3. LINK-01 (hub->block): click each of the 7 cards (identity-trust, memory-context, payments-settlement, autonomy-control, negotiation-coordination, governance-accountability, psychology-disposition). Each must land on the right block page with back arrow + title + subtitle + maturity pill + a non-empty body.
    4. PREV-01 (pills): confirm the pills render the three distinct stages — emerging (the substrate trio: identity-trust / memory-context / payments-settlement), contested (autonomy-control, governance-accountability), nascent (negotiation-coordination, psychology-disposition). NOTE the Flag F-2 trap: the substrate trio renders emerging, NOT the literal doc word "building" — do not flag emerging as wrong.
    5. LINK-01 (block->block): on each block page, click the in-body cross-links (the 15 block->block instances) and confirm each resolves to the correct target block page.
    6. Spine check: in a SEPARATE normal browser tab, open the PUBLIC live aiagentspulse.com/#/map (anon, no preview flag) and confirm it is unchanged — none of the drafts are visible there.
  </how-to-verify>
  <resume-signal>Type "approved" if every link resolves, the pills show emerging/contested/nascent, the hub shows the intro + single card list (no duplicated prose list), and the public live #/map is unchanged — or describe any dead/mis-routed link, wrong pill, or duplicated block list.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local preview container (service_role) -> economy_map | the LOCAL-ONLY container substitutes the service_role key (full RLS bypass) to read drafts; the deployed container is untouched and keeps the anon key |
| tracked repo / deployed image -> the service_role key value | the key must NEVER enter a tracked file or the deployed image — only the local run env |
| draft markdown -> marked.parse -> DOM (preview only) | unsanitized markdown render on the local preview only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SVC-ROLE-LEAK | Elevation of Privilege / Information Disclosure | the service_role key (full RLS bypass) substituted into the LOCAL preview container | mitigate | (1) The key is set ONLY in the local container's run env and substituted at container start by the UNCHANGED entrypoint.sh:3-4 — never written to a tracked file. (2) The mandatory D-02 guard (Task 1 + re-confirmed Task 2): git-tracked docker/web/site/app.js lines 4-5 keep the __SUPABASE_URL__ / __SUPABASE_ANON_KEY__ placeholders, and git grep for the key prefix returns nothing in tracked files (PLACEHOLDER-INTACT gate). (3) The deployed container is NOT rebuilt this phase — the service_role key never reaches the deployed image. Blast radius contained to a throwaway local container in the operator's own browser. The HIGH threat is mitigated in actual acceptance_criteria (Task 1 guard criterion + Task 2 PLACEHOLDER-INTACT gate). |
| T-17-XSS-MD | Tampering (XSS via markdown) | marked.parse(bodyMd) on draft bodies + hub intro on the LOCAL preview | accept | Carried disposition T-04-03-01: same marked.parse path as the live renderer; compensating control = the operator publish gate (Phase 18 — only operator-approved content publishes). The preview is local-only (D-01), so a malicious draft's blast radius is the operator's own browser. No sanitizer added (matches existing precedent). |
| T-17-DRAFT-EXPOSE | Information Disclosure (unpublished drafts leaking to public anon visitors) | the draft content on the public live site | mitigate | Double-safe (D-04): the deployed app.js ships with PREVIEW_ENABLED unset AND published-only RLS (033:367-370) independently returns no draft to the anon key. The Task-3 spine check explicitly confirms the public live #/map is unchanged. Phase 17 publishes nothing and writes nothing to economy_map. |
| T-17-DRIFT | Tampering (silent cross-link rot / future content drift) | the 22 #/map/<slug> cross-links | mitigate | The D-05 fail-loud harness (Task 1) asserts every extracted target slug is in the live blocks roster and exits non-zero on any miss — it is trivially green today but guards against future drift; the operator manual click-through (Task 3) is the human confirmation. |
</threat_model>

<verification>
- scripts/verify_economy_map_crosslinks.py parses, uses direct PostgREST + Accept-Profile, uses sys.exit for fail-loud, and contains no supabase-py .in_ array filter (the three Task-1 gates).
- The harness run against the live DB exits 0 (all 22 instances resolve to the 7 in-roster targets; none to regulation-legal).
- A local elevated preview container is up (docker ps); the deployed/prod container and docker/web/entrypoint.sh have zero diff.
- D-02 guard: git-tracked docker/web/site/app.js lines 4-5 still hold the __SUPABASE_URL__ / __SUPABASE_ANON_KEY__ placeholders; git grep for the service_role key prefix returns nothing in tracked files.
- Operator approval recorded (Task 3): every cross-link resolves, pills show emerging/contested/nascent, the hub shows intro + single card list, and the public live #/map is unchanged.
- No migration / schema / RLS / RPC / pipeline / proxy / agent-service file modified; the only new file is scripts/verify_economy_map_crosslinks.py; no economy_map write performed.
</verification>

<success_criteria>
- The fail-loud harness exists, uses the Python-service PostgREST + Accept-Profile idiom (never .in_), and exits non-zero on any off-roster cross-link target (D-05 automated half).
- A LOCAL-ONLY elevated agentpulse-web preview container renders the loaded drafts (D-01/D-02), built on plan-17-01's app.js, via the unchanged entrypoint.sh substitution.
- The mandatory T-SVC-ROLE-LEAK guard holds: the deployed app.js keeps the anon-key placeholder and the service_role key value is in no tracked file or deployed image.
- The operator's manual click-through confirms LINK-01 (all 22 cross-links resolve), PREV-01 (pills emerging/contested/nascent on a non-published preview), and HUB-01 (hub intro + single card block list), with the public live #/map confirmed unchanged.
</success_criteria>

<output>
Create `.planning/phases/17-cross-link-wiring-preview/17-02-SUMMARY.md` when done.
</output>
