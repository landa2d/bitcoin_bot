---
phase: 18-gated-batch-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/web/site/app.js
autonomous: true
requirements: [PUB-01]
must_haves:
  truths:
    - "After the hub agent-economy body is published, the prod #/map landing renders the trimmed published hub article (not the HUB_STORYLINE constant), independent of PREVIEW_ENABLED."
    - "Before the hub body is published (current_body_version_id NULL on agent-economy), the deployed #/map renders the HUB_STORYLINE constant exactly as today — the change is a visual no-op pre-publish (D-04)."
    - "The Phase-17 PREVIEW_ENABLED draft-fetch path in loadHub is unchanged and still takes precedence in preview mode."
  artifacts:
    - path: "docker/web/site/app.js"
      provides: "loadHub prod published-hub-body fetch (flag-independent) mirroring loadBlock :674-677, feeding the existing trimHubBody/HUB_STORYLINE render fork in renderHub"
      contains: "current_body_version_id"
  key_links:
    - from: "loadHub (app.js)"
      to: "economy_map.block_body_versions.body_md"
      via: "sb.schema('economy_map').from('block_body_versions').select('body_md').eq('id', hubRow.current_body_version_id).single()"
      pattern: "current_body_version_id"
    - from: "loadHub hubBodyMd"
      to: "renderHub trimHubBody fork (:594-597)"
      via: "hubBodyMd argument passed to renderHub, trimmed then marked.parse'd"
      pattern: "trimHubBody"
---

<objective>
Author the single prod `app.js` change that renders the PUBLISHED hub `agent-economy` body as a trimmed framing article at `#/map`, fulfilling D-01/D-02/D-03. Today `loadHub` only fetches the hub body behind `PREVIEW_ENABLED`, so prod shows the hardcoded `HUB_STORYLINE` constant even after the hub body is published. This plan adds a NEW flag-INDEPENDENT prod path that fetches the hub row's `current_body_version_id` body (mirroring the `loadBlock` published-body path), feeding it into the EXISTING `trimHubBody()` + `HUB_STORYLINE`-fallback render fork.

Purpose: This is the one renderer change the phase needs. Deploy-first (D-04) requires it to be a provable visual no-op pre-publish — the `agent-economy.current_body_version_id` is NULL until the Phase-18 publish batch runs, so `hubBodyMd` stays null and `renderHub` falls back to `HUB_STORYLINE`. The plan does NOT deploy or run anything live; it only authors the code so the Wave-2 go-live plan can ship it.

Output: A modified `docker/web/site/app.js` whose `loadHub` carries the new prod published-hub fetch, keeping the Phase-17 preview path and `HUB_STORYLINE` fallback intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-gated-batch-publish/18-CONTEXT.md
@.planning/phases/18-gated-batch-publish/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the flag-independent prod published-hub-body fetch to loadHub</name>
  <files>docker/web/site/app.js</files>
  <read_first>
    - docker/web/site/app.js :449-518 (loadHub — the function being modified; the existing `blocks` select at :458-462 already returns `current_body_version_id`, and the Phase-17 PREVIEW_ENABLED draft fetch is at :478-488)
    - docker/web/site/app.js :671-677 (loadBlock published-body path — the EXACT analog to mirror: `sb.schema('economy_map').from('block_body_versions').select('body_md').eq('id', current_body_version_id).single()`, NO defensive `.eq('status','published')`)
    - docker/web/site/app.js :28-85 (HUB_STORYLINE constant :32, PREVIEW_ENABLED :57-60, trimHubBody :74-85 — all reused unchanged)
    - docker/web/site/app.js :520-609 (renderHub — the existing trimHubBody/HUB_STORYLINE render fork at :594-597 that consumes hubBodyMd; DO NOT change this fork)
    - .planning/phases/18-gated-batch-publish/18-PATTERNS.md (the `app.js` section — the exact pattern-to-add and the precedence rule)
  </read_first>
  <action>
    In `loadHub` (app.js :449-518), add a NEW published-hub-body fetch that mirrors the `loadBlock` published-body path at :674-677 and is INDEPENDENT of `PREVIEW_ENABLED` (D-01). Place it AFTER the existing Phase-17 PREVIEW_ENABLED draft fetch (:478-488) so the draft fetch keeps precedence in preview mode (same precedence `loadBlock` uses: draft first, published fallback). Resolve the hub row from the EXISTING `loadHub` `blocks` select result with `data.find(b => b.slug === 'agent-economy')` (the select at :458-462 reads all rows ordered by `sort_order` with NO tier filter, so the `agent-economy` row — tier `hub` per migration 043 — is returned; do NOT add a separate hub-row read). Gate the fetch on `!hubBodyMd && hubRow && hubRow.current_body_version_id` so: (a) it never runs when the preview draft already populated `hubBodyMd`, and (b) it stays a no-op pre-publish when `current_body_version_id` is NULL → `hubBodyMd` stays null → renderHub falls back to `HUB_STORYLINE`. Use `sb.schema('economy_map').from('block_body_versions').select('body_md').eq('id', hubRow.current_body_version_id).single()` — `sb.schema('economy_map')` sets `Accept-Profile` automatically (D-16); NO defensive `.eq('status','published')` filter (D-17 — anon RLS exposes only published versions). Graceful-degrade: on any error/empty leave `hubBodyMd` null, never throw (mirror :676). Do NOT change the existing `renderHub` `trimHubBody`/`HUB_STORYLINE` fork at :594-597 — once `hubBodyMd` is populated it renders the trimmed published article (D-02), and when null it renders the `HUB_STORYLINE` constant (the pre-publish fallback — NOT deleted, D-02). Leave `PREVIEW_ENABLED` (:57-60), the preview draft fetch (:478-488), and the preview proposed_maturity fetch (:498-515) UNTOUCHED.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node --check docker/web/site/app.js && echo "JS-SYNTAX-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check docker/web/site/app.js` exits 0 (prints `JS-SYNTAX-OK`).
    - `loadHub` contains a new fetch keyed off `hubRow.current_body_version_id` (resolved via `data.find(b => b.slug === 'agent-economy')`) that runs WITHOUT a `PREVIEW_ENABLED` guard — confirm by reading the function: the new fetch block is NOT inside an `if (PREVIEW_ENABLED)` block.
    - The new fetch uses `sb.schema('economy_map')...select('body_md').eq('id', hubRow.current_body_version_id).single()` and carries NO `.eq('status', 'published')` filter.
    - The existing Phase-17 preview draft fetch at the original :478-488 (the `if (PREVIEW_ENABLED)` block fetching `block_slug='agent-economy'`, `status='draft'`) is unchanged and still present.
    - `HUB_STORYLINE` (the :32 constant) is still present and still referenced by the `renderHub` fork (`trimmedHubBody ? ... : ... escapeHtml(HUB_STORYLINE)`).
    - The new fetch is ordered so the preview draft (when set) takes precedence: the published fetch is gated on `!hubBodyMd`.
  </acceptance_criteria>
  <done>
    `loadHub` populates `hubBodyMd` from the hub row's published `current_body_version_id` body via a flag-independent fetch mirroring `loadBlock` :674-677; the preview draft fetch and `HUB_STORYLINE` fallback are intact; `node --check` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the change is a deploy-first visual no-op pre-publish (D-04 reasoning + placeholder integrity)</name>
  <files>docker/web/site/app.js</files>
  <read_first>
    - docker/web/site/app.js :48-60 (the DOUBLE-SAFE no-op reasoning the Phase-17 preview comment documents — reuse the same reasoning for the prod hub path)
    - docker/web/site/app.js :4-5 (the `__SUPABASE_URL__` / `__SUPABASE_ANON_KEY__` placeholders that entrypoint.sh sed-substitutes at container start — MUST stay placeholders in the tracked file)
    - scripts/verify_economy_map_crosslinks.py :367-474 (run_svc_role_guard — the placeholder + service_role-leak guard this change must still pass) and :345-351 (WEB_DEPLOY_PATH_FILES — app.js is in this set)
  </read_first>
  <action>
    Add a short code comment in `loadHub` next to the new fetch (mirroring the Phase-17 DOUBLE-SAFE comment at :48-60) recording WHY this change is a deploy-first visual no-op pre-publish (D-04): the `agent-economy.current_body_version_id` is NULL until the Phase-18 publish batch runs → `hubBodyMd` stays null → `renderHub` renders `HUB_STORYLINE`; AND anon RLS exposes only published versions, so even a non-null pointer resolves only to published content. Confirm the tracked `app.js` still holds the `__SUPABASE_URL__` and `__SUPABASE_ANON_KEY__` placeholders at lines 4-5 (entrypoint.sh substitutes at container start — never commit a substituted key; T-18-LEAK). Do NOT introduce any new view, route, component, CSS class, or `blocks`-select column — the only behavioral change is populating `hubBodyMd` from the published path. Verify the existing service_role-leak guard still passes against the modified file.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -c '__SUPABASE_URL__\|__SUPABASE_ANON_KEY__' docker/web/site/app.js && python3 scripts/verify_economy_map_crosslinks.py --guard-only</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '__SUPABASE_URL__\|__SUPABASE_ANON_KEY__' docker/web/site/app.js` returns >= 2 (both placeholders intact in the tracked file).
    - `python3 scripts/verify_economy_map_crosslinks.py --guard-only` exits 0 and prints `PLACEHOLDER-INTACT` and `SVC-ROLE-NOT-IN-WEB-DEPLOY-PATH` (no service_role key in the web-deploy-path files including the modified app.js).
    - A `git diff docker/web/site/app.js` shows additions ONLY inside `loadHub` (the new fetch + comment); no change to `renderHub`'s `:594-597` fork, no new CSS class, no new `blocks`-select column, no new route/view/component.
    - The new fetch comment names the NULL `current_body_version_id` → `HUB_STORYLINE` fallback as the pre-publish no-op proof.
  </acceptance_criteria>
  <done>
    The diff is confined to `loadHub`; the deploy-first no-op reasoning is documented in-code; both URL/key placeholders are intact; `--guard-only` passes (no new service_role leak).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon browser → economy_map (PostgREST/supabase-js) | The deployed app.js runs in the visitor's browser with the ANON key only; reads cross into the DB where published-only RLS is the boundary. |
| tracked source → deployed image | app.js ships verbatim in the agentpulse-web image (entrypoint.sh sed-substitutes the anon-key placeholder at container start). A committed real key would leak into the image. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | new loadHub published-body fetch | mitigate | Fetch uses the anon-keyed `sb.schema('economy_map')` client; anon RLS (033:367-370) returns ONLY `status='published'` bodies — a draft/superseded hub body is never reachable from prod. NO service_role in app.js. |
| T-18-02 | Elevation of Privilege | service_role key in app.js | mitigate | Task 2 asserts `__SUPABASE_URL__`/`__SUPABASE_ANON_KEY__` placeholders intact + `--guard-only` confirms the service_role key is in NONE of the web-deploy-path files (T-18-LEAK). |
| T-18-03 | Tampering | premature live content via the new path | accept | The change is a documented no-op until publish (NULL `current_body_version_id` → `HUB_STORYLINE`); no live content flips in this plan — publish is the Wave-2 gated batch. Low risk: no write surface here. |
</threat_model>

<verification>
- `node --check docker/web/site/app.js` passes (JS syntax).
- `python3 scripts/verify_economy_map_crosslinks.py --guard-only` exits 0 (placeholders intact, no service_role leak in the web-deploy path).
- `git diff` confined to `loadHub`; `renderHub` :594-597 fork and `HUB_STORYLINE` constant unchanged.
- The new fetch is flag-INDEPENDENT (not inside `if (PREVIEW_ENABLED)`) and carries no `.eq('status','published')` filter.
</verification>

<success_criteria>
- `loadHub` renders the trimmed PUBLISHED hub article when the hub body is published (D-01/D-02), and falls back to `HUB_STORYLINE` pre-publish (D-04 no-op).
- The Phase-17 preview path and `PREVIEW_ENABLED` flag are untouched.
- No new UI feature, route, or service_role exposure; web-deploy-path leak guard green.
</success_criteria>

<output>
Create `.planning/phases/18-gated-batch-publish/18-01-SUMMARY.md` when done.
</output>
