---
phase: 21-single-scroll-landing-scroll-spy-nav
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - docker/web/site/app.js
  - docker/web/site/style-base.css
autonomous: false
requirements: [SCROLL-02]
must_haves:
  truths:
    - "The persistent nav is a scroll-spy: as the user scrolls the landing, the active section's nav tab highlights (IntersectionObserver, rootMargin '-50% 0px -50% 0px'); clicking a nav tab smooth-scrolls to that section (CSS scroll-behavior, reduced-motion-gated)."
    - "Opening a detail route (edition/block) leaves the landing; returning (the in-page '<- Back' / nav to #/) restores the landing with scroll position preserved via the landingScrollY module var."
    - "The sticky body > header does not overlap a section heading on an anchor jump — landing sections carry scroll-margin-top accounting for the ~60px header."
    - "On the assembled landing, WIDTH-01 holds (no dead left gutter, ~60-70 char reading copy via --measure, tiled sections on --wide, nav edges == content edges) and RHYTHM-01 holds (token-only color, major/within section-rhythm hierarchy) — re-verified holistically on the LIVE render."
  artifacts:
    - path: "docker/web/site/app.js"
      provides: "initScrollSpy() (IntersectionObserver over the 4 static-literal section ids, toggling .active + aria-current on the .tab anchors); landing scroll-to-section + initial-active sync in showLanding(); detail->back landingScrollY restore wired through route()."
      contains: "IntersectionObserver"
    - path: "docker/web/site/style-base.css"
      provides: "html{scroll-behavior:smooth} + @media (prefers-reduced-motion:reduce){html{scroll-behavior:auto}}; #landing>section{scroll-margin-top:calc(60px + var(--space-md))}; #landing>section + section major-boundary rule (1px var(--line-strong))."
      contains: "scroll-behavior"
  key_links:
    - from: "scroll position on the landing"
      to: "the active nav .tab"
      via: "IntersectionObserver -> classList.toggle('active') + aria-current"
      pattern: "rootMargin"
    - from: "detail -> back navigation"
      to: "restored landing scroll position"
      via: "window.scrollTo(0, landingScrollY)"
      pattern: "landingScrollY"
    - from: "nav anchor click"
      to: "smooth scroll to the section"
      via: "CSS scroll-behavior + scroll-margin-top"
      pattern: "scroll-behavior:\\s*smooth"
---

<objective>
Layer the SCROLL-02 behaviors onto the Plan 01 two-mode landing: the IntersectionObserver scroll-spy (active-nav highlight), smooth-scroll-on-click + sticky-header offset (CSS), the detail->back landing scroll restore (consuming `landingScrollY`), and a `prefers-reduced-motion` gate on the net-new motion — then deploy (scoped `web` rebuild, orchestrator-owned) and holistically re-verify WIDTH-01 + RHYTHM-01 on the assembled live render.

Purpose: Plan 01 made the landing/detail structure; SCROLL-02 makes it FEEL like a scroll-spy single-page app and proves it on the live site. Per 21-RESEARCH.md the scroll-spy is the operator-approved mockup pattern verbatim-adaptable (`rootMargin:'-50% 0px -50% 0px'`), the smooth-scroll + sticky offset are pure-CSS (reduced-motion-aware for free via the media query), and the scroll-restore is the house module-var idiom. This plan also absorbs Phase 20's folded-in WIDTH-01/RHYTHM-01 holistic visual sign-off — the foundation is layout-agnostic and re-verified on the merged page. The deploy is orchestrator-owned + worktree-unsafe (the scoped rebuild cds to the main tree; a worktree executor would build stale code — MEMORY scoped-rebuild-worktree-unsafe).

Output: `initScrollSpy()` + landing scroll/active-sync + scroll-restore in `app.js`; the four net-new CSS rules (smooth-scroll + reduced-motion gate + scroll-margin-top + `#landing>section + section` rhythm) in `style-base.css`; and a passing holistic live-render verification of the assembled landing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-single-scroll-landing-scroll-spy-nav/21-CONTEXT.md
@.planning/phases/21-single-scroll-landing-scroll-spy-nav/21-RESEARCH.md
@.planning/phases/21-single-scroll-landing-scroll-spy-nav/21-PATTERNS.md
@.planning/phases/21-single-scroll-landing-scroll-spy-nav/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scroll-spy IntersectionObserver + landing scroll/active-sync + detail->back scroll restore (SCROLL-02)</name>
  <files>docker/web/site/app.js</files>
  <read_first>
    - docker/web/site/app.js (the file being modified, post-Plan-01) — read the Plan 01 `showLanding(section)` and `route()` mode branch, `var landingScrollY = 0;`, and the init region: `document.addEventListener('DOMContentLoaded', ...)` (`:1070-1073`), `window.addEventListener('hashchange', route)` (`:1075`), and the sibling `hashchange`/`visibilitychange` listeners (`:1083-1100`). Read `setActiveTab()` (`:1029-1051`) — the `el.classList.toggle('active', isActive)` + `aria-current` pair the IO must replicate. Read `scrollToSubscribe()` (`:388-390`) — the in-codebase `scrollIntoView({behavior:'smooth'})` idiom (note: it does NOT branch on reduced-motion — prefer CSS).
    - .planning/phases/21-single-scroll-landing-scroll-spy-nav/21-PATTERNS.md §"scroll-spy initScrollSpy()" (`:130-166`) — mockup pattern (`:501-510`) adapted to the AgentPulse `.tab[data-tab]`/`href` anchors; register ONCE at init alongside the existing listeners; static-literal section ids in the LOCKED order; IO replicates `classList.toggle('active', ...)` + `aria-current='page'`/`removeAttribute` for NAV-02 a11y parity; Pitfall 3 mitigation (set initial `.active` from the target section in `showLanding` BEFORE IO takes over).
    - .planning/phases/21-single-scroll-landing-scroll-spy-nav/21-PATTERNS.md §"landingScrollY module var" (`:204-218`) + §"Active-tab classList.toggle + aria-current" + §"Hash-as-allowlist (security)".
    - .planning/phases/21-single-scroll-landing-scroll-spy-nav/21-RESEARCH.md §"Pattern 3" (`:160-178`) — the IO + `rootMargin:'-50% 0px -50% 0px'`; IO is silent while `#landing` is `display:none` (correct on detail). §"Pattern 4" (`:180-213`) — the scroll-restore options; option 1 (module var, on the in-page Back `<a href="#/">`/`hashchange`) is the LOCKED choice. §"Anti-Patterns" (`:215-220`) — do NOT call `setActiveTab` AND the scroll-spy on the landing.
    - .planning/phases/21-single-scroll-landing-scroll-spy-nav/21-CONTEXT.md §"Router two-mode model" (scroll-spy IO, smooth-scroll, detail->back scroll restore via module var) + §"Signals shell seam" (no fetch).
  </read_first>
  <action>
    Add the scroll-spy IO, the landing scroll-to-section + initial-active sync, and the detail->back scroll restore. Use `var` + the commented-global idiom; section ids are STATIC LITERALS (never `location.hash`).

    (A) `initScrollSpy()`: build ONE `IntersectionObserver` over the four landing section elements, looked up by their static-literal ids in the LOCKED order `['newsletter','signals','map','about']` (each `getElementById`, defensively skipped if null). On an entry's `isIntersecting`, toggle `.active` on the nav `.tab` anchors so the tab whose `href === '#' + e.target.id` is active — replicate the `setActiveTab` mechanism exactly: `el.classList.toggle('active', isMatch)` AND `aria-current='page'` on the match / `removeAttribute('aria-current')` otherwise (NAV-02 a11y parity). Construct with `{ rootMargin: '-50% 0px -50% 0px' }` (the canonical viewport-center scroll-spy). Select the nav anchors via the existing `.tab` set (the `data-tab`/`href` join). Register `initScrollSpy()` ONCE at init — call it in `DOMContentLoaded` after `route()` (`:1072`), same flat top-level style as the existing listeners. Observing hidden sections is harmless (silent while `#landing` is `display:none`).

    (B) Landing scroll-to-section + initial-active sync in `showLanding(section)`: after showing `#landing` + loading data (Plan 01), scroll to the target `section` element (`getElementById(section)`) so deep-link load + nav clicks land on it. Prefer the declarative CSS path (Task 2 adds `scroll-behavior:smooth` + `scroll-margin-top`), so a plain `el.scrollIntoView({block:'start'})` (no JS behavior branch) is sufficient — CSS owns the smoothness + reduced-motion gate; fall back to `window.scrollTo(0,0)` if the element is missing. BEFORE the IO takes over (Pitfall 3: IO fires zero events while the landing was `display:none`), set the initial `.active` explicitly on the nav `.tab` whose `href === '#' + section` (same toggle + `aria-current` pair) so returning from a detail route does not leave a stale tab highlighted.

    (C) Detail->back scroll restore: `landingScrollY` is already stashed on landing->detail (Plan 01 `route()`). Now, in the landing branch of `route()`, when returning to the ROOT landing hash (`#/` or empty -> `section:'newsletter'`) AFTER a detail route, restore the stashed position instead of jumping to top: if `landingScrollY > 0` and the navigation is a return-from-detail (e.g. guard on the landing having just been hidden, or a `cameFromDetail` flag set in the detail branch), call `window.scrollTo(0, landingScrollY)` instead of the default top scroll. The in-page "<- Back" controls are `<a href="#/">`/`href="#/map">` (hashchange), so the module-var-on-hashchange path covers them (research A3). Do NOT use `history.scrollRestoration`/`sessionStorage` (locked: module var). When navigating to a SPECIFIC bare section anchor (`#map`/`#signals`/`#about`), scroll to that section (do not restore the old position).

    Do NOT call `setActiveTab` on the landing (IO + the initial sync own it — Anti-Pattern). Do NOT touch the config block / Supabase placeholders / any query. Do NOT add data fetching to `#signals` (SIGNAL-04). Verify `node --check` passes.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node --check docker/web/site/app.js && grep -q 'function initScrollSpy' docker/web/site/app.js && grep -q 'new IntersectionObserver' docker/web/site/app.js && grep -q "rootMargin: *'-50% 0px -50% 0px'" docker/web/site/app.js && grep -Eq "\['newsletter', *'signals', *'map', *'about'\]" docker/web/site/app.js && grep -q "window.scrollTo(0, landingScrollY)" docker/web/site/app.js && grep -q "aria-current" docker/web/site/app.js && grep -q 'initScrollSpy()' docker/web/site/app.js && ! grep -q 'source_posts' docker/web/site/app.js && grep -q "const SUPABASE_URL = '__SUPABASE_URL__';" docker/web/site/app.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app.js` passes `node --check`.
    - Source: `initScrollSpy()` constructs ONE `IntersectionObserver` with `{ rootMargin: '-50% 0px -50% 0px' }` over the four sections looked up by the static-literal id array `['newsletter','signals','map','about']` (LOCKED order); on intersection it toggles `.active` + sets/clears `aria-current` on the `.tab` anchor whose `href === '#' + e.target.id`.
    - Source: `initScrollSpy()` is called exactly once at init (after `route()` in `DOMContentLoaded`); section ids passed to `getElementById`/`observe` are static literals, never `location.hash` (security allowlist).
    - Source: `showLanding(section)` scrolls to the target section and sets the initial `.active`/`aria-current` on the matching tab BEFORE the IO takes over (Pitfall 3).
    - Source: `route()`'s landing branch restores `window.scrollTo(0, landingScrollY)` when returning to the root landing hash after a detail route (module-var restore; no `history.scrollRestoration`/`sessionStorage`); a specific section anchor scrolls to that section instead.
    - Source: `setActiveTab` is NOT called on the landing; no `source_posts` fetch added; the `__SUPABASE_*__` placeholders intact; no query changed.
    - Behavior (verified live in Task 3): scrolling the landing highlights the active tab; clicking a tab scrolls to its section; opening then backing out of an edition restores the prior landing scroll position; the section heading is not hidden under the sticky nav.
  </acceptance_criteria>
  <done>`initScrollSpy()` drives the active nav via IntersectionObserver (rootMargin -50%/-50%, static-literal ids, `.active`+`aria-current` parity), registered once at init; `showLanding` scrolls to the section + sets the initial active before IO; `route()` restores `landingScrollY` on detail->back; no fetch added; `node --check` passes.</done>
</task>

<task type="auto">
  <name>Task 2: CSS — smooth-scroll + reduced-motion gate + sticky-header scroll-margin + #landing section-rhythm (SCROLL-02 / A11Y-no-regress / RHYTHM-01)</name>
  <files>docker/web/site/style-base.css</files>
  <read_first>
    - docker/web/site/style-base.css (the file being modified) — read the `:root` token block: `--space-md:16px` (`:59`), `--line-strong:#d8d2c7` (`:18`); read the `body > header { position:sticky; top:0; ... }` rule (`:142-149`) — DO NOT touch it; the header rendered height is ~60px (nav `padding:12px` at `:155` + ~12.5px mono content). Read the `.tab` / `.tab.active` rules (`:190-211`) — the scroll-spy REUSES `.active`; do NOT add a new active-state rule.
    - .planning/phases/21-single-scroll-landing-scroll-spy-nav/21-PATTERNS.md §"No Analog Found" (`:413-424`) — these scroll/motion rules are the ONLY net-new CSS (grep-confirmed: zero pre-existing `scroll-behavior`/`scroll-margin`/`prefers-reduced-motion`/`IntersectionObserver`); source = mockup `:231` + 21-RESEARCH Code Examples; the reduced-motion gate is MANDATORY (A11Y-01 must-not-regress).
    - .planning/phases/21-single-scroll-landing-scroll-spy-nav/21-PATTERNS.md §"style-base.css — scroll-spy active state reuse" (`:329-352`, do NOT duplicate `.active`) + §"section-boundary rhythm" (`:356-368`) — extend the `X + X { border-top:1px solid var(--line-strong) }` adjacent-sibling pattern to `#landing > section + section`; token-only (no hex literal); do NOT alter the Phase-20 width tokens.
    - .planning/phases/21-single-scroll-landing-scroll-spy-nav/21-RESEARCH.md §"Code Examples" (`:294-303`) — the exact `html{scroll-behavior:smooth}` + `#landing>section{scroll-margin-top:calc(60px + var(--space-md))}` + `@media (prefers-reduced-motion:reduce){html{scroll-behavior:auto}}` block. Pitfall 2 (`:256-260`, sticky offset) + A7 (`:382`, the ~60px value tunes against the live render).
    - .planning/phases/21-single-scroll-landing-scroll-spy-nav/21-CONTEXT.md §"Must NOT change" (Phase-20 width/rhythm tokens; `body > header` sticky scoping) + the §"Claude's Discretion" reduced-motion guard note.
  </read_first>
  <action>
    Add the four net-new CSS rules to `style-base.css` (the only genuinely new patterns in this phase). Use tokens, never hex literals. Do NOT touch the Phase-20 width tokens (`--measure`/`--wide`/`--gutter`) or the `body > header` sticky rule.

    (A) Smooth-scroll + reduced-motion gate: add `html { scroll-behavior: smooth; }` and the mandatory gate `@media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto; } }`. This makes nav-anchor clicks smooth-scroll declaratively AND suppresses the motion under reduced-motion for free (A11Y-01 must-not-regress; the net-new motion this phase introduces must be motion-safe).

    (B) Sticky-header offset: add `#landing > section { scroll-margin-top: calc(60px + var(--space-md)); }` so an anchor jump (deep-link or nav click) lands the section heading BELOW the sticky `body > header`, not hidden under it (Pitfall 2). The `60px` is the rendered header height estimate (nav padding 12px + mono content) — flagged for live-render measurement in Task 3 (research A7); tune there if the heading is clipped or over-offset.

    (C) Section-rhythm between landing sections: extend the existing `X + X { border-top:1px solid var(--line-strong) }` major-boundary pattern (style-shared `.tier-section + .tier-section` `:235`, `.content-area` top `:132`) to the landing: `#landing > section + section { border-top: 1px solid var(--line-strong); padding-top: var(--space-xl); }` (or the established major-boundary spacing) — one full-strength `--line-strong` rule between each major landing section (RHYTHM-01 hierarchy: major between sections; the Phase-20 within-section hairlines are unchanged). Use ONLY `--line-strong` + `--space-*` tokens — never a hex literal (`#d8d2c7`).

    Do NOT add a new `.active` rule (the scroll-spy reuses the existing `.tab.active` accent-soft fill — `:206-211`). Do NOT touch `body > header`'s `position`/`top`/`z-index`/`border-bottom` or the `body >` scoping (maturity-overlap fix, MEMORY `260609-ivq`). Group the new rules under a clearly-commented section header (e.g. "Phase 21: single-scroll landing — scroll behavior + section rhythm").
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -Eq 'html *\{[^}]*scroll-behavior: *smooth' docker/web/site/style-base.css && grep -Eq 'prefers-reduced-motion: *reduce' docker/web/site/style-base.css && grep -Eq 'scroll-behavior: *auto' docker/web/site/style-base.css && grep -Eq '#landing *> *section *\{[^}]*scroll-margin-top: *calc\(60px \+ var\(--space-md\)\)' docker/web/site/style-base.css && grep -Eq '#landing *> *section *\+ *section *\{[^}]*border-top: *1px solid var\(--line-strong\)' docker/web/site/style-base.css && grep -q 'position:sticky' docker/web/site/style-base.css && grep -q 'body > header' docker/web/site/style-base.css && [ "$(grep -vE '^\s*/?\*|^\s*//' docker/web/site/style-base.css | grep -Ec 'scroll-margin-top: *[0-9]+px;')" = "0" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: `style-base.css` contains `html { scroll-behavior: smooth; }` AND `@media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto; } }` (the reduced-motion gate is present — A11Y-01 not regressed).
    - Source: `#landing > section { scroll-margin-top: calc(60px + var(--space-md)); }` is present (token-based offset; no raw-px scroll-margin literal).
    - Source: `#landing > section + section { border-top: 1px solid var(--line-strong); ... }` extends the existing major-boundary adjacent-sibling pattern, using only `--line-strong`/`--space-*` tokens (no hex literal).
    - Source: NO new `.tab.active`/`.active` rule is added (the scroll-spy reuses the existing one); the `body > header { position:sticky ... }` rule + its `body >` scoping are unchanged.
    - Behavior (verified live in Task 3): nav clicks smooth-scroll (instant under reduced-motion); anchor jumps land below the sticky nav; a full-strength rule separates each major landing section.
  </acceptance_criteria>
  <done>The four net-new CSS rules are added (smooth-scroll, reduced-motion gate, `#landing>section` scroll-margin-top, `#landing>section + section` major rhythm) using tokens only; the existing `.tab.active` and `body > header` sticky rules are untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Deploy (scoped web rebuild, orchestrator-owned) + holistic live-render verification of SCROLL-01/02 + folded WIDTH-01/RHYTHM-01 (worktree-unsafe)</name>
  <what-built>
    Plan 01 restructured `index.html` into the bare-anchor nav + `#landing` with 4 stacked sections + a static `#signals` shell, and refactored `app.js` into the two-mode router (`getRoute` mode discriminator, `route()` branch, `showLanding`/`showDetail`, decoupled loaders, `landingScrollY`). Plan 02 Tasks 1-2 added the scroll-spy IntersectionObserver + landing scroll/active-sync + detail->back scroll restore (`app.js`), and the smooth-scroll + reduced-motion gate + sticky-header `scroll-margin-top` + `#landing>section` rhythm (`style-base.css`). This checkpoint deploys the assembled landing and verifies SCROLL-01 + SCROLL-02 on the ACTUAL rendered output — AND re-verifies WIDTH-01 + RHYTHM-01 holistically on the merged page (Phase 20's folded-in visual sign-off). Per the Phase-19 lesson (MEMORY [[feedback_verify_render_bugs_end_to_end]]): reproduce the rendered output; never infer a clean result from source bytes.
  </what-built>
  <how-to-verify>
    DEPLOY (orchestrator-owned, from the MAIN tree — worktree-unsafe; do NOT run from a worktree executor; the scoped rebuild cds to the absolute main-tree path so a worktree would build stale code — MEMORY scoped-rebuild-worktree-unsafe):
    1. Prod<->main drift check FIRST; get operator approval BEFORE any deploy (standing v2.2 deploy discipline + MEMORY scoped/approved deploys). Branch + `/diff` the `index.html`/`app.js`/`style-base.css` work group.
    2. Scoped rebuild ONLY the web service: `cd /root/bitcoin_bot/docker && docker compose up -d --build web` — use the SERVICE key `web`, NOT the `agentpulse-web` container_name (MEMORY: web compose service name). NO `--delete`.
    3. Confirm the served `app.js` over the wire is the substituted version (the `__SUPABASE_URL__` sed-substitution ran — no literal placeholder in the served file; the SPA loads, not a blank page).

    VERIFY ON THE LIVE SITE (wide ~1440px viewport unless noted):
    4. SCROLL-01 single-scroll landing: loading `#/` shows ONE scroll page with the four sections stacked in the LOCKED order Newsletter -> Signals -> Agent-Economy -> About; scrolling moves through all four; there are no longer separate top-level routes.
    5. SCROLL-01 detail-route coexistence (HARD criterion): deep-link each of `#/edition/30` and `#/map/identity-trust` (use a real edition/slug) directly in the address bar — each opens the standalone DETAIL view (NOT the landing), the landing is hidden, `marked`-rendered body shows. `#/status` and `#/unsubscribe?id=x` still resolve. The "<- Back" link returns to the landing.
    6. SCROLL-01 bare anchors: `#newsletter`, `#signals`, `#map`, `#about` each show the landing scrolled to that section; `#signals` is a STATIC placeholder shell (heading + empty state, NO data, NO console 401 on `source_posts`).
    7. SCROLL-02 scroll-spy: as you scroll the landing, the active nav tab highlights the section straddling the viewport center (IO rootMargin -50%/-50%); the highlighted tab uses the existing accent-soft `.active` fill and carries `aria-current="page"`.
    8. SCROLL-02 smooth-scroll on click: clicking a nav tab smooth-scrolls to its section; the section heading lands BELOW the sticky nav (scroll-margin-top offset correct — NOT clipped under the header; if clipped or over-offset, tune the `calc(60px + ...)` value per research A7).
    9. SCROLL-02 scroll-restore: from `#/` scroll partway down (e.g. to the Map section), open an edition (`#/edition/<n>`), then "<- Back" — the landing returns with the prior scroll position PRESERVED (not jumped to top).
    10. SCROLL-02 reduced-motion: with OS "reduce motion" on, nav clicks jump instantly (no smooth animation) — the `@media (prefers-reduced-motion:reduce)` gate works (net-new motion is motion-safe; A11Y not regressed).
    11. WIDTH-01 (folded re-verify) on the ASSEMBLED landing: no dead left gutter — content fills a centered band; reading copy (an opened edition + block page) holds a ~60-70 char line via `--measure`; the tiled sections (list/map) sit on `--wide`; the sticky nav's left/right edges line up with the wide content edges below.
    12. RHYTHM-01 (folded re-verify): every surface themes off the off-white + violet token system (Subscribe button + active toggle render white-on-violet; the active scroll-spy tab is accent-soft); a full-strength rule separates each major landing section, hairlines within.
    13. Regression guards (Pitfall 5 / no-regress): the Technical/Strategic mode toggle is visible on the landing (inside the Newsletter section) and works; it does NOT appear on a block/edition detail page; the subscribe form works; existing deep links (editions/blocks) still resolve; the maturity pill does NOT overlap the nav on a block page (the `body > header` scoping is intact).
    14. Scope guard: no per-section visual fix shipped (map still 2-col, About grid unchanged, edition-header still has its current suffix — those are Phase 22); Signals has NO live data (Phase 24); no full responsive/a11y pass beyond the reduced-motion gate (Phase 25).
  </how-to-verify>
  <resume-signal>Operator types "approved" once the live render shows the single-scroll landing (4 sections, locked order) with a working scroll-spy + smooth-scroll + scroll-restore + reduced-motion gate, editions/blocks still deep-linkable, the `#signals` shell static, and WIDTH-01/RHYTHM-01 holding holistically — or describes the specific issue (e.g. a scroll-margin-top tune, a stale active tab on back) to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a client-side `IntersectionObserver` + scroll-restore logic (`app.js`) and four static CSS rules (`style-base.css`), then runs a scoped `web` rebuild + live verification. No new input path beyond the URL hash (validated by the Plan 01 anchored allowlist); the IO operates on STATIC-LITERAL section ids, never hash-derived values; the Signals shell stays static (NO `source_posts` fetch). No new data flow, network call, auth surface, or HTML/JS/URL sink. The pre-existing `__SUPABASE_*__` substitution + anon-key reads are untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | `IntersectionObserver` / `scrollIntoView` target ids | mitigate | The IO observes and `scrollIntoView` targets STATIC-LITERAL section ids (`['newsletter','signals','map','about']`) or the Plan 01 allowlisted `getRoute().section` — NEVER raw `location.hash` (21-RESEARCH Security V5). The acceptance gate asserts the static-literal id array; no hash value reaches a DOM sink. |
| T-21-05 | Information Disclosure | scoped `web` rebuild / deploy | mitigate | Deploy is orchestrator-owned from the main tree, scoped to the `web` SERVICE key, NO `--delete`, with a prod<->main drift check + operator approval BEFORE any deploy (Task 3 steps 1-2). Prevents the worktree-stale-build + rsync-blast-radius failure modes (MEMORY: scoped/approved deploys, web compose service name, scoped-rebuild-worktree-unsafe). The plan is `autonomous: false`. |
| T-21-06 | Information Disclosure | static `#signals` shell (no premature fetch) | mitigate | Task 1 forbids any `source_posts` fetch (`! grep source_posts` gate); the live-verify (step 6) confirms no console 401 / empty-feed silent failure. A premature anon read of the RLS-blocked table = the silent-empty-feed fail-loud violation Phase 24's RLS migration is designed to prevent (SIGNAL-04). |
| T-21-07 | Denial of Service | reduced-motion / net-new motion | accept | The only net-new motion (smooth-scroll) is gated by `@media (prefers-reduced-motion: reduce)` (Task 2); no auto-playing animation, no scroll-jacking, no infinite observer churn (IO fires only on threshold crossings, not per frame). A11Y not regressed. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs — no `package.json` (the web service has none), no new CDN `<script>`, no PyPI/crates dependency; the two existing CDN scripts are unchanged. Package Legitimacy Gate not applicable (21-RESEARCH §"Package Legitimacy Audit": zero packages). |

Net security delta: zero new attack surface. The scroll-spy + scroll-restore operate on static-literal ids; no hash value reaches a sink; no new fetch; the reduced-motion gate keeps the net-new motion accessible; the deploy follows the scoped/approved/drift-checked discipline.
</threat_model>

<verification>
- `initScrollSpy()` drives the active nav via `IntersectionObserver` (rootMargin -50%/-50%, static-literal ids, `.active`+`aria-current` parity), registered once at init; `showLanding` scrolls to the section + sets the initial active before IO; `route()` restores `landingScrollY` on detail->back; no `source_posts` fetch; `node --check` passes (Task 1 gate).
- The four net-new CSS rules are present (smooth-scroll, reduced-motion gate, `#landing>section` scroll-margin-top, `#landing>section + section` rhythm), token-only; the existing `.tab.active` + `body > header` rules untouched (Task 2 gate).
- Holistic live-render verification (orchestrator-run, worktree-unsafe scoped `web` rebuild) confirms on the ACTUAL rendered output: SCROLL-01 (single-scroll landing in the locked order; editions/blocks still deep-linkable; static `#signals` shell), SCROLL-02 (scroll-spy highlight, smooth-scroll, scroll-restore, reduced-motion gate), and the folded WIDTH-01/RHYTHM-01 (no dead gutter, ~60-70 char reading line, aligned nav/content edges, token color, section-rhythm hierarchy) — plus the regression guards (toggle scoping, maturity/nav non-overlap) and no Phase 22-25 scope creep (Task 3).
- Render verification reproduces the rendered pages — it does NOT infer a clean result from source bytes (Phase-19 lesson).
</verification>

<success_criteria>
SCROLL-02 ships and the phase is holistically verified on the live render:
- The persistent nav is a scroll-spy (active highlight on scroll via IntersectionObserver; smooth-scroll on click; reduced-motion-safe).
- Opening a detail route leaves the landing; returning restores the landing scroll position via `landingScrollY`.
- The sticky header does not overlap a section heading on an anchor jump (scroll-margin-top offset).
- On the assembled landing, WIDTH-01 holds (no dead gutter, ~60-70 char reading copy, tiled on --wide, nav edges == content edges) and RHYTHM-01 holds (token color, major/within rhythm) — re-verified holistically.
- No regression: the mode toggle, subscribe form, and existing deep links work; the maturity pill does not overlap the nav. No scope creep into Phases 22-25.
</success_criteria>

<output>
Create `.planning/phases/21-single-scroll-landing-scroll-spy-nav/21-02-SUMMARY.md` when done.
</output>
