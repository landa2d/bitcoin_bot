---
phase: 22-per-section-visual-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/web/site/app.js
autonomous: true
requirements: [HEAD-01]
must_haves:
  truths:
    - "An opened edition page H1 contains ONLY the headline — no `— Edition #N | <date>` suffix (stripped at render in getModeTitle, covering both data.title (Technical) and data.title_impact (Strategic); a no-op when the suffix is absent)."
    - "Edition number, date, and mode (Technical/Strategic) appear EXACTLY ONCE on the reader chrome — in the single byline below the title; the duplicate eyebrow line above the title is gone (D-01)."
    - "The byline format/order is unchanged: `Edition #N · date · Mode` (U+00B7 separator, formatDate(), MODES[currentMode].label) — D-02."
    - "No stored data is mutated — the strip is render-only (D-03); the D-04 stored-bytes finding (whether the suffix is baked into newsletters.title/title_impact and its exact pattern) is recorded in the SUMMARY."
  artifacts:
    - path: "docker/web/site/app.js"
      provides: "getModeTitle() applies a module-level EDITION_SUFFIX_RE strip to the mode-resolved title before returning (single chokepoint for both modes); renderArticle()'s header drops the .eyebrow line and keeps the .byline verbatim."
      contains: "EDITION_SUFFIX_RE"
  key_links:
    - from: "stored newsletters.title / title_impact (mode-resolved)"
      to: "the edition H1 (escapeHtml'd)"
      via: "getModeTitle() -> .replace(EDITION_SUFFIX_RE,'').trim() -> escapeHtml at the H1 sink"
      pattern: "replace\\(EDITION_SUFFIX_RE"
---

<objective>
HEAD-01 — edition-header de-duplication on the detail route. Today `renderArticle` (`app.js:430-435`) prints edition/date/mode TWICE inside the reader chrome — an `.eyebrow` `Edition #N · Mode` ABOVE the title AND a `.byline` `Edition #N · date · Mode` BELOW it — and the H1 may additionally carry a baked `— Edition #N | <date>` suffix from the stored title. This plan makes the edition number/date/mode appear exactly once.

Purpose: per the ROADMAP success criteria, an edition page H1 must contain only the headline, with edition/date/mode in the one meta line below. Two independent fixes: (1) strip a baked title suffix at render in `getModeTitle` so it covers BOTH Technical (`data.title`) and Strategic (`data.title_impact`) — render-only, never a storage mutation (D-03); (2) drop the duplicate eyebrow line, keeping the existing byline verbatim (D-01/D-02). The strip regex is written AFTER confirming the real stored bytes (D-04, Phase-19 discipline — reproduce the bytes, do not infer from the brief's example pattern).

Output: a module-level `EDITION_SUFFIX_RE` + its application in `getModeTitle`, and the eyebrow-line deletion in `renderArticle`, both in `app.js`. Source-only — the live render is verified in the orchestrator-owned deploy/verify plan (22-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-per-section-visual-fixes/22-CONTEXT.md
@.planning/phases/22-per-section-visual-fixes/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm stored title bytes (D-04) + add the render-only suffix strip to getModeTitle (HEAD-01 / D-03)</name>
  <files>docker/web/site/app.js</files>
  <read_first>
    - docker/web/site/app.js — `getModeTitle(data)` (`:563-566`, the SINGLE strip chokepoint covering both `data.title` and `data.title_impact`), `getModeContent` (`:568-571`, sibling — do NOT touch), `escapeHtml` (`:542-546`, the H1 sink — strip the RAW string FIRST, then escapeHtml), and the title flow into `renderArticle` (`var title = getModeTitle(data)` at `:409` -> `escapeHtml(title)` at `:433`). Note `currentMode` is the `'technical' | 'strategic'` discriminator.
    - .planning/phases/22-per-section-visual-fixes/22-PATTERNS.md §"HEAD-01" (`:25-51`) — the title-source excerpt, the D-04 discipline note (em-dash `—` U+2014 vs hyphen, `|` vs `·`, date format must be CONFIRMED), and the strip-raw-then-escape ordering.
    - .planning/phases/22-per-section-visual-fixes/22-CONTEXT.md §"HEAD-01" decisions D-03 (strip at render, NEVER storage; unconditional/defensive) + D-04 (confirm the stored format first).
    - .planning/docs/REDESIGN_CC_BRIEF.md §"TASK 3" (`:93-102`) — the brief's STARTING pattern ` — Edition #\d+ \| .*$` and the edition-30 acceptance string (a starting point, not confirmed).
  </read_first>
  <action>
    FIRST (D-04, Phase-19 discipline — reproduce the bytes, do not infer): query the raw stored `newsletters.title` and `title_impact` for a known edition via DIRECT PostgREST (anon REST, NOT supabase-py `.in_()`). Run, from /root/bitcoin_bot, sourcing `config/.env` for `SUPABASE_URL` + `SUPABASE_ANON_KEY`, a curl GET to `$SUPABASE_URL/rest/v1/newsletters?edition_number=in.(29,30)&select=edition_number,title,title_impact` with the `apikey` + `Authorization: Bearer` anon-key headers. Inspect the returned bytes to determine (a) whether a `— Edition #N | <date>`-style suffix is actually baked into `title` and/or `title_impact`, and (b) its EXACT separator (em-dash U+2014 `—` vs hyphen `-` vs en-dash `–`), the `|` vs `·` delimiter, and the date format. RECORD this finding in the SUMMARY (which fields carry the suffix, the exact pattern, or "no suffix baked — strip is purely defensive").

    THEN add a module-level constant `EDITION_SUFFIX_RE` whose regex matches the CONFIRMED suffix. Use the brief's `/\s*[—–-]\s*Edition\s*#\d+\s*\|.*$/i` ONLY as a starting shape and ADJUST it to the real separator/delimiter/date format observed in the curl output (anchor it to `$` so it only strips a trailing suffix; tolerate em-dash, en-dash, and hyphen; case-insensitive). Place the constant near the other top-of-file constants (alongside `MODES`/`MATURITY_STAGE`).

    THEN apply it as the SINGLE chokepoint inside `getModeTitle`: resolve the mode title as today (`currentMode === 'strategic' && data.title_impact` -> `data.title_impact`, else `data.title`), then return `String(rawTitle || '').replace(EDITION_SUFFIX_RE, '').trim()`. Applying it to the resolved value covers BOTH Technical and Strategic in one place (D-03). The regex is UNCONDITIONAL/defensive — a no-op when no suffix is present, so a future edition that bakes the suffix is also covered. Strip the RAW string here, BEFORE `escapeHtml` runs at the `:433` H1 sink (escape order preserved). Do NOT mutate stored data; do NOT touch `getModeContent`, the Supabase queries, or the `__SUPABASE_*__` placeholders. Confirm `node --check` passes.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node --check docker/web/site/app.js && grep -q 'EDITION_SUFFIX_RE' docker/web/site/app.js && grep -q 'replace(EDITION_SUFFIX_RE' docker/web/site/app.js && grep -q "const SUPABASE_URL = '__SUPABASE_URL__';" docker/web/site/app.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The D-04 stored-bytes finding is recorded in the SUMMARY: which of `title` / `title_impact` (if any) carry the suffix, and the EXACT confirmed pattern (separator/delimiter/date format) — or an explicit "no suffix baked; strip is defensive".
    - Source: `app.js` defines a module-level `EDITION_SUFFIX_RE` whose regex matches the CONFIRMED suffix (anchored to `$`, tolerant of em/en-dash/hyphen, case-insensitive).
    - Source: `getModeTitle` applies `.replace(EDITION_SUFFIX_RE, '').trim()` to the mode-resolved title (single chokepoint covering both `data.title` and `data.title_impact`) and strips the RAW string before the `escapeHtml(title)` H1 sink.
    - Source: no stored-data mutation, no query change, `__SUPABASE_*__` placeholders intact; `node --check` passes.
    - Behavior (verified live in 22-04): edition 30's H1 reads only the headline (no `— Edition #N | <date>` suffix) in both Technical and Strategic modes.
  </acceptance_criteria>
  <done>The stored title bytes are confirmed (D-04, recorded in SUMMARY); `EDITION_SUFFIX_RE` strips the confirmed suffix render-only inside `getModeTitle` for both modes (defensive no-op when absent); no storage mutation; `node --check` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Drop the duplicate eyebrow line from the reader header, keep the byline verbatim (HEAD-01 / D-01 / D-02)</name>
  <files>docker/web/site/app.js</files>
  <read_first>
    - docker/web/site/app.js — `renderArticle` header builder (`:428-435`): `var sep = ' ' + String.fromCharCode(0xB7) + ' ';` (`:428`), `var modeLabel = MODES[currentMode].label;` (`:429`), the `.eyebrow` line at `:432` (the one to DELETE), the `.page-title` H1 at `:433` (keep, now headline-only after Task 1), and the `.byline` line at `:434` (KEEP verbatim). Note `updateHero(...)` at `:416` is the list-scoped hero (harmless on the reader route) — do NOT touch it.
    - .planning/phases/22-per-section-visual-fixes/22-PATTERNS.md §"HEAD-01" (`:29-51`) — the exact header excerpt marking `:432` DELETE / `:434` KEEP, and the CSS-no-change note (the now-dead `.article-header .eyebrow` rule at `style-shared.css:649-651` is harmless and is NOT removed in this phase).
    - .planning/phases/22-per-section-visual-fixes/22-CONTEXT.md §"HEAD-01" D-01 (drop the eyebrow entirely; no replacement kicker) + D-02 (byline format/order unchanged: `Edition #N · date · Mode`).
  </read_first>
  <action>
    In `renderArticle`'s `header` string (`:430-435`), DELETE the `.eyebrow` line (the `'<p class="eyebrow">Edition #' + data.edition_number + sep + modeLabel + '</p>' +` fragment at `:432`) so the `.article-header` div contains only the `.page-title` H1 (headline-only after Task 1's strip) and the `.byline` (D-01). KEEP the `.byline` line (`:434`) EXACTLY as-is — `'<p class="byline">Edition #' + data.edition_number + sep + date + sep + modeLabel + '</p>'` — preserving the U+00B7 `sep`, `formatDate()` `date`, and the resolved `MODES[currentMode].label` `modeLabel` (D-02). `sep` and `modeLabel` remain used by the byline, so leave their declarations (`:428-429`). Do NOT add a replacement kicker/eyebrow (operator rejected keeping the slot). Do NOT touch the CSS, `updateHero`, the preview banner, or `marked.parse`. Confirm `node --check` passes.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node --check docker/web/site/app.js && ! grep -q 'class="eyebrow">Edition #' docker/web/site/app.js && grep -q 'class="byline">Edition #' docker/web/site/app.js && grep -q 'MODES\[currentMode\].label' docker/web/site/app.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: the `.eyebrow` `Edition #...` line is removed from `renderArticle`'s header (`! grep 'class="eyebrow">Edition #'`); the `.article-header` div now holds only the `.page-title` H1 + the `.byline`.
    - Source: the `.byline` line is unchanged — `Edition #N` + U+00B7 `sep` + `date` + `sep` + `MODES[currentMode].label` (D-02 format/order verbatim).
    - Source: no replacement kicker added; CSS / `updateHero` / preview banner untouched; `node --check` passes.
    - Behavior (verified live in 22-04): the reader chrome shows the headline H1 + ONE byline (`Edition #N · date · Mode`); edition/date/mode appear exactly once.
  </acceptance_criteria>
  <done>The duplicate eyebrow line is gone; the byline survives verbatim (`Edition #N · date · Mode`); no replacement kicker; `node --check` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored newsletters.title/title_impact -> edition H1 | The only data crossing into markup. It already flowed through `escapeHtml` at the H1 sink (`app.js:433`); this plan inserts a `.replace()` on the RAW string BEFORE that escape, preserving the existing sanitization order. No NEW input path, endpoint, query, or write verb is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Tampering | `getModeTitle` strip vs the `escapeHtml` H1 sink | mitigate | The `EDITION_SUFFIX_RE` strip runs on the raw mode-resolved string and the result is still passed through the existing `escapeHtml(title)` at `:433` — escaping is NOT bypassed or reordered. The regex is anchored to `$` (trailing-suffix only), so it cannot remove or alter interior headline content. Acceptance asserts the strip precedes the escape sink. |
| T-22-01-02 | Information Disclosure | D-04 PostgREST read | accept | The D-04 confirmation is a READ-ONLY anon SELECT of `newsletters` (already anon-readable for published editions), no write, no schema/RLS change, no stored-data mutation (D-03). Reproduces stored bytes for diagnosis only. |
| T-22-01-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs — the web service has no `package.json`, no new CDN `<script>`; the two existing CDN scripts are unchanged. Package Legitimacy Gate not applicable. |

Net security delta: zero new attack surface. The change is a render-time string strip that preserves the existing escape order plus a read-only diagnostic query; ASVS L1, no high-severity threat introduced.
</threat_model>

<verification>
- `getModeTitle` strips the CONFIRMED `EDITION_SUFFIX_RE` from the mode-resolved title before `escapeHtml`, covering both modes; `node --check` passes (Task 1 gate).
- The duplicate `.eyebrow` line is removed and the `.byline` survives verbatim with the U+00B7 separator + `MODES[currentMode].label` (Task 2 gate).
- No stored-data mutation; the D-04 finding is documented in the SUMMARY (Phase-19 discipline — reproduce, do not infer).
- Live confirmation (edition 30 H1 = headline only; one byline) is owned by the orchestrator deploy/verify plan 22-04.
</verification>

<success_criteria>
HEAD-01 ships at source:
- An edition page H1 contains only the headline (baked `— Edition #N | <date>` suffix stripped at render for both Technical and Strategic; never mutated in storage).
- Edition number, date, and mode appear exactly once — in the single byline below the title (`Edition #N · date · Mode`, format unchanged).
- The duplicate eyebrow line is gone; no replacement kicker.
- The D-04 stored-bytes finding is recorded.
</success_criteria>

<output>
Create `.planning/phases/22-per-section-visual-fixes/22-01-SUMMARY.md` when done (include the D-04 stored-bytes finding).
</output>
