---
phase: 22-per-section-visual-fixes
plan: 04
type: execute
wave: 3
depends_on: ["22-01", "22-02", "22-03"]
files_modified: []
autonomous: false
requirements: [HEAD-01, GRID-01, GRID-02, AGENTS-01]
must_haves:
  truths:
    - "Deployed via a scoped `docker compose ... web` rebuild (SERVICE key `web`, NOT `agentpulse-web`; NO `--delete`) from the absolute main tree `/root/bitcoin_bot/docker`, after a prod<->main drift check + per-work-group `/diff` + explicit operator approval (orchestrator-owned, worktree-unsafe)."
    - "HEAD-01 verified live: an edition page H1 reads only the headline; edition number/date/mode appear exactly once in the byline below (both Technical and Strategic modes)."
    - "GRID-01/02 verified live: the map renders 3-col on desktop, collapsing 3->2 at <=880px -> 1 at <=600px; a maturity legend sits under the heading with a scale matching the cards; each block's pill fill matches its stored economy_map maturity."
    - "AGENTS-01 verified live: the About section shows the numbered 01-04 pipeline + bulleted supporting layer with no orphaned card, and the violet `Nothing publishes without human approval.` callout; the D-12 copy-accuracy point (Gato vs Gato Brain, `eight cooperating services`) is signed off by the operator."
    - "No regression to Phase 20 width axes (no dead left gutter, ~60-70 char reading copy on `--measure`, tiled sections on `--wide`) or Phase 21 scroll-spy / two-mode router / sticky-header non-overlap."
  artifacts:
    - path: ".planning/phases/22-per-section-visual-fixes/22-04-SUMMARY.md"
      provides: "Record of the drift-check result, the scoped web rebuild, the operator deploy approval, the live-verification of the 3 success criteria, the D-12 sign-off, and the no-regression confirmation."
      contains: "deploy"
  key_links:
    - from: "the three source plans' committed changes"
      to: "the live aiagentspulse.com render"
      via: "scoped `docker compose up -d --build web` from the main tree"
      pattern: "docker compose"
---

<objective>
Deploy + live-verify Phase 22. Plans 22-01/02/03 land the three source fixes (HEAD-01 edition-header de-dup, GRID-01/02 map 3-col + maturity legend, AGENTS-01 About pipeline/supporting split + approval callout). This plan ships them via the established scoped `web` rebuild and verifies all three ROADMAP success criteria on the ACTUAL rendered site — including the D-12 copy-accuracy operator sign-off and a no-regression check against Phase 20 (width axes) + Phase 21 (scroll-spy / two-mode router).

Purpose: source assertions prove the fixes exist; only the live render proves they WORK (Phase-19 lesson, MEMORY [[feedback_verify_render_bugs_end_to_end]] — reproduce the rendered output, never infer from source bytes). The deploy is orchestrator-owned and worktree-unsafe: the scoped rebuild cds to the absolute main-tree path, so a worktree executor would build stale code (MEMORY: scoped-rebuild-worktree-unsafe; web compose service name). Deploy discipline is enforced: prod<->main drift check first, branch + `/diff` per work group, operator approval BEFORE any rebuild, scoped to the `web` SERVICE key, NO `--delete`.

Output: a deployed, operator-approved live render of Phase 22 + a SUMMARY recording the drift result, the deploy, the live verification, and the D-12 sign-off. This plan has `autonomous: false` (it contains a blocking-human checkpoint) and modifies no source files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-per-section-visual-fixes/22-CONTEXT.md
@.planning/phases/22-per-section-visual-fixes/22-01-SUMMARY.md
@.planning/phases/22-per-section-visual-fixes/22-02-SUMMARY.md
@.planning/phases/22-per-section-visual-fixes/22-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deploy gate — prod<->main drift check + branch + source-marker preflight (orchestrator-owned)</name>
  <files></files>
  <read_first>
    - scripts/drift-check.sh — the prod<->main drift checker invoked before every v2.2 deploy (expect only the KNOWN non-zero line for `lab-data-provider` — the v1.0 D-07 carry-over; any OTHER non-zero drift halts for operator review).
    - ./CLAUDE.md §"Build & Run Commands" + §"Deploy" — the scoped rebuild discipline; web compose SERVICE key is `web` (NOT `agentpulse-web`); `__SUPABASE_URL__` entrypoint substitution (preview vs live).
    - .planning/phases/22-per-section-visual-fixes/22-CONTEXT.md §"Deploy (worktree-unsafe — orchestrator-owned)" (`:199-205`).
    - The three source SUMMARYs (22-01/02/03) — confirm each fix shipped + read the D-04 finding (22-01) and the D-12 copy point (22-02) to surface at the checkpoint.
  </read_first>
  <action>
    Orchestrator-owned preflight from the MAIN tree (do NOT run from a worktree). (1) Run `scripts/drift-check.sh`; confirm the ONLY non-zero line is the known `lab-data-provider` carry-over — if any other service shows drift, STOP and surface it for operator review before proceeding. (2) Create/confirm the working branch for this phase's deploy. (3) Run the source preflight on the committed three-plan changes — `node --check docker/web/site/app.js` passes, and assert each fix's marker is present in the tree: `EDITION_SUFFIX_RE` + no `class="eyebrow">Edition #` (HEAD-01); `repeat(3, 1fr)` + `max-width: 880px` + no `640px` (GRID-01); `class="legend"` + `data-stage="1"` (GRID-02); `class="made-cols"` + `class="approval"` + `Gato Brain` + no `class="agent-row"` (AGENTS-01); and the RHYTHM-01 guard — style-shared.css + index.html both hex-free, no `--violet`/`--line-soft` anywhere. Do NOT rebuild yet (the rebuild is gated on the Task 2 operator approval). Record the drift result + preflight outcome for the checkpoint.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && node --check docker/web/site/app.js && grep -q 'EDITION_SUFFIX_RE' docker/web/site/app.js && ! grep -q 'class="eyebrow">Edition #' docker/web/site/app.js && grep -Eq 'grid-template-columns: *repeat\(3, *1fr\)' docker/web/site/style-shared.css && grep -q 'max-width: 880px' docker/web/site/style-shared.css && ! grep -q '640px' docker/web/site/style-shared.css && grep -q 'class="legend"' docker/web/site/app.js && grep -q 'class="made-cols"' docker/web/site/index.html && grep -q 'class="approval"' docker/web/site/index.html && grep -q 'Gato Brain' docker/web/site/index.html && ! grep -q 'class="agent-row"' docker/web/site/index.html && ! grep -q -- '--violet' docker/web/site/style-shared.css && [ "$(grep -oE '#[0-9a-fA-F]{3,6}' docker/web/site/style-shared.css | wc -l)" -eq 0 ] && [ "$(grep -oE '#[0-9a-fA-F]{3,6}' docker/web/site/index.html | wc -l)" -eq 0 ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/drift-check.sh` shows only the known `lab-data-provider` non-zero line (any other drift is surfaced for operator review before deploy).
    - All three fixes' source markers are present (HEAD-01/GRID-01/GRID-02/AGENTS-01 per the gate); `node --check` passes.
    - RHYTHM-01 guard holds: style-shared.css + index.html are hex-free; no `--violet`/`--line-soft` anywhere.
    - No rebuild has run yet (gated on Task 2 approval); the drift result + D-04 + D-12 points are staged for the checkpoint.
  </acceptance_criteria>
  <done>The prod<->main drift check is clean (only the known carry-over), the working branch exists, every Phase-22 source marker is present, and the RHYTHM-01 hex-free guard holds — staged for the operator deploy approval; nothing rebuilt yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Operator-approved scoped web rebuild + live verification of the 3 success criteria + D-12 sign-off + no-regression (worktree-unsafe)</name>
  <what-built>
    Plan 22-01 stripped the baked `— Edition #N | <date>` title suffix at render in `getModeTitle` (both modes) and dropped the duplicate `.eyebrow` line from the reader header (HEAD-01). Plan 22-03 changed the map tier grids to 3-col with 3->2 (<=880px) -> 1 (<=600px) breakpoints and added a maturity legend under the heading mirroring the real 5-seg pill (GRID-01/02). Plan 22-02 rewrote the About agent grid into a numbered 01-04 pipeline + bulleted supporting layer with a violet "human approval" callout, de-duping the approval clause from the intro (AGENTS-01). This checkpoint deploys all three via the scoped `web` rebuild and verifies them on the ACTUAL live render — never inferring a clean result from source bytes (Phase-19 lesson).
  </what-built>
  <how-to-verify>
    DEPLOY (orchestrator-owned, from the MAIN tree — worktree-unsafe; the scoped rebuild cds to the absolute main-tree path, so a worktree executor would build stale code — MEMORY scoped-rebuild-worktree-unsafe):
    1. Present the Task-1 drift result + a `/diff` per work group (HEAD-01 app.js; GRID app.js+style-shared.css; AGENTS index.html+style-shared.css) and the D-04 finding (22-01) + the D-12 copy point (22-02) to the operator. Get explicit approval BEFORE any rebuild (standing v2.2 deploy discipline + MEMORY scoped/approved deploys).
    2. On approval, scoped rebuild ONLY the web service from the main tree: `cd /root/bitcoin_bot/docker && docker compose up -d --build web` — SERVICE key `web`, NOT the `agentpulse-web` container_name (MEMORY: web compose service name); NO `--delete`.
    3. Confirm the served `app.js`/`index.html` over the wire are the substituted versions (the `__SUPABASE_URL__` sed-substitution ran — the SPA loads, not a blank page).

    VERIFY ON THE LIVE SITE (wide ~1440px viewport unless noted; use the `?preview=1` BEFORE the `#` only if a draft/preview-gated view is needed — `/?preview=1#/...`):
    4. HEAD-01 (SC1): open an edition detail route (e.g. `#/edition/30`). The H1 reads ONLY the headline (no `— Edition #N | <date>` suffix); edition number, date, and mode appear EXACTLY ONCE, in the byline below the title (`Edition #N · date · Mode`). Toggle Technical <-> Strategic — both modes' H1s are suffix-free (the strip covers `title` and `title_impact`). No eyebrow line above the title.
    5. GRID-01 (SC2): the Agent Economy `#map` section renders 3 columns on desktop; the three tier sections (SUBSTRATE / BEHAVIOR / FRAME) + labels are intact; the deferred `frame` (regulation-legal) card spans full width. Narrow the viewport: at <=880px the grid collapses to 2 columns, at <=600px to 1 column.
    6. GRID-02 (SC2): a maturity legend sits directly under the "The Agent Economy" heading, showing `Maturity` + a 5-segment sample (1 filled + 4 empty) + `nascent -> established`; the legend's segment scale visibly MATCHES the cards' pills. Spot-check 2-3 blocks: each card's filled-segment count equals its stored `economy_map` maturity (read-only — cross-check against the stored `blocks.maturity` values; no data change).
    7. AGENTS-01 (SC3): the About `#about` section shows a LEFT numbered 01-04 pipeline (Processor/Analyst/Research/Newsletter) and a RIGHT bulleted (dot) supporting layer — NO orphaned single card on its own row. "Nothing publishes without human approval." renders as its own distinct violet callout. The intro prose states the approval line only once (not duplicated). At <=880px the two columns collapse to one.
    8. D-12 copy sign-off: surface the supporting-layer copy for operator review — Gato vs Gato Brain are distinct (Gato Brain NOT silently dropped), and the "eight cooperating services" count (4 pipeline + 4 supporting) is consistent. Operator confirms or adjusts the wording.
    9. No-regression — Phase 20 (WIDTH-01/RHYTHM-01): no dead left gutter; reading copy (an opened edition) holds ~60-70 chars on `--measure`; the map/list tiled sections sit on `--wide`; every surface themes off the violet/off-white token system (the new legend labels + approval callout included).
    10. No-regression — Phase 21 (SCROLL-01/02): the single-scroll landing still scrolls through the four sections in the locked order; the scroll-spy still highlights the active section; editions/blocks still open as deep-linkable detail routes and "<- Back" restores the landing; the maturity pill does NOT overlap the sticky nav.
    11. Scope guard: no Phase 23 excerpt change, no Phase 24 Signals data, no Phase 25 holistic responsive/a11y pass beyond the prescribed 3/2/1 + 2->1 breakpoints shipped here.
  </how-to-verify>
  <resume-signal>Operator types "approved" once the live render shows: (HEAD-01) edition H1 = headline only with one byline in both modes; (GRID-01/02) the 3-col map with correct breakpoints + a matching-scale maturity legend + pill fill == stored maturity; (AGENTS-01) the numbered pipeline + bulleted supporting + violet approval callout with no orphaned card; the D-12 copy signed off; and no Phase 20/21 regression — or describes the specific issue (e.g. a breakpoint tune, a stale title suffix, a copy edit) to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo (main tree) -> live prod render | The scoped `web` rebuild is the only boundary crossed. It is orchestrator-owned from the main tree, scoped to the `web` SERVICE key, NO `--delete`, gated by a prod<->main drift check + operator approval. No new runtime input path, endpoint, query, schema, or write verb ships — the three source plans are frontend-only and read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | Tampering | scoped `web` rebuild / deploy blast radius | mitigate | Deploy is orchestrator-owned from the main tree, scoped to the `web` SERVICE key (NOT `agentpulse-web`), NO `--delete`, with a prod<->main drift check + operator approval BEFORE any rebuild (Task 1 + Task 2 steps 1-2). Prevents the worktree-stale-build + rsync-blast-radius failure modes (MEMORY: scoped/approved deploys, web compose service name, scoped-rebuild-worktree-unsafe). The plan is `autonomous: false`. |
| T-22-04-02 | Spoofing/Repudiation | render-vs-source mismatch | mitigate | Verification reproduces the ACTUAL rendered pages (steps 4-11), never inferring a clean result from stored/source bytes (Phase-19 lesson, MEMORY [[feedback_verify_render_bugs_end_to_end]]); the `__SUPABASE_URL__` substitution is confirmed (step 3) so the live SPA — not a placeholder shell — is what is verified. |
| T-22-04-03 | Information Disclosure | About copy accuracy (D-12) | mitigate | The operator signs off on the supporting-layer copy (step 8) so the architecture is not silently misrepresented (Gato Brain retained; "eight cooperating services" consistent) before the copy ships live. |
| T-22-04-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase — no `package.json`, no new CDN `<script>`, no PyPI/crates dependency; the rebuild recompiles the existing web image only. Package Legitimacy Gate not applicable. |

Net security delta: zero new attack surface. A scoped, drift-checked, operator-approved web rebuild of three frontend-only, read-only source fixes + a live-render verification. ASVS L1, no high-severity threat.
</threat_model>

<verification>
- Pre-deploy: drift check clean (only the known carry-over), all Phase-22 source markers present, hex-free RHYTHM-01 guard holds, `node --check` passes (Task 1 gate).
- Deploy: scoped `docker compose up -d --build web` from the main tree (SERVICE key `web`, NO `--delete`), operator-approved after `/diff` per work group; `__SUPABASE_URL__` substitution confirmed (Task 2 steps 1-3).
- Live verification on the ACTUAL render: HEAD-01 (H1 headline-only + single byline, both modes), GRID-01/02 (3-col + 3/2/1 breakpoints + matching-scale legend + pill fill == stored maturity), AGENTS-01 (numbered pipeline + bulleted supporting + violet callout, no orphaned card), D-12 copy sign-off, and no Phase 20/21 regression (Task 2 steps 4-11).
- Render verification reproduces the rendered pages — it does NOT infer a clean result from source bytes (Phase-19 lesson).
</verification>

<success_criteria>
Phase 22 is deployed and holistically verified on the live render:
- HEAD-01: edition page H1 = headline only; edition/date/mode appear exactly once in the byline (both modes).
- GRID-01/02: the map renders 3-col (3->2 at <=880px -> 1 at <=600px) with a maturity legend matching the cards' scale and pill fill == stored economy_map maturity.
- AGENTS-01: the About section shows the numbered pipeline + bulleted supporting layer with no orphaned card and the violet human-approval callout; the D-12 copy is operator-signed-off.
- No regression: Phase 20 width axes and Phase 21 scroll-spy / two-mode router / sticky-header non-overlap all hold; no scope creep into Phases 23-25.
</success_criteria>

<output>
Create `.planning/phases/22-per-section-visual-fixes/22-04-SUMMARY.md` when done (record the drift result, the deploy, the live-verification of all 3 criteria, the D-12 sign-off, and the no-regression confirmation).
</output>
