---
phase: 23-distinct-newsletter-excerpts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/web/site/app.js
  - docker/web/site/style-shared.css
autonomous: true
requirements: [EXCERPT-01]

must_haves:
  truths:
    - "D-01: the `## Read This, Skip the Rest` H2 is stripped before excerpting (render-only, never mutates stored data; no-op when the header is absent)"
    - "D-02: after the header, the leading recap sentence(s) are skipped and the first remaining 'This week…' thesis sentence is surfaced as the row summary"
    - "D-03: a leading sentence is skipped ONLY if it matches a look-back recap opener; when no opener matches the first sentence is kept (fail-loud no-silent-drop); stacked recaps (two leading look-backs) are both skipped, but at least one sentence is always retained"
    - "D-04: a chosen summary sentence below the ~40-char floor (e.g. ed29 'This week it got specific.') gets the next sentence appended so the row carries real substance"
    - "D-05: markdown links `[text](url)` become `text` (URL dropped) and bare URLs are dropped BEFORE sentence segmentation — the ed30 'stablecoin rollout(https://unchained…' leak is gone; the crude `content.replace(/[#*_\\[\\]\\`>]/g,'').substring(0,150)` strip is removed"
    - "D-06: the row summary is clamped to 2 visual lines via `-webkit-line-clamp:2`; the full extracted text stays in the DOM (no hard character truncation)"
    - "D-07: graceful degradation — when no recap matches, the first cleaned sentence is shown; when extraction yields nothing, the row renders number·title·date with NO summary line, never the legacy 150-char clamp, never fabricated text"
    - "D-08: the summary is mode-aware via getModeContent() (Technical content_markdown vs Strategic content_markdown_impact), flipping with the existing newsletter toggle"
    - "D-09: the list renders as the mockup indexed-row grid — num=edition_number, title=getModeTitle (Phase 22 EDITION_SUFFIX_RE chokepoint reused), date=formatDate(published_at); the whole row is the `#/edition/<n>` deep-link"
    - "D-10: every new CSS rule is token-only (zero hardcoded hex); the mockup `--line-soft` border maps to prod `--line`, the mockup `--violet` hover maps to prod `--accent`"
  artifacts:
    - path: "docker/web/site/app.js"
      provides: "RECAP_OPENER_RE const + cleanExcerptMarkdown / splitSentences / extractDistinctExcerpt pure helpers + rewritten renderList() emitting the indexed-row markup"
      contains: "extractDistinctExcerpt"
    - path: "docker/web/site/style-shared.css"
      provides: ".row / .num / .title / .sum (2-line clamp) / .date / .archive-label token-only grid CSS"
      contains: "-webkit-line-clamp"
  key_links:
    - from: "docker/web/site/app.js renderList()"
      to: "extractDistinctExcerpt(getModeContent(n))"
      via: "mode-aware excerpt pipeline (D-02/D-08)"
      pattern: "extractDistinctExcerpt\\("
    - from: "docker/web/site/app.js renderList()"
      to: "escapeHtml(title) / escapeHtml(sum)"
      via: "every DB-derived string escaped at the innerHTML sink (XSS)"
      pattern: "escapeHtml\\("
    - from: "docker/web/site/style-shared.css .row .sum"
      to: "-webkit-line-clamp:2"
      via: "D-06 visual clamp, full text retained in DOM"
      pattern: "-webkit-line-clamp:\\s*2"
---

<objective>
Replace the newsletter archive list's duplicate-excerpt render path with the
mockup's indexed-row format (number · title · one-line summary · date) and a
strip-at-render excerpt pipeline that skips the boilerplate `## Read This, Skip
the Rest` header + the shared recap intro and surfaces each edition's first
genuinely-distinct "This week…" sentence. Implements all ten CONTEXT decisions
D-01..D-10. Frontend-only; no schema change, no new Supabase query, no
content-pipeline change (the stored-summary path is the deferred EXCERPT-F1).

Purpose: editions 29 and 30 currently show near-identical opening words in the
archive list because the crude 150-char strip leaks the shared boilerplate intro
(and link URLs). This makes consecutive editions visually distinct (EXCERPT-01).

Output: new pure string helpers + a rewritten `renderList()` in `app.js`, and
the net-new token-only `.row` grid CSS (incl. the D-06 2-line clamp) in
`style-shared.css`. Source-only — the scoped web rebuild + live verification is
Plan 02 (orchestrator-owned, worktree-unsafe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-distinct-newsletter-excerpts/23-CONTEXT.md
@.planning/phases/23-distinct-newsletter-excerpts/23-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Excerpt pipeline — recap-detection / sentence-extraction / markdown-cleanup pure helpers (D-01..D-05, D-07)</name>
  <files>docker/web/site/app.js</files>
  <read_first>
    - /root/bitcoin_bot/docker/web/site/app.js (lines 40-128: the `EDITION_SUFFIX_RE` const idiom :52 and the `trimHubBody` :85-96 / `stripLeadingTitleH1` :112-128 defensive-no-op helper templates these new helpers MUST mirror; lines 363-415: current `renderList`/`loadList`; lines 575-588: `getModeContent`/`getModeTitle`)
    - /root/bitcoin_bot/.planning/phases/23-distinct-newsletter-excerpts/23-CONTEXT.md (decisions D-01..D-05, D-07; the observed edition structure: H2 header → recap sentence(s) → "This week…" pivot → lead story)
    - /root/bitcoin_bot/.planning/phases/23-distinct-newsletter-excerpts/23-PATTERNS.md (the render-only/never-over-strip spine; the "No Analog Found" guidance for the net-new link-cleanup + sentence-segmentation idioms)
  </read_first>
  <behavior>
    Fixtures = the REAL opening text of editions 28/29/30 (both modes), embedded
    as string constants in the node harness. Assertions the helpers must satisfy:
    - extractDistinctExcerpt(ED29_TECH) !== extractDistinctExcerpt(ED30_TECH) (SC#1, Technical)
    - extractDistinctExcerpt(ED29_IMPACT) !== extractDistinctExcerpt(ED30_IMPACT) (SC#1, Strategic)
    - extractDistinctExcerpt(ED29_TECH) starts with "This week it got specific" AND its length > 40 (D-04 thin-pivot append fired)
    - extractDistinctExcerpt(ED30_TECH) contains "Block kicked off Cash App" AND does NOT contain "https://" NOR "](" (D-05 link cleanup; the leak is gone)
    - extractDistinctExcerpt(ED28_TECH) starts with "This week" (D-02/D-03 recap "For three editions…" skipped, pivot surfaced)
    - extractDistinctExcerpt(ED29_IMPACT) starts with "This week the gap got more concrete" (D-03 closing-quote-aware split: the recap ending `…"The End of Trust."` is its own sentence and skipped)
    - extractDistinctExcerpt(STACKED) where STACKED = "## Read This, Skip the Rest\n\nLast week we did X. For weeks we have tracked Y. This week Z happened and it matters." → starts with "This week Z happened" (D-03 stacks two recaps, keeps a sentence)
    - extractDistinctExcerpt("## Read This, Skip the Rest\n\nA brand-new opener with no look-back at all here.") === "A brand-new opener with no look-back at all here." (D-03 no recap → keep sentence 1, no silent drop)
    - extractDistinctExcerpt("") === "" AND extractDistinctExcerpt(null) === "" (D-07 empty → caller omits .sum)
    - cleanExcerptMarkdown("ends with [link](https://x.com/y).") === "ends with link." (D-05 link→text)
    - splitSentences('scale: Block kicked off') has length 1 (no split on colon) AND splitSentences('them — Google launched') has length 1 (no split on em-dash) AND splitSentences('A. B') has length 2
  </behavior>
  <action>
    Add a single CONTIGUOUS block of new module-level code immediately after
    `stripLeadingTitleH1` (~app.js:129), wrapped in two sentinel comments
    `// --- excerpt helpers (node-testable) START ---` and
    `// --- excerpt helpers (node-testable) END ---` so the node harness in
    <verify> can extract + evaluate the SHIPPED source offline. Inside the block:

    1. `const RECAP_OPENER_RE` (D-03) — anchored to the sentence START (`^`),
    case-insensitive (`i`), matching the corpus look-back openers: `Last week`
    (covers "Last week", "Last week's", "Last week we"), `Last month`, `For
    weeks`, `For <N|number-word> editions` (digits OR one|two|three|…|ten|several|
    many), and `<N|number-word> editions ago`. Document the covered phrases in a
    contract comment matching the `EDITION_SUFFIX_RE` comment style (:43-52).

    2. `const EXCERPT_MIN_CHARS = 40;` (D-04 length floor; planner-finalized at 40
    — ed29-tech "This week it got specific." (26) is the sole corpus trigger).

    3. `function cleanExcerptMarkdown(md)` (D-05) — pure, falsy-guard returns ''.
    In order on a LOCAL copy: convert `[text](url)` → `text` (drop the URL), drop
    bare `https?://…` URLs, then strip residual inline markers (`#`, `*`, `_`,
    backtick, `>`). MUST NOT contain the old `.substring(0, 150)` truncation and
    MUST NOT use the exact crude class `/[#*_\[\]\`>]/g` (the `[` `]` link
    brackets are consumed by the link conversion step, not a blanket strip).

    4. `function splitSentences(text)` (D-02 helper) — pure, falsy-guard returns
    []. Split on a sentence terminator `[.!?]` OPTIONALLY followed by a closing
    quote/paren/bracket (`"`, `'`, `”`, `’`, `)`, `]`) then whitespace then a
    capital letter or opening quote (use lookbehind/lookahead). It MUST split
    `…"The End of Trust." This week…` into two sentences, MUST NOT split on a
    colon (`scale: Block`) or em-dash (`them — Google`). Trim + drop empty pieces.

    5. `function extractDistinctExcerpt(md)` (D-01/D-02/D-03/D-04/D-07) —
    orchestrator, mirrors the `trimHubBody`/`stripLeadingTitleH1` defensive spine.
    Falsy → return ''. (a) Defensively strip a leading `## Read This, Skip the
    Rest` heading LINE if present (line-based splice like stripLeadingTitleH1;
    no-op if absent, D-01/D-07). (b) cleanExcerptMarkdown the working text (D-05),
    BEFORE segmentation so link-URL periods can't create false sentence breaks.
    (c) splitSentences. (d) While the leading sentence matches RECAP_OPENER_RE AND
    more than one sentence remains, drop it (D-03; never drop the last remaining
    sentence — fail-loud keep ≥1). (e) Take the first surviving sentence; if its
    length < EXCERPT_MIN_CHARS, append the single next surviving sentence with a
    space (D-04; append at most one — the D-06 clamp bounds the combined length).
    (f) Return the trimmed result, '' if nothing survives (D-07).

    Do NOT mutate stored data; operate only on local string copies. Do NOT add a
    DOM dependency to these helpers (they must run under node for <verify>).
  </action>
  <acceptance_criteria>
    - `node --check docker/web/site/app.js` exits 0 (syntax valid).
    - `grep -n "const RECAP_OPENER_RE" docker/web/site/app.js` matches, and the regex source contains both `^` and the `i` flag (`grep -E "RECAP_OPENER_RE\\s*=\\s*/\\^.*/[a-z]*i" docker/web/site/app.js` matches).
    - `grep -c "substring(0, 150)" docker/web/site/app.js` returns 0 (crude truncation removed).
    - `grep -c '\[#\*_\\\[\\\]' docker/web/site/app.js` returns 0 (exact crude strip class removed).
    - Both sentinel comments (`excerpt helpers (node-testable) START` / `END`) are present (`grep -c "node-testable" docker/web/site/app.js` returns 2).
    - The node harness (see <verify>) passes EVERY assertion in <behavior>, exiting 0; in particular extractDistinctExcerpt(ED29_*) !== extractDistinctExcerpt(ED30_*) in BOTH modes, ED30_TECH output has no `https://` and no `](`, and ED29_TECH output length > 40 starting "This week it got specific".
  </acceptance_criteria>
  <verify>
    <automated>node --check docker/web/site/app.js && node /tmp/excerpt_check.mjs</automated>
    Where /tmp/excerpt_check.mjs (executor-authored, NOT shipped): reads
    docker/web/site/app.js, extracts the substring between the two sentinel
    comments, evaluates it (vm/new Function) to obtain cleanExcerptMarkdown /
    splitSentences / extractDistinctExcerpt, embeds the ED28/29/30 fixture strings
    (the real opening text — first ~3 sentences of each edition's content_markdown
    and content_markdown_impact), runs every <behavior> assertion, and process.exit(1)
    on the first failure. Offline + deterministic (no network, no config/.env) so
    the task stays worktree-safe.
  </verify>
  <done>
    The three pure helpers + two consts exist between the sentinels; the crude
    strip is gone; all <behavior> assertions pass under node; ed29/ed30 produce
    distinct text in both modes; the ed30 link leak and the ed29 thin-pivot are
    both handled.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite renderList() to the indexed-row markup (D-06 DOM-side, D-07 conditional summary, D-08 mode-aware, D-09 row grid)</name>
  <files>docker/web/site/app.js</files>
  <read_first>
    - /root/bitcoin_bot/docker/web/site/app.js (lines 363-415: current `renderList`/`loadList` — the empty-state + hero-update :364-374 to preserve verbatim, the :376-388 map/excerpt/`.article-entry` body to replace; lines 554-558 `escapeHtml`, 568-573 `formatDate`, 575-588 `getModeTitle`/`getModeContent`; lines 743-763 `renderHub`→`renderTile` for the whole-`<a>`-is-the-click-target row builder analog; lines 595-607 `renderMaturityPill` precedent for interpolating a numeric value raw)
    - /root/bitcoin_bot/.planning/phases/23-distinct-newsletter-excerpts/23-CONTEXT.md (D-06, D-07, D-08, D-09; the "Latest" featured block is Claude's-Discretion default-OFF — keep uniform rows, do not fight the existing hero)
    - /root/bitcoin_bot/.planning/phases/23-distinct-newsletter-excerpts/23-PATTERNS.md (the `renderTile` row-builder analog + the conditional-fragment composition idiom for the D-04/D-07 cases; the escapeHtml-every-DB-sink / numeric-raw rule)
    - /root/bitcoin_bot/.planning/docs/agentpulse-redesign (1).html (lines 268-301: the `.archive-label` + `.row` markup shape — `<a class="row"><span class="num">…<span><p class="title">…<p class="sum">…</span><span class="date">…`)
  </read_first>
  <behavior>
    - renderList([ed29, ed30]) innerHTML contains two `class="row"` anchors, each `href="#/edition/<edition_number>"`.
    - For ed30 the `.sum` text contains "Block kicked off Cash App" and contains NO "https://" and NO "](" (consumes Task 1's extractDistinctExcerpt).
    - For ed29 vs ed30 the `.sum` text differs (SC#1).
    - When extractDistinctExcerpt returns '' the row has NO `<p class="sum">` element (D-07), and the innerHTML never contains the legacy 150-char fallback.
    - currentMode flip (setMode 'strategic' then renderList) changes `.sum` + `.title` to the *_impact source (D-08) — both flip via the existing getModeContent/getModeTitle.
    - A DB title containing `<img onerror=…>` renders escaped (`&lt;img`), never as live markup (XSS).
  </behavior>
  <action>
    Replace the `data.map(...)` body inside `renderList` (app.js:376-386) only.
    Preserve verbatim the empty-state branch + the `updateHero('AI Agents
    Pulse', 'Latest: Edition #' + latest.edition_number + …)` lines (:364-374)
    and the final `document.getElementById('newsletter-list').innerHTML = html;`
    assignment. Prepend a single static `<p class="archive-label">Archive</p>`
    before the joined rows (mockup label; static text, no DB interpolation).

    For each edition `n`, build one row using the `renderTile` whole-`<a>`-is-the-
    click-target idiom: `<a href="#/edition/' + n.edition_number + '"
    class="row">` wrapping `<span class="num">` (n.edition_number interpolated
    RAW — numeric, precedent renderMaturityPill :604), a nested `<span>`
    containing `<p class="title">' + escapeHtml(getModeTitle(n)) + '</p>'` (D-09,
    reuses the Phase 22 EDITION_SUFFIX_RE strip chokepoint — do NOT regress it)
    and the CONDITIONAL `<p class="sum">' + escapeHtml(sum) + '</p>'` where
    `sum = extractDistinctExcerpt(getModeContent(n))` (D-02/D-08); emit the `.sum`
    `<p>` ONLY when sum is non-empty (D-07 — never the legacy 150-char fallback,
    never fabricate). Close with `<span class="date">' + escapeHtml(formatDate(
    n.published_at)) + '</span>'`. escapeHtml EVERY DB-derived string (title,
    sum, date); edition_number is the only raw interpolation.

    Do NOT touch `loadList()` (:391-415) — no new query, no new field, no new
    status filter (strip-at-render; D-08 reuses the existing rows). Do NOT adopt
    the mockup "Latest" featured block (discretion default: uniform rows; the
    existing hero already shows "Latest: Edition #N").
  </action>
  <acceptance_criteria>
    - `node --check docker/web/site/app.js` exits 0.
    - renderList emits `class="row"` and `href="#/edition/` (`grep -c 'class="row"' docker/web/site/app.js` >= 1) and no longer emits `class="article-entry"` inside the row builder (`grep -c 'class="article-entry"' docker/web/site/app.js` returns 0).
    - The row title is `escapeHtml(getModeTitle(n))` and the summary is `escapeHtml(...extractDistinctExcerpt(getModeContent(n))...)` — both DB strings pass through escapeHtml (`grep -c "escapeHtml" docker/web/site/app.js` count increases vs baseline; the row builder contains `escapeHtml(getModeTitle` and `escapeHtml(` around the sum variable).
    - `loadList` Supabase read is byte-identical: `grep -c "\.in('status', \['published', 'preview'\])" docker/web/site/app.js` returns 2 (unchanged baseline — loadList :403 + the reader route :460, neither touched by this phase) and no new `.eq('status'` was added (the file-wide `.eq('status'` count stays 11, D-17 frozen).
    - A node harness extends /tmp/excerpt_check.mjs (or a sibling) to assert the row-builder behavior in <behavior> using stubbed `escapeHtml`/`formatDate`/`getModeTitle`/`getModeContent` + currentMode; passes, exits 0.
    - The empty-state branch (`No newsletters`) and the `updateHero('AI Agents Pulse', 'Latest: Edition #'…)` line are still present (`grep -c "Latest: Edition #" docker/web/site/app.js` >= 1).
  </acceptance_criteria>
  <verify>
    <automated>node --check docker/web/site/app.js && node /tmp/excerpt_check.mjs</automated>
  </verify>
  <done>
    renderList emits the indexed-row markup (num·title·sum·date) as a single
    deep-link `<a>`; every DB string is escaped; the `.sum` is conditional
    (D-07); mode flips title+sum (D-08); loadList + the empty-state are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Indexed-row token-only CSS — .row grid + 2-line clamp + .archive-label, legacy .article-entry left dormant (D-06, D-09, D-10)</name>
  <files>docker/web/site/style-shared.css</files>
  <read_first>
    - /root/bitcoin_bot/docker/web/site/style-shared.css (lines 136-180: the superseded `.article-entry`/`.section-label`/`.entry-title`/`.entry-preview` family — leave DORMANT, do not delete; lines 1003-1012: the Phase 22 `.made-cols`/`.agent` grid-row analog to port from; lines 217-237: the `.tier-label`/`.legend-label` mono-chrome recipe for `.archive-label`)
    - /root/bitcoin_bot/docker/web/site/style-base.css (lines 10-79: the `:root` token layer — confirm `--line`, `--accent`, `--ink`, `--ink-soft`, `--ink-faint`, `--serif`, `--mono`, `--space-*`, `--radius-dot` all EXIST; `--line-soft` and `--violet` do NOT exist)
    - /root/bitcoin_bot/.planning/phases/23-distinct-newsletter-excerpts/23-CONTEXT.md (D-06, D-09, D-10)
    - /root/bitcoin_bot/.planning/phases/23-distinct-newsletter-excerpts/23-PATTERNS.md (the CSS section + the ⚠ CRITICAL GOTCHA: mockup `--line-soft` → prod `--line`, mockup `--violet` → prod `--accent`; the net-new 2-line clamp four-property idiom)
    - /root/bitcoin_bot/.planning/docs/agentpulse-redesign (1).html (lines 109-128: `.archive-label`/`.row`/`.num`/`.title`/`.sum`/`.date` values; lines 220-236: the `@media (max-width:600px)` row reflow + the `:focus-visible` outline)
  </read_first>
  <action>
    Add a net-new, token-only CSS block in style-shared.css directly AFTER the
    `.entry-preview` rule (~:180), under a header comment citing Phase 23
    EXCERPT-01 / D-06 / D-09 / D-10 and noting the legacy `.article-entry` family
    above is superseded-but-DORMANT (NOT deleted — `.entry-preview` is still
    consumed by the map error state at app.js:635 and the list empty-state).

    Port the mockup `.row` block onto the `.agent`/`.made-cols` analog structure:
    - `.archive-label` — mono 11px/600 uppercase `var(--ink-faint)`, letter-spacing
      .16em, `border-bottom: 1px solid var(--line)`, `padding-bottom: var(--space-sm)`,
      `margin: var(--space-2xl) 0 0` (the `.tier-label`/`.legend-label` recipe).
    - `.row` — `display: grid; grid-template-columns: 56px 1fr auto; gap: 0
      var(--space-lg); align-items: baseline; padding: var(--space-lg) 0;
      border-bottom: 1px solid var(--line);` (⚠ NOT `var(--line-soft)`),
      `text-decoration: none; color: inherit; transition: padding-left .18s;`
    - `.row:last-child { border-bottom: none; }`
    - `.row:hover { padding-left: var(--space-sm); }` and
      `.row:hover .num { color: var(--accent); }` (⚠ NOT `var(--violet)`)
    - `.row .num { font-family: var(--serif); font-size: 16px; color: var(--ink-faint); }`
    - `.row .title { font-family: var(--serif); font-weight: 500; font-size: 19px;
      line-height: 1.3; margin: 0 0 var(--space-xs); color: var(--ink); }`
    - `.row .sum { font-size: 14px; color: var(--ink-soft); margin: 0; line-height:
      1.5; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient:
      vertical; overflow: hidden; }` (D-06 net-new four-property clamp; full text
      stays in the DOM — visual clamp only)
    - `.row .date { font-family: var(--mono); font-size: 12px; color:
      var(--ink-faint); white-space: nowrap; }`
    - `.row:focus-visible { outline: 2px solid var(--accent); outline-offset: 3px;
      border-radius: var(--radius-dot); }` (a11y — keep focus visible, do not
      regress; map mockup `--violet` → `--accent`)
    - Inside `@media (max-width: 600px)`: `.row { grid-template-columns: 40px 1fr; }
      .row .date { grid-column: 2; font-size: 11px; margin-top: var(--space-xs); }`
      (basic mobile reflow; the holistic RESP-01/A11Y-01 sign-off is Phase 25).

    EVERY color value must be a `var(--…)` token — zero hex literals (D-10/RHYTHM-01).
    Use the existing `:root` tokens only; add NO new tokens to style-base.css.
  </action>
  <acceptance_criteria>
    - The `.row .sum` rule contains all four clamp properties: `grep -c "\-webkit-line-clamp: 2" docker/web/site/style-shared.css` returns 1 and `-webkit-box-orient: vertical` + `display: -webkit-box` + `overflow: hidden` are present on the same rule.
    - The `.row` `border-bottom` uses `var(--line)` and the file contains NO `var(--line-soft)` and NO `var(--violet)`: `grep -c "var(--line-soft)\|var(--violet)" docker/web/site/style-shared.css` returns 0.
    - The new `.row`/`.num`/`.title`/`.sum`/`.date`/`.archive-label` block contains zero hex color literals — every color is `var(--…)`: inspect the added block; `grep -nE "#[0-9a-fA-F]{3,6}" ` over the added line range returns nothing (filter out comments with `grep -v '^\s*/\*'`).
    - The legacy rules are still present (dormant, not deleted): `grep -c "^.entry-preview" docker/web/site/style-shared.css` returns 1 and `grep -c "^.article-entry" docker/web/site/style-shared.css` returns 1.
    - `.row:hover .num` resolves to `var(--accent)` and `.row:focus-visible` outline uses `var(--accent)`.
    - A CSS sanity parse passes (no unbalanced braces): `node -e "const c=require('fs').readFileSync('docker/web/site/style-shared.css','utf8'); if((c.split('{').length)!==(c.split('}').length)) process.exit(1)"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('docker/web/site/style-shared.css','utf8'); if(c.split('{').length!==c.split('}').length) process.exit(1); if(/var\(--line-soft\)|var\(--violet\)/.test(c)) process.exit(1); if(!/-webkit-line-clamp:\s*2/.test(c)) process.exit(1);"</automated>
  </verify>
  <done>
    The token-only `.row` grid + 2-line clamp + `.archive-label` exist in
    style-shared.css; the `--line-soft`→`--line` and `--violet`→`--accent`
    mappings are applied; zero hex colors; the legacy `.article-entry` family is
    dormant (kept); braces balanced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase anon REST → browser DOM | Operator-authored `newsletters` rows (title, title_impact, content_markdown, content_markdown_impact) are fetched by the anon key and interpolated into `#newsletter-list` innerHTML. Treated as UNTRUSTED at the render sink. |
| Raw markdown string → extracted plain text | The list excerpt is hand-extracted from the raw markdown (NOT via marked.parse); the cleanup + segmentation operate on untrusted strings before the escape sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering/Elevation (XSS) | renderList() row template (app.js) | mitigate | Every DB-derived interpolation (row title via getModeTitle, summary via extractDistinctExcerpt, date via formatDate) is wrapped in `escapeHtml()` at the innerHTML sink. `edition_number` is numeric → interpolated raw (precedent renderMaturityPill :604). Acceptance asserts escapeHtml wraps title+sum; a `<img onerror>` title fixture renders escaped. |
| T-23-02 | Tampering (markup re-introduction) | cleanExcerptMarkdown / extractDistinctExcerpt ordering | mitigate | D-05 cleanup converts links→text + strips markers producing PLAIN TEXT, but escaping is performed unconditionally at the renderList sink AFTER extraction — cleanup output is still treated as untrusted text, so even if a residual `<`/`>` survives cleanup it is HTML-entity-encoded by escapeHtml. The list path never calls marked.parse. |
| T-23-03 | Information disclosure (silent content drop) | recap-skip loop (D-03/D-07) | mitigate | Fail-loud spine: skip a leading sentence ONLY on a RECAP_OPENER_RE match; never drop the last remaining sentence; no recap match → keep sentence 1; empty → omit the `.sum` line, never fabricate, never the legacy 150-char clamp. Asserted by the no-recap + empty + stacked-recap fixtures. |
| T-23-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No package-manager install tasks in this phase — vanilla-JS SPA, no build step, marked.js already loaded via existing CDN tag (untouched). No `__SUPABASE_*__` placeholder edits. N/A. |
</threat_model>

<verification>
- `node --check docker/web/site/app.js` exits 0 after both app.js tasks.
- `node /tmp/excerpt_check.mjs` passes every behavior assertion (offline, embedded ed28/29/30 fixtures) — extractDistinctExcerpt(ed29) !== extractDistinctExcerpt(ed30) in BOTH modes; ed30 link leak gone; ed29 thin-pivot appended.
- The crude `substring(0, 150)` strip is removed (grep returns 0).
- style-shared.css contains the 2-line clamp, uses `var(--line)`/`var(--accent)` (never `--line-soft`/`--violet`), zero hex in the new block, braces balanced.
- loadList Supabase read byte-identical (no new query / status filter); `__SUPABASE_*__` placeholders intact.
</verification>

<success_criteria>
- All ten decisions D-01..D-10 are implemented and asserted (recap skip, this-week
  pivot, fail-loud no-over-strip, thin-pivot append, link cleanup, 2-line clamp,
  graceful degradation, mode-aware, indexed-row grid, token-only CSS).
- Offline node harness proves SC#1 at the unit level (ed29 ≠ ed30, both modes).
- Source-only — NO `docker compose` build/deploy run here (Plan 02 owns the
  scoped rebuild + live verification). This plan is worktree-safe (no network,
  no config/.env dependency).
</success_criteria>

<output>
Create `.planning/phases/23-distinct-newsletter-excerpts/23-01-SUMMARY.md` when done.
</output>
