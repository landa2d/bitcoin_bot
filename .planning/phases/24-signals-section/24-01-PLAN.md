---
phase: 24-signals-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/044_signals_anon_view.sql
autonomous: true
requirements: [SIGNAL-04]
must_haves:
  truths:
    - "The anon key can SELECT tier-1 source links through public.signals_feed (D-01)"
    - "The anon key cannot reach body, metadata, score, author, comment_count, or tags through public.signals_feed (the column ceiling lives in the view, not the frontend select — D-01)"
    - "The base source_posts table stays anon-blocked: RLS enabled, no anon policy, untouched by this migration (D-01)"
  artifacts:
    - path: "supabase/migrations/044_signals_anon_view.sql"
      provides: "Security-definer view public.signals_feed over tier-1 source_posts + anon GRANT SELECT"
      contains: "CREATE OR REPLACE VIEW public.signals_feed"
  key_links:
    - from: "public.signals_feed"
      to: "source_posts"
      via: "SELECT of exactly 5 whitelisted columns WHERE source_tier = 1 AND source_url IS NOT NULL"
      pattern: "source_tier = 1"
    - from: "anon"
      to: "public.signals_feed"
      via: "GRANT SELECT"
      pattern: "GRANT SELECT ON public.signals_feed TO anon"
---

<objective>
Author the milestone's ONLY Supabase migration (`044_signals_anon_view.sql`): a **security-definer** view `public.signals_feed` exposing ONLY the 5 whitelisted columns of tier-1, linkable `source_posts`, plus `GRANT SELECT ... TO anon`. This is the structural anon read path that SIGNAL-04 requires — the column/row ceiling lives in the view, NOT the frontend (D-01: "RLS can't hide columns"). The base `source_posts` table is NOT touched; it stays RLS-enabled with no anon policy.

Purpose: Open a narrow, read-only, tier-1-scoped public read path so the `#signals` feed can render real data, while `body` (copyright/leak), `metadata` (internal extraction JSONB), `score`, `author`, `comment_count`, and `tags` remain unreachable by the anon key.

Output: `supabase/migrations/044_signals_anon_view.sql` (source only — the LIVE apply is orchestrator-owned and lands in Plan 24-03).

Scope note: This plan AUTHORS the .sql file only (worktree-safe, source-only — no docker, no network, no migration apply). The worktree-unsafe live apply via the Supabase MCP `apply_migration` tool is Plan 24-03 (orchestrator/operator-owned).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-signals-section/24-CONTEXT.md
@.planning/phases/24-signals-section/24-PATTERNS.md

# Migration precedents (read before authoring)
@supabase/migrations/004_core_tables.sql
@supabase/migrations/006_rls_policies.sql
@supabase/migrations/001_initial_schema.sql
@supabase/migrations/033_economy_map_schema.sql
@supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 044 — security-definer signals_feed view + anon grant</name>
  <read_first>
    - supabase/migrations/004_core_tables.sql (lines 9-24) — confirm `source_posts` has columns `id, source, source_url, source_tier, title, body, ..., metadata, scraped_at`; tier-1 = `source_tier = 1`.
    - supabase/migrations/006_rls_policies.sql (line ~21) — `source_posts` RLS is ENABLED with no anon policy (the current full anon block this migration narrowly lifts via a view).
    - supabase/migrations/001_initial_schema.sql (lines 153-162) — the only in-repo `CREATE VIEW` idiom. ⚠ It uses `WITH (security_invoker = on)` — do NOT copy that; see the inversion note below.
    - supabase/migrations/033_economy_map_schema.sql (lines 378-386) — the explicit `GRANT SELECT ... TO anon` idiom for a freshly-created object.
    - supabase/migrations/043_economy_map_hub_and_negotiation_blocks.sql (lines 1-28) — the migration header style to match (numbered title + multi-line rationale block + idempotency note + the D-NN decisions landed).
    - supabase/migrations/028_newsletter_preview.sql (lines 1-12) — the project's "narrow anon read" idiom (DROP-then-create, anon grantee).
    - .planning/phases/24-signals-section/24-CONTEXT.md (D-01, D-02, D-04) and 24-PATTERNS.md (§migration 044) — the authoritative contract.
  </read_first>
  <action>
    Create `supabase/migrations/044_signals_anon_view.sql`.

    Header: match the 043 convention — a `-- Migration 044: <one-line purpose>` line, then a multi-line `--` rationale block stating: (1) it creates `public.signals_feed` exposing ONLY `id, title, source_url, source, scraped_at` for tier-1 linkable rows and GRANTs SELECT to anon (D-01); (2) the base `source_posts` table is NOT touched — RLS stays on, no anon policy, so `body`/`metadata`/`score`/`author`/`comment_count`/`tags` stay anon-unreachable (D-01); (3) the security-definer rationale (below); (4) the row ceiling `source_tier = 1` (D-01) AND `source_url IS NOT NULL` (D-02), newest-first via `scraped_at DESC, id DESC` (D-04 + stable tie-break), backed by `idx_source_posts_tier_scraped` (migration 005 — no new index); (5) idempotency via `CREATE OR REPLACE VIEW` + `GRANT` (replay-safe). When you write the "do not use security-invoker" line, phrase it WITHOUT the literal token `security_invoker` in the comment so the comment cannot self-trip a grep gate (e.g. "this is a security-DEFINER view, the Postgres default — do NOT add an invoker-rights clause").

    View body: `CREATE OR REPLACE VIEW public.signals_feed AS SELECT id, title, source_url, source, scraped_at FROM source_posts WHERE source_tier = 1 AND source_url IS NOT NULL ORDER BY scraped_at DESC, id DESC;` — exactly these 5 columns, no more.

    ⚠ LOAD-BEARING (the inversion landmine, 24-PATTERNS): do NOT add `WITH (security_invoker = on)`. A plain `CREATE OR REPLACE VIEW` is security-DEFINER by default (runs as the view owner = the migration role `postgres` = the table owner, which bypasses `source_posts` RLS and returns the whitelisted set). An invoker-rights view would run as anon, hit the RLS-with-no-anon-policy base table, and return ZERO rows forever — a silent empty feed, which is the exact fail-loud violation this phase guards against. Security-definer here is correct AND must NOT expose more than the 5 whitelisted columns / tier-1 rows (the WHERE + column list ARE the boundary).

    Grant: `GRANT SELECT ON public.signals_feed TO anon;` — explicit, do not rely on public-schema default privileges for a freshly-created object (033 precedent). Do NOT emit any policy, grant, or ALTER against the base `source_posts` table.
  </action>
  <acceptance_criteria>
    - File exists: `test -f supabase/migrations/044_signals_anon_view.sql`.
    - View present: `grep -q "CREATE OR REPLACE VIEW public.signals_feed" supabase/migrations/044_signals_anon_view.sql`.
    - Exactly the 5 columns in the SELECT (comment-filtered to avoid prose self-trip): `grep -v '^[[:space:]]*--' supabase/migrations/044_signals_anon_view.sql | grep -iE "select[[:space:]]+id,[[:space:]]*title,[[:space:]]*source_url,[[:space:]]*source,[[:space:]]*scraped_at"` matches.
    - Row ceiling present: SQL body matches `source_tier = 1` AND `source_url IS NOT NULL` — `grep -v '^[[:space:]]*--' supabase/migrations/044_signals_anon_view.sql | grep -qiE "source_tier[[:space:]]*=[[:space:]]*1"` and `... | grep -qi "source_url IS NOT NULL"`.
    - Newest-first ordering: `grep -v '^[[:space:]]*--' ... | grep -qi "ORDER BY scraped_at DESC"`.
    - NO sensitive column leaks (comment-filtered SQL body only): `grep -v '^[[:space:]]*--' supabase/migrations/044_signals_anon_view.sql | grep -ciwE "(body|metadata|score|author|comment_count|tags)"` returns `0`.
    - NO invoker-rights clause in the SQL body: `grep -v '^[[:space:]]*--' supabase/migrations/044_signals_anon_view.sql | grep -ci "security_invoker"` returns `0`.
    - Anon grant present: `grep -q "GRANT SELECT ON public.signals_feed TO anon" supabase/migrations/044_signals_anon_view.sql`.
    - Base table untouched: `grep -v '^[[:space:]]*--' supabase/migrations/044_signals_anon_view.sql | grep -ciE "(ALTER TABLE source_posts|CREATE POLICY|ON source_posts)"` returns `0`.
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/migrations/044_signals_anon_view.sql && grep -q "CREATE OR REPLACE VIEW public.signals_feed" supabase/migrations/044_signals_anon_view.sql && grep -q "GRANT SELECT ON public.signals_feed TO anon" supabase/migrations/044_signals_anon_view.sql && [ "$(grep -v '^[[:space:]]*--' supabase/migrations/044_signals_anon_view.sql | grep -ci 'security_invoker')" = "0" ] && [ "$(grep -v '^[[:space:]]*--' supabase/migrations/044_signals_anon_view.sql | grep -ciwE '(body|metadata|score|author|comment_count|tags)')" = "0" ] && echo PASS</automated>
  </verify>
  <done>
    `044_signals_anon_view.sql` exists, defines a security-definer `public.signals_feed` over exactly the 5 whitelisted columns of tier-1 linkable `source_posts` newest-first, grants SELECT to anon, leaves the base table untouched, and has zero sensitive-column / invoker-clause leaks in the SQL body. The live apply is Plan 24-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (public internet) → Postgres via PostgREST | An untrusted, unauthenticated reader can issue arbitrary `select`/filter against any object the `anon` role is granted. The view body is the only thing standing between anon and the full `source_posts` table. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | `public.signals_feed` column set | mitigate | View SELECTs ONLY `id, title, source_url, source, scraped_at`; `body`/`metadata`/`score`/`author`/`comment_count`/`tags` are never in the view body. Column ceiling lives in the view (D-01), verified by the comment-filtered no-sensitive-column gate. |
| T-24-02 | Information Disclosure | row scope (`source_tier`) | mitigate | View `WHERE source_tier = 1` restricts rows to tier-1 authority sources; tier-2/3 rows never exposed (D-01). |
| T-24-03 | Elevation of Privilege / Information Disclosure | view execution rights (the inversion landmine) | mitigate | View is security-DEFINER (Postgres default; NO `security_invoker = on`). An invoker view would run as anon, hit base RLS, and return 0 rows (silent empty feed = fail-loud violation). Definer scope is bounded by the column list + WHERE, NOT by RLS. |
| T-24-04 | Information Disclosure | base `source_posts` table | accept (structurally enforced) | Migration 044 does NOT touch `source_posts`: RLS stays ENABLED with no anon policy, so anon reading the base table returns no rows. `body`/`metadata` remain unreachable via the table. Gate asserts zero `ALTER TABLE source_posts`/`CREATE POLICY`/`ON source_posts` in the migration body. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase — no new dependencies introduced. |
</threat_model>

<verification>
- The SQL file passes every acceptance gate above (view, 5-column ceiling, tier-1 + non-null filter, newest-first order, anon grant, zero sensitive-column / invoker / base-table-mutation leaks).
- Migration number is `044` (next after the in-repo high-water mark `043`).
- HIGH-severity over-exposure threats (T-24-01/02/03) are all dispositioned `mitigate` and structurally encoded in the view body — no HIGH threat is left unmitigated.
</verification>

<success_criteria>
- `supabase/migrations/044_signals_anon_view.sql` defines a security-definer `public.signals_feed` exposing exactly `id, title, source_url, source, scraped_at` for `source_tier = 1 AND source_url IS NOT NULL`, newest-first, granted to anon.
- The base `source_posts` table is not modified anywhere in the migration.
- The file is ready for the orchestrator-owned live apply in Plan 24-03.
</success_criteria>

<output>
Create `.planning/phases/24-signals-section/24-01-SUMMARY.md` when done.
</output>
