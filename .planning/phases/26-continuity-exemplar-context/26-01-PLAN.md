---
phase: 26-continuity-exemplar-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/newsletter/newsletter_poller.py
  - docker/newsletter/block_pipeline.py
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03, CTX-04, CTX-05]

must_haves:
  truths:
    - "load_edition_context returns the last 3 published editions with {edition_number, title, primary_theme, opening_excerpt, weeks_ago} via .eq('status','published') (CTX-01, D-11)"
    - "The loader returns operator-written-only exemplar paragraphs (>=40 words, non-header/non-list), capped at exemplar_paras (CTX-02, D-01/D-05/D-06)"
    - "An empty corpus returns {previous_editions:[], exemplars:[], empty:true}, logs a WARNING, and generation still completes (CTX-03, D-16)"
    - "An empty operator-written pool returns a distinguishable 'not scored' exemplar marker, never a silent score:0 and never a fallback to any-published (D-02/D-03)"
    - "Both writer paths and the prepass receive narrative_context via input_data.setdefault; an upstream-provided narrative_context wins (CTX-04, D-14)"
    - "Phase E returns a real voice score with >=1 observation when exemplars are present, and a distinguishable 'not scored' result when absent (CTX-05, D-04)"
  artifacts:
    - path: "docker/newsletter/newsletter_poller.py"
      provides: "load_edition_context loader + narrative_context/avoided_themes injection + exemplars pass-through at both generate_from_blocks call sites"
      contains: "def load_edition_context("
    - path: "docker/newsletter/block_pipeline.py"
      provides: "Phase E 'not scored' resurrection — empty-exemplar branches no longer return score:0"
      contains: "not_scored"
  key_links:
    - from: "process_task (newsletter_poller.py ~:2034)"
      to: "load_edition_context"
      via: "input_data.setdefault('narrative_context', ctx)"
      pattern: "setdefault\\('narrative_context'"
    - from: "generate_from_blocks call sites (:2078, :2269)"
      to: "narrative_context['exemplars']"
      via: "exemplars= kwarg"
      pattern: "exemplars="
    - from: "loader query"
      to: "newsletters table"
      via: ".eq('status','published')"
      pattern: "eq\\('status', 'published'\\)"
---

<objective>
Build the additive continuity + exemplar machinery in the newsletter service. Add ONE loader `load_edition_context(supabase, limit=3, exemplar_paras=8)`, inject its output into `process_task` via `setdefault`, feed avoided-angles, pass the already-declared `exemplars=` kwarg at BOTH `generate_from_blocks` call sites, and resurrect Phase E's dead empty-exemplar branch (replace the silent `score:0` sentinel with a distinguishable "not scored" result).

Purpose: The continuity machinery is already wired into consumers but starved of data. This plan feeds it — the hard dependency of the downstream Layer-2 judge (Phase 29) and the lowest-risk start of v2.3. Implements locked decisions D-01..D-11, D-14, D-19 and closes the CTX-05 silent-zero gap (D-04).
Output: A working loader + wiring in `newsletter_poller.py` and a resurrected Phase E in `block_pipeline.py`. No new LLM call surface, no schema changes, no new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-continuity-exemplar-context/26-CONTEXT.md
@.planning/phases/26-continuity-exemplar-context/26-PATTERNS.md
@docs/audit/specs/07_continuity_and_exemplars.md

# Source surfaces this plan modifies / mirrors
@docker/newsletter/newsletter_poller.py
@docker/newsletter/block_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the load_edition_context loader</name>
  <files>docker/newsletter/newsletter_poller.py</files>
  <read_first>
    - docker/newsletter/newsletter_poller.py lines 1-20 (imports: `re` :12, `datetime, timezone` :14, module `logger` :10) and 1145-1182 (the single-pass continuity consumer the loader output must satisfy) and 2031-2046 (the injection region)
    - docker/processor/agentpulse_processor.py:5577-5631 (the EXACT behavioral twin — copy this query + previous_editions build, then apply the D-07/D-09/D-10/D-11 deltas)
    - docker/newsletter/block_selection.py:62-69 (the in-service `.table().select().eq().order().limit().execute()` convention + `result.data or []` guard)
    - docker/newsletter/verification.py:90-104 (the `_SECTION_HEADER` / `_BOLD_HEADER` regex idiom and the `len(x.split())` word-count idiom for the >=40-word/non-header/non-list exemplar filter)
    - .planning/phases/26-continuity-exemplar-context/26-CONTEXT.md (locked decisions D-01..D-11)
    - .planning/phases/26-continuity-exemplar-context/26-PATTERNS.md (the loader pattern assignment + the deltas table)
  </read_first>
  <action>
Add a module-level function `load_edition_context(supabase, limit=3, exemplar_paras=8)` to newsletter_poller.py that returns a dict with top-level keys `previous_editions` (list), `exemplars` (list[str]), `empty` (bool), plus a distinguishable exemplar-pool marker (Claude's Discretion per CONTEXT: e.g. a top-level `exemplars_status` of `'scored'`/`'not_scored'` or an `exemplars=None` sentinel — it MUST be unambiguously distinct from BOTH `exemplars:[]`-on-empty-corpus AND a real `score:0`).

Query (CTX-01, D-11): one published-set read — `supabase.table('newsletters').select('edition_number, title, title_impact, content_markdown, content_markdown_impact, data_snapshot, published_at').eq('status', 'published').order('edition_number', desc=True).limit(N)` where N is a window sized to surface the 2-3 most-recent operator-written editions (use the twin's window of 8). Plain `.eq()` ONLY — never `.in_()` (the silent-failure bug; anti-pattern at :1792). Guard with `recent.data or []`.

Build `previous_editions` from the first `limit` (3) rows — ALL published, not just operator-written (D-11) — reversed oldest-first, each `{edition_number, title, primary_theme, opening_excerpt, weeks_ago}` with these deltas:
  - `title` ← `title_impact or title or ''` (twin behavior).
  - `primary_theme` ← `(data_snapshot or {}).get('lead_theme')` when present, else `None` (D-07). NEVER read `data_snapshot.primary_theme` (always null in the live DB); NEVER derive from the title.
  - `opening_excerpt` ← first ~300 chars of `content_markdown` AFTER stripping a leading `## Read This, Skip the Rest` header line (published bodies have no H1; the section label is not prose — D-10).
  - `weeks_ago` ← `round((now - published_at).days / 7)` parsing the `published_at` timestamptz (tz-aware `datetime.now(timezone.utc)`; handle a trailing `Z`). OMIT the key entirely when `published_at` is null — do NOT fall back to the edition-number gap, which reintroduces cadence error on null-timestamp rows (D-09).

Build `exemplars` (CTX-02): Python-filter the fetched rows to operator-written editions only — `(data_snapshot or {}).get('operator_written') == 'true'` (string in the live DB; this excludes edition 29 / `pipeline_version='block_v1'` — D-01, the anti-tic guard). From the 2 most-recent such editions (expand to a 3rd ONLY to reach the cap — D-06), split each edition's `content_markdown` into paragraphs (blank-line split `re.split(r'\n\s*\n', md)` — Claude's Discretion), keep paragraphs that are >=40 words (`len(para.split()) >= 40`) AND not a header (reuse the `_SECTION_HEADER`/`_BOLD_HEADER` regex idiom) AND not a list item (line begins with `-`, `*`, `+`, or a `\d.` marker), selected in document order, front-loaded (favors the "Read This, Skip the Rest" essay that models the cross-edition bridge — D-05), capped at `exemplar_paras` (=8, D-06).

Degrade states (fail-loud-but-not-fatal — the milestone spine):
  - Zero published editions (CTX-03, D-16): `logger.warning("continuity context empty")` (this exact string — spec 07 §1, used by the live grep and the test) and return the explicit `{'previous_editions': [], 'exemplars': [], 'empty': True}`. Never a bare `{}`.
  - Published editions exist but ZERO operator-written (D-02/D-03): `previous_editions` is still populated (`empty` is False); return the distinguishable "not scored" exemplar marker (NOT a bare `exemplars:[]`, NOT a fallback to any-published) + a `logger.warning(...)`.
  - Success: `logger.info(f"Narrative context: {len(previous_editions)} edition(s), {len(exemplars)} exemplar(s)")`.
Wrap the whole body in `try/except Exception as e:` that logs `logger.warning(f"... (non-critical): {e}")` and returns the explicit empty marker — NEVER raise into the generation path.

The loader takes `supabase` as an explicit parameter (not the module global) so the degrade paths are fixture-testable without a live DB (Plan 02 depends on this signature).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('docker/newsletter/newsletter_poller.py').read())" && test "$(grep -c 'def load_edition_context(' docker/newsletter/newsletter_poller.py)" = "1" && test "$(grep -c '\.in_(' docker/newsletter/newsletter_poller.py)" = "1" && test "$(grep -c 'continuity context empty' docker/newsletter/newsletter_poller.py)" = "1" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - newsletter_poller.py contains `def load_edition_context(supabase, limit=3, exemplar_paras=8)`.
    - The loader query uses `.eq('status', 'published')`; the file's TOTAL `.in_(` count is unchanged at exactly 1 (the pre-existing :1792 bug, untouched — no new `.in_()`).
    - The returned dict always carries keys `previous_editions`, `exemplars`, `empty`, plus a distinguishable exemplar-pool "not scored" marker distinct from both `exemplars:[]`-empty-corpus and a real `score:0`.
    - `primary_theme` is read from `data_snapshot.lead_theme` (grep shows `lead_theme`); `weeks_ago` is omitted for rows with null `published_at`.
    - The empty-corpus path logs the literal `"continuity context empty"` at WARNING and returns `{'previous_editions': [], 'exemplars': [], 'empty': True}`.
    - `python3 -c "import ast; ast.parse(...)"` exits 0.
  </acceptance_criteria>
  <done>load_edition_context exists, is syntactically valid, queries `.eq('status','published')`-only (no new `.in_()`), returns the three-state contract (empty corpus / not-scored operator pool / scored), reads theme from data_snapshot.lead_theme, omits weeks_ago on null published_at, and exemplars are operator-written-only >=40-word non-header/non-list paragraphs capped at exemplar_paras.</done>
</task>

<task type="auto">
  <name>Task 2: Inject narrative_context + avoided-themes feed + omit null Theme in the continuity block</name>
  <files>docker/newsletter/newsletter_poller.py</files>
  <read_first>
    - docker/newsletter/newsletter_poller.py:2031-2046 (the budget-injection analog at :2031-2034 and the `if _use_block_pipeline:` branch boundary at :2046 — placement target)
    - docker/newsletter/newsletter_poller.py:1148-1182 (the single-pass continuity consumer; the D-08 `— Theme: {... '?'}` site at :1158)
    - docker/newsletter/newsletter_poller.py:915-931 and 960-970 (the `newsletter_prepass_tracking` insert columns + the `editorial_prepass` read of `input_data.get('avoided_themes', [])` at :970)
    - docker/newsletter/block_pipeline.py:470-516 (the block prepass consuming narrative_context + avoided_themes — confirms the injected keys are read)
    - .planning/phases/26-continuity-exemplar-context/26-CONTEXT.md (D-08, D-14, injection decision)
    - .planning/phases/26-continuity-exemplar-context/26-PATTERNS.md (injection point + avoided-themes feed sections)
  </read_first>
  <action>
In `process_task`, immediately AFTER budget injection (~:2034) and BEFORE the `if _use_block_pipeline:` branch (~:2046):
  - Call `ctx = load_edition_context(supabase)` and `input_data.setdefault('narrative_context', ctx)` (CTX-04). `setdefault` ensures an upstream-provided narrative_context still wins. Both writer paths read `input_data.get('narrative_context')`, so this single injection covers the single-pass writer (:1149), the A/B prepass (:2253), and the block prepass — no consumer changes needed. `supabase` is the module-level global.
  - Avoided-angles feed (D-14): query the last 3 prepass angles — `supabase.table('newsletter_prepass_tracking').select('chosen_angle').order('created_at', desc=True).limit(3).execute()` (plain ordered select; never `.in_()`), build a list of the non-empty `chosen_angle` strings, and `input_data.setdefault('avoided_themes', [...])`. Both prepass consumers already read `input_data.get('avoided_themes', [])` (:970 and block_pipeline.py:473) — currently unfed. Wrap this query in `try/except` → `logger.warning(f"... (non-critical): {e}")` defaulting to `[]` (fail-loud-but-not-fatal).

D-08 consumer fix at ~:1158: in the single-pass continuity block, change the line that currently renders `— Theme: {ed.get('primary_theme', '?')}` so the "— Theme: ..." segment is OMITTED entirely when `primary_theme` is falsy/None, so older/null-theme editions render a clean edition line instead of `Theme: ?`. Keep `opening_excerpt` carrying the edition (D-07 intent: rely on the excerpt when theme is null).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('docker/newsletter/newsletter_poller.py').read())" && test "$(grep -c "setdefault('narrative_context'" docker/newsletter/newsletter_poller.py)" = "1" && test "$(grep -c "setdefault('avoided_themes'" docker/newsletter/newsletter_poller.py)" = "1" && test "$(grep -c '\.in_(' docker/newsletter/newsletter_poller.py)" = "1" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `process_task` calls `load_edition_context(supabase)` and `input_data.setdefault('narrative_context', ctx)` after budget injection and before the `if _use_block_pipeline:` branch.
    - `input_data.setdefault('avoided_themes', ...)` is fed from a `newsletter_prepass_tracking.chosen_angle` ordered `.limit(3)` select (no `.in_()`).
    - The single-pass continuity block at ~:1158 omits the "Theme:" segment when primary_theme is null (no `Theme: ?` rendered for null-theme editions).
    - ast.parse exits 0; the file's total `.in_(` count is still exactly 1.
  </acceptance_criteria>
  <done>narrative_context and avoided_themes are injected via setdefault before the generation branch; the D-08 Theme-omit is applied at the single-pass consumer; no new `.in_()`; file is syntactically valid.</done>
</task>

<task type="auto">
  <name>Task 3: Pass exemplars at both call sites and resurrect Phase E "not scored"</name>
  <files>docker/newsletter/newsletter_poller.py, docker/newsletter/block_pipeline.py</files>
  <read_first>
    - docker/newsletter/newsletter_poller.py:2078-2085 (call site 1 — primary block path) and :2269-2276 (call site 2 — the A/B path that runs live today)
    - docker/newsletter/block_pipeline.py:404-432 (phase_e_voice_check: the empty-exemplar guard at :410-411 AND the genuine-error branch at :432 that must be LEFT untouched) and :673-685 (generate_from_blocks Phase E default at :674 and the `if exemplars:` gate)
    - docker/newsletter/block_pipeline.py:563-571 (generate_from_blocks signature — `exemplars: list[str] | None = None` already declared, never passed)
    - docker/newsletter/newsletter_poller.py:2308-2335 (the A/B row insert where data_snapshot.voice_score is wired at :2329)
    - .planning/phases/26-continuity-exemplar-context/26-CONTEXT.md (D-02/D-04 not-scored, D-19 two call sites)
    - .planning/phases/26-continuity-exemplar-context/26-PATTERNS.md (exemplars pass-through + Phase E resurrection sections)
  </read_first>
  <action>
In newsletter_poller.py, add `exemplars=(input_data.get('narrative_context') or {}).get('exemplars'),` to BOTH `generate_from_blocks(...)` calls — `:2078` (primary block path, runs when `block_pipeline.enabled=true`) AND `:2269` (the A/B comparison path that runs on every live generation today, `enabled:false`/`ab_comparison:true` — D-19). Mirror the existing `model_voice=` kwarg style. Pass through whatever the loader's "not scored" marker is so Phase E can distinguish the three states. The :2269 site is what makes the Plan 03 live Phase E verification work without flipping `enabled`.

In block_pipeline.py, resurrect Phase E (CTX-05, D-04 — closes the silent-zero gap):
  - Replace the empty-exemplar guard return at :410-411 (`{"score": 0, "observations": ["No exemplars provided — voice check skipped"]}`) AND the default before the `if exemplars:` gate at :674 (`{"score": 0, "observations": ["Skipped — no exemplars"]}`) with an unambiguous "not scored" result — Claude's Discretion, e.g. `{"score": None, "status": "not_scored", "observations": ["No operator exemplars available — voice not scored"]}` — that is distinct from a real `score:0`.
  - LEAVE the genuine-error branch at :432 (`{"score": 0, "observations": [f"Voice check failed: {e}"]}`) UNTOUCHED — that is a real error, distinct from "no exemplars" (D-04).
With the 7 operator-written editions present today, the `if exemplars:` branch fires and Phase E returns a real DeepSeek voice score (`model_voice` defaults to `deepseek-chat`, independent of the Phase-29 Sonnet judge); that score lands in the A/B row's `data_snapshot.voice_score` already wired at :2329.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('docker/newsletter/block_pipeline.py').read())" && python3 -c "import ast; ast.parse(open('docker/newsletter/newsletter_poller.py').read())" && test "$(grep -c 'exemplars=' docker/newsletter/newsletter_poller.py)" -ge "2" && test "$(grep -c '"score": 0' docker/newsletter/block_pipeline.py)" = "1" && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Both `generate_from_blocks` calls (:2078 AND :2269) pass `exemplars=` sourced from `input_data['narrative_context']['exemplars']` (`grep -c 'exemplars='` in newsletter_poller.py >= 2).
    - block_pipeline.py `"score": 0` occurrences drop from 3 to exactly 1 (only the :432 `Voice check failed` error branch remains); the empty-exemplar guard (:411) and the default (:674) now return a distinguishable "not scored" result whose `score` is not 0.
    - Both files `python3 -c "import ast; ast.parse(...)"` exit 0.
  </acceptance_criteria>
  <done>exemplars are passed at both call sites; Phase E's two empty-exemplar branches return a distinguishable "not scored" verdict (not score:0) while the genuine-error branch is untouched; both files are syntactically valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published `newsletters` rows -> LLM prompt context | Prior-edition `opening_excerpt` + exemplar paragraphs are injected into the writer/judge prompts; provenance must be controlled |
| `data_snapshot.operator_written` flag -> exemplar pool gate | Only operator-authored prose may anchor the voice — pipeline output must never re-anchor the judge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information disclosure / Tampering | load_edition_context exemplar selection | mitigate | Exemplar pool restricted to `data_snapshot.operator_written == 'true'`; edition 29 (`block_v1`) and all pipeline output excluded so the eval never re-learns the pipeline's tics (D-01) |
| T-26-02 | Information disclosure | load_edition_context + avoided-themes query | mitigate | `.eq('status','published')` only; held/unpublished rows are never read into prompt context; never `.in_()` (silent-failure bug) |
| T-26-03 | Spoofing (silent zero) | empty operator pool / empty corpus | mitigate | Distinguishable "not scored" marker + explicit `{empty:true}` + WARNING; never a silent `score:0` and never an any-published substitution (D-02/D-03, CTX-03) |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase — the loader reuses already-imported `re`/`datetime`/`logging` and the module-global `supabase` |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(...)"` passes for both modified files (syntax-check before any rebuild — project rule).
- `grep -c 'def load_edition_context(' docker/newsletter/newsletter_poller.py` == 1.
- `grep -c '\.in_(' docker/newsletter/newsletter_poller.py` == 1 (no new `.in_()` introduced anywhere in this plan).
- `grep -c '"score": 0' docker/newsletter/block_pipeline.py` == 1 (only the genuine-error branch remains).
- `grep -c 'exemplars=' docker/newsletter/newsletter_poller.py` >= 2 (both call sites).
- Plan 02's pytest suite (Wave 2) is the behavioral proof; Plan 03 (Wave 3) is the live end-to-end proof.
</verification>

<success_criteria>
- load_edition_context returns the three-state contract (scored / not-scored operator pool / empty corpus) using plain `.eq('status','published')`.
- narrative_context + avoided_themes injected via `setdefault` before the generation branch; both writer paths + the prepass are covered.
- exemplars passed at both `generate_from_blocks` sites; Phase E no longer returns a silent `score:0` on absent exemplars.
- No new `.in_()`, no new LLM call surface, no schema change, no new dependency.
</success_criteria>

<output>
Create `.planning/phases/26-continuity-exemplar-context/26-01-SUMMARY.md` when done.
</output>
