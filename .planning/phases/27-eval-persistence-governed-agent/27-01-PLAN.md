---
phase: 27-eval-persistence-governed-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/045_edition_evals.sql
autonomous: true
requirements: [EVAL-01, GOV-01, GOV-02]

must_haves:
  truths:
    - "Migration 045 file exists with the edition_evals table DDL matching the authoritative REQUIREMENTS.md shape (JSONB-only, no spec-01 materialized columns) per D-04/D-07"
    - "The verdict-iff-ok CHECK, UNIQUE(newsletter_id, layer, attempt), and the idx_edition_evals_trend index are present"
    - "SECTION 2 seeds the edition_eval agent with the GOV-02 wallet values (allow_negative=FALSE, spending_cap_sats=5000 weekly, on_cap_behavior='reject', uncapped=FALSE) and model id claude-sonnet-4-6 (never the EOL claude-sonnet-4-20250514)"
    - "Both sections are idempotent / re-apply-safe (034 / 029 pattern) and the agent seed still carries the <bcrypt-hash> placeholder (orchestrator substitutes the real hash in 27-03)"
  artifacts:
    - path: "supabase/migrations/045_edition_evals.sql"
      provides: "edition_evals DDL (SECTION 1) + governed edition_eval agent_registry/agent_wallets_v2 seed (SECTION 2)"
      contains: "edition_evals_verdict_iff_ok"
      min_lines: 60
  key_links:
    - from: "supabase/migrations/045_edition_evals.sql SECTION 2 agent_wallets_v2 seed"
      to: "agent_wallets_v2_cap_or_uncapped CHECK (migration 034)"
      via: "spending_cap_sats=5000 > 0 satisfies the structural cap-or-uncapped invariant"
      pattern: "spending_cap_sats"
---

<objective>
Author `supabase/migrations/045_edition_evals.sql` — ONE sectioned, idempotent migration file (D-11): SECTION 1 creates the `edition_evals` per-attempt telemetry table with the fail-loud `verdict-iff-ok` CHECK, the `UNIQUE(newsletter_id, layer, attempt)` constraint, and the `idx_edition_evals_trend` index; SECTION 2 seeds the governed `edition_eval` proxy agent (`agent_registry` + `agent_wallets_v2`) with its hard-capped, reject-on-cap wallet.

This plan AUTHORS the SQL text only — it is worktree-SAFE and autonomous. The agent seed carries the literal `<bcrypt-hash>` placeholder; the orchestrator mints the real key, substitutes the real bcrypt hash, and MCP-applies the file in plan 27-03 (D-12/D-13). Do NOT apply the migration here and do NOT run `supabase db push`.

Purpose: stand up the persistence + budget core both eval layers (Phases 28–30) write through, realizing the milestone-wide fail-loud-no-silent-zero invariant structurally in the schema (EVAL-01) and the governed-budget agent (GOV-01/GOV-02).
Output: `supabase/migrations/045_edition_evals.sql` (two sections, re-apply-safe, hash placeholder unsubstituted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative WHAT + the verbatim DDL block (overrides spec-01 on shape/taxonomy/model-id)
@.planning/REQUIREMENTS.md
# Locked decisions D-01..D-15 (read D-04, D-07, D-11, D-13 in full)
@.planning/phases/27-eval-persistence-governed-agent/27-CONTEXT.md
# Pattern map: SECTION 1 analog (034) + SECTION 2 analog (029) with concrete excerpts
@.planning/phases/27-eval-persistence-governed-agent/27-PATTERNS.md

# Analog migrations (the idempotency + seed templates)
@supabase/migrations/034_governance_caps_and_oncap_behavior.sql
@supabase/migrations/029_rivalscope_agent.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: SECTION 1 — edition_evals table, constraints, and trend index</name>
  <files>supabase/migrations/045_edition_evals.sql</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 99-137 (the authoritative "Proposed DDL" block — copy the column shape + constraints VERBATIM; this overrides spec-01 per D-07)
    - .planning/phases/27-eval-persistence-governed-agent/27-CONTEXT.md D-04, D-05, D-07 (JSONB-only; NO materialized headline columns)
    - supabase/migrations/034_governance_caps_and_oncap_behavior.sql lines 29-47 (section-banner header style + inline CHECK syntax) and 131-147 (DO $$ … EXCEPTION WHEN duplicate_object guard)
    - .planning/phases/27-eval-persistence-governed-agent/27-PATTERNS.md "SECTION 1" (idempotency wrapper guidance)
  </read_first>
  <action>
    Begin the file with a header comment block (purpose, migration number 045, "SQL-first — operator applies via MCP after DDL review; hash substituted at key-mint time, do not apply from a worktree"). Add a 034-style SECTION 1 banner.

    Create table `edition_evals` re-apply-safe via `CREATE TABLE IF NOT EXISTS` (D-11 discretion permits this over a DO-block guard). Columns EXACTLY per REQUIREMENTS.md lines 103-125, JSONB-only (D-04, NO spec-01 materialized columns like tier1_count/voice_score/continuity/regression):
    - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
    - `newsletter_id UUID NOT NULL REFERENCES newsletters(id) ON DELETE CASCADE`
    - `edition_number INT NOT NULL`
    - `pipeline_version TEXT NOT NULL CHECK (pipeline_version IN ('single_pass','block_v1'))`
    - `attempt INT NOT NULL DEFAULT 0`
    - `layer TEXT NOT NULL CHECK (layer IN ('deterministic','judge'))`
    - `eval_status TEXT NOT NULL CHECK (eval_status IN ('ok','error'))`
    - `error TEXT`
    - `verdict TEXT CHECK (verdict IN ('passed','held_fabrication','held_voice','escalated'))`
    - the named CHECK `CONSTRAINT edition_evals_verdict_iff_ok CHECK ((eval_status='ok' AND verdict IS NOT NULL AND error IS NULL) OR (eval_status='error' AND verdict IS NULL AND error IS NOT NULL))`
    - `deterministic_flags JSONB NOT NULL DEFAULT '{}'::jsonb`
    - `judge_scores JSONB NOT NULL DEFAULT '{}'::jsonb`
    - `judge_feedback TEXT`
    - `sats_spent INT NOT NULL DEFAULT 0`
    - `model_calls JSONB NOT NULL DEFAULT '[]'::jsonb`
    - `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`
    - the table constraint `UNIQUE (newsletter_id, layer, attempt)` (one row per draft per layer per attempt — NOT spec-01's `UNIQUE(newsletter_id)`).

    Then `CREATE INDEX IF NOT EXISTS idx_edition_evals_trend ON edition_evals (edition_number DESC, pipeline_version);`.

    Do NOT add `edition_revisions` (REV-01 is deferred, D-07). Do NOT add baseline_newsletter_id or any spec-01-only column.
  </action>
  <acceptance_criteria>
    - `grep -c 'CREATE TABLE IF NOT EXISTS edition_evals' supabase/migrations/045_edition_evals.sql` returns 1
    - file contains `edition_evals_verdict_iff_ok`
    - file contains `UNIQUE (newsletter_id, layer, attempt)`
    - file contains `pipeline_version IN ('single_pass','block_v1')`, `layer IN ('deterministic','judge')`, `eval_status IN ('ok','error')`, and `verdict IN ('passed','held_fabrication','held_voice','escalated')`
    - file contains `idx_edition_evals_trend` and `edition_number DESC`
    - file contains `attempt INT  NOT NULL DEFAULT 0` (or equivalent `attempt ... DEFAULT 0`) — `grep -E 'attempt[[:space:]]+INT.*DEFAULT 0' supabase/migrations/045_edition_evals.sql` returns a match
    - `grep -c -E 'tier1_count|voice_score|baseline_newsletter_id|edition_revisions' supabase/migrations/045_edition_evals.sql` returns 0 (no spec-01 materialized columns / deferred table)
  </acceptance_criteria>
  <verify>
    <automated>cd /root/bitcoin_bot && grep -q 'edition_evals_verdict_iff_ok' supabase/migrations/045_edition_evals.sql && grep -q 'UNIQUE (newsletter_id, layer, attempt)' supabase/migrations/045_edition_evals.sql && grep -q 'idx_edition_evals_trend' supabase/migrations/045_edition_evals.sql && [ "$(grep -c -E 'tier1_count|voice_score|baseline_newsletter_id|edition_revisions' supabase/migrations/045_edition_evals.sql)" = "0" ] && echo PASS</automated>
  </verify>
  <done>SECTION 1 of 045 creates the JSONB-only edition_evals table with the verdict-iff-ok CHECK, the per-attempt UNIQUE constraint, and the trend index — verbatim from the authoritative REQUIREMENTS.md DDL, no spec-01 materialized columns.</done>
</task>

<task type="auto">
  <name>Task 2: SECTION 2 — governed edition_eval agent seed (registry + wallet)</name>
  <files>supabase/migrations/045_edition_evals.sql</files>
  <read_first>
    - supabase/migrations/029_rivalscope_agent.sql (the whole file — the agent_registry + agent_wallets_v2 INSERT + ON CONFLICT (agent_name) DO UPDATE template, and the committed-bcrypt-hash precedent)
    - .planning/REQUIREMENTS.md lines 128-134 (the seed INSERT values — copy verbatim; allowed_models, wallet numbers)
    - .planning/phases/27-eval-persistence-governed-agent/27-CONTEXT.md D-01, D-12, D-13, D-15
    - .planning/phases/27-eval-persistence-governed-agent/27-PATTERNS.md "SECTION 2" (the EXTENDED agent_wallets_v2 column list — NOT 029's pre-034 short form)
    - supabase/migrations/034_governance_caps_and_oncap_behavior.sql lines 39-47, 140-142 (the agent_wallets_v2 governance columns + the cap_or_uncapped CHECK the seed must satisfy)
  </read_first>
  <action>
    Add a 034-style SECTION 2 banner. Insert the `edition_eval` agent following the 029 pattern, with the values locked by REQUIREMENTS.md lines 128-134 and D-01:

    `INSERT INTO agent_registry (agent_name, agent_type, api_key_hash, access_tier, allowed_models, rate_limit_rpm, is_active)` VALUES `'edition_eval'`, `'internal'`, the literal placeholder `'<bcrypt-hash>'` (D-12 — the orchestrator substitutes the REAL bcrypt hash in plan 27-03; leave the placeholder here), `'internal'`, `ARRAY['deepseek-chat','claude-sonnet-4-6']` (the non-EOL model id per D-07 — NEVER `claude-sonnet-4-20250514`), `10`, `TRUE`. Add `ON CONFLICT (agent_name) DO UPDATE SET api_key_hash = EXCLUDED.api_key_hash, allowed_models = EXCLUDED.allowed_models, is_active = TRUE` (029 idempotency).

    Then the wallet, using the EXTENDED post-034 column list (so the governance columns are populated explicitly): `INSERT INTO agent_wallets_v2 (agent_name, balance_sats, total_deposited_sats, allow_negative, spending_cap_sats, spending_cap_window, uncapped, on_cap_behavior, downgrade_map)` VALUES `'edition_eval', 25000, 25000, FALSE, 5000, 'weekly', FALSE, 'reject', '{}'::jsonb`. Add `ON CONFLICT (agent_name) DO UPDATE SET balance_sats = EXCLUDED.balance_sats, total_deposited_sats = EXCLUDED.total_deposited_sats, allow_negative = EXCLUDED.allow_negative, spending_cap_sats = EXCLUDED.spending_cap_sats, spending_cap_window = EXCLUDED.spending_cap_window, uncapped = EXCLUDED.uncapped, on_cap_behavior = EXCLUDED.on_cap_behavior, downgrade_map = EXCLUDED.downgrade_map` (029 idempotency, re-asserts the canonical values on re-apply).

    Add a one-line comment at the placeholder noting the hash is substituted at key-mint time (plan 27-03) and the committed hash is the audit record (D-13). Do NOT generate or invent a bcrypt hash here — the placeholder stays literal.
  </action>
  <acceptance_criteria>
    - file contains `INSERT INTO agent_registry` and the literal `'edition_eval'`
    - file contains the literal placeholder `<bcrypt-hash>` (proves the real hash is NOT yet substituted — orchestrator-owned in 27-03) — `grep -c '<bcrypt-hash>' supabase/migrations/045_edition_evals.sql` returns 1
    - `grep -c 'claude-sonnet-4-20250514' supabase/migrations/045_edition_evals.sql` returns 0 (EOL model id forbidden — D-07)
    - file contains `claude-sonnet-4-6`
    - file contains `INSERT INTO agent_wallets_v2` AND every GOV-02 token: `allow_negative`, `spending_cap_sats`, `5000`, `'weekly'`, `uncapped`, `on_cap_behavior`, `'reject'`, `downgrade_map`, and `25000`
    - file contains `ON CONFLICT (agent_name) DO UPDATE` at least twice (registry + wallet) — `grep -c 'ON CONFLICT (agent_name) DO UPDATE' supabase/migrations/045_edition_evals.sql` returns 2
    - the wallet seed satisfies the cap_or_uncapped CHECK (spending_cap_sats=5000 > 0) — visually confirm the FALSE for uncapped pairs with the 5000 cap
  </acceptance_criteria>
  <verify>
    <automated>cd /root/bitcoin_bot && [ "$(grep -c '<bcrypt-hash>' supabase/migrations/045_edition_evals.sql)" = "1" ] && [ "$(grep -c 'claude-sonnet-4-20250514' supabase/migrations/045_edition_evals.sql)" = "0" ] && grep -q "claude-sonnet-4-6" supabase/migrations/045_edition_evals.sql && grep -q "INSERT INTO agent_wallets_v2" supabase/migrations/045_edition_evals.sql && grep -q "'reject'" supabase/migrations/045_edition_evals.sql && [ "$(grep -c 'ON CONFLICT (agent_name) DO UPDATE' supabase/migrations/045_edition_evals.sql)" = "2" ] && echo PASS</automated>
  </verify>
  <done>SECTION 2 seeds the edition_eval agent with the GOV-02 hard-capped reject-on-cap wallet, the non-EOL model id, the committed-hash placeholder, and idempotent ON CONFLICT upserts — ready for orchestrator hash-substitution + MCP apply in 27-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → live DB | the committed SQL becomes live schema + governance state when applied (in 27-03) |
| committed file → git repo | the file (incl. SECTION 2 seed) is tracked and shareable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | committed `api_key_hash` in SECTION 2 | accept | This plan commits only the literal `<bcrypt-hash>` PLACEHOLDER, not a real secret. When the real hash lands (27-03), it is a one-way bcrypt (cost 12) of an INTERNAL proxy key — not an external credential; the plaintext key never enters git (lives only in config/.env). Precedent: 029 committed its literal hash (D-13). NOT a HIGH finding. |
| T-27-02 | Tampering | edition_evals verdict/eval_status integrity | mitigate | The `edition_evals_verdict_iff_ok` CHECK makes a silent-zero / verdict-without-eval row UNREPRESENTABLE at the DB level (structural fail-loud, operator preference structure>application). Acceptance criteria assert the CHECK string is present. |
| T-27-03 | Elevation of Privilege | budget guard weakened by a wrong wallet seed | mitigate | Wallet seeded `allow_negative=FALSE`, `spending_cap_sats=5000 (>0)`, `uncapped=FALSE`, `on_cap_behavior='reject'` — a runaway eval loop hard-stops (GOV-02). Reject-on-cap is a SAFE failure by design (D-02): the proxy returns 402 → the eval records an error and escalates loudly, it never silently passes. Acceptance criteria assert every seed token. |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (pure SQL authoring). No supply-chain surface. |
</threat_model>

<verification>
- `python3 -c "import pathlib,sys; sys.exit(0 if pathlib.Path('supabase/migrations/045_edition_evals.sql').exists() else 1)"` (file exists)
- All Task 1 + Task 2 automated grep gates PASS.
- Manual DDL review (operator, in 27-03 before MCP apply): the file is valid Postgres and matches REQUIREMENTS.md lines 103-134 verbatim.
</verification>

<success_criteria>
- `supabase/migrations/045_edition_evals.sql` exists with both sections, idempotent, JSONB-only.
- SECTION 1: edition_evals with verdict-iff-ok CHECK, UNIQUE(newsletter_id, layer, attempt), idx_edition_evals_trend (EVAL-01).
- SECTION 2: governed edition_eval agent seed with GOV-02 wallet + claude-sonnet-4-6 + ON CONFLICT idempotency + `<bcrypt-hash>` placeholder (GOV-01, GOV-02).
- No spec-01 materialized columns, no edition_revisions, no EOL model id.
</success_criteria>

<output>
Create `.planning/phases/27-eval-persistence-governed-agent/27-01-SUMMARY.md` when done.
</output>
