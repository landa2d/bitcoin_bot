---
phase: 28-layer-1-deterministic-gate
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - docker/newsletter/deterministic_gate.py
  - tests/test_28_deterministic_gate.py
autonomous: true
requirements: [GATE-02, GATE-03]
must_haves:
  truths:
    - "A github.com/owner/repo ref returning GitHub API 404 produces a fabrication flag (kind=github_repo); the 404 is NEVER retried (D-02)"
    - "A repo whose draft-asserted star count differs >20% from the live stargazers_count produces a fabrication flag (GATE-02)"
    - "A GitHub 403/429/quota or 5xx produces an UNVERIFIED entry (kind=github_repo) — never a fabrication flag, never folded into pass (D-01); a transient failure is retried exactly once before settling unverified (D-02)"
    - "A URL returning HTTP 404/410 produces a fabrication flag (kind=dead_url); a timeout/connection-refused/5xx produces an UNVERIFIED entry, retried once (D-01/D-02)"
    - "Every unique owner/repo ref and URL is network-checked exactly once via a per-run cache; duplicates reuse the cached result (D-03)"
    - "A draft URL whose host is loopback/private/link-local or a known internal service (llm-proxy, supabase, *.internal) is routed to unverified (reason=unsafe_host) and NEVER fetched (SSRF guard)"
    - "GITHUB_TOKEN is read from env, sent only to api.github.com over HTTPS, and never written to logs or the flags object (meta.github_token_present is a bool only)"
  artifacts:
    - path: "docker/newsletter/deterministic_gate.py"
      provides: "_is_safe_public_url SSRF guard, _classify_github, _classify_url, ref/URL extraction, per-run dedup cache, retry-once; wired into run_deterministic_gate"
      contains: "def _is_safe_public_url"
    - path: "tests/test_28_deterministic_gate.py"
      provides: "Fake injectable httpx client double + GATE-02/03 + D-01/D-02/D-03 + SSRF tests"
      contains: "http_client="
  key_links:
    - from: "run_deterministic_gate"
      to: "GitHub API + arbitrary URL hosts"
      via: "injected http_client.get/head with token + 5s timeout"
      pattern: "http_client"
    - from: "_classify_url"
      to: "_is_safe_public_url"
      via: "SSRF gate before any fetch"
      pattern: "_is_safe_public_url"
---

<objective>
Add the network-liveness layer (GATE-02 GitHub repo + star-drift, GATE-03 URL HEAD) to `run_deterministic_gate`, implementing the locked D-01 three-outcome classifier (confirmed-fabricated / unverified / verified-ok), D-02 retry-once-on-transient, D-03 per-run dedup cache, and an SSRF guard for the URL HEAD layer. All checks run sequentially against the injected `http_client` seam (no real egress in tests). This is the security-critical plan: the gate issues outbound requests against URLs/repo refs extracted from untrusted LLM-generated draft text.

Purpose: Make `unverified` a real, populated, first-class outcome (the operator's explicit requirement: "an error is not evidence" — a transient 5xx/rate-limit must never be a fabrication flag and never a silent pass). Catch the dead/404 GitHub repos and dead URLs that are historical worst offenders, while bounding egress (5s timeout, dedup, SSRF allowlist).

Output: `_is_safe_public_url`, `_classify_github`, `_classify_url`, ref/URL extraction, the dedup cache, retry-once, and their wiring into the orchestrator; plus the injectable fake httpx client double and the GATE-02/03 + D-01/D-02/D-03 + SSRF tests. Build-only, worktree-safe — no live network calls (tests mock the client via the `http_client` param), no wiring into the poller, no DB write (D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-layer-1-deterministic-gate/28-CONTEXT.md
@.planning/phases/28-layer-1-deterministic-gate/28-RESEARCH.md
@.planning/phases/28-layer-1-deterministic-gate/28-PATTERNS.md
@.planning/phases/28-layer-1-deterministic-gate/28-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSRF guard + GitHub repo/star classifier + ref extraction + dedup cache + retry-once (GATE-02, D-01/D-02/D-03)</name>
  <files>docker/newsletter/deterministic_gate.py, tests/test_28_deterministic_gate.py</files>
  <read_first>
    - docker/newsletter/deterministic_gate.py — the module from Plan 01 (the orchestrator, `_GITHUB_URL`, the meta keys to populate). You are extending it, not rewriting it.
    - docker/processor/agentpulse_processor.py — :96 (`GITHUB_TOKEN = os.getenv('GITHUB_TOKEN')`), :1134-1136 (the `Accept: application/vnd.github.v3+json` header + `Authorization: token` convention — copy verbatim), :1157-1167 (sync `httpx.Client` + 403-rate-limit handling), :1177 (`stargazers_count` field). The canonical GitHub-API idiom to reuse.
    - .planning/phases/28-layer-1-deterministic-gate/28-RESEARCH.md — §"Pattern 2: Network three-outcome classifier" (the exact `_classify_github` skeleton with D-01/D-02 mapping); §"Pattern 3: Per-run dedup cache"; §"Security Domain" (the `_is_safe_public_url` SSRF guard recommendation, ASVS V5/V12, token-never-logged); §"Pitfall 5" (GitHub unauthenticated 60/hr → tests MUST mock; 403/429 → unverified, not fabricated).
    - .planning/phases/28-layer-1-deterministic-gate/28-CONTEXT.md — D-01 (three outcomes, do NOT collapse), D-02 (retry once on transient, NEVER a 404), D-03 (dedup + per-run cache, sequential, token via env).
    - tests/test_27_edition_eval.py :43-70 — the payload-capturing/call-behavior stub shape to mirror for the fake httpx client (with a call-counter for D-02/D-03 asserts).
  </read_first>
  <action>
    Add `_is_safe_public_url(url: str) -> bool` (SSRF guard, ~10-15 lines, ASVS L1 per RESEARCH §Security Domain): parse with `urllib.parse.urlparse`; return False unless scheme is in {"http","https"}; resolve the hostname and return False if it is loopback/localhost, an RFC-1918 private range (10/8, 172.16/12, 192.168/16), link-local (169.254/16, fe80::/10), unique-local (fc00::/7), `::1`, or matches the internal-service denylist `{"llm-proxy","supabase","gato_brain","gato-brain","processor","analyst","research","newsletter","web","lab-data-provider"}` or a `*.internal` suffix. Use `ipaddress` for literal IP hosts. This helper gates the URL HEAD layer in Task 2; build it here so the test fixtures are shared.

    Implement `_classify_github(owner, repo, *, client, token) -> tuple[str, int | None]` exactly per RESEARCH §"Pattern 2": headers `{'Accept':'application/vnd.github.v3+json'}` plus `Authorization: token <token>` when a token is present (processor:1134-1136 convention). Hit `https://api.github.com/repos/{owner}/{repo}` with `timeout=5`. D-01 mapping: HTTP 404 → return `("fabricated", None)` and NEVER retry (D-02); 403/429 or >=500 → `("unverified", None)` (retry ONCE only on >=500 transient per D-02, never on 403/429 quota); 200 → `("verified", json.get("stargazers_count"))`; on `httpx.TimeoutException`/`httpx.ConnectError` retry once then `("unverified", None)`. The token is read as `github_token or os.getenv('GITHUB_TOKEN')`; it is sent only to api.github.com over HTTPS and is NEVER logged or placed in the flags object.

    Add GitHub ref extraction: `_GITHUB_URL.finditer` over both bodies → the unique set of `(owner, repo)` tuples (strip a trailing `.git`/path segment). Add the per-run dedup cache (D-03): a dict keyed `("gh", owner, repo)` populated on first classify, reused for every later occurrence; iterate sequentially.

    Star-drift (GATE-02): when a star count is asserted in the draft adjacent to a repo ref (a number immediately followed by "stars"/"stargazers" within the same line/sentence as the github.com ref), and the repo is `verified`, flag `fabrication` `{"kind":"github_stars","ref":"owner/repo","version":version,"detail":"asserted N vs live M (>20% drift)"}` iff `abs(asserted - live) / max(live,1) > 0.20`. If no count is asserted, existence-only is sufficient (no star flag).

    Wire into `run_deterministic_gate`: when `http_client` is provided (or construct a default `httpx.Client(timeout=5)` for live use — but tests always inject), run the GitHub layer across both versions after the fabrication core. Map `fabricated` → `fabrication` entry `{"kind":"github_repo","ref":"owner/repo","version":version,"detail":"GitHub 404"}`; `unverified` → `unverified` entry `{"kind":"github_repo","ref":"owner/repo","reason":<rate_limit_403|server_error_5xx|timeout|conn_refused>}`. Update `meta["github_checked"]` to the count of unique repos checked. NEVER fold unverified into fabrication (D-01).

    Add the fake injectable httpx client double to the test file: a class accepting a per-URL queue of `(status_code, json_dict)` results, raising `httpx.TimeoutException`/`httpx.ConnectError` on demand, with a `.calls` counter list capturing every requested URL (to assert D-02 retry-once and D-03 dedup). Inject via the `http_client` param — NO monkeypatch of the real network. Add GitHub tests: 404 → fabrication kind=github_repo and exactly 1 call (no retry, D-02); 403 → unverified kind=github_repo (not fabrication, D-01); 5xx → unverified after exactly 2 calls (retry-once, D-02); 200 with star drift >20% → github_stars fabrication; 200 within band → clean; the same repo referenced 3× → exactly 1 GitHub call (D-03 dedup); token-present → meta.github_token_present True and the token never appears in the flags object or any captured log.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_28_deterministic_gate.py -k "github or ssrf or dedup or retry or stars or unverified" -x</automated>
  </verify>
  <acceptance_criteria>
    - GitHub API 404 → `fabrication` entry `kind=="github_repo"`; the fake client records exactly 1 call for that repo (404 never retried — D-02).
    - GitHub 403/429 → `unverified` entry `kind=="github_repo"`, NOT a fabrication flag (D-01).
    - GitHub 5xx → `unverified` after exactly 2 calls (retry-once on transient — D-02).
    - 200 with asserted star count >20% off live `stargazers_count` → `fabrication` `kind=="github_stars"`; within 20% → no star flag.
    - The same repo referenced multiple times triggers exactly ONE GitHub call (D-03 dedup, asserted via the call counter).
    - `_is_safe_public_url("http://127.0.0.1/x")`, `("http://llm-proxy:8200")`, `("http://169.254.169.254/")`, `("file:///etc/passwd")` all return False; `("https://github.com/a/b")` returns True.
    - `grep -rn "GITHUB_TOKEN\|github_token" docker/newsletter/deterministic_gate.py` shows the token only read from env/param and never passed to `logger`/the flags dict; `meta["github_token_present"]` is a bool.
    - `pytest tests/test_28_deterministic_gate.py -k "github or ssrf or dedup or retry or stars or unverified" -x` exits 0.
  </acceptance_criteria>
  <done>The GitHub layer maps every result into the locked three-outcome taxonomy with retry-once + dedup, flags star drift >20%, and the SSRF guard rejects internal/private hosts — all proven against the injected fake client with call-count assertions; the token is never leaked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: URL HEAD liveness classifier + URL extraction + SSRF routing, wired into the orchestrator (GATE-03, D-01/D-02)</name>
  <files>docker/newsletter/deterministic_gate.py, tests/test_28_deterministic_gate.py</files>
  <read_first>
    - docker/newsletter/deterministic_gate.py — the module after Task 1 (`_is_safe_public_url`, `_MD_LINK`, the dedup cache, the orchestrator wiring you extend).
    - docker/newsletter/newsletter_poller.py — :235-239 (the in-service sync httpx 5s-timeout idiom to mirror for `httpx.head`).
    - .planning/phases/28-layer-1-deterministic-gate/28-RESEARCH.md — §GATE-03 (`httpx.head(url, timeout=5, follow_redirects=True)`); §"User Constraints" D-01 (URL: 404/410 → fabricated; timeout/5xx/conn-refused → unverified); §"Security Domain" (SSRF table: untrusted draft URLs, follow_redirects cap, treat draft URLs as untrusted, never include response bodies in flags); §"Pitfall 5".
    - .planning/phases/28-layer-1-deterministic-gate/28-CONTEXT.md — D-01/D-02/D-03 (the same three-outcome/retry/dedup semantics applied to URLs).
  </read_first>
  <action>
    Implement `_classify_url(url, *, client) -> tuple[str, str]` (GATE-03, D-01): FIRST call `_is_safe_public_url(url)`; if False, return `("unverified", "unsafe_host")` WITHOUT issuing any request (SSRF mitigation — internal/private hosts are never fetched). Otherwise `client.head(url, timeout=5, follow_redirects=True)`. D-01 mapping: 404/410 → `("fabricated", "http_<code>")`; on `httpx.TimeoutException`/`httpx.ConnectError` retry once (D-02) then `("unverified", "timeout"|"conn_refused")`; >=500 → retry once then `("unverified", "server_error_5xx")`; other 4xx (401/403/429) → `("unverified", "http_<code>")` (an auth/rate wall is not evidence of fabrication — "an error is not evidence"); 200/3xx-resolved-200 → `("verified", "ok")`. Never read or store the response body (only status). Cap redirects via httpx's default; do not follow into an unsafe host (re-check is acceptable but not required this phase — note as a Phase-30 hardening candidate).

    Add URL extraction: `_MD_LINK.finditer` (markdown-link targets) plus a bare-URL scan (`https?://[^\s)]+`) over both bodies → the unique URL set. EXCLUDE `github.com/owner/repo` URLs already handled by the GitHub API layer (Task 1) to avoid double-checking; all other URLs get HEAD. Reuse the same per-run dedup cache keyed `("url", normalized_url)` (D-03), sequential iteration.

    Wire into `run_deterministic_gate`: after the GitHub layer, run the URL layer across both versions. Map `fabricated` → `fabrication` entry `{"kind":"dead_url","url":url,"version":version,"detail":"HTTP <code>"}`; `unverified` → `unverified` entry `{"kind":"url","url":url,"reason":<unsafe_host|timeout|conn_refused|server_error_5xx|http_4xx>}`. Update `meta["urls_checked"]`. Keep unverified strictly separate from fabrication (D-01).

    Add tests: a dead URL returning 404 → `dead_url` fabrication; a 410 → `dead_url` fabrication; a timeout → `unverified` reason=timeout after exactly 2 calls (retry-once); a 5xx → unverified server_error_5xx; a 403 → unverified http_403 (NOT fabrication); a URL with host `http://llm-proxy:8200/x` or `http://169.254.169.254/` → unverified reason=unsafe_host with ZERO calls to the fake client (asserted via the call counter — the SSRF guard short-circuits before fetch); a live 200 URL → clean; the same URL twice → exactly 1 HEAD call (dedup).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_28_deterministic_gate.py -k "url or dead or unsafe or head" -x</automated>
  </verify>
  <acceptance_criteria>
    - A URL returning 404 or 410 → `fabrication` entry `kind=="dead_url"`.
    - A URL timeout/conn-refused/5xx → `unverified` entry `kind=="url"` with the matching reason, after at most 2 calls (retry-once — D-02).
    - A URL returning 403/401/429 → `unverified` (NOT fabrication — "an error is not evidence", D-01).
    - An unsafe-host URL (loopback/private/internal-service) → `unverified` reason `"unsafe_host"` with ZERO requests issued to the injected client (SSRF guard short-circuits — asserted via call counter).
    - github.com/owner/repo URLs are NOT double-checked by the URL HEAD layer (handled by the GitHub API layer instead).
    - A duplicate URL is HEAD-checked exactly once (D-03 dedup).
    - The response body is never read into the flags object (only status drives the outcome).
    - `pytest tests/test_28_deterministic_gate.py -k "url or dead or unsafe or head" -x` exits 0.
  </acceptance_criteria>
  <done>The URL HEAD layer classifies liveness into the locked three-outcome taxonomy, routes unsafe/internal hosts to unverified without fetching, and reuses the dedup cache + retry-once — all proven against the injected fake client; `unverified` is populated and strictly distinct from `fabrication`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Draft prose → extracted URL/repo ref → outbound HTTP | URLs and owner/repo refs are pulled from untrusted LLM output and used to issue network requests (the dominant attack surface — SSRF) |
| Newsletter container env → GitHub API | `GITHUB_TOKEN` crosses into an outbound HTTPS request; must never leak back into logs/flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Information Disclosure / Elevation | SSRF via a draft-supplied URL targeting internal hosts (`http://llm-proxy:8200`, `169.254.169.254`, RFC-1918, loopback) | mitigate | `_is_safe_public_url` rejects non-http(s) schemes + loopback/private/link-local/unique-local IPs + the internal-service-name denylist + `*.internal`; rejected URLs route to `unverified` (reason=unsafe_host) and are NEVER fetched (verified by a zero-calls assert) |
| T-28-05 | Information Disclosure | `GITHUB_TOKEN` leakage into logs or the flags object | mitigate | Token read only from env/param, sent only to api.github.com over HTTPS, never logged, never placed in flags; `meta.github_token_present` is a bool only (grep-asserted) |
| T-28-06 | Denial of Service | Link-heavy/adversarial draft causing many slow requests | mitigate | 5s timeout per request, D-03 per-run dedup (each unique ref/URL checked once), sequential bounded iteration; redirects use httpx default cap |
| T-28-07 | Tampering | Treating a transient 5xx/403 as fabrication (false hold) — or folding it into pass (silent miss) | mitigate | D-01 three-outcome classifier: only definitive 404/410 → fabricated; transient/quota → first-class `unverified`, never collapsed (asserted in tests) |
| T-28-08 | Information Disclosure | Storing an untrusted response body in flags | mitigate | Only the HTTP status code drives the outcome; response bodies are never read into the flags object |
| T-28-SC | Tampering | pip installs | accept | Zero new packages (httpx already in newsletter requirements.txt per RESEARCH); no install task |
</threat_model>

<verification>
- `python3 -m pytest tests/test_28_deterministic_gate.py -x` green for all GATE-02/03 + D-01/D-02/D-03 + SSRF cases.
- No live egress: every network test injects the fake `http_client`; `grep -n "httpx.Client()\|httpx.get(\|httpx.head(" tests/test_28_deterministic_gate.py` returns nothing (tests never call the real network).
- SSRF assertion: an unsafe-host URL produces a zero-call result (the guard short-circuits before any request).
- Token-leak assertion: the literal token value never appears in the returned flags object nor in captured log output.
- Regression: Plan-01 GATE-01/04/05/08 tests still green (orchestrator extended, not broken).
</verification>

<success_criteria>
- GATE-02: every unique owner/repo + github.com ref checked once against the live GitHub API contract; 404 → fabricated; >20% star drift → flagged; 403/429/5xx → unverified.
- GATE-03: every non-github URL HEAD-checked with a 5s timeout; 404/410 → fabricated; timeout/5xx/conn-refused → unverified; unsafe host → unverified without fetch.
- D-01/D-02/D-03 honored exactly: three outcomes never collapsed, 404 never retried, transient retried once, each ref/URL checked once.
- SSRF guard + token hygiene proven by tests; no live network egress in the suite.
- Emit-only: `unverified` populated and first-class; no DB write, no verdict, no action (D-05).
</success_criteria>

<output>
Create `.planning/phases/28-layer-1-deterministic-gate/28-02-SUMMARY.md` when done.
</output>
