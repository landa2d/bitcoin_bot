---
phase: 28-layer-1-deterministic-gate
plan: 03
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - docker/newsletter/deterministic_gate.py
  - tests/test_28_deterministic_gate.py
autonomous: true
requirements: [GATE-06, GATE-07]
must_haves:
  truths:
    - "A body containing a single-hash '# ' H1 line, or echoing draft['title']/draft['title_impact'] as a header line, produces a mechanical flag (GATE-06)"
    - "A leaked reading-mode/audience label from the tunable READING_MODE_LABELS blacklist (e.g. 'AUDIENCE:') in the body produces a mechanical flag; bare 'IMPACT'/'Technical' in legitimate prose do NOT (GATE-06)"
    - "A closer line matching the previous edition's closer (normalized: lowercase + collapse whitespace + strip trailing punctuation) produces a mechanical flag (GATE-07/D-06)"
    - "A numeric stat token appearing verbatim (normalized) in both the current and previous edition produces a mechanical flag per token (GATE-07/D-06, reusing _STATISTIC)"
    - "Mechanical flags are emitted under the mechanical key — distinct from fabrication; prior_edition=None skips the cross-edition checks without error"
    - "The full golden-draft suite (ed-36 MCP-auth, ed-34 GroupMemBench, fake arXiv, 404 repo, dead URL, transient-5xx→unverified, recycled closer, leaked label) passes on the REAL module with mocked network; full pytest tests/ is green"
  artifacts:
    - path: "docker/newsletter/deterministic_gate.py"
      provides: "H1/title-echo + reading-mode-label-leak checks (GATE-06), normalized-exact recycled-closer + duplicated-stat checks (GATE-07); wired into run_deterministic_gate"
      contains: "READING_MODE_LABELS"
    - path: "tests/test_28_deterministic_gate.py"
      provides: "GATE-06/07 tests + the combined golden-draft integration fixtures + full-flags-shape assertion"
      contains: "recycled_closer"
  key_links:
    - from: "GATE-07 stat check"
      to: "verification._STATISTIC"
      via: "reused stat-token regex, normalized per D-06"
      pattern: "_STATISTIC"
    - from: "run_deterministic_gate mechanical"
      to: "prior_edition full content_markdown"
      via: "normalized closer + stat-token comparison"
      pattern: "prior_edition"
---

<objective>
Add the mechanical-editorial checks (GATE-06 H1/title echo + reading-mode-label leak; GATE-07 recycled closer + duplicated stat vs the previous published edition) to `run_deterministic_gate`, then complete the golden-draft integration suite and prove the whole gate against the historical worst offenders with mocked network. GATE-07 uses normalized-exact matching (D-06: lowercase + collapse whitespace + strip trailing punctuation; NO fuzzy threshold). This plan closes the phase: all GATE-01..08 behaviors verified, full `pytest tests/` green.

Purpose: The mechanical checks catch the editorial misses (an H1 echo, a leaked `AUDIENCE:` scaffolding token, a recycled closer, a stat copy-pasted from last week) that are mechanical-only (may feed the Phase 29 rewrite loop, never a hard fabrication hold). The integration fixtures lock the gate's calibration against the named offenders before it ever touches the live path in Phase 30.

Output: the GATE-06/07 sub-checks wired into the orchestrator's `mechanical` list, the tunable `READING_MODE_LABELS` constant seeded from code-derived strings, the GATE-07 normalized-exact helpers (reusing `_STATISTIC`), and the combined golden-draft integration tests. Build-only, worktree-safe — no wiring into the poller, no DB write, no LLM, no container rebuild (D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-layer-1-deterministic-gate/28-CONTEXT.md
@.planning/phases/28-layer-1-deterministic-gate/28-RESEARCH.md
@.planning/phases/28-layer-1-deterministic-gate/28-PATTERNS.md
@.planning/phases/28-layer-1-deterministic-gate/28-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GATE-06 — H1/title echo + reading-mode-label leak (tunable blacklist)</name>
  <files>docker/newsletter/deterministic_gate.py, tests/test_28_deterministic_gate.py</files>
  <read_first>
    - docker/newsletter/deterministic_gate.py — the module after Plan 02 (the `BODY_START_MARKER` and `READING_MODE_LABELS` constants defined in Plan 01; the orchestrator's per-version loop and the `mechanical` list to populate).
    - docker/newsletter/block_pipeline.py — :660-661 (`## Read This, Skip the Rest` produced verbatim — the body start with no H1), :347-350 (the `AUDIENCE: Business leaders...` / `AUDIENCE: Technical builders...` literals — the realistic leak vector seeding the blacklist).
    - docker/newsletter/newsletter_poller.py — :595-602 + :1398 (the body must use the `##` header, not `#`), :1678 (`title_impact` resolution), :1709-1710 region (title/title_impact stored as separate DB columns, never in the body).
    - .planning/phases/28-layer-1-deterministic-gate/28-RESEARCH.md — §"TODO 3" (body-start marker confirmed verbatim; title is a separate column → the echo check is: body must NOT contain a single-hash `# ` H1 AND draft title must NOT appear as a header line; the curated tunable blacklist + the explicit "do NOT blacklist bare 'IMPACT'/'Technical'" rule; open question A1 — ship tunable).
    - .planning/phases/28-layer-1-deterministic-gate/28-PATTERNS.md — §"Layer 3 — Mechanical regex checks" (GATE-06 strings confirmed).
  </read_first>
  <action>
    Confirm `READING_MODE_LABELS` (defined in Plan 01) is the seeded, operator-tunable module-level constant per RESEARCH §TODO-3: `["AUDIENCE:", "READING MODE", "BUILDER MODE", "IMPACT MODE", "TECHNICAL MODE", "STRATEGIC MODE", "TECHNICAL READING MODE", "IMPACT READING MODE", "STRATEGIC READING MODE", "TECHNICAL VERSION", "IMPACT VERSION", "STRATEGIC VERSION"]`. Add a code comment noting this is operator-tunable during the Phase 30 report-only window (open question A1) and that bare `"IMPACT"`/`"Technical"` are deliberately NOT included (they appear in legitimate prose).

    Implement `_check_h1_and_title_echo(body, title, version) -> list[dict]` (GATE-06): emit `mechanical` `{"kind":"h1_in_body","version":version}` if the body contains a line matching `^# ` (single-hash H1 — bodies must start at the `##` `BODY_START_MARKER` with no H1, per poller:2120). Emit `mechanical` `{"kind":"title_echo","version":version,"value":title}` if the non-empty `title` (or `title_impact` for the impact version) appears as a markdown header line (`^#{1,6}\s*<title>` case-insensitive, normalized whitespace) anywhere in its body. Use the version-appropriate title (technical→`draft['title']`, impact→`draft['title_impact']`).

    Implement `_check_reading_mode_leak(body, version) -> list[dict]` (GATE-06): case-insensitive scan of the body for each label in `READING_MODE_LABELS`; on a hit emit `mechanical` `{"kind":"reading_mode_leak","version":version,"label":<matched label>}`. Match the multi-word labels as substrings (they are uppercase scaffolding tokens unlikely in edited prose); do NOT match bare `"IMPACT"`/`"Technical"`.

    Wire both into `run_deterministic_gate`'s per-version loop, extending the `mechanical` list (the orchestrator already has the version label + title available). Mechanical flags stay distinct from `fabrication`/`unverified`.

    Add tests: a body with a `# Some Title` H1 → `h1_in_body`; a clean body starting at `## Read This, Skip the Rest` → no h1 flag; a body that repeats the edition title as a header → `title_echo`; a body containing `AUDIENCE: Technical builders...` → `reading_mode_leak` label="AUDIENCE:"; a body containing the word "impact" in normal prose → NO reading_mode_leak (the bare-word false-positive guard).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_28_deterministic_gate.py -k "h1 or title_echo or reading_mode or mechanical_gate06 or label" -x</automated>
  </verify>
  <acceptance_criteria>
    - A single-hash `# ` H1 line in the body → `mechanical` entry `kind=="h1_in_body"`.
    - A body whose header line echoes the edition title → `mechanical` entry `kind=="title_echo"`.
    - A body containing a `READING_MODE_LABELS` member (e.g. `"AUDIENCE:"`) → `mechanical` entry `kind=="reading_mode_leak"` with the matched `label`.
    - A body containing only bare `"impact"`/`"Technical"` in prose produces NO reading_mode_leak flag.
    - `READING_MODE_LABELS` is a module-level constant (grep-confirmed) with the operator-tunable comment; bare single words are absent from it.
    - These flags appear under `mechanical`, never `fabrication`.
    - `pytest tests/test_28_deterministic_gate.py -k "h1 or title_echo or reading_mode or mechanical_gate06 or label" -x` exits 0.
  </acceptance_criteria>
  <done>The GATE-06 mechanical checks flag H1/title echoes and leaked reading-mode/audience labels via a tunable blacklist, without false-positiving on legitimate bare-word prose — proven by tests on the real module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GATE-07 — normalized-exact recycled closer + duplicated stat vs prior edition (D-06)</name>
  <files>docker/newsletter/deterministic_gate.py, tests/test_28_deterministic_gate.py</files>
  <read_first>
    - docker/newsletter/deterministic_gate.py — the module after Task 1 (the imported `_STATISTIC`, the orchestrator, the `prior_edition` param).
    - docker/newsletter/verification.py — :112-120 (`_STATISTIC` — reuse this for stat tokens; do NOT write a new number regex).
    - .planning/phases/28-layer-1-deterministic-gate/28-RESEARCH.md — §"GATE-07 normalized-exact closer + stat check (D-06)" (the `_normalize`, `_closer_line`, `_stat_tokens` helpers); §"State of the Art" + Pitfall 4 (`validate_stat_repetition` poller:367 is intra-edition + One-Number-keyed → a NO-OP, NOT reusable; `load_edition_context` exposes only a truncated excerpt → `prior_edition` must be the FULL previous-published body, supplied by the Phase 30 caller; Phase 28 stubs it).
    - .planning/phases/28-layer-1-deterministic-gate/28-CONTEXT.md — D-06 (normalized-exact: lowercase + collapse whitespace + strip trailing punctuation; NO fuzzy similarity threshold).
  </read_first>
  <action>
    Implement the D-06 normalized-exact helpers per RESEARCH §GATE-07: `_normalize(s)` = collapse whitespace, strip, lowercase, strip trailing punctuation (`.!?,:;—-`); `_closer_line(body)` = `_normalize` of the last non-empty paragraph (split on blank lines); `_stat_tokens(body)` = the set of `_normalize(match)` for every `_STATISTIC.finditer(body)` match (reuse the imported engine regex — no new number regex).

    Implement `_check_cross_edition(body, prior_body, version) -> list[dict]` (GATE-07): if `prior_body` is empty/None, return `[]` (no prior edition → skip, no error). Otherwise: emit `mechanical` `{"kind":"recycled_closer","version":version,"prior_edition":<prior edition_number>}` iff `_closer_line(body) == _closer_line(prior_body)` and the closer is non-empty. For each stat token in `_stat_tokens(body) & _stat_tokens(prior_body)`, emit `mechanical` `{"kind":"duplicated_stat","version":version,"stat":<token>,"prior_edition":<n>}`. Normalized-exact only — NO fuzzy threshold (D-06).

    Wire into `run_deterministic_gate`: the `prior_edition` param is a dict `{content_markdown, content_markdown_impact, edition_number}` (the FULL previous-published body — a Phase-30 wiring responsibility; the gate takes it as a param and trusts it). For the technical version compare against `prior_edition['content_markdown']`, for impact against `prior_edition['content_markdown_impact']`. Extend the `mechanical` list. When `prior_edition is None`, run GATE-06 but skip GATE-07 cleanly.

    Add tests: a draft whose closer equals (after normalization, e.g. differing only in trailing period/casing/whitespace) the prior edition's closer → `recycled_closer`; a distinct closer → no flag; a stat token (e.g. "42%") present in both editions → `duplicated_stat` for that token; a stat only in the current edition → no flag; `prior_edition=None` → GATE-07 produces nothing and does not raise.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_28_deterministic_gate.py -k "recycled or duplicated_stat or cross_edition or prior_none" -x</automated>
  </verify>
  <acceptance_criteria>
    - A closer line equal to the prior edition's closer after normalization (lowercase + collapse ws + strip trailing punct) → `mechanical` entry `kind=="recycled_closer"`.
    - A closer differing only by trailing punctuation/casing/whitespace from the prior closer STILL matches (normalization proven).
    - A stat token present verbatim (normalized) in both editions → `mechanical` entry `kind=="duplicated_stat"` for that token; a current-only stat → no flag.
    - `grep -n "_STATISTIC" docker/newsletter/deterministic_gate.py` confirms the engine regex is reused (no new number regex defined).
    - `prior_edition=None` runs the gate without raising and produces zero GATE-07 flags.
    - No fuzzy/similarity threshold is used (normalized-exact only — D-06).
    - `pytest tests/test_28_deterministic_gate.py -k "recycled or duplicated_stat or cross_edition or prior_none" -x` exits 0.
  </acceptance_criteria>
  <done>The GATE-07 cross-edition checks flag recycled closers and verbatim-duplicated stats against the FULL prior edition using normalized-exact matching (no fuzzy threshold), reusing the engine's stat regex — proven by tests including the prior_edition=None skip path.</done>
</task>

<task type="auto">
  <name>Task 3: Golden-draft integration suite + full-flags-shape lock + phase gate</name>
  <files>tests/test_28_deterministic_gate.py</files>
  <read_first>
    - docker/newsletter/deterministic_gate.py — the complete module (all checks wired) — to assert the end-to-end aggregated flags shape.
    - tests/test_28_deterministic_gate.py — all cases from Plans 01-03 (the fixture builders + fake httpx client to reuse for the combined fixtures).
    - .planning/phases/28-layer-1-deterministic-gate/28-RESEARCH.md — §"Wave 0 Gaps" + §"Validation Architecture" / Phase Requirements → Test Map (the full GATE-01..08 + D-01..D-03 coverage matrix and the per-GATE `-k` selectors); the golden-fixture list (ed-36 MCP-auth, ed-34 GroupMemBench, fake arXiv, 404 repo, dead URL, transient-5xx→unverified, recycled closer, leaked label).
    - supabase/migrations/045_edition_evals.sql — `deterministic_flags JSONB` shape (:47) — assert the returned object fits (fabrication + mechanical present; unverified first-class extra).
  </read_first>
  <action>
    Add the combined golden-draft integration tests that exercise `run_deterministic_gate` end-to-end with the injected fake httpx client. Build two realistic offender drafts (one single-pass `input_data` fact_base, one block_v1 `{'blocks':[...]}` fact_base — locking both GATE-08 paths) whose bodies start at `## Read This, Skip the Rest` and embed multiple offenders at once: the ed-36 invented "MCP authentication" study, the ed-34 "GroupMemBench" benchmark, a fake arXiv ID, a github.com/owner/repo that the fake client 404s, a dead URL the fake client 404/410s, a repo/URL the fake client returns 5xx for (→ unverified), a closer line matching the stubbed `prior_edition`, a duplicated stat vs the prior edition, and a leaked `AUDIENCE:` label.

    Assert the AGGREGATED flags object: `fabrication` contains the tier1 study, the arxiv, the github_repo 404, and the dead_url entries; `unverified` contains the 5xx entries and is NON-EMPTY and DISTINCT from fabrication (D-01 — the headline assertion: the transient failure is visible, never a fabrication, never a silent pass); `mechanical` contains the recycled_closer, duplicated_stat, and reading_mode_leak entries; `meta` reports the correct `fact_base_path`, the github/url counts, and `github_token_present` as a bool. Assert the top-level keys exactly match `{fabrication, unverified, mechanical, meta}` so the object fits migration 045's `deterministic_flags` JSONB (Phase 30 maps it). Assert NO DB/LLM/network-real call occurred and NO verdict key is present (emit-only, D-05).

    Run the full phase gate: `python3 -m pytest tests/test_28_deterministic_gate.py` green, then `python3 -m pytest tests/` green (no regression in the existing suite). Fix any failures in the gate module or tests surfaced here before closing.
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -m pytest tests/test_28_deterministic_gate.py && python3 -m pytest tests/</automated>
  </verify>
  <acceptance_criteria>
    - A single end-to-end golden draft produces fabrication entries for the study, arXiv, github 404, and dead URL simultaneously.
    - The same draft's 5xx-returning ref/URL lands in `unverified` (non-empty, distinct from fabrication) — the D-01 "an error is not evidence" invariant proven end-to-end.
    - The same draft's recycled closer, duplicated stat, and leaked label land in `mechanical`.
    - The returned object's top-level keys are exactly `{fabrication, unverified, mechanical, meta}` (fits 045 deterministic_flags JSONB); no `verdict` key (emit-only, D-05).
    - Both a single-pass and a block_v1 fact_base path are exercised end-to-end (GATE-08).
    - `python3 -m pytest tests/test_28_deterministic_gate.py` exits 0 AND `python3 -m pytest tests/` exits 0 (no regression).
  </acceptance_criteria>
  <done>The full gate is proven against the historical worst offenders with mocked network in one aggregated assertion per fact-base path; the flags object fits the migration-045 JSONB contract; the whole repo test suite is green; the gate emits flags only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Draft prose → mechanical regex/normalization | The body and the prior-edition body are untrusted text run through normalization + the reused stat regex |
| prior_edition dict → cross-edition compare | Caller-supplied full prior body (Phase-30 wiring); None must be a clean skip, not a crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-09 | Denial of Service | ReDoS on adversarial draft via the closer/normalize regex | mitigate | `_normalize` uses a simple `\s+` collapse; `_STATISTIC` is the already-linear reused engine regex; no new nested-quantifier pattern added |
| T-28-10 | Tampering (log injection) | Logging draft-derived mechanical content | mitigate | Log only flag kinds/counts, never raw closer/body prose at INFO |
| T-28-11 | Tampering | `prior_edition=None` or malformed crashing the gate (a crash could mask real flags) | mitigate | Explicit None/empty guard returns no GATE-07 flags without raising; GATE-06 still runs |
| T-28-SC | Tampering | pip installs | accept | Zero new packages this phase (all deps present per RESEARCH); no install task |
</threat_model>

<verification>
- `python3 -m pytest tests/test_28_deterministic_gate.py` green across every GATE-01..08 + D-01..D-03 case (the full Phase Requirements → Test Map).
- `python3 -m pytest tests/` green — no regression in test_26/test_27 or the broader suite (engine reuse + new module are additive).
- The aggregated flags object top-level keys are exactly `{fabrication, unverified, mechanical, meta}` and contain no `verdict` (emit-only, D-05).
- `unverified` is non-empty and distinct from `fabrication` in the combined fixture (D-01 end-to-end).
- No live network egress anywhere in the suite (fake `http_client` injected throughout).
</verification>

<success_criteria>
- GATE-06: H1/title echo and leaked reading-mode/audience labels flagged via a tunable blacklist, with no bare-word false positives.
- GATE-07: recycled closers and verbatim-duplicated stats flagged against the FULL prior edition using normalized-exact matching (D-06), reusing `_STATISTIC`; prior_edition=None is a clean skip.
- The complete gate is calibrated against the named historical offenders (ed-36, ed-34, fake arXiv, 404 repo, dead URL, transient→unverified, recycled closer, leaked label) with mocked network.
- The flags object fits migration 045 `deterministic_flags` JSONB; emit-only (no verdict, no DB write, no LLM, no action — D-05); full repo suite green.
</success_criteria>

<output>
Create `.planning/phases/28-layer-1-deterministic-gate/28-03-SUMMARY.md` when done.
</output>
