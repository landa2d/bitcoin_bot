---
phase: 29-layer-2-judge-feedback-rewrite-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/newsletter/judge_loop.py
  - tests/test_29_judge_loop.py
  - config/agentpulse-config.json
autonomous: true
requirements: [JUDGE-01, JUDGE-02, JUDGE-03, JUDGE-04, JUDGE-05]
must_haves:
  truths:
    - "run_layer2(draft, fact_base, prior_context, det_flags, config, llm_client, *, http_client=None, github_token=None) exists; a non-empty det_flags['fabrication'] raises ValueError (JUDGE-01 — Layer 2 must never run on a fabricated draft)"
    - "The Sonnet judge scores BOTH bodies (technical content_markdown + impact content_markdown_impact) 1-5 across the five dimensions continuity/hedging_filler/clickbait/repeated_subtopics/specificity, each with a numeric score + quoted evidence + before/after exemplar, in ONE call per pipeline_version (JUDGE-02, D-08)"
    - "The judge is handed the last 3 editions' angles; continuity scores 1 when the lead has no explicit bridge; when prior_context.empty/no previous_editions the judge scores continuity 'n/a' and _compute_failing_dims EXCLUDES it (JUDGE-03, D-05)"
    - "A judge response missing required evidence/before-after exemplars is schema-rejected, retried ONCE, then eval_status='error' → verdict 'escalated' — never a fabricated 0 (JUDGE-05)"
    - "Per-dimension pass/fail is config-tunable: hedging fails on score<hedging_fail_below OR >=hedging_filler_hits_max deterministic filler hits; clickbait/specificity/repeated_subtopics <fail_below; continuity <continuity_fail_below (=4); a dim fails for the version if EITHER body fails, via min(technical,impact) (JUDGE-04, D-04, D-08)"
    - "parse_llm_json (newsletter_poller:777) is used for every judge parse — NEVER phase_e_voice_check's startswith('```') strip (JUDGE-05, Phase-26 char-0 fix)"
    - "The injected llm_client is documented as REQUIRED to authenticate as the governed edition_eval identity; judge_loop.py never references claude_client or OPENAI_API_KEY (GOV-01, D-09)"
  artifacts:
    - path: "docker/newsletter/judge_loop.py"
      provides: "Module scaffold, run_layer2 signature + entry guard, DEFAULT_* + config merge, DEFAULT_FILLER_BLACKLIST, _count_filler_hits, _compute_failing_dims, the 5-dim judge (_judge_draft + _validate_judge_response) reusing _llm_call + parse_llm_json"
      contains: "def run_layer2"
    - path: "tests/test_29_judge_loop.py"
      provides: "Test harness importing the REAL judge_loop module; _FakeLLM FIFO OpenAI-shape client; reused _FakeHTTPClient/_stub_dns/fixtures from test_28; JUDGE-01/02/03/04/05 cases"
      contains: "import judge_loop as jl"
    - path: "config/agentpulse-config.json"
      provides: "Top-level edition_eval block (thresholds verbatim, continuity_fail_below=4)"
      contains: "edition_eval"
  key_links:
    - from: "docker/newsletter/judge_loop.py"
      to: "docker/newsletter/newsletter_poller.py"
      via: "from newsletter_poller import parse_llm_json"
      pattern: "parse_llm_json"
    - from: "docker/newsletter/judge_loop.py"
      to: "docker/newsletter/block_pipeline.py"
      via: "from block_pipeline import _llm_call"
      pattern: "_llm_call"
    - from: "run_layer2 verdict"
      to: "config edition_eval.thresholds"
      via: "config-tunable per-dimension fail_below (D-04)"
      pattern: "continuity_fail_below"
---

<objective>
Build the input + scoring core of the Layer-2 module: the new pure module `docker/newsletter/judge_loop.py` with the `run_layer2(...)` signature + fail-loud entry guard, the config-merge scaffold, the deterministic filler pre-pass, the five-dimension exemplar-anchored Sonnet judge (score + quoted evidence + before/after exemplar per dimension, both bodies), the JUDGE-05 schema-validate→one-retry→error contract, and the config-tunable per-dimension threshold engine (`_compute_failing_dims`). Also add the `edition_eval` config block. This plan locks the module contract, the judge output schema, and the verdict-input scoring so Plan 02 can build the N=2 loop on a stable base (interface-first).

Purpose: Lock the judge + scoring first. The judge is a generalization of the existing `phase_e_voice_check` from one dimension to five × two bodies, with a strict schema gate (JUDGE-05) and the robust `parse_llm_json` parser (the Phase-26 char-0 fix). The threshold logic (`_compute_failing_dims`) is the pure input to the loop's verdict — building it here means Plan 02's loop just consumes a failing-dimension set. D-04 thresholds land in config now (worktree-safe file edit, no rebuild) with the operator-confirmed `continuity_fail_below=4`.

Output: `judge_loop.py` (scaffold + entry guard + `_count_filler_hits` + `_compute_failing_dims` + `_judge_draft` + `_validate_judge_response` + attempt-0 `run_layer2` returning passed/escalated), `tests/test_29_judge_loop.py` (import preamble, `_FakeLLM`, reused Phase-28 fakes/fixtures, JUDGE-01..05 cases), and the `edition_eval` config block. Build-only, worktree-safe — no supabase, no `edition_evals` write, no live proxy, no container rebuild (D-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-layer-2-judge-feedback-rewrite-loop/29-CONTEXT.md
@.planning/phases/29-layer-2-judge-feedback-rewrite-loop/29-RESEARCH.md
@.planning/phases/29-layer-2-judge-feedback-rewrite-loop/29-PATTERNS.md
@.planning/phases/29-layer-2-judge-feedback-rewrite-loop/29-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config edition_eval block + module scaffold + entry guard + filler pre-pass + threshold engine + test harness (JUDGE-01, JUDGE-04)</name>
  <files>docker/newsletter/judge_loop.py, tests/test_29_judge_loop.py, config/agentpulse-config.json</files>
  <read_first>
    - config/agentpulse-config.json — the `block_pipeline` sibling block at :127-134 (the closest feature-flag + model-params analog) and `wallet_pricing` at :135; add `edition_eval` as a NEW top-level sibling key between them. `edition_eval` is verified ABSENT.
    - docker/newsletter/deterministic_gate.py — run_deterministic_gate signature + fail-loud type guards :93-141 (the ValueError shape to mirror for the entry guard); _fact_base_source_texts :245 (imported by Plan 02, confirm the import path exists).
    - docker/newsletter/newsletter_poller.py — parse_llm_json :777 (the REQUIRED parser), _JSON_FENCE_RE :732, _first_balanced_object :748, MODEL/STRATEGIC_MODEL='claude-sonnet-4-6' :56-57. Read so you import parse_llm_json rather than re-roll it.
    - docker/newsletter/block_pipeline.py — _llm_call :26 (anthropic-vs-OpenAI branch :33 — why the OpenAI-shape mock works), the anthropic guard pattern :18-21.
    - docker/newsletter/edition_eval.py — _get_eval_api_key :43, LLM_PROXY_EVAL_KEY :40 (the governed identity the module DOCUMENTS but does NOT construct — D-09/GOV-01).
    - data/openclaw/agents/newsletter/agent/IDENTITY.md:12-18 — the 12-phrase filler blacklist to seed DEFAULT_FILLER_BLACKLIST verbatim.
    - tests/test_28_deterministic_gate.py — import preamble :34-41 (NL_DIR + import the REAL module), _stub_dns autouse :44-54, fixture builders _make_draft :64, _single_pass_fact_base :77, _block_fact_base :89, _FakeResponse :126, _FakeHTTPClient :135-159 (reuse verbatim for Plan 03's re-check).
    - .planning/phases/29-layer-2-judge-feedback-rewrite-loop/29-RESEARCH.md — §Config Keys :568-594 (the exact edition_eval block + the filler-hit combination rule); §Pattern 1 (signature); §Code Examples 3 (the _FakeLLM shape); §Pitfall 2 (anthropic None → OpenAI-shape mock).
    - .planning/phases/29-layer-2-judge-feedback-rewrite-loop/29-PATTERNS.md — §A (purity + entry guard + keyword-only seam), §D (filler pre-pass), the config-block-to-add.
  </read_first>
  <action>
    Add the top-level `edition_eval` block to `config/agentpulse-config.json` as a NEW sibling key after `block_pipeline` (:134), VERBATIM from RESEARCH §Config Keys with the operator-confirmed `continuity_fail_below: 4`: keys `enabled:false`, `enforce:false`, `max_attempts:2`, `judge_model:"claude-sonnet-4-6"`, `judge_temperature:0.2`, `judge_max_tokens:1500`, `revise_model:"claude-sonnet-4-6"`, `revise_temperature:0.3`, `revise_max_tokens:3000`, a `thresholds` object (`continuity_fail_below:4`, `hedging_fail_below:3`, `hedging_filler_hits_max:3`, `clickbait_fail_below:3`, `repeated_subtopics_fail_below:3`, `specificity_fail_below:3`, `warn_below:4`), and `filler_blacklist:[]`. Preserve valid JSON (the file is machine-read by every service).

    Create `docker/newsletter/judge_loop.py`. Module top: `import json, re, logging`; `from typing import Any`; `from newsletter_poller import parse_llm_json`; `from block_pipeline import _llm_call`; `from deterministic_gate import run_deterministic_gate, _fact_base_source_texts`; `logger = logging.getLogger("newsletter")`. Add a module docstring stating this is a PURE build-only module (D-09): no supabase, no edition_evals write, no live proxy client construction — and that the injected `llm_client` MUST authenticate as the governed `edition_eval` identity (`_get_eval_api_key`, GOV-01); NEVER reference `claude_client` / `OPENAI_API_KEY` here (Pitfall 4).

    Define module constants: `DEFAULT_FILLER_BLACKLIST` seeded VERBATIM from IDENTITY.md:12-18 (the 12 phrases); `_BODIES = [("technical","content_markdown"),("impact","content_markdown_impact")]`; `_DIMENSIONS = ("continuity","hedging_filler","clickbait","repeated_subtopics","specificity")`; and `DEFAULT_CONFIG` mirroring the config block (so a partial/absent `config['edition_eval']` still works — RESEARCH A4). Add `_merged_config(config: dict) -> dict` that deep-merges `config.get("edition_eval", {})` over `DEFAULT_CONFIG` (thresholds sub-dict merged, filler_blacklist falls back to DEFAULT_FILLER_BLACKLIST when empty/absent).

    Define the full `run_layer2(draft: dict, fact_base: dict, prior_context: dict, det_flags: dict, config: dict, llm_client, *, http_client=None, github_token=None) -> dict` signature (D-09/Pattern 1). Implement ONLY the fail-loud entry guard this task (JUDGE-01): `if det_flags.get("fabrication"): raise ValueError("run_layer2 called with a non-empty fabrication list — Layer 1 must short-circuit before Layer 2 (mis-wired caller)")`; also `isinstance` guards on draft/fact_base mirroring deterministic_gate.py:132-141. Leave the loop body as an explicit extension point for Plan 02 (a documented `raise NotImplementedError` is acceptable this task since Task 2 replaces it with the attempt-0 path — do NOT ship a wrong verdict).

    Implement `_count_filler_hits(text: str, blacklist: list[str]) -> int` (D-04): case-insensitive `sum(low.count(phrase.lower()) for phrase in blacklist)`.

    Implement `_compute_failing_dims(judge_scores: dict, filler_hits: dict, cfg: dict, *, continuity_applicable: bool) -> list[str]` (JUDGE-04, D-04/D-05/D-08). It reads the both-bodies judge schema (see the schema contract below) and returns the list of failing dimension names. Per-dim, a dimension fails for the pipeline_version if EITHER body fails → use `min(technical_score, impact_score) < <dim>_fail_below`. Special cases: `continuity` is EXCLUDED entirely when `continuity_applicable` is False (D-05 — never a fail, never counted); `hedging_filler` fails when `min(tech,impact hedging score) < hedging_fail_below` OR `max(tech_filler_hits, impact_filler_hits) >= hedging_filler_hits_max` (the D-04 combination). Mechanical flags are NEVER read here (they never enter `failing` — D-12). Document the judge output SCHEMA CONTRACT as a comment so Task 2's `_judge_draft` produces exactly this shape: `{"technical": {<dim>: {"score": int|"n/a", "evidence": str, "exemplar_before": str, "exemplar_after": str}}, "impact": {...}}`.

    Create `tests/test_29_judge_loop.py`: copy the test_28 import preamble VERBATIM (sys.path.insert NL_DIR, then `import judge_loop as jl` — the REAL module, never re-implemented, per the test_19 rule). Reuse `_stub_dns`, `_make_draft`, `_single_pass_fact_base`, `_block_fact_base`, `_FakeResponse`, `_FakeHTTPClient` from test_28 (import them via `from test_28_deterministic_gate import ...` OR copy the builders — either is fine as long as they are the SAME shapes; the fake httpx is only exercised in Plan 03). Add the NET-NEW `_FakeLLM` FIFO OpenAI-shape client per RESEARCH §Code Examples 3 / PATTERNS §_FakeLLM: `.chat.completions.create(*, model, messages, temperature, max_tokens, **k)` returns canned JSON strings in FIFO order (last one sticky) and records `.calls`. Add canned-judge-JSON builders (a helper that emits a valid both-bodies × 5-dim judge JSON with per-dim scores you pass in). Add the `if __name__ == "__main__": sys.exit(pytest.main([__file__, "-v"]))` footer. Write tests: entry guard (`run_layer2` with `det_flags={"fabrication":[{...}]}` raises ValueError — `guard_and_shortcircuit`); `_count_filler_hits` counts blacklist phrases; `_compute_failing_dims` — filler combination (score>=3 but 3 filler hits → hedging_filler in failing; score 4 + 0 hits → not failing) is `filler_hit_combination`; continuity score 1 → in failing when applicable; continuity `"n/a"` excluded when not applicable; both-bodies min (impact score 2, technical 5 → dim fails).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import json; json.load(open('config/agentpulse-config.json'))" && python3 -c "import ast; ast.parse(open('docker/newsletter/judge_loop.py').read())" && python3 -m pytest tests/test_29_judge_loop.py -k "guard_and_shortcircuit or filler_hit_combination or compute_failing or continuity_na" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; json.load(open('config/agentpulse-config.json'))"` exits 0 and `grep -n '"edition_eval"' config/agentpulse-config.json` matches; `grep -n '"continuity_fail_below": 4' config/agentpulse-config.json` matches (operator-confirmed threshold).
    - `judge_loop.py` contains `def run_layer2(` with params `draft, fact_base, prior_context, det_flags, config, llm_client, *, http_client=None, github_token=None`.
    - `grep -n "from newsletter_poller import parse_llm_json" docker/newsletter/judge_loop.py` matches; `grep -c "startswith('\`\`\`')\|split('\`\`\`')" docker/newsletter/judge_loop.py` returns 0 (the brittle strip is never used).
    - `grep -nc "claude_client\|OPENAI_API_KEY" docker/newsletter/judge_loop.py` returns 0 (governed-identity discipline, Pitfall 4).
    - `run_layer2` with `det_flags={"fabrication":[{"kind":"tier1_entity"}]}` raises `ValueError` (JUDGE-01 guard) — asserted in `guard_and_shortcircuit`.
    - `_compute_failing_dims` returns `hedging_filler` in the failing list when filler hits ≥ `hedging_filler_hits_max` even with score ≥ `hedging_fail_below` (JUDGE-04 combination); continuity `"n/a"` is never in the failing list when `continuity_applicable=False` (D-05); a dim with impact score below fail_below and technical above still fails (D-08 min).
    - `pytest tests/test_29_judge_loop.py -k "guard_and_shortcircuit or filler_hit_combination or compute_failing or continuity_na" -x -q` exits 0.
  </acceptance_criteria>
  <done>The module scaffold, the config block (continuity_fail_below=4), the fail-loud entry guard (JUDGE-01), the deterministic filler pre-pass, and the config-tunable both-bodies threshold engine (JUDGE-04, D-04/D-05/D-08) exist and are proven by tests on the real module; the governed-identity + no-brittle-strip disciplines are grep-verified; the judge output schema contract is documented for Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The 5-dimension exemplar-anchored judge + schema-reject→retry→error + attempt-0 run_layer2 (JUDGE-02, JUDGE-03, JUDGE-05, D-05)</name>
  <files>docker/newsletter/judge_loop.py, tests/test_29_judge_loop.py</files>
  <read_first>
    - docker/newsletter/judge_loop.py — the scaffold from Task 1 (the schema-contract comment, `_merged_config`, `_count_filler_hits`, `_compute_failing_dims`, `_BODIES`, `_DIMENSIONS`, `run_layer2` signature + entry guard).
    - docker/newsletter/block_pipeline.py — phase_e_voice_check :404-441 (the exemplar-anchored, JSON-out, distinguishable `not_scored` template to generalize from 1 dim to 5×2 bodies — temperature 0.2), PHASE_E_PROMPT :378-401, the JSON-strip ANTI-PATTERN :428-432 (DO NOT COPY), the prior-editions render shape :516-525 (reuse for the continuity dimension).
    - docker/newsletter/newsletter_poller.py — parse_llm_json :777-814 (raw→fenced→first-balanced, FAILS LOUD; use context="layer2_judge").
    - .planning/phases/29-layer-2-judge-feedback-rewrite-loop/29-RESEARCH.md — §Pattern 2 (5-dim judge schema + continuity n/a + prompt anchoring + one-call-per-pipeline_version), §Pattern 3 (parse + schema-reject → 1 retry → error), §Pattern 6 (continuity_applicable derivation), §Code Examples 1 (judge prompt skeleton), §Pattern 7 (escalated on error).
    - .planning/phases/29-layer-2-judge-feedback-rewrite-loop/29-PATTERNS.md — §B (the judge template + prior_context.previous_editions render + the schema-reject-on-missing-evidence rule).
  </read_first>
  <action>
    Add `JUDGE_SYSTEM` and `JUDGE_PROMPT` module constants generalizing PHASE_E_PROMPT (RESEARCH §Code Examples 1): the system prompt requires, for EVERY dimension, a numeric score 1-5, a quoted `evidence` sentence from the draft, and a before/after `exemplar_before`/`exemplar_after`; respond ONLY with valid JSON. The user prompt injects the last-3-editions render (reuse the block_pipeline.py:516-525 shape reading `prior_context.previous_editions[*].edition_number/title/opening_excerpt/weeks_ago`), the operator `prior_context.exemplars`, and BOTH bodies (`content_markdown`, `content_markdown_impact`). List the 5 dimensions with their must-catch targets: continuity (lead MUST bridge to a prior edition; absent → 1), hedging_filler, clickbait (professor voice = concept-first, defines terms, no 2nd-person fear hooks), repeated_subtopics (vs last edition), specificity (named entities, dated numbers, a falsifiable prediction). When continuity is NOT applicable, the prompt states "NO prior published editions exist — score continuity as the string \"n/a\" and do not invent a bridge."

    Implement `_build_judge_prompt(draft, prior_context, continuity_applicable, cfg) -> tuple[str,str]` returning `(system, user)`.

    Implement `_validate_judge_response(parsed: dict, *, continuity_applicable: bool) -> None` (JUDGE-05 schema gate): raise a `ValueError` if `parsed` is missing `"technical"` or `"impact"`, or if any of the 5 `_DIMENSIONS` is missing from either body, or if any dimension entry lacks a numeric `score` / non-empty `evidence` / `exemplar_before` / `exemplar_after`. Accept the string `"n/a"` for the `continuity` score ONLY when `continuity_applicable is False`; a numeric continuity score must be 1-5. This enforces "a score without quoted evidence/exemplars is rejected."

    Implement `_judge_draft(draft, prior_context, llm_client, cfg) -> dict` (JUDGE-02, JUDGE-05): compute `continuity_applicable = not (prior_context.get("empty") or not prior_context.get("previous_editions"))` (D-05, Pattern 6). Build the prompt; loop at most twice (1 call + 1 retry): `text = _llm_call(llm_client, cfg["judge_model"], system, user, temperature=cfg["judge_temperature"], max_tokens=cfg["judge_max_tokens"])`; `parsed = parse_llm_json(text, context="layer2_judge")`; `_validate_judge_response(parsed, continuity_applicable=...)`; on success return `{"status":"ok","scores":parsed,"continuity_applicable":continuity_applicable}`. On any exception (parse or schema) record and retry once; after the retry fails return `{"status":"error","error":f"judge schema-invalid after retry: {last_err}"}`. ONE call per pipeline_version scoring both bodies (D-08) — never split per body.

    Complete the attempt-0 path in `run_layer2` (replace the Task-1 extension-point stub): after the entry guard, `cfg = _merged_config(config)`; run `_judge_draft` once on the passed-in draft; if `status=='error'` return `{"final_draft": draft, "verdict": "escalated", "selected_attempt": 0, "attempts": [ ... one error attempt ... ]}` (JUDGE-05 — an error is not evidence; does not hold). Otherwise compute `filler_hits` per body via `_count_filler_hits(draft[body], cfg["filler_blacklist"])`, compute `failing = _compute_failing_dims(scores, filler_hits, cfg, continuity_applicable=...)`; if `failing` is empty return `verdict="passed"`, `selected_attempt=0`. If `failing` is non-empty, leave the revise/loop path as a documented Plan-02 extension point (a `raise NotImplementedError("N=2 rewrite loop — Plan 02")` is acceptable; do NOT emit a wrong verdict). Assemble a minimal attempt-0 telemetry object `{attempt:0, eval_status:"ok"|"error", error, judge_scores, feedback:None, reverify_flags:det_flags, sats:0, model_calls:[...]}` (Plan 03 finalizes the mapping).

    Add tests: `judge_scores_both_bodies` — a canned judge JSON with per-body per-dim scores drives `_judge_draft`; assert both `technical` and `impact` are present with all 5 dims and evidence+exemplars, and that the `_FakeLLM` recorded exactly ONE call (one call per pipeline_version, D-08). `schema_reject_retry_then_error` — queue an invalid judge JSON (missing `evidence`) then a valid one → recovers `ok` after exactly 2 calls; queue two invalids → `run_layer2` returns `verdict="escalated"` (JUDGE-05). `guard_and_shortcircuit` (extend Task-1 test) — a clean all-pass canned judge → `verdict="passed"`, and `_FakeLLM.calls` shows no revise. `continuity_na_excluded` — `prior_context={"empty":True,"previous_editions":[],"exemplars":[]}`, a canned judge scoring continuity `"n/a"` and the other 4 dims ≥4 → `verdict="passed"` (D-05).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && python3 -c "import ast; ast.parse(open('docker/newsletter/judge_loop.py').read())" && python3 -m pytest tests/test_29_judge_loop.py -k "judge_scores_both_bodies or schema_reject_retry_then_error or guard_and_shortcircuit or continuity_na_excluded" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `_judge_draft` produces `{technical:{5 dims}, impact:{5 dims}}` each with numeric `score` + non-empty `evidence`/`exemplar_before`/`exemplar_after`, in exactly ONE `_FakeLLM` call per invocation (JUDGE-02, D-08).
    - A judge response missing `evidence` is rejected by `_validate_judge_response`; `_judge_draft` retries exactly ONCE and returns `status="error"` when both attempts are invalid; `run_layer2` maps that to `verdict="escalated"` (JUDGE-05).
    - A clean all-pass canned judge → `run_layer2` returns `verdict="passed"`, `selected_attempt=0`, and no revise call is made (JUDGE-01 clean path).
    - With `prior_context.empty=True`, a judge scoring continuity `"n/a"` (other dims ≥4) yields `verdict="passed"` — continuity excluded, no bridge fabricated (JUDGE-03/D-05).
    - `grep -nc "startswith('\`\`\`')\|split('\`\`\`')" docker/newsletter/judge_loop.py` returns 0; `grep -n 'context="layer2_judge"' docker/newsletter/judge_loop.py` matches (robust parser used).
    - `pytest tests/test_29_judge_loop.py -k "judge_scores_both_bodies or schema_reject_retry_then_error or guard_and_shortcircuit or continuity_na_excluded" -x -q` exits 0.
  </acceptance_criteria>
  <done>The five-dimension exemplar-anchored judge scores both bodies in one call, enforces the evidence+exemplar schema with a single retry then a fail-loud `escalated` (never a fabricated 0), handles the continuity n/a exclusion (D-05), and drives the attempt-0 passed/escalated verdicts — all proven by tests on the real module using the OpenAI-shape mock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sonnet judge JSON → judge_loop schema gate | The judge output is untrusted LLM text; every dimension's score/evidence/exemplars are validated before use |
| draft prose → judge prompt | Untrusted model-generated draft text is embedded into the judge prompt; never logged raw at INFO |
| config dict → threshold engine | Caller-supplied config; partial/absent `edition_eval` must fall back to DEFAULT_CONFIG, never crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-NOEVID | Repudiation | Judge returns a bare score with no evidence/exemplar (unauditable) | mitigate | `_validate_judge_response` rejects any dimension missing `evidence`/`exemplar_before`/`exemplar_after` → one retry → `eval_status='error'` → `escalated` (JUDGE-05, ASVS V5) |
| T-29-ZERO | Tampering | A silent 0 score masking a failed/unparseable eval | mitigate | Fail-loud: schema-invalid → `escalated` (never a fabricated 0); `parse_llm_json` FAILS LOUD, never silent-empty (ASVS V7) |
| T-29-KEY | Info disclosure | Wrong wallet / leaked key via reusing `claude_client`/`OPENAI_API_KEY` | mitigate | Module documents the injected `llm_client` MUST be the governed `edition_eval` identity; grep-verified zero `claude_client`/`OPENAI_API_KEY` references (GOV-01, Pitfall 4) |
| T-29-LOG | Tampering (log injection) | Logging raw draft/judge prose at INFO | mitigate | Log only labels/counts/verdicts, never raw draft or judge prose blocks at INFO |
| T-29-SC | Tampering | pip installs | accept | Zero new packages this phase (stdlib + four in-repo engines already present per RESEARCH Package Legitimacy Audit); no install task exists |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('docker/newsletter/judge_loop.py').read())"` passes (CLAUDE.md syntax gate).
- `python3 -c "import json; json.load(open('config/agentpulse-config.json'))"` passes (config stays valid JSON).
- `python3 -m pytest tests/test_29_judge_loop.py -x -q` green for all JUDGE-01/02/03/04/05 cases authored this plan.
- No regression: `python3 -m pytest tests/test_26_continuity_loader.py tests/test_27_edition_eval.py tests/test_28_deterministic_gate.py -q` still green (engine imports must not break the existing suite).
- Source assertions: `parse_llm_json` imported (not re-rolled); no `startswith('```')`/`split('```')`; no `claude_client`/`OPENAI_API_KEY`.
</verification>

<success_criteria>
- `run_layer2` exists with the full signature and the fail-loud entry guard (JUDGE-01); the `edition_eval` config block exists with `continuity_fail_below=4`.
- The 5-dimension both-bodies judge scores 1-5 with evidence+exemplars per dim in one call per pipeline_version (JUDGE-02, D-08), reusing `_llm_call` + `parse_llm_json`.
- The judge is handed the last-3-editions angles; continuity=1 when the bridge is absent and n/a-excluded on an empty corpus (JUDGE-03, D-05).
- Schema-invalid judge → one retry → `escalated`, never a fabricated 0 (JUDGE-05).
- Per-dimension config-tunable thresholds + the deterministic filler combination compute the failing-dimension set, both-bodies worst-case via `min` (JUDGE-04, D-04/D-08).
- Build-only: no supabase, no `edition_evals` write, no live proxy, no container rebuild (D-09); all tests import the REAL module with mocked LLM (no egress).
</success_criteria>

<output>
Create `.planning/phases/29-layer-2-judge-feedback-rewrite-loop/29-01-SUMMARY.md` when done.
</output>
