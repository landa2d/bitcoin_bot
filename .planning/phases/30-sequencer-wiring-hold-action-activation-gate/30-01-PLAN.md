---
phase: 30-sequencer-wiring-hold-action-activation-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/046_do_not_publish_columns.sql
  - docker/processor/agentpulse_processor.py
autonomous: true
requirements: [WIRE-02, WIRE-04, WIRE-05]
must_haves:
  truths:
    - "newsletters has first-class do_not_publish + do_not_publish_reason columns (migration 046)"
    - "Both processor publish gates refuse to publish a row whose do_not_publish column is true"
    - "The Processor calls no eval logic — it only reads the column and owns the publish gate"
  artifacts:
    - path: "supabase/migrations/046_do_not_publish_columns.sql"
      provides: "ADD COLUMN do_not_publish boolean NOT NULL DEFAULT false + do_not_publish_reason text on newsletters"
      contains: "do_not_publish boolean NOT NULL DEFAULT false"
    - path: "docker/processor/agentpulse_processor.py"
      provides: "do_not_publish column read/guard in publish_newsletter + scheduled_auto_publish_newsletter"
      contains: "do_not_publish"
  key_links:
    - from: "docker/processor/agentpulse_processor.py:publish_newsletter"
      to: "newsletters.do_not_publish"
      via: "in-Python guard on the fetched draft row"
      pattern: "get\\(['\"]do_not_publish"
---

<objective>
Add the net-new hold-state persistence surface and the publish-gate defense-in-depth that the poller's verdict→action step (Plan 30-03) writes through, WITHOUT putting any eval logic in the Processor.

Two deliverables:
1. Migration `046_do_not_publish_columns.sql` (SQL-first, operator-applied via MCP) adds `do_not_publish boolean NOT NULL DEFAULT false` + `do_not_publish_reason text` as first-class columns on `newsletters` (D-01). Today `do_not_publish` lives only inside `data_snapshot` JSONB — this makes it queryable/structural.
2. The two Processor publish gates (`publish_newsletter`, `scheduled_auto_publish_newsletter`) read the new column and refuse to publish a held row — structural belt-and-suspenders behind the existing `status='held'` exclusion, so a mis-statused-but-held row still cannot ship (D-01/D-11, WIRE-04). The Processor stays a dumb sequencer: no LLM, no eval calls, no retry state (WIRE-05).

Purpose: give the hold action exactly ONE canonical, queryable home (the column) and make the publish gate honor it.
Output: migration 046 + two guarded publish gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-CONTEXT.md
@.planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-PATTERNS.md
@supabase/migrations/020_newsletter_verification.sql
@supabase/migrations/045_edition_evals.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 046 — do_not_publish columns on newsletters</name>
  <files>supabase/migrations/046_do_not_publish_columns.sql</files>
  <read_first>
    - supabase/migrations/020_newsletter_verification.sql (the ADD COLUMN IF NOT EXISTS + COMMENT ON COLUMN house style — copy this shape)
    - supabase/migrations/045_edition_evals.sql lines 1-21 (the SQL-FIRST / MCP-apply / "do NOT run supabase db push, do NOT apply from a worktree" header block — copy this operator-runbook header in spirit)
    - .planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-PATTERNS.md ("046 migration" section — the exact two columns + backfill discretion)
  </read_first>
  <action>
    Create `supabase/migrations/046_do_not_publish_columns.sql` following the migration-020 house style verbatim (re-apply-safe, idempotent). Add a top comment header naming the migration ("Migration 046: first-class do_not_publish + do_not_publish_reason columns on newsletters — the single canonical home for hold state, D-01/WIRE-02") plus the SQL-FIRST operator-apply banner adapted from 045 (operator applies via MCP after DDL review, project ref zxzaaqfowtqvmsbitqpu; do NOT run `supabase db push`; do NOT apply from a worktree). Then a single `ALTER TABLE newsletters` that `ADD COLUMN IF NOT EXISTS do_not_publish boolean NOT NULL DEFAULT false` and `ADD COLUMN IF NOT EXISTS do_not_publish_reason text`. Add a `COMMENT ON COLUMN` for each: do_not_publish = "hard hold flag; the publish gate refuses to ship a row with this true (Phase 30 eval hold action)"; do_not_publish_reason = "human-readable reason the eval held this edition (fabrication categories / failing judge dimensions)". Backfill decision (D-13 discretion): leave historical A/B shadow rows as-is — `DEFAULT false` covers new rows and the always-`held` A/B rows are already excluded by status; do NOT write a data-migration backfill (low-risk, avoids touching historical rows). This file is authored now (worktree-safe); the APPLY is an operator-owned MCP step in the Plan 30-04 activation runbook — NOT this task.
  </action>
  <verify>
    <automated>test -f supabase/migrations/046_do_not_publish_columns.sql && grep -q "do_not_publish boolean NOT NULL DEFAULT false" supabase/migrations/046_do_not_publish_columns.sql && grep -q "ADD COLUMN IF NOT EXISTS do_not_publish_reason text" supabase/migrations/046_do_not_publish_columns.sql && grep -qi "do NOT run" supabase/migrations/046_do_not_publish_columns.sql && echo GATE-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `046_do_not_publish_columns.sql` exists and contains the exact string `do_not_publish boolean NOT NULL DEFAULT false`.
    - It contains `ADD COLUMN IF NOT EXISTS do_not_publish_reason text`.
    - It uses `ADD COLUMN IF NOT EXISTS` (re-apply-safe) for BOTH columns (grep-count of `ADD COLUMN IF NOT EXISTS` == 2 on non-comment lines).
    - It carries a `COMMENT ON COLUMN` for both new columns.
    - It carries the SQL-FIRST / do-not-`supabase db push` / do-not-apply-from-worktree operator banner.
    - No `INSERT`/`UPDATE`/`DELETE` data-migration statements (schema-only; grep -v '^--' shows zero DML).
  </acceptance_criteria>
  <done>Migration 046 authored in the correct house style, schema-only, operator-apply-banner present; the file is committed but NOT applied (apply is the 30-04 runbook).</done>
</task>

<task type="auto">
  <name>Task 2: Guard both Processor publish gates on the do_not_publish column (D-01/D-11, WIRE-04/WIRE-05)</name>
  <files>docker/processor/agentpulse_processor.py</files>
  <read_first>
    - docker/processor/agentpulse_processor.py lines 5858-5915 (publish_newsletter: the `select('*').in_('status', [...])` fetch → `newsletter = draft.data[0]` → verify → send/flip)
    - docker/processor/agentpulse_processor.py lines 10629-10666 (scheduled_auto_publish_newsletter: the narrow `select('id, edition_number, created_at')` fetch + the <1h freshness guard)
    - .planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-PATTERNS.md ("agentpulse_processor.py — the publish gate" section: `.in_('status', ...)` on public.newsletters is the established working idiom — do NOT "fix" it; add a column guard, not an economy_map-style change)
  </read_first>
  <action>
    Add a structural do_not_publish guard to BOTH publish gates, reading the column via in-Python `.get('do_not_publish', False)` so the code is robust to migration-046 apply ordering (an absent column → `.get` default False → publishes as before; a present-and-true column → held). (a) In `publish_newsletter` (fetch is already `select('*')`, so the column is present once 046 is applied): immediately after `newsletter = draft.data[0]`, if `newsletter.get('do_not_publish')` is truthy → log a WARNING naming ONLY the edition number + a fixed label (never the raw do_not_publish_reason prose at INFO/WARNING — log-injection mitigation, T-30-LOG) and `return {'error': 'held: do_not_publish set'}` WITHOUT sending Telegram/email or flipping status. (b) In `scheduled_auto_publish_newsletter`, widen the narrow select to `select('*')` (ordering-agnostic — avoids referencing a column that may not yet exist) and add the same `.get('do_not_publish')` guard immediately after `newsletter = draft.data[0]`: log a WARNING (edition number + label only) and `return` without calling `publish_newsletter()`. Keep the existing `.in_('status', [...])` filters and the <1h freshness guard unchanged. Do NOT import or call any eval module, run_edition_eval, run_layer2, or run_deterministic_gate here — the Processor stays a dumb sequencer (WIRE-05); this task adds a column read ONLY. Syntax-check with `python3 -c "import ast; ast.parse(...)"` before finishing.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())" && grep -c "get('do_not_publish'" docker/processor/agentpulse_processor.py | grep -q 2 && [ "$(grep -c 'run_layer2\|run_deterministic_gate\|run_edition_eval\|import edition_eval\|import judge_loop' docker/processor/agentpulse_processor.py)" = "0" ] && echo GATE-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"` exits 0.
    - `publish_newsletter` returns early (no send, no status flip) when the fetched row's `do_not_publish` is truthy — source shows `get('do_not_publish'` guard immediately after `newsletter = draft.data[0]` in that function.
    - `scheduled_auto_publish_newsletter` selects `'*'` and has the same guard before calling `publish_newsletter()`.
    - `grep -c "get('do_not_publish'" docker/processor/agentpulse_processor.py` == 2.
    - `grep -c "run_layer2\|run_deterministic_gate\|run_edition_eval\|import edition_eval\|import judge_loop" docker/processor/agentpulse_processor.py` == 0 (WIRE-05: Processor stays dumb — no eval logic).
    - The `.in_('status', [...])` filters are unchanged (guard is additive, not a rewrite of the status exclusion).
  </acceptance_criteria>
  <done>Both publish gates read the do_not_publish column and refuse a held row; the Processor contains no eval/LLM/retry logic; AST parse clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval verdict → newsletters.do_not_publish column | Untrusted eval output becomes the publish-gate authority; must be read structurally, not inferred from status alone |
| newsletters row → Processor logs | do_not_publish_reason is built from draft/verdict data (untrusted for log-injection) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-EOP | Elevation of Privilege | publish gate bypass | mitigate | Publish gates read `do_not_publish` column via in-Python guard AND keep the `status='held'` exclusion — a held row cannot ship even if mis-statused (D-01) |
| T-30-LOG | Information disclosure / log-injection | Processor WARNING lines | mitigate | Log ONLY the edition number + a fixed label on a held row; never emit the raw `do_not_publish_reason` prose at INFO/WARNING |
| T-30-FAILOPEN | Tampering | fail-open-to-PUBLISH | accept | This plan adds NO fail-open path in the Processor; the fail-open (eval error → continue drafting) lives in the poller (30-02) and never flips publish state |
| T-30-SC | Tampering | package installs | accept | No npm/pip/cargo installs this phase — all modules pre-built; httpx/anthropic already in the images. No supply-chain surface. |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"` exits 0.
- Migration 046 present with both columns + operator banner; schema-only.
- Processor eval-ref count is 0 (WIRE-05 baseline preserved).
- The migration is NOT applied by this plan (apply is the 30-04 operator runbook).
</verification>

<success_criteria>
- `newsletters` gains do_not_publish + do_not_publish_reason via authored migration 046 (D-01, WIRE-02).
- Both Processor publish gates refuse a do_not_publish=true row (D-01/D-11, WIRE-04).
- The Processor still calls no eval logic (WIRE-05).
</success_criteria>

<output>
Create `.planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-01-SUMMARY.md` when done.
</output>
