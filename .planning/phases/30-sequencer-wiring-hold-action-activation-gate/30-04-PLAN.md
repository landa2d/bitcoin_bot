---
phase: 30-sequencer-wiring-hold-action-activation-gate
plan: 04
type: execute
wave: 3
depends_on: [30-01, 30-02, 30-03]
files_modified: []
autonomous: false
requirements: [WIRE-06]
user_setup:
  - service: supabase
    why: "Apply migrations 045 (edition_evals + governed agent) and 046 (do_not_publish columns) to the live DB via MCP"
    dashboard_config:
      - task: "MCP-apply migration 045_edition_evals.sql (after bcrypt hash substitution) then 046_do_not_publish_columns.sql"
        location: "Supabase MCP tool (project ref zxzaaqfowtqvmsbitqpu) — NOT supabase db push, NOT from a worktree"
  - service: llm-proxy (edition_eval agent)
    why: "Mint the governed edition_eval proxy key; write LLM_PROXY_EVAL_KEY so the newsletter service authenticates the eval as the governed identity"
    env_vars:
      - name: LLM_PROXY_EVAL_KEY
        source: "Mint ap_edition_eval_<hash> in 27-03; write to config/.env (never compose)"
must_haves:
  truths:
    - "Migrations 045 + 046 are applied to the live DB via MCP (not supabase db push)"
    - "LLM_PROXY_EVAL_KEY is set in config/.env and a settled governed edition_eval proxy call is verified"
    - "edition_eval.enabled is flipped true with enforce=false (report-only window) only after code + schema + key are live"
    - "The step is rollback-safe: setting enabled=false disables invocation"
  artifacts:
    - path: ".planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-04-SUMMARY.md"
      provides: "the executed activation runbook record (which steps ran, verification evidence)"
  key_links:
    - from: "config/.env:LLM_PROXY_EVAL_KEY"
      to: "newsletter_poller._build_eval_llm_client"
      via: "governed edition_eval identity read at generation time"
      pattern: "LLM_PROXY_EVAL_KEY"
---

<objective>
The explicit, operator-owned LIVE-ACTIVATION runbook (D-04). Phase 30's "done" is the worktree-safe wiring shipped `enabled=false` with no live edition required (D-03; Plans 30-01/02/03) — this plan ARMS it, and every step here is orchestrator/operator-owned on the MAIN TREE (worktree-UNSAFE): MCP migration applies and scoped `docker compose up -d --build` rebuilds build from the absolute main-tree path, so a worktree executor would apply/build stale artifacts.

Sequence (D-04): (1) complete the still-pending Phase 27-03 — mint the `edition_eval` proxy key, substitute the real bcrypt hash into `045_edition_evals.sql` §2, write `LLM_PROXY_EVAL_KEY` to `config/.env`; (2) MCP-apply migration 045 (edition_evals table + governed agent) AND migration 046 (do_not_publish columns); (3) verify a SETTLED governed `edition_eval` proxy call (the first live prerequisite); (4) scoped-rebuild `newsletter` + `processor` on the main tree; (5) flip `edition_eval.enabled=true`, keep `enforce=false` → the report-only calibration window (~first 2 editions); (6) LATER, after calibration, the operator flips `enforce=true` to arm auto-hold.

Purpose: turn on the dormant gate safely, calibrate against real drafts before any auto-hold fires (WIRE-06), rollback-safe throughout.
Output: a live, report-only eval on the two save points + this runbook record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-CONTEXT.md
@.planning/phases/29-layer-2-judge-feedback-rewrite-loop/29-03-SUMMARY.md
@supabase/migrations/045_edition_evals.sql
@supabase/migrations/046_do_not_publish_columns.sql
@config/agentpulse-config.json
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Mint the edition_eval key + substitute bcrypt hash + deliver LLM_PROXY_EVAL_KEY (Phase 27-03 prerequisite)</name>
  <read_first>
    - supabase/migrations/045_edition_evals.sql (SECTION 2: the governed agent seed with the `<bcrypt-hash>` placeholder to substitute)
    - .planning/phases/29-layer-2-judge-feedback-rewrite-loop/29-03-SUMMARY.md ("STILL PENDING" note: 27-03 key mint is the prerequisite for the FIRST live Phase-30 invocation)
    - docker/newsletter/edition_eval.py lines 40-52 (_get_eval_api_key reads LLM_PROXY_EVAL_KEY from env)
  </read_first>
  <what-built>
    Plans 30-01/02/03 shipped the wiring `enabled=false`: `run_edition_eval` builds the governed client from `_get_eval_api_key()` (`LLM_PROXY_EVAL_KEY`). That key does not exist yet — 27-03 was never executed.
  </what-built>
  <how-to-verify>
    Operator/orchestrator steps (main tree only):
    1. Mint `ap_edition_eval_<hash>` for the `edition_eval` agent (the proxy key-mint flow).
    2. Substitute the real bcrypt hash into `supabase/migrations/045_edition_evals.sql` SECTION 2 (replace the `<bcrypt-hash>` placeholder).
    3. Write `LLM_PROXY_EVAL_KEY=ap_edition_eval_<...>` into `config/.env` (NEVER into docker-compose.yml — the newsletter container reads it via `env_file: config/.env`).
    4. Confirm the key is NOT committed to git (config/.env is gitignored) and NOT echoed into any log.
  </how-to-verify>
  <acceptance_criteria>
    - `045_edition_evals.sql` SECTION 2 no longer contains the literal `<bcrypt-hash>` placeholder (a real bcrypt hash is in place).
    - `grep -q LLM_PROXY_EVAL_KEY config/.env` succeeds; `git check-ignore config/.env` confirms it is gitignored (key never committed).
    - `LLM_PROXY_EVAL_KEY` does NOT appear in docker-compose.yml.
  </acceptance_criteria>
  <resume-signal>Type "key minted" once the hash is substituted into 045 §2 and LLM_PROXY_EVAL_KEY is in config/.env; or describe blockers.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: MCP-apply migrations 045 + 046 (operator, main tree)</name>
  <read_first>
    - supabase/migrations/045_edition_evals.sql (edition_evals table + governed agent; header says operator applies via MCP, not supabase db push)
    - supabase/migrations/046_do_not_publish_columns.sql (do_not_publish + do_not_publish_reason columns)
    - CLAUDE.md ("Apply Supabase migration (use MCP tool)" — project ref zxzaaqfowtqvmsbitqpu)
  </read_first>
  <what-built>
    Migration 045 (Phase 27) and migration 046 (Plan 30-01) are authored SQL-first, unapplied. The poller writes `edition_evals` rows and (when enforce) the `do_not_publish` column — both require the applied schema.
  </what-built>
  <how-to-verify>
    Via the Supabase MCP tool (project ref zxzaaqfowtqvmsbitqpu), on the MAIN TREE only — NEVER `supabase db push`, NEVER from a worktree:
    1. Apply `045_edition_evals.sql` (after the Task-1 bcrypt substitution).
    2. Apply `046_do_not_publish_columns.sql`.
    3. 046 must be applied BEFORE the processor scoped rebuild (Task 4) so the widened publish-gate select is column-safe.
  </how-to-verify>
  <acceptance_criteria>
    - `list_tables` shows `edition_evals` with the `verdict-iff-ok` CHECK + UNIQUE(newsletter_id, layer, attempt).
    - `agent_registry` + `agent_wallets_v2` each have an `edition_eval` row (allow_negative=false, on_cap_behavior='reject', uncapped=false).
    - `newsletters` has `do_not_publish` (boolean NOT NULL DEFAULT false) + `do_not_publish_reason` (text) columns.
    - Both migrations applied via MCP (not `supabase db push`), on the main tree.
  </acceptance_criteria>
  <resume-signal>Type "migrations applied" with confirmation both tables/columns exist; or report the MCP error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Verify a settled governed edition_eval proxy call</name>
  <read_first>
    - .planning/STATE.md (memory: "verify a governed cycle (settled call) not container-up"; proxy spend lands in wallet_transactions batched, decrement the edition_eval wallet)
    - config/agentpulse-config.json → edition_eval (judge_model claude-sonnet-4-6 via /anthropic/v1/messages)
  </read_first>
  <what-built>
    The governed `edition_eval` agent + key now exist; before arming, prove the identity authenticates and settles through the proxy (the first live prerequisite, D-04 step 3).
  </what-built>
  <how-to-verify>
    1. From the host (or newsletter container), issue ONE minimal `claude-sonnet-4-6` call through `http://llm-proxy:8200/anthropic/v1/messages` (or host `127.0.0.1:8200`) authenticating with `LLM_PROXY_EVAL_KEY`.
    2. Confirm HTTP 200 and that the call SETTLED — a `wallet_transactions` row (transaction_type='llm_call') for `edition_eval` and a decrement of the `edition_eval` wallet balance.
  </how-to-verify>
  <acceptance_criteria>
    - The proxy call returns HTTP 200 authenticating as `LLM_PROXY_EVAL_KEY` (not 401/402).
    - A `wallet_transactions` row (transaction_type='llm_call') exists for `edition_eval` AND the `agent_wallets_v2.edition_eval` balance decremented.
    - A 402/cap-hit or 401 → HALT and fix the agent/key/wallet seed before proceeding (do not arm on a mis-seeded identity).
  </acceptance_criteria>
  <resume-signal>Type "settled call verified" with the wallet decrement evidence; or report the proxy/auth failure.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: Scoped-rebuild newsletter + processor on the main tree (worktree-UNSAFE)</name>
  <read_first>
    - .planning/STATE.md (memory: scoped rebuild → worktree-unsafe; `docker compose up -d --build` cd's to the absolute main-tree path)
    - CLAUDE.md (Build & Run: `cd /root/bitcoin_bot/docker && docker compose up -d --build <service>`)
  </read_first>
  <what-built>
    The Plan 30-01 processor guard and the Plan 30-02/03 poller wiring are in the repo but not in the running images.
  </what-built>
  <how-to-verify>
    On the MAIN TREE (`/root/bitcoin_bot/docker`), operator-owned:
    1. `cd /root/bitcoin_bot/docker && docker compose up -d --build newsletter processor`.
    2. Confirm both containers are healthy and the newsletter poller process is running.
    3. Migration 046 must already be applied (Task 2) so the processor's widened publish-gate select is column-safe.
  </how-to-verify>
  <acceptance_criteria>
    - `docker compose ps` shows `agentpulse-newsletter` + `agentpulse-processor` healthy after the rebuild.
    - The newsletter poller process is running (healthcheck `pgrep -f newsletter_poller.py` passes).
    - The rebuild ran on the main tree (not a worktree), after migration 046 was applied.
  </acceptance_criteria>
  <resume-signal>Type "rebuilt" with both services healthy; or report the build/health failure.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 5: Flip enabled=true, keep enforce=false (report-only window) — WIRE-06</name>
  <read_first>
    - config/agentpulse-config.json → edition_eval (enabled:false, enforce:false today)
    - .planning/REQUIREMENTS.md line ~95 (Activation note: report-only for the first ~2 editions, then flip enforce)
  </read_first>
  <decision>Arm the eval in REPORT-ONLY mode now, or hold until the next generation window?</decision>
  <context>
    `enabled` gates invocation; `enforce` gates the auto-hold status flip (D-15). Flipping `enabled=true` with `enforce=false` runs the full eval on both save points, writes edition_evals rows, and surfaces "would-have-held" alerts — but flips NO status (report-only calibration). This is the WIRE-06 human gate: calibrate thresholds against ~2 real drafts BEFORE any auto-hold fires. Rollback = set `enabled=false`.
  </context>
  <options>
    <option id="arm-report-only">
      <name>Flip edition_eval.enabled=true, enforce=false now</name>
      <pros>Starts the report-only calibration window; verdicts recorded + surfaced; zero publish-state risk (no flips)</pros>
      <cons>Adds ~2 governed eval passes of LLM spend per edition (within the hard-capped wallet)</cons>
    </option>
    <option id="hold">
      <name>Keep enabled=false for now</name>
      <pros>Fully dormant; no eval spend</pros>
      <cons>No calibration data accumulates; arming deferred</cons>
    </option>
  </options>
  <acceptance_criteria>
    - On `arm-report-only`: `config/agentpulse-config.json → edition_eval.enabled=true` AND `enforce=false`.
    - After the next generation: 1–2 `edition_evals` rows per draft with `eval_status='ok'`.
    - NO newsletter row had its status flipped to `held` by the eval (report-only — verify no eval-driven status change).
  </acceptance_criteria>
  <resume-signal>Select: arm-report-only (then confirm edition_evals rows appear, no status flip) or hold.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 6: (Later) flip enforce=true to arm auto-hold — after calibration</name>
  <read_first>
    - .planning/REQUIREMENTS.md (verdict→action mapping + the "operator explicitly flips enforce:true" activation note)
    - the accumulated edition_evals rows from the report-only window (verdict distribution vs the config thresholds)
  </read_first>
  <decision>After ~2 report-only editions, arm auto-hold (enforce=true), or tune thresholds first?</decision>
  <context>
    This is the final, deliberate arming: with `enforce=true`, a `held_fabrication`/`held_voice` verdict on the PRIMARY draft flips `status='held'` + `do_not_publish=true` and the publish gates (Plan 30-01) refuse it. Do this ONLY after reviewing the report-only verdicts against real drafts and tuning `agentpulse-config.json → edition_eval.thresholds` if the "would-have-held" calls looked wrong. Rollback = set `enforce=false` (or `enabled=false`).
  </context>
  <options>
    <option id="arm-enforce">
      <name>Set enforce=true</name>
      <pros>Auto-hold live — fabrication/voice failures are held + escalated before the Monday gate</pros>
      <cons>A mis-tuned threshold could hold a good edition; mitigated by the report-only calibration + the human gate</cons>
    </option>
    <option id="tune-first">
      <name>Tune thresholds, stay report-only</name>
      <pros>More calibration before any auto-hold</pros>
      <cons>Auto-hold not yet active</cons>
    </option>
  </options>
  <acceptance_criteria>
    - On `arm-enforce`: `edition_eval.enforce=true` in config.
    - The next edition that trips a fabrication/voice fail: the primary row flips `status='held'` + `do_not_publish=true` + a reason, an "[EVAL HELD]" operator alert fires, and the publish gates refuse it.
    - A `passed` edition still reaches the normal Monday human gate (no auto-publish, WIRE-04).
  </acceptance_criteria>
  <resume-signal>Select: arm-enforce (confirm a held edition is blocked + a pass still reviewed) or tune-first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM_PROXY_EVAL_KEY → config/.env / git | The governed secret; must never be committed, logged, or placed in compose |
| unapplied SQL → live DB (MCP) | Schema changes reach production; must be reviewed + operator-applied, never auto-pushed |
| enforce flag → live publish state | Arming auto-hold; only after report-only calibration + the human gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-KEYMINT | Information disclosure | LLM_PROXY_EVAL_KEY delivery | mitigate | Key written to config/.env (gitignored), never compose/log; bcrypt hash (not the plaintext key) in migration 045 §2 |
| T-30-MIGRATE | Tampering | migration apply | mitigate | 045/046 applied via MCP after DDL review on the main tree — never `supabase db push`, never from a worktree; verified via list_tables |
| T-30-ARM | Elevation of Privilege | enforce=true auto-hold | mitigate | Two-stage arming: enabled→report-only calibration (~2 editions) BEFORE enforce=true; both flips are blocking human checkpoints; rollback = enabled=false |
| T-30-STALE | Tampering | worktree-stale build | mitigate | newsletter+processor scoped rebuild is operator-owned on the main tree (worktree-unsafe); 046 applied before the processor rebuild |
| T-30-SC | Tampering | package installs | accept | No new package installs — activation applies existing migrations + flips config flags; no supply-chain surface |
</threat_model>

<verification>
- Migrations 045 + 046 applied via MCP; edition_evals table + governed agent + do_not_publish columns exist.
- LLM_PROXY_EVAL_KEY in config/.env; a settled governed edition_eval proxy call verified (wallet decrement).
- newsletter + processor rebuilt on the main tree, healthy.
- enabled=true / enforce=false produces edition_evals rows with no status flip (report-only); enforce=true (later) holds a failing edition and leaves a pass to the human gate.
</verification>

<success_criteria>
- The eval is live in report-only mode, rollback-safe (WIRE-06); auto-hold arms only after calibration.
- No key/migration/rebuild step ran from a worktree; all operator/orchestrator-owned on the main tree.
</success_criteria>

<output>
Create `.planning/phases/30-sequencer-wiring-hold-action-activation-gate/30-04-SUMMARY.md` when done (record which steps ran + verification evidence).
</output>
