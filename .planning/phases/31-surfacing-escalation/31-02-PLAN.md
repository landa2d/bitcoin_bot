---
phase: 31-surfacing-escalation
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - docker/processor/agentpulse_processor.py
  - tests/test_31_notify_eval.py
autonomous: true
requirements: [SURF-02]
must_haves:
  truths:
    - "The Friday newsletter notify appends a per-draft eval section covering BOTH pipeline_versions — single_pass primary leads, block_v1 telemetry follows (D-05)"
    - "Each draft block shows verdict + final-attempt per-dimension judge scores + fabrication/unverified/mechanical flag counts + attempts used; the mechanical count is ALWAYS shown even on a passed verdict (D-06, honoring P29 D-12)"
    - "A pipeline_version with no eval rows renders an explicit '⚠ no eval recorded for this draft' line — the eval section is NEVER omitted (D-07, NULL != intent)"
    - "While enforce=false, a held_fabrication/held_voice verdict renders '⚠ WOULD HAVE HELD (report-only)' at the top of that draft's block (D-08)"
    - "The eval rows are read via a local .eq()-only select on edition_evals (no .in_(), no LLM in the Processor)"
    - "The Friday notify is a hold/eval-critical caller that checks send_telegram's bool return and CRITICAL-logs on delivery failure (D-03)"
  artifacts:
    - path: "docker/processor/agentpulse_processor.py"
      provides: "_read_edition_evals (.eq()-only), _format_notify_eval_section (pure formatter), extended scheduled_notify_newsletter with the eval section + critical-caller return check"
      contains: "def _format_notify_eval_section"
    - path: "tests/test_31_notify_eval.py"
      provides: "formatter coverage: D-06 mechanical-on-passed, D-07 no-eval line, D-08 would-have-held; .eq()-only select proven structurally"
      contains: "def test_"
  key_links:
    - from: "scheduled_notify_newsletter"
      to: "edition_evals"
      via: "_read_edition_evals(supabase, edition_number) .eq()-only"
      pattern: "_read_edition_evals"
    - from: "scheduled_notify_newsletter"
      to: "send_telegram"
      via: "bool return checked, CRITICAL-log on False"
      pattern: "if not send_telegram"
---

<objective>
SURF-02: extend the Friday 12:00 UTC newsletter notify (`scheduled_notify_newsletter`, today a static one-liner) with a compact per-draft eval summary read from `edition_evals` via a plain `.eq()`-only select — no LLM, no retry state (the Processor stays a dumb sequencer). The notify becomes the operator's calibration instrument: it makes the report-only would-have-held verdicts legible so the operator can decide when to flip `enforce=true` without querying the DB.

Purpose: surface the eval verdicts the operator needs weekly, in the message they already receive, with a missing eval distinguishable from a passing one (fail-loud on the read surface) and the mechanical Layer-1 flags visible even on a `passed` verdict (the operator-flagged P29 D-12 dependency).
Output: two small local functions (`_read_edition_evals` `.eq()`-only, `_format_notify_eval_section` pure) + an extended `scheduled_notify_newsletter` that checks send_telegram's return; a unit test suite proving D-06/D-07/D-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-surfacing-escalation/31-CONTEXT.md
@.planning/phases/31-surfacing-escalation/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a local .eq()-only edition_evals reader + a pure notify eval-section formatter</name>
  <read_first>
    - docker/newsletter/edition_eval.py (READ `read_evals_by_newsletter` :186-195 and `read_eval_trend` :198-217 — the .eq()-only read semantics to MIRROR; `write_eval_row` :136-183 for the persisted row shape: `layer` deterministic|judge, `attempt` 0/1/2, `eval_status` ok|error, `verdict`, `deterministic_flags` JSONB, `judge_scores` JSONB, `judge_feedback`, `sats_spent`. This file is NOT importable across containers — re-implement locally)
    - docker/newsletter/judge_loop.py (READ ~:55-171 — the exact `judge_scores` shape: `{ "technical": {dim: {"score": int|"n/a", "evidence", "exemplar_before", "exemplar_after"}}, "impact": {...} }`; the five `_DIMENSIONS` = continuity, hedging_filler, clickbait, repeated_subtopics, specificity; the DEFAULT_CONFIG fail-below thresholds continuity=4, hedging=?, clickbait=3, repeated_subtopics=3, specificity=3 and `hedging_filler_hits_max=3`; `_compute_failing_dims` excludes continuity when score=="n/a")
    - supabase/migrations/045_edition_evals.sql (READ the DDL — columns `edition_number`, `pipeline_version` CHECK IN ('single_pass','block_v1'), `deterministic_flags` `{fabrication,unverified,mechanical,...}`, `UNIQUE(newsletter_id, layer, attempt)`)
    - docker/processor/agentpulse_processor.py (READ where module-level helpers live and how `supabase` global is referenced; `scheduled_auto_publish_newsletter` ~:10650 for the newsletters row-lookup pattern)
    - tests/test_27_edition_eval.py (READ ~:54-146 — the in-memory `StubSupabase`/`_StubQuery` double whose `_StubQuery` defines NO in-list method, so an accidental `.in_()` raises AttributeError, structurally proving `.eq()`-only)
  </read_first>
  <action>
    Add two module-level functions to `docker/processor/agentpulse_processor.py`. (1) `_read_edition_evals(supabase, edition_number) -> list` mirroring `read_evals_by_newsletter` semantics but keyed on the edition: `supabase.table("edition_evals").select("*").eq("edition_number", edition_number).order("pipeline_version").order("attempt").execute()`, returning `result.data or []`. This one read returns ALL rows for the edition across BOTH pipeline_versions and both layers/attempts (edition_evals carries `edition_number` + `pipeline_version` columns). MUST be `.eq()`-only — the `/newsletter_preview` `.in_()` and `scheduled_auto_publish_newsletter`'s `.in_('status')` are the anti-pattern for eval reads. (2) `_format_notify_eval_section(eval_rows, enforce) -> str` — a PURE function (no supabase, no LLM) that groups `eval_rows` by `pipeline_version`, renders single_pass first then block_v1 (D-05), and for each pipeline_version builds a ~5-6 line block: pick the deterministic-layer row (layer=='deterministic') for the `deterministic_flags` counts `len(fabrication)/len(unverified)/len(mechanical)` (mechanical count ALWAYS printed, even on passed — D-06); pick the highest-`attempt` judge-layer row (layer=='judge') for the final verdict + per-dimension scores (render each of the 5 dims as `dim: min(technical.score, impact.score)`), and `attempts used` = max judge attempt + 1. When a pipeline_version has NO rows in `eval_rows`, emit the literal line `⚠ no eval recorded for this draft` (D-07) — never omit the block. When the effective verdict is `held_fabrication` or `held_voice` AND `enforce` is False, prepend `⚠ WOULD HAVE HELD (report-only)` at the TOP of that block (D-08); when `enforce` is True, render the held verdict plainly. Render labels/counts/scores ONLY — never raw draft prose, evidence text, or exemplars (that bounded detail belongs to `/newsletter_eval` in plan 31-03; the notify stays compact, T-30-LOG).
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())" && python3 -m pytest tests/test_31_notify_eval.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _read_edition_evals" docker/processor/agentpulse_processor.py` and `grep -n "def _format_notify_eval_section" docker/processor/agentpulse_processor.py` each return one match
    - `_read_edition_evals` uses `.eq("edition_number"` and contains NO `.in_(` (grep the function region)
    - A test builds `StubSupabase` (from the test_27 pattern, `_StubQuery` lacking an in-list method) and asserts `_read_edition_evals(stub, 103)` returns the seeded rows AND that the query used `.eq` (an accidental `.in_` would raise AttributeError)
    - Formatter tests assert: a `passed` single_pass draft still prints a `mechanical` count line (D-06); a pipeline_version with no rows prints `⚠ no eval recorded for this draft` (D-07); a `held_voice` verdict with `enforce=False` prints `⚠ WOULD HAVE HELD (report-only)` at the top of its block, and with `enforce=True` prints the plain held verdict (D-08); single_pass renders before block_v1 (D-05)
    - The formatter output contains NO evidence/exemplar prose keys (grep the rendered string in-test for absence of `exemplar_before`/`evidence` values)
  </acceptance_criteria>
  <done>A `.eq()`-only edition-keyed reader and a pure compact formatter exist and are proven to satisfy D-05/D-06/D-07/D-08 with labels/counts only.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the eval section into scheduled_notify_newsletter + critical-caller return check</name>
  <read_first>
    - docker/processor/agentpulse_processor.py (READ `scheduled_notify_newsletter` ~:10632-10638 — the static one-liner to extend; the schedule wiring `schedule.every().friday.at("12:00").do(scheduled_notify_newsletter)` ~:11380 stays unchanged; the config accessor the processor uses for `agentpulse-config.json` so `edition_eval.enforce` can be read plainly; `scheduled_auto_publish_newsletter` ~:10650 for the latest-draft `newsletters` lookup pattern and the `if not supabase: return` guard at ~:10644)
    - config/agentpulse-config.json (READ the `edition_eval` block — the `enabled`/`enforce` flags D-08 renders)
    - docker/processor/agentpulse_processor.py `send_telegram` (post-31-01: now returns bool)
    - tests/test_31_notify_eval.py (extend it)
  </read_first>
  <action>
    Extend `scheduled_notify_newsletter` to: guard `if not supabase: return` (mirror :10644); locate the current edition's `edition_number` via the existing latest-draft `newsletters` lookup (reuse the `scheduled_auto_publish_newsletter` pattern — the newsletters `.status` lookup is pre-existing and acceptable; ONLY the new edition_evals read must be `.eq()`-only); read `edition_eval.enforce` from the loaded `agentpulse-config.json` (plain config read, default False if absent — NULL != intent); call `_read_edition_evals(supabase, edition_number)` and `_format_notify_eval_section(rows, enforce)`; append the returned section AFTER the existing static "New AgentPulse Brief is ready for review" text so the message is the static line + the eval section. Wrap the whole eval-section build in try/except that on failure logs an ERROR with a fixed label and still sends the static notify (fail-open-but-loud — an eval-render error must never suppress the Friday notify entirely). Capture `send_telegram`'s bool return (D-03 critical caller): on `False`, `logger.critical("[EVAL-ALERT] CRITICAL — Friday notify delivery FAILED for edition #%s", edition_number)` (edition number only, T-30-LOG). Do NOT add any LLM call or retry state (WIRE-05 invariant).
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())" && python3 -m pytest tests/test_31_notify_eval.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `scheduled_notify_newsletter` calls `_read_edition_evals` and `_format_notify_eval_section` and appends the section to the notify text (grep confirms both calls inside the function region)
    - `grep -n "logger.critical" docker/processor/agentpulse_processor.py` shows a `[EVAL-ALERT] CRITICAL — Friday notify` line guarded on the `send_telegram` bool return
    - No `.in_(` appears anywhere in the new notify eval read path; no LLM/`routed_llm_call`/proxy call is added to `scheduled_notify_newsletter` (grep the function region for absence)
    - A seam test monkeypatches `proc.supabase` (StubSupabase with seeded eval rows), `proc.send_telegram` (capture the sent message + control its bool return), and the config accessor; asserts the sent message contains the static notify text AND the eval section (mechanical count on a passed draft, `⚠ no eval recorded` for a missing block, `⚠ WOULD HAVE HELD (report-only)` when enforce=False); asserts a `False` send_telegram return produces a CRITICAL record; asserts an eval-read exception still sends the static notify (fail-open). It passes.
  </acceptance_criteria>
  <done>The Friday notify carries the per-draft eval summary via a plain `.eq()`-only select, is fail-open-but-loud, and checks send_telegram's return as a critical caller; the seam test proves the message content and both failure modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edition_evals (DB) → processor | eval rows contain judge evidence/exemplars (unpublished draft prose) — must not leak into the compact notify |
| processor → Telegram API | the Friday notify is a hold/eval-critical delivery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Information disclosure | notify eval section | mitigate | `_format_notify_eval_section` renders labels/counts/scores ONLY — never `evidence`/`exemplar_*` prose (T-30-LOG); the bounded-excerpt view is the owner-gated `/newsletter_eval` (31-03) |
| T-31-05 | Tampering (silent failure) | edition_evals reads | mitigate | `.eq()`-only local reader; the `.in_()` silent-failure anti-pattern is structurally excluded by the StubSupabase test |
| T-31-06 | Denial (missing/errored eval hidden) | notify render | mitigate | Missing rows render `⚠ no eval recorded`; an eval-render exception is fail-open-but-loud (static notify still sends + ERROR logged) — NULL != intent (D-07) |
| T-31-SC | Tampering | package installs | accept | No npm/pip/cargo installs this plan — edits to an existing file only; RESEARCH package-legitimacy gate N/A |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"` passes.
- `python3 -m pytest tests/test_31_notify_eval.py tests/test_31_send_telegram.py -q` green.
- `python3 -m pytest tests/test_27_edition_eval.py tests/test_30_orchestration.py -q` still green.
</verification>

<success_criteria>
- The Friday notify appends a compact per-draft eval section (both pipeline_versions) with D-06 detail, D-07 missing-eval line, D-08 report-only tag.
- Reads are `.eq()`-only; no LLM in the Processor.
- The notify checks send_telegram's return (critical caller).
- Deploy is DEFERRED to plan 31-04 (worktree-unsafe scoped processor rebuild; a manual notify-path invocation is the D-13 acceptance there).
</success_criteria>

<output>
Create `.planning/phases/31-surfacing-escalation/31-02-SUMMARY.md` when done.
</output>
