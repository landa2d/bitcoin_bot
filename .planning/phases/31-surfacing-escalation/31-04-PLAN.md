---
phase: 31-surfacing-escalation
plan: 04
type: execute
wave: 3
depends_on: ["31-01", "31-02", "31-03"]
files_modified: []
autonomous: false
requirements: [SURF-01, SURF-02, SURF-03]
must_haves:
  truths:
    - "processor, gato_brain, and gato are scoped-rebuilt on the main tree carrying the 31-01/31-02/31-03 code; the newsletter container is NOT rebuilt (D-14, calibration-window protection)"
    - "A live Telegram round-trip of /newsletter_eval returns a real answer (the explicit 'No eval has run yet' answer counts) — proving the isGatoBrainCommand allowlist + gato_brain handler + .eq() select are wired end-to-end (D-13)"
    - "The Friday notify path is manually invoked and the operator sees the static notify + the eval section deliver (D-13)"
  artifacts:
    - path: ".planning/phases/31-surfacing-escalation/31-04-SUMMARY.md"
      provides: "deploy + live-verification evidence closing the SURF-01..03 done-boundary"
      contains: "SURF-0"
  key_links:
    - from: "inject-gato-brain.mjs (rebuilt gato)"
      to: "gato_brain /newsletter_eval handler (rebuilt gato_brain)"
      via: "live Telegram round-trip"
      pattern: "newsletter_eval"
---

<objective>
Close the Phase 31 done-boundary (D-13): deploy the SURF-01/02/03 code via scoped rebuilds on the main tree and verify it live. Send `/newsletter_eval` over real Telegram (the explicit "no eval recorded yet" answer counts — it proves allowlist + handler + select) and manually invoke the Friday-notify path to confirm the eval section delivers.

Purpose: the gato rebuild + live checks are WORKTREE-UNSAFE (a worktree executor would build stale code) and orchestrator-owned on the main tree; the `/newsletter_eval` command is a DEAD command over Telegram until gato is rebuilt (the Phase 9 `/map-*` lesson). This plan is the operator/orchestrator gate that turns the merged code into a live, trustworthy surface — WITHOUT rebuilding the newsletter container (D-14).
Output: a running processor+gato_brain+gato carrying the phase code; a live `/newsletter_eval` round-trip; a manual notify-path invocation; the phase SUMMARY with evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/31-surfacing-escalation/31-CONTEXT.md
@.planning/phases/31-surfacing-escalation/31-01-PLAN.md
@.planning/phases/31-surfacing-escalation/31-02-PLAN.md
@.planning/phases/31-surfacing-escalation/31-03-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Scoped rebuild of processor + gato_brain + gato on the main tree (orchestrator-owned, worktree-UNSAFE)</name>
  <read_first>
    - CLAUDE.md (the Build & Run commands — scoped rebuild via compose SERVICE names; python3 ast syntax-check before rebuild)
    - .planning/phases/31-surfacing-escalation/31-01-PLAN.md, 31-02-PLAN.md, 31-03-PLAN.md (confirm all three merged before deploying)
  </read_first>
  <what-built>
    The SURF-01/02/03 code in docker/processor/agentpulse_processor.py, docker/gato_brain/gato_brain.py, and docker/gato/inject-gato-brain.mjs (plans 31-01/02/03), ready to deploy.
  </what-built>
  <how-to-verify>
    Run on the MAIN tree (/root/bitcoin_bot), operator-approved, NO `--delete`:
    1. Syntax-check both Python files:
       `python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"`
       `python3 -c "import ast; ast.parse(open('docker/gato_brain/gato_brain.py').read())"`
    2. Run the full phase test suite green: `python3 -m pytest tests/test_31_send_telegram.py tests/test_31_notify_eval.py tests/test_31_newsletter_eval_handler.py -q`
    3. Scoped rebuild EXACTLY these three services (NOT newsletter — D-14):
       `cd /root/bitcoin_bot/docker && docker compose up -d --build processor gato_brain gato`
    4. Confirm all three containers are healthy/up and that at container boot the processor logged the `[TELEGRAM-CONFIG]` line ONLY if env is unset (it should be absent when env is set):
       `docker compose -f /root/bitcoin_bot/docker/docker-compose.yml logs --tail=50 processor | grep -i "TELEGRAM-CONFIG" || echo "no config error (env set) — OK"`
    5. Confirm the newsletter container was NOT rebuilt (its image/created time is unchanged): `docker compose -f /root/bitcoin_bot/docker/docker-compose.yml ps newsletter`
  </how-to-verify>
  <acceptance_criteria>
    - Both `ast.parse` checks succeed; the three test files pass.
    - `docker compose ps` shows processor, gato_brain, gato Up (recreated); newsletter Up but NOT recreated in this operation (D-14).
    - The processor boot log shows no `[TELEGRAM-CONFIG]` ERROR when the Telegram env is set (or, if intentionally unset, the labeled ERROR appears and the service still runs).
  </acceptance_criteria>
  <resume-signal>Type "deployed" once the three services are rebuilt healthy and newsletter is confirmed untouched, or describe any failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Telegram round-trip of /newsletter_eval (D-13)</name>
  <read_first>
    - docker/gato/inject-gato-brain.mjs (confirm isNewsletterEval is in the running image after rebuild)
    - .planning/phases/31-surfacing-escalation/31-03-PLAN.md (the expected handler behavior D-09/D-10/D-11/D-12)
  </read_first>
  <what-built>
    The live `/newsletter_eval` Gato command (owner-gated handler + allowlist + rebuilt gato).
  </what-built>
  <how-to-verify>
    From the operator's (owner) Telegram chat with Gato:
    1. Send `/newsletter_eval` — expect a real reply: either the current/latest edition's per-dimension eval view (if Friday 2026-07-03 rows exist) OR the explicit "No eval has run for any edition yet." message. EITHER counts (D-13 — the explicit no-eval answer proves allowlist + handler + `.eq()` select are wired).
    2. Send `/newsletter_eval trend` — expect the last-8-editions verdict lines (or the no-eval message).
    3. Confirm the command did NOT fall through to OpenClaw's default path (it returned a NEWSLETTER_COMMAND reply, not a generic Gato chat answer).
    4. Owner-gate spot check (D-12): if a non-owner test chat is available, sending `/newsletter_eval` there returns the owner-only refusal and NO draft prose. (If no non-owner chat is handy, confirm the refusal string exists in the handler via the 31-03 unit test instead.)
  </how-to-verify>
  <acceptance_criteria>
    - `/newsletter_eval` over real Telegram returns a NEWSLETTER_COMMAND reply (eval view OR the explicit no-eval message) — not an OpenClaw fall-through and not "Command processed but no response".
    - `/newsletter_eval trend` returns a trend reply or the no-eval message.
    - The owner-gate is confirmed (live non-owner refusal OR the 31-03 unit-test refusal assertion).
  </acceptance_criteria>
  <resume-signal>Type "eval-live" with a one-line note of what the reply showed, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual invocation of the Friday-notify path (D-13)</name>
  <read_first>
    - docker/processor/agentpulse_processor.py (`scheduled_notify_newsletter` as extended in 31-02)
    - .planning/phases/31-surfacing-escalation/31-02-PLAN.md (expected eval section D-05..D-08)
  </read_first>
  <what-built>
    The extended `scheduled_notify_newsletter` (static notify + per-draft eval section via `.eq()` select).
  </what-built>
  <how-to-verify>
    On the main tree, manually invoke the notify path inside the running processor container (do NOT wait for Friday 12:00 UTC):
    `docker compose -f /root/bitcoin_bot/docker/docker-compose.yml exec processor python -c "import agentpulse_processor as p; p.init_clients(); p.scheduled_notify_newsletter()"`
    (adjust the init call to match the module's actual client-init entrypoint). Then confirm in the operator Telegram chat that the message arrived and contains: the static "New AgentPulse Brief is ready for review" line PLUS an eval section — a per-draft block with a verdict + dimension scores + a mechanical count, and either real numbers (if rows exist) or the `⚠ no eval recorded for this draft` line for a pipeline_version with no rows. Check the processor logs for a `[EVAL-ALERT] CRITICAL — Friday notify` line ONLY if delivery failed (it should be absent on success).
  </how-to-verify>
  <acceptance_criteria>
    - A Telegram message arrives carrying the static notify text AND the eval section (real per-dim numbers or the explicit `⚠ no eval recorded for this draft` line).
    - If report-only holds exist, they render as `⚠ WOULD HAVE HELD (report-only)` (D-08).
    - No `[EVAL-ALERT] CRITICAL — Friday notify` failure log on a successful send; the processor did not crash (fail-open-but-loud held).
  </acceptance_criteria>
  <resume-signal>Type "notify-verified" with a one-line note of what the message showed, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main-tree deploy → live containers | worktree executors would build stale code; deploy is orchestrator-owned on the main tree |
| live Telegram user → /newsletter_eval | the owner-gate must actually hold in the running image, not just in source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-11 | Information disclosure | live /newsletter_eval owner-gate | mitigate | Task 2 live-verifies a non-owner receives the refusal (or falls back to the 31-03 unit-test assertion) — the gate is confirmed in the running image, not just source |
| T-31-12 | Tampering | rebuilding the wrong/too-many services | mitigate | Scoped rebuild names EXACTLY processor/gato_brain/gato; Task 1 confirms newsletter is NOT recreated (D-14) — no drift into the calibration-window service |
| T-31-13 | Denial | a bad deploy takes down ingestion | mitigate | Syntax-check + full phase test suite green BEFORE the rebuild; no `--delete`; services still run even if Telegram env is unset (31-01 D-04) |
| T-31-SC | Tampering | package installs | accept | No new npm/pip/cargo installs — the scoped rebuilds reuse existing images + pinned deps; RESEARCH package-legitimacy gate N/A |
</threat_model>

<verification>
- processor, gato_brain, gato rebuilt and Up on the main tree; newsletter NOT recreated (D-14).
- `/newsletter_eval` + `/newsletter_eval trend` return real replies over Telegram (no-eval answer counts, D-13).
- The Friday-notify path, manually invoked, delivers the static text + eval section.
- SURF-01, SURF-02, SURF-03 close: code + tests (31-01/02/03) + scoped deploys + live round-trip + manual notify invocation.
</verification>

<success_criteria>
- The done-boundary D-13 is satisfied: code + tests + scoped deploys (processor, gato_brain, gato) + a live Telegram `/newsletter_eval` round-trip + a manual notify-path invocation.
- The newsletter container is untouched this phase (D-14) — the report-only calibration window is protected.
</success_criteria>

<output>
Create `.planning/phases/31-surfacing-escalation/31-04-SUMMARY.md` when done, recording the deploy evidence (which services recreated, newsletter untouched), the `/newsletter_eval` reply, and the notify-path result.
</output>
