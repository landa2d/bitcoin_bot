---
phase: quick-260612-kh9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/docker-compose.yml
  - docker/web/entrypoint.sh
  - docker/processor/agentpulse_processor.py
  - docker/gato_brain/gato_brain.py
  - docker/research/research_agent.py
  - docker/analyst/analyst_poller.py
  - docker/newsletter/newsletter_poller.py
  - .claude/settings.local.json
autonomous: true
requirements: [WS-01, WS-02, WS-14-partial, WS-24, WS-25]

must_haves:
  truths:
    - "Processor gpt-4o/gpt-4o-mini traffic goes through llm-proxy and produces a settled wallet_transactions row for agent processor (WS-01)"
    - "Starting processor, gato_brain, or research with LLM_PROXY_URL or AGENT_API_KEY unset/empty exits non-zero quickly with a one-line reason (WS-02, WS-24)"
    - "No working ap_ agent key literal exists in any tracked file (WS-14 compose-literal slice; rotation to NEW values deferred per LD-1)"
    - "Proxy wiring (OPENAI_BASE_URL / DEEPSEEK_BASE_URL / LLM_PROXY_URL) is single-sourced via one x-proxy-env compose anchor (WS-25)"
    - "Web entrypoint refuses to boot when SUPABASE_URL or SUPABASE_ANON_KEY is unset instead of sed-ing placeholders into a dead SPA (WS-24)"
  artifacts:
    - path: "docker/docker-compose.yml"
      provides: "x-proxy-env anchor merged into the 6 Python services; ${LLM_PROXY_*_KEY:?} references instead of 4 ap_ literals"
      contains: "x-proxy-env"
    - path: "docker/web/entrypoint.sh"
      provides: "fail-loud env guards before sed"
      contains: "SUPABASE_URL:?"
    - path: "docker/processor/agentpulse_processor.py"
      provides: "explicit-base_url client construction + require_env guard"
      contains: "def require_env"
    - path: "docker/gato_brain/gato_brain.py"
      provides: "proxy-only clients, no direct-SDK fallback, require_env guard"
      contains: "def require_env"
    - path: "docker/research/research_agent.py"
      provides: "proxy-only Anthropic client, no direct fallback, require_env guard"
      contains: "def require_env"
  key_links:
    - from: "docker/docker-compose.yml processor environment"
      to: "agentpulse_processor.py init_clients()"
      via: "OPENAI_BASE_URL from x-proxy-env anchor, consumed as explicit base_url="
      pattern: "base_url=OPENAI_BASE_URL"
    - from: "docker/docker-compose.yml ${LLM_PROXY_*_KEY:?} interpolations"
      to: "config/.env lines 255-265 (already synced by orchestrator)"
      via: "docker/.env symlink -> ../config/.env (compose project-dir interpolation source)"
      pattern: "LLM_PROXY_PROCESSOR_KEY"
---

<objective>
Implement audit spec 02 (Proxy Governance Wiring) items 1, 2, 3, 5, plus the compose-literal-removal slice of item 4: make "all LLM calls route through llm-proxy" structurally true. Processor OpenAI traffic becomes governed (WS-01, headline fix), direct-provider fallback branches are deleted (WS-02), the four hardcoded ap_ keys leave docker-compose.yml (WS-14 slice — NO key rotation, LD-1), proxy wiring is single-sourced via a compose anchor (WS-25), and every Python service plus the web entrypoint refuses to boot on missing hard deps (WS-24).

Purpose: today governance holds only where compose wiring happens to be right — 3 distinct wirings exist, the processor's OpenAI branch is a silent dead end, and a dropped env var silently un-governs Sonnet spend (the RivalScope lesson).
Output: edited compose + 5 Python services + web entrypoint, rebuilt live one service at a time, with a governed call observed end-to-end and a startup-guard proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/audit/specs/02_proxy_governance_wiring.md   (canonical spec)
@docker/docker-compose.yml
@docker/web/entrypoint.sh
@docker/processor/agentpulse_processor.py  (lines 55-70 constants, 404-535 clients/routing, 11446+ main)
@docker/gato_brain/gato_brain.py           (lines 38-50 constants, 602-669 init_clients, 3009+ main)
@docker/research/research_agent.py         (lines 40-78 constants, 100-145 init, 620-630 + 785-790 call sites, 986+ main)
@docker/analyst/analyst_poller.py          (lines 32-40 constants, 1212 main)
@docker/newsletter/newsletter_poller.py    (lines 39-50 constants, 2390 main)

LOCKED DECISIONS from the orchestrator (LD-1..LD-5) — non-negotiable:
- LD-1: NO key rotation/minting/bcrypt/agent_registry SQL. config/.env already holds LLM_PROXY_PROCESSOR_KEY, LLM_PROXY_ANALYST_KEY, LLM_PROXY_NEWSLETTER_KEY, LLM_PROXY_LABDATA_KEY (lines ~255-265) with the CURRENT working values. Compose only switches to ${VAR} references.
- LD-2: No worktree, sequential execution from the main tree. Rebuilds use compose SERVICE names (processor, gato_brain, research, analyst, newsletter, lab-data-provider, web), never container names.
- LD-3: gato_brain's DeepSeek/OpenAI direct fallback branches (~:631-665) are deleted together with the Anthropic one — the startup guard makes them unreachable; delete rather than leave dead code.
- LD-4: Fail-loud everywhere (RuntimeError / sys.exit non-zero / ${VAR:?} — never warn-and-continue for hard deps).
- LD-5: x-proxy-env anchor; processor MUST end up with OPENAI_BASE_URL set; verify YAML merge with `docker compose config`.

PLANNER-VERIFIED FACTS (do not re-derive):
- Compose v5 `${VAR}` interpolation does NOT read `env_file:` entries. Verified: with a clean environment (`env -i`), only the 4 literal ap_ keys resolve; `${LLM_PROXY_GATO_KEY:-}` / `${LLM_PROXY_RESEARCH_KEY:-}` resolve to EMPTY (live containers got values only because the operator's shell sources config/.env). The plan therefore creates `docker/.env -> ../config/.env` (symlink, compose project-dir interpolation source). `.gitignore` line 3 (`.env`) already covers it — confirm with `git check-ignore docker/.env`.
- `.claude/settings.local.json:53` contains a working `ap_analyst_<32hex>` literal inside an old permission-allowlist entry — it fails the spec's grep gate and must be scrubbed (Task 1).
- processor `--task health_check` is a valid argparse choice (line ~11448); CMD is `python3 /home/openclaw/agentpulse_processor.py --task watch`; WORKDIR is /home/openclaw.
- Supabase key name varies: every service reads `SUPABASE_SERVICE_KEY` or `SUPABASE_KEY` — require_env must support `|`-separated alternatives.
- analyst (:221) and newsletter (:271-292) already construct clients with proxy base_url — they only need require_env, no client changes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose single-sourcing — x-proxy-env anchor, ${LLM_PROXY_*_KEY:?} references, interpolation source, web entrypoint guards, key-literal scrub</name>
  <files>docker/docker-compose.yml, docker/web/entrypoint.sh, .claude/settings.local.json, docker/.env (symlink, gitignored)</files>
  <action>
  Implements spec 02 items 2 + 4(compose slice) + 5(web half) per LD-1/LD-5; closes WS-14 literal exposure and WS-25.

  1. In docker/docker-compose.yml, directly below the existing `x-common-env: &common-env` block, add a second top-level extension block `x-proxy-env: &proxy-env` with exactly three keys: OPENAI_BASE_URL set to http://llm-proxy:8200/v1, DEEPSEEK_BASE_URL set to http://llm-proxy:8200/v1, LLM_PROXY_URL set to http://llm-proxy:8200.
  2. Merge it into the SIX Python services only — gato_brain, analyst, newsletter, research, processor, lab-data-provider — by changing each service's `<<: *common-env` to the list form `<<: [*common-env, *proxy-env]` (YAML merge with multiple anchors, LD-5). Do NOT merge into gato (Node), web, or llm-proxy — llm-proxy must keep pointing at real providers; giving it OPENAI_BASE_URL=itself would create a loop.
  3. Delete the per-service lines the anchor now provides: OPENAI_BASE_URL/DEEPSEEK_BASE_URL/LLM_PROXY_URL from the analyst (:124,:126,:128), newsletter (:174,:176,:178), research (:219), processor (:264,:266), gato_brain (:71), and lab-data-provider (:340) environment blocks. Per-service blocks keep ONLY the key vars (spec item 2).
  4. Replace the four hardcoded ap_ literals with interpolations using the `:?` (fail-loud, LD-4) form, message "must be set in config/.env":
     - analyst: OPENAI_API_KEY, DEEPSEEK_API_KEY, AGENT_API_KEY all -> ${LLM_PROXY_ANALYST_KEY:?...}
     - newsletter: same three -> ${LLM_PROXY_NEWSLETTER_KEY:?...}
     - processor: OPENAI_API_KEY, DEEPSEEK_API_KEY, AGENT_API_KEY -> ${LLM_PROXY_PROCESSOR_KEY:?...}
     - lab-data-provider: AGENT_API_KEY -> ${LLM_PROXY_LABDATA_KEY:?...}
     Also tighten the two existing soft interpolations: gato_brain AGENT_API_KEY ${LLM_PROXY_GATO_KEY:-} -> :? form; research AGENT_API_KEY ${LLM_PROXY_RESEARCH_KEY:-} -> :? form. Leave LLM_PROXY_ADMIN_KEY (:- on llm-proxy) untouched — out of scope.
  5. Interpolation source (planner-verified gap): create the symlink `ln -sfn ../config/.env /root/bitcoin_bot/docker/.env` so compose reads config/.env for ${VAR} interpolation regardless of shell state. Confirm `git check-ignore docker/.env` reports it ignored (gitignore line 3 `.env` covers it). CONTINGENCY: if `docker compose config` then errors parsing the .env (incompatible line in config/.env), remove the symlink and instead write a plain gitignored docker/.env containing only the six LLM_PROXY_*_KEY lines copied from config/.env, and note the substitution in the summary.
  6. docker/web/entrypoint.sh: before the two sed lines, add the guards `: "${SUPABASE_URL:?must be set}"` and `: "${SUPABASE_ANON_KEY:?must be set}"` (WS-24 web half — kills the known dead-SPA pattern).
  7. Scrub the stale ap_analyst literal from .claude/settings.local.json: delete the single allowlist array entry at ~line 53 (the long `Bash(PROXY_URL=...` permitted-command string embedding `ap_analyst_<32hex>`). Keep the file's other entries; valid JSON after edit (`python3 -m json.tool` it). Note: the file has pre-existing uncommitted churn — commit it as-is with this task and flag that in the summary.

  Commit (code/config only, no .planning docs): "fix(governance): single-source proxy wiring via x-proxy-env anchor, remove ap_ key literals from compose (spec 02 items 2+4-slice, WS-14/WS-25)". The docker/.env symlink is gitignored and cannot be committed — record its creation in the commit body and summary (host-bootstrap fact).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && docker compose -f docker/docker-compose.yml config --quiet && env -i PATH=/usr/bin:/bin:/usr/local/bin sh -c 'cd /root/bitcoin_bot/docker && docker compose config' | grep -cE 'AGENT_API_KEY: ap_' | grep -qx 6 && env -i PATH=/usr/bin:/bin:/usr/local/bin sh -c 'cd /root/bitcoin_bot/docker && docker compose config' | awk '/^  processor:/,/^  [a-z-]+:$/' | grep -q 'OPENAI_BASE_URL: http://llm-proxy:8200/v1' && git grep -nE 'ap_[a-z_-]+_[0-9a-f]{32}' -- ':!*.md' | wc -l | grep -qx 0 && sh -n docker/web/entrypoint.sh && python3 -m json.tool .claude/settings.local.json > /dev/null && echo PASS</automated>
  </verify>
  <done>compose config validates with a clean environment; all 6 AGENT_API_KEY values resolve to ap_ keys WITHOUT shell help; rendered processor env contains OPENAI_BASE_URL pointing at the proxy; the ap_-literal grep gate over tracked non-md files is empty; entrypoint parses; settings.local.json is valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Fail-loud code guards — processor explicit base_url, delete direct-SDK fallbacks in gato_brain + research, require_env in all 5 service mains</name>
  <files>docker/processor/agentpulse_processor.py, docker/gato_brain/gato_brain.py, docker/research/research_agent.py, docker/analyst/analyst_poller.py, docker/newsletter/newsletter_poller.py</files>
  <action>
  Implements spec 02 items 1 + 3 + 5(Python half) per LD-3/LD-4; closes WS-01 (code half), WS-02, WS-24.

  SHARED HELPER (duplicate per service — project convention is self-contained services, no shared lib): a `require_env(names)` function of at most 10 lines near the top of each file (after imports). Each element of names is a string of `|`-separated alternative env var names; an element is satisfied when ANY alternative is non-empty via os.getenv (empty string counts as missing — the startup-guard proof injects `-e LLM_PROXY_URL=`). Collect unsatisfied elements; if any, raise RuntimeError with a single line listing them ("missing required env: ..."). Uncaught RuntimeError exits non-zero immediately — satisfies spec AC-3 (exit within 5s).

  PROCESSOR (agentpulse_processor.py):
  - Constants (~:61-64): add `OPENAI_BASE_URL = os.getenv('OPENAI_BASE_URL')` (NO default — a proxy-keyed client pointed at api.openai.com must be impossible to construct); change DEEPSEEK_BASE_URL to `os.getenv('DEEPSEEK_BASE_URL')` (drop the 'https://api.deepseek.com' default, LD-4).
  - init_clients() (:412-432): raise RuntimeError (one-line reason) if OPENAI_BASE_URL or DEEPSEEK_BASE_URL is unset/empty, and if OPENAI_API_KEY or DEEPSEEK_API_KEY is missing (replace the warn-and-continue branches — no more silent "OpenAI not configured"). Construct openai_client as OpenAI(api_key=OPENAI_API_KEY, base_url=OPENAI_BASE_URL); deepseek_client keeps its explicit base_url. Leave the Supabase branch as-is (its absence is covered by require_env).
  - main entry: insert `require_env([...])` as the FIRST statement inside the `if __name__ == '__main__':` block (line ~11662), before main() — it must fire for every --task including health_check. List: 'SUPABASE_URL', 'SUPABASE_SERVICE_KEY|SUPABASE_KEY', 'OPENAI_API_KEY', 'DEEPSEEK_API_KEY', 'OPENAI_BASE_URL', 'DEEPSEEK_BASE_URL', 'LLM_PROXY_URL', 'AGENT_API_KEY'.

  GATO_BRAIN (gato_brain.py):
  - Delete the ANTHROPIC_AGENT_KEY constant (:40) and its init check (:612-614).
  - init_clients() (:604-669): first statements after the Supabase check — raise RuntimeError if LLM_PROXY_URL or AGENT_API_KEY is unset/empty ("proxy wiring missing — refusing to start ungoverned", LD-4). Then make all three client paths unconditionally proxy-routed, deleting the direct branches (LD-3 — the guard makes them unreachable; delete, don't dead-code):
    a. claude_client: keep only the anthropic.Anthropic(api_key=AGENT_API_KEY, base_url=f"{LLM_PROXY_URL}/anthropic") construction; delete the else branch (:625-627).
    b. corpus_probe block (:631-638): replace with an unconditional corpus_probe.init(AGENT_API_KEY, base_url=f"{LLM_PROXY_URL}/v1") — drop the OPENAI_API_KEY gate and the direct corpus_probe.init(OPENAI_API_KEY) branch.
    c. DeepSeek block (:646-665): construct the httpx.Client unconditionally with base_url f"{LLM_PROXY_URL}/v1" and Bearer AGENT_API_KEY; delete the ds_base/ds_key direct branch and the "DEEPSEEK_API_KEY not set" warning path; intent_router.init always runs. Keep the DEEPSEEK_API_KEY constant itself (:41) — still referenced at :373, out of scope (LD-3 scopes deletion to the init fallback branches).
  - main (:3009): insert require_env(['SUPABASE_URL', 'SUPABASE_SERVICE_KEY|SUPABASE_KEY', 'LLM_PROXY_URL', 'AGENT_API_KEY']) before init_clients().

  RESEARCH (research_agent.py):
  - Delete the ANTHROPIC_AGENT_KEY constant (:42), the claude_client global (:68), and the direct-client init branch (:113-118). In init() (:103-130): after the Supabase check, fail (keep the file's logger.error + sys.exit(1) pattern — exits non-zero either way) if _agent_api_key or LLM_PROXY_URL is unset/empty; construct proxy_client unconditionally; delete the "No proxy API key found — bypass spend tracking" warning branch and the now-dead `_get_agent_api_key_from_db()` function (:133-140+) — AGENT_API_KEY comes from compose ${LLM_PROXY_RESEARCH_KEY:?}.
  - Call sites :624 and :788: change `llm = proxy_client or claude_client` to `llm = proxy_client`.
  - main (:986): insert require_env(['SUPABASE_URL', 'SUPABASE_SERVICE_KEY|SUPABASE_KEY', 'LLM_PROXY_URL', 'AGENT_API_KEY']) as the first statement of the __main__ block, before init().

  ANALYST (analyst_poller.py) — require_env only (clients already proxy-correct at :221-223): insert require_env(['SUPABASE_URL', 'SUPABASE_SERVICE_KEY|SUPABASE_KEY', 'OPENAI_API_KEY', 'DEEPSEEK_API_KEY', 'LLM_PROXY_URL']) at the top of the __main__ block (:1212), before main().

  NEWSLETTER (newsletter_poller.py) — require_env only (clients already proxy-correct at :271-292): insert require_env(['SUPABASE_URL', 'SUPABASE_SERVICE_KEY|SUPABASE_KEY', 'OPENAI_API_KEY', 'LLM_PROXY_URL']) at the top of the __main__ block (:2390), before main().

  Commit: "fix(governance): fail-loud proxy-only clients — explicit base_url in processor, delete direct-SDK fallbacks, require_env guards in 5 services (spec 02 items 1+3+5, WS-01/WS-02/WS-24)".
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot && for f in docker/processor/agentpulse_processor.py docker/gato_brain/gato_brain.py docker/research/research_agent.py docker/analyst/analyst_poller.py docker/newsletter/newsletter_poller.py; do python3 -c "import ast; ast.parse(open('$f').read())" || exit 1; done && grep -c "def require_env" docker/processor/agentpulse_processor.py docker/gato_brain/gato_brain.py docker/research/research_agent.py docker/analyst/analyst_poller.py docker/newsletter/newsletter_poller.py | grep -cv ':1$' | grep -qx 0 && ! grep -n "ANTHROPIC_AGENT_KEY" docker/gato_brain/gato_brain.py docker/research/research_agent.py && ! grep -n "claude_client" docker/research/research_agent.py && ! (grep -nE 'OpenAI\(' docker/processor/agentpulse_processor.py | grep -v base_url) && ! (grep -nE 'anthropic\.Anthropic\(' docker/gato_brain/gato_brain.py docker/research/research_agent.py | grep -v -e base_url -e '^\s*#') && ! grep -q 'api.deepseek.com' docker/processor/agentpulse_processor.py && echo PASS</automated>
  </verify>
  <done>All 5 files parse; each defines require_env exactly once; ANTHROPIC_AGENT_KEY gone from gato_brain + research; claude_client gone from research; every OpenAI( construction in the processor and every anthropic.Anthropic( construction in gato_brain/research carries an explicit base_url; the api.deepseek.com default is gone from the processor.</done>
</task>

<task type="auto">
  <name>Task 3: Sequential scoped rebuilds + live acceptance (governed call proof + startup-guard proof)</name>
  <files>none (deploy + verification only)</files>
  <action>
  Implements spec 02 execution-sequential step + acceptance criteria 2-4 (AC-4 Telegram round-trip is operator-owned — note it as a follow-up, do not attempt). LD-2 applies: main tree, sequential, compose SERVICE names only. SAFETY: if ANY docker or credential command is DENIED by the permission system, STOP this task immediately and record the denial in the summary — do not work around it.

  1. Pre-flight: `cd /root/bitcoin_bot/docker && docker compose config --quiet` (must pass — ${VAR:?} interpolation resolves via the Task-1 docker/.env source).
  2. Rebuild ONE service at a time, in this order, waiting for health between each: processor, gato_brain, research, analyst, newsletter, lab-data-provider, web. For each: `docker compose up -d --build <service>` then poll `docker compose ps <service>` until status is healthy (processor/gato_brain/research/analyst/newsletter have healthchecks; lab-data-provider and web have none — "Up" + 15s with no restart counts as pass). If a service goes unhealthy/restarting, STOP (do not proceed to the next service), capture `docker compose logs --tail 50 <service>`, and record the failure — rollback for compose-only causes is git checkout docker-compose.yml + rebuild of the affected service.
  3. Governed-call proof (spec AC-2 / WS-01): record `date -u +%FT%TZ` as the request timestamp, then run one tiny governed call from INSIDE the processor container through routed_llm_call: `docker compose exec -T processor python3 -c "import sys; sys.path.insert(0, '/home/openclaw'); import agentpulse_processor as p; p.init_clients(); r = p.routed_llm_call('gpt-4o-mini', [{'role': 'user', 'content': 'Reply with exactly: governed'}], max_tokens=8); print(r.choices[0].message.content)"` — must print a completion (proves the proxy accepted the ap_processor key and the openai_client base_url points at it). Then confirm the proxy saw it: `docker logs agentpulse-llm-proxy --since 5m 2>&1 | grep -i processor | tail -5` shows the request. Record the timestamp + printed output in the summary — the ORCHESTRATOR verifies the settled wallet_transactions row for agent processor via MCP; the executor does NOT query Supabase.
  4. Startup-guard proof (spec AC-3 / WS-24): `cd /root/bitcoin_bot/docker && timeout 30 docker compose run --rm --no-deps -e LLM_PROXY_URL= processor python3 /home/openclaw/agentpulse_processor.py --task health_check; echo "exit=$?"` — must exit NON-ZERO quickly (require_env treats the injected empty LLM_PROXY_URL as missing) with the one-line "missing required env" reason visible in output. Record exit code + the reason line.
  5. Final state: `docker compose ps` — all services Up, previously-healthchecked ones healthy. Record in summary: (a) the governed-call timestamp for orchestrator MCP verification, (b) AC-4 (gato Telegram round-trip) left to the operator, (c) the docker/.env symlink as a host-bootstrap fact, (d) any pre-existing settings.local.json churn committed in Task 1.

  No commit (no file changes in this task).
  </action>
  <verify>
    <automated>cd /root/bitcoin_bot/docker && docker compose ps --format '{{.Service}} {{.Status}}' | grep -E 'processor|gato_brain|research|analyst|newsletter' | grep -c healthy | grep -qx 5 && timeout 30 docker compose run --rm --no-deps -e LLM_PROXY_URL= processor python3 /home/openclaw/agentpulse_processor.py --task health_check > /tmp/guard_proof.log 2>&1; test $? -ne 0 && grep -qi 'missing required env' /tmp/guard_proof.log && echo PASS</automated>
  </verify>
  <done>All five healthchecked services report healthy after sequential rebuilds; the governed gpt-4o-mini call returned a completion and appears in proxy logs with the timestamp recorded for orchestrator DB verification; the LLM_PROXY_URL-unset boot exits non-zero within the timeout with a one-line reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services -> llm-proxy | every agent LLM call must cross here (governance: auth, reserve/settle, caps) |
| git repo -> world | tracked files must not carry working wallet-backed keys |
| compose env -> container boot | misconfiguration must surface at boot, not as silent per-call failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q02-01 | Information Disclosure | docker-compose.yml + settings.local.json ap_ literals | mitigate | Task 1 ${VAR:?} references + allowlist scrub; rotation to NEW values deferred (LD-1, operator gate); git history exposure accepted until rotation |
| T-q02-02 | Elevation of Privilege (governance bypass) | gato_brain/research direct-SDK fallbacks | mitigate | Task 2 deletes branches + startup RuntimeError — bypass becomes structurally impossible |
| T-q02-03 | Repudiation (unmetered spend) | processor OpenAI branch -> api.openai.com | mitigate | Task 1 anchor env + Task 2 explicit base_url, proven by governed-call settle row (Task 3) |
| T-q02-04 | Denial of Service | ${VAR:?} + require_env make boot stricter | accept | intended fail-loud posture; rollback is git checkout + per-service rebuild |
| T-q02-SC | Tampering | package installs | accept | no new packages installed in this plan |
</threat_model>

<verification>
- `docker compose -f docker/docker-compose.yml config --quiet` passes from a clean environment (interpolation source independent of shell).
- Spec grep gates all green: no ap_ literal in tracked non-md files; no ANTHROPIC_AGENT_KEY in gato_brain/research; no OpenAI(/Anthropic( construction without explicit base_url in processor/gato_brain/research.
- One governed gpt-4o-mini call from the processor observed at the proxy; settled wallet_transactions row verified by the orchestrator via MCP (timestamp recorded).
- Processor boot with empty LLM_PROXY_URL exits non-zero quickly with a one-line reason.
- All rebuilt services healthy; AC-4 (gato Telegram round-trip) handed to the operator.
</verification>

<success_criteria>
- WS-01 closed: processor OPENAI_BASE_URL set via anchor + explicit base_url in code + governed-call proof.
- WS-02 closed: zero direct-provider fallback paths remain in gato_brain/research; misconfig is loud.
- WS-14 compose slice closed: zero ap_ literals in tracked files (rotation itself deferred per LD-1).
- WS-24 closed (minimal slice): require_env in all 5 Python mains + web entrypoint :? guards.
- WS-25 closed: one x-proxy-env anchor is the single source of proxy wiring for all 6 Python services.
- Two atomic code commits (Tasks 1, 2); live system healthy after sequential scoped rebuilds.
</success_criteria>

<output>
Create `.planning/quick/260612-kh9-implement-audit-spec-02-proxy-governance/260612-kh9-SUMMARY.md` when done. Must record: governed-call timestamp (for orchestrator MCP wallet_transactions check), startup-guard exit code + reason line, docker/.env symlink creation (host-bootstrap fact, gitignored), settings.local.json pre-existing churn included in the Task 1 commit, AC-4 operator follow-up, and any permission denials (verbatim, with the task halted).
</output>
