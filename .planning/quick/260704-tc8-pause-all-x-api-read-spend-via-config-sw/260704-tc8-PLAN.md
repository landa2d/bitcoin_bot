---
phase: quick-260704-tc8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/agentpulse-config.json
  - docker/processor/agentpulse_processor.py
autonomous: true
requirements: [QUICK-260704-TC8]
must_haves:
  truths:
    - "With x_scraping.enabled=false, every X API read path returns [] with zero network calls to api.x.com (all 5 call sites funnel through the single gated function)"
    - "While paused, the hourly health check reports x_budget as ok with a 'paused via config' note — no more hourly Telegram fail alerts about $0.29 remaining"
    - "Flipping enabled=true (or deleting the x_scraping block) restores exact current behavior with NO container restart — the gate fresh-reads the config file on every call"
    - "Any config read error fails OPEN (scraping stays enabled) — a broken config can never silently disable scraping"
  artifacts:
    - path: "config/agentpulse-config.json"
      provides: "Top-level x_scraping.enabled=false pause switch"
      contains: '"x_scraping"'
    - path: "docker/processor/agentpulse_processor.py"
      provides: "_x_scraping_enabled() fresh-read helper + gate in _x_api_search + health-check pause branch"
      contains: "_x_scraping_enabled"
  key_links:
    - from: "_x_api_search (docker/processor/agentpulse_processor.py:8372)"
      to: "_x_scraping_enabled()"
      via: "guard at top of function, returns [] when paused"
      pattern: "if not _x_scraping_enabled"
    - from: "health check x_budget section (~:11383)"
      to: "_x_scraping_enabled()"
      via: "paused branch sets status ok, skips remaining-threshold fail"
      pattern: "paused via config"
    - from: "_x_scraping_enabled()"
      to: "/home/openclaw/.openclaw/config/agentpulse-config.json"
      via: "fresh json.load per call (NOT get_full_config — that caches for process lifetime)"
      pattern: "x_scraping"
---

<objective>
Pause all X/Twitter API read spend via a config switch, and silence the now-moot hourly x_budget health alert while paused.

Purpose: The $5/week X read budget is nearly exhausted ($4.71/$5.00 spent, $0.29 remaining), so the hourly health check pings the operator's Telegram every hour until Monday. The operator wants X read spend paused indefinitely via config — reversible without a rebuild — and the alert silenced while paused.

Output: One atomic commit touching `config/agentpulse-config.json` + `docker/processor/agentpulse_processor.py`, then a scoped processor rebuild with in-container verification (operator already approved the deploy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docker/processor/agentpulse_processor.py (read only the ranges below — 10k+ line file)

Verified anchors (line numbers re-confirmed 2026-07-04):
- `_x_api_search()` at :8372 — ALL 5 X read call sites (:1432, :1505, :8805, :8838, :8857) funnel through it. Gating this ONE function stops all X read spend.
- `_check_x_budget()` / `_log_x_api_cost()` helpers at :8349-8369 — place the new helper adjacent to these.
- `get_full_config()` at :558 — CACHES for process lifetime via `_full_config_cache`. DO NOT use it for this gate. Config path inside the container: `/home/openclaw/.openclaw/config/agentpulse-config.json` (repo `config/` tree is bind-mounted ro, comment at :3046 — config edits are live without rebuild).
- Health check x_budget section at :11383-11392: `if remaining <= 0.50: results['x_budget'] = {'status': 'fail', ...}`.
- Pattern to mirror: `_read_edition_eval_config` in docker/newsletter/newsletter_poller.py:312 (fresh read, try/except, logger.warning on failure) — adapted to the processor's path and INVERTED default (see below).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add x_scraping config switch, _x_scraping_enabled() helper, gate _x_api_search, silence health alert while paused</name>
  <files>config/agentpulse-config.json, docker/processor/agentpulse_processor.py</files>
  <action>
1. `config/agentpulse-config.json`: add a top-level block `"x_scraping": {"enabled": false}` (e.g. immediately after the `"version"` key — any top-level position is fine, keep valid JSON). This is the pause switch and pauses immediately on deploy.

2. `docker/processor/agentpulse_processor.py`: add helper `_x_scraping_enabled() -> bool` adjacent to `_check_x_budget()` (~:8349). It must fresh-read `/home/openclaw/.openclaw/config/agentpulse-config.json` on EVERY call (json.load inside the function — the file is small and this fires at most a few dozen times/day), returning `cfg.get('x_scraping', {}).get('enabled', True)`. On ANY exception (missing file, bad JSON, wrong types) log a `logger.warning` and return True. NOTE THE INVERSION vs `_read_edition_eval_config`'s fail-safe: here default/failure = ENABLED, because the pause is the explicit opt-in state — a config read error must never silently disable scraping. Do NOT touch `get_full_config()` or its cache.

3. At the TOP of `_x_api_search()` (:8372, before the `X_BEARER_TOKEN` check): if `not _x_scraping_enabled()`, log INFO exactly `[X-SCRAPE] X API reads paused via config (x_scraping.enabled=false)` and `return []`. Plain INFO on each call is fine (bounded by call volume, ≤ a few dozen/day) — no throttle flag needed.

4. Health check x_budget section (:11383-11392, comment `# 9. X API budget`): inside the existing try block, first check `if not _x_scraping_enabled(): results['x_budget'] = {'status': 'ok', 'note': 'paused via config'}` and skip the budget query + remaining-threshold check entirely (else branch keeps the current logic verbatim). While paused, x_budget must NEVER be 'fail'. Keep the outer `except Exception` as-is.

Zero behavior change when `x_scraping.enabled=true` or the block is absent. Conventions: snake_case, logger "agentpulse" (module logger already exists), broad try/except acceptable here, no new dependencies, minimal diff.

Commit both files atomically: `feat(processor): pause X API reads via x_scraping config switch, silence x_budget alert while paused`.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())" && python3 -c "import json; c=json.load(open('config/agentpulse-config.json')); assert c['x_scraping']['enabled'] is False, c.get('x_scraping')" && grep -c "_x_scraping_enabled" docker/processor/agentpulse_processor.py | awk '$1>=3{exit 0}{exit 1}'</automated>
  </verify>
  <done>Syntax-clean processor module; config has x_scraping.enabled=false; helper defined and referenced from both _x_api_search and the health check (>=3 occurrences); single atomic commit of both files.</done>
</task>

<task type="auto">
  <name>Task 2: Scoped processor rebuild + in-container verification</name>
  <files>(none — deploy/verify only)</files>
  <action>
WORKTREE-UNSAFE: this task MUST run on the main tree at /root/bitcoin_bot (the rebuild cds to the absolute compose path; a worktree executor would build stale code). Operator already approved this deploy.

1. Rebuild only the processor: `cd /root/bitcoin_bot/docker && docker compose up -d --build processor`.
2. Confirm clean startup: `docker logs agentpulse-processor --tail 50` (or `docker compose -f /root/bitcoin_bot/docker/docker-compose.yml logs --tail 50 processor` if the container name differs) — no tracebacks.
3. Prove the new code is IN the running container (the v2.3 packaging lesson — packaging failures are invisible to the test suite): `docker exec agentpulse-processor sh -c "grep -rn 'X-SCRAPE' /app/ /home/openclaw/ 2>/dev/null | head -5"` — must show the paused-log line in the deployed module.
4. Prove the switch is live from INSIDE the container (ro bind mount): `docker exec agentpulse-processor python3 -c "import json; print(json.load(open('/home/openclaw/.openclaw/config/agentpulse-config.json'))['x_scraping'])"` — must print `{'enabled': False}`. This is a plain json.load one-liner, NOT an import of the 10k-line module (module-level side effects: config loads, Supabase client init — do not import it in an exec).
5. Optional confirmation (do not block on it): within the next scheduled scrape or hourly health cycle, `docker logs agentpulse-processor 2>&1 | grep 'X-SCRAPE'` shows the paused INFO line, and the operator stops receiving hourly x_budget fail pings.
  </action>
  <verify>
    <automated>docker exec agentpulse-processor sh -c "grep -rln 'X-SCRAPE' /app/ /home/openclaw/ 2>/dev/null | head -1" && docker exec agentpulse-processor python3 -c "import json; assert json.load(open('/home/openclaw/.openclaw/config/agentpulse-config.json'))['x_scraping']['enabled'] is False; print('paused: OK')"</automated>
  </verify>
  <done>Processor container rebuilt and running clean; deployed module inside the container contains the X-SCRAPE gate; in-container config read confirms x_scraping.enabled=false. Alert silence + paused-log line confirmed opportunistically on the next hourly cycle.</done>
</task>

</tasks>

<verification>
- `python3 -c "import ast; ast.parse(open('docker/processor/agentpulse_processor.py').read())"` passes.
- `config/agentpulse-config.json` parses and `x_scraping.enabled` is `false`.
- Running container carries the new code (grep inside container) and sees `enabled=false` via the ro mount.
- No changes to `get_full_config()`, its cache, or any non-X code path; diff limited to the helper, the `_x_api_search` guard, the health-check branch, and the config block.
</verification>

<success_criteria>
- All X API read spend stops immediately on deploy (every path through `_x_api_search` returns [] before any network call or cost log).
- Hourly health check reports `x_budget: {'status': 'ok', 'note': 'paused via config'}` while paused — no more fail alerts until the operator re-enables.
- Re-enabling is a config-only flip (`"enabled": true` or delete the block) — no rebuild, no restart, thanks to the per-call fresh read against the live ro mount.
- Fail-open proven by code shape: any exception in the helper returns True (enabled).
</success_criteria>

<output>
Create `.planning/quick/260704-tc8-pause-all-x-api-read-spend-via-config-sw/260704-tc8-SUMMARY.md` when done.
</output>
